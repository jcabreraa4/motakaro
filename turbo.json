{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["NEXT_RUNTIME", "CI"],
  "globalPassThroughEnv": ["TRIGGER_SECRET_KEY", "CONVEX_DEPLOY_KEY", "CLERK_SECRET_KEY", "TRIGGER_ACCESS_TOKEN", "TELEGRAM_BOT_USERNAME", "TELEGRAM_BOT_TOKEN"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "passThroughEnv": ["SENTRY_AUTH_TOKEN"]
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "deploy": {
      "cache": false
    }
  }
}
