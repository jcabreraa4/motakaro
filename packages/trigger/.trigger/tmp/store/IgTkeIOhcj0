import {
  COLD_VARIANT,
  ChatChunkTooLargeError,
  InputStreamOncePromise,
  InputStreamTimeoutError,
  InternalError,
  OTEL_ATTRIBUTE_PER_EVENT_COUNT_LIMIT,
  OTEL_ATTRIBUTE_PER_LINK_COUNT_LIMIT,
  OTEL_LINK_COUNT_LIMIT,
  OTEL_LOG_ATTRIBUTE_COUNT_LIMIT,
  OTEL_LOG_ATTRIBUTE_VALUE_LENGTH_LIMIT,
  OTEL_SPAN_ATTRIBUTE_COUNT_LIMIT,
  OTEL_SPAN_ATTRIBUTE_VALUE_LENGTH_LIMIT,
  OTEL_SPAN_EVENT_COUNT_LIMIT,
  SemanticInternalAttributes,
  TaskPayloadParsedError,
  TaskRunErrorCodes,
  VERSION,
  WARM_VARIANT,
  accessoryAttributes,
  applyMetadataOperations,
  assertExhaustive,
  attemptKey,
  calculateNextRetryDelay,
  clock,
  collapseOperations,
  conditionallyExportPacket,
  conditionallyImportPacket,
  controlSubtype,
  createPacketAttributes,
  ensureReadableStream,
  esm_exports as esm_exports2,
  external_exports,
  flattenAttributes,
  generateFriendlyId,
  getEnvVar,
  iconStringForSeverity,
  init_esm as init_esm3,
  inputStreams,
  isCompleteTaskWithOutput,
  isInternalError,
  lifecycleHooks,
  nanoid,
  parseError,
  parsePacket,
  promiseWithResolvers,
  realtimeStreams,
  recordSpanException,
  require_src,
  require_src2,
  require_src3,
  runMetadata,
  runTimelineMetrics,
  sanitizeError,
  sessionStreams,
  stringifyIO,
  taskContext,
  traceContext,
  tryCatch,
  waitUntil
} from "./chunk-D7SSRRJA.mjs";
import {
  DiagConsoleLogger,
  DiagLogLevel,
  SpanKind,
  TraceFlags,
  context,
  diag,
  esm_exports,
  init_esm as init_esm2,
  metrics,
  propagation,
  trace
} from "./chunk-4QYVZTYP.mjs";
import {
  AppendInput,
  AppendRecord,
  DEFAULT_USER_AGENT,
  FifoQueue,
  RangeNotSatisfiableError,
  RetryAppendSession,
  RetryReadSession,
  S2Error,
  S2_ENCRYPTION_KEY_HEADER,
  canSetUserAgentHeader,
  computeAppendRecordFormat,
  decodeFromBase64,
  decodeProtoAppendAck,
  decodeProtoReadBatch,
  encodeProtoAppendInput,
  err,
  errClose,
  fromAPIAppendAck,
  fromAPIReadBatchBytes,
  fromAPIReadBatchString,
  fromAPIStreamPosition,
  fromAPITailResponse,
  isCommandRecord,
  make,
  makeAppendPreconditionError,
  makeServerError,
  meteredBytes,
  ok,
  okClose,
  protoAppendAckToJson,
  randomToken,
  require_src as require_src4,
  resolveEncryptionKey,
  s2Error,
  supportsHttp2,
  toAPIAppendRecord,
  toAPIReadQuery,
  value,
  withRetries,
  withS2Data,
  withS2Error
} from "./chunk-IJTH3K37.mjs";
import {
  __commonJS,
  __esm,
  __export,
  __name,
  __require,
  __toCommonJS,
  __toESM,
  init_esm
} from "./chunk-Y7JS2ICJ.mjs";

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/OTLPExporterBase.js
var OTLPExporterBase;
var init_OTLPExporterBase = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/OTLPExporterBase.js"() {
    init_esm();
    OTLPExporterBase = class {
      static {
        __name(this, "OTLPExporterBase");
      }
      _delegate;
      constructor(delegate) {
        this._delegate = delegate;
      }
      /**
       * Export items.
       * @param items
       * @param resultCallback
       */
      export(items, resultCallback) {
        this._delegate.export(items, resultCallback);
      }
      forceFlush() {
        return this._delegate.forceFlush();
      }
      shutdown() {
        return this._delegate.shutdown();
      }
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/types.js
var OTLPExporterError;
var init_types = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/types.js"() {
    init_esm();
    OTLPExporterError = class extends Error {
      static {
        __name(this, "OTLPExporterError");
      }
      code;
      name = "OTLPExporterError";
      data;
      constructor(message, code, data) {
        super(message);
        this.data = data;
        this.code = code;
      }
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/shared-configuration.js
function validateTimeoutMillis(timeoutMillis) {
  if (Number.isFinite(timeoutMillis) && timeoutMillis > 0) {
    return timeoutMillis;
  }
  throw new Error(`Configuration: timeoutMillis is invalid, expected number greater than 0 (actual: '${timeoutMillis}')`);
}
function wrapStaticHeadersInFunction(headers) {
  if (headers == null) {
    return void 0;
  }
  return async () => headers;
}
function mergeOtlpSharedConfigurationWithDefaults(userProvidedConfiguration, fallbackConfiguration, defaultConfiguration) {
  return {
    timeoutMillis: validateTimeoutMillis(userProvidedConfiguration.timeoutMillis ?? fallbackConfiguration.timeoutMillis ?? defaultConfiguration.timeoutMillis),
    concurrencyLimit: userProvidedConfiguration.concurrencyLimit ?? fallbackConfiguration.concurrencyLimit ?? defaultConfiguration.concurrencyLimit,
    compression: userProvidedConfiguration.compression ?? fallbackConfiguration.compression ?? defaultConfiguration.compression
  };
}
function getSharedConfigurationDefaults() {
  return {
    timeoutMillis: 1e4,
    concurrencyLimit: 30,
    compression: "none"
  };
}
var init_shared_configuration = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/shared-configuration.js"() {
    init_esm();
    __name(validateTimeoutMillis, "validateTimeoutMillis");
    __name(wrapStaticHeadersInFunction, "wrapStaticHeadersInFunction");
    __name(mergeOtlpSharedConfigurationWithDefaults, "mergeOtlpSharedConfigurationWithDefaults");
    __name(getSharedConfigurationDefaults, "getSharedConfigurationDefaults");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/legacy-node-configuration.js
var CompressionAlgorithm;
var init_legacy_node_configuration = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/legacy-node-configuration.js"() {
    init_esm();
    (function(CompressionAlgorithm2) {
      CompressionAlgorithm2["NONE"] = "none";
      CompressionAlgorithm2["GZIP"] = "gzip";
    })(CompressionAlgorithm || (CompressionAlgorithm = {}));
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/bounded-queue-export-promise-handler.js
function createBoundedQueueExportPromiseHandler(options) {
  return new BoundedQueueExportPromiseHandler(options.concurrencyLimit);
}
var BoundedQueueExportPromiseHandler;
var init_bounded_queue_export_promise_handler = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/bounded-queue-export-promise-handler.js"() {
    init_esm();
    BoundedQueueExportPromiseHandler = class {
      static {
        __name(this, "BoundedQueueExportPromiseHandler");
      }
      _concurrencyLimit;
      _sendingPromises = [];
      /**
       * @param concurrencyLimit maximum promises allowed in a queue at the same time.
       */
      constructor(concurrencyLimit) {
        this._concurrencyLimit = concurrencyLimit;
      }
      pushPromise(promise) {
        if (this.hasReachedLimit()) {
          throw new Error("Concurrency Limit reached");
        }
        this._sendingPromises.push(promise);
        const popPromise = /* @__PURE__ */ __name(() => {
          const index = this._sendingPromises.indexOf(promise);
          void this._sendingPromises.splice(index, 1);
        }, "popPromise");
        promise.then(popPromise, popPromise);
      }
      hasReachedLimit() {
        return this._sendingPromises.length >= this._concurrencyLimit;
      }
      async awaitAll() {
        await Promise.all(this._sendingPromises);
      }
    };
    __name(createBoundedQueueExportPromiseHandler, "createBoundedQueueExportPromiseHandler");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/logging-response-handler.js
function isPartialSuccessResponse(response) {
  return Object.prototype.hasOwnProperty.call(response, "partialSuccess");
}
function createLoggingPartialSuccessResponseHandler() {
  return {
    handleResponse(response) {
      if (response == null || !isPartialSuccessResponse(response) || response.partialSuccess == null || Object.keys(response.partialSuccess).length === 0) {
        return;
      }
      diag.warn("Received Partial Success response:", JSON.stringify(response.partialSuccess));
    }
  };
}
var init_logging_response_handler = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/logging-response-handler.js"() {
    init_esm();
    init_esm2();
    __name(isPartialSuccessResponse, "isPartialSuccessResponse");
    __name(createLoggingPartialSuccessResponseHandler, "createLoggingPartialSuccessResponseHandler");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/otlp-export-delegate.js
function createOtlpExportDelegate(components, settings) {
  return new OTLPExportDelegate(components.transport, components.serializer, createLoggingPartialSuccessResponseHandler(), components.promiseHandler, settings.timeout);
}
var import_core, OTLPExportDelegate;
var init_otlp_export_delegate = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/otlp-export-delegate.js"() {
    init_esm();
    import_core = __toESM(require_src());
    init_types();
    init_logging_response_handler();
    init_esm2();
    OTLPExportDelegate = class {
      static {
        __name(this, "OTLPExportDelegate");
      }
      _diagLogger;
      _transport;
      _serializer;
      _responseHandler;
      _promiseQueue;
      _timeout;
      constructor(transport, serializer, responseHandler, promiseQueue, timeout) {
        this._transport = transport;
        this._serializer = serializer;
        this._responseHandler = responseHandler;
        this._promiseQueue = promiseQueue;
        this._timeout = timeout;
        this._diagLogger = diag.createComponentLogger({
          namespace: "OTLPExportDelegate"
        });
      }
      export(internalRepresentation, resultCallback) {
        this._diagLogger.debug("items to be sent", internalRepresentation);
        if (this._promiseQueue.hasReachedLimit()) {
          resultCallback({
            code: import_core.ExportResultCode.FAILED,
            error: new Error("Concurrent export limit reached")
          });
          return;
        }
        const serializedRequest = this._serializer.serializeRequest(internalRepresentation);
        if (serializedRequest == null) {
          resultCallback({
            code: import_core.ExportResultCode.FAILED,
            error: new Error("Nothing to send")
          });
          return;
        }
        this._promiseQueue.pushPromise(this._transport.send(serializedRequest, this._timeout).then((response) => {
          if (response.status === "success") {
            if (response.data != null) {
              try {
                this._responseHandler.handleResponse(this._serializer.deserializeResponse(response.data));
              } catch (e) {
                this._diagLogger.warn("Export succeeded but could not deserialize response - is the response specification compliant?", e, response.data);
              }
            }
            resultCallback({
              code: import_core.ExportResultCode.SUCCESS
            });
            return;
          } else if (response.status === "failure" && response.error) {
            resultCallback({
              code: import_core.ExportResultCode.FAILED,
              error: response.error
            });
            return;
          } else if (response.status === "retryable") {
            resultCallback({
              code: import_core.ExportResultCode.FAILED,
              error: response.error ?? new OTLPExporterError("Export failed with retryable status")
            });
          } else {
            resultCallback({
              code: import_core.ExportResultCode.FAILED,
              error: new OTLPExporterError("Export failed with unknown error")
            });
          }
        }, (reason) => resultCallback({
          code: import_core.ExportResultCode.FAILED,
          error: reason
        })));
      }
      forceFlush() {
        return this._promiseQueue.awaitAll();
      }
      async shutdown() {
        this._diagLogger.debug("shutdown started");
        await this.forceFlush();
        this._transport.shutdown();
      }
    };
    __name(createOtlpExportDelegate, "createOtlpExportDelegate");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/otlp-network-export-delegate.js
function createOtlpNetworkExportDelegate(options, serializer, transport) {
  return createOtlpExportDelegate({
    transport,
    serializer,
    promiseHandler: createBoundedQueueExportPromiseHandler(options)
  }, { timeout: options.timeoutMillis });
}
var init_otlp_network_export_delegate = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/otlp-network-export-delegate.js"() {
    init_esm();
    init_bounded_queue_export_promise_handler();
    init_otlp_export_delegate();
    __name(createOtlpNetworkExportDelegate, "createOtlpNetworkExportDelegate");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/index.js
var esm_exports3 = {};
__export(esm_exports3, {
  CompressionAlgorithm: () => CompressionAlgorithm,
  OTLPExporterBase: () => OTLPExporterBase,
  OTLPExporterError: () => OTLPExporterError,
  createOtlpNetworkExportDelegate: () => createOtlpNetworkExportDelegate,
  getSharedConfigurationDefaults: () => getSharedConfigurationDefaults,
  mergeOtlpSharedConfigurationWithDefaults: () => mergeOtlpSharedConfigurationWithDefaults
});
var init_esm4 = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/index.js"() {
    init_esm();
    init_OTLPExporterBase();
    init_types();
    init_shared_configuration();
    init_legacy_node_configuration();
    init_otlp_network_export_delegate();
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/utils.js
var require_utils = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/utils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.estimateVarintSize = void 0;
    function estimateVarintSize(v) {
      if (v < 0)
        return 10;
      if (v < 128)
        return 1;
      if (v < 16384)
        return 2;
      if (v < 2097152)
        return 3;
      if (v < 268435456)
        return 4;
      if (v < 34359738368)
        return 5;
      if (v < 4398046511104)
        return 6;
      if (v < 562949953421312)
        return 7;
      if (v < 72057594037927940)
        return 8;
      return 9;
    }
    __name(estimateVarintSize, "estimateVarintSize");
    exports.estimateVarintSize = estimateVarintSize;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/protobuf-writer.js
var require_protobuf_writer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/protobuf-writer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufWriter = exports.GROWING_BUFFER_DEBUG_MESSAGE = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var utils_1 = require_utils();
    exports.GROWING_BUFFER_DEBUG_MESSAGE = "ProtobufWriter: estimated size was too small, growing buffer.";
    var RESERVED_LENGTH_BYTES = 1;
    var ProtobufWriter = class {
      static {
        __name(this, "ProtobufWriter");
      }
      _buffer;
      // Avoid using TextEncoder type. While the global is there on all supported runtimes, types may differ.
      _textEncoder;
      _dataView;
      pos = 0;
      constructor(estimatedSize = 65536) {
        this._buffer = new Uint8Array(estimatedSize);
        this._textEncoder = new TextEncoder();
        this._dataView = new DataView(this._buffer.buffer, this._buffer.byteOffset);
      }
      /**
       * Ensure buffer has capacity for at least size more bytes
       */
      _ensureCapacity(size) {
        const needed = this.pos + size;
        if (needed <= this._buffer.length) {
          return;
        }
        api_1.diag.debug(exports.GROWING_BUFFER_DEBUG_MESSAGE);
        let newSize = this._buffer.length * 2;
        while (newSize < needed) {
          newSize *= 2;
        }
        const newBuffer = new Uint8Array(newSize);
        newBuffer.set(this._buffer);
        this._buffer = newBuffer;
        this._dataView = new DataView(this._buffer.buffer, this._buffer.byteOffset);
      }
      /**
       * Get the written bytes as a Uint8Array
       */
      finish() {
        return this._buffer.subarray(0, this.pos);
      }
      /**
       * Insert placeholder for length. Update later with {@link finishLengthDelimited}
       * Returns the position where to write the length.
       */
      startLengthDelimited() {
        const lengthPos = this.pos;
        this._ensureCapacity(RESERVED_LENGTH_BYTES);
        this.pos += RESERVED_LENGTH_BYTES;
        return lengthPos;
      }
      /**
       * Write length varint at placeholder position and shift content forward if needed.
       * Most messages are small (< 128 bytes), so we reserve 1 byte and only shift
       * when the length needs more bytes.
       */
      finishLengthDelimited(pos, length) {
        const v = length >>> 0;
        const varintSize = (0, utils_1.estimateVarintSize)(v);
        if (varintSize > RESERVED_LENGTH_BYTES) {
          const additionalBytes = varintSize - RESERVED_LENGTH_BYTES;
          this._ensureCapacity(additionalBytes);
          this._buffer.copyWithin(pos + varintSize, pos + RESERVED_LENGTH_BYTES, this.pos);
          this.pos += additionalBytes;
        }
        let writePos = pos;
        if (v < 128) {
          this._buffer[writePos] = v;
        } else if (v < 16384) {
          this._buffer[writePos++] = v & 127 | 128;
          this._buffer[writePos] = v >>> 7;
        } else if (v < 2097152) {
          this._buffer[writePos++] = v & 127 | 128;
          this._buffer[writePos++] = v >>> 7 & 127 | 128;
          this._buffer[writePos] = v >>> 14;
        } else if (v < 268435456) {
          this._buffer[writePos++] = v & 127 | 128;
          this._buffer[writePos++] = v >>> 7 & 127 | 128;
          this._buffer[writePos++] = v >>> 14 & 127 | 128;
          this._buffer[writePos] = v >>> 21;
        } else {
          this._buffer[writePos++] = v & 127 | 128;
          this._buffer[writePos++] = v >>> 7 & 127 | 128;
          this._buffer[writePos++] = v >>> 14 & 127 | 128;
          this._buffer[writePos++] = v >>> 21 & 127 | 128;
          this._buffer[writePos] = v >>> 28;
        }
      }
      /**
       * Write a sint32 value using zigzag encoding
       */
      writeSint32(value2) {
        this.writeVarint((value2 << 1 ^ value2 >> 31) >>> 0);
      }
      /**
       * Write a signed 64-bit fixed integer (sfixed64) from a JS number.
       * Handles negative values via two's complement.
       */
      writeSfixed64(value2) {
        let low;
        let high;
        if (value2 >= 0) {
          low = value2 >>> 0;
          high = value2 / 4294967296 >>> 0;
        } else {
          const abs = Math.abs(value2);
          low = abs >>> 0;
          high = abs / 4294967296 >>> 0;
          low = ~low >>> 0;
          high = ~high >>> 0;
          low = low + 1 >>> 0;
          if (low === 0) {
            high = high + 1 >>> 0;
          }
        }
        this.writeFixed64(low, high);
      }
      /**
       * Write a varint (variable-length integer)
       */
      writeVarint(value2) {
        this._ensureCapacity((0, utils_1.estimateVarintSize)(value2));
        if (value2 >= 0 && value2 <= 4294967295) {
          let v = value2 >>> 0;
          while (v > 127) {
            this._buffer[this.pos++] = v & 127 | 128;
            v >>>= 7;
          }
          this._buffer[this.pos++] = v;
        } else {
          let low;
          let high;
          if (value2 >= 0) {
            low = value2 >>> 0;
            high = value2 / 4294967296 >>> 0;
          } else {
            const abs = Math.abs(value2);
            low = abs >>> 0;
            high = abs / 4294967296 >>> 0;
            low = ~low >>> 0;
            high = ~high >>> 0;
            low = low + 1 >>> 0;
            if (low === 0) {
              high = high + 1 >>> 0;
            }
          }
          while (high > 0 || low > 127) {
            this._buffer[this.pos++] = low & 127 | 128;
            low = (low >>> 7 | high << 25) >>> 0;
            high >>>= 7;
          }
          this._buffer[this.pos++] = low & 127;
        }
      }
      /**
       * Write a 32-bit fixed integer (little-endian)
       */
      writeFixed32(value2) {
        this._ensureCapacity(4);
        const v = value2 >>> 0;
        this._buffer[this.pos++] = v & 255;
        this._buffer[this.pos++] = v >>> 8 & 255;
        this._buffer[this.pos++] = v >>> 16 & 255;
        this._buffer[this.pos++] = v >>> 24 & 255;
      }
      /**
       * Write a 64-bit fixed integer (little-endian)
       * @param low - Low 32 bits
       * @param high - High 32 bits
       */
      writeFixed64(low, high) {
        this._ensureCapacity(8);
        const l = low >>> 0;
        const h = high >>> 0;
        this._buffer[this.pos++] = l & 255;
        this._buffer[this.pos++] = l >>> 8 & 255;
        this._buffer[this.pos++] = l >>> 16 & 255;
        this._buffer[this.pos++] = l >>> 24 & 255;
        this._buffer[this.pos++] = h & 255;
        this._buffer[this.pos++] = h >>> 8 & 255;
        this._buffer[this.pos++] = h >>> 16 & 255;
        this._buffer[this.pos++] = h >>> 24 & 255;
      }
      /**
       * Write length-delimited data (varint length + bytes)
       */
      writeBytes(bytes) {
        this.writeVarint(bytes.length);
        this._ensureCapacity(bytes.length);
        this._buffer.set(bytes, this.pos);
        this.pos += bytes.length;
      }
      /**
       * Write a field key (field number + wire type)
       */
      writeTag(fieldNumber, wireType) {
        this.writeVarint(fieldNumber << 3 | wireType);
      }
      /**
       * Write a double (64-bit IEEE 754)
       */
      writeDouble(value2) {
        this._ensureCapacity(8);
        this._dataView.setFloat64(this.pos, value2, true);
        this.pos += 8;
      }
      /**
       * Write a string as UTF-8 bytes (length-delimited)
       */
      writeString(str) {
        let isAscii = true;
        const len = str.length;
        for (let i = 0; i < len; i++) {
          if (str.charCodeAt(i) > 127) {
            isAscii = false;
            break;
          }
        }
        if (isAscii) {
          this.writeVarint(len);
          this._ensureCapacity(len);
          for (let i = 0; i < len; i++) {
            this._buffer[this.pos++] = str.charCodeAt(i);
          }
        } else {
          const bytes = this._textEncoder.encode(str);
          this.writeBytes(bytes);
        }
      }
    };
    exports.ProtobufWriter = ProtobufWriter;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/hex-to-binary.js
var require_hex_to_binary = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/hex-to-binary.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.hexToBinary = void 0;
    function intValue(charCode) {
      if (charCode >= 48 && charCode <= 57) {
        return charCode - 48;
      }
      if (charCode >= 97 && charCode <= 102) {
        return charCode - 87;
      }
      return charCode - 55;
    }
    __name(intValue, "intValue");
    function hexToBinary(hexStr) {
      const buf = new Uint8Array(hexStr.length / 2);
      let offset = 0;
      for (let i = 0; i < hexStr.length; i += 2) {
        const hi = intValue(hexStr.charCodeAt(i));
        const lo = intValue(hexStr.charCodeAt(i + 1));
        buf[offset++] = hi << 4 | lo;
      }
      return buf;
    }
    __name(hexToBinary, "hexToBinary");
    exports.hexToBinary = hexToBinary;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/common-serializer.js
var require_common_serializer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/common-serializer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.writeResource = exports.writeInstrumentationScope = exports.writeAnyValue = exports.writeKeyValue = exports.writeAttributes = exports.writeHrTimeAsFixed64 = void 0;
    function writeHrTimeAsFixed64(serializer, hrTime) {
      const seconds = hrTime[0];
      const nanos = hrTime[1];
      const nanosPerSecond = 1e9;
      const secondsLower16Bits = seconds & 65535;
      const secondsUpperBits = seconds / 65536 >>> 0;
      const nanosFromLower16Bits = secondsLower16Bits * nanosPerSecond;
      const nanosFromUpperBits = secondsUpperBits * nanosPerSecond;
      const lower16ContributionLow32 = nanosFromLower16Bits >>> 0;
      const lower16ContributionHigh32 = Math.floor(nanosFromLower16Bits / 4294967296);
      const upperBitsContributionLow32 = (nanosFromUpperBits & 65535) * 65536 >>> 0;
      const upperBitsContributionHigh32 = nanosFromUpperBits / 65536 >>> 0;
      const low32WithCarry = lower16ContributionLow32 + upperBitsContributionLow32 + nanos;
      const totalLow = low32WithCarry >>> 0;
      const carry = Math.floor(low32WithCarry / 4294967296);
      const totalHigh = lower16ContributionHigh32 + upperBitsContributionHigh32 + carry >>> 0;
      serializer.writeFixed64(totalLow, totalHigh);
    }
    __name(writeHrTimeAsFixed64, "writeHrTimeAsFixed64");
    exports.writeHrTimeAsFixed64 = writeHrTimeAsFixed64;
    function writeAttributes(writer, attributes, fieldNumber) {
      for (const key in attributes) {
        if (!Object.prototype.hasOwnProperty.call(attributes, key)) {
          continue;
        }
        const value2 = attributes[key];
        writer.writeTag(fieldNumber, 2);
        const kvStart = writer.startLengthDelimited();
        const startPos = writer.pos;
        writeKeyValue(writer, key, value2);
        writer.finishLengthDelimited(kvStart, writer.pos - startPos);
      }
    }
    __name(writeAttributes, "writeAttributes");
    exports.writeAttributes = writeAttributes;
    function writeKeyValue(writer, key, value2) {
      writer.writeTag(1, 2);
      writer.writeString(key);
      writer.writeTag(2, 2);
      const valueStart = writer.startLengthDelimited();
      const startPos = writer.pos;
      writeAnyValue(writer, value2);
      writer.finishLengthDelimited(valueStart, writer.pos - startPos);
    }
    __name(writeKeyValue, "writeKeyValue");
    exports.writeKeyValue = writeKeyValue;
    var MIN_64_BIT_INT = -(2 ** 63);
    var MAX_64_BIT_INT = 2 ** 63;
    function writeAnyValue(writer, value2) {
      const t = typeof value2;
      if (t === "string") {
        writer.writeTag(1, 2);
        writer.writeString(value2);
      } else if (t === "boolean") {
        writer.writeTag(2, 0);
        writer.writeVarint(value2 ? 1 : 0);
      } else if (t === "number") {
        const numValue = value2;
        if (Number.isInteger(numValue) && numValue >= MIN_64_BIT_INT && numValue < MAX_64_BIT_INT) {
          writer.writeTag(3, 0);
          writer.writeVarint(numValue);
        } else {
          writer.writeTag(4, 1);
          writer.writeDouble(numValue);
        }
      } else if (value2 instanceof Uint8Array) {
        writer.writeTag(7, 2);
        writer.writeBytes(value2);
      } else if (Array.isArray(value2)) {
        writer.writeTag(5, 2);
        const arrayStart = writer.startLengthDelimited();
        const arrayStartPos = writer.pos;
        for (const item of value2) {
          writer.writeTag(1, 2);
          const itemStart = writer.startLengthDelimited();
          const itemStartPos = writer.pos;
          writeAnyValue(writer, item);
          writer.finishLengthDelimited(itemStart, writer.pos - itemStartPos);
        }
        writer.finishLengthDelimited(arrayStart, writer.pos - arrayStartPos);
      } else if (t === "object" && value2 != null) {
        writer.writeTag(6, 2);
        const kvlistStart = writer.startLengthDelimited();
        const kvlistStartPos = writer.pos;
        const obj = value2;
        for (const k in obj) {
          if (!Object.prototype.hasOwnProperty.call(obj, k)) {
            continue;
          }
          const v = obj[k];
          writer.writeTag(1, 2);
          const kvStart = writer.startLengthDelimited();
          const kvStartPos = writer.pos;
          writer.writeTag(1, 2);
          writer.writeString(k);
          writer.writeTag(2, 2);
          const valueStart = writer.startLengthDelimited();
          const valueStartPos = writer.pos;
          writeAnyValue(writer, v);
          writer.finishLengthDelimited(valueStart, writer.pos - valueStartPos);
          writer.finishLengthDelimited(kvStart, writer.pos - kvStartPos);
        }
        writer.finishLengthDelimited(kvlistStart, writer.pos - kvlistStartPos);
      }
    }
    __name(writeAnyValue, "writeAnyValue");
    exports.writeAnyValue = writeAnyValue;
    function writeInstrumentationScope(writer, scope, fieldNumber) {
      writer.writeTag(fieldNumber, 2);
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      writer.writeTag(1, 2);
      writer.writeString(scope.name);
      if (scope.version) {
        writer.writeTag(2, 2);
        writer.writeString(scope.version);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(writeInstrumentationScope, "writeInstrumentationScope");
    exports.writeInstrumentationScope = writeInstrumentationScope;
    function writeResource(writer, resource, fieldNumber) {
      writer.writeTag(fieldNumber, 2);
      const resourceStart = writer.startLengthDelimited();
      const resourceStartPos = writer.pos;
      if (resource.attributes) {
        writeAttributes(writer, resource.attributes, 1);
      }
      writer.writeTag(2, 0);
      writer.writeVarint(0);
      writer.finishLengthDelimited(resourceStart, writer.pos - resourceStartPos);
    }
    __name(writeResource, "writeResource");
    exports.writeResource = writeResource;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/protobuf-size-estimator.js
var require_protobuf_size_estimator = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/protobuf-size-estimator.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufSizeEstimator = void 0;
    var utils_1 = require_utils();
    function utf8ByteLength(str) {
      const len = str.length;
      let byteLen = 0;
      for (let i = 0; i < len; i++) {
        const code = str.charCodeAt(i);
        if (code < 128) {
          byteLen += 1;
        } else if (code < 2048) {
          byteLen += 2;
        } else if (code < 55296 || code >= 57344) {
          byteLen += 3;
        } else {
          i++;
          byteLen += 4;
        }
      }
      return byteLen;
    }
    __name(utf8ByteLength, "utf8ByteLength");
    var ProtobufSizeEstimator = class {
      static {
        __name(this, "ProtobufSizeEstimator");
      }
      pos = 0;
      startLengthDelimited() {
        return this.pos;
      }
      finishLengthDelimited(_, length) {
        this.pos += (0, utils_1.estimateVarintSize)(length);
      }
      writeVarint(value2) {
        this.pos += (0, utils_1.estimateVarintSize)(value2);
      }
      writeSint32(value2) {
        this.pos += (0, utils_1.estimateVarintSize)((value2 << 1 ^ value2 >> 31) >>> 0);
      }
      writeSfixed64(_value) {
        this.pos += 8;
      }
      writeFixed32(_value) {
        this.pos += 4;
      }
      writeFixed64(_low, _high) {
        this.pos += 8;
      }
      writeBytes(bytes) {
        this.pos += (0, utils_1.estimateVarintSize)(bytes.length);
        this.pos += bytes.length;
      }
      writeTag(fieldNumber, wireType) {
        this.writeVarint(fieldNumber << 3 | wireType);
      }
      writeDouble(_value) {
        this.pos += 8;
      }
      writeString(str) {
        const byteLen = utf8ByteLength(str);
        this.pos += (0, utils_1.estimateVarintSize)(byteLen);
        this.pos += byteLen;
      }
    };
    exports.ProtobufSizeEstimator = ProtobufSizeEstimator;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/logs-serializer.js
var require_logs_serializer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/logs-serializer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serializeLogsExportRequest = void 0;
    var protobuf_writer_1 = require_protobuf_writer();
    var hex_to_binary_1 = require_hex_to_binary();
    var api_logs_1 = require_src3();
    var common_serializer_1 = require_common_serializer();
    var protobuf_size_estimator_1 = require_protobuf_size_estimator();
    function serializeLogRecord(writer, logRecord) {
      const logStart = writer.startLengthDelimited();
      const logStartPos = writer.pos;
      writer.writeTag(1, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, logRecord.hrTime);
      if (logRecord.severityNumber !== void 0 && logRecord.severityNumber !== api_logs_1.SeverityNumber.UNSPECIFIED) {
        writer.writeTag(2, 0);
        writer.writeVarint(logRecord.severityNumber);
      }
      if (logRecord.severityText) {
        writer.writeTag(3, 2);
        writer.writeString(logRecord.severityText);
      }
      if (logRecord.body !== void 0) {
        writer.writeTag(5, 2);
        const bodyStart = writer.startLengthDelimited();
        const bodyStartPos = writer.pos;
        (0, common_serializer_1.writeAnyValue)(writer, logRecord.body);
        writer.finishLengthDelimited(bodyStart, writer.pos - bodyStartPos);
      }
      if (logRecord.attributes) {
        (0, common_serializer_1.writeAttributes)(writer, logRecord.attributes, 6);
      }
      writer.writeTag(7, 0);
      writer.writeVarint(logRecord.droppedAttributesCount);
      if (logRecord.spanContext?.traceFlags) {
        writer.writeTag(8, 5);
        writer.writeFixed32(logRecord.spanContext.traceFlags);
      }
      if (logRecord.spanContext?.traceId) {
        writer.writeTag(9, 2);
        writer.writeBytes((0, hex_to_binary_1.hexToBinary)(logRecord.spanContext.traceId));
      }
      if (logRecord.spanContext?.spanId) {
        writer.writeTag(10, 2);
        writer.writeBytes((0, hex_to_binary_1.hexToBinary)(logRecord.spanContext.spanId));
      }
      writer.writeTag(11, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, logRecord.hrTimeObserved);
      if (logRecord.eventName) {
        writer.writeTag(12, 2);
        writer.writeString(logRecord.eventName);
      }
      writer.finishLengthDelimited(logStart, writer.pos - logStartPos);
    }
    __name(serializeLogRecord, "serializeLogRecord");
    function serializeScopeLogs(writer, scope, logRecords) {
      const scopeLogsStart = writer.startLengthDelimited();
      const scopeLogsStartPos = writer.pos;
      (0, common_serializer_1.writeInstrumentationScope)(writer, scope, 1);
      for (const logRecord of logRecords) {
        writer.writeTag(2, 2);
        serializeLogRecord(writer, logRecord);
      }
      if (scope.schemaUrl) {
        writer.writeTag(3, 2);
        writer.writeString(scope.schemaUrl);
      }
      writer.finishLengthDelimited(scopeLogsStart, writer.pos - scopeLogsStartPos);
    }
    __name(serializeScopeLogs, "serializeScopeLogs");
    function serializeResourceLogs(writer, resource, scopeMap) {
      const resourceLogsStart = writer.startLengthDelimited();
      const resourceLogsStartPos = writer.pos;
      (0, common_serializer_1.writeResource)(writer, resource, 1);
      for (const scopeLogs of scopeMap.values()) {
        writer.writeTag(2, 2);
        const scope = scopeLogs[0].instrumentationScope;
        serializeScopeLogs(writer, scope, scopeLogs);
      }
      if (resource.schemaUrl) {
        writer.writeTag(3, 2);
        writer.writeString(resource.schemaUrl);
      }
      writer.finishLengthDelimited(resourceLogsStart, writer.pos - resourceLogsStartPos);
    }
    __name(serializeResourceLogs, "serializeResourceLogs");
    function createResourceMap(logRecords) {
      const resourceMap = /* @__PURE__ */ new Map();
      for (const record of logRecords) {
        const resource = record.resource;
        const scope = record.instrumentationScope;
        let ismMap = resourceMap.get(resource);
        if (!ismMap) {
          ismMap = /* @__PURE__ */ new Map();
          resourceMap.set(resource, ismMap);
        }
        let records = ismMap.get(scope);
        if (!records) {
          records = [];
          ismMap.set(scope, records);
        }
        records.push(record);
      }
      return resourceMap;
    }
    __name(createResourceMap, "createResourceMap");
    function serializeLogsExportRequest(logRecords) {
      const resourceMap = createResourceMap(logRecords);
      const estimator = new protobuf_size_estimator_1.ProtobufSizeEstimator();
      for (const [resource, scopeMap] of resourceMap) {
        estimator.writeTag(1, 2);
        serializeResourceLogs(estimator, resource, scopeMap);
      }
      const writer = new protobuf_writer_1.ProtobufWriter(estimator.pos);
      for (const [resource, scopeMap] of resourceMap) {
        writer.writeTag(1, 2);
        serializeResourceLogs(writer, resource, scopeMap);
      }
      return writer.finish();
    }
    __name(serializeLogsExportRequest, "serializeLogsExportRequest");
    exports.serializeLogsExportRequest = serializeLogsExportRequest;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/protobuf-reader.js
var require_protobuf_reader = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/protobuf/protobuf-reader.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufReader = void 0;
    var ProtobufReader = class {
      static {
        __name(this, "ProtobufReader");
      }
      pos = 0;
      _buf;
      _textDecoder;
      constructor(buf) {
        this._buf = buf;
        this._textDecoder = new TextDecoder();
      }
      isAtEnd() {
        return this.pos >= this._buf.length;
      }
      /** Read a varint and decode it as a tag, returning field number and wire type. */
      readTag() {
        const raw = this.readVarint();
        return { fieldNumber: raw >>> 3, wireType: raw & 7 };
      }
      /**
       * Read a base-128 varint.
       * Returns a JS `number`; precision above 2^53 is silently lost.
       * Throws if the buffer is truncated mid-varint.
       */
      readVarint() {
        let result = 0;
        let shift = 0;
        let terminated = false;
        while (this.pos < this._buf.length) {
          const b = this._buf[this.pos++];
          result += (b & 127) * Math.pow(2, shift);
          shift += 7;
          if ((b & 128) === 0) {
            terminated = true;
            break;
          }
        }
        if (!terminated) {
          throw new Error("Truncated buffer: unexpected end of data while reading varint");
        }
        return result;
      }
      /** Read a length-delimited byte sequence (bytes field or embedded message). */
      readBytes() {
        const len = this.readVarint();
        if (this.pos + len > this._buf.length) {
          throw new Error(`Truncated buffer: expected ${len} bytes at position ${this.pos}, but only ${this._buf.length - this.pos} available`);
        }
        const slice = this._buf.subarray(this.pos, this.pos + len);
        this.pos += len;
        return slice;
      }
      /** Read a length-delimited UTF-8 string. */
      readString() {
        return this._textDecoder.decode(this.readBytes());
      }
      /**
       * Skip an unknown field.
       * Handles wire types 0 (varint), 1 (64-bit), 2 (length-delimited),
       * 3 (start-group), 4 (end-group), and 5 (32-bit).
       */
      skip(wireType) {
        switch (wireType) {
          case 0:
            this.readVarint();
            break;
          case 1:
            this.pos += 8;
            break;
          case 2:
            this.readBytes();
            break;
          case 3:
            while (!this.isAtEnd()) {
              const { wireType: nestedWireType } = this.readTag();
              if (nestedWireType === 4) {
                break;
              }
              this.skip(nestedWireType);
            }
            break;
          case 4:
            break;
          case 5:
            this.pos += 4;
            break;
          default:
            throw new Error(`Unknown wire type ${wireType}, cannot safely skip`);
        }
      }
    };
    exports.ProtobufReader = ProtobufReader;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/response-deserializer.js
var require_response_deserializer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/response-deserializer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.deserializeExportLogsServiceResponse = void 0;
    var protobuf_reader_1 = require_protobuf_reader();
    function deserializePartialSuccess(data) {
      const reader = new protobuf_reader_1.ProtobufReader(data);
      const result = {};
      while (!reader.isAtEnd()) {
        const { fieldNumber, wireType } = reader.readTag();
        switch (fieldNumber) {
          case 1:
            if (wireType === 0) {
              result.rejectedLogRecords = reader.readVarint();
            } else {
              reader.skip(wireType);
            }
            break;
          case 2:
            if (wireType === 2) {
              result.errorMessage = reader.readString();
            } else {
              reader.skip(wireType);
            }
            break;
          default:
            reader.skip(wireType);
            break;
        }
      }
      return result;
    }
    __name(deserializePartialSuccess, "deserializePartialSuccess");
    function deserializeExportLogsServiceResponse(data) {
      const reader = new protobuf_reader_1.ProtobufReader(data);
      const result = {};
      while (!reader.isAtEnd()) {
        const { fieldNumber, wireType } = reader.readTag();
        switch (fieldNumber) {
          case 1:
            if (wireType === 2) {
              result.partialSuccess = deserializePartialSuccess(reader.readBytes());
            } else {
              reader.skip(wireType);
            }
            break;
          default:
            reader.skip(wireType);
            break;
        }
      }
      return result;
    }
    __name(deserializeExportLogsServiceResponse, "deserializeExportLogsServiceResponse");
    exports.deserializeExportLogsServiceResponse = deserializeExportLogsServiceResponse;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/logs.js
var require_logs = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/logs.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufLogsSerializer = void 0;
    var logs_serializer_1 = require_logs_serializer();
    var response_deserializer_1 = require_response_deserializer();
    exports.ProtobufLogsSerializer = {
      serializeRequest: /* @__PURE__ */ __name((arg) => {
        return (0, logs_serializer_1.serializeLogsExportRequest)(arg);
      }, "serializeRequest"),
      deserializeResponse: /* @__PURE__ */ __name((arg) => {
        return (0, response_deserializer_1.deserializeExportLogsServiceResponse)(arg);
      }, "deserializeResponse")
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/index.js
var require_protobuf = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/protobuf/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufLogsSerializer = void 0;
    var logs_1 = require_logs();
    Object.defineProperty(exports, "ProtobufLogsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return logs_1.ProtobufLogsSerializer;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/AggregationTemporality.js
var require_AggregationTemporality = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/AggregationTemporality.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AggregationTemporality = void 0;
    var AggregationTemporality;
    (function(AggregationTemporality2) {
      AggregationTemporality2[AggregationTemporality2["DELTA"] = 0] = "DELTA";
      AggregationTemporality2[AggregationTemporality2["CUMULATIVE"] = 1] = "CUMULATIVE";
    })(AggregationTemporality = exports.AggregationTemporality || (exports.AggregationTemporality = {}));
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/MetricData.js
var require_MetricData = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/MetricData.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DataPointType = exports.InstrumentType = void 0;
    var InstrumentType;
    (function(InstrumentType2) {
      InstrumentType2["COUNTER"] = "COUNTER";
      InstrumentType2["GAUGE"] = "GAUGE";
      InstrumentType2["HISTOGRAM"] = "HISTOGRAM";
      InstrumentType2["UP_DOWN_COUNTER"] = "UP_DOWN_COUNTER";
      InstrumentType2["OBSERVABLE_COUNTER"] = "OBSERVABLE_COUNTER";
      InstrumentType2["OBSERVABLE_GAUGE"] = "OBSERVABLE_GAUGE";
      InstrumentType2["OBSERVABLE_UP_DOWN_COUNTER"] = "OBSERVABLE_UP_DOWN_COUNTER";
    })(InstrumentType = exports.InstrumentType || (exports.InstrumentType = {}));
    var DataPointType;
    (function(DataPointType2) {
      DataPointType2[DataPointType2["HISTOGRAM"] = 0] = "HISTOGRAM";
      DataPointType2[DataPointType2["EXPONENTIAL_HISTOGRAM"] = 1] = "EXPONENTIAL_HISTOGRAM";
      DataPointType2[DataPointType2["GAUGE"] = 2] = "GAUGE";
      DataPointType2[DataPointType2["SUM"] = 3] = "SUM";
    })(DataPointType = exports.DataPointType || (exports.DataPointType = {}));
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/utils.js
var require_utils2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/utils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.equalsCaseInsensitive = exports.binarySearchUB = exports.setEquals = exports.callWithTimeout = exports.TimeoutError = exports.instrumentationScopeId = exports.hashAttributes = void 0;
    function hashAttributes(attributes) {
      let keys = Object.keys(attributes);
      if (keys.length === 0)
        return "";
      keys = keys.sort();
      return JSON.stringify(keys.map((key) => [key, attributes[key]]));
    }
    __name(hashAttributes, "hashAttributes");
    exports.hashAttributes = hashAttributes;
    function instrumentationScopeId(instrumentationScope) {
      return `${instrumentationScope.name}:${instrumentationScope.version ?? ""}:${instrumentationScope.schemaUrl ?? ""}`;
    }
    __name(instrumentationScopeId, "instrumentationScopeId");
    exports.instrumentationScopeId = instrumentationScopeId;
    var TimeoutError = class _TimeoutError extends Error {
      static {
        __name(this, "TimeoutError");
      }
      constructor(message) {
        super(message);
        Object.setPrototypeOf(this, _TimeoutError.prototype);
      }
    };
    exports.TimeoutError = TimeoutError;
    function callWithTimeout(promise, timeout) {
      let timeoutHandle;
      const timeoutPromise = new Promise(/* @__PURE__ */ __name(function timeoutFunction(_resolve, reject) {
        timeoutHandle = setTimeout(/* @__PURE__ */ __name(function timeoutHandler() {
          reject(new TimeoutError("Operation timed out."));
        }, "timeoutHandler"), timeout);
      }, "timeoutFunction"));
      return Promise.race([promise, timeoutPromise]).then((result) => {
        clearTimeout(timeoutHandle);
        return result;
      }, (reason) => {
        clearTimeout(timeoutHandle);
        throw reason;
      });
    }
    __name(callWithTimeout, "callWithTimeout");
    exports.callWithTimeout = callWithTimeout;
    function setEquals(lhs, rhs) {
      if (lhs.size !== rhs.size) {
        return false;
      }
      for (const item of lhs) {
        if (!rhs.has(item)) {
          return false;
        }
      }
      return true;
    }
    __name(setEquals, "setEquals");
    exports.setEquals = setEquals;
    function binarySearchUB(arr, value2) {
      let lo = 0;
      let hi = arr.length - 1;
      let ret = arr.length;
      while (hi >= lo) {
        const mid = lo + Math.trunc((hi - lo) / 2);
        if (arr[mid] < value2) {
          lo = mid + 1;
        } else {
          ret = mid;
          hi = mid - 1;
        }
      }
      return ret;
    }
    __name(binarySearchUB, "binarySearchUB");
    exports.binarySearchUB = binarySearchUB;
    function equalsCaseInsensitive(lhs, rhs) {
      return lhs.toLowerCase() === rhs.toLowerCase();
    }
    __name(equalsCaseInsensitive, "equalsCaseInsensitive");
    exports.equalsCaseInsensitive = equalsCaseInsensitive;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/types.js
var require_types = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/types.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AggregatorKind = void 0;
    var AggregatorKind;
    (function(AggregatorKind2) {
      AggregatorKind2[AggregatorKind2["DROP"] = 0] = "DROP";
      AggregatorKind2[AggregatorKind2["SUM"] = 1] = "SUM";
      AggregatorKind2[AggregatorKind2["LAST_VALUE"] = 2] = "LAST_VALUE";
      AggregatorKind2[AggregatorKind2["HISTOGRAM"] = 3] = "HISTOGRAM";
      AggregatorKind2[AggregatorKind2["EXPONENTIAL_HISTOGRAM"] = 4] = "EXPONENTIAL_HISTOGRAM";
    })(AggregatorKind = exports.AggregatorKind || (exports.AggregatorKind = {}));
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/Drop.js
var require_Drop = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/Drop.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DropAggregator = void 0;
    var types_1 = require_types();
    var DropAggregator = class {
      static {
        __name(this, "DropAggregator");
      }
      kind = types_1.AggregatorKind.DROP;
      createAccumulation() {
        return void 0;
      }
      merge(_previous, _delta) {
        return void 0;
      }
      diff(_previous, _current) {
        return void 0;
      }
      toMetricData(_descriptor, _aggregationTemporality, _accumulationByAttributes, _endTime) {
        return void 0;
      }
    };
    exports.DropAggregator = DropAggregator;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/Histogram.js
var require_Histogram = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/Histogram.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HistogramAggregator = exports.HistogramAccumulation = void 0;
    var types_1 = require_types();
    var MetricData_1 = require_MetricData();
    var utils_1 = require_utils2();
    function createNewEmptyCheckpoint(boundaries) {
      const counts = boundaries.map(() => 0);
      counts.push(0);
      return {
        buckets: {
          boundaries,
          counts
        },
        sum: 0,
        count: 0,
        hasMinMax: false,
        min: Infinity,
        max: -Infinity
      };
    }
    __name(createNewEmptyCheckpoint, "createNewEmptyCheckpoint");
    var HistogramAccumulation = class {
      static {
        __name(this, "HistogramAccumulation");
      }
      startTime;
      _boundaries;
      _recordMinMax;
      _current;
      constructor(startTime, boundaries, recordMinMax = true, current = createNewEmptyCheckpoint(boundaries)) {
        this.startTime = startTime;
        this._boundaries = boundaries;
        this._recordMinMax = recordMinMax;
        this._current = current;
      }
      record(value2) {
        if (Number.isNaN(value2)) {
          return;
        }
        this._current.count += 1;
        this._current.sum += value2;
        if (this._recordMinMax) {
          this._current.min = Math.min(value2, this._current.min);
          this._current.max = Math.max(value2, this._current.max);
          this._current.hasMinMax = true;
        }
        const idx = (0, utils_1.binarySearchUB)(this._boundaries, value2);
        this._current.buckets.counts[idx] += 1;
      }
      setStartTime(startTime) {
        this.startTime = startTime;
      }
      toPointValue() {
        return this._current;
      }
    };
    exports.HistogramAccumulation = HistogramAccumulation;
    var HistogramAggregator = class {
      static {
        __name(this, "HistogramAggregator");
      }
      kind = types_1.AggregatorKind.HISTOGRAM;
      _boundaries;
      _recordMinMax;
      /**
       * @param _boundaries sorted upper bounds of recorded values.
       * @param _recordMinMax If set to true, min and max will be recorded. Otherwise, min and max will not be recorded.
       */
      constructor(boundaries, recordMinMax) {
        this._boundaries = boundaries;
        this._recordMinMax = recordMinMax;
      }
      createAccumulation(startTime) {
        return new HistogramAccumulation(startTime, this._boundaries, this._recordMinMax);
      }
      /**
       * Return the result of the merge of two histogram accumulations. As long as one Aggregator
       * instance produces all Accumulations with constant boundaries we don't need to worry about
       * merging accumulations with different boundaries.
       */
      merge(previous, delta) {
        const previousValue = previous.toPointValue();
        const deltaValue = delta.toPointValue();
        const previousCounts = previousValue.buckets.counts;
        const deltaCounts = deltaValue.buckets.counts;
        const mergedCounts = new Array(previousCounts.length);
        for (let idx = 0; idx < previousCounts.length; idx++) {
          mergedCounts[idx] = previousCounts[idx] + deltaCounts[idx];
        }
        let min = Infinity;
        let max = -Infinity;
        if (this._recordMinMax) {
          if (previousValue.hasMinMax && deltaValue.hasMinMax) {
            min = Math.min(previousValue.min, deltaValue.min);
            max = Math.max(previousValue.max, deltaValue.max);
          } else if (previousValue.hasMinMax) {
            min = previousValue.min;
            max = previousValue.max;
          } else if (deltaValue.hasMinMax) {
            min = deltaValue.min;
            max = deltaValue.max;
          }
        }
        return new HistogramAccumulation(previous.startTime, previousValue.buckets.boundaries, this._recordMinMax, {
          buckets: {
            boundaries: previousValue.buckets.boundaries,
            counts: mergedCounts
          },
          count: previousValue.count + deltaValue.count,
          sum: previousValue.sum + deltaValue.sum,
          hasMinMax: this._recordMinMax && (previousValue.hasMinMax || deltaValue.hasMinMax),
          min,
          max
        });
      }
      /**
       * Returns a new DELTA aggregation by comparing two cumulative measurements.
       */
      diff(previous, current) {
        const previousValue = previous.toPointValue();
        const currentValue = current.toPointValue();
        const previousCounts = previousValue.buckets.counts;
        const currentCounts = currentValue.buckets.counts;
        const diffedCounts = new Array(previousCounts.length);
        for (let idx = 0; idx < previousCounts.length; idx++) {
          diffedCounts[idx] = currentCounts[idx] - previousCounts[idx];
        }
        return new HistogramAccumulation(current.startTime, previousValue.buckets.boundaries, this._recordMinMax, {
          buckets: {
            boundaries: previousValue.buckets.boundaries,
            counts: diffedCounts
          },
          count: currentValue.count - previousValue.count,
          sum: currentValue.sum - previousValue.sum,
          hasMinMax: false,
          min: Infinity,
          max: -Infinity
        });
      }
      toMetricData(descriptor, aggregationTemporality, accumulationByAttributes, endTime) {
        return {
          descriptor,
          aggregationTemporality,
          dataPointType: MetricData_1.DataPointType.HISTOGRAM,
          dataPoints: accumulationByAttributes.map(([attributes, accumulation]) => {
            const pointValue = accumulation.toPointValue();
            const allowsNegativeValues = descriptor.type === MetricData_1.InstrumentType.GAUGE || descriptor.type === MetricData_1.InstrumentType.UP_DOWN_COUNTER || descriptor.type === MetricData_1.InstrumentType.OBSERVABLE_GAUGE || descriptor.type === MetricData_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER;
            return {
              attributes,
              startTime: accumulation.startTime,
              endTime,
              value: {
                min: pointValue.hasMinMax ? pointValue.min : void 0,
                max: pointValue.hasMinMax ? pointValue.max : void 0,
                sum: !allowsNegativeValues ? pointValue.sum : void 0,
                buckets: pointValue.buckets,
                count: pointValue.count
              }
            };
          })
        };
      }
    };
    exports.HistogramAggregator = HistogramAggregator;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/Buckets.js
var require_Buckets = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/Buckets.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Buckets = void 0;
    var Buckets = class _Buckets {
      static {
        __name(this, "Buckets");
      }
      backing;
      indexBase;
      indexStart;
      indexEnd;
      /**
       * The term index refers to the number of the exponential histogram bucket
       * used to determine its boundaries. The lower boundary of a bucket is
       * determined by base ** index and the upper boundary of a bucket is
       * determined by base ** (index + 1). index values are signed to account
       * for values less than or equal to 1.
       *
       * indexBase is the index of the 0th position in the
       * backing array, i.e., backing[0] is the count
       * in the bucket with index `indexBase`.
       *
       * indexStart is the smallest index value represented
       * in the backing array.
       *
       * indexEnd is the largest index value represented in
       * the backing array.
       */
      constructor(backing = new BucketsBacking(), indexBase = 0, indexStart = 0, indexEnd = 0) {
        this.backing = backing;
        this.indexBase = indexBase;
        this.indexStart = indexStart;
        this.indexEnd = indexEnd;
      }
      /**
       * Offset is the bucket index of the smallest entry in the counts array
       * @returns {number}
       */
      get offset() {
        return this.indexStart;
      }
      /**
       * Buckets is a view into the backing array.
       * @returns {number}
       */
      get length() {
        if (this.backing.length === 0) {
          return 0;
        }
        if (this.indexEnd === this.indexStart && this.at(0) === 0) {
          return 0;
        }
        return this.indexEnd - this.indexStart + 1;
      }
      /**
       * An array of counts, where count[i] carries the count
       * of the bucket at index (offset+i).  count[i] is the count of
       * values greater than base^(offset+i) and less than or equal to
       * base^(offset+i+1).
       * @returns {number} The logical counts based on the backing array
       */
      counts() {
        return Array.from({ length: this.length }, (_, i) => this.at(i));
      }
      /**
       * At returns the count of the bucket at a position in the logical
       * array of counts.
       * @param position
       * @returns {number}
       */
      at(position) {
        const bias = this.indexBase - this.indexStart;
        if (position < bias) {
          position += this.backing.length;
        }
        position -= bias;
        return this.backing.countAt(position);
      }
      /**
       * incrementBucket increments the backing array index by `increment`
       * @param bucketIndex
       * @param increment
       */
      incrementBucket(bucketIndex, increment) {
        this.backing.increment(bucketIndex, increment);
      }
      /**
       * decrementBucket decrements the backing array index by `decrement`
       * if decrement is greater than the current value, it's set to 0.
       * @param bucketIndex
       * @param decrement
       */
      decrementBucket(bucketIndex, decrement) {
        this.backing.decrement(bucketIndex, decrement);
      }
      /**
       * trim removes leading and / or trailing zero buckets (which can occur
       * after diffing two histos) and rotates the backing array so that the
       * smallest non-zero index is in the 0th position of the backing array
       */
      trim() {
        for (let i = 0; i < this.length; i++) {
          if (this.at(i) !== 0) {
            this.indexStart += i;
            break;
          } else if (i === this.length - 1) {
            this.indexStart = this.indexEnd = this.indexBase = 0;
            return;
          }
        }
        for (let i = this.length - 1; i >= 0; i--) {
          if (this.at(i) !== 0) {
            this.indexEnd -= this.length - i - 1;
            break;
          }
        }
        this._rotate();
      }
      /**
       * downscale first rotates, then collapses 2**`by`-to-1 buckets.
       * @param by
       */
      downscale(by) {
        this._rotate();
        const size = 1 + this.indexEnd - this.indexStart;
        const each = 1 << by;
        let inpos = 0;
        let outpos = 0;
        for (let pos = this.indexStart; pos <= this.indexEnd; ) {
          let mod = pos % each;
          if (mod < 0) {
            mod += each;
          }
          for (let i = mod; i < each && inpos < size; i++) {
            this._relocateBucket(outpos, inpos);
            inpos++;
            pos++;
          }
          outpos++;
        }
        this.indexStart >>= by;
        this.indexEnd >>= by;
        this.indexBase = this.indexStart;
      }
      /**
       * Clone returns a deep copy of Buckets
       * @returns {Buckets}
       */
      clone() {
        return new _Buckets(this.backing.clone(), this.indexBase, this.indexStart, this.indexEnd);
      }
      /**
       * _rotate shifts the backing array contents so that indexStart ==
       * indexBase to simplify the downscale logic.
       */
      _rotate() {
        const bias = this.indexBase - this.indexStart;
        if (bias === 0) {
          return;
        } else if (bias > 0) {
          this.backing.reverse(0, this.backing.length);
          this.backing.reverse(0, bias);
          this.backing.reverse(bias, this.backing.length);
        } else {
          this.backing.reverse(0, this.backing.length);
          this.backing.reverse(0, this.backing.length + bias);
        }
        this.indexBase = this.indexStart;
      }
      /**
       * _relocateBucket adds the count in counts[src] to counts[dest] and
       * resets count[src] to zero.
       */
      _relocateBucket(dest, src) {
        if (dest === src) {
          return;
        }
        this.incrementBucket(dest, this.backing.emptyBucket(src));
      }
    };
    exports.Buckets = Buckets;
    var BucketsBacking = class _BucketsBacking {
      static {
        __name(this, "BucketsBacking");
      }
      _counts;
      constructor(counts = [0]) {
        this._counts = counts;
      }
      /**
       * length returns the physical size of the backing array, which
       * is >= buckets.length()
       */
      get length() {
        return this._counts.length;
      }
      /**
       * countAt returns the count in a specific bucket
       */
      countAt(pos) {
        return this._counts[pos];
      }
      /**
       * growTo grows a backing array and copies old entries
       * into their correct new positions.
       */
      growTo(newSize, oldPositiveLimit, newPositiveLimit) {
        const tmp = new Array(newSize).fill(0);
        tmp.splice(newPositiveLimit, this._counts.length - oldPositiveLimit, ...this._counts.slice(oldPositiveLimit));
        tmp.splice(0, oldPositiveLimit, ...this._counts.slice(0, oldPositiveLimit));
        this._counts = tmp;
      }
      /**
       * reverse the items in the backing array in the range [from, limit).
       */
      reverse(from, limit) {
        const num = Math.floor((from + limit) / 2) - from;
        for (let i = 0; i < num; i++) {
          const tmp = this._counts[from + i];
          this._counts[from + i] = this._counts[limit - i - 1];
          this._counts[limit - i - 1] = tmp;
        }
      }
      /**
       * emptyBucket empties the count from a bucket, for
       * moving into another.
       */
      emptyBucket(src) {
        const tmp = this._counts[src];
        this._counts[src] = 0;
        return tmp;
      }
      /**
       * increments a bucket by `increment`
       */
      increment(bucketIndex, increment) {
        this._counts[bucketIndex] += increment;
      }
      /**
       * decrements a bucket by `decrement`
       */
      decrement(bucketIndex, decrement) {
        if (this._counts[bucketIndex] >= decrement) {
          this._counts[bucketIndex] -= decrement;
        } else {
          this._counts[bucketIndex] = 0;
        }
      }
      /**
       * clone returns a deep copy of BucketsBacking
       */
      clone() {
        return new _BucketsBacking([...this._counts]);
      }
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/ieee754.js
var require_ieee754 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/ieee754.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getSignificand = exports.getNormalBase2 = exports.MIN_VALUE = exports.MAX_NORMAL_EXPONENT = exports.MIN_NORMAL_EXPONENT = exports.SIGNIFICAND_WIDTH = void 0;
    exports.SIGNIFICAND_WIDTH = 52;
    var EXPONENT_MASK = 2146435072;
    var SIGNIFICAND_MASK = 1048575;
    var EXPONENT_BIAS = 1023;
    exports.MIN_NORMAL_EXPONENT = -EXPONENT_BIAS + 1;
    exports.MAX_NORMAL_EXPONENT = EXPONENT_BIAS;
    exports.MIN_VALUE = Math.pow(2, -1022);
    function getNormalBase2(value2) {
      const dv = new DataView(new ArrayBuffer(8));
      dv.setFloat64(0, value2);
      const hiBits = dv.getUint32(0);
      const expBits = (hiBits & EXPONENT_MASK) >> 20;
      return expBits - EXPONENT_BIAS;
    }
    __name(getNormalBase2, "getNormalBase2");
    exports.getNormalBase2 = getNormalBase2;
    function getSignificand(value2) {
      const dv = new DataView(new ArrayBuffer(8));
      dv.setFloat64(0, value2);
      const hiBits = dv.getUint32(0);
      const loBits = dv.getUint32(4);
      const significandHiBits = (hiBits & SIGNIFICAND_MASK) * Math.pow(2, 32);
      return significandHiBits + loBits;
    }
    __name(getSignificand, "getSignificand");
    exports.getSignificand = getSignificand;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/util.js
var require_util = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/util.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.nextGreaterSquare = exports.ldexp = void 0;
    function ldexp(frac, exp) {
      if (frac === 0 || frac === Number.POSITIVE_INFINITY || frac === Number.NEGATIVE_INFINITY || Number.isNaN(frac)) {
        return frac;
      }
      return frac * Math.pow(2, exp);
    }
    __name(ldexp, "ldexp");
    exports.ldexp = ldexp;
    function nextGreaterSquare(v) {
      v--;
      v |= v >> 1;
      v |= v >> 2;
      v |= v >> 4;
      v |= v >> 8;
      v |= v >> 16;
      v++;
      return v;
    }
    __name(nextGreaterSquare, "nextGreaterSquare");
    exports.nextGreaterSquare = nextGreaterSquare;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/types.js
var require_types2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/types.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MappingError = void 0;
    var MappingError = class extends Error {
      static {
        __name(this, "MappingError");
      }
    };
    exports.MappingError = MappingError;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/ExponentMapping.js
var require_ExponentMapping = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/ExponentMapping.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ExponentMapping = void 0;
    var ieee754 = require_ieee754();
    var util2 = require_util();
    var types_1 = require_types2();
    var ExponentMapping = class {
      static {
        __name(this, "ExponentMapping");
      }
      _shift;
      constructor(scale) {
        this._shift = -scale;
      }
      /**
       * Maps positive floating point values to indexes corresponding to scale
       * @param value
       * @returns {number} index for provided value at the current scale
       */
      mapToIndex(value2) {
        if (value2 < ieee754.MIN_VALUE) {
          return this._minNormalLowerBoundaryIndex();
        }
        const exp = ieee754.getNormalBase2(value2);
        const correction = this._rightShift(ieee754.getSignificand(value2) - 1, ieee754.SIGNIFICAND_WIDTH);
        return exp + correction >> this._shift;
      }
      /**
       * Returns the lower bucket boundary for the given index for scale
       *
       * @param index
       * @returns {number}
       */
      lowerBoundary(index) {
        const minIndex = this._minNormalLowerBoundaryIndex();
        if (index < minIndex) {
          throw new types_1.MappingError(`underflow: ${index} is < minimum lower boundary: ${minIndex}`);
        }
        const maxIndex = this._maxNormalLowerBoundaryIndex();
        if (index > maxIndex) {
          throw new types_1.MappingError(`overflow: ${index} is > maximum lower boundary: ${maxIndex}`);
        }
        return util2.ldexp(1, index << this._shift);
      }
      /**
       * The scale used by this mapping
       * @returns {number}
       */
      get scale() {
        if (this._shift === 0) {
          return 0;
        }
        return -this._shift;
      }
      _minNormalLowerBoundaryIndex() {
        let index = ieee754.MIN_NORMAL_EXPONENT >> this._shift;
        if (this._shift < 2) {
          index--;
        }
        return index;
      }
      _maxNormalLowerBoundaryIndex() {
        return ieee754.MAX_NORMAL_EXPONENT >> this._shift;
      }
      _rightShift(value2, shift) {
        return Math.floor(value2 * Math.pow(2, -shift));
      }
    };
    exports.ExponentMapping = ExponentMapping;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/LogarithmMapping.js
var require_LogarithmMapping = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/LogarithmMapping.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LogarithmMapping = void 0;
    var ieee754 = require_ieee754();
    var util2 = require_util();
    var types_1 = require_types2();
    var LogarithmMapping = class {
      static {
        __name(this, "LogarithmMapping");
      }
      _scale;
      _scaleFactor;
      _inverseFactor;
      constructor(scale) {
        this._scale = scale;
        this._scaleFactor = util2.ldexp(Math.LOG2E, scale);
        this._inverseFactor = util2.ldexp(Math.LN2, -scale);
      }
      /**
       * Maps positive floating point values to indexes corresponding to scale
       * @param value
       * @returns {number} index for provided value at the current scale
       */
      mapToIndex(value2) {
        if (value2 <= ieee754.MIN_VALUE) {
          return this._minNormalLowerBoundaryIndex() - 1;
        }
        if (ieee754.getSignificand(value2) === 0) {
          const exp = ieee754.getNormalBase2(value2);
          return (exp << this._scale) - 1;
        }
        const index = Math.floor(Math.log(value2) * this._scaleFactor);
        const maxIndex = this._maxNormalLowerBoundaryIndex();
        if (index >= maxIndex) {
          return maxIndex;
        }
        return index;
      }
      /**
       * Returns the lower bucket boundary for the given index for scale
       *
       * @param index
       * @returns {number}
       */
      lowerBoundary(index) {
        const maxIndex = this._maxNormalLowerBoundaryIndex();
        if (index >= maxIndex) {
          if (index === maxIndex) {
            return 2 * Math.exp((index - (1 << this._scale)) / this._scaleFactor);
          }
          throw new types_1.MappingError(`overflow: ${index} is > maximum lower boundary: ${maxIndex}`);
        }
        const minIndex = this._minNormalLowerBoundaryIndex();
        if (index <= minIndex) {
          if (index === minIndex) {
            return ieee754.MIN_VALUE;
          } else if (index === minIndex - 1) {
            return Math.exp((index + (1 << this._scale)) / this._scaleFactor) / 2;
          }
          throw new types_1.MappingError(`overflow: ${index} is < minimum lower boundary: ${minIndex}`);
        }
        return Math.exp(index * this._inverseFactor);
      }
      /**
       * The scale used by this mapping
       * @returns {number}
       */
      get scale() {
        return this._scale;
      }
      _minNormalLowerBoundaryIndex() {
        return ieee754.MIN_NORMAL_EXPONENT << this._scale;
      }
      _maxNormalLowerBoundaryIndex() {
        return (ieee754.MAX_NORMAL_EXPONENT + 1 << this._scale) - 1;
      }
    };
    exports.LogarithmMapping = LogarithmMapping;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/getMapping.js
var require_getMapping = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/exponential-histogram/mapping/getMapping.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getMapping = void 0;
    var ExponentMapping_1 = require_ExponentMapping();
    var LogarithmMapping_1 = require_LogarithmMapping();
    var types_1 = require_types2();
    var MIN_SCALE = -10;
    var MAX_SCALE = 20;
    var PREBUILT_MAPPINGS = Array.from({ length: 31 }, (_, i) => {
      if (i > 10) {
        return new LogarithmMapping_1.LogarithmMapping(i - 10);
      }
      return new ExponentMapping_1.ExponentMapping(i - 10);
    });
    function getMapping(scale) {
      if (scale > MAX_SCALE || scale < MIN_SCALE) {
        throw new types_1.MappingError(`expected scale >= ${MIN_SCALE} && <= ${MAX_SCALE}, got: ${scale}`);
      }
      return PREBUILT_MAPPINGS[scale + 10];
    }
    __name(getMapping, "getMapping");
    exports.getMapping = getMapping;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/ExponentialHistogram.js
var require_ExponentialHistogram = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/ExponentialHistogram.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ExponentialHistogramAggregator = exports.ExponentialHistogramAccumulation = void 0;
    var types_1 = require_types();
    var MetricData_1 = require_MetricData();
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var Buckets_1 = require_Buckets();
    var getMapping_1 = require_getMapping();
    var util_1 = require_util();
    var HighLow = class _HighLow {
      static {
        __name(this, "HighLow");
      }
      static combine(h1, h2) {
        return new _HighLow(Math.min(h1.low, h2.low), Math.max(h1.high, h2.high));
      }
      low;
      high;
      constructor(low, high) {
        this.low = low;
        this.high = high;
      }
    };
    var MAX_SCALE = 20;
    var DEFAULT_MAX_SIZE = 160;
    var MIN_MAX_SIZE = 2;
    var ExponentialHistogramAccumulation = class _ExponentialHistogramAccumulation {
      static {
        __name(this, "ExponentialHistogramAccumulation");
      }
      startTime;
      _maxSize;
      _recordMinMax;
      _sum;
      _count;
      _zeroCount;
      _min;
      _max;
      _positive;
      _negative;
      _mapping;
      constructor(startTime, maxSize = DEFAULT_MAX_SIZE, recordMinMax = true, sum = 0, count = 0, zeroCount = 0, min = Number.POSITIVE_INFINITY, max = Number.NEGATIVE_INFINITY, positive = new Buckets_1.Buckets(), negative = new Buckets_1.Buckets(), mapping = (0, getMapping_1.getMapping)(MAX_SCALE)) {
        this.startTime = startTime;
        this._maxSize = maxSize;
        this._recordMinMax = recordMinMax;
        this._sum = sum;
        this._count = count;
        this._zeroCount = zeroCount;
        this._min = min;
        this._max = max;
        this._positive = positive;
        this._negative = negative;
        this._mapping = mapping;
        if (this._maxSize < MIN_MAX_SIZE) {
          api_1.diag.warn(`Exponential Histogram Max Size set to ${this._maxSize},                 changing to the minimum size of: ${MIN_MAX_SIZE}`);
          this._maxSize = MIN_MAX_SIZE;
        }
      }
      /**
       * record updates a histogram with a single count
       * @param {Number} value
       */
      record(value2) {
        this.updateByIncrement(value2, 1);
      }
      /**
       * Sets the start time for this accumulation
       * @param {HrTime} startTime
       */
      setStartTime(startTime) {
        this.startTime = startTime;
      }
      /**
       * Returns the datapoint representation of this accumulation
       * @param {HrTime} startTime
       */
      toPointValue() {
        return {
          hasMinMax: this._recordMinMax,
          min: this.min,
          max: this.max,
          sum: this.sum,
          positive: {
            offset: this.positive.offset,
            bucketCounts: this.positive.counts()
          },
          negative: {
            offset: this.negative.offset,
            bucketCounts: this.negative.counts()
          },
          count: this.count,
          scale: this.scale,
          zeroCount: this.zeroCount
        };
      }
      /**
       * @returns {Number} The sum of values recorded by this accumulation
       */
      get sum() {
        return this._sum;
      }
      /**
       * @returns {Number} The minimum value recorded by this accumulation
       */
      get min() {
        return this._min;
      }
      /**
       * @returns {Number} The maximum value recorded by this accumulation
       */
      get max() {
        return this._max;
      }
      /**
       * @returns {Number} The count of values recorded by this accumulation
       */
      get count() {
        return this._count;
      }
      /**
       * @returns {Number} The number of 0 values recorded by this accumulation
       */
      get zeroCount() {
        return this._zeroCount;
      }
      /**
       * @returns {Number} The scale used by this accumulation
       */
      get scale() {
        if (this._count === this._zeroCount) {
          return 0;
        }
        return this._mapping.scale;
      }
      /**
       * positive holds the positive values
       * @returns {Buckets}
       */
      get positive() {
        return this._positive;
      }
      /**
       * negative holds the negative values by their absolute value
       * @returns {Buckets}
       */
      get negative() {
        return this._negative;
      }
      /**
       * updateByIncr supports updating a histogram with a non-negative
       * increment.
       * @param value
       * @param increment
       */
      updateByIncrement(value2, increment) {
        if (Number.isNaN(value2)) {
          return;
        }
        if (value2 > this._max) {
          this._max = value2;
        }
        if (value2 < this._min) {
          this._min = value2;
        }
        this._count += increment;
        if (value2 === 0) {
          this._zeroCount += increment;
          return;
        }
        this._sum += value2 * increment;
        if (value2 > 0) {
          this._updateBuckets(this._positive, value2, increment);
        } else {
          this._updateBuckets(this._negative, -value2, increment);
        }
      }
      /**
       * merge combines data from previous value into self
       * @param {ExponentialHistogramAccumulation} previous
       */
      merge(previous) {
        if (this._count === 0) {
          this._min = previous.min;
          this._max = previous.max;
        } else if (previous.count !== 0) {
          if (previous.min < this.min) {
            this._min = previous.min;
          }
          if (previous.max > this.max) {
            this._max = previous.max;
          }
        }
        this.startTime = previous.startTime;
        this._sum += previous.sum;
        this._count += previous.count;
        this._zeroCount += previous.zeroCount;
        const minScale = this._minScale(previous);
        this._downscale(this.scale - minScale);
        this._mergeBuckets(this.positive, previous, previous.positive, minScale);
        this._mergeBuckets(this.negative, previous, previous.negative, minScale);
      }
      /**
       * diff subtracts other from self
       * @param {ExponentialHistogramAccumulation} other
       */
      diff(other) {
        this._min = Infinity;
        this._max = -Infinity;
        this._sum -= other.sum;
        this._count -= other.count;
        this._zeroCount -= other.zeroCount;
        const minScale = this._minScale(other);
        this._downscale(this.scale - minScale);
        this._diffBuckets(this.positive, other, other.positive, minScale);
        this._diffBuckets(this.negative, other, other.negative, minScale);
      }
      /**
       * clone returns a deep copy of self
       * @returns {ExponentialHistogramAccumulation}
       */
      clone() {
        return new _ExponentialHistogramAccumulation(this.startTime, this._maxSize, this._recordMinMax, this._sum, this._count, this._zeroCount, this._min, this._max, this.positive.clone(), this.negative.clone(), this._mapping);
      }
      /**
       * _updateBuckets maps the incoming value to a bucket index for the current
       * scale. If the bucket index is outside of the range of the backing array,
       * it will rescale the backing array and update the mapping for the new scale.
       */
      _updateBuckets(buckets, value2, increment) {
        let index = this._mapping.mapToIndex(value2);
        let rescalingNeeded = false;
        let high = 0;
        let low = 0;
        if (buckets.length === 0) {
          buckets.indexStart = index;
          buckets.indexEnd = buckets.indexStart;
          buckets.indexBase = buckets.indexStart;
        } else if (index < buckets.indexStart && buckets.indexEnd - index >= this._maxSize) {
          rescalingNeeded = true;
          low = index;
          high = buckets.indexEnd;
        } else if (index > buckets.indexEnd && index - buckets.indexStart >= this._maxSize) {
          rescalingNeeded = true;
          low = buckets.indexStart;
          high = index;
        }
        if (rescalingNeeded) {
          const change = this._changeScale(high, low);
          this._downscale(change);
          index = this._mapping.mapToIndex(value2);
        }
        this._incrementIndexBy(buckets, index, increment);
      }
      /**
       * _incrementIndexBy increments the count of the bucket specified by `index`.
       * If the index is outside of the range [buckets.indexStart, buckets.indexEnd]
       * the boundaries of the backing array will be adjusted and more buckets will
       * be added if needed.
       */
      _incrementIndexBy(buckets, index, increment) {
        if (increment === 0) {
          return;
        }
        if (buckets.length === 0) {
          buckets.indexStart = buckets.indexEnd = buckets.indexBase = index;
        }
        if (index < buckets.indexStart) {
          const span = buckets.indexEnd - index;
          if (span >= buckets.backing.length) {
            this._grow(buckets, span + 1);
          }
          buckets.indexStart = index;
        } else if (index > buckets.indexEnd) {
          const span = index - buckets.indexStart;
          if (span >= buckets.backing.length) {
            this._grow(buckets, span + 1);
          }
          buckets.indexEnd = index;
        }
        let bucketIndex = index - buckets.indexBase;
        if (bucketIndex < 0) {
          bucketIndex += buckets.backing.length;
        }
        buckets.incrementBucket(bucketIndex, increment);
      }
      /**
       * grow resizes the backing array by doubling in size up to maxSize.
       * This extends the array with a bunch of zeros and copies the
       * existing counts to the same position.
       */
      _grow(buckets, needed) {
        const size = buckets.backing.length;
        const bias = buckets.indexBase - buckets.indexStart;
        const oldPositiveLimit = size - bias;
        let newSize = (0, util_1.nextGreaterSquare)(needed);
        if (newSize > this._maxSize) {
          newSize = this._maxSize;
        }
        const newPositiveLimit = newSize - bias;
        buckets.backing.growTo(newSize, oldPositiveLimit, newPositiveLimit);
      }
      /**
       * _changeScale computes how much downscaling is needed by shifting the
       * high and low values until they are separated by no more than size.
       */
      _changeScale(high, low) {
        let change = 0;
        while (high - low >= this._maxSize) {
          high >>= 1;
          low >>= 1;
          change++;
        }
        return change;
      }
      /**
       * _downscale subtracts `change` from the current mapping scale.
       */
      _downscale(change) {
        if (change === 0) {
          return;
        }
        if (change < 0) {
          throw new Error(`impossible change of scale: ${this.scale}`);
        }
        const newScale = this._mapping.scale - change;
        this._positive.downscale(change);
        this._negative.downscale(change);
        this._mapping = (0, getMapping_1.getMapping)(newScale);
      }
      /**
       * _minScale is used by diff and merge to compute an ideal combined scale
       */
      _minScale(other) {
        const minScale = Math.min(this.scale, other.scale);
        const highLowPos = HighLow.combine(this._highLowAtScale(this.positive, this.scale, minScale), this._highLowAtScale(other.positive, other.scale, minScale));
        const highLowNeg = HighLow.combine(this._highLowAtScale(this.negative, this.scale, minScale), this._highLowAtScale(other.negative, other.scale, minScale));
        return Math.min(minScale - this._changeScale(highLowPos.high, highLowPos.low), minScale - this._changeScale(highLowNeg.high, highLowNeg.low));
      }
      /**
       * _highLowAtScale is used by diff and merge to compute an ideal combined scale.
       */
      _highLowAtScale(buckets, currentScale, newScale) {
        if (buckets.length === 0) {
          return new HighLow(0, -1);
        }
        const shift = currentScale - newScale;
        return new HighLow(buckets.indexStart >> shift, buckets.indexEnd >> shift);
      }
      /**
       * _mergeBuckets translates index values from another histogram and
       * adds the values into the corresponding buckets of this histogram.
       */
      _mergeBuckets(ours, other, theirs, scale) {
        const theirOffset = theirs.offset;
        const theirChange = other.scale - scale;
        for (let i = 0; i < theirs.length; i++) {
          this._incrementIndexBy(ours, theirOffset + i >> theirChange, theirs.at(i));
        }
      }
      /**
       * _diffBuckets translates index values from another histogram and
       * subtracts the values in the corresponding buckets of this histogram.
       */
      _diffBuckets(ours, other, theirs, scale) {
        const theirOffset = theirs.offset;
        const theirChange = other.scale - scale;
        for (let i = 0; i < theirs.length; i++) {
          const ourIndex = theirOffset + i >> theirChange;
          let bucketIndex = ourIndex - ours.indexBase;
          if (bucketIndex < 0) {
            bucketIndex += ours.backing.length;
          }
          ours.decrementBucket(bucketIndex, theirs.at(i));
        }
        ours.trim();
      }
    };
    exports.ExponentialHistogramAccumulation = ExponentialHistogramAccumulation;
    var ExponentialHistogramAggregator = class {
      static {
        __name(this, "ExponentialHistogramAggregator");
      }
      kind = types_1.AggregatorKind.EXPONENTIAL_HISTOGRAM;
      _maxSize;
      _recordMinMax;
      /**
       * @param _maxSize Maximum number of buckets for each of the positive
       *    and negative ranges, exclusive of the zero-bucket.
       * @param _recordMinMax If set to true, min and max will be recorded.
       *    Otherwise, min and max will not be recorded.
       */
      constructor(maxSize, recordMinMax) {
        this._maxSize = maxSize;
        this._recordMinMax = recordMinMax;
      }
      createAccumulation(startTime) {
        return new ExponentialHistogramAccumulation(startTime, this._maxSize, this._recordMinMax);
      }
      /**
       * Return the result of the merge of two exponential histogram accumulations.
       */
      merge(previous, delta) {
        const result = delta.clone();
        result.merge(previous);
        return result;
      }
      /**
       * Returns a new DELTA aggregation by comparing two cumulative measurements.
       */
      diff(previous, current) {
        const result = current.clone();
        result.diff(previous);
        return result;
      }
      toMetricData(descriptor, aggregationTemporality, accumulationByAttributes, endTime) {
        return {
          descriptor,
          aggregationTemporality,
          dataPointType: MetricData_1.DataPointType.EXPONENTIAL_HISTOGRAM,
          dataPoints: accumulationByAttributes.map(([attributes, accumulation]) => {
            const pointValue = accumulation.toPointValue();
            const allowsNegativeValues = descriptor.type === MetricData_1.InstrumentType.GAUGE || descriptor.type === MetricData_1.InstrumentType.UP_DOWN_COUNTER || descriptor.type === MetricData_1.InstrumentType.OBSERVABLE_GAUGE || descriptor.type === MetricData_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER;
            return {
              attributes,
              startTime: accumulation.startTime,
              endTime,
              value: {
                min: pointValue.hasMinMax ? pointValue.min : void 0,
                max: pointValue.hasMinMax ? pointValue.max : void 0,
                sum: !allowsNegativeValues ? pointValue.sum : void 0,
                positive: {
                  offset: pointValue.positive.offset,
                  bucketCounts: pointValue.positive.bucketCounts
                },
                negative: {
                  offset: pointValue.negative.offset,
                  bucketCounts: pointValue.negative.bucketCounts
                },
                count: pointValue.count,
                scale: pointValue.scale,
                zeroCount: pointValue.zeroCount
              }
            };
          })
        };
      }
    };
    exports.ExponentialHistogramAggregator = ExponentialHistogramAggregator;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/LastValue.js
var require_LastValue = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/LastValue.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LastValueAggregator = exports.LastValueAccumulation = void 0;
    var types_1 = require_types();
    var core_1 = require_src();
    var MetricData_1 = require_MetricData();
    var LastValueAccumulation = class {
      static {
        __name(this, "LastValueAccumulation");
      }
      startTime;
      _current;
      sampleTime;
      constructor(startTime, current = 0, sampleTime = [0, 0]) {
        this.startTime = startTime;
        this._current = current;
        this.sampleTime = sampleTime;
      }
      record(value2) {
        this._current = value2;
        this.sampleTime = (0, core_1.millisToHrTime)(Date.now());
      }
      setStartTime(startTime) {
        this.startTime = startTime;
      }
      toPointValue() {
        return this._current;
      }
    };
    exports.LastValueAccumulation = LastValueAccumulation;
    var LastValueAggregator = class {
      static {
        __name(this, "LastValueAggregator");
      }
      kind = types_1.AggregatorKind.LAST_VALUE;
      createAccumulation(startTime) {
        return new LastValueAccumulation(startTime);
      }
      /**
       * Returns the result of the merge of the given accumulations.
       *
       * Return the newly captured (delta) accumulation for LastValueAggregator.
       */
      merge(previous, delta) {
        const latestAccumulation = (0, core_1.hrTimeToMicroseconds)(delta.sampleTime) >= (0, core_1.hrTimeToMicroseconds)(previous.sampleTime) ? delta : previous;
        return new LastValueAccumulation(previous.startTime, latestAccumulation.toPointValue(), latestAccumulation.sampleTime);
      }
      /**
       * Returns a new DELTA aggregation by comparing two cumulative measurements.
       *
       * A delta aggregation is not meaningful to LastValueAggregator, just return
       * the newly captured (delta) accumulation for LastValueAggregator.
       */
      diff(previous, current) {
        const latestAccumulation = (0, core_1.hrTimeToMicroseconds)(current.sampleTime) >= (0, core_1.hrTimeToMicroseconds)(previous.sampleTime) ? current : previous;
        return new LastValueAccumulation(current.startTime, latestAccumulation.toPointValue(), latestAccumulation.sampleTime);
      }
      toMetricData(descriptor, aggregationTemporality, accumulationByAttributes, endTime) {
        return {
          descriptor,
          aggregationTemporality,
          dataPointType: MetricData_1.DataPointType.GAUGE,
          dataPoints: accumulationByAttributes.map(([attributes, accumulation]) => {
            return {
              attributes,
              startTime: accumulation.startTime,
              endTime,
              value: accumulation.toPointValue()
            };
          })
        };
      }
    };
    exports.LastValueAggregator = LastValueAggregator;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/Sum.js
var require_Sum = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/Sum.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SumAggregator = exports.SumAccumulation = void 0;
    var types_1 = require_types();
    var MetricData_1 = require_MetricData();
    var SumAccumulation = class {
      static {
        __name(this, "SumAccumulation");
      }
      startTime;
      monotonic;
      _current;
      reset;
      constructor(startTime, monotonic, current = 0, reset = false) {
        this.startTime = startTime;
        this.monotonic = monotonic;
        this._current = current;
        this.reset = reset;
      }
      record(value2) {
        if (this.monotonic && value2 < 0) {
          return;
        }
        this._current += value2;
      }
      setStartTime(startTime) {
        this.startTime = startTime;
      }
      toPointValue() {
        return this._current;
      }
    };
    exports.SumAccumulation = SumAccumulation;
    var SumAggregator = class {
      static {
        __name(this, "SumAggregator");
      }
      kind = types_1.AggregatorKind.SUM;
      monotonic;
      constructor(monotonic) {
        this.monotonic = monotonic;
      }
      createAccumulation(startTime) {
        return new SumAccumulation(startTime, this.monotonic);
      }
      /**
       * Returns the result of the merge of the given accumulations.
       */
      merge(previous, delta) {
        const prevPv = previous.toPointValue();
        const deltaPv = delta.toPointValue();
        if (delta.reset) {
          return new SumAccumulation(delta.startTime, this.monotonic, deltaPv, delta.reset);
        }
        return new SumAccumulation(previous.startTime, this.monotonic, prevPv + deltaPv);
      }
      /**
       * Returns a new DELTA aggregation by comparing two cumulative measurements.
       */
      diff(previous, current) {
        const prevPv = previous.toPointValue();
        const currPv = current.toPointValue();
        if (this.monotonic && prevPv > currPv) {
          return new SumAccumulation(current.startTime, this.monotonic, currPv, true);
        }
        return new SumAccumulation(current.startTime, this.monotonic, currPv - prevPv);
      }
      toMetricData(descriptor, aggregationTemporality, accumulationByAttributes, endTime) {
        return {
          descriptor,
          aggregationTemporality,
          dataPointType: MetricData_1.DataPointType.SUM,
          dataPoints: accumulationByAttributes.map(([attributes, accumulation]) => {
            return {
              attributes,
              startTime: accumulation.startTime,
              endTime,
              value: accumulation.toPointValue()
            };
          }),
          isMonotonic: this.monotonic
        };
      }
    };
    exports.SumAggregator = SumAggregator;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/index.js
var require_aggregator = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/aggregator/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SumAggregator = exports.SumAccumulation = exports.LastValueAggregator = exports.LastValueAccumulation = exports.ExponentialHistogramAggregator = exports.ExponentialHistogramAccumulation = exports.HistogramAggregator = exports.HistogramAccumulation = exports.DropAggregator = void 0;
    var Drop_1 = require_Drop();
    Object.defineProperty(exports, "DropAggregator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return Drop_1.DropAggregator;
    }, "get") });
    var Histogram_1 = require_Histogram();
    Object.defineProperty(exports, "HistogramAccumulation", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return Histogram_1.HistogramAccumulation;
    }, "get") });
    Object.defineProperty(exports, "HistogramAggregator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return Histogram_1.HistogramAggregator;
    }, "get") });
    var ExponentialHistogram_1 = require_ExponentialHistogram();
    Object.defineProperty(exports, "ExponentialHistogramAccumulation", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ExponentialHistogram_1.ExponentialHistogramAccumulation;
    }, "get") });
    Object.defineProperty(exports, "ExponentialHistogramAggregator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ExponentialHistogram_1.ExponentialHistogramAggregator;
    }, "get") });
    var LastValue_1 = require_LastValue();
    Object.defineProperty(exports, "LastValueAccumulation", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return LastValue_1.LastValueAccumulation;
    }, "get") });
    Object.defineProperty(exports, "LastValueAggregator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return LastValue_1.LastValueAggregator;
    }, "get") });
    var Sum_1 = require_Sum();
    Object.defineProperty(exports, "SumAccumulation", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return Sum_1.SumAccumulation;
    }, "get") });
    Object.defineProperty(exports, "SumAggregator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return Sum_1.SumAggregator;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/Aggregation.js
var require_Aggregation = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/Aggregation.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DEFAULT_AGGREGATION = exports.EXPONENTIAL_HISTOGRAM_AGGREGATION = exports.HISTOGRAM_AGGREGATION = exports.LAST_VALUE_AGGREGATION = exports.SUM_AGGREGATION = exports.DROP_AGGREGATION = exports.DefaultAggregation = exports.ExponentialHistogramAggregation = exports.ExplicitBucketHistogramAggregation = exports.HistogramAggregation = exports.LastValueAggregation = exports.SumAggregation = exports.DropAggregation = void 0;
    var api = (init_esm2(), __toCommonJS(esm_exports));
    var aggregator_1 = require_aggregator();
    var MetricData_1 = require_MetricData();
    var DropAggregation = class _DropAggregation {
      static {
        __name(this, "DropAggregation");
      }
      static DEFAULT_INSTANCE = new aggregator_1.DropAggregator();
      createAggregator(_instrument) {
        return _DropAggregation.DEFAULT_INSTANCE;
      }
    };
    exports.DropAggregation = DropAggregation;
    var SumAggregation = class _SumAggregation {
      static {
        __name(this, "SumAggregation");
      }
      static MONOTONIC_INSTANCE = new aggregator_1.SumAggregator(true);
      static NON_MONOTONIC_INSTANCE = new aggregator_1.SumAggregator(false);
      createAggregator(instrument) {
        switch (instrument.type) {
          case MetricData_1.InstrumentType.COUNTER:
          case MetricData_1.InstrumentType.OBSERVABLE_COUNTER:
          case MetricData_1.InstrumentType.HISTOGRAM: {
            return _SumAggregation.MONOTONIC_INSTANCE;
          }
          default: {
            return _SumAggregation.NON_MONOTONIC_INSTANCE;
          }
        }
      }
    };
    exports.SumAggregation = SumAggregation;
    var LastValueAggregation = class _LastValueAggregation {
      static {
        __name(this, "LastValueAggregation");
      }
      static DEFAULT_INSTANCE = new aggregator_1.LastValueAggregator();
      createAggregator(_instrument) {
        return _LastValueAggregation.DEFAULT_INSTANCE;
      }
    };
    exports.LastValueAggregation = LastValueAggregation;
    var HistogramAggregation = class _HistogramAggregation {
      static {
        __name(this, "HistogramAggregation");
      }
      static DEFAULT_INSTANCE = new aggregator_1.HistogramAggregator([0, 5, 10, 25, 50, 75, 100, 250, 500, 750, 1e3, 2500, 5e3, 7500, 1e4], true);
      createAggregator(_instrument) {
        return _HistogramAggregation.DEFAULT_INSTANCE;
      }
    };
    exports.HistogramAggregation = HistogramAggregation;
    var ExplicitBucketHistogramAggregation = class {
      static {
        __name(this, "ExplicitBucketHistogramAggregation");
      }
      _boundaries;
      _recordMinMax;
      /**
       * @param boundaries the bucket boundaries of the histogram aggregation
       * @param _recordMinMax If set to true, min and max will be recorded. Otherwise, min and max will not be recorded.
       */
      constructor(boundaries, recordMinMax = true) {
        if (boundaries == null) {
          throw new Error("ExplicitBucketHistogramAggregation should be created with explicit boundaries, if a single bucket histogram is required, please pass an empty array");
        }
        boundaries = boundaries.concat();
        boundaries = boundaries.sort((a, b) => a - b);
        const minusInfinityIndex = boundaries.lastIndexOf(-Infinity);
        let infinityIndex = boundaries.indexOf(Infinity);
        if (infinityIndex === -1) {
          infinityIndex = void 0;
        }
        this._boundaries = boundaries.slice(minusInfinityIndex + 1, infinityIndex);
        this._recordMinMax = recordMinMax;
      }
      createAggregator(_instrument) {
        return new aggregator_1.HistogramAggregator(this._boundaries, this._recordMinMax);
      }
    };
    exports.ExplicitBucketHistogramAggregation = ExplicitBucketHistogramAggregation;
    var ExponentialHistogramAggregation = class {
      static {
        __name(this, "ExponentialHistogramAggregation");
      }
      _maxSize;
      _recordMinMax;
      constructor(maxSize = 160, recordMinMax = true) {
        this._maxSize = maxSize;
        this._recordMinMax = recordMinMax;
      }
      createAggregator(_instrument) {
        return new aggregator_1.ExponentialHistogramAggregator(this._maxSize, this._recordMinMax);
      }
    };
    exports.ExponentialHistogramAggregation = ExponentialHistogramAggregation;
    var DefaultAggregation = class {
      static {
        __name(this, "DefaultAggregation");
      }
      _resolve(instrument) {
        switch (instrument.type) {
          case MetricData_1.InstrumentType.COUNTER:
          case MetricData_1.InstrumentType.UP_DOWN_COUNTER:
          case MetricData_1.InstrumentType.OBSERVABLE_COUNTER:
          case MetricData_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER: {
            return exports.SUM_AGGREGATION;
          }
          case MetricData_1.InstrumentType.GAUGE:
          case MetricData_1.InstrumentType.OBSERVABLE_GAUGE: {
            return exports.LAST_VALUE_AGGREGATION;
          }
          case MetricData_1.InstrumentType.HISTOGRAM: {
            if (instrument.advice.explicitBucketBoundaries) {
              return new ExplicitBucketHistogramAggregation(instrument.advice.explicitBucketBoundaries);
            }
            return exports.HISTOGRAM_AGGREGATION;
          }
        }
        api.diag.warn(`Unable to recognize instrument type: ${instrument.type}`);
        return exports.DROP_AGGREGATION;
      }
      createAggregator(instrument) {
        return this._resolve(instrument).createAggregator(instrument);
      }
    };
    exports.DefaultAggregation = DefaultAggregation;
    exports.DROP_AGGREGATION = new DropAggregation();
    exports.SUM_AGGREGATION = new SumAggregation();
    exports.LAST_VALUE_AGGREGATION = new LastValueAggregation();
    exports.HISTOGRAM_AGGREGATION = new HistogramAggregation();
    exports.EXPONENTIAL_HISTOGRAM_AGGREGATION = new ExponentialHistogramAggregation();
    exports.DEFAULT_AGGREGATION = new DefaultAggregation();
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/AggregationOption.js
var require_AggregationOption = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/AggregationOption.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.toAggregation = exports.AggregationType = void 0;
    var Aggregation_1 = require_Aggregation();
    var AggregationType2;
    (function(AggregationType3) {
      AggregationType3[AggregationType3["DEFAULT"] = 0] = "DEFAULT";
      AggregationType3[AggregationType3["DROP"] = 1] = "DROP";
      AggregationType3[AggregationType3["SUM"] = 2] = "SUM";
      AggregationType3[AggregationType3["LAST_VALUE"] = 3] = "LAST_VALUE";
      AggregationType3[AggregationType3["EXPLICIT_BUCKET_HISTOGRAM"] = 4] = "EXPLICIT_BUCKET_HISTOGRAM";
      AggregationType3[AggregationType3["EXPONENTIAL_HISTOGRAM"] = 5] = "EXPONENTIAL_HISTOGRAM";
    })(AggregationType2 = exports.AggregationType || (exports.AggregationType = {}));
    function toAggregation(option) {
      switch (option.type) {
        case AggregationType2.DEFAULT:
          return Aggregation_1.DEFAULT_AGGREGATION;
        case AggregationType2.DROP:
          return Aggregation_1.DROP_AGGREGATION;
        case AggregationType2.SUM:
          return Aggregation_1.SUM_AGGREGATION;
        case AggregationType2.LAST_VALUE:
          return Aggregation_1.LAST_VALUE_AGGREGATION;
        case AggregationType2.EXPONENTIAL_HISTOGRAM: {
          const expOption = option;
          return new Aggregation_1.ExponentialHistogramAggregation(expOption.options?.maxSize, expOption.options?.recordMinMax);
        }
        case AggregationType2.EXPLICIT_BUCKET_HISTOGRAM: {
          const expOption = option;
          if (expOption.options == null) {
            return Aggregation_1.HISTOGRAM_AGGREGATION;
          } else {
            return new Aggregation_1.ExplicitBucketHistogramAggregation(expOption.options?.boundaries, expOption.options?.recordMinMax);
          }
        }
        default:
          throw new Error("Unsupported Aggregation");
      }
    }
    __name(toAggregation, "toAggregation");
    exports.toAggregation = toAggregation;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/AggregationSelector.js
var require_AggregationSelector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/AggregationSelector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DEFAULT_AGGREGATION_TEMPORALITY_SELECTOR = exports.DEFAULT_AGGREGATION_SELECTOR = void 0;
    var AggregationTemporality_1 = require_AggregationTemporality();
    var AggregationOption_1 = require_AggregationOption();
    var DEFAULT_AGGREGATION_SELECTOR = /* @__PURE__ */ __name((_instrumentType) => {
      return {
        type: AggregationOption_1.AggregationType.DEFAULT
      };
    }, "DEFAULT_AGGREGATION_SELECTOR");
    exports.DEFAULT_AGGREGATION_SELECTOR = DEFAULT_AGGREGATION_SELECTOR;
    var DEFAULT_AGGREGATION_TEMPORALITY_SELECTOR = /* @__PURE__ */ __name((_instrumentType) => AggregationTemporality_1.AggregationTemporality.CUMULATIVE, "DEFAULT_AGGREGATION_TEMPORALITY_SELECTOR");
    exports.DEFAULT_AGGREGATION_TEMPORALITY_SELECTOR = DEFAULT_AGGREGATION_TEMPORALITY_SELECTOR;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/MetricReader.js
var require_MetricReader = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/MetricReader.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MetricReader = void 0;
    var api = (init_esm2(), __toCommonJS(esm_exports));
    var utils_1 = require_utils2();
    var AggregationSelector_1 = require_AggregationSelector();
    var MetricReader = class {
      static {
        __name(this, "MetricReader");
      }
      // Tracks the shutdown state.
      // TODO: use BindOncePromise here once a new version of @opentelemetry/core is available.
      _shutdown = false;
      // Additional MetricProducers which will be combined with the SDK's output
      _metricProducers;
      // MetricProducer used by this instance which produces metrics from the SDK
      _sdkMetricProducer;
      _aggregationTemporalitySelector;
      _aggregationSelector;
      _cardinalitySelector;
      constructor(options) {
        this._aggregationSelector = options?.aggregationSelector ?? AggregationSelector_1.DEFAULT_AGGREGATION_SELECTOR;
        this._aggregationTemporalitySelector = options?.aggregationTemporalitySelector ?? AggregationSelector_1.DEFAULT_AGGREGATION_TEMPORALITY_SELECTOR;
        this._metricProducers = options?.metricProducers ?? [];
        this._cardinalitySelector = options?.cardinalitySelector;
      }
      setMetricProducer(metricProducer) {
        if (this._sdkMetricProducer) {
          throw new Error("MetricReader can not be bound to a MeterProvider again.");
        }
        this._sdkMetricProducer = metricProducer;
        this.onInitialized();
      }
      selectAggregation(instrumentType) {
        return this._aggregationSelector(instrumentType);
      }
      selectAggregationTemporality(instrumentType) {
        return this._aggregationTemporalitySelector(instrumentType);
      }
      selectCardinalityLimit(instrumentType) {
        return this._cardinalitySelector ? this._cardinalitySelector(instrumentType) : 2e3;
      }
      /**
       * Handle once the SDK has initialized this {@link MetricReader}
       * Overriding this method is optional.
       */
      onInitialized() {
      }
      async collect(options) {
        if (this._sdkMetricProducer === void 0) {
          throw new Error("MetricReader is not bound to a MetricProducer");
        }
        if (this._shutdown) {
          throw new Error("MetricReader is shutdown");
        }
        const [sdkCollectionResults, ...additionalCollectionResults] = await Promise.all([
          this._sdkMetricProducer.collect({
            timeoutMillis: options?.timeoutMillis
          }),
          ...this._metricProducers.map((producer) => producer.collect({
            timeoutMillis: options?.timeoutMillis
          }))
        ]);
        const errors = sdkCollectionResults.errors.concat(additionalCollectionResults.flatMap((result) => result.errors));
        const resource = sdkCollectionResults.resourceMetrics.resource;
        const scopeMetrics = sdkCollectionResults.resourceMetrics.scopeMetrics.concat(additionalCollectionResults.flatMap((result) => result.resourceMetrics.scopeMetrics));
        return {
          resourceMetrics: {
            resource,
            scopeMetrics
          },
          errors
        };
      }
      async shutdown(options) {
        if (this._shutdown) {
          api.diag.error("Cannot call shutdown twice.");
          return;
        }
        if (options?.timeoutMillis == null) {
          await this.onShutdown();
        } else {
          await (0, utils_1.callWithTimeout)(this.onShutdown(), options.timeoutMillis);
        }
        this._shutdown = true;
      }
      async forceFlush(options) {
        if (this._shutdown) {
          api.diag.warn("Cannot forceFlush on already shutdown MetricReader.");
          return;
        }
        if (options?.timeoutMillis == null) {
          await this.onForceFlush();
          return;
        }
        await (0, utils_1.callWithTimeout)(this.onForceFlush(), options.timeoutMillis);
      }
    };
    exports.MetricReader = MetricReader;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/PeriodicExportingMetricReader.js
var require_PeriodicExportingMetricReader = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/PeriodicExportingMetricReader.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PeriodicExportingMetricReader = void 0;
    var api = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var MetricReader_1 = require_MetricReader();
    var utils_1 = require_utils2();
    var MetricData_1 = require_MetricData();
    var PeriodicExportingMetricReader2 = class extends MetricReader_1.MetricReader {
      static {
        __name(this, "PeriodicExportingMetricReader");
      }
      _interval;
      _exporter;
      _exportInterval;
      _exportTimeout;
      constructor(options) {
        const { exporter, exportIntervalMillis = 6e4, metricProducers, cardinalityLimits } = options;
        let { exportTimeoutMillis = 3e4 } = options;
        super({
          aggregationSelector: exporter.selectAggregation?.bind(exporter),
          aggregationTemporalitySelector: exporter.selectAggregationTemporality?.bind(exporter),
          metricProducers,
          cardinalitySelector: /* @__PURE__ */ __name((instrumentType) => {
            const limits = {
              default: 2e3,
              ...cardinalityLimits
            };
            switch (instrumentType) {
              case MetricData_1.InstrumentType.COUNTER:
                return limits.counter ?? limits.default;
              case MetricData_1.InstrumentType.GAUGE:
                return limits.gauge ?? limits.default;
              case MetricData_1.InstrumentType.HISTOGRAM:
                return limits.histogram ?? limits.default;
              case MetricData_1.InstrumentType.OBSERVABLE_COUNTER:
                return limits.observableCounter ?? limits.default;
              case MetricData_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER:
                return limits.observableUpDownCounter ?? limits.default;
              case MetricData_1.InstrumentType.OBSERVABLE_GAUGE:
                return limits.observableGauge ?? limits.default;
              case MetricData_1.InstrumentType.UP_DOWN_COUNTER:
                return limits.upDownCounter ?? limits.default;
              default:
                return limits.default;
            }
          }, "cardinalitySelector")
        });
        if (exportIntervalMillis <= 0) {
          throw Error("exportIntervalMillis must be greater than 0");
        }
        if (exportTimeoutMillis <= 0) {
          throw Error("exportTimeoutMillis must be greater than 0");
        }
        if (exportIntervalMillis < exportTimeoutMillis) {
          if ("exportIntervalMillis" in options && "exportTimeoutMillis" in options) {
            throw Error("exportIntervalMillis must be greater than or equal to exportTimeoutMillis");
          } else {
            api.diag.info(`Timeout of ${exportTimeoutMillis} exceeds the interval of ${exportIntervalMillis}. Clamping timeout to interval duration.`);
            exportTimeoutMillis = exportIntervalMillis;
          }
        }
        this._exportInterval = exportIntervalMillis;
        this._exportTimeout = exportTimeoutMillis;
        this._exporter = exporter;
      }
      async _runOnce() {
        try {
          await (0, utils_1.callWithTimeout)(this._doRun(), this._exportTimeout);
        } catch (err2) {
          if (err2 instanceof utils_1.TimeoutError) {
            api.diag.error("Export took longer than %s milliseconds and timed out.", this._exportTimeout);
            return;
          }
          (0, core_1.globalErrorHandler)(err2);
        }
      }
      async _doRun() {
        const { resourceMetrics, errors } = await this.collect({
          timeoutMillis: this._exportTimeout
        });
        if (errors.length > 0) {
          api.diag.error("PeriodicExportingMetricReader: metrics collection errors", ...errors);
        }
        if (resourceMetrics.resource.asyncAttributesPending) {
          try {
            await resourceMetrics.resource.waitForAsyncAttributes?.();
          } catch (e) {
            api.diag.debug("Error while resolving async portion of resource: ", e);
            (0, core_1.globalErrorHandler)(e);
          }
        }
        if (resourceMetrics.scopeMetrics.length === 0) {
          return;
        }
        const result = await core_1.internal._export(this._exporter, resourceMetrics);
        if (result.code !== core_1.ExportResultCode.SUCCESS) {
          throw new Error(`PeriodicExportingMetricReader: metrics export failed (error ${result.error})`);
        }
      }
      onInitialized() {
        this._interval = setInterval(() => {
          void this._runOnce();
        }, this._exportInterval);
        if (typeof this._interval !== "number") {
          this._interval.unref();
        }
      }
      async onForceFlush() {
        await this._runOnce();
        await this._exporter.forceFlush();
      }
      async onShutdown() {
        if (this._interval) {
          clearInterval(this._interval);
        }
        await this.onForceFlush();
        await this._exporter.shutdown();
      }
    };
    exports.PeriodicExportingMetricReader = PeriodicExportingMetricReader2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/InMemoryMetricExporter.js
var require_InMemoryMetricExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/InMemoryMetricExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InMemoryMetricExporter = void 0;
    var core_1 = require_src();
    var InMemoryMetricExporter = class {
      static {
        __name(this, "InMemoryMetricExporter");
      }
      _shutdown = false;
      _aggregationTemporality;
      _metrics = [];
      constructor(aggregationTemporality) {
        this._aggregationTemporality = aggregationTemporality;
      }
      /**
       * @inheritedDoc
       */
      export(metrics2, resultCallback) {
        if (this._shutdown) {
          setTimeout(() => resultCallback({ code: core_1.ExportResultCode.FAILED }), 0);
          return;
        }
        this._metrics.push(metrics2);
        setTimeout(() => resultCallback({ code: core_1.ExportResultCode.SUCCESS }), 0);
      }
      /**
       * Returns all the collected resource metrics
       * @returns ResourceMetrics[]
       */
      getMetrics() {
        return this._metrics;
      }
      forceFlush() {
        return Promise.resolve();
      }
      reset() {
        this._metrics = [];
      }
      selectAggregationTemporality(_instrumentType) {
        return this._aggregationTemporality;
      }
      shutdown() {
        this._shutdown = true;
        return Promise.resolve();
      }
    };
    exports.InMemoryMetricExporter = InMemoryMetricExporter;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/ConsoleMetricExporter.js
var require_ConsoleMetricExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/export/ConsoleMetricExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConsoleMetricExporter = void 0;
    var core_1 = require_src();
    var AggregationSelector_1 = require_AggregationSelector();
    var ConsoleMetricExporter = class _ConsoleMetricExporter {
      static {
        __name(this, "ConsoleMetricExporter");
      }
      _shutdown = false;
      _temporalitySelector;
      constructor(options) {
        this._temporalitySelector = options?.temporalitySelector ?? AggregationSelector_1.DEFAULT_AGGREGATION_TEMPORALITY_SELECTOR;
      }
      export(metrics2, resultCallback) {
        if (this._shutdown) {
          resultCallback({ code: core_1.ExportResultCode.FAILED });
          return;
        }
        return _ConsoleMetricExporter._sendMetrics(metrics2, resultCallback);
      }
      forceFlush() {
        return Promise.resolve();
      }
      selectAggregationTemporality(_instrumentType) {
        return this._temporalitySelector(_instrumentType);
      }
      shutdown() {
        this._shutdown = true;
        return Promise.resolve();
      }
      static _sendMetrics(metrics2, done) {
        for (const scopeMetrics of metrics2.scopeMetrics) {
          for (const metric of scopeMetrics.metrics) {
            console.dir({
              descriptor: metric.descriptor,
              dataPointType: metric.dataPointType,
              dataPoints: metric.dataPoints
            }, { depth: null });
          }
        }
        done({ code: core_1.ExportResultCode.SUCCESS });
      }
    };
    exports.ConsoleMetricExporter = ConsoleMetricExporter;
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/default-service-name.js
var require_default_service_name = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/default-service-name.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports._clearDefaultServiceNameCache = exports.defaultServiceName = void 0;
    var serviceName;
    function defaultServiceName() {
      if (serviceName === void 0) {
        try {
          const argv0 = globalThis.process.argv0;
          serviceName = argv0 ? `unknown_service:${argv0}` : "unknown_service";
        } catch {
          serviceName = "unknown_service";
        }
      }
      return serviceName;
    }
    __name(defaultServiceName, "defaultServiceName");
    exports.defaultServiceName = defaultServiceName;
    function _clearDefaultServiceNameCache() {
      serviceName = void 0;
    }
    __name(_clearDefaultServiceNameCache, "_clearDefaultServiceNameCache");
    exports._clearDefaultServiceNameCache = _clearDefaultServiceNameCache;
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/utils.js
var require_utils3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/utils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isPromiseLike = void 0;
    var isPromiseLike = /* @__PURE__ */ __name((val) => {
      return val !== null && typeof val === "object" && typeof val.then === "function";
    }, "isPromiseLike");
    exports.isPromiseLike = isPromiseLike;
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/ResourceImpl.js
var require_ResourceImpl = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/ResourceImpl.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.defaultResource = exports.emptyResource = exports.resourceFromDetectedResource = exports.resourceFromAttributes = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var semantic_conventions_1 = (init_esm3(), __toCommonJS(esm_exports2));
    var default_service_name_1 = require_default_service_name();
    var utils_1 = require_utils3();
    var ResourceImpl = class _ResourceImpl {
      static {
        __name(this, "ResourceImpl");
      }
      _rawAttributes;
      _asyncAttributesPending = false;
      _schemaUrl;
      _memoizedAttributes;
      static FromAttributeList(attributes, options) {
        const res = new _ResourceImpl({}, options);
        res._rawAttributes = guardedRawAttributes(attributes);
        res._asyncAttributesPending = attributes.filter(([_, val]) => (0, utils_1.isPromiseLike)(val)).length > 0;
        return res;
      }
      constructor(resource, options) {
        const attributes = resource.attributes ?? {};
        this._rawAttributes = Object.entries(attributes).map(([k, v]) => {
          if ((0, utils_1.isPromiseLike)(v)) {
            this._asyncAttributesPending = true;
          }
          return [k, v];
        });
        this._rawAttributes = guardedRawAttributes(this._rawAttributes);
        this._schemaUrl = validateSchemaUrl(options?.schemaUrl);
      }
      get asyncAttributesPending() {
        return this._asyncAttributesPending;
      }
      async waitForAsyncAttributes() {
        if (!this.asyncAttributesPending) {
          return;
        }
        for (let i = 0; i < this._rawAttributes.length; i++) {
          const [k, v] = this._rawAttributes[i];
          this._rawAttributes[i] = [k, (0, utils_1.isPromiseLike)(v) ? await v : v];
        }
        this._asyncAttributesPending = false;
      }
      get attributes() {
        if (this.asyncAttributesPending) {
          api_1.diag.error("Accessing resource attributes before async attributes settled");
        }
        if (this._memoizedAttributes) {
          return this._memoizedAttributes;
        }
        const attrs = {};
        for (const [k, v] of this._rawAttributes) {
          if ((0, utils_1.isPromiseLike)(v)) {
            api_1.diag.debug(`Unsettled resource attribute ${k} skipped`);
            continue;
          }
          if (v != null) {
            attrs[k] ??= v;
          }
        }
        if (!this._asyncAttributesPending) {
          this._memoizedAttributes = attrs;
        }
        return attrs;
      }
      getRawAttributes() {
        return this._rawAttributes;
      }
      get schemaUrl() {
        return this._schemaUrl;
      }
      merge(resource) {
        if (resource == null)
          return this;
        const mergedSchemaUrl = mergeSchemaUrl(this, resource);
        const mergedOptions = mergedSchemaUrl ? { schemaUrl: mergedSchemaUrl } : void 0;
        return _ResourceImpl.FromAttributeList([...resource.getRawAttributes(), ...this.getRawAttributes()], mergedOptions);
      }
    };
    function resourceFromAttributes2(attributes, options) {
      return ResourceImpl.FromAttributeList(Object.entries(attributes), options);
    }
    __name(resourceFromAttributes2, "resourceFromAttributes");
    exports.resourceFromAttributes = resourceFromAttributes2;
    function resourceFromDetectedResource(detectedResource, options) {
      return new ResourceImpl(detectedResource, options);
    }
    __name(resourceFromDetectedResource, "resourceFromDetectedResource");
    exports.resourceFromDetectedResource = resourceFromDetectedResource;
    function emptyResource() {
      return resourceFromAttributes2({});
    }
    __name(emptyResource, "emptyResource");
    exports.emptyResource = emptyResource;
    function defaultResource() {
      return resourceFromAttributes2({
        [semantic_conventions_1.ATTR_SERVICE_NAME]: (0, default_service_name_1.defaultServiceName)(),
        [semantic_conventions_1.ATTR_TELEMETRY_SDK_LANGUAGE]: core_1.SDK_INFO[semantic_conventions_1.ATTR_TELEMETRY_SDK_LANGUAGE],
        [semantic_conventions_1.ATTR_TELEMETRY_SDK_NAME]: core_1.SDK_INFO[semantic_conventions_1.ATTR_TELEMETRY_SDK_NAME],
        [semantic_conventions_1.ATTR_TELEMETRY_SDK_VERSION]: core_1.SDK_INFO[semantic_conventions_1.ATTR_TELEMETRY_SDK_VERSION]
      });
    }
    __name(defaultResource, "defaultResource");
    exports.defaultResource = defaultResource;
    function guardedRawAttributes(attributes) {
      return attributes.map(([k, v]) => {
        if ((0, utils_1.isPromiseLike)(v)) {
          return [
            k,
            v.catch((err2) => {
              api_1.diag.debug("promise rejection for resource attribute: %s - %s", k, err2);
              return void 0;
            })
          ];
        }
        return [k, v];
      });
    }
    __name(guardedRawAttributes, "guardedRawAttributes");
    function validateSchemaUrl(schemaUrl) {
      if (typeof schemaUrl === "string" || schemaUrl === void 0) {
        return schemaUrl;
      }
      api_1.diag.warn("Schema URL must be string or undefined, got %s. Schema URL will be ignored.", schemaUrl);
      return void 0;
    }
    __name(validateSchemaUrl, "validateSchemaUrl");
    function mergeSchemaUrl(old, updating) {
      const oldSchemaUrl = old?.schemaUrl;
      const updatingSchemaUrl = updating?.schemaUrl;
      const isOldEmpty = oldSchemaUrl === void 0 || oldSchemaUrl === "";
      const isUpdatingEmpty = updatingSchemaUrl === void 0 || updatingSchemaUrl === "";
      if (isOldEmpty) {
        return updatingSchemaUrl;
      }
      if (isUpdatingEmpty) {
        return oldSchemaUrl;
      }
      if (oldSchemaUrl === updatingSchemaUrl) {
        return oldSchemaUrl;
      }
      api_1.diag.warn('Schema URL merge conflict: old resource has "%s", updating resource has "%s". Resulting resource will have undefined Schema URL.', oldSchemaUrl, updatingSchemaUrl);
      return void 0;
    }
    __name(mergeSchemaUrl, "mergeSchemaUrl");
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detect-resources.js
var require_detect_resources = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detect-resources.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.detectResources = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var ResourceImpl_1 = require_ResourceImpl();
    var detectResources2 = /* @__PURE__ */ __name((config = {}) => {
      const resources = (config.detectors || []).map((d) => {
        try {
          const resource = (0, ResourceImpl_1.resourceFromDetectedResource)(d.detect(config));
          api_1.diag.debug(`${d.constructor.name} found resource.`, resource);
          return resource;
        } catch (e) {
          api_1.diag.debug(`${d.constructor.name} failed: ${e.message}`);
          return (0, ResourceImpl_1.emptyResource)();
        }
      });
      return resources.reduce((acc, resource) => acc.merge(resource), (0, ResourceImpl_1.emptyResource)());
    }, "detectResources");
    exports.detectResources = detectResources2;
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/EnvDetector.js
var require_EnvDetector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/EnvDetector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.envDetector = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var semantic_conventions_1 = (init_esm3(), __toCommonJS(esm_exports2));
    var core_1 = require_src();
    var EnvDetector = class {
      static {
        __name(this, "EnvDetector");
      }
      // Type, attribute keys, and attribute values should not exceed 256 characters.
      _MAX_LENGTH = 255;
      // OTEL_RESOURCE_ATTRIBUTES is a comma-separated list of attributes.
      _COMMA_SEPARATOR = ",";
      // OTEL_RESOURCE_ATTRIBUTES contains key value pair separated by '='.
      _LABEL_KEY_VALUE_SPLITTER = "=";
      /**
       * Returns a {@link Resource} populated with attributes from the
       * OTEL_RESOURCE_ATTRIBUTES environment variable. Note this is an async
       * function to conform to the Detector interface.
       *
       * @param config The resource detection config
       */
      detect(_config) {
        const attributes = {};
        const rawAttributes = (0, core_1.getStringFromEnv)("OTEL_RESOURCE_ATTRIBUTES");
        const serviceName = (0, core_1.getStringFromEnv)("OTEL_SERVICE_NAME");
        if (rawAttributes) {
          try {
            const parsedAttributes = this._parseResourceAttributes(rawAttributes);
            Object.assign(attributes, parsedAttributes);
          } catch (e) {
            api_1.diag.debug(`EnvDetector failed: ${e instanceof Error ? e.message : e}`);
          }
        }
        if (serviceName) {
          attributes[semantic_conventions_1.ATTR_SERVICE_NAME] = serviceName;
        }
        return { attributes };
      }
      /**
       * Creates an attribute map from the OTEL_RESOURCE_ATTRIBUTES environment
       * variable.
       *
       * OTEL_RESOURCE_ATTRIBUTES: A comma-separated list of attributes in the
       * format "key1=value1,key2=value2". The ',' and '=' characters in keys
       * and values MUST be percent-encoded. Other characters MAY be percent-encoded.
       *
       * Per the spec, on any error (e.g., decoding failure), the entire environment
       * variable value is discarded.
       *
       * @param rawEnvAttributes The resource attributes as a comma-separated list
       * of key/value pairs.
       * @returns The parsed resource attributes.
       * @throws Error if parsing fails (caller handles by discarding all attributes)
       */
      _parseResourceAttributes(rawEnvAttributes) {
        if (!rawEnvAttributes)
          return {};
        const attributes = {};
        const rawAttributes = rawEnvAttributes.split(this._COMMA_SEPARATOR).filter((attr) => attr.trim() !== "");
        for (const rawAttribute of rawAttributes) {
          const keyValuePair = rawAttribute.split(this._LABEL_KEY_VALUE_SPLITTER);
          if (keyValuePair.length !== 2) {
            throw new Error(`Invalid format for OTEL_RESOURCE_ATTRIBUTES: "${rawAttribute}". Expected format: key=value. The ',' and '=' characters must be percent-encoded in keys and values.`);
          }
          const [rawKey, rawValue] = keyValuePair;
          const key = rawKey.trim();
          const value2 = rawValue.trim();
          if (key.length === 0) {
            throw new Error(`Invalid OTEL_RESOURCE_ATTRIBUTES: empty attribute key in "${rawAttribute}".`);
          }
          let decodedKey;
          let decodedValue;
          try {
            decodedKey = decodeURIComponent(key);
            decodedValue = decodeURIComponent(value2);
          } catch (e) {
            throw new Error(`Failed to percent-decode OTEL_RESOURCE_ATTRIBUTES entry "${rawAttribute}": ${e instanceof Error ? e.message : e}`);
          }
          if (decodedKey.length > this._MAX_LENGTH) {
            throw new Error(`Attribute key exceeds the maximum length of ${this._MAX_LENGTH} characters: "${decodedKey}".`);
          }
          if (decodedValue.length > this._MAX_LENGTH) {
            throw new Error(`Attribute value exceeds the maximum length of ${this._MAX_LENGTH} characters for key "${decodedKey}".`);
          }
          attributes[decodedKey] = decodedValue;
        }
        return attributes;
      }
    };
    exports.envDetector = new EnvDetector();
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/semconv.js
var require_semconv = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/semconv.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ATTR_WEBENGINE_VERSION = exports.ATTR_WEBENGINE_NAME = exports.ATTR_WEBENGINE_DESCRIPTION = exports.ATTR_SERVICE_NAMESPACE = exports.ATTR_SERVICE_INSTANCE_ID = exports.ATTR_PROCESS_RUNTIME_VERSION = exports.ATTR_PROCESS_RUNTIME_NAME = exports.ATTR_PROCESS_RUNTIME_DESCRIPTION = exports.ATTR_PROCESS_PID = exports.ATTR_PROCESS_OWNER = exports.ATTR_PROCESS_EXECUTABLE_PATH = exports.ATTR_PROCESS_EXECUTABLE_NAME = exports.ATTR_PROCESS_COMMAND_ARGS = exports.ATTR_PROCESS_COMMAND = exports.ATTR_OS_VERSION = exports.ATTR_OS_TYPE = exports.ATTR_K8S_POD_NAME = exports.ATTR_K8S_NAMESPACE_NAME = exports.ATTR_K8S_DEPLOYMENT_NAME = exports.ATTR_K8S_CLUSTER_NAME = exports.ATTR_HOST_TYPE = exports.ATTR_HOST_NAME = exports.ATTR_HOST_IMAGE_VERSION = exports.ATTR_HOST_IMAGE_NAME = exports.ATTR_HOST_IMAGE_ID = exports.ATTR_HOST_ID = exports.ATTR_HOST_ARCH = exports.ATTR_CONTAINER_NAME = exports.ATTR_CONTAINER_IMAGE_TAGS = exports.ATTR_CONTAINER_IMAGE_NAME = exports.ATTR_CONTAINER_ID = exports.ATTR_CLOUD_REGION = exports.ATTR_CLOUD_PROVIDER = exports.ATTR_CLOUD_AVAILABILITY_ZONE = exports.ATTR_CLOUD_ACCOUNT_ID = void 0;
    exports.ATTR_CLOUD_ACCOUNT_ID = "cloud.account.id";
    exports.ATTR_CLOUD_AVAILABILITY_ZONE = "cloud.availability_zone";
    exports.ATTR_CLOUD_PROVIDER = "cloud.provider";
    exports.ATTR_CLOUD_REGION = "cloud.region";
    exports.ATTR_CONTAINER_ID = "container.id";
    exports.ATTR_CONTAINER_IMAGE_NAME = "container.image.name";
    exports.ATTR_CONTAINER_IMAGE_TAGS = "container.image.tags";
    exports.ATTR_CONTAINER_NAME = "container.name";
    exports.ATTR_HOST_ARCH = "host.arch";
    exports.ATTR_HOST_ID = "host.id";
    exports.ATTR_HOST_IMAGE_ID = "host.image.id";
    exports.ATTR_HOST_IMAGE_NAME = "host.image.name";
    exports.ATTR_HOST_IMAGE_VERSION = "host.image.version";
    exports.ATTR_HOST_NAME = "host.name";
    exports.ATTR_HOST_TYPE = "host.type";
    exports.ATTR_K8S_CLUSTER_NAME = "k8s.cluster.name";
    exports.ATTR_K8S_DEPLOYMENT_NAME = "k8s.deployment.name";
    exports.ATTR_K8S_NAMESPACE_NAME = "k8s.namespace.name";
    exports.ATTR_K8S_POD_NAME = "k8s.pod.name";
    exports.ATTR_OS_TYPE = "os.type";
    exports.ATTR_OS_VERSION = "os.version";
    exports.ATTR_PROCESS_COMMAND = "process.command";
    exports.ATTR_PROCESS_COMMAND_ARGS = "process.command_args";
    exports.ATTR_PROCESS_EXECUTABLE_NAME = "process.executable.name";
    exports.ATTR_PROCESS_EXECUTABLE_PATH = "process.executable.path";
    exports.ATTR_PROCESS_OWNER = "process.owner";
    exports.ATTR_PROCESS_PID = "process.pid";
    exports.ATTR_PROCESS_RUNTIME_DESCRIPTION = "process.runtime.description";
    exports.ATTR_PROCESS_RUNTIME_NAME = "process.runtime.name";
    exports.ATTR_PROCESS_RUNTIME_VERSION = "process.runtime.version";
    exports.ATTR_SERVICE_INSTANCE_ID = "service.instance.id";
    exports.ATTR_SERVICE_NAMESPACE = "service.namespace";
    exports.ATTR_WEBENGINE_DESCRIPTION = "webengine.description";
    exports.ATTR_WEBENGINE_NAME = "webengine.name";
    exports.ATTR_WEBENGINE_VERSION = "webengine.version";
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/machine-id/getMachineId.js
var require_getMachineId = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/machine-id/getMachineId.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getMachineId = void 0;
    var process2 = __require("process");
    var getMachineIdImpl;
    async function getMachineId() {
      if (!getMachineIdImpl) {
        switch (process2.platform) {
          case "darwin":
            getMachineIdImpl = (await import("./getMachineId-darwin-6YJZPTJU.mjs")).getMachineId;
            break;
          case "linux":
            getMachineIdImpl = (await import("./getMachineId-linux-BVVB47H7.mjs")).getMachineId;
            break;
          case "freebsd":
            getMachineIdImpl = (await import("./getMachineId-bsd-WS5A7H2M.mjs")).getMachineId;
            break;
          case "win32":
            getMachineIdImpl = (await import("./getMachineId-win-V7DOIQEI.mjs")).getMachineId;
            break;
          default:
            getMachineIdImpl = (await import("./getMachineId-unsupported-CNTO434P.mjs")).getMachineId;
            break;
        }
      }
      return getMachineIdImpl();
    }
    __name(getMachineId, "getMachineId");
    exports.getMachineId = getMachineId;
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/utils.js
var require_utils4 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/utils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.normalizeType = exports.normalizeArch = void 0;
    var normalizeArch = /* @__PURE__ */ __name((nodeArchString) => {
      switch (nodeArchString) {
        case "arm":
          return "arm32";
        case "ppc":
          return "ppc32";
        case "x64":
          return "amd64";
        default:
          return nodeArchString;
      }
    }, "normalizeArch");
    exports.normalizeArch = normalizeArch;
    var normalizeType = /* @__PURE__ */ __name((nodePlatform) => {
      switch (nodePlatform) {
        case "sunos":
          return "solaris";
        case "win32":
          return "windows";
        default:
          return nodePlatform;
      }
    }, "normalizeType");
    exports.normalizeType = normalizeType;
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/HostDetector.js
var require_HostDetector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/HostDetector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.hostDetector = void 0;
    var semconv_1 = require_semconv();
    var os_1 = __require("os");
    var getMachineId_1 = require_getMachineId();
    var utils_1 = require_utils4();
    var HostDetector = class {
      static {
        __name(this, "HostDetector");
      }
      detect(_config) {
        const attributes = {
          [semconv_1.ATTR_HOST_NAME]: (0, os_1.hostname)(),
          [semconv_1.ATTR_HOST_ARCH]: (0, utils_1.normalizeArch)((0, os_1.arch)()),
          [semconv_1.ATTR_HOST_ID]: (0, getMachineId_1.getMachineId)()
        };
        return { attributes };
      }
    };
    exports.hostDetector = new HostDetector();
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/OSDetector.js
var require_OSDetector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/OSDetector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.osDetector = void 0;
    var semconv_1 = require_semconv();
    var os_1 = __require("os");
    var utils_1 = require_utils4();
    var OSDetector = class {
      static {
        __name(this, "OSDetector");
      }
      detect(_config) {
        const attributes = {
          [semconv_1.ATTR_OS_TYPE]: (0, utils_1.normalizeType)((0, os_1.platform)()),
          [semconv_1.ATTR_OS_VERSION]: (0, os_1.release)()
        };
        return { attributes };
      }
    };
    exports.osDetector = new OSDetector();
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/ProcessDetector.js
var require_ProcessDetector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/ProcessDetector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.processDetector = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var semconv_1 = require_semconv();
    var os = __require("os");
    var ProcessDetector = class {
      static {
        __name(this, "ProcessDetector");
      }
      detect(_config) {
        const attributes = {
          [semconv_1.ATTR_PROCESS_PID]: process.pid,
          [semconv_1.ATTR_PROCESS_EXECUTABLE_NAME]: process.title,
          [semconv_1.ATTR_PROCESS_EXECUTABLE_PATH]: process.execPath,
          [semconv_1.ATTR_PROCESS_COMMAND_ARGS]: [
            process.argv[0],
            ...process.execArgv,
            ...process.argv.slice(1)
          ],
          [semconv_1.ATTR_PROCESS_RUNTIME_VERSION]: process.versions.node,
          [semconv_1.ATTR_PROCESS_RUNTIME_NAME]: "nodejs",
          [semconv_1.ATTR_PROCESS_RUNTIME_DESCRIPTION]: "Node.js"
        };
        if (process.argv.length > 1) {
          attributes[semconv_1.ATTR_PROCESS_COMMAND] = process.argv[1];
        }
        try {
          const userInfo = os.userInfo();
          attributes[semconv_1.ATTR_PROCESS_OWNER] = userInfo.username;
        } catch (e) {
          api_1.diag.debug(`error obtaining process owner: ${e}`);
        }
        return { attributes };
      }
    };
    exports.processDetector = new ProcessDetector();
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/ServiceInstanceIdDetector.js
var require_ServiceInstanceIdDetector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/ServiceInstanceIdDetector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serviceInstanceIdDetector = void 0;
    var semconv_1 = require_semconv();
    var crypto_1 = __require("crypto");
    var ServiceInstanceIdDetector = class {
      static {
        __name(this, "ServiceInstanceIdDetector");
      }
      detect(_config) {
        return {
          attributes: {
            [semconv_1.ATTR_SERVICE_INSTANCE_ID]: (0, crypto_1.randomUUID)()
          }
        };
      }
    };
    exports.serviceInstanceIdDetector = new ServiceInstanceIdDetector();
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/index.js
var require_node = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/node/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serviceInstanceIdDetector = exports.processDetector = exports.osDetector = exports.hostDetector = void 0;
    var HostDetector_1 = require_HostDetector();
    Object.defineProperty(exports, "hostDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return HostDetector_1.hostDetector;
    }, "get") });
    var OSDetector_1 = require_OSDetector();
    Object.defineProperty(exports, "osDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OSDetector_1.osDetector;
    }, "get") });
    var ProcessDetector_1 = require_ProcessDetector();
    Object.defineProperty(exports, "processDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ProcessDetector_1.processDetector;
    }, "get") });
    var ServiceInstanceIdDetector_1 = require_ServiceInstanceIdDetector();
    Object.defineProperty(exports, "serviceInstanceIdDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ServiceInstanceIdDetector_1.serviceInstanceIdDetector;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/index.js
var require_platform = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/platform/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serviceInstanceIdDetector = exports.processDetector = exports.osDetector = exports.hostDetector = void 0;
    var node_1 = require_node();
    Object.defineProperty(exports, "hostDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.hostDetector;
    }, "get") });
    Object.defineProperty(exports, "osDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.osDetector;
    }, "get") });
    Object.defineProperty(exports, "processDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.processDetector;
    }, "get") });
    Object.defineProperty(exports, "serviceInstanceIdDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.serviceInstanceIdDetector;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/NoopDetector.js
var require_NoopDetector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/NoopDetector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.noopDetector = exports.NoopDetector = void 0;
    var NoopDetector = class {
      static {
        __name(this, "NoopDetector");
      }
      detect() {
        return {
          attributes: {}
        };
      }
    };
    exports.NoopDetector = NoopDetector;
    exports.noopDetector = new NoopDetector();
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/index.js
var require_detectors = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/detectors/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.noopDetector = exports.serviceInstanceIdDetector = exports.processDetector = exports.osDetector = exports.hostDetector = exports.envDetector = void 0;
    var EnvDetector_1 = require_EnvDetector();
    Object.defineProperty(exports, "envDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return EnvDetector_1.envDetector;
    }, "get") });
    var platform_1 = require_platform();
    Object.defineProperty(exports, "hostDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.hostDetector;
    }, "get") });
    Object.defineProperty(exports, "osDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.osDetector;
    }, "get") });
    Object.defineProperty(exports, "processDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.processDetector;
    }, "get") });
    Object.defineProperty(exports, "serviceInstanceIdDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.serviceInstanceIdDetector;
    }, "get") });
    var NoopDetector_1 = require_NoopDetector();
    Object.defineProperty(exports, "noopDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return NoopDetector_1.noopDetector;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/index.js
var require_src5 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+resources@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/resources/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.defaultServiceName = exports.emptyResource = exports.defaultResource = exports.resourceFromAttributes = exports.serviceInstanceIdDetector = exports.processDetector = exports.osDetector = exports.hostDetector = exports.envDetector = exports.detectResources = void 0;
    var detect_resources_1 = require_detect_resources();
    Object.defineProperty(exports, "detectResources", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return detect_resources_1.detectResources;
    }, "get") });
    var detectors_1 = require_detectors();
    Object.defineProperty(exports, "envDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return detectors_1.envDetector;
    }, "get") });
    Object.defineProperty(exports, "hostDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return detectors_1.hostDetector;
    }, "get") });
    Object.defineProperty(exports, "osDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return detectors_1.osDetector;
    }, "get") });
    Object.defineProperty(exports, "processDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return detectors_1.processDetector;
    }, "get") });
    Object.defineProperty(exports, "serviceInstanceIdDetector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return detectors_1.serviceInstanceIdDetector;
    }, "get") });
    var ResourceImpl_1 = require_ResourceImpl();
    Object.defineProperty(exports, "resourceFromAttributes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ResourceImpl_1.resourceFromAttributes;
    }, "get") });
    Object.defineProperty(exports, "defaultResource", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ResourceImpl_1.defaultResource;
    }, "get") });
    Object.defineProperty(exports, "emptyResource", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ResourceImpl_1.emptyResource;
    }, "get") });
    var default_service_name_1 = require_default_service_name();
    Object.defineProperty(exports, "defaultServiceName", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return default_service_name_1.defaultServiceName;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/ViewRegistry.js
var require_ViewRegistry = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/ViewRegistry.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ViewRegistry = void 0;
    var ViewRegistry = class {
      static {
        __name(this, "ViewRegistry");
      }
      _registeredViews = [];
      addView(view) {
        this._registeredViews.push(view);
      }
      findViews(instrument, meter) {
        const views = this._registeredViews.filter((registeredView) => {
          return this._matchInstrument(registeredView.instrumentSelector, instrument) && this._matchMeter(registeredView.meterSelector, meter);
        });
        return views;
      }
      _matchInstrument(selector, instrument) {
        return (selector.getType() === void 0 || instrument.type === selector.getType()) && selector.getNameFilter().match(instrument.name) && selector.getUnitFilter().match(instrument.unit);
      }
      _matchMeter(selector, meter) {
        return selector.getNameFilter().match(meter.name) && (meter.version === void 0 || selector.getVersionFilter().match(meter.version)) && (meter.schemaUrl === void 0 || selector.getSchemaUrlFilter().match(meter.schemaUrl));
      }
    };
    exports.ViewRegistry = ViewRegistry;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/InstrumentDescriptor.js
var require_InstrumentDescriptor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/InstrumentDescriptor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isValidName = exports.isDescriptorCompatibleWith = exports.createInstrumentDescriptorWithView = exports.createInstrumentDescriptor = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var utils_1 = require_utils2();
    function createInstrumentDescriptor(name, type, options) {
      if (!isValidName(name)) {
        api_1.diag.warn(`Invalid metric name: "${name}". The metric name should be a ASCII string with a length no greater than 255 characters.`);
      }
      return {
        name,
        type,
        description: options?.description ?? "",
        unit: options?.unit ?? "",
        valueType: options?.valueType ?? api_1.ValueType.DOUBLE,
        advice: options?.advice ?? {}
      };
    }
    __name(createInstrumentDescriptor, "createInstrumentDescriptor");
    exports.createInstrumentDescriptor = createInstrumentDescriptor;
    function createInstrumentDescriptorWithView(view, instrument) {
      return {
        name: view.name ?? instrument.name,
        description: view.description ?? instrument.description,
        type: instrument.type,
        unit: instrument.unit,
        valueType: instrument.valueType,
        advice: instrument.advice
      };
    }
    __name(createInstrumentDescriptorWithView, "createInstrumentDescriptorWithView");
    exports.createInstrumentDescriptorWithView = createInstrumentDescriptorWithView;
    function isDescriptorCompatibleWith(descriptor, otherDescriptor) {
      return (0, utils_1.equalsCaseInsensitive)(descriptor.name, otherDescriptor.name) && descriptor.unit === otherDescriptor.unit && descriptor.type === otherDescriptor.type && descriptor.valueType === otherDescriptor.valueType;
    }
    __name(isDescriptorCompatibleWith, "isDescriptorCompatibleWith");
    exports.isDescriptorCompatibleWith = isDescriptorCompatibleWith;
    var NAME_REGEXP = /^[a-z][a-z0-9_.\-/]{0,254}$/i;
    function isValidName(name) {
      return NAME_REGEXP.test(name);
    }
    __name(isValidName, "isValidName");
    exports.isValidName = isValidName;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/Instruments.js
var require_Instruments = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/Instruments.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isObservableInstrument = exports.ObservableUpDownCounterInstrument = exports.ObservableGaugeInstrument = exports.ObservableCounterInstrument = exports.ObservableInstrument = exports.HistogramInstrument = exports.GaugeInstrument = exports.CounterInstrument = exports.UpDownCounterInstrument = exports.SyncInstrument = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var SyncInstrument = class {
      static {
        __name(this, "SyncInstrument");
      }
      _writableMetricStorage;
      _descriptor;
      constructor(writableMetricStorage, descriptor) {
        this._writableMetricStorage = writableMetricStorage;
        this._descriptor = descriptor;
      }
      _record(value2, attributes = {}, context2 = api_1.context.active()) {
        if (typeof value2 !== "number") {
          api_1.diag.warn(`non-number value provided to metric ${this._descriptor.name}: ${value2}`);
          return;
        }
        if (this._descriptor.valueType === api_1.ValueType.INT && !Number.isInteger(value2)) {
          api_1.diag.warn(`INT value type cannot accept a floating-point value for ${this._descriptor.name}, ignoring the fractional digits.`);
          value2 = Math.trunc(value2);
          if (!Number.isInteger(value2)) {
            return;
          }
        }
        this._writableMetricStorage.record(value2, attributes, context2, (0, core_1.millisToHrTime)(Date.now()));
      }
    };
    exports.SyncInstrument = SyncInstrument;
    var UpDownCounterInstrument = class extends SyncInstrument {
      static {
        __name(this, "UpDownCounterInstrument");
      }
      /**
       * Increment value of counter by the input. Inputs may be negative.
       */
      add(value2, attributes, ctx) {
        this._record(value2, attributes, ctx);
      }
    };
    exports.UpDownCounterInstrument = UpDownCounterInstrument;
    var CounterInstrument = class extends SyncInstrument {
      static {
        __name(this, "CounterInstrument");
      }
      /**
       * Increment value of counter by the input. Inputs may not be negative.
       */
      add(value2, attributes, ctx) {
        if (value2 < 0) {
          api_1.diag.warn(`negative value provided to counter ${this._descriptor.name}: ${value2}`);
          return;
        }
        this._record(value2, attributes, ctx);
      }
    };
    exports.CounterInstrument = CounterInstrument;
    var GaugeInstrument = class extends SyncInstrument {
      static {
        __name(this, "GaugeInstrument");
      }
      /**
       * Records a measurement.
       */
      record(value2, attributes, ctx) {
        this._record(value2, attributes, ctx);
      }
    };
    exports.GaugeInstrument = GaugeInstrument;
    var HistogramInstrument = class extends SyncInstrument {
      static {
        __name(this, "HistogramInstrument");
      }
      /**
       * Records a measurement. Value of the measurement must not be negative.
       */
      record(value2, attributes, ctx) {
        if (value2 < 0) {
          api_1.diag.warn(`negative value provided to histogram ${this._descriptor.name}: ${value2}`);
          return;
        }
        this._record(value2, attributes, ctx);
      }
    };
    exports.HistogramInstrument = HistogramInstrument;
    var ObservableInstrument = class {
      static {
        __name(this, "ObservableInstrument");
      }
      /** @internal */
      _metricStorages;
      /** @internal */
      _descriptor;
      _observableRegistry;
      constructor(descriptor, metricStorages, observableRegistry) {
        this._descriptor = descriptor;
        this._metricStorages = metricStorages;
        this._observableRegistry = observableRegistry;
      }
      /**
       * @see {Observable.addCallback}
       */
      addCallback(callback) {
        this._observableRegistry.addCallback(callback, this);
      }
      /**
       * @see {Observable.removeCallback}
       */
      removeCallback(callback) {
        this._observableRegistry.removeCallback(callback, this);
      }
    };
    exports.ObservableInstrument = ObservableInstrument;
    var ObservableCounterInstrument = class extends ObservableInstrument {
      static {
        __name(this, "ObservableCounterInstrument");
      }
    };
    exports.ObservableCounterInstrument = ObservableCounterInstrument;
    var ObservableGaugeInstrument = class extends ObservableInstrument {
      static {
        __name(this, "ObservableGaugeInstrument");
      }
    };
    exports.ObservableGaugeInstrument = ObservableGaugeInstrument;
    var ObservableUpDownCounterInstrument = class extends ObservableInstrument {
      static {
        __name(this, "ObservableUpDownCounterInstrument");
      }
    };
    exports.ObservableUpDownCounterInstrument = ObservableUpDownCounterInstrument;
    function isObservableInstrument(it) {
      return it instanceof ObservableInstrument;
    }
    __name(isObservableInstrument, "isObservableInstrument");
    exports.isObservableInstrument = isObservableInstrument;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/Meter.js
var require_Meter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/Meter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Meter = void 0;
    var InstrumentDescriptor_1 = require_InstrumentDescriptor();
    var Instruments_1 = require_Instruments();
    var MetricData_1 = require_MetricData();
    var Meter = class {
      static {
        __name(this, "Meter");
      }
      _meterSharedState;
      constructor(meterSharedState) {
        this._meterSharedState = meterSharedState;
      }
      /**
       * Create a {@link Gauge} instrument.
       */
      createGauge(name, options) {
        const descriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(name, MetricData_1.InstrumentType.GAUGE, options);
        const storage = this._meterSharedState.registerMetricStorage(descriptor);
        return new Instruments_1.GaugeInstrument(storage, descriptor);
      }
      /**
       * Create a {@link Histogram} instrument.
       */
      createHistogram(name, options) {
        const descriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(name, MetricData_1.InstrumentType.HISTOGRAM, options);
        const storage = this._meterSharedState.registerMetricStorage(descriptor);
        return new Instruments_1.HistogramInstrument(storage, descriptor);
      }
      /**
       * Create a {@link Counter} instrument.
       */
      createCounter(name, options) {
        const descriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(name, MetricData_1.InstrumentType.COUNTER, options);
        const storage = this._meterSharedState.registerMetricStorage(descriptor);
        return new Instruments_1.CounterInstrument(storage, descriptor);
      }
      /**
       * Create a {@link UpDownCounter} instrument.
       */
      createUpDownCounter(name, options) {
        const descriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(name, MetricData_1.InstrumentType.UP_DOWN_COUNTER, options);
        const storage = this._meterSharedState.registerMetricStorage(descriptor);
        return new Instruments_1.UpDownCounterInstrument(storage, descriptor);
      }
      /**
       * Create a {@link ObservableGauge} instrument.
       */
      createObservableGauge(name, options) {
        const descriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(name, MetricData_1.InstrumentType.OBSERVABLE_GAUGE, options);
        const storages = this._meterSharedState.registerAsyncMetricStorage(descriptor);
        return new Instruments_1.ObservableGaugeInstrument(descriptor, storages, this._meterSharedState.observableRegistry);
      }
      /**
       * Create a {@link ObservableCounter} instrument.
       */
      createObservableCounter(name, options) {
        const descriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(name, MetricData_1.InstrumentType.OBSERVABLE_COUNTER, options);
        const storages = this._meterSharedState.registerAsyncMetricStorage(descriptor);
        return new Instruments_1.ObservableCounterInstrument(descriptor, storages, this._meterSharedState.observableRegistry);
      }
      /**
       * Create a {@link ObservableUpDownCounter} instrument.
       */
      createObservableUpDownCounter(name, options) {
        const descriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(name, MetricData_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER, options);
        const storages = this._meterSharedState.registerAsyncMetricStorage(descriptor);
        return new Instruments_1.ObservableUpDownCounterInstrument(descriptor, storages, this._meterSharedState.observableRegistry);
      }
      /**
       * @see {@link Meter.addBatchObservableCallback}
       */
      addBatchObservableCallback(callback, observables) {
        this._meterSharedState.observableRegistry.addBatchCallback(callback, observables);
      }
      /**
       * @see {@link Meter.removeBatchObservableCallback}
       */
      removeBatchObservableCallback(callback, observables) {
        this._meterSharedState.observableRegistry.removeBatchCallback(callback, observables);
      }
    };
    exports.Meter = Meter;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MetricStorage.js
var require_MetricStorage = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MetricStorage.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MetricStorage = void 0;
    var InstrumentDescriptor_1 = require_InstrumentDescriptor();
    var MetricStorage = class {
      static {
        __name(this, "MetricStorage");
      }
      _instrumentDescriptor;
      constructor(instrumentDescriptor) {
        this._instrumentDescriptor = instrumentDescriptor;
      }
      getInstrumentDescriptor() {
        return this._instrumentDescriptor;
      }
      updateDescription(description) {
        this._instrumentDescriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptor)(this._instrumentDescriptor.name, this._instrumentDescriptor.type, {
          description,
          valueType: this._instrumentDescriptor.valueType,
          unit: this._instrumentDescriptor.unit,
          advice: this._instrumentDescriptor.advice
        });
      }
    };
    exports.MetricStorage = MetricStorage;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/HashMap.js
var require_HashMap = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/HashMap.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AttributeHashMap = exports.HashMap = void 0;
    var utils_1 = require_utils2();
    var HashMap = class {
      static {
        __name(this, "HashMap");
      }
      _valueMap = /* @__PURE__ */ new Map();
      _keyMap = /* @__PURE__ */ new Map();
      _hash;
      constructor(hash) {
        this._hash = hash;
      }
      get(key, hashCode) {
        hashCode ??= this._hash(key);
        return this._valueMap.get(hashCode);
      }
      getOrDefault(key, defaultFactory) {
        const hash = this._hash(key);
        if (this._valueMap.has(hash)) {
          return this._valueMap.get(hash);
        }
        const val = defaultFactory();
        if (!this._keyMap.has(hash)) {
          this._keyMap.set(hash, key);
        }
        this._valueMap.set(hash, val);
        return val;
      }
      set(key, value2, hashCode) {
        hashCode ??= this._hash(key);
        if (!this._keyMap.has(hashCode)) {
          this._keyMap.set(hashCode, key);
        }
        this._valueMap.set(hashCode, value2);
      }
      has(key, hashCode) {
        hashCode ??= this._hash(key);
        return this._valueMap.has(hashCode);
      }
      *keys() {
        const keyIterator = this._keyMap.entries();
        let next = keyIterator.next();
        while (next.done !== true) {
          yield [next.value[1], next.value[0]];
          next = keyIterator.next();
        }
      }
      *entries() {
        const valueIterator = this._valueMap.entries();
        let next = valueIterator.next();
        while (next.done !== true) {
          yield [this._keyMap.get(next.value[0]), next.value[1], next.value[0]];
          next = valueIterator.next();
        }
      }
      get size() {
        return this._valueMap.size;
      }
    };
    exports.HashMap = HashMap;
    var AttributeHashMap = class extends HashMap {
      static {
        __name(this, "AttributeHashMap");
      }
      constructor() {
        super(utils_1.hashAttributes);
      }
    };
    exports.AttributeHashMap = AttributeHashMap;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/DeltaMetricProcessor.js
var require_DeltaMetricProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/DeltaMetricProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DeltaMetricProcessor = void 0;
    var utils_1 = require_utils2();
    var HashMap_1 = require_HashMap();
    var DeltaMetricProcessor = class {
      static {
        __name(this, "DeltaMetricProcessor");
      }
      _activeCollectionStorage = new HashMap_1.AttributeHashMap();
      // TODO: find a reasonable mean to clean the memo;
      // https://github.com/open-telemetry/opentelemetry-specification/pull/2208
      _cumulativeMemoStorage = new HashMap_1.AttributeHashMap();
      _cardinalityLimit;
      _overflowAttributes = { "otel.metric.overflow": true };
      _overflowHashCode;
      _aggregator;
      constructor(aggregator, aggregationCardinalityLimit) {
        this._aggregator = aggregator;
        this._cardinalityLimit = (aggregationCardinalityLimit ?? 2e3) - 1;
        this._overflowHashCode = (0, utils_1.hashAttributes)(this._overflowAttributes);
      }
      record(value2, attributes, _context, collectionTime) {
        let accumulation = this._activeCollectionStorage.get(attributes);
        if (!accumulation) {
          if (this._activeCollectionStorage.size >= this._cardinalityLimit) {
            const overflowAccumulation = this._activeCollectionStorage.getOrDefault(this._overflowAttributes, () => this._aggregator.createAccumulation(collectionTime));
            overflowAccumulation?.record(value2);
            return;
          }
          accumulation = this._aggregator.createAccumulation(collectionTime);
          this._activeCollectionStorage.set(attributes, accumulation);
        }
        accumulation?.record(value2);
      }
      batchCumulate(measurements, collectionTime) {
        for (const [originalAttributes, value2, originalHashCode] of measurements.entries()) {
          let attributes = originalAttributes;
          let hashCode = originalHashCode;
          const accumulation = this._aggregator.createAccumulation(collectionTime);
          accumulation?.record(value2);
          let delta = accumulation;
          if (this._cumulativeMemoStorage.has(attributes, hashCode)) {
            const previous = this._cumulativeMemoStorage.get(attributes, hashCode);
            delta = this._aggregator.diff(previous, accumulation);
          } else {
            if (this._cumulativeMemoStorage.size >= this._cardinalityLimit) {
              attributes = this._overflowAttributes;
              hashCode = this._overflowHashCode;
              if (this._cumulativeMemoStorage.has(attributes, hashCode)) {
                const previous = this._cumulativeMemoStorage.get(attributes, hashCode);
                delta = this._aggregator.diff(previous, accumulation);
              }
            }
          }
          if (this._activeCollectionStorage.has(attributes, hashCode)) {
            const active = this._activeCollectionStorage.get(attributes, hashCode);
            delta = this._aggregator.merge(active, delta);
          }
          this._cumulativeMemoStorage.set(attributes, accumulation, hashCode);
          this._activeCollectionStorage.set(attributes, delta, hashCode);
        }
      }
      /**
       * Returns a collection of delta metrics. Start time is the when first
       * time event collected.
       */
      collect() {
        const unreportedDelta = this._activeCollectionStorage;
        this._activeCollectionStorage = new HashMap_1.AttributeHashMap();
        return unreportedDelta;
      }
    };
    exports.DeltaMetricProcessor = DeltaMetricProcessor;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/TemporalMetricProcessor.js
var require_TemporalMetricProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/TemporalMetricProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TemporalMetricProcessor = void 0;
    var AggregationTemporality_1 = require_AggregationTemporality();
    var HashMap_1 = require_HashMap();
    var TemporalMetricProcessor = class _TemporalMetricProcessor {
      static {
        __name(this, "TemporalMetricProcessor");
      }
      _aggregator;
      _unreportedAccumulations = /* @__PURE__ */ new Map();
      _reportHistory = /* @__PURE__ */ new Map();
      constructor(aggregator, collectorHandles) {
        this._aggregator = aggregator;
        collectorHandles.forEach((handle) => {
          this._unreportedAccumulations.set(handle, []);
        });
      }
      /**
       * Builds the {@link MetricData} streams to report against a specific MetricCollector.
       * @param collector The information of the MetricCollector.
       * @param collectors The registered collectors.
       * @param instrumentDescriptor The instrumentation descriptor that these metrics generated with.
       * @param currentAccumulations The current accumulation of metric data from instruments.
       * @param collectionTime The current collection timestamp.
       * @returns The {@link MetricData} points or `null`.
       */
      buildMetrics(collector, instrumentDescriptor, currentAccumulations, collectionTime) {
        this._stashAccumulations(currentAccumulations);
        const unreportedAccumulations = this._getMergedUnreportedAccumulations(collector);
        let result = unreportedAccumulations;
        let aggregationTemporality;
        if (this._reportHistory.has(collector)) {
          const last = this._reportHistory.get(collector);
          const lastCollectionTime = last.collectionTime;
          aggregationTemporality = last.aggregationTemporality;
          if (aggregationTemporality === AggregationTemporality_1.AggregationTemporality.CUMULATIVE) {
            result = _TemporalMetricProcessor.merge(last.accumulations, unreportedAccumulations, this._aggregator);
          } else {
            result = _TemporalMetricProcessor.calibrateStartTime(last.accumulations, unreportedAccumulations, lastCollectionTime);
          }
        } else {
          aggregationTemporality = collector.selectAggregationTemporality(instrumentDescriptor.type);
        }
        this._reportHistory.set(collector, {
          accumulations: result,
          collectionTime,
          aggregationTemporality
        });
        const accumulationRecords = AttributesMapToAccumulationRecords(result);
        if (accumulationRecords.length === 0) {
          return void 0;
        }
        return this._aggregator.toMetricData(
          instrumentDescriptor,
          aggregationTemporality,
          accumulationRecords,
          /* endTime */
          collectionTime
        );
      }
      _stashAccumulations(currentAccumulation) {
        const registeredCollectors = this._unreportedAccumulations.keys();
        for (const collector of registeredCollectors) {
          let stash = this._unreportedAccumulations.get(collector);
          if (stash === void 0) {
            stash = [];
            this._unreportedAccumulations.set(collector, stash);
          }
          stash.push(currentAccumulation);
        }
      }
      _getMergedUnreportedAccumulations(collector) {
        let result = new HashMap_1.AttributeHashMap();
        const unreportedList = this._unreportedAccumulations.get(collector);
        this._unreportedAccumulations.set(collector, []);
        if (unreportedList === void 0) {
          return result;
        }
        for (const it of unreportedList) {
          result = _TemporalMetricProcessor.merge(result, it, this._aggregator);
        }
        return result;
      }
      static merge(last, current, aggregator) {
        const result = last;
        const iterator = current.entries();
        let next = iterator.next();
        while (next.done !== true) {
          const [key, record, hash] = next.value;
          if (last.has(key, hash)) {
            const lastAccumulation = last.get(key, hash);
            const accumulation = aggregator.merge(lastAccumulation, record);
            result.set(key, accumulation, hash);
          } else {
            result.set(key, record, hash);
          }
          next = iterator.next();
        }
        return result;
      }
      /**
       * Calibrate the reported metric streams' startTime to lastCollectionTime. Leaves
       * the new stream to be the initial observation time unchanged.
       */
      static calibrateStartTime(last, current, lastCollectionTime) {
        for (const [key, hash] of last.keys()) {
          const currentAccumulation = current.get(key, hash);
          currentAccumulation?.setStartTime(lastCollectionTime);
        }
        return current;
      }
    };
    exports.TemporalMetricProcessor = TemporalMetricProcessor;
    function AttributesMapToAccumulationRecords(map) {
      return Array.from(map.entries());
    }
    __name(AttributesMapToAccumulationRecords, "AttributesMapToAccumulationRecords");
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/AsyncMetricStorage.js
var require_AsyncMetricStorage = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/AsyncMetricStorage.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AsyncMetricStorage = void 0;
    var MetricStorage_1 = require_MetricStorage();
    var DeltaMetricProcessor_1 = require_DeltaMetricProcessor();
    var TemporalMetricProcessor_1 = require_TemporalMetricProcessor();
    var HashMap_1 = require_HashMap();
    var AsyncMetricStorage = class extends MetricStorage_1.MetricStorage {
      static {
        __name(this, "AsyncMetricStorage");
      }
      _aggregationCardinalityLimit;
      _deltaMetricStorage;
      _temporalMetricStorage;
      _attributesProcessor;
      constructor(_instrumentDescriptor, aggregator, attributesProcessor, collectorHandles, aggregationCardinalityLimit) {
        super(_instrumentDescriptor);
        this._aggregationCardinalityLimit = aggregationCardinalityLimit;
        this._deltaMetricStorage = new DeltaMetricProcessor_1.DeltaMetricProcessor(aggregator, this._aggregationCardinalityLimit);
        this._temporalMetricStorage = new TemporalMetricProcessor_1.TemporalMetricProcessor(aggregator, collectorHandles);
        this._attributesProcessor = attributesProcessor;
      }
      record(measurements, observationTime) {
        const processed = new HashMap_1.AttributeHashMap();
        for (const [attributes, value2] of measurements.entries()) {
          processed.set(this._attributesProcessor.process(attributes), value2);
        }
        this._deltaMetricStorage.batchCumulate(processed, observationTime);
      }
      /**
       * Collects the metrics from this storage. The ObservableCallback is invoked
       * during the collection.
       *
       * Note: This is a stateful operation and may reset any interval-related
       * state for the MetricCollector.
       */
      collect(collector, collectionTime) {
        const accumulations = this._deltaMetricStorage.collect();
        return this._temporalMetricStorage.buildMetrics(collector, this._instrumentDescriptor, accumulations, collectionTime);
      }
    };
    exports.AsyncMetricStorage = AsyncMetricStorage;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/RegistrationConflicts.js
var require_RegistrationConflicts = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/RegistrationConflicts.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getConflictResolutionRecipe = exports.getDescriptionResolutionRecipe = exports.getTypeConflictResolutionRecipe = exports.getUnitConflictResolutionRecipe = exports.getValueTypeConflictResolutionRecipe = exports.getIncompatibilityDetails = void 0;
    function getIncompatibilityDetails(existing, otherDescriptor) {
      let incompatibility = "";
      if (existing.unit !== otherDescriptor.unit) {
        incompatibility += `	- Unit '${existing.unit}' does not match '${otherDescriptor.unit}'
`;
      }
      if (existing.type !== otherDescriptor.type) {
        incompatibility += `	- Type '${existing.type}' does not match '${otherDescriptor.type}'
`;
      }
      if (existing.valueType !== otherDescriptor.valueType) {
        incompatibility += `	- Value Type '${existing.valueType}' does not match '${otherDescriptor.valueType}'
`;
      }
      if (existing.description !== otherDescriptor.description) {
        incompatibility += `	- Description '${existing.description}' does not match '${otherDescriptor.description}'
`;
      }
      return incompatibility;
    }
    __name(getIncompatibilityDetails, "getIncompatibilityDetails");
    exports.getIncompatibilityDetails = getIncompatibilityDetails;
    function getValueTypeConflictResolutionRecipe(existing, otherDescriptor) {
      return `	- use valueType '${existing.valueType}' on instrument creation or use an instrument name other than '${otherDescriptor.name}'`;
    }
    __name(getValueTypeConflictResolutionRecipe, "getValueTypeConflictResolutionRecipe");
    exports.getValueTypeConflictResolutionRecipe = getValueTypeConflictResolutionRecipe;
    function getUnitConflictResolutionRecipe(existing, otherDescriptor) {
      return `	- use unit '${existing.unit}' on instrument creation or use an instrument name other than '${otherDescriptor.name}'`;
    }
    __name(getUnitConflictResolutionRecipe, "getUnitConflictResolutionRecipe");
    exports.getUnitConflictResolutionRecipe = getUnitConflictResolutionRecipe;
    function getTypeConflictResolutionRecipe(existing, otherDescriptor) {
      const selector = {
        name: otherDescriptor.name,
        type: otherDescriptor.type,
        unit: otherDescriptor.unit
      };
      const selectorString = JSON.stringify(selector);
      return `	- create a new view with a name other than '${existing.name}' and InstrumentSelector '${selectorString}'`;
    }
    __name(getTypeConflictResolutionRecipe, "getTypeConflictResolutionRecipe");
    exports.getTypeConflictResolutionRecipe = getTypeConflictResolutionRecipe;
    function getDescriptionResolutionRecipe(existing, otherDescriptor) {
      const selector = {
        name: otherDescriptor.name,
        type: otherDescriptor.type,
        unit: otherDescriptor.unit
      };
      const selectorString = JSON.stringify(selector);
      return `	- create a new view with a name other than '${existing.name}' and InstrumentSelector '${selectorString}'
    	- OR - create a new view with the name ${existing.name} and description '${existing.description}' and InstrumentSelector ${selectorString}
    	- OR - create a new view with the name ${otherDescriptor.name} and description '${existing.description}' and InstrumentSelector ${selectorString}`;
    }
    __name(getDescriptionResolutionRecipe, "getDescriptionResolutionRecipe");
    exports.getDescriptionResolutionRecipe = getDescriptionResolutionRecipe;
    function getConflictResolutionRecipe(existing, otherDescriptor) {
      if (existing.valueType !== otherDescriptor.valueType) {
        return getValueTypeConflictResolutionRecipe(existing, otherDescriptor);
      }
      if (existing.unit !== otherDescriptor.unit) {
        return getUnitConflictResolutionRecipe(existing, otherDescriptor);
      }
      if (existing.type !== otherDescriptor.type) {
        return getTypeConflictResolutionRecipe(existing, otherDescriptor);
      }
      if (existing.description !== otherDescriptor.description) {
        return getDescriptionResolutionRecipe(existing, otherDescriptor);
      }
      return "";
    }
    __name(getConflictResolutionRecipe, "getConflictResolutionRecipe");
    exports.getConflictResolutionRecipe = getConflictResolutionRecipe;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MetricStorageRegistry.js
var require_MetricStorageRegistry = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MetricStorageRegistry.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MetricStorageRegistry = void 0;
    var InstrumentDescriptor_1 = require_InstrumentDescriptor();
    var api = (init_esm2(), __toCommonJS(esm_exports));
    var RegistrationConflicts_1 = require_RegistrationConflicts();
    var MetricStorageRegistry = class _MetricStorageRegistry {
      static {
        __name(this, "MetricStorageRegistry");
      }
      _sharedRegistry = /* @__PURE__ */ new Map();
      _perCollectorRegistry = /* @__PURE__ */ new Map();
      static create() {
        return new _MetricStorageRegistry();
      }
      getStorages(collector) {
        let storages = [];
        for (const metricStorages of this._sharedRegistry.values()) {
          storages = storages.concat(metricStorages);
        }
        const perCollectorStorages = this._perCollectorRegistry.get(collector);
        if (perCollectorStorages != null) {
          for (const metricStorages of perCollectorStorages.values()) {
            storages = storages.concat(metricStorages);
          }
        }
        return storages;
      }
      register(storage) {
        this._registerStorage(storage, this._sharedRegistry);
      }
      registerForCollector(collector, storage) {
        let storageMap = this._perCollectorRegistry.get(collector);
        if (storageMap == null) {
          storageMap = /* @__PURE__ */ new Map();
          this._perCollectorRegistry.set(collector, storageMap);
        }
        this._registerStorage(storage, storageMap);
      }
      findOrUpdateCompatibleStorage(expectedDescriptor) {
        const storages = this._sharedRegistry.get(expectedDescriptor.name);
        if (storages === void 0) {
          return null;
        }
        return this._findOrUpdateCompatibleStorage(expectedDescriptor, storages);
      }
      findOrUpdateCompatibleCollectorStorage(collector, expectedDescriptor) {
        const storageMap = this._perCollectorRegistry.get(collector);
        if (storageMap === void 0) {
          return null;
        }
        const storages = storageMap.get(expectedDescriptor.name);
        if (storages === void 0) {
          return null;
        }
        return this._findOrUpdateCompatibleStorage(expectedDescriptor, storages);
      }
      _registerStorage(storage, storageMap) {
        const descriptor = storage.getInstrumentDescriptor();
        const storages = storageMap.get(descriptor.name);
        if (storages === void 0) {
          storageMap.set(descriptor.name, [storage]);
          return;
        }
        storages.push(storage);
      }
      _findOrUpdateCompatibleStorage(expectedDescriptor, existingStorages) {
        let compatibleStorage = null;
        for (const existingStorage of existingStorages) {
          const existingDescriptor = existingStorage.getInstrumentDescriptor();
          if ((0, InstrumentDescriptor_1.isDescriptorCompatibleWith)(existingDescriptor, expectedDescriptor)) {
            if (existingDescriptor.description !== expectedDescriptor.description) {
              if (expectedDescriptor.description.length > existingDescriptor.description.length) {
                existingStorage.updateDescription(expectedDescriptor.description);
              }
              api.diag.warn("A view or instrument with the name ", expectedDescriptor.name, " has already been registered, but has a different description and is incompatible with another registered view.\n", "Details:\n", (0, RegistrationConflicts_1.getIncompatibilityDetails)(existingDescriptor, expectedDescriptor), "The longer description will be used.\nTo resolve the conflict:", (0, RegistrationConflicts_1.getConflictResolutionRecipe)(existingDescriptor, expectedDescriptor));
            }
            compatibleStorage = existingStorage;
          } else {
            api.diag.warn("A view or instrument with the name ", expectedDescriptor.name, " has already been registered and is incompatible with another registered view.\n", "Details:\n", (0, RegistrationConflicts_1.getIncompatibilityDetails)(existingDescriptor, expectedDescriptor), "To resolve the conflict:\n", (0, RegistrationConflicts_1.getConflictResolutionRecipe)(existingDescriptor, expectedDescriptor));
          }
        }
        return compatibleStorage;
      }
    };
    exports.MetricStorageRegistry = MetricStorageRegistry;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MultiWritableMetricStorage.js
var require_MultiWritableMetricStorage = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MultiWritableMetricStorage.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MultiMetricStorage = void 0;
    var MultiMetricStorage = class {
      static {
        __name(this, "MultiMetricStorage");
      }
      _backingStorages;
      constructor(backingStorages) {
        this._backingStorages = backingStorages;
      }
      record(value2, attributes, context2, recordTime) {
        const storages = this._backingStorages;
        for (let i = 0; i < storages.length; i++) {
          storages[i].record(value2, attributes, context2, recordTime);
        }
      }
    };
    exports.MultiMetricStorage = MultiMetricStorage;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/ObservableResult.js
var require_ObservableResult = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/ObservableResult.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BatchObservableResultImpl = exports.ObservableResultImpl = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var HashMap_1 = require_HashMap();
    var Instruments_1 = require_Instruments();
    var ObservableResultImpl = class {
      static {
        __name(this, "ObservableResultImpl");
      }
      /**
       * @internal
       */
      _buffer = new HashMap_1.AttributeHashMap();
      _instrumentName;
      _valueType;
      constructor(instrumentName, valueType) {
        this._instrumentName = instrumentName;
        this._valueType = valueType;
      }
      /**
       * Observe a measurement of the value associated with the given attributes.
       */
      observe(value2, attributes = {}) {
        if (typeof value2 !== "number") {
          api_1.diag.warn(`non-number value provided to metric ${this._instrumentName}: ${value2}`);
          return;
        }
        if (this._valueType === api_1.ValueType.INT && !Number.isInteger(value2)) {
          api_1.diag.warn(`INT value type cannot accept a floating-point value for ${this._instrumentName}, ignoring the fractional digits.`);
          value2 = Math.trunc(value2);
          if (!Number.isInteger(value2)) {
            return;
          }
        }
        this._buffer.set(attributes, value2);
      }
    };
    exports.ObservableResultImpl = ObservableResultImpl;
    var BatchObservableResultImpl = class {
      static {
        __name(this, "BatchObservableResultImpl");
      }
      /**
       * @internal
       */
      _buffer = /* @__PURE__ */ new Map();
      /**
       * Observe a measurement of the value associated with the given attributes.
       */
      observe(metric, value2, attributes = {}) {
        if (!(0, Instruments_1.isObservableInstrument)(metric)) {
          return;
        }
        let map = this._buffer.get(metric);
        if (map == null) {
          map = new HashMap_1.AttributeHashMap();
          this._buffer.set(metric, map);
        }
        if (typeof value2 !== "number") {
          api_1.diag.warn(`non-number value provided to metric ${metric._descriptor.name}: ${value2}`);
          return;
        }
        if (metric._descriptor.valueType === api_1.ValueType.INT && !Number.isInteger(value2)) {
          api_1.diag.warn(`INT value type cannot accept a floating-point value for ${metric._descriptor.name}, ignoring the fractional digits.`);
          value2 = Math.trunc(value2);
          if (!Number.isInteger(value2)) {
            return;
          }
        }
        map.set(attributes, value2);
      }
    };
    exports.BatchObservableResultImpl = BatchObservableResultImpl;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/ObservableRegistry.js
var require_ObservableRegistry = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/ObservableRegistry.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ObservableRegistry = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var Instruments_1 = require_Instruments();
    var ObservableResult_1 = require_ObservableResult();
    var utils_1 = require_utils2();
    var ObservableRegistry = class {
      static {
        __name(this, "ObservableRegistry");
      }
      _callbacks = [];
      _batchCallbacks = [];
      addCallback(callback, instrument) {
        const idx = this._findCallback(callback, instrument);
        if (idx >= 0) {
          return;
        }
        this._callbacks.push({ callback, instrument });
      }
      removeCallback(callback, instrument) {
        const idx = this._findCallback(callback, instrument);
        if (idx < 0) {
          return;
        }
        this._callbacks.splice(idx, 1);
      }
      addBatchCallback(callback, instruments) {
        const observableInstruments = new Set(instruments.filter(Instruments_1.isObservableInstrument));
        if (observableInstruments.size === 0) {
          api_1.diag.error("BatchObservableCallback is not associated with valid instruments", instruments);
          return;
        }
        const idx = this._findBatchCallback(callback, observableInstruments);
        if (idx >= 0) {
          return;
        }
        this._batchCallbacks.push({ callback, instruments: observableInstruments });
      }
      removeBatchCallback(callback, instruments) {
        const observableInstruments = new Set(instruments.filter(Instruments_1.isObservableInstrument));
        const idx = this._findBatchCallback(callback, observableInstruments);
        if (idx < 0) {
          return;
        }
        this._batchCallbacks.splice(idx, 1);
      }
      /**
       * @returns a promise of rejected reasons for invoking callbacks.
       */
      async observe(collectionTime, timeoutMillis) {
        const callbackFutures = this._observeCallbacks(collectionTime, timeoutMillis);
        const batchCallbackFutures = this._observeBatchCallbacks(collectionTime, timeoutMillis);
        const results = await Promise.allSettled([
          ...callbackFutures,
          ...batchCallbackFutures
        ]);
        const rejections = results.filter((result) => result.status === "rejected").map((result) => result.reason);
        return rejections;
      }
      _observeCallbacks(observationTime, timeoutMillis) {
        return this._callbacks.map(async ({ callback, instrument }) => {
          const observableResult = new ObservableResult_1.ObservableResultImpl(instrument._descriptor.name, instrument._descriptor.valueType);
          let callPromise = Promise.resolve(callback(observableResult));
          if (timeoutMillis != null) {
            callPromise = (0, utils_1.callWithTimeout)(callPromise, timeoutMillis);
          }
          await callPromise;
          instrument._metricStorages.forEach((metricStorage) => {
            metricStorage.record(observableResult._buffer, observationTime);
          });
        });
      }
      _observeBatchCallbacks(observationTime, timeoutMillis) {
        return this._batchCallbacks.map(async ({ callback, instruments }) => {
          const observableResult = new ObservableResult_1.BatchObservableResultImpl();
          let callPromise = Promise.resolve(callback(observableResult));
          if (timeoutMillis != null) {
            callPromise = (0, utils_1.callWithTimeout)(callPromise, timeoutMillis);
          }
          await callPromise;
          instruments.forEach((instrument) => {
            const buffer = observableResult._buffer.get(instrument);
            if (buffer == null) {
              return;
            }
            instrument._metricStorages.forEach((metricStorage) => {
              metricStorage.record(buffer, observationTime);
            });
          });
        });
      }
      _findCallback(callback, instrument) {
        return this._callbacks.findIndex((record) => {
          return record.callback === callback && record.instrument === instrument;
        });
      }
      _findBatchCallback(callback, instruments) {
        return this._batchCallbacks.findIndex((record) => {
          return record.callback === callback && (0, utils_1.setEquals)(record.instruments, instruments);
        });
      }
    };
    exports.ObservableRegistry = ObservableRegistry;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/SyncMetricStorage.js
var require_SyncMetricStorage = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/SyncMetricStorage.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SyncMetricStorage = void 0;
    var MetricStorage_1 = require_MetricStorage();
    var DeltaMetricProcessor_1 = require_DeltaMetricProcessor();
    var TemporalMetricProcessor_1 = require_TemporalMetricProcessor();
    var SyncMetricStorage = class extends MetricStorage_1.MetricStorage {
      static {
        __name(this, "SyncMetricStorage");
      }
      _aggregationCardinalityLimit;
      _deltaMetricStorage;
      _temporalMetricStorage;
      _attributesProcessor;
      constructor(instrumentDescriptor, aggregator, attributesProcessor, collectorHandles, aggregationCardinalityLimit) {
        super(instrumentDescriptor);
        this._aggregationCardinalityLimit = aggregationCardinalityLimit;
        this._deltaMetricStorage = new DeltaMetricProcessor_1.DeltaMetricProcessor(aggregator, this._aggregationCardinalityLimit);
        this._temporalMetricStorage = new TemporalMetricProcessor_1.TemporalMetricProcessor(aggregator, collectorHandles);
        this._attributesProcessor = attributesProcessor;
      }
      record(value2, attributes, context2, recordTime) {
        attributes = this._attributesProcessor.process(attributes, context2);
        this._deltaMetricStorage.record(value2, attributes, context2, recordTime);
      }
      /**
       * Collects the metrics from this storage.
       *
       * Note: This is a stateful operation and may reset any interval-related
       * state for the MetricCollector.
       */
      collect(collector, collectionTime) {
        const accumulations = this._deltaMetricStorage.collect();
        return this._temporalMetricStorage.buildMetrics(collector, this._instrumentDescriptor, accumulations, collectionTime);
      }
    };
    exports.SyncMetricStorage = SyncMetricStorage;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/AttributesProcessor.js
var require_AttributesProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/AttributesProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createDenyListAttributesProcessor = exports.createAllowListAttributesProcessor = exports.createMultiAttributesProcessor = exports.createNoopAttributesProcessor = void 0;
    var NoopAttributesProcessor = class {
      static {
        __name(this, "NoopAttributesProcessor");
      }
      process(incoming, _context) {
        return incoming;
      }
    };
    var MultiAttributesProcessor = class {
      static {
        __name(this, "MultiAttributesProcessor");
      }
      _processors;
      constructor(processors) {
        this._processors = processors;
      }
      process(incoming, context2) {
        let filteredAttributes = incoming;
        for (const processor of this._processors) {
          filteredAttributes = processor.process(filteredAttributes, context2);
        }
        return filteredAttributes;
      }
    };
    var AllowListProcessor = class {
      static {
        __name(this, "AllowListProcessor");
      }
      _allowedAttributeNames;
      constructor(allowedAttributeNames) {
        this._allowedAttributeNames = new Set(allowedAttributeNames);
      }
      process(incoming, _context) {
        const filteredAttributes = {};
        for (const attributeName in incoming) {
          if (Object.prototype.hasOwnProperty.call(incoming, attributeName) && this._allowedAttributeNames.has(attributeName)) {
            filteredAttributes[attributeName] = incoming[attributeName];
          }
        }
        return filteredAttributes;
      }
    };
    var DenyListProcessor = class {
      static {
        __name(this, "DenyListProcessor");
      }
      _deniedAttributeNames;
      constructor(deniedAttributeNames) {
        this._deniedAttributeNames = new Set(deniedAttributeNames);
      }
      process(incoming, _context) {
        const filteredAttributes = {};
        for (const attributeName in incoming) {
          if (Object.prototype.hasOwnProperty.call(incoming, attributeName) && !this._deniedAttributeNames.has(attributeName)) {
            filteredAttributes[attributeName] = incoming[attributeName];
          }
        }
        return filteredAttributes;
      }
    };
    function createNoopAttributesProcessor() {
      return NOOP;
    }
    __name(createNoopAttributesProcessor, "createNoopAttributesProcessor");
    exports.createNoopAttributesProcessor = createNoopAttributesProcessor;
    function createMultiAttributesProcessor(processors) {
      return new MultiAttributesProcessor(processors);
    }
    __name(createMultiAttributesProcessor, "createMultiAttributesProcessor");
    exports.createMultiAttributesProcessor = createMultiAttributesProcessor;
    function createAllowListAttributesProcessor(attributeAllowList) {
      return new AllowListProcessor(attributeAllowList);
    }
    __name(createAllowListAttributesProcessor, "createAllowListAttributesProcessor");
    exports.createAllowListAttributesProcessor = createAllowListAttributesProcessor;
    function createDenyListAttributesProcessor(attributeDenyList) {
      return new DenyListProcessor(attributeDenyList);
    }
    __name(createDenyListAttributesProcessor, "createDenyListAttributesProcessor");
    exports.createDenyListAttributesProcessor = createDenyListAttributesProcessor;
    var NOOP = new NoopAttributesProcessor();
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MeterSharedState.js
var require_MeterSharedState = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MeterSharedState.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MeterSharedState = void 0;
    var InstrumentDescriptor_1 = require_InstrumentDescriptor();
    var Meter_1 = require_Meter();
    var AsyncMetricStorage_1 = require_AsyncMetricStorage();
    var MetricStorageRegistry_1 = require_MetricStorageRegistry();
    var MultiWritableMetricStorage_1 = require_MultiWritableMetricStorage();
    var ObservableRegistry_1 = require_ObservableRegistry();
    var SyncMetricStorage_1 = require_SyncMetricStorage();
    var AttributesProcessor_1 = require_AttributesProcessor();
    var MeterSharedState = class {
      static {
        __name(this, "MeterSharedState");
      }
      metricStorageRegistry = new MetricStorageRegistry_1.MetricStorageRegistry();
      observableRegistry = new ObservableRegistry_1.ObservableRegistry();
      meter;
      _meterProviderSharedState;
      _instrumentationScope;
      constructor(meterProviderSharedState, instrumentationScope) {
        this.meter = new Meter_1.Meter(this);
        this._meterProviderSharedState = meterProviderSharedState;
        this._instrumentationScope = instrumentationScope;
      }
      registerMetricStorage(descriptor) {
        const storages = this._registerMetricStorage(descriptor, SyncMetricStorage_1.SyncMetricStorage);
        if (storages.length === 1) {
          return storages[0];
        }
        return new MultiWritableMetricStorage_1.MultiMetricStorage(storages);
      }
      registerAsyncMetricStorage(descriptor) {
        const storages = this._registerMetricStorage(descriptor, AsyncMetricStorage_1.AsyncMetricStorage);
        return storages;
      }
      /**
       * @param collector opaque handle of {@link MetricCollector} which initiated the collection.
       * @param collectionTime the HrTime at which the collection was initiated.
       * @param options options for collection.
       * @returns the list of metric data collected.
       */
      async collect(collector, collectionTime, options) {
        const errors = await this.observableRegistry.observe(collectionTime, options?.timeoutMillis);
        const storages = this.metricStorageRegistry.getStorages(collector);
        if (storages.length === 0) {
          return null;
        }
        const metricDataList = [];
        storages.forEach((metricStorage) => {
          const metricData = metricStorage.collect(collector, collectionTime);
          if (metricData != null) {
            metricDataList.push(metricData);
          }
        });
        if (metricDataList.length === 0) {
          return { errors };
        }
        return {
          scopeMetrics: {
            scope: this._instrumentationScope,
            metrics: metricDataList
          },
          errors
        };
      }
      _registerMetricStorage(descriptor, MetricStorageType) {
        const views = this._meterProviderSharedState.viewRegistry.findViews(descriptor, this._instrumentationScope);
        let storages = views.map((view) => {
          const viewDescriptor = (0, InstrumentDescriptor_1.createInstrumentDescriptorWithView)(view, descriptor);
          const compatibleStorage = this.metricStorageRegistry.findOrUpdateCompatibleStorage(viewDescriptor);
          if (compatibleStorage != null) {
            return compatibleStorage;
          }
          const aggregator = view.aggregation.createAggregator(viewDescriptor);
          const viewStorage = new MetricStorageType(viewDescriptor, aggregator, view.attributesProcessor, this._meterProviderSharedState.metricCollectors, view.aggregationCardinalityLimit);
          this.metricStorageRegistry.register(viewStorage);
          return viewStorage;
        });
        if (storages.length === 0) {
          const perCollectorAggregations = this._meterProviderSharedState.selectAggregations(descriptor.type);
          const collectorStorages = perCollectorAggregations.map(([collector, aggregation]) => {
            const compatibleStorage = this.metricStorageRegistry.findOrUpdateCompatibleCollectorStorage(collector, descriptor);
            if (compatibleStorage != null) {
              return compatibleStorage;
            }
            const aggregator = aggregation.createAggregator(descriptor);
            const cardinalityLimit = collector.selectCardinalityLimit(descriptor.type);
            const storage = new MetricStorageType(descriptor, aggregator, (0, AttributesProcessor_1.createNoopAttributesProcessor)(), [collector], cardinalityLimit);
            this.metricStorageRegistry.registerForCollector(collector, storage);
            return storage;
          });
          storages = storages.concat(collectorStorages);
        }
        return storages;
      }
    };
    exports.MeterSharedState = MeterSharedState;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MeterProviderSharedState.js
var require_MeterProviderSharedState = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MeterProviderSharedState.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MeterProviderSharedState = void 0;
    var utils_1 = require_utils2();
    var ViewRegistry_1 = require_ViewRegistry();
    var MeterSharedState_1 = require_MeterSharedState();
    var AggregationOption_1 = require_AggregationOption();
    var MeterProviderSharedState = class {
      static {
        __name(this, "MeterProviderSharedState");
      }
      viewRegistry = new ViewRegistry_1.ViewRegistry();
      metricCollectors = [];
      meterSharedStates = /* @__PURE__ */ new Map();
      resource;
      constructor(resource) {
        this.resource = resource;
      }
      getMeterSharedState(instrumentationScope) {
        const id = (0, utils_1.instrumentationScopeId)(instrumentationScope);
        let meterSharedState = this.meterSharedStates.get(id);
        if (meterSharedState == null) {
          meterSharedState = new MeterSharedState_1.MeterSharedState(this, instrumentationScope);
          this.meterSharedStates.set(id, meterSharedState);
        }
        return meterSharedState;
      }
      selectAggregations(instrumentType) {
        const result = [];
        for (const collector of this.metricCollectors) {
          result.push([
            collector,
            (0, AggregationOption_1.toAggregation)(collector.selectAggregation(instrumentType))
          ]);
        }
        return result;
      }
    };
    exports.MeterProviderSharedState = MeterProviderSharedState;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MetricCollector.js
var require_MetricCollector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/state/MetricCollector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MetricCollector = void 0;
    var core_1 = require_src();
    var MetricCollector = class {
      static {
        __name(this, "MetricCollector");
      }
      _sharedState;
      _metricReader;
      constructor(sharedState, metricReader) {
        this._sharedState = sharedState;
        this._metricReader = metricReader;
      }
      async collect(options) {
        const collectionTime = (0, core_1.millisToHrTime)(Date.now());
        const scopeMetrics = [];
        const errors = [];
        const meterCollectionPromises = Array.from(this._sharedState.meterSharedStates.values()).map(async (meterSharedState) => {
          const current = await meterSharedState.collect(this, collectionTime, options);
          if (current?.scopeMetrics != null) {
            scopeMetrics.push(current.scopeMetrics);
          }
          if (current?.errors != null) {
            errors.push(...current.errors);
          }
        });
        await Promise.all(meterCollectionPromises);
        return {
          resourceMetrics: {
            resource: this._sharedState.resource,
            scopeMetrics
          },
          errors
        };
      }
      /**
       * Delegates for MetricReader.forceFlush.
       */
      async forceFlush(options) {
        await this._metricReader.forceFlush(options);
      }
      /**
       * Delegates for MetricReader.shutdown.
       */
      async shutdown(options) {
        await this._metricReader.shutdown(options);
      }
      selectAggregationTemporality(instrumentType) {
        return this._metricReader.selectAggregationTemporality(instrumentType);
      }
      selectAggregation(instrumentType) {
        return this._metricReader.selectAggregation(instrumentType);
      }
      /**
       * Select the cardinality limit for the given {@link InstrumentType} for this
       * collector.
       */
      selectCardinalityLimit(instrumentType) {
        return this._metricReader.selectCardinalityLimit?.(instrumentType) ?? 2e3;
      }
    };
    exports.MetricCollector = MetricCollector;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/Predicate.js
var require_Predicate = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/Predicate.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ExactPredicate = exports.PatternPredicate = void 0;
    var ESCAPE = /[\^$\\.+?()[\]{}|]/g;
    var PatternPredicate = class _PatternPredicate {
      static {
        __name(this, "PatternPredicate");
      }
      _matchAll;
      _regexp;
      constructor(pattern) {
        if (pattern === "*") {
          this._matchAll = true;
          this._regexp = /.*/;
        } else {
          this._matchAll = false;
          this._regexp = new RegExp(_PatternPredicate.escapePattern(pattern));
        }
      }
      match(str) {
        if (this._matchAll) {
          return true;
        }
        return this._regexp.test(str);
      }
      static escapePattern(pattern) {
        return `^${pattern.replace(ESCAPE, "\\$&").replace("*", ".*")}$`;
      }
      static hasWildcard(pattern) {
        return pattern.includes("*");
      }
    };
    exports.PatternPredicate = PatternPredicate;
    var ExactPredicate = class {
      static {
        __name(this, "ExactPredicate");
      }
      _matchAll;
      _pattern;
      constructor(pattern) {
        this._matchAll = pattern === void 0;
        this._pattern = pattern;
      }
      match(str) {
        if (this._matchAll) {
          return true;
        }
        if (str === this._pattern) {
          return true;
        }
        return false;
      }
    };
    exports.ExactPredicate = ExactPredicate;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/InstrumentSelector.js
var require_InstrumentSelector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/InstrumentSelector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InstrumentSelector = void 0;
    var Predicate_1 = require_Predicate();
    var InstrumentSelector = class {
      static {
        __name(this, "InstrumentSelector");
      }
      _nameFilter;
      _type;
      _unitFilter;
      constructor(criteria) {
        this._nameFilter = new Predicate_1.PatternPredicate(criteria?.name ?? "*");
        this._type = criteria?.type;
        this._unitFilter = new Predicate_1.ExactPredicate(criteria?.unit);
      }
      getType() {
        return this._type;
      }
      getNameFilter() {
        return this._nameFilter;
      }
      getUnitFilter() {
        return this._unitFilter;
      }
    };
    exports.InstrumentSelector = InstrumentSelector;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/MeterSelector.js
var require_MeterSelector = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/MeterSelector.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MeterSelector = void 0;
    var Predicate_1 = require_Predicate();
    var MeterSelector = class {
      static {
        __name(this, "MeterSelector");
      }
      _nameFilter;
      _versionFilter;
      _schemaUrlFilter;
      constructor(criteria) {
        this._nameFilter = new Predicate_1.ExactPredicate(criteria?.name);
        this._versionFilter = new Predicate_1.ExactPredicate(criteria?.version);
        this._schemaUrlFilter = new Predicate_1.ExactPredicate(criteria?.schemaUrl);
      }
      getNameFilter() {
        return this._nameFilter;
      }
      /**
       * TODO: semver filter? no spec yet.
       */
      getVersionFilter() {
        return this._versionFilter;
      }
      getSchemaUrlFilter() {
        return this._schemaUrlFilter;
      }
    };
    exports.MeterSelector = MeterSelector;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/View.js
var require_View = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/view/View.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.View = void 0;
    var Predicate_1 = require_Predicate();
    var AttributesProcessor_1 = require_AttributesProcessor();
    var InstrumentSelector_1 = require_InstrumentSelector();
    var MeterSelector_1 = require_MeterSelector();
    var AggregationOption_1 = require_AggregationOption();
    function isSelectorNotProvided(options) {
      return options.instrumentName == null && options.instrumentType == null && options.instrumentUnit == null && options.meterName == null && options.meterVersion == null && options.meterSchemaUrl == null;
    }
    __name(isSelectorNotProvided, "isSelectorNotProvided");
    function validateViewOptions(viewOptions) {
      if (isSelectorNotProvided(viewOptions)) {
        throw new Error("Cannot create view with no selector arguments supplied");
      }
      if (viewOptions.name != null && (viewOptions?.instrumentName == null || Predicate_1.PatternPredicate.hasWildcard(viewOptions.instrumentName))) {
        throw new Error("Views with a specified name must be declared with an instrument selector that selects at most one instrument per meter.");
      }
    }
    __name(validateViewOptions, "validateViewOptions");
    var View = class {
      static {
        __name(this, "View");
      }
      name;
      description;
      aggregation;
      attributesProcessor;
      instrumentSelector;
      meterSelector;
      aggregationCardinalityLimit;
      /**
       * Create a new {@link View} instance.
       *
       * Parameters can be categorized as two types:
       *  Instrument selection criteria: Used to describe the instrument(s) this view will be applied to.
       *  Will be treated as additive (the Instrument has to meet all the provided criteria to be selected).
       *
       *  Metric stream altering: Alter the metric stream of instruments selected by instrument selection criteria.
       *
       * @param viewOptions {@link ViewOptions} for altering the metric stream and instrument selection.
       * @param viewOptions.name
       * Alters the metric stream:
       *  This will be used as the name of the metrics stream.
       *  If not provided, the original Instrument name will be used.
       * @param viewOptions.description
       * Alters the metric stream:
       *  This will be used as the description of the metrics stream.
       *  If not provided, the original Instrument description will be used by default.
       * @param viewOptions.attributesProcessors
       * Alters the metric stream:
       *  If provided, the attributes will be modified as defined by the added processors.
       *  If not provided, all attribute keys will be used by default.
       * @param viewOptions.aggregationCardinalityLimit
       * Alters the metric stream:
       *  Sets a limit on the number of unique attribute combinations (cardinality) that can be aggregated.
       *  If not provided, the default limit of 2000 will be used.
       * @param viewOptions.aggregation
       * Alters the metric stream:
       *  Alters the {@link Aggregation} of the metric stream.
       * @param viewOptions.instrumentName
       * Instrument selection criteria:
       *  Original name of the Instrument(s) with wildcard support.
       * @param viewOptions.instrumentType
       * Instrument selection criteria:
       *  The original type of the Instrument(s).
       * @param viewOptions.instrumentUnit
       * Instrument selection criteria:
       *  The unit of the Instrument(s).
       * @param viewOptions.meterName
       * Instrument selection criteria:
       *  The name of the Meter. No wildcard support, name must match the meter exactly.
       * @param viewOptions.meterVersion
       * Instrument selection criteria:
       *  The version of the Meter. No wildcard support, version must match exactly.
       * @param viewOptions.meterSchemaUrl
       * Instrument selection criteria:
       *  The schema URL of the Meter. No wildcard support, schema URL must match exactly.
       *
       * @example
       * // Create a view that changes the Instrument 'my.instrument' to use to an
       * // ExplicitBucketHistogramAggregation with the boundaries [20, 30, 40]
       * new View({
       *   aggregation: new ExplicitBucketHistogramAggregation([20, 30, 40]),
       *   instrumentName: 'my.instrument'
       * })
       */
      constructor(viewOptions) {
        validateViewOptions(viewOptions);
        if (viewOptions.attributesProcessors != null) {
          this.attributesProcessor = (0, AttributesProcessor_1.createMultiAttributesProcessor)(viewOptions.attributesProcessors);
        } else {
          this.attributesProcessor = (0, AttributesProcessor_1.createNoopAttributesProcessor)();
        }
        this.name = viewOptions.name;
        this.description = viewOptions.description;
        this.aggregation = (0, AggregationOption_1.toAggregation)(viewOptions.aggregation ?? { type: AggregationOption_1.AggregationType.DEFAULT });
        this.instrumentSelector = new InstrumentSelector_1.InstrumentSelector({
          name: viewOptions.instrumentName,
          type: viewOptions.instrumentType,
          unit: viewOptions.instrumentUnit
        });
        this.meterSelector = new MeterSelector_1.MeterSelector({
          name: viewOptions.meterName,
          version: viewOptions.meterVersion,
          schemaUrl: viewOptions.meterSchemaUrl
        });
        this.aggregationCardinalityLimit = viewOptions.aggregationCardinalityLimit;
      }
    };
    exports.View = View;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/MeterProvider.js
var require_MeterProvider = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/MeterProvider.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MeterProvider = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var resources_1 = require_src5();
    var MeterProviderSharedState_1 = require_MeterProviderSharedState();
    var MetricCollector_1 = require_MetricCollector();
    var View_1 = require_View();
    var MeterProvider2 = class {
      static {
        __name(this, "MeterProvider");
      }
      _sharedState;
      _shutdown = false;
      constructor(options) {
        this._sharedState = new MeterProviderSharedState_1.MeterProviderSharedState(options?.resource ?? (0, resources_1.defaultResource)());
        if (options?.views != null && options.views.length > 0) {
          for (const viewOption of options.views) {
            this._sharedState.viewRegistry.addView(new View_1.View(viewOption));
          }
        }
        if (options?.readers != null && options.readers.length > 0) {
          for (const metricReader of options.readers) {
            const collector = new MetricCollector_1.MetricCollector(this._sharedState, metricReader);
            metricReader.setMetricProducer(collector);
            this._sharedState.metricCollectors.push(collector);
          }
        }
      }
      /**
       * Get a meter with the configuration of the MeterProvider.
       */
      getMeter(name, version = "", options = {}) {
        if (this._shutdown) {
          api_1.diag.warn("A shutdown MeterProvider cannot provide a Meter");
          return (0, api_1.createNoopMeter)();
        }
        return this._sharedState.getMeterSharedState({
          name,
          version,
          schemaUrl: options.schemaUrl
        }).meter;
      }
      /**
       * Shut down the MeterProvider and all registered
       * MetricReaders.
       *
       * Returns a promise which is resolved when all flushes are complete.
       */
      async shutdown(options) {
        if (this._shutdown) {
          api_1.diag.warn("shutdown may only be called once per MeterProvider");
          return;
        }
        this._shutdown = true;
        await Promise.all(this._sharedState.metricCollectors.map((collector) => {
          return collector.shutdown(options);
        }));
      }
      /**
       * Notifies all registered MetricReaders to flush any buffered data.
       *
       * Returns a promise which is resolved when all flushes are complete.
       */
      async forceFlush(options) {
        if (this._shutdown) {
          api_1.diag.warn("invalid attempt to force flush after MeterProvider shutdown");
          return;
        }
        await Promise.all(this._sharedState.metricCollectors.map((collector) => {
          return collector.forceFlush(options);
        }));
      }
    };
    exports.MeterProvider = MeterProvider2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/index.js
var require_src6 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-metrics@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-metrics/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TimeoutError = exports.createDenyListAttributesProcessor = exports.createAllowListAttributesProcessor = exports.AggregationType = exports.MeterProvider = exports.ConsoleMetricExporter = exports.InMemoryMetricExporter = exports.PeriodicExportingMetricReader = exports.MetricReader = exports.InstrumentType = exports.DataPointType = exports.AggregationTemporality = void 0;
    var AggregationTemporality_1 = require_AggregationTemporality();
    Object.defineProperty(exports, "AggregationTemporality", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AggregationTemporality_1.AggregationTemporality;
    }, "get") });
    var MetricData_1 = require_MetricData();
    Object.defineProperty(exports, "DataPointType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return MetricData_1.DataPointType;
    }, "get") });
    Object.defineProperty(exports, "InstrumentType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return MetricData_1.InstrumentType;
    }, "get") });
    var MetricReader_1 = require_MetricReader();
    Object.defineProperty(exports, "MetricReader", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return MetricReader_1.MetricReader;
    }, "get") });
    var PeriodicExportingMetricReader_1 = require_PeriodicExportingMetricReader();
    Object.defineProperty(exports, "PeriodicExportingMetricReader", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return PeriodicExportingMetricReader_1.PeriodicExportingMetricReader;
    }, "get") });
    var InMemoryMetricExporter_1 = require_InMemoryMetricExporter();
    Object.defineProperty(exports, "InMemoryMetricExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return InMemoryMetricExporter_1.InMemoryMetricExporter;
    }, "get") });
    var ConsoleMetricExporter_1 = require_ConsoleMetricExporter();
    Object.defineProperty(exports, "ConsoleMetricExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ConsoleMetricExporter_1.ConsoleMetricExporter;
    }, "get") });
    var MeterProvider_1 = require_MeterProvider();
    Object.defineProperty(exports, "MeterProvider", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return MeterProvider_1.MeterProvider;
    }, "get") });
    var AggregationOption_1 = require_AggregationOption();
    Object.defineProperty(exports, "AggregationType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AggregationOption_1.AggregationType;
    }, "get") });
    var AttributesProcessor_1 = require_AttributesProcessor();
    Object.defineProperty(exports, "createAllowListAttributesProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AttributesProcessor_1.createAllowListAttributesProcessor;
    }, "get") });
    Object.defineProperty(exports, "createDenyListAttributesProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AttributesProcessor_1.createDenyListAttributesProcessor;
    }, "get") });
    var utils_1 = require_utils2();
    Object.defineProperty(exports, "TimeoutError", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return utils_1.TimeoutError;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/metrics-serializer.js
var require_metrics_serializer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/metrics-serializer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serializeMetricsExportRequest = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var sdk_metrics_1 = require_src6();
    var common_serializer_1 = require_common_serializer();
    var protobuf_size_estimator_1 = require_protobuf_size_estimator();
    var protobuf_writer_1 = require_protobuf_writer();
    function serializeNumberDataPoint(writer, dataPoint, valueType) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      writer.writeTag(2, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, dataPoint.startTime);
      writer.writeTag(3, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, dataPoint.endTime);
      if (valueType === api_1.ValueType.INT) {
        writer.writeTag(6, 1);
        writer.writeSfixed64(dataPoint.value);
      } else {
        writer.writeTag(4, 1);
        writer.writeDouble(dataPoint.value);
      }
      if (dataPoint.attributes) {
        (0, common_serializer_1.writeAttributes)(writer, dataPoint.attributes, 7);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeNumberDataPoint, "serializeNumberDataPoint");
    function serializeHistogramDataPoint(writer, dataPoint) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      const histogram = dataPoint.value;
      writer.writeTag(2, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, dataPoint.startTime);
      writer.writeTag(3, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, dataPoint.endTime);
      writer.writeTag(4, 1);
      writer.writeFixed64(histogram.count >>> 0, histogram.count / 4294967296 >>> 0);
      if (histogram.sum !== void 0) {
        writer.writeTag(5, 1);
        writer.writeDouble(histogram.sum);
      }
      if (histogram.buckets.counts.length > 0) {
        writer.writeTag(6, 2);
        const countsStart = writer.startLengthDelimited();
        const countsStartPos = writer.pos;
        for (const count of histogram.buckets.counts) {
          writer.writeFixed64(count >>> 0, count / 4294967296 >>> 0);
        }
        writer.finishLengthDelimited(countsStart, writer.pos - countsStartPos);
      }
      if (histogram.buckets.boundaries.length > 0) {
        writer.writeTag(7, 2);
        const boundsStart = writer.startLengthDelimited();
        const boundsStartPos = writer.pos;
        for (const bound of histogram.buckets.boundaries) {
          writer.writeDouble(bound);
        }
        writer.finishLengthDelimited(boundsStart, writer.pos - boundsStartPos);
      }
      if (dataPoint.attributes) {
        (0, common_serializer_1.writeAttributes)(writer, dataPoint.attributes, 9);
      }
      if (histogram.min !== void 0) {
        writer.writeTag(11, 1);
        writer.writeDouble(histogram.min);
      }
      if (histogram.max !== void 0) {
        writer.writeTag(12, 1);
        writer.writeDouble(histogram.max);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeHistogramDataPoint, "serializeHistogramDataPoint");
    function serializeExponentialBuckets(writer, offset, bucketCounts) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      if (offset !== 0) {
        writer.writeTag(1, 0);
        writer.writeSint32(offset);
      }
      if (bucketCounts.length > 0) {
        writer.writeTag(2, 2);
        const bcStart = writer.startLengthDelimited();
        const bcStartPos = writer.pos;
        for (const count of bucketCounts) {
          writer.writeVarint(count);
        }
        writer.finishLengthDelimited(bcStart, writer.pos - bcStartPos);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeExponentialBuckets, "serializeExponentialBuckets");
    function serializeExponentialHistogramDataPoint(writer, dataPoint) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      const histogram = dataPoint.value;
      if (dataPoint.attributes) {
        (0, common_serializer_1.writeAttributes)(writer, dataPoint.attributes, 1);
      }
      writer.writeTag(2, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, dataPoint.startTime);
      writer.writeTag(3, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, dataPoint.endTime);
      writer.writeTag(4, 1);
      writer.writeFixed64(histogram.count >>> 0, histogram.count / 4294967296 >>> 0);
      if (histogram.sum !== void 0) {
        writer.writeTag(5, 1);
        writer.writeDouble(histogram.sum);
      }
      if (histogram.scale !== 0) {
        writer.writeTag(6, 0);
        writer.writeSint32(histogram.scale);
      }
      writer.writeTag(7, 1);
      writer.writeFixed64(histogram.zeroCount >>> 0, histogram.zeroCount / 4294967296 >>> 0);
      writer.writeTag(8, 2);
      serializeExponentialBuckets(writer, histogram.positive.offset, histogram.positive.bucketCounts);
      writer.writeTag(9, 2);
      serializeExponentialBuckets(writer, histogram.negative.offset, histogram.negative.bucketCounts);
      if (histogram.min !== void 0) {
        writer.writeTag(12, 1);
        writer.writeDouble(histogram.min);
      }
      if (histogram.max !== void 0) {
        writer.writeTag(13, 1);
        writer.writeDouble(histogram.max);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeExponentialHistogramDataPoint, "serializeExponentialHistogramDataPoint");
    function serializeMetric(writer, metricData) {
      const metricStart = writer.startLengthDelimited();
      const metricStartPos = writer.pos;
      writer.writeTag(1, 2);
      writer.writeString(metricData.descriptor.name);
      if (metricData.descriptor.description) {
        writer.writeTag(2, 2);
        writer.writeString(metricData.descriptor.description);
      }
      if (metricData.descriptor.unit) {
        writer.writeTag(3, 2);
        writer.writeString(metricData.descriptor.unit);
      }
      switch (metricData.dataPointType) {
        case sdk_metrics_1.DataPointType.GAUGE:
          writer.writeTag(5, 2);
          serializeGauge(writer, metricData);
          break;
        case sdk_metrics_1.DataPointType.SUM:
          writer.writeTag(7, 2);
          serializeSum(writer, metricData);
          break;
        case sdk_metrics_1.DataPointType.HISTOGRAM:
          writer.writeTag(9, 2);
          serializeHistogramMetric(writer, metricData);
          break;
        case sdk_metrics_1.DataPointType.EXPONENTIAL_HISTOGRAM:
          writer.writeTag(10, 2);
          serializeExponentialHistogramMetric(writer, metricData);
          break;
        default: {
          const _exhaustive = metricData;
        }
      }
      writer.finishLengthDelimited(metricStart, writer.pos - metricStartPos);
    }
    __name(serializeMetric, "serializeMetric");
    function serializeGauge(writer, metricData) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      for (const dataPoint of metricData.dataPoints) {
        writer.writeTag(1, 2);
        serializeNumberDataPoint(writer, dataPoint, metricData.descriptor.valueType);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeGauge, "serializeGauge");
    function serializeSum(writer, metricData) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      for (const dataPoint of metricData.dataPoints) {
        writer.writeTag(1, 2);
        serializeNumberDataPoint(writer, dataPoint, metricData.descriptor.valueType);
      }
      const temporality = toProtoAggregationTemporality(metricData.aggregationTemporality);
      if (temporality !== 0) {
        writer.writeTag(2, 0);
        writer.writeVarint(temporality);
      }
      if (metricData.isMonotonic) {
        writer.writeTag(3, 0);
        writer.writeVarint(1);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeSum, "serializeSum");
    function serializeHistogramMetric(writer, metricData) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      for (const dataPoint of metricData.dataPoints) {
        writer.writeTag(1, 2);
        serializeHistogramDataPoint(writer, dataPoint);
      }
      const temporality = toProtoAggregationTemporality(metricData.aggregationTemporality);
      if (temporality !== 0) {
        writer.writeTag(2, 0);
        writer.writeVarint(temporality);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeHistogramMetric, "serializeHistogramMetric");
    function serializeExponentialHistogramMetric(writer, metricData) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      for (const dataPoint of metricData.dataPoints) {
        writer.writeTag(1, 2);
        serializeExponentialHistogramDataPoint(writer, dataPoint);
      }
      const temporality = toProtoAggregationTemporality(metricData.aggregationTemporality);
      if (temporality !== 0) {
        writer.writeTag(2, 0);
        writer.writeVarint(temporality);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeExponentialHistogramMetric, "serializeExponentialHistogramMetric");
    function serializeScopeMetrics(writer, scopeMetrics) {
      const scopeStart = writer.startLengthDelimited();
      const scopeStartPos = writer.pos;
      (0, common_serializer_1.writeInstrumentationScope)(writer, scopeMetrics.scope, 1);
      for (const metric of scopeMetrics.metrics) {
        writer.writeTag(2, 2);
        serializeMetric(writer, metric);
      }
      if (scopeMetrics.scope.schemaUrl) {
        writer.writeTag(3, 2);
        writer.writeString(scopeMetrics.scope.schemaUrl);
      }
      writer.finishLengthDelimited(scopeStart, writer.pos - scopeStartPos);
    }
    __name(serializeScopeMetrics, "serializeScopeMetrics");
    function serializeResourceMetrics(writer, resourceMetrics) {
      const start = writer.startLengthDelimited();
      const startPos = writer.pos;
      (0, common_serializer_1.writeResource)(writer, resourceMetrics.resource, 1);
      for (const scopeMetrics of resourceMetrics.scopeMetrics) {
        writer.writeTag(2, 2);
        serializeScopeMetrics(writer, scopeMetrics);
      }
      if (resourceMetrics.resource.schemaUrl) {
        writer.writeTag(3, 2);
        writer.writeString(resourceMetrics.resource.schemaUrl);
      }
      writer.finishLengthDelimited(start, writer.pos - startPos);
    }
    __name(serializeResourceMetrics, "serializeResourceMetrics");
    function toProtoAggregationTemporality(temporality) {
      switch (temporality) {
        case sdk_metrics_1.AggregationTemporality.DELTA:
          return 1;
        case sdk_metrics_1.AggregationTemporality.CUMULATIVE:
          return 2;
        default:
          return 0;
      }
    }
    __name(toProtoAggregationTemporality, "toProtoAggregationTemporality");
    function serializeMetricsExportRequest(resourceMetrics) {
      const estimator = new protobuf_size_estimator_1.ProtobufSizeEstimator();
      estimator.writeTag(1, 2);
      serializeResourceMetrics(estimator, resourceMetrics);
      const writer = new protobuf_writer_1.ProtobufWriter(estimator.pos);
      writer.writeTag(1, 2);
      serializeResourceMetrics(writer, resourceMetrics);
      return writer.finish();
    }
    __name(serializeMetricsExportRequest, "serializeMetricsExportRequest");
    exports.serializeMetricsExportRequest = serializeMetricsExportRequest;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/response-deserializer.js
var require_response_deserializer2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/response-deserializer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.deserializeExportMetricsServiceResponse = void 0;
    var protobuf_reader_1 = require_protobuf_reader();
    function deserializePartialSuccess(data) {
      const reader = new protobuf_reader_1.ProtobufReader(data);
      const result = {};
      while (!reader.isAtEnd()) {
        const { fieldNumber, wireType } = reader.readTag();
        switch (fieldNumber) {
          case 1:
            if (wireType === 0) {
              result.rejectedDataPoints = reader.readVarint();
            } else {
              reader.skip(wireType);
            }
            break;
          case 2:
            if (wireType === 2) {
              result.errorMessage = reader.readString();
            } else {
              reader.skip(wireType);
            }
            break;
          default:
            reader.skip(wireType);
            break;
        }
      }
      return result;
    }
    __name(deserializePartialSuccess, "deserializePartialSuccess");
    function deserializeExportMetricsServiceResponse(data) {
      const reader = new protobuf_reader_1.ProtobufReader(data);
      const result = {};
      while (!reader.isAtEnd()) {
        const { fieldNumber, wireType } = reader.readTag();
        switch (fieldNumber) {
          case 1:
            if (wireType === 2) {
              result.partialSuccess = deserializePartialSuccess(reader.readBytes());
            } else {
              reader.skip(wireType);
            }
            break;
          default:
            reader.skip(wireType);
            break;
        }
      }
      return result;
    }
    __name(deserializeExportMetricsServiceResponse, "deserializeExportMetricsServiceResponse");
    exports.deserializeExportMetricsServiceResponse = deserializeExportMetricsServiceResponse;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/metrics.js
var require_metrics = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/metrics.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufMetricsSerializer = void 0;
    var metrics_serializer_1 = require_metrics_serializer();
    var response_deserializer_1 = require_response_deserializer2();
    exports.ProtobufMetricsSerializer = {
      serializeRequest: /* @__PURE__ */ __name((arg) => {
        return (0, metrics_serializer_1.serializeMetricsExportRequest)(arg);
      }, "serializeRequest"),
      deserializeResponse: /* @__PURE__ */ __name((arg) => {
        return (0, response_deserializer_1.deserializeExportMetricsServiceResponse)(arg);
      }, "deserializeResponse")
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/index.js
var require_protobuf2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/protobuf/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufMetricsSerializer = void 0;
    var metrics_1 = require_metrics();
    Object.defineProperty(exports, "ProtobufMetricsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return metrics_1.ProtobufMetricsSerializer;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/trace-serializer.js
var require_trace_serializer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/trace-serializer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serializeTraceExportRequest = void 0;
    var protobuf_writer_1 = require_protobuf_writer();
    var hex_to_binary_1 = require_hex_to_binary();
    var common_serializer_1 = require_common_serializer();
    var protobuf_size_estimator_1 = require_protobuf_size_estimator();
    var SPAN_FLAGS_CONTEXT_HAS_IS_REMOTE_MASK = 256;
    var SPAN_FLAGS_CONTEXT_IS_REMOTE_MASK = 512;
    function buildSpanFlags(traceFlags, isRemote) {
      let flags = traceFlags & 255 | SPAN_FLAGS_CONTEXT_HAS_IS_REMOTE_MASK;
      if (isRemote) {
        flags |= SPAN_FLAGS_CONTEXT_IS_REMOTE_MASK;
      }
      return flags;
    }
    __name(buildSpanFlags, "buildSpanFlags");
    function serializeStatus(writer, status) {
      const statusStart = writer.startLengthDelimited();
      const statusStartPos = writer.pos;
      if (status.message) {
        writer.writeTag(2, 2);
        writer.writeString(status.message);
      }
      writer.writeTag(3, 0);
      writer.writeVarint(status.code);
      writer.finishLengthDelimited(statusStart, writer.pos - statusStartPos);
    }
    __name(serializeStatus, "serializeStatus");
    function serializeEvent(writer, event) {
      const eventStart = writer.startLengthDelimited();
      const eventStartPos = writer.pos;
      writer.writeTag(1, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, event.time);
      writer.writeTag(2, 2);
      writer.writeString(event.name);
      if (event.attributes) {
        (0, common_serializer_1.writeAttributes)(writer, event.attributes, 3);
      }
      writer.writeTag(4, 0);
      writer.writeVarint(event.droppedAttributesCount || 0);
      writer.finishLengthDelimited(eventStart, writer.pos - eventStartPos);
    }
    __name(serializeEvent, "serializeEvent");
    function serializeLink(writer, link) {
      const linkStart = writer.startLengthDelimited();
      const linkStartPos = writer.pos;
      const context2 = link.context;
      writer.writeTag(1, 2);
      writer.writeBytes((0, hex_to_binary_1.hexToBinary)(context2.traceId));
      writer.writeTag(2, 2);
      writer.writeBytes((0, hex_to_binary_1.hexToBinary)(context2.spanId));
      const linkTraceState = context2.traceState?.serialize();
      if (linkTraceState) {
        writer.writeTag(3, 2);
        writer.writeString(linkTraceState);
      }
      if (link.attributes) {
        (0, common_serializer_1.writeAttributes)(writer, link.attributes, 4);
      }
      writer.writeTag(5, 0);
      writer.writeVarint(link.droppedAttributesCount || 0);
      const linkFlags = buildSpanFlags(context2.traceFlags, context2.isRemote);
      if (linkFlags) {
        writer.writeTag(6, 5);
        writer.writeFixed32(linkFlags);
      }
      writer.finishLengthDelimited(linkStart, writer.pos - linkStartPos);
    }
    __name(serializeLink, "serializeLink");
    function serializeSpan(writer, span) {
      const spanStart = writer.startLengthDelimited();
      const spanStartPos = writer.pos;
      const ctx = span.spanContext();
      writer.writeTag(1, 2);
      writer.writeBytes((0, hex_to_binary_1.hexToBinary)(ctx.traceId));
      writer.writeTag(2, 2);
      writer.writeBytes((0, hex_to_binary_1.hexToBinary)(ctx.spanId));
      const traceState = ctx.traceState?.serialize();
      if (traceState) {
        writer.writeTag(3, 2);
        writer.writeString(traceState);
      }
      if (span.parentSpanContext?.spanId) {
        writer.writeTag(4, 2);
        writer.writeBytes((0, hex_to_binary_1.hexToBinary)(span.parentSpanContext.spanId));
      }
      writer.writeTag(5, 2);
      writer.writeString(span.name);
      const kind = span.kind == null ? 0 : span.kind + 1;
      if (kind !== 0) {
        writer.writeTag(6, 0);
        writer.writeVarint(kind);
      }
      writer.writeTag(7, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, span.startTime);
      writer.writeTag(8, 1);
      (0, common_serializer_1.writeHrTimeAsFixed64)(writer, span.endTime);
      if (span.attributes) {
        (0, common_serializer_1.writeAttributes)(writer, span.attributes, 9);
      }
      writer.writeTag(10, 0);
      writer.writeVarint(span.droppedAttributesCount);
      for (const event of span.events) {
        writer.writeTag(11, 2);
        serializeEvent(writer, event);
      }
      writer.writeTag(12, 0);
      writer.writeVarint(span.droppedEventsCount);
      for (const link of span.links) {
        writer.writeTag(13, 2);
        serializeLink(writer, link);
      }
      writer.writeTag(14, 0);
      writer.writeVarint(span.droppedLinksCount);
      writer.writeTag(15, 2);
      serializeStatus(writer, span.status);
      const flags = buildSpanFlags(ctx.traceFlags, span.parentSpanContext?.isRemote);
      if (flags) {
        writer.writeTag(16, 5);
        writer.writeFixed32(flags);
      }
      writer.finishLengthDelimited(spanStart, writer.pos - spanStartPos);
    }
    __name(serializeSpan, "serializeSpan");
    function serializeScopeSpans(writer, scope, spans) {
      const scopeSpansStart = writer.startLengthDelimited();
      const scopeSpansStartPos = writer.pos;
      (0, common_serializer_1.writeInstrumentationScope)(writer, scope, 1);
      for (const span of spans) {
        writer.writeTag(2, 2);
        serializeSpan(writer, span);
      }
      if (scope.schemaUrl) {
        writer.writeTag(3, 2);
        writer.writeString(scope.schemaUrl);
      }
      writer.finishLengthDelimited(scopeSpansStart, writer.pos - scopeSpansStartPos);
    }
    __name(serializeScopeSpans, "serializeScopeSpans");
    function serializeResourceSpans(writer, resource, scopeMap) {
      const resourceSpansStart = writer.startLengthDelimited();
      const resourceSpansStartPos = writer.pos;
      (0, common_serializer_1.writeResource)(writer, resource, 1);
      for (const scopeSpans of scopeMap.values()) {
        writer.writeTag(2, 2);
        const scope = scopeSpans[0].instrumentationScope;
        serializeScopeSpans(writer, scope, scopeSpans);
      }
      if (resource.schemaUrl) {
        writer.writeTag(3, 2);
        writer.writeString(resource.schemaUrl);
      }
      writer.finishLengthDelimited(resourceSpansStart, writer.pos - resourceSpansStartPos);
    }
    __name(serializeResourceSpans, "serializeResourceSpans");
    function createResourceMap(spans) {
      const resourceMap = /* @__PURE__ */ new Map();
      for (const span of spans) {
        const resource = span.resource;
        const scope = span.instrumentationScope;
        let scopeMap = resourceMap.get(resource);
        if (!scopeMap) {
          scopeMap = /* @__PURE__ */ new Map();
          resourceMap.set(resource, scopeMap);
        }
        let records = scopeMap.get(scope);
        if (!records) {
          records = [];
          scopeMap.set(scope, records);
        }
        records.push(span);
      }
      return resourceMap;
    }
    __name(createResourceMap, "createResourceMap");
    function serializeTraceExportRequest(spans) {
      const resourceMap = createResourceMap(spans);
      const estimator = new protobuf_size_estimator_1.ProtobufSizeEstimator();
      for (const [resource, scopeMap] of resourceMap) {
        estimator.writeTag(1, 2);
        serializeResourceSpans(estimator, resource, scopeMap);
      }
      const writer = new protobuf_writer_1.ProtobufWriter(estimator.pos);
      for (const [resource, scopeMap] of resourceMap) {
        writer.writeTag(1, 2);
        serializeResourceSpans(writer, resource, scopeMap);
      }
      return writer.finish();
    }
    __name(serializeTraceExportRequest, "serializeTraceExportRequest");
    exports.serializeTraceExportRequest = serializeTraceExportRequest;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/response-deserializer.js
var require_response_deserializer3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/response-deserializer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.deserializeExportTraceServiceResponse = void 0;
    var protobuf_reader_1 = require_protobuf_reader();
    function deserializePartialSuccess(data) {
      const reader = new protobuf_reader_1.ProtobufReader(data);
      const result = {};
      while (!reader.isAtEnd()) {
        const { fieldNumber, wireType } = reader.readTag();
        switch (fieldNumber) {
          case 1:
            if (wireType === 0) {
              result.rejectedSpans = reader.readVarint();
            } else {
              reader.skip(wireType);
            }
            break;
          case 2:
            if (wireType === 2) {
              result.errorMessage = reader.readString();
            } else {
              reader.skip(wireType);
            }
            break;
          default:
            reader.skip(wireType);
            break;
        }
      }
      return result;
    }
    __name(deserializePartialSuccess, "deserializePartialSuccess");
    function deserializeExportTraceServiceResponse(data) {
      const reader = new protobuf_reader_1.ProtobufReader(data);
      const result = {};
      while (!reader.isAtEnd()) {
        const { fieldNumber, wireType } = reader.readTag();
        switch (fieldNumber) {
          case 1:
            if (wireType === 2) {
              result.partialSuccess = deserializePartialSuccess(reader.readBytes());
            } else {
              reader.skip(wireType);
            }
            break;
          default:
            reader.skip(wireType);
            break;
        }
      }
      return result;
    }
    __name(deserializeExportTraceServiceResponse, "deserializeExportTraceServiceResponse");
    exports.deserializeExportTraceServiceResponse = deserializeExportTraceServiceResponse;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/trace.js
var require_trace = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/trace.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufTraceSerializer = void 0;
    var trace_serializer_1 = require_trace_serializer();
    var response_deserializer_1 = require_response_deserializer3();
    exports.ProtobufTraceSerializer = {
      serializeRequest: /* @__PURE__ */ __name((arg) => {
        return (0, trace_serializer_1.serializeTraceExportRequest)(arg);
      }, "serializeRequest"),
      deserializeResponse: /* @__PURE__ */ __name((arg) => {
        return (0, response_deserializer_1.deserializeExportTraceServiceResponse)(arg);
      }, "deserializeResponse")
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/index.js
var require_protobuf3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/protobuf/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ProtobufTraceSerializer = void 0;
    var trace_1 = require_trace();
    Object.defineProperty(exports, "ProtobufTraceSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trace_1.ProtobufTraceSerializer;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/internal.js
var require_internal = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/internal.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.toAnyValue = exports.toKeyValue = exports.toAttributes = exports.createInstrumentationScope = exports.createResource = void 0;
    function createResource(resource, encoder) {
      const result = {
        attributes: toAttributes(resource.attributes, encoder),
        droppedAttributesCount: 0
      };
      const schemaUrl = resource.schemaUrl;
      if (schemaUrl && schemaUrl !== "")
        result.schemaUrl = schemaUrl;
      return result;
    }
    __name(createResource, "createResource");
    exports.createResource = createResource;
    function createInstrumentationScope(scope) {
      return {
        name: scope.name,
        version: scope.version
      };
    }
    __name(createInstrumentationScope, "createInstrumentationScope");
    exports.createInstrumentationScope = createInstrumentationScope;
    function toAttributes(attributes, encoder) {
      return Object.keys(attributes).map((key) => toKeyValue(key, attributes[key], encoder));
    }
    __name(toAttributes, "toAttributes");
    exports.toAttributes = toAttributes;
    function toKeyValue(key, value2, encoder) {
      return {
        key,
        value: toAnyValue(value2, encoder)
      };
    }
    __name(toKeyValue, "toKeyValue");
    exports.toKeyValue = toKeyValue;
    function toAnyValue(value2, encoder) {
      const t = typeof value2;
      if (t === "string")
        return { stringValue: value2 };
      if (t === "number") {
        if (!Number.isInteger(value2))
          return { doubleValue: value2 };
        return { intValue: value2 };
      }
      if (t === "boolean")
        return { boolValue: value2 };
      if (value2 instanceof Uint8Array)
        return { bytesValue: encoder.encodeUint8Array(value2) };
      if (Array.isArray(value2)) {
        const values = new Array(value2.length);
        for (let i = 0; i < value2.length; i++) {
          values[i] = toAnyValue(value2[i], encoder);
        }
        return { arrayValue: { values } };
      }
      if (t === "object" && value2 != null) {
        const keys = Object.keys(value2);
        const values = new Array(keys.length);
        for (let i = 0; i < keys.length; i++) {
          values[i] = {
            key: keys[i],
            value: toAnyValue(value2[keys[i]], encoder)
          };
        }
        return { kvlistValue: { values } };
      }
      return {};
    }
    __name(toAnyValue, "toAnyValue");
    exports.toAnyValue = toAnyValue;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/internal.js
var require_internal2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/internal.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.toLogAttributes = exports.createExportLogsServiceRequest = void 0;
    var internal_1 = require_internal();
    function createExportLogsServiceRequest(logRecords, encoder) {
      return {
        resourceLogs: logRecordsToResourceLogs(logRecords, encoder)
      };
    }
    __name(createExportLogsServiceRequest, "createExportLogsServiceRequest");
    exports.createExportLogsServiceRequest = createExportLogsServiceRequest;
    function createResourceMap(logRecords) {
      const resourceMap = /* @__PURE__ */ new Map();
      for (const record of logRecords) {
        const { resource, instrumentationScope: { name, version = "", schemaUrl = "" } } = record;
        let ismMap = resourceMap.get(resource);
        if (!ismMap) {
          ismMap = /* @__PURE__ */ new Map();
          resourceMap.set(resource, ismMap);
        }
        const ismKey = `${name}@${version}:${schemaUrl}`;
        let records = ismMap.get(ismKey);
        if (!records) {
          records = [];
          ismMap.set(ismKey, records);
        }
        records.push(record);
      }
      return resourceMap;
    }
    __name(createResourceMap, "createResourceMap");
    function logRecordsToResourceLogs(logRecords, encoder) {
      const resourceMap = createResourceMap(logRecords);
      return Array.from(resourceMap, ([resource, ismMap]) => {
        const processedResource = (0, internal_1.createResource)(resource, encoder);
        return {
          resource: processedResource,
          scopeLogs: Array.from(ismMap, ([, scopeLogs]) => {
            return {
              scope: (0, internal_1.createInstrumentationScope)(scopeLogs[0].instrumentationScope),
              logRecords: scopeLogs.map((log) => toLogRecord(log, encoder)),
              schemaUrl: scopeLogs[0].instrumentationScope.schemaUrl
            };
          }),
          schemaUrl: processedResource.schemaUrl
        };
      });
    }
    __name(logRecordsToResourceLogs, "logRecordsToResourceLogs");
    function toLogRecord(log, encoder) {
      return {
        timeUnixNano: encoder.encodeHrTime(log.hrTime),
        observedTimeUnixNano: encoder.encodeHrTime(log.hrTimeObserved),
        severityNumber: toSeverityNumber(log.severityNumber),
        severityText: log.severityText,
        body: (0, internal_1.toAnyValue)(log.body, encoder),
        eventName: log.eventName,
        attributes: toLogAttributes(log.attributes, encoder),
        droppedAttributesCount: log.droppedAttributesCount,
        flags: log.spanContext?.traceFlags,
        traceId: encoder.encodeOptionalSpanContext(log.spanContext?.traceId),
        spanId: encoder.encodeOptionalSpanContext(log.spanContext?.spanId)
      };
    }
    __name(toLogRecord, "toLogRecord");
    function toSeverityNumber(severityNumber) {
      return severityNumber;
    }
    __name(toSeverityNumber, "toSeverityNumber");
    function toLogAttributes(attributes, encoder) {
      return Object.keys(attributes).map((key) => (0, internal_1.toKeyValue)(key, attributes[key], encoder));
    }
    __name(toLogAttributes, "toLogAttributes");
    exports.toLogAttributes = toLogAttributes;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/utils.js
var require_utils5 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/common/utils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JSON_ENCODER = exports.PROTOBUF_ENCODER = exports.encodeAsString = exports.encodeAsLongBits = exports.toLongBits = exports.hrTimeToNanos = void 0;
    var core_1 = require_src();
    var hex_to_binary_1 = require_hex_to_binary();
    function hrTimeToNanos(hrTime) {
      const NANOSECONDS = BigInt(1e9);
      return BigInt(Math.trunc(hrTime[0])) * NANOSECONDS + BigInt(Math.trunc(hrTime[1]));
    }
    __name(hrTimeToNanos, "hrTimeToNanos");
    exports.hrTimeToNanos = hrTimeToNanos;
    function toLongBits(value2) {
      const low = Number(BigInt.asUintN(32, value2));
      const high = Number(BigInt.asUintN(32, value2 >> BigInt(32)));
      return { low, high };
    }
    __name(toLongBits, "toLongBits");
    exports.toLongBits = toLongBits;
    function encodeAsLongBits(hrTime) {
      const nanos = hrTimeToNanos(hrTime);
      return toLongBits(nanos);
    }
    __name(encodeAsLongBits, "encodeAsLongBits");
    exports.encodeAsLongBits = encodeAsLongBits;
    function encodeAsString(hrTime) {
      const nanos = hrTimeToNanos(hrTime);
      return nanos.toString();
    }
    __name(encodeAsString, "encodeAsString");
    exports.encodeAsString = encodeAsString;
    var encodeTimestamp = typeof BigInt !== "undefined" ? encodeAsString : core_1.hrTimeToNanoseconds;
    function identity(value2) {
      return value2;
    }
    __name(identity, "identity");
    function optionalHexToBinary(str) {
      if (str === void 0)
        return void 0;
      return (0, hex_to_binary_1.hexToBinary)(str);
    }
    __name(optionalHexToBinary, "optionalHexToBinary");
    exports.PROTOBUF_ENCODER = {
      encodeHrTime: encodeAsLongBits,
      encodeSpanContext: hex_to_binary_1.hexToBinary,
      encodeOptionalSpanContext: optionalHexToBinary,
      encodeUint8Array: identity
    };
    exports.JSON_ENCODER = {
      encodeHrTime: encodeTimestamp,
      encodeSpanContext: identity,
      encodeOptionalSpanContext: identity,
      encodeUint8Array: /* @__PURE__ */ __name((bytes) => {
        if (typeof Buffer !== "undefined") {
          return Buffer.from(bytes).toString("base64");
        }
        const chars = new Array(bytes.length);
        for (let i = 0; i < bytes.length; i++) {
          chars[i] = String.fromCharCode(bytes[i]);
        }
        return btoa(chars.join(""));
      }, "encodeUint8Array")
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/json/logs.js
var require_logs2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/json/logs.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JsonLogsSerializer = void 0;
    var internal_1 = require_internal2();
    var utils_1 = require_utils5();
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    exports.JsonLogsSerializer = {
      serializeRequest: /* @__PURE__ */ __name((arg) => {
        const request = (0, internal_1.createExportLogsServiceRequest)(arg, utils_1.JSON_ENCODER);
        const encoder = new TextEncoder();
        return encoder.encode(JSON.stringify(request));
      }, "serializeRequest"),
      deserializeResponse: /* @__PURE__ */ __name((arg) => {
        if (arg.length === 0) {
          return {};
        }
        const decoder = new TextDecoder();
        try {
          return JSON.parse(decoder.decode(arg));
        } catch (err2) {
          api_1.diag.warn(`Failed to parse logs export response: ${err2.message}. Returning empty response`);
          return {};
        }
      }, "deserializeResponse")
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/json/index.js
var require_json = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/logs/json/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JsonLogsSerializer = void 0;
    var logs_1 = require_logs2();
    Object.defineProperty(exports, "JsonLogsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return logs_1.JsonLogsSerializer;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/internal-types.js
var require_internal_types = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/internal-types.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EAggregationTemporality = void 0;
    var EAggregationTemporality;
    (function(EAggregationTemporality2) {
      EAggregationTemporality2[EAggregationTemporality2["AGGREGATION_TEMPORALITY_UNSPECIFIED"] = 0] = "AGGREGATION_TEMPORALITY_UNSPECIFIED";
      EAggregationTemporality2[EAggregationTemporality2["AGGREGATION_TEMPORALITY_DELTA"] = 1] = "AGGREGATION_TEMPORALITY_DELTA";
      EAggregationTemporality2[EAggregationTemporality2["AGGREGATION_TEMPORALITY_CUMULATIVE"] = 2] = "AGGREGATION_TEMPORALITY_CUMULATIVE";
    })(EAggregationTemporality = exports.EAggregationTemporality || (exports.EAggregationTemporality = {}));
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/internal.js
var require_internal3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/internal.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createExportMetricsServiceRequest = exports.toMetric = exports.toScopeMetrics = exports.toResourceMetrics = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var sdk_metrics_1 = require_src6();
    var internal_types_1 = require_internal_types();
    var internal_1 = require_internal();
    function toResourceMetrics(resourceMetrics, encoder) {
      const processedResource = (0, internal_1.createResource)(resourceMetrics.resource, encoder);
      return {
        resource: processedResource,
        schemaUrl: processedResource.schemaUrl,
        scopeMetrics: toScopeMetrics(resourceMetrics.scopeMetrics, encoder)
      };
    }
    __name(toResourceMetrics, "toResourceMetrics");
    exports.toResourceMetrics = toResourceMetrics;
    function toScopeMetrics(scopeMetrics, encoder) {
      return Array.from(scopeMetrics.map((metrics2) => ({
        scope: (0, internal_1.createInstrumentationScope)(metrics2.scope),
        metrics: metrics2.metrics.map((metricData) => toMetric(metricData, encoder)),
        schemaUrl: metrics2.scope.schemaUrl
      })));
    }
    __name(toScopeMetrics, "toScopeMetrics");
    exports.toScopeMetrics = toScopeMetrics;
    function toMetric(metricData, encoder) {
      const out = {
        name: metricData.descriptor.name,
        description: metricData.descriptor.description,
        unit: metricData.descriptor.unit
      };
      const aggregationTemporality = toAggregationTemporality(metricData.aggregationTemporality);
      switch (metricData.dataPointType) {
        case sdk_metrics_1.DataPointType.SUM:
          out.sum = {
            aggregationTemporality,
            isMonotonic: metricData.isMonotonic,
            dataPoints: toSingularDataPoints(metricData, encoder)
          };
          break;
        case sdk_metrics_1.DataPointType.GAUGE:
          out.gauge = {
            dataPoints: toSingularDataPoints(metricData, encoder)
          };
          break;
        case sdk_metrics_1.DataPointType.HISTOGRAM:
          out.histogram = {
            aggregationTemporality,
            dataPoints: toHistogramDataPoints(metricData, encoder)
          };
          break;
        case sdk_metrics_1.DataPointType.EXPONENTIAL_HISTOGRAM:
          out.exponentialHistogram = {
            aggregationTemporality,
            dataPoints: toExponentialHistogramDataPoints(metricData, encoder)
          };
          break;
      }
      return out;
    }
    __name(toMetric, "toMetric");
    exports.toMetric = toMetric;
    function toSingularDataPoint(dataPoint, valueType, encoder) {
      const out = {
        attributes: (0, internal_1.toAttributes)(dataPoint.attributes, encoder),
        startTimeUnixNano: encoder.encodeHrTime(dataPoint.startTime),
        timeUnixNano: encoder.encodeHrTime(dataPoint.endTime)
      };
      switch (valueType) {
        case api_1.ValueType.INT:
          out.asInt = dataPoint.value;
          break;
        case api_1.ValueType.DOUBLE:
          out.asDouble = dataPoint.value;
          break;
      }
      return out;
    }
    __name(toSingularDataPoint, "toSingularDataPoint");
    function toSingularDataPoints(metricData, encoder) {
      return metricData.dataPoints.map((dataPoint) => {
        return toSingularDataPoint(dataPoint, metricData.descriptor.valueType, encoder);
      });
    }
    __name(toSingularDataPoints, "toSingularDataPoints");
    function toHistogramDataPoints(metricData, encoder) {
      return metricData.dataPoints.map((dataPoint) => {
        const histogram = dataPoint.value;
        return {
          attributes: (0, internal_1.toAttributes)(dataPoint.attributes, encoder),
          bucketCounts: histogram.buckets.counts,
          explicitBounds: histogram.buckets.boundaries,
          count: histogram.count,
          sum: histogram.sum,
          min: histogram.min,
          max: histogram.max,
          startTimeUnixNano: encoder.encodeHrTime(dataPoint.startTime),
          timeUnixNano: encoder.encodeHrTime(dataPoint.endTime)
        };
      });
    }
    __name(toHistogramDataPoints, "toHistogramDataPoints");
    function toExponentialHistogramDataPoints(metricData, encoder) {
      return metricData.dataPoints.map((dataPoint) => {
        const histogram = dataPoint.value;
        return {
          attributes: (0, internal_1.toAttributes)(dataPoint.attributes, encoder),
          count: histogram.count,
          min: histogram.min,
          max: histogram.max,
          sum: histogram.sum,
          positive: {
            offset: histogram.positive.offset,
            bucketCounts: histogram.positive.bucketCounts
          },
          negative: {
            offset: histogram.negative.offset,
            bucketCounts: histogram.negative.bucketCounts
          },
          scale: histogram.scale,
          zeroCount: histogram.zeroCount,
          startTimeUnixNano: encoder.encodeHrTime(dataPoint.startTime),
          timeUnixNano: encoder.encodeHrTime(dataPoint.endTime)
        };
      });
    }
    __name(toExponentialHistogramDataPoints, "toExponentialHistogramDataPoints");
    function toAggregationTemporality(temporality) {
      switch (temporality) {
        case sdk_metrics_1.AggregationTemporality.DELTA:
          return internal_types_1.EAggregationTemporality.AGGREGATION_TEMPORALITY_DELTA;
        case sdk_metrics_1.AggregationTemporality.CUMULATIVE:
          return internal_types_1.EAggregationTemporality.AGGREGATION_TEMPORALITY_CUMULATIVE;
      }
    }
    __name(toAggregationTemporality, "toAggregationTemporality");
    function createExportMetricsServiceRequest(resourceMetrics, encoder) {
      return {
        resourceMetrics: resourceMetrics.map((metrics2) => toResourceMetrics(metrics2, encoder))
      };
    }
    __name(createExportMetricsServiceRequest, "createExportMetricsServiceRequest");
    exports.createExportMetricsServiceRequest = createExportMetricsServiceRequest;
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/json/metrics.js
var require_metrics2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/json/metrics.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JsonMetricsSerializer = void 0;
    var internal_1 = require_internal3();
    var utils_1 = require_utils5();
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    exports.JsonMetricsSerializer = {
      serializeRequest: /* @__PURE__ */ __name((arg) => {
        const request = (0, internal_1.createExportMetricsServiceRequest)([arg], utils_1.JSON_ENCODER);
        const encoder = new TextEncoder();
        return encoder.encode(JSON.stringify(request));
      }, "serializeRequest"),
      deserializeResponse: /* @__PURE__ */ __name((arg) => {
        if (arg.length === 0) {
          return {};
        }
        const decoder = new TextDecoder();
        try {
          return JSON.parse(decoder.decode(arg));
        } catch (err2) {
          api_1.diag.warn(`Failed to parse metrics export response: ${err2.message}. Returning empty response`);
          return {};
        }
      }, "deserializeResponse")
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/json/index.js
var require_json2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/metrics/json/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JsonMetricsSerializer = void 0;
    var metrics_1 = require_metrics2();
    Object.defineProperty(exports, "JsonMetricsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return metrics_1.JsonMetricsSerializer;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/internal.js
var require_internal4 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/internal.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createExportTraceServiceRequest = exports.toOtlpSpanEvent = exports.toOtlpLink = exports.sdkSpanToOtlpSpan = void 0;
    var internal_1 = require_internal();
    var SPAN_FLAGS_CONTEXT_HAS_IS_REMOTE_MASK = 256;
    var SPAN_FLAGS_CONTEXT_IS_REMOTE_MASK = 512;
    function buildSpanFlagsFrom(traceFlags, isRemote) {
      let flags = traceFlags & 255 | SPAN_FLAGS_CONTEXT_HAS_IS_REMOTE_MASK;
      if (isRemote) {
        flags |= SPAN_FLAGS_CONTEXT_IS_REMOTE_MASK;
      }
      return flags;
    }
    __name(buildSpanFlagsFrom, "buildSpanFlagsFrom");
    function sdkSpanToOtlpSpan(span, encoder) {
      const ctx = span.spanContext();
      const status = span.status;
      const parentSpanId = span.parentSpanContext?.spanId ? encoder.encodeSpanContext(span.parentSpanContext?.spanId) : void 0;
      return {
        traceId: encoder.encodeSpanContext(ctx.traceId),
        spanId: encoder.encodeSpanContext(ctx.spanId),
        parentSpanId,
        traceState: ctx.traceState?.serialize(),
        name: span.name,
        // Span kind is offset by 1 because the API does not define a value for unset
        kind: span.kind == null ? 0 : span.kind + 1,
        startTimeUnixNano: encoder.encodeHrTime(span.startTime),
        endTimeUnixNano: encoder.encodeHrTime(span.endTime),
        attributes: (0, internal_1.toAttributes)(span.attributes, encoder),
        droppedAttributesCount: span.droppedAttributesCount,
        events: span.events.map((event) => toOtlpSpanEvent(event, encoder)),
        droppedEventsCount: span.droppedEventsCount,
        status: {
          // API and proto enums share the same values
          code: status.code,
          message: status.message
        },
        links: span.links.map((link) => toOtlpLink(link, encoder)),
        droppedLinksCount: span.droppedLinksCount,
        flags: buildSpanFlagsFrom(ctx.traceFlags, span.parentSpanContext?.isRemote)
      };
    }
    __name(sdkSpanToOtlpSpan, "sdkSpanToOtlpSpan");
    exports.sdkSpanToOtlpSpan = sdkSpanToOtlpSpan;
    function toOtlpLink(link, encoder) {
      return {
        attributes: link.attributes ? (0, internal_1.toAttributes)(link.attributes, encoder) : [],
        spanId: encoder.encodeSpanContext(link.context.spanId),
        traceId: encoder.encodeSpanContext(link.context.traceId),
        traceState: link.context.traceState?.serialize(),
        droppedAttributesCount: link.droppedAttributesCount || 0,
        flags: buildSpanFlagsFrom(link.context.traceFlags, link.context.isRemote)
      };
    }
    __name(toOtlpLink, "toOtlpLink");
    exports.toOtlpLink = toOtlpLink;
    function toOtlpSpanEvent(timedEvent, encoder) {
      return {
        attributes: timedEvent.attributes ? (0, internal_1.toAttributes)(timedEvent.attributes, encoder) : [],
        name: timedEvent.name,
        timeUnixNano: encoder.encodeHrTime(timedEvent.time),
        droppedAttributesCount: timedEvent.droppedAttributesCount || 0
      };
    }
    __name(toOtlpSpanEvent, "toOtlpSpanEvent");
    exports.toOtlpSpanEvent = toOtlpSpanEvent;
    function createExportTraceServiceRequest(spans, encoder) {
      return {
        resourceSpans: spanRecordsToResourceSpans(spans, encoder)
      };
    }
    __name(createExportTraceServiceRequest, "createExportTraceServiceRequest");
    exports.createExportTraceServiceRequest = createExportTraceServiceRequest;
    function createResourceMap(readableSpans) {
      const resourceMap = /* @__PURE__ */ new Map();
      for (const record of readableSpans) {
        let ilsMap = resourceMap.get(record.resource);
        if (!ilsMap) {
          ilsMap = /* @__PURE__ */ new Map();
          resourceMap.set(record.resource, ilsMap);
        }
        const instrumentationScopeKey = `${record.instrumentationScope.name}@${record.instrumentationScope.version || ""}:${record.instrumentationScope.schemaUrl || ""}`;
        let records = ilsMap.get(instrumentationScopeKey);
        if (!records) {
          records = [];
          ilsMap.set(instrumentationScopeKey, records);
        }
        records.push(record);
      }
      return resourceMap;
    }
    __name(createResourceMap, "createResourceMap");
    function spanRecordsToResourceSpans(readableSpans, encoder) {
      const resourceMap = createResourceMap(readableSpans);
      const out = [];
      const entryIterator = resourceMap.entries();
      let entry = entryIterator.next();
      while (!entry.done) {
        const [resource, ilmMap] = entry.value;
        const scopeResourceSpans = [];
        const ilmIterator = ilmMap.values();
        let ilmEntry = ilmIterator.next();
        while (!ilmEntry.done) {
          const scopeSpans = ilmEntry.value;
          if (scopeSpans.length > 0) {
            const spans = scopeSpans.map((readableSpan) => sdkSpanToOtlpSpan(readableSpan, encoder));
            scopeResourceSpans.push({
              scope: (0, internal_1.createInstrumentationScope)(scopeSpans[0].instrumentationScope),
              spans,
              schemaUrl: scopeSpans[0].instrumentationScope.schemaUrl
            });
          }
          ilmEntry = ilmIterator.next();
        }
        const processedResource = (0, internal_1.createResource)(resource, encoder);
        const transformedSpans = {
          resource: processedResource,
          scopeSpans: scopeResourceSpans,
          schemaUrl: processedResource.schemaUrl
        };
        out.push(transformedSpans);
        entry = entryIterator.next();
      }
      return out;
    }
    __name(spanRecordsToResourceSpans, "spanRecordsToResourceSpans");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/json/trace.js
var require_trace2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/json/trace.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JsonTraceSerializer = void 0;
    var internal_1 = require_internal4();
    var utils_1 = require_utils5();
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    exports.JsonTraceSerializer = {
      serializeRequest: /* @__PURE__ */ __name((arg) => {
        const request = (0, internal_1.createExportTraceServiceRequest)(arg, utils_1.JSON_ENCODER);
        const encoder = new TextEncoder();
        return encoder.encode(JSON.stringify(request));
      }, "serializeRequest"),
      deserializeResponse: /* @__PURE__ */ __name((arg) => {
        if (arg.length === 0) {
          return {};
        }
        const decoder = new TextDecoder();
        try {
          return JSON.parse(decoder.decode(arg));
        } catch (err2) {
          api_1.diag.warn(`Failed to parse trace export response: ${err2.message}. Returning empty response`);
          return {};
        }
      }, "deserializeResponse")
    };
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/json/index.js
var require_json3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/trace/json/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JsonTraceSerializer = void 0;
    var trace_1 = require_trace2();
    Object.defineProperty(exports, "JsonTraceSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trace_1.JsonTraceSerializer;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/index.js
var require_src7 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+otlp-transformer@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-transformer/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JsonTraceSerializer = exports.JsonMetricsSerializer = exports.JsonLogsSerializer = exports.ProtobufTraceSerializer = exports.ProtobufMetricsSerializer = exports.ProtobufLogsSerializer = void 0;
    var protobuf_1 = require_protobuf();
    Object.defineProperty(exports, "ProtobufLogsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return protobuf_1.ProtobufLogsSerializer;
    }, "get") });
    var protobuf_2 = require_protobuf2();
    Object.defineProperty(exports, "ProtobufMetricsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return protobuf_2.ProtobufMetricsSerializer;
    }, "get") });
    var protobuf_3 = require_protobuf3();
    Object.defineProperty(exports, "ProtobufTraceSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return protobuf_3.ProtobufTraceSerializer;
    }, "get") });
    var json_1 = require_json();
    Object.defineProperty(exports, "JsonLogsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return json_1.JsonLogsSerializer;
    }, "get") });
    var json_2 = require_json2();
    Object.defineProperty(exports, "JsonMetricsSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return json_2.JsonMetricsSerializer;
    }, "get") });
    var json_3 = require_json3();
    Object.defineProperty(exports, "JsonTraceSerializer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return json_3.JsonTraceSerializer;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/util.js
function validateAndNormalizeHeaders(partialHeaders) {
  const headers = {};
  Object.entries(partialHeaders ?? {}).forEach(([key, value2]) => {
    if (typeof value2 !== "undefined") {
      headers[key] = String(value2);
    } else {
      diag.warn(`Header "${key}" has invalid value (${value2}) and will be ignored`);
    }
  });
  return headers;
}
var init_util = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/util.js"() {
    init_esm();
    init_esm2();
    __name(validateAndNormalizeHeaders, "validateAndNormalizeHeaders");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/otlp-http-configuration.js
function mergeHeaders(userProvidedHeaders, fallbackHeaders, defaultHeaders2) {
  return async () => {
    const requiredHeaders = {
      ...await defaultHeaders2()
    };
    const headers = {};
    if (fallbackHeaders != null) {
      Object.assign(headers, await fallbackHeaders());
    }
    if (userProvidedHeaders != null) {
      Object.assign(headers, validateAndNormalizeHeaders(await userProvidedHeaders()));
    }
    return Object.assign(headers, requiredHeaders);
  };
}
function validateUserProvidedUrl(url) {
  if (url == null) {
    return void 0;
  }
  try {
    const base = globalThis.location?.href;
    return new URL(url, base).href;
  } catch {
    throw new Error(`Configuration: Could not parse user-provided export URL: '${url}'`);
  }
}
function mergeOtlpHttpConfigurationWithDefaults(userProvidedConfiguration, fallbackConfiguration, defaultConfiguration) {
  return {
    ...mergeOtlpSharedConfigurationWithDefaults(userProvidedConfiguration, fallbackConfiguration, defaultConfiguration),
    headers: mergeHeaders(userProvidedConfiguration.headers, fallbackConfiguration.headers, defaultConfiguration.headers),
    url: validateUserProvidedUrl(userProvidedConfiguration.url) ?? fallbackConfiguration.url ?? defaultConfiguration.url
  };
}
function getHttpConfigurationDefaults(requiredHeaders, signalResourcePath) {
  return {
    ...getSharedConfigurationDefaults(),
    headers: /* @__PURE__ */ __name(async () => requiredHeaders, "headers"),
    url: "http://localhost:4318/" + signalResourcePath
  };
}
var init_otlp_http_configuration = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/otlp-http-configuration.js"() {
    init_esm();
    init_shared_configuration();
    init_util();
    __name(mergeHeaders, "mergeHeaders");
    __name(validateUserProvidedUrl, "validateUserProvidedUrl");
    __name(mergeOtlpHttpConfigurationWithDefaults, "mergeOtlpHttpConfigurationWithDefaults");
    __name(getHttpConfigurationDefaults, "getHttpConfigurationDefaults");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/otlp-node-http-configuration.js
function httpAgentFactoryFromOptions(options) {
  return async (protocol) => {
    const isInsecure = protocol === "http:";
    const module = isInsecure ? import("http") : import("https");
    const { Agent } = await module;
    if (isInsecure) {
      const { ca, cert, key, ...insecureOptions } = options;
      return new Agent(insecureOptions);
    }
    return new Agent(options);
  };
}
function mergeOtlpNodeHttpConfigurationWithDefaults(userProvidedConfiguration, fallbackConfiguration, defaultConfiguration) {
  return {
    ...mergeOtlpHttpConfigurationWithDefaults(userProvidedConfiguration, fallbackConfiguration, defaultConfiguration),
    agentFactory: userProvidedConfiguration.agentFactory ?? fallbackConfiguration.agentFactory ?? defaultConfiguration.agentFactory,
    userAgent: userProvidedConfiguration.userAgent
  };
}
function getNodeHttpConfigurationDefaults(requiredHeaders, signalResourcePath) {
  return {
    ...getHttpConfigurationDefaults(requiredHeaders, signalResourcePath),
    agentFactory: httpAgentFactoryFromOptions({ keepAlive: true })
  };
}
var init_otlp_node_http_configuration = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/otlp-node-http-configuration.js"() {
    init_esm();
    init_otlp_http_configuration();
    __name(httpAgentFactoryFromOptions, "httpAgentFactoryFromOptions");
    __name(mergeOtlpNodeHttpConfigurationWithDefaults, "mergeOtlpNodeHttpConfigurationWithDefaults");
    __name(getNodeHttpConfigurationDefaults, "getNodeHttpConfigurationDefaults");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/is-export-retryable.js
function isExportHTTPErrorRetryable(statusCode) {
  return statusCode === 429 || statusCode === 502 || statusCode === 503 || statusCode === 504;
}
function parseRetryAfterToMills(retryAfter) {
  if (retryAfter == null) {
    return void 0;
  }
  const seconds = Number.parseInt(retryAfter, 10);
  if (Number.isInteger(seconds)) {
    return seconds > 0 ? seconds * 1e3 : -1;
  }
  const delay = new Date(retryAfter).getTime() - Date.now();
  if (delay >= 0) {
    return delay;
  }
  return 0;
}
var init_is_export_retryable = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/is-export-retryable.js"() {
    init_esm();
    __name(isExportHTTPErrorRetryable, "isExportHTTPErrorRetryable");
    __name(parseRetryAfterToMills, "parseRetryAfterToMills");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/version.js
var VERSION2;
var init_version = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/version.js"() {
    init_esm();
    VERSION2 = "0.218.0";
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/transport/http-transport-utils.js
import * as zlib from "zlib";
import { Readable } from "stream";
function sendWithHttp(request, url, headers, compression, userAgent, agent, data, timeoutMillis) {
  return new Promise((resolve2) => {
    const parsedUrl = new URL(url);
    if (userAgent) {
      headers["User-Agent"] = `${userAgent} ${DEFAULT_USER_AGENT2}`;
    } else {
      headers["User-Agent"] = DEFAULT_USER_AGENT2;
    }
    const options = {
      method: "POST",
      headers,
      agent
    };
    const req = request(parsedUrl, options, (res) => {
      const responseData = [];
      let responseSize = 0;
      res.on("data", (chunk) => {
        responseSize += chunk.length;
        if (responseSize > MAX_RESPONSE_BODY_SIZE) {
          const sizeError = new Error(`OTLP export response body exceeded size limit of ${MAX_RESPONSE_BODY_SIZE} bytes`);
          resolve2({ status: "failure", error: sizeError });
          res.destroy();
          return;
        }
        responseData.push(chunk);
      });
      res.on("end", () => {
        if (res.statusCode && res.statusCode <= 299) {
          resolve2({
            status: "success",
            data: Buffer.concat(responseData)
          });
        } else if (res.statusCode && isExportHTTPErrorRetryable(res.statusCode)) {
          resolve2({
            status: "retryable",
            retryInMillis: parseRetryAfterToMills(res.headers["retry-after"])
          });
        } else {
          const error = new OTLPExporterError(res.statusMessage, res.statusCode, Buffer.concat(responseData).toString());
          resolve2({
            status: "failure",
            error
          });
        }
      });
      res.on("error", (error) => {
        if (res.statusCode && res.statusCode <= 299) {
          resolve2({
            status: "success"
          });
        } else if (res.statusCode && isExportHTTPErrorRetryable(res.statusCode)) {
          resolve2({
            status: "retryable",
            error,
            retryInMillis: parseRetryAfterToMills(res.headers["retry-after"])
          });
        } else {
          resolve2({
            status: "failure",
            error
          });
        }
      });
    });
    req.setTimeout(timeoutMillis, () => {
      req.destroy();
      resolve2({
        status: "retryable",
        error: new Error("Request timed out")
      });
    });
    req.on("error", (error) => {
      if (isHttpTransportNetworkErrorRetryable(error)) {
        resolve2({
          status: "retryable",
          error
        });
      } else {
        resolve2({
          status: "failure",
          error
        });
      }
    });
    compressAndSend(req, compression, data, (error) => {
      resolve2({
        status: "failure",
        error
      });
    });
  });
}
function compressAndSend(req, compression, data, onError) {
  let dataStream = readableFromUint8Array(data);
  if (compression === "gzip") {
    req.setHeader("Content-Encoding", "gzip");
    dataStream = dataStream.on("error", onError).pipe(zlib.createGzip()).on("error", onError);
  }
  dataStream.pipe(req).on("error", onError);
}
function readableFromUint8Array(buff) {
  const readable = new Readable();
  readable.push(buff);
  readable.push(null);
  return readable;
}
function isHttpTransportNetworkErrorRetryable(error) {
  const RETRYABLE_NETWORK_ERROR_CODES = /* @__PURE__ */ new Set([
    "ECONNRESET",
    "ECONNREFUSED",
    "EPIPE",
    "ETIMEDOUT",
    "EAI_AGAIN",
    "ENOTFOUND",
    "ENETUNREACH",
    "EHOSTUNREACH"
  ]);
  if ("code" in error && typeof error.code === "string") {
    return RETRYABLE_NETWORK_ERROR_CODES.has(error.code);
  }
  return false;
}
var DEFAULT_USER_AGENT2, MAX_RESPONSE_BODY_SIZE;
var init_http_transport_utils = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/transport/http-transport-utils.js"() {
    init_esm();
    init_is_export_retryable();
    init_types();
    init_version();
    DEFAULT_USER_AGENT2 = `OTel-OTLP-Exporter-JavaScript/${VERSION2}`;
    MAX_RESPONSE_BODY_SIZE = 4 * 1024 * 1024;
    __name(sendWithHttp, "sendWithHttp");
    __name(compressAndSend, "compressAndSend");
    __name(readableFromUint8Array, "readableFromUint8Array");
    __name(isHttpTransportNetworkErrorRetryable, "isHttpTransportNetworkErrorRetryable");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/transport/http-exporter-transport.js
async function requestFunctionFactory(protocol) {
  const module = protocol === "http:" ? import("http") : import("https");
  const { request } = await module;
  return request;
}
function createHttpExporterTransport(parameters) {
  return new HttpExporterTransport(parameters);
}
var HttpExporterTransport;
var init_http_exporter_transport = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/transport/http-exporter-transport.js"() {
    init_esm();
    init_http_transport_utils();
    HttpExporterTransport = class {
      static {
        __name(this, "HttpExporterTransport");
      }
      _utils = null;
      _parameters;
      constructor(parameters) {
        this._parameters = parameters;
      }
      async send(data, timeoutMillis) {
        const { agent, request } = await this._loadUtils();
        const headers = await this._parameters.headers();
        return sendWithHttp(request, this._parameters.url, headers, this._parameters.compression, this._parameters.userAgent, agent, data, timeoutMillis);
      }
      shutdown() {
      }
      async _loadUtils() {
        let utils = this._utils;
        if (utils === null) {
          const protocol = new URL(this._parameters.url).protocol;
          const [agent, request] = await Promise.all([
            this._parameters.agentFactory(protocol),
            requestFunctionFactory(protocol)
          ]);
          utils = this._utils = { agent, request };
        }
        return utils;
      }
    };
    __name(requestFunctionFactory, "requestFunctionFactory");
    __name(createHttpExporterTransport, "createHttpExporterTransport");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/retrying-transport.js
function getJitter() {
  return Math.random() * (2 * JITTER) - JITTER;
}
function createRetryingTransport(options) {
  return new RetryingTransport(options.transport);
}
var MAX_ATTEMPTS, INITIAL_BACKOFF, MAX_BACKOFF, BACKOFF_MULTIPLIER, JITTER, RetryingTransport;
var init_retrying_transport = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/retrying-transport.js"() {
    init_esm();
    init_esm2();
    MAX_ATTEMPTS = 5;
    INITIAL_BACKOFF = 1e3;
    MAX_BACKOFF = 5e3;
    BACKOFF_MULTIPLIER = 1.5;
    JITTER = 0.2;
    __name(getJitter, "getJitter");
    RetryingTransport = class {
      static {
        __name(this, "RetryingTransport");
      }
      _transport;
      constructor(transport) {
        this._transport = transport;
      }
      retry(data, timeoutMillis, inMillis) {
        return new Promise((resolve2, reject) => {
          setTimeout(() => {
            this._transport.send(data, timeoutMillis).then(resolve2, reject);
          }, inMillis);
        });
      }
      async send(data, timeoutMillis) {
        let attempts = MAX_ATTEMPTS;
        let nextBackoff = INITIAL_BACKOFF;
        const deadline = Date.now() + timeoutMillis;
        let result = await this._transport.send(data, timeoutMillis);
        while (result.status === "retryable" && attempts > 0) {
          attempts--;
          const backoff = Math.max(Math.min(nextBackoff * (1 + getJitter()), MAX_BACKOFF), 0);
          nextBackoff = nextBackoff * BACKOFF_MULTIPLIER;
          const retryInMillis = result.retryInMillis ?? backoff;
          const remainingTimeoutMillis = deadline - Date.now();
          if (retryInMillis > remainingTimeoutMillis) {
            diag.info(`Export retry time ${Math.round(retryInMillis)}ms exceeds remaining timeout ${Math.round(remainingTimeoutMillis)}ms, not retrying further.`);
            return result;
          }
          diag.verbose(`Scheduling export retry in ${Math.round(retryInMillis)}ms`);
          result = await this.retry(data, remainingTimeoutMillis, retryInMillis);
        }
        if (result.status === "success") {
          diag.verbose(`Export succeeded after ${MAX_ATTEMPTS - attempts} retry attempts.`);
        } else if (result.status === "retryable") {
          diag.info(`Export failed after maximum retry attempts (${MAX_ATTEMPTS}).`);
        } else {
          diag.info(`Export failed with non-retryable error: ${result.error}`);
        }
        return result;
      }
      shutdown() {
        return this._transport.shutdown();
      }
    };
    __name(createRetryingTransport, "createRetryingTransport");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/otlp-http-export-delegate.js
function createOtlpHttpExportDelegate(options, serializer) {
  return createOtlpExportDelegate({
    transport: createRetryingTransport({
      transport: createHttpExporterTransport(options)
    }),
    serializer,
    promiseHandler: createBoundedQueueExportPromiseHandler(options)
  }, { timeout: options.timeoutMillis });
}
var init_otlp_http_export_delegate = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/otlp-http-export-delegate.js"() {
    init_esm();
    init_otlp_export_delegate();
    init_http_exporter_transport();
    init_bounded_queue_export_promise_handler();
    init_retrying_transport();
    __name(createOtlpHttpExportDelegate, "createOtlpHttpExportDelegate");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/shared-env-configuration.js
function parseAndValidateTimeoutFromEnv(timeoutEnvVar) {
  const envTimeout = (0, import_core2.getNumberFromEnv)(timeoutEnvVar);
  if (envTimeout != null) {
    if (Number.isFinite(envTimeout) && envTimeout > 0) {
      return envTimeout;
    }
    diag.warn(`Configuration: ${timeoutEnvVar} is invalid, expected number greater than 0 (actual: ${envTimeout})`);
  }
  return void 0;
}
function getTimeoutFromEnv(signalIdentifier) {
  const specificTimeout = parseAndValidateTimeoutFromEnv(`OTEL_EXPORTER_OTLP_${signalIdentifier}_TIMEOUT`);
  const nonSpecificTimeout = parseAndValidateTimeoutFromEnv("OTEL_EXPORTER_OTLP_TIMEOUT");
  return specificTimeout ?? nonSpecificTimeout;
}
function parseAndValidateCompressionFromEnv(compressionEnvVar) {
  const compression = (0, import_core2.getStringFromEnv)(compressionEnvVar)?.trim();
  if (compression == null || compression === "none" || compression === "gzip") {
    return compression;
  }
  diag.warn(`Configuration: ${compressionEnvVar} is invalid, expected 'none' or 'gzip' (actual: '${compression}')`);
  return void 0;
}
function getCompressionFromEnv(signalIdentifier) {
  const specificCompression = parseAndValidateCompressionFromEnv(`OTEL_EXPORTER_OTLP_${signalIdentifier}_COMPRESSION`);
  const nonSpecificCompression = parseAndValidateCompressionFromEnv("OTEL_EXPORTER_OTLP_COMPRESSION");
  return specificCompression ?? nonSpecificCompression;
}
function getSharedConfigurationFromEnvironment(signalIdentifier) {
  return {
    timeoutMillis: getTimeoutFromEnv(signalIdentifier),
    compression: getCompressionFromEnv(signalIdentifier)
  };
}
var import_core2;
var init_shared_env_configuration = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/shared-env-configuration.js"() {
    init_esm();
    import_core2 = __toESM(require_src());
    init_esm2();
    __name(parseAndValidateTimeoutFromEnv, "parseAndValidateTimeoutFromEnv");
    __name(getTimeoutFromEnv, "getTimeoutFromEnv");
    __name(parseAndValidateCompressionFromEnv, "parseAndValidateCompressionFromEnv");
    __name(getCompressionFromEnv, "getCompressionFromEnv");
    __name(getSharedConfigurationFromEnvironment, "getSharedConfigurationFromEnvironment");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/otlp-node-http-env-configuration.js
import * as fs from "fs";
import * as path from "path";
function getStaticHeadersFromEnv(signalIdentifier) {
  const signalSpecificRawHeaders = (0, import_core3.getStringFromEnv)(`OTEL_EXPORTER_OTLP_${signalIdentifier}_HEADERS`);
  const nonSignalSpecificRawHeaders = (0, import_core3.getStringFromEnv)("OTEL_EXPORTER_OTLP_HEADERS");
  const signalSpecificHeaders = (0, import_core3.parseKeyPairsIntoRecord)(signalSpecificRawHeaders);
  const nonSignalSpecificHeaders = (0, import_core3.parseKeyPairsIntoRecord)(nonSignalSpecificRawHeaders);
  if (Object.keys(signalSpecificHeaders).length === 0 && Object.keys(nonSignalSpecificHeaders).length === 0) {
    return void 0;
  }
  return Object.assign({}, (0, import_core3.parseKeyPairsIntoRecord)(nonSignalSpecificRawHeaders), (0, import_core3.parseKeyPairsIntoRecord)(signalSpecificRawHeaders));
}
function appendRootPathToUrlIfNeeded(url) {
  try {
    const parsedUrl = new URL(url);
    return parsedUrl.toString();
  } catch {
    diag.warn(`Configuration: Could not parse environment-provided export URL: '${url}', falling back to undefined`);
    return void 0;
  }
}
function appendResourcePathToUrl(url, path2) {
  try {
    new URL(url);
  } catch {
    diag.warn(`Configuration: Could not parse environment-provided export URL: '${url}', falling back to undefined`);
    return void 0;
  }
  if (!url.endsWith("/")) {
    url = url + "/";
  }
  url += path2;
  try {
    new URL(url);
  } catch {
    diag.warn(`Configuration: Provided URL appended with '${path2}' is not a valid URL, using 'undefined' instead of '${url}'`);
    return void 0;
  }
  return url;
}
function getNonSpecificUrlFromEnv(signalResourcePath) {
  const envUrl = (0, import_core3.getStringFromEnv)("OTEL_EXPORTER_OTLP_ENDPOINT");
  if (envUrl === void 0) {
    return void 0;
  }
  return appendResourcePathToUrl(envUrl, signalResourcePath);
}
function getSpecificUrlFromEnv(signalIdentifier) {
  const envUrl = (0, import_core3.getStringFromEnv)(`OTEL_EXPORTER_OTLP_${signalIdentifier}_ENDPOINT`);
  if (envUrl === void 0) {
    return void 0;
  }
  return appendRootPathToUrlIfNeeded(envUrl);
}
function readFileFromEnv(signalSpecificEnvVar, nonSignalSpecificEnvVar, warningMessage) {
  const signalSpecificPath = (0, import_core3.getStringFromEnv)(signalSpecificEnvVar);
  const nonSignalSpecificPath = (0, import_core3.getStringFromEnv)(nonSignalSpecificEnvVar);
  const filePath = signalSpecificPath ?? nonSignalSpecificPath;
  if (filePath != null) {
    try {
      return fs.readFileSync(path.resolve(process.cwd(), filePath));
    } catch {
      diag.warn(warningMessage);
      return void 0;
    }
  } else {
    return void 0;
  }
}
function getClientCertificateFromEnv(signalIdentifier) {
  return readFileFromEnv(`OTEL_EXPORTER_OTLP_${signalIdentifier}_CLIENT_CERTIFICATE`, "OTEL_EXPORTER_OTLP_CLIENT_CERTIFICATE", "Failed to read client certificate chain file");
}
function getClientKeyFromEnv(signalIdentifier) {
  return readFileFromEnv(`OTEL_EXPORTER_OTLP_${signalIdentifier}_CLIENT_KEY`, "OTEL_EXPORTER_OTLP_CLIENT_KEY", "Failed to read client certificate private key file");
}
function getRootCertificateFromEnv(signalIdentifier) {
  return readFileFromEnv(`OTEL_EXPORTER_OTLP_${signalIdentifier}_CERTIFICATE`, "OTEL_EXPORTER_OTLP_CERTIFICATE", "Failed to read root certificate file");
}
function getNodeHttpConfigurationFromEnvironment(signalIdentifier, signalResourcePath) {
  return {
    ...getSharedConfigurationFromEnvironment(signalIdentifier),
    url: getSpecificUrlFromEnv(signalIdentifier) ?? getNonSpecificUrlFromEnv(signalResourcePath),
    headers: wrapStaticHeadersInFunction(getStaticHeadersFromEnv(signalIdentifier)),
    agentFactory: httpAgentFactoryFromOptions({
      keepAlive: true,
      ca: getRootCertificateFromEnv(signalIdentifier),
      cert: getClientCertificateFromEnv(signalIdentifier),
      key: getClientKeyFromEnv(signalIdentifier)
    })
  };
}
var import_core3;
var init_otlp_node_http_env_configuration = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/otlp-node-http-env-configuration.js"() {
    init_esm();
    import_core3 = __toESM(require_src());
    init_esm2();
    init_shared_env_configuration();
    init_shared_configuration();
    init_otlp_node_http_configuration();
    __name(getStaticHeadersFromEnv, "getStaticHeadersFromEnv");
    __name(appendRootPathToUrlIfNeeded, "appendRootPathToUrlIfNeeded");
    __name(appendResourcePathToUrl, "appendResourcePathToUrl");
    __name(getNonSpecificUrlFromEnv, "getNonSpecificUrlFromEnv");
    __name(getSpecificUrlFromEnv, "getSpecificUrlFromEnv");
    __name(readFileFromEnv, "readFileFromEnv");
    __name(getClientCertificateFromEnv, "getClientCertificateFromEnv");
    __name(getClientKeyFromEnv, "getClientKeyFromEnv");
    __name(getRootCertificateFromEnv, "getRootCertificateFromEnv");
    __name(getNodeHttpConfigurationFromEnvironment, "getNodeHttpConfigurationFromEnvironment");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/convert-legacy-http-options.js
function convertLegacyHeaders(config) {
  if (typeof config.headers === "function") {
    return config.headers;
  }
  return wrapStaticHeadersInFunction(config.headers);
}
var init_convert_legacy_http_options = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/convert-legacy-http-options.js"() {
    init_esm();
    init_shared_configuration();
    __name(convertLegacyHeaders, "convertLegacyHeaders");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/convert-legacy-node-http-options.js
function convertLegacyAgentOptions(config) {
  if (typeof config.httpAgentOptions === "function") {
    return config.httpAgentOptions;
  }
  let legacy = config.httpAgentOptions;
  if (config.keepAlive != null) {
    legacy = { keepAlive: config.keepAlive, ...legacy };
  }
  if (legacy != null) {
    return httpAgentFactoryFromOptions(legacy);
  } else {
    return void 0;
  }
}
function convertLegacyHttpOptions(config, signalIdentifier, signalResourcePath, requiredHeaders) {
  if (config.metadata) {
    diag.warn("Metadata cannot be set when using http");
  }
  return mergeOtlpNodeHttpConfigurationWithDefaults({
    url: config.url,
    headers: convertLegacyHeaders(config),
    concurrencyLimit: config.concurrencyLimit,
    timeoutMillis: config.timeoutMillis,
    compression: config.compression,
    agentFactory: convertLegacyAgentOptions(config),
    userAgent: config.userAgent
  }, getNodeHttpConfigurationFromEnvironment(signalIdentifier, signalResourcePath), getNodeHttpConfigurationDefaults(requiredHeaders, signalResourcePath));
}
var init_convert_legacy_node_http_options = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/configuration/convert-legacy-node-http-options.js"() {
    init_esm();
    init_esm2();
    init_otlp_node_http_configuration();
    init_index_node_http();
    init_otlp_node_http_env_configuration();
    init_convert_legacy_http_options();
    __name(convertLegacyAgentOptions, "convertLegacyAgentOptions");
    __name(convertLegacyHttpOptions, "convertLegacyHttpOptions");
  }
});

// ../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/index-node-http.js
var index_node_http_exports = {};
__export(index_node_http_exports, {
  convertLegacyHttpOptions: () => convertLegacyHttpOptions,
  createOtlpHttpExportDelegate: () => createOtlpHttpExportDelegate,
  getSharedConfigurationFromEnvironment: () => getSharedConfigurationFromEnvironment,
  httpAgentFactoryFromOptions: () => httpAgentFactoryFromOptions
});
var init_index_node_http = __esm({
  "../../node_modules/.bun/@opentelemetry+otlp-exporter-base@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/otlp-exporter-base/build/esm/index-node-http.js"() {
    init_esm();
    init_otlp_node_http_configuration();
    init_otlp_http_export_delegate();
    init_shared_env_configuration();
    init_convert_legacy_node_http_options();
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/platform/node/OTLPLogExporter.js
var require_OTLPLogExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/platform/node/OTLPLogExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPLogExporter = void 0;
    var otlp_exporter_base_1 = (init_esm4(), __toCommonJS(esm_exports3));
    var otlp_transformer_1 = require_src7();
    var node_http_1 = (init_index_node_http(), __toCommonJS(index_node_http_exports));
    var OTLPLogExporter2 = class extends otlp_exporter_base_1.OTLPExporterBase {
      static {
        __name(this, "OTLPLogExporter");
      }
      constructor(config = {}) {
        super((0, node_http_1.createOtlpHttpExportDelegate)((0, node_http_1.convertLegacyHttpOptions)(config, "LOGS", "v1/logs", {
          "Content-Type": "application/json"
        }), otlp_transformer_1.JsonLogsSerializer));
      }
    };
    exports.OTLPLogExporter = OTLPLogExporter2;
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/platform/node/index.js
var require_node2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/platform/node/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPLogExporter = void 0;
    var OTLPLogExporter_1 = require_OTLPLogExporter();
    Object.defineProperty(exports, "OTLPLogExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPLogExporter_1.OTLPLogExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/platform/index.js
var require_platform2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/platform/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPLogExporter = void 0;
    var node_1 = require_node2();
    Object.defineProperty(exports, "OTLPLogExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.OTLPLogExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/index.js
var require_src8 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-logs-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-logs-otlp-http/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPLogExporter = void 0;
    var platform_1 = require_platform2();
    Object.defineProperty(exports, "OTLPLogExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.OTLPLogExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/platform/node/OTLPTraceExporter.js
var require_OTLPTraceExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/platform/node/OTLPTraceExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPTraceExporter = void 0;
    var otlp_exporter_base_1 = (init_esm4(), __toCommonJS(esm_exports3));
    var otlp_transformer_1 = require_src7();
    var node_http_1 = (init_index_node_http(), __toCommonJS(index_node_http_exports));
    var OTLPTraceExporter2 = class extends otlp_exporter_base_1.OTLPExporterBase {
      static {
        __name(this, "OTLPTraceExporter");
      }
      constructor(config = {}) {
        super((0, node_http_1.createOtlpHttpExportDelegate)((0, node_http_1.convertLegacyHttpOptions)(config, "TRACES", "v1/traces", {
          "Content-Type": "application/json"
        }), otlp_transformer_1.JsonTraceSerializer));
      }
    };
    exports.OTLPTraceExporter = OTLPTraceExporter2;
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/platform/node/index.js
var require_node3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/platform/node/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPTraceExporter = void 0;
    var OTLPTraceExporter_1 = require_OTLPTraceExporter();
    Object.defineProperty(exports, "OTLPTraceExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPTraceExporter_1.OTLPTraceExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/platform/index.js
var require_platform3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/platform/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPTraceExporter = void 0;
    var node_1 = require_node3();
    Object.defineProperty(exports, "OTLPTraceExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.OTLPTraceExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/index.js
var require_src9 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-trace-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-trace-otlp-http/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPTraceExporter = void 0;
    var platform_1 = require_platform3();
    Object.defineProperty(exports, "OTLPTraceExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.OTLPTraceExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/OTLPMetricExporterOptions.js
var require_OTLPMetricExporterOptions = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/OTLPMetricExporterOptions.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AggregationTemporalityPreference = void 0;
    var AggregationTemporalityPreference;
    (function(AggregationTemporalityPreference2) {
      AggregationTemporalityPreference2[AggregationTemporalityPreference2["DELTA"] = 0] = "DELTA";
      AggregationTemporalityPreference2[AggregationTemporalityPreference2["CUMULATIVE"] = 1] = "CUMULATIVE";
      AggregationTemporalityPreference2[AggregationTemporalityPreference2["LOWMEMORY"] = 2] = "LOWMEMORY";
    })(AggregationTemporalityPreference = exports.AggregationTemporalityPreference || (exports.AggregationTemporalityPreference = {}));
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/OTLPMetricExporterBase.js
var require_OTLPMetricExporterBase = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/OTLPMetricExporterBase.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPMetricExporterBase = exports.LowMemoryTemporalitySelector = exports.DeltaTemporalitySelector = exports.CumulativeTemporalitySelector = void 0;
    var core_1 = require_src();
    var sdk_metrics_1 = require_src6();
    var OTLPMetricExporterOptions_1 = require_OTLPMetricExporterOptions();
    var otlp_exporter_base_1 = (init_esm4(), __toCommonJS(esm_exports3));
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var CumulativeTemporalitySelector = /* @__PURE__ */ __name(() => sdk_metrics_1.AggregationTemporality.CUMULATIVE, "CumulativeTemporalitySelector");
    exports.CumulativeTemporalitySelector = CumulativeTemporalitySelector;
    var DeltaTemporalitySelector = /* @__PURE__ */ __name((instrumentType) => {
      switch (instrumentType) {
        case sdk_metrics_1.InstrumentType.COUNTER:
        case sdk_metrics_1.InstrumentType.OBSERVABLE_COUNTER:
        case sdk_metrics_1.InstrumentType.GAUGE:
        case sdk_metrics_1.InstrumentType.HISTOGRAM:
        case sdk_metrics_1.InstrumentType.OBSERVABLE_GAUGE:
          return sdk_metrics_1.AggregationTemporality.DELTA;
        case sdk_metrics_1.InstrumentType.UP_DOWN_COUNTER:
        case sdk_metrics_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER:
          return sdk_metrics_1.AggregationTemporality.CUMULATIVE;
      }
    }, "DeltaTemporalitySelector");
    exports.DeltaTemporalitySelector = DeltaTemporalitySelector;
    var LowMemoryTemporalitySelector = /* @__PURE__ */ __name((instrumentType) => {
      switch (instrumentType) {
        case sdk_metrics_1.InstrumentType.COUNTER:
        case sdk_metrics_1.InstrumentType.HISTOGRAM:
          return sdk_metrics_1.AggregationTemporality.DELTA;
        case sdk_metrics_1.InstrumentType.GAUGE:
        case sdk_metrics_1.InstrumentType.UP_DOWN_COUNTER:
        case sdk_metrics_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER:
        case sdk_metrics_1.InstrumentType.OBSERVABLE_COUNTER:
        case sdk_metrics_1.InstrumentType.OBSERVABLE_GAUGE:
          return sdk_metrics_1.AggregationTemporality.CUMULATIVE;
      }
    }, "LowMemoryTemporalitySelector");
    exports.LowMemoryTemporalitySelector = LowMemoryTemporalitySelector;
    function chooseTemporalitySelectorFromEnvironment() {
      const configuredTemporality = ((0, core_1.getStringFromEnv)("OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE") ?? "cumulative").toLowerCase();
      if (configuredTemporality === "cumulative") {
        return exports.CumulativeTemporalitySelector;
      }
      if (configuredTemporality === "delta") {
        return exports.DeltaTemporalitySelector;
      }
      if (configuredTemporality === "lowmemory") {
        return exports.LowMemoryTemporalitySelector;
      }
      api_1.diag.warn(`OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE is set to '${configuredTemporality}', but only 'cumulative' and 'delta' are allowed. Using default ('cumulative') instead.`);
      return exports.CumulativeTemporalitySelector;
    }
    __name(chooseTemporalitySelectorFromEnvironment, "chooseTemporalitySelectorFromEnvironment");
    function chooseTemporalitySelector(temporalityPreference) {
      if (temporalityPreference != null) {
        if (temporalityPreference === OTLPMetricExporterOptions_1.AggregationTemporalityPreference.DELTA) {
          return exports.DeltaTemporalitySelector;
        } else if (temporalityPreference === OTLPMetricExporterOptions_1.AggregationTemporalityPreference.LOWMEMORY) {
          return exports.LowMemoryTemporalitySelector;
        }
        return exports.CumulativeTemporalitySelector;
      }
      return chooseTemporalitySelectorFromEnvironment();
    }
    __name(chooseTemporalitySelector, "chooseTemporalitySelector");
    var DEFAULT_AGGREGATION = Object.freeze({
      type: sdk_metrics_1.AggregationType.DEFAULT
    });
    function chooseAggregationSelector(config) {
      return config?.aggregationPreference ?? (() => DEFAULT_AGGREGATION);
    }
    __name(chooseAggregationSelector, "chooseAggregationSelector");
    var OTLPMetricExporterBase = class extends otlp_exporter_base_1.OTLPExporterBase {
      static {
        __name(this, "OTLPMetricExporterBase");
      }
      _aggregationTemporalitySelector;
      _aggregationSelector;
      constructor(delegate, config) {
        super(delegate);
        this._aggregationSelector = chooseAggregationSelector(config);
        this._aggregationTemporalitySelector = chooseTemporalitySelector(config?.temporalityPreference);
      }
      selectAggregation(instrumentType) {
        return this._aggregationSelector(instrumentType);
      }
      selectAggregationTemporality(instrumentType) {
        return this._aggregationTemporalitySelector(instrumentType);
      }
    };
    exports.OTLPMetricExporterBase = OTLPMetricExporterBase;
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/platform/node/OTLPMetricExporter.js
var require_OTLPMetricExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/platform/node/OTLPMetricExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPMetricExporter = void 0;
    var OTLPMetricExporterBase_1 = require_OTLPMetricExporterBase();
    var otlp_transformer_1 = require_src7();
    var node_http_1 = (init_index_node_http(), __toCommonJS(index_node_http_exports));
    var OTLPMetricExporter2 = class extends OTLPMetricExporterBase_1.OTLPMetricExporterBase {
      static {
        __name(this, "OTLPMetricExporter");
      }
      constructor(config) {
        super((0, node_http_1.createOtlpHttpExportDelegate)((0, node_http_1.convertLegacyHttpOptions)(config ?? {}, "METRICS", "v1/metrics", {
          "Content-Type": "application/json"
        }), otlp_transformer_1.JsonMetricsSerializer), config);
      }
    };
    exports.OTLPMetricExporter = OTLPMetricExporter2;
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/platform/node/index.js
var require_node4 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/platform/node/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPMetricExporter = void 0;
    var OTLPMetricExporter_1 = require_OTLPMetricExporter();
    Object.defineProperty(exports, "OTLPMetricExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPMetricExporter_1.OTLPMetricExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/platform/index.js
var require_platform4 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/platform/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPMetricExporter = void 0;
    var node_1 = require_node4();
    Object.defineProperty(exports, "OTLPMetricExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.OTLPMetricExporter;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/index.js
var require_src10 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+exporter-metrics-otlp-http@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/exporter-metrics-otlp-http/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OTLPMetricExporterBase = exports.LowMemoryTemporalitySelector = exports.DeltaTemporalitySelector = exports.CumulativeTemporalitySelector = exports.AggregationTemporalityPreference = exports.OTLPMetricExporter = void 0;
    var platform_1 = require_platform4();
    Object.defineProperty(exports, "OTLPMetricExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.OTLPMetricExporter;
    }, "get") });
    var OTLPMetricExporterOptions_1 = require_OTLPMetricExporterOptions();
    Object.defineProperty(exports, "AggregationTemporalityPreference", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPMetricExporterOptions_1.AggregationTemporalityPreference;
    }, "get") });
    var OTLPMetricExporterBase_1 = require_OTLPMetricExporterBase();
    Object.defineProperty(exports, "CumulativeTemporalitySelector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPMetricExporterBase_1.CumulativeTemporalitySelector;
    }, "get") });
    Object.defineProperty(exports, "DeltaTemporalitySelector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPMetricExporterBase_1.DeltaTemporalitySelector;
    }, "get") });
    Object.defineProperty(exports, "LowMemoryTemporalitySelector", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPMetricExporterBase_1.LowMemoryTemporalitySelector;
    }, "get") });
    Object.defineProperty(exports, "OTLPMetricExporterBase", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return OTLPMetricExporterBase_1.OTLPMetricExporterBase;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/version.js
var require_version = __commonJS({
  "../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/version.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PACKAGE_NAME = exports.PACKAGE_VERSION = void 0;
    exports.PACKAGE_VERSION = "0.38.3";
    exports.PACKAGE_NAME = "@opentelemetry/host-metrics";
  }
});

// ../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/BaseMetrics.js
var require_BaseMetrics = __commonJS({
  "../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/BaseMetrics.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BaseMetrics = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var version_1 = require_version();
    var DEFAULT_NAME = version_1.PACKAGE_NAME;
    var BaseMetrics = class {
      static {
        __name(this, "BaseMetrics");
      }
      _meter;
      _name;
      _metricGroups;
      constructor(config) {
        this._name = config?.name || DEFAULT_NAME;
        const meterProvider = config?.meterProvider ?? api_1.metrics.getMeterProvider();
        this._meter = meterProvider.getMeter(this._name, version_1.PACKAGE_VERSION);
        this._metricGroups = config?.metricGroups;
      }
    };
    exports.BaseMetrics = BaseMetrics;
  }
});

// ../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/semconv.js
var require_semconv2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/semconv.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SYSTEM_MEMORY_STATE_VALUE_USED = exports.SYSTEM_MEMORY_STATE_VALUE_FREE = exports.SYSTEM_CPU_STATE_VALUE_USER = exports.SYSTEM_CPU_STATE_VALUE_SYSTEM = exports.SYSTEM_CPU_STATE_VALUE_NICE = exports.SYSTEM_CPU_STATE_VALUE_INTERRUPT = exports.SYSTEM_CPU_STATE_VALUE_IDLE = exports.PROCESS_CPU_STATE_VALUE_USER = exports.PROCESS_CPU_STATE_VALUE_SYSTEM = exports.NETWORK_IO_DIRECTION_VALUE_TRANSMIT = exports.NETWORK_IO_DIRECTION_VALUE_RECEIVE = exports.METRIC_SYSTEM_NETWORK_IO = exports.METRIC_SYSTEM_NETWORK_ERRORS = exports.METRIC_SYSTEM_MEMORY_UTILIZATION = exports.METRIC_SYSTEM_MEMORY_USAGE = exports.METRIC_SYSTEM_CPU_UTILIZATION = exports.METRIC_SYSTEM_CPU_TIME = exports.METRIC_PROCESS_MEMORY_USAGE = exports.METRIC_PROCESS_CPU_UTILIZATION = exports.METRIC_PROCESS_CPU_TIME = exports.ATTR_SYSTEM_MEMORY_STATE = exports.ATTR_SYSTEM_DEVICE = exports.ATTR_SYSTEM_CPU_STATE = exports.ATTR_SYSTEM_CPU_LOGICAL_NUMBER = exports.ATTR_PROCESS_CPU_STATE = exports.ATTR_NETWORK_IO_DIRECTION = void 0;
    exports.ATTR_NETWORK_IO_DIRECTION = "network.io.direction";
    exports.ATTR_PROCESS_CPU_STATE = "process.cpu.state";
    exports.ATTR_SYSTEM_CPU_LOGICAL_NUMBER = "system.cpu.logical_number";
    exports.ATTR_SYSTEM_CPU_STATE = "system.cpu.state";
    exports.ATTR_SYSTEM_DEVICE = "system.device";
    exports.ATTR_SYSTEM_MEMORY_STATE = "system.memory.state";
    exports.METRIC_PROCESS_CPU_TIME = "process.cpu.time";
    exports.METRIC_PROCESS_CPU_UTILIZATION = "process.cpu.utilization";
    exports.METRIC_PROCESS_MEMORY_USAGE = "process.memory.usage";
    exports.METRIC_SYSTEM_CPU_TIME = "system.cpu.time";
    exports.METRIC_SYSTEM_CPU_UTILIZATION = "system.cpu.utilization";
    exports.METRIC_SYSTEM_MEMORY_USAGE = "system.memory.usage";
    exports.METRIC_SYSTEM_MEMORY_UTILIZATION = "system.memory.utilization";
    exports.METRIC_SYSTEM_NETWORK_ERRORS = "system.network.errors";
    exports.METRIC_SYSTEM_NETWORK_IO = "system.network.io";
    exports.NETWORK_IO_DIRECTION_VALUE_RECEIVE = "receive";
    exports.NETWORK_IO_DIRECTION_VALUE_TRANSMIT = "transmit";
    exports.PROCESS_CPU_STATE_VALUE_SYSTEM = "system";
    exports.PROCESS_CPU_STATE_VALUE_USER = "user";
    exports.SYSTEM_CPU_STATE_VALUE_IDLE = "idle";
    exports.SYSTEM_CPU_STATE_VALUE_INTERRUPT = "interrupt";
    exports.SYSTEM_CPU_STATE_VALUE_NICE = "nice";
    exports.SYSTEM_CPU_STATE_VALUE_SYSTEM = "system";
    exports.SYSTEM_CPU_STATE_VALUE_USER = "user";
    exports.SYSTEM_MEMORY_STATE_VALUE_FREE = "free";
    exports.SYSTEM_MEMORY_STATE_VALUE_USED = "used";
  }
});

// ../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/stats/common.js
var require_common = __commonJS({
  "../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/stats/common.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getProcessMemoryData = exports.getMemoryData = exports.getProcessCpuUsageData = exports.getCpuUsageData = void 0;
    var os_1 = __require("os");
    var MILLISECOND = 1 / 1e3;
    var MICROSECOND = 1 / 1e6;
    var prevOsData = {
      time: Date.now(),
      cpus: (0, os_1.cpus)()
    };
    function getCpuUsageData() {
      const currentTime = Date.now();
      const timeElapsed = currentTime - prevOsData.time;
      const currentOsData = { time: currentTime, cpus: (0, os_1.cpus)() };
      const usageData = currentOsData.cpus.map((cpu, cpuNumber) => {
        const prevTimes = prevOsData.cpus[cpuNumber].times;
        const currTimes = cpu.times;
        const idle = currTimes.idle * MILLISECOND;
        const user = currTimes.user * MILLISECOND;
        const system = currTimes.sys * MILLISECOND;
        const interrupt = currTimes.irq * MILLISECOND;
        const nice = currTimes.nice * MILLISECOND;
        const idleP = (currTimes.idle - prevTimes.idle) / timeElapsed;
        const userP = (currTimes.user - prevTimes.user) / timeElapsed;
        const systemP = (currTimes.sys - prevTimes.sys) / timeElapsed;
        const interruptP = (currTimes.irq - prevTimes.irq) / timeElapsed;
        const niceP = (currTimes.nice - prevTimes.nice) / timeElapsed;
        return {
          cpuNumber: String(cpuNumber),
          idle,
          user,
          system,
          interrupt,
          nice,
          userP,
          systemP,
          idleP,
          interruptP,
          niceP
        };
      });
      prevOsData = currentOsData;
      return usageData;
    }
    __name(getCpuUsageData, "getCpuUsageData");
    exports.getCpuUsageData = getCpuUsageData;
    var prevProcData = {
      time: Date.now(),
      usage: process.cpuUsage()
    };
    function getProcessCpuUsageData() {
      const currentTime = Date.now();
      const currentUsage = process.cpuUsage();
      const prevUsage = prevProcData.usage;
      const timeElapsed = (currentTime - prevProcData.time) * 1e3;
      const cpusTimeElapsed = timeElapsed * prevOsData.cpus.length;
      const user = currentUsage.user * MICROSECOND;
      const system = currentUsage.system * MICROSECOND;
      const userP = (currentUsage.user - prevUsage.user) / cpusTimeElapsed;
      const systemP = (currentUsage.system - prevUsage.system) / cpusTimeElapsed;
      prevProcData = { time: currentTime, usage: currentUsage };
      return {
        user,
        system,
        userP,
        systemP
      };
    }
    __name(getProcessCpuUsageData, "getProcessCpuUsageData");
    exports.getProcessCpuUsageData = getProcessCpuUsageData;
    function getMemoryData() {
      const total = (0, os_1.totalmem)();
      const free = (0, os_1.freemem)();
      const used = total - free;
      const freeP = free / total;
      const usedP = used / total;
      return {
        used,
        free,
        usedP,
        freeP
        // this is frac part (0-1)
      };
    }
    __name(getMemoryData, "getMemoryData");
    exports.getMemoryData = getMemoryData;
    function getProcessMemoryData() {
      if (process.memoryUsage.rss) {
        return process.memoryUsage.rss();
      }
      return process.memoryUsage().rss;
    }
    __name(getProcessMemoryData, "getProcessMemoryData");
    exports.getProcessMemoryData = getProcessMemoryData;
  }
});

// ../../node_modules/.bun/systeminformation@5.33.1/node_modules/systeminformation/lib/util.js
var require_util2 = __commonJS({
  "../../node_modules/.bun/systeminformation@5.33.1/node_modules/systeminformation/lib/util.js"(exports) {
    "use strict";
    init_esm();
    var os = __require("os");
    var fs4 = __require("fs");
    var path2 = __require("path");
    var spawn = __require("child_process").spawn;
    var exec = __require("child_process").exec;
    var execSync = __require("child_process").execSync;
    var util2 = __require("util");
    var _platform = process.platform;
    var _linux = _platform === "linux" || _platform === "android";
    var _darwin = _platform === "darwin";
    var _windows = _platform === "win32";
    var _freebsd = _platform === "freebsd";
    var _openbsd = _platform === "openbsd";
    var _netbsd = _platform === "netbsd";
    var _cores = 0;
    var codepage = "";
    var _smartMonToolsInstalled = null;
    var _rpi_cpuinfo = null;
    var WINDIR = process.env.WINDIR || "C:\\Windows";
    var _psChild;
    var _psResult = "";
    var _psCmds = [];
    var _psPersistent = false;
    var _powerShell = "";
    var _psToUTF8 = "$OutputEncoding = [System.Console]::OutputEncoding = [System.Console]::InputEncoding = [System.Text.Encoding]::UTF8 ; ";
    var _psCmdStart = "--###START###--";
    var _psError = "--ERROR--";
    var _psCmdSeperator = "--###ENDCMD###--";
    var _psIdSeperator = "--##ID##--";
    var execOptsWin = {
      windowsHide: true,
      maxBuffer: 1024 * 102400,
      encoding: "UTF-8",
      env: Object.assign({}, process.env, { LANG: "en_US.UTF-8" })
    };
    var execOptsLinux = {
      maxBuffer: 1024 * 102400,
      encoding: "UTF-8",
      stdio: ["pipe", "pipe", "ignore"]
    };
    function toInt(value2) {
      let result = parseInt(value2, 10);
      if (isNaN(result)) {
        result = 0;
      }
      return result;
    }
    __name(toInt, "toInt");
    function splitByNumber(str) {
      let numberStarted = false;
      let num = "";
      let cpart = "";
      for (const c of str) {
        if (c >= "0" && c <= "9" || numberStarted) {
          numberStarted = true;
          num += c;
        } else {
          cpart += c;
        }
      }
      return [cpart, num];
    }
    __name(splitByNumber, "splitByNumber");
    var stringObj = new String();
    var stringReplace = new String().replace;
    var stringToLower = new String().toLowerCase;
    var stringToString = new String().toString;
    var stringSubstr = new String().substr;
    var stringSubstring = new String().substring;
    var stringTrim = new String().trim;
    var stringStartWith = new String().startsWith;
    var mathMin = Math.min;
    function isFunction(functionToCheck) {
      let getType = {};
      return functionToCheck && getType.toString.call(functionToCheck) === "[object Function]";
    }
    __name(isFunction, "isFunction");
    function unique(obj) {
      const uniques = [];
      const stringify = {};
      for (let i = 0; i < obj.length; i++) {
        let keys = Object.keys(obj[i]);
        keys.sort((a, b) => {
          return a - b;
        });
        let str = "";
        for (let j = 0; j < keys.length; j++) {
          str += JSON.stringify(keys[j]);
          str += JSON.stringify(obj[i][keys[j]]);
        }
        if (!{}.hasOwnProperty.call(stringify, str)) {
          uniques.push(obj[i]);
          stringify[str] = true;
        }
      }
      return uniques;
    }
    __name(unique, "unique");
    function sortByKey(array, keys) {
      return array.sort((a, b) => {
        let x = "";
        let y = "";
        keys.forEach((key) => {
          x = x + a[key];
          y = y + b[key];
        });
        return x < y ? -1 : x > y ? 1 : 0;
      });
    }
    __name(sortByKey, "sortByKey");
    function cores() {
      if (_cores === 0) {
        _cores = os.cpus().length;
      }
      return _cores;
    }
    __name(cores, "cores");
    function getValue(lines, property, separator, trimmed, lineMatch) {
      separator = separator || ":";
      property = property.toLowerCase();
      trimmed = trimmed || false;
      lineMatch = lineMatch || false;
      let result = "";
      lines.some((line) => {
        let lineLower = line.toLowerCase().replace(/\t/g, "");
        if (trimmed) {
          lineLower = lineLower.trim();
        }
        if (lineLower.startsWith(property) && (lineMatch ? lineLower.match(property + separator) || lineLower.match(property + " " + separator) : true)) {
          const parts = trimmed ? line.trim().split(separator) : line.split(separator);
          if (parts.length >= 2) {
            parts.shift();
            result = parts.join(separator).trim();
            return true;
          }
        }
        return false;
      });
      return result;
    }
    __name(getValue, "getValue");
    function decodeEscapeSequence(str, base) {
      base = base || 16;
      return str.replace(/\\x([0-9A-Fa-f]{2})/g, function() {
        return String.fromCharCode(parseInt(arguments[1], base));
      });
    }
    __name(decodeEscapeSequence, "decodeEscapeSequence");
    function detectSplit(str) {
      let seperator = "";
      let part = 0;
      str.split("").forEach((element) => {
        if (element >= "0" && element <= "9") {
          if (part === 1) {
            part++;
          }
        } else {
          if (part === 0) {
            part++;
          }
          if (part === 1) {
            seperator += element;
          }
        }
      });
      return seperator;
    }
    __name(detectSplit, "detectSplit");
    function parseTime(t, pmDesignator) {
      pmDesignator = pmDesignator || "";
      t = t.toUpperCase();
      let hour = 0;
      let min = 0;
      const splitter = detectSplit(t);
      const parts = t.split(splitter);
      if (parts.length >= 2) {
        if (parts[2]) {
          parts[1] += parts[2];
        }
        const p1 = (parts[1] || "").toLowerCase();
        let isPM = p1.indexOf("pm") > -1 || p1.indexOf("p.m.") > -1 || p1.indexOf("p. m.") > -1 || p1.indexOf("n") > -1 || p1.indexOf("ch") > -1 || p1.indexOf("ös") > -1 || pmDesignator && p1.indexOf(pmDesignator) > -1;
        hour = parseInt(parts[0], 10);
        min = parseInt(parts[1], 10);
        hour = isPM && hour < 12 ? hour + 12 : hour;
        return ("0" + hour).substr(-2) + ":" + ("0" + min).substr(-2);
      }
    }
    __name(parseTime, "parseTime");
    function parseDateTime(dt, culture) {
      const result = {
        date: "",
        time: ""
      };
      culture = culture || {};
      const dateFormat = (culture.dateFormat || "").toLowerCase();
      const pmDesignator = culture.pmDesignator || "";
      const parts = dt.split(" ");
      if (parts[0]) {
        if (parts[0].indexOf("/") >= 0) {
          const dtparts = parts[0].split("/");
          if (dtparts.length === 3) {
            if (dtparts[0].length === 4) {
              result.date = dtparts[0] + "-" + ("0" + dtparts[1]).substr(-2) + "-" + ("0" + dtparts[2]).substr(-2);
            } else if (dtparts[2].length === 2) {
              if (dateFormat.indexOf("/d/") > -1 || dateFormat.indexOf("/dd/") > -1) {
                result.date = "20" + dtparts[2] + "-" + ("0" + dtparts[1]).substr(-2) + "-" + ("0" + dtparts[0]).substr(-2);
              } else {
                result.date = "20" + dtparts[2] + "-" + ("0" + dtparts[1]).substr(-2) + "-" + ("0" + dtparts[0]).substr(-2);
              }
            } else {
              const isEN = dt.toLowerCase().indexOf("pm") > -1 || dt.toLowerCase().indexOf("p.m.") > -1 || dt.toLowerCase().indexOf("p. m.") > -1 || dt.toLowerCase().indexOf("am") > -1 || dt.toLowerCase().indexOf("a.m.") > -1 || dt.toLowerCase().indexOf("a. m.") > -1;
              if ((isEN || dateFormat.indexOf("/d/") > -1 || dateFormat.indexOf("/dd/") > -1) && dateFormat.indexOf("dd/") !== 0) {
                result.date = dtparts[2] + "-" + ("0" + dtparts[0]).substr(-2) + "-" + ("0" + dtparts[1]).substr(-2);
              } else {
                result.date = dtparts[2] + "-" + ("0" + dtparts[1]).substr(-2) + "-" + ("0" + dtparts[0]).substr(-2);
              }
            }
          }
        }
        if (parts[0].indexOf(".") >= 0) {
          const dtparts = parts[0].split(".");
          if (dtparts.length === 3) {
            if (dateFormat.indexOf(".d.") > -1 || dateFormat.indexOf(".dd.") > -1) {
              result.date = dtparts[2] + "-" + ("0" + dtparts[0]).substr(-2) + "-" + ("0" + dtparts[1]).substr(-2);
            } else {
              result.date = dtparts[2] + "-" + ("0" + dtparts[1]).substr(-2) + "-" + ("0" + dtparts[0]).substr(-2);
            }
          }
        }
        if (parts[0].indexOf("-") >= 0) {
          const dtparts = parts[0].split("-");
          if (dtparts.length === 3) {
            result.date = dtparts[0] + "-" + ("0" + dtparts[1]).substr(-2) + "-" + ("0" + dtparts[2]).substr(-2);
          }
        }
      }
      if (parts[1]) {
        parts.shift();
        const time = parts.join(" ");
        result.time = parseTime(time, pmDesignator);
      }
      return result;
    }
    __name(parseDateTime, "parseDateTime");
    function parseHead(head, rights) {
      let space = rights > 0;
      let count = 1;
      let from = 0;
      let to = 0;
      const result = [];
      for (let i = 0; i < head.length; i++) {
        if (count <= rights) {
          if (/\s/.test(head[i]) && !space) {
            to = i - 1;
            result.push({
              from,
              to: to + 1,
              cap: head.substring(from, to + 1)
            });
            from = to + 2;
            count++;
          }
          space = head[i] === " ";
        } else {
          if (!/\s/.test(head[i]) && space) {
            to = i - 1;
            if (from < to) {
              result.push({
                from,
                to,
                cap: head.substring(from, to)
              });
            }
            from = to + 1;
            count++;
          }
          space = head[i] === " ";
        }
      }
      to = 5e3;
      result.push({
        from,
        to,
        cap: head.substring(from, to)
      });
      let len = result.length;
      for (let i = 0; i < len; i++) {
        if (result[i].cap.replace(/\s/g, "").length === 0) {
          if (i + 1 < len) {
            result[i].to = result[i + 1].to;
            result[i].cap = result[i].cap + result[i + 1].cap;
            result.splice(i + 1, 1);
            len = len - 1;
          }
        }
      }
      return result;
    }
    __name(parseHead, "parseHead");
    function findObjectByKey(array, key, value2) {
      for (let i = 0; i < array.length; i++) {
        if (array[i][key] === value2) {
          return i;
        }
      }
      return -1;
    }
    __name(findObjectByKey, "findObjectByKey");
    function getPowershell() {
      _powerShell = "powershell.exe";
      if (_windows) {
        const defaultPath = `${WINDIR}\\system32\\WindowsPowerShell\\v1.0\\powershell.exe`;
        if (fs4.existsSync(defaultPath)) {
          _powerShell = defaultPath;
        }
      }
    }
    __name(getPowershell, "getPowershell");
    function getVboxmanage() {
      const vboxPath = (process.env.VBOX_INSTALL_PATH || process.env.VBOX_MSI_INSTALL_PATH || "").replace(/["%!^&|<>`$;\r\n]/g, "");
      return _windows ? `"${vboxPath}\\VBoxManage.exe"` : "vboxmanage";
    }
    __name(getVboxmanage, "getVboxmanage");
    function powerShellProceedResults(data) {
      let id = "";
      let parts;
      let res = "";
      if (data.indexOf(_psCmdStart) >= 0) {
        parts = data.split(_psCmdStart);
        const parts2 = parts[1].split(_psIdSeperator);
        id = parts2[0];
        if (parts2.length > 1) {
          data = parts2.slice(1).join(_psIdSeperator);
        }
      }
      if (data.indexOf(_psCmdSeperator) >= 0) {
        parts = data.split(_psCmdSeperator);
        res = parts[0];
      }
      let remove = -1;
      for (let i = 0; i < _psCmds.length; i++) {
        if (_psCmds[i].id === id) {
          remove = i;
          _psCmds[i].callback(res);
        }
      }
      if (remove >= 0) {
        _psCmds.splice(remove, 1);
      }
    }
    __name(powerShellProceedResults, "powerShellProceedResults");
    function powerShellStart() {
      if (!_psChild) {
        _psChild = spawn(_powerShell, ["-NoProfile", "-NoLogo", "-InputFormat", "Text", "-NoExit", "-Command", "-"], {
          stdio: "pipe",
          windowsHide: true,
          maxBuffer: 1024 * 102400,
          encoding: "UTF-8",
          env: Object.assign({}, process.env, { LANG: "en_US.UTF-8" })
        });
        if (_psChild && _psChild.pid) {
          _psPersistent = true;
          _psChild.stdout.on("data", (data) => {
            _psResult = _psResult + data.toString("utf8");
            if (data.indexOf(_psCmdSeperator) >= 0) {
              powerShellProceedResults(_psResult);
              _psResult = "";
            }
          });
          _psChild.stderr.on("data", () => {
            powerShellProceedResults(_psResult + _psError);
          });
          _psChild.on("error", () => {
            powerShellProceedResults(_psResult + _psError);
          });
          _psChild.on("close", () => {
            if (_psChild) {
              _psChild.kill();
            }
          });
        }
      }
    }
    __name(powerShellStart, "powerShellStart");
    function powerShellRelease() {
      try {
        if (_psChild) {
          _psChild.stdin.write("exit" + os.EOL);
          _psChild.stdin.end();
        }
      } catch {
        if (_psChild) {
          _psChild.kill();
        }
      }
      _psPersistent = false;
      _psChild = null;
    }
    __name(powerShellRelease, "powerShellRelease");
    function powerShell(cmd) {
      if (_psPersistent) {
        const id = Math.random().toString(36).substring(2, 12);
        return new Promise((resolve2) => {
          process.nextTick(() => {
            function callback(data) {
              resolve2(data);
            }
            __name(callback, "callback");
            _psCmds.push({
              id,
              cmd,
              callback,
              start: /* @__PURE__ */ new Date()
            });
            try {
              if (_psChild && _psChild.pid) {
                _psChild.stdin.write(_psToUTF8 + "echo " + _psCmdStart + id + _psIdSeperator + "; " + os.EOL + cmd + os.EOL + "echo " + _psCmdSeperator + os.EOL);
              }
            } catch {
              resolve2("");
            }
          });
        });
      } else {
        let result = "";
        return new Promise((resolve2) => {
          process.nextTick(() => {
            try {
              const osVersion = os.release().split(".").map(Number);
              const spanOptions = osVersion[0] < 10 ? ["-NoProfile", "-NoLogo", "-InputFormat", "Text", "-NoExit", "-ExecutionPolicy", "Unrestricted", "-Command", "-"] : ["-NoProfile", "-NoLogo", "-InputFormat", "Text", "-ExecutionPolicy", "Unrestricted", "-Command", _psToUTF8 + cmd];
              const child = spawn(_powerShell, spanOptions, {
                stdio: "pipe",
                windowsHide: true,
                maxBuffer: 1024 * 102400,
                encoding: "UTF-8",
                env: Object.assign({}, process.env, { LANG: "en_US.UTF-8" })
              });
              if (child && !child.pid) {
                child.on("error", () => {
                  resolve2(result);
                });
              }
              if (child && child.pid) {
                child.stdout.on("data", (data) => {
                  result = result + data.toString("utf8");
                });
                child.stderr.on("data", () => {
                  child.kill();
                  resolve2(result);
                });
                child.on("close", () => {
                  child.kill();
                  resolve2(result);
                });
                child.on("error", () => {
                  child.kill();
                  resolve2(result);
                });
                if (osVersion[0] < 10) {
                  try {
                    child.stdin.write(_psToUTF8 + cmd + os.EOL);
                    child.stdin.write("exit" + os.EOL);
                    child.stdin.end();
                  } catch {
                    child.kill();
                    resolve2(result);
                  }
                }
              } else {
                resolve2(result);
              }
            } catch {
              resolve2(result);
            }
          });
        });
      }
    }
    __name(powerShell, "powerShell");
    function execSafe(cmd, args, options) {
      let result = "";
      options = options || {};
      return new Promise((resolve2) => {
        process.nextTick(() => {
          try {
            const child = spawn(cmd, args, options);
            if (child && !child.pid) {
              child.on("error", () => {
                resolve2(result);
              });
            }
            if (child && child.pid) {
              child.stdout.on("data", (data) => {
                result += data.toString();
              });
              child.on("close", () => {
                child.kill();
                resolve2(result);
              });
              child.on("error", () => {
                child.kill();
                resolve2(result);
              });
            } else {
              resolve2(result);
            }
          } catch {
            resolve2(result);
          }
        });
      });
    }
    __name(execSafe, "execSafe");
    function getCodepage() {
      if (_windows) {
        if (!codepage) {
          try {
            const stdout = execSync("chcp", execOptsWin);
            const lines = stdout.toString().split("\r\n");
            const parts = lines[0].split(":");
            codepage = parts.length > 1 ? parts[1].replace(".", "").trim() : "";
          } catch {
            codepage = "437";
          }
        }
        return codepage;
      }
      if (_linux || _darwin || _freebsd || _openbsd || _netbsd) {
        if (!codepage) {
          try {
            const stdout = execSync("echo $LANG", execOptsLinux);
            const lines = stdout.toString().split("\r\n");
            const parts = lines[0].split(".");
            codepage = parts.length > 1 ? parts[1].trim() : "";
            if (!codepage) {
              codepage = "UTF-8";
            }
          } catch {
            codepage = "UTF-8";
          }
        }
        return codepage;
      }
    }
    __name(getCodepage, "getCodepage");
    function smartMonToolsInstalled() {
      if (_smartMonToolsInstalled !== null) {
        return _smartMonToolsInstalled;
      }
      _smartMonToolsInstalled = false;
      if (_windows) {
        try {
          const pathArray = execSync("WHERE smartctl 2>nul", execOptsWin).toString().split("\r\n");
          if (pathArray && pathArray.length) {
            _smartMonToolsInstalled = pathArray[0].indexOf(":\\") >= 0;
          } else {
            _smartMonToolsInstalled = false;
          }
        } catch {
          _smartMonToolsInstalled = false;
        }
      }
      if (_linux || _darwin || _freebsd || _openbsd || _netbsd) {
        try {
          const pathArray = execSync("which smartctl 2>/dev/null", execOptsLinux).toString().split("\r\n");
          _smartMonToolsInstalled = pathArray.length > 0;
        } catch {
          noop();
        }
      }
      return _smartMonToolsInstalled;
    }
    __name(smartMonToolsInstalled, "smartMonToolsInstalled");
    function isRaspberry(cpuinfo) {
      const PI_MODEL_NO = ["BCM2708", "BCM2709", "BCM2710", "BCM2711", "BCM2712", "BCM2835", "BCM2836", "BCM2837", "BCM2837B0"];
      if (_rpi_cpuinfo !== null) {
        cpuinfo = _rpi_cpuinfo;
      } else if (cpuinfo === void 0) {
        try {
          cpuinfo = fs4.readFileSync("/proc/cpuinfo", { encoding: "utf8" }).toString().split("\n");
          _rpi_cpuinfo = cpuinfo;
        } catch {
          return false;
        }
      }
      const hardware = getValue(cpuinfo, "hardware");
      const model = getValue(cpuinfo, "model");
      return hardware && PI_MODEL_NO.indexOf(hardware) > -1 || model && model.indexOf("Raspberry Pi") > -1;
    }
    __name(isRaspberry, "isRaspberry");
    function isRaspbian() {
      let osrelease = [];
      try {
        osrelease = fs4.readFileSync("/etc/os-release", { encoding: "utf8" }).toString().split("\n");
      } catch {
        return false;
      }
      const id = getValue(osrelease, "id", "=");
      return id && id.indexOf("raspbian") > -1;
    }
    __name(isRaspbian, "isRaspbian");
    function execWin(cmd, opts, callback) {
      if (!callback) {
        callback = opts;
        opts = execOptsWin;
      }
      let newCmd = "chcp 65001 > nul && cmd /C " + cmd + " && chcp " + codepage + " > nul";
      exec(newCmd, opts, (error, stdout) => {
        callback(error, stdout);
      });
    }
    __name(execWin, "execWin");
    function darwinXcodeExists() {
      const cmdLineToolsExists = fs4.existsSync("/Library/Developer/CommandLineTools/usr/bin/");
      const xcodeAppExists = fs4.existsSync("/Applications/Xcode.app/Contents/Developer/Tools");
      const xcodeExists = fs4.existsSync("/Library/Developer/Xcode/");
      return cmdLineToolsExists || xcodeExists || xcodeAppExists;
    }
    __name(darwinXcodeExists, "darwinXcodeExists");
    function nanoSeconds() {
      const time = process.hrtime();
      if (!Array.isArray(time) || time.length !== 2) {
        return 0;
      }
      return +time[0] * 1e9 + +time[1];
    }
    __name(nanoSeconds, "nanoSeconds");
    function countUniqueLines(lines, startingWith) {
      startingWith = startingWith || "";
      const uniqueLines = [];
      lines.forEach((line) => {
        if (line.startsWith(startingWith)) {
          if (uniqueLines.indexOf(line) === -1) {
            uniqueLines.push(line);
          }
        }
      });
      return uniqueLines.length;
    }
    __name(countUniqueLines, "countUniqueLines");
    function countLines(lines, startingWith) {
      startingWith = startingWith || "";
      const uniqueLines = [];
      lines.forEach((line) => {
        if (line.startsWith(startingWith)) {
          uniqueLines.push(line);
        }
      });
      return uniqueLines.length;
    }
    __name(countLines, "countLines");
    function sanitizeShellString(str, strict) {
      if (typeof strict === "undefined") {
        strict = false;
      }
      const s = str || "";
      let result = "";
      const l = mathMin(s.length, 2e3);
      for (let i = 0; i <= l; i++) {
        if (!(s[i] === void 0 || s[i] === ">" || s[i] === "<" || s[i] === "*" || s[i] === "?" || s[i] === "[" || s[i] === "]" || s[i] === "|" || s[i] === "˚" || s[i] === "$" || s[i] === ";" || s[i] === "&" || s[i] === "]" || s[i] === "#" || s[i] === "%" || s[i] === "!" || s[i] === "^" || s[i] === "\\" || s[i] === "	" || s[i] === "\n" || s[i] === "\r" || s[i] === "'" || s[i] === "`" || s[i] === '"' || s[i].length > 1 || strict && s[i] === "(" || strict && s[i] === ")" || strict && s[i] === "@" || strict && s[i] === " " || strict && s[i] === "{" || strict && s[i] === ";" || strict && s[i] === "}")) {
          result = result + s[i];
        }
      }
      return result;
    }
    __name(sanitizeShellString, "sanitizeShellString");
    function sanitizeContainerID(str) {
      const s = String(str || "").substring(0, 2e3).replace(/[^a-zA-Z0-9_.,*-]/g, "");
      return s.indexOf("..") === -1 ? s : "";
    }
    __name(sanitizeContainerID, "sanitizeContainerID");
    function sanitizeImageID(str) {
      const s = String(str || "").substring(0, 2e3).replace(/[^a-zA-Z0-9_.,:@/-]/g, "");
      return s.indexOf("..") === -1 ? s : "";
    }
    __name(sanitizeImageID, "sanitizeImageID");
    function isPrototypePolluted() {
      const s = "1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ";
      let notPolluted = true;
      let st = "";
      try {
        st.__proto__.replace = stringReplace;
        st.__proto__.toLowerCase = stringToLower;
        st.__proto__.toString = stringToString;
        st.__proto__.substr = stringSubstr;
        st.__proto__.substring = stringSubstring;
        st.__proto__.trim = stringTrim;
        st.__proto__.startsWith = stringStartWith;
      } catch (e) {
        Object.setPrototypeOf(st, stringObj);
      }
      notPolluted = notPolluted || s.length !== 62;
      const ms = Date.now();
      if (typeof ms === "number" && ms > 16e11) {
        const l = ms % 100 + 15;
        for (let i = 0; i < l; i++) {
          const r = Math.random() * 61.99999999 + 1;
          const rs = parseInt(Math.floor(r).toString(), 10);
          const rs2 = parseInt(r.toString().split(".")[0], 10);
          const q = Math.random() * 61.99999999 + 1;
          const qs = parseInt(Math.floor(q).toString(), 10);
          const qs2 = parseInt(q.toString().split(".")[0], 10);
          notPolluted = notPolluted && r !== q;
          notPolluted = notPolluted && rs === rs2 && qs === qs2;
          st += s[rs - 1];
        }
        notPolluted = notPolluted && st.length === l;
        let p = Math.random() * l * 0.9999999999;
        let stm = st.substr(0, p) + " " + st.substr(p, 2e3);
        try {
          stm.__proto__.replace = stringReplace;
        } catch (e) {
          Object.setPrototypeOf(stm, stringObj);
        }
        let sto = stm.replace(/ /g, "");
        notPolluted = notPolluted && st === sto;
        p = Math.random() * l * 0.9999999999;
        stm = st.substr(0, p) + "{" + st.substr(p, 2e3);
        sto = stm.replace(/{/g, "");
        notPolluted = notPolluted && st === sto;
        p = Math.random() * l * 0.9999999999;
        stm = st.substr(0, p) + "*" + st.substr(p, 2e3);
        sto = stm.replace(/\*/g, "");
        notPolluted = notPolluted && st === sto;
        p = Math.random() * l * 0.9999999999;
        stm = st.substr(0, p) + "$" + st.substr(p, 2e3);
        sto = stm.replace(/\$/g, "");
        notPolluted = notPolluted && st === sto;
        const stl = st.toLowerCase();
        notPolluted = notPolluted && stl.length === l && stl[l - 1] && !stl[l];
        for (let i = 0; i < l; i++) {
          const s1 = st[i];
          try {
            s1.__proto__.toLowerCase = stringToLower;
          } catch {
            Object.setPrototypeOf(st, stringObj);
          }
          const s2 = stl ? stl[i] : "";
          const s1l = s1.toLowerCase();
          notPolluted = notPolluted && s1l[0] === s2 && s1l[0] && !s1l[1];
        }
      }
      return !notPolluted;
    }
    __name(isPrototypePolluted, "isPrototypePolluted");
    function sanitizeString(str, strict) {
      if (typeof strict === "undefined") {
        strict = false;
      }
      let result = "";
      const s = isPrototypePolluted() ? "---" : sanitizeShellString(str, strict);
      const l = mathMin(s.length, 2e3);
      for (let i = 0; i <= l; i++) {
        if (s[i] !== void 0) {
          result = result + s[i];
        }
      }
      return result;
    }
    __name(sanitizeString, "sanitizeString");
    function hex2bin(hex) {
      return ("00000000" + parseInt(hex, 16).toString(2)).substr(-8);
    }
    __name(hex2bin, "hex2bin");
    function getFilesInPath(source) {
      const lstatSync = fs4.lstatSync;
      const readdirSync = fs4.readdirSync;
      const join = path2.join;
      function isDirectory(source2) {
        return lstatSync(source2).isDirectory();
      }
      __name(isDirectory, "isDirectory");
      function isFile(source2) {
        return lstatSync(source2).isFile();
      }
      __name(isFile, "isFile");
      function getDirectories(source2) {
        return readdirSync(source2).map((name) => {
          return join(source2, name);
        }).filter(isDirectory);
      }
      __name(getDirectories, "getDirectories");
      function getFiles(source2) {
        return readdirSync(source2).map((name) => {
          return join(source2, name);
        }).filter(isFile);
      }
      __name(getFiles, "getFiles");
      function getFilesRecursively(source2) {
        try {
          const dirs = getDirectories(source2);
          const files = dirs.map((dir) => {
            return getFilesRecursively(dir);
          }).reduce((a, b) => {
            return a.concat(b);
          }, []);
          return files.concat(getFiles(source2));
        } catch {
          return [];
        }
      }
      __name(getFilesRecursively, "getFilesRecursively");
      if (fs4.existsSync(source)) {
        return getFilesRecursively(source);
      } else {
        return [];
      }
    }
    __name(getFilesInPath, "getFilesInPath");
    function decodePiCpuinfo(lines) {
      if (_rpi_cpuinfo === null) {
        _rpi_cpuinfo = lines;
      } else if (lines === void 0) {
        lines = _rpi_cpuinfo;
      }
      const oldRevisionCodes = {
        "0002": {
          type: "B",
          revision: "1.0",
          memory: 256,
          manufacturer: "Egoman",
          processor: "BCM2835"
        },
        "0003": {
          type: "B",
          revision: "1.0",
          memory: 256,
          manufacturer: "Egoman",
          processor: "BCM2835"
        },
        "0004": {
          type: "B",
          revision: "2.0",
          memory: 256,
          manufacturer: "Sony UK",
          processor: "BCM2835"
        },
        "0005": {
          type: "B",
          revision: "2.0",
          memory: 256,
          manufacturer: "Qisda",
          processor: "BCM2835"
        },
        "0006": {
          type: "B",
          revision: "2.0",
          memory: 256,
          manufacturer: "Egoman",
          processor: "BCM2835"
        },
        "0007": {
          type: "A",
          revision: "2.0",
          memory: 256,
          manufacturer: "Egoman",
          processor: "BCM2835"
        },
        "0008": {
          type: "A",
          revision: "2.0",
          memory: 256,
          manufacturer: "Sony UK",
          processor: "BCM2835"
        },
        "0009": {
          type: "A",
          revision: "2.0",
          memory: 256,
          manufacturer: "Qisda",
          processor: "BCM2835"
        },
        "000d": {
          type: "B",
          revision: "2.0",
          memory: 512,
          manufacturer: "Egoman",
          processor: "BCM2835"
        },
        "000e": {
          type: "B",
          revision: "2.0",
          memory: 512,
          manufacturer: "Sony UK",
          processor: "BCM2835"
        },
        "000f": {
          type: "B",
          revision: "2.0",
          memory: 512,
          manufacturer: "Egoman",
          processor: "BCM2835"
        },
        "0010": {
          type: "B+",
          revision: "1.2",
          memory: 512,
          manufacturer: "Sony UK",
          processor: "BCM2835"
        },
        "0011": {
          type: "CM1",
          revision: "1.0",
          memory: 512,
          manufacturer: "Sony UK",
          processor: "BCM2835"
        },
        "0012": {
          type: "A+",
          revision: "1.1",
          memory: 256,
          manufacturer: "Sony UK",
          processor: "BCM2835"
        },
        "0013": {
          type: "B+",
          revision: "1.2",
          memory: 512,
          manufacturer: "Embest",
          processor: "BCM2835"
        },
        "0014": {
          type: "CM1",
          revision: "1.0",
          memory: 512,
          manufacturer: "Embest",
          processor: "BCM2835"
        },
        "0015": {
          type: "A+",
          revision: "1.1",
          memory: 256,
          manufacturer: "512MB	Embest",
          processor: "BCM2835"
        }
      };
      const processorList = ["BCM2835", "BCM2836", "BCM2837", "BCM2711", "BCM2712"];
      const manufacturerList = ["Sony UK", "Egoman", "Embest", "Sony Japan", "Embest", "Stadium"];
      const typeList = {
        "00": "A",
        "01": "B",
        "02": "A+",
        "03": "B+",
        "04": "2B",
        "05": "Alpha (early prototype)",
        "06": "CM1",
        "08": "3B",
        "09": "Zero",
        "0a": "CM3",
        "0c": "Zero W",
        "0d": "3B+",
        "0e": "3A+",
        "0f": "Internal use only",
        10: "CM3+",
        11: "4B",
        12: "Zero 2 W",
        13: "400",
        14: "CM4",
        15: "CM4S",
        16: "Internal use only",
        17: "5",
        18: "CM5",
        19: "500/500+",
        "1a": "CM5 Lite"
      };
      const revisionCode = getValue(lines, "revision", ":", true);
      const model = getValue(lines, "model:", ":", true);
      const serial = getValue(lines, "serial", ":", true);
      let result = {};
      if ({}.hasOwnProperty.call(oldRevisionCodes, revisionCode)) {
        result = {
          model,
          serial,
          revisionCode,
          memory: oldRevisionCodes[revisionCode].memory,
          manufacturer: oldRevisionCodes[revisionCode].manufacturer,
          processor: oldRevisionCodes[revisionCode].processor,
          type: oldRevisionCodes[revisionCode].type,
          revision: oldRevisionCodes[revisionCode].revision
        };
      } else {
        const revision = ("00000000" + getValue(lines, "revision", ":", true).toLowerCase()).substr(-8);
        const memSizeCode = parseInt(hex2bin(revision.substr(2, 1)).substr(5, 3), 2) || 0;
        const manufacturer = manufacturerList[parseInt(revision.substr(3, 1), 10)];
        const processor = processorList[parseInt(revision.substr(4, 1), 10)];
        const typeCode = revision.substr(5, 2);
        result = {
          model,
          serial,
          revisionCode,
          memory: 256 * Math.pow(2, memSizeCode),
          manufacturer,
          processor,
          type: {}.hasOwnProperty.call(typeList, typeCode) ? typeList[typeCode] : "",
          revision: "1." + revision.substr(7, 1)
        };
      }
      return result;
    }
    __name(decodePiCpuinfo, "decodePiCpuinfo");
    function getRpiGpu(cpuinfo) {
      if (_rpi_cpuinfo === null && cpuinfo !== void 0) {
        _rpi_cpuinfo = cpuinfo;
      } else if (cpuinfo === void 0 && _rpi_cpuinfo !== null) {
        cpuinfo = _rpi_cpuinfo;
      } else {
        try {
          cpuinfo = fs4.readFileSync("/proc/cpuinfo", { encoding: "utf8" }).toString().split("\n");
          _rpi_cpuinfo = cpuinfo;
        } catch {
          return false;
        }
      }
      const rpi = decodePiCpuinfo(cpuinfo);
      if (rpi.type === "4B" || rpi.type === "CM4" || rpi.type === "CM4S" || rpi.type === "400") {
        return "VideoCore VI";
      }
      if (rpi.type === "5" || rpi.type === "500") {
        return "VideoCore VII";
      }
      return "VideoCore IV";
    }
    __name(getRpiGpu, "getRpiGpu");
    function promiseAll(promises) {
      const resolvingPromises = promises.map(
        (promise) => new Promise((resolve2) => {
          const payload = new Array(2);
          promise.then((result) => {
            payload[0] = result;
          }).catch((error) => {
            payload[1] = error;
          }).then(() => {
            resolve2(payload);
          });
        })
      );
      const errors = [];
      const results = [];
      return Promise.all(resolvingPromises).then((items) => {
        items.forEach((payload) => {
          if (payload[1]) {
            errors.push(payload[1]);
            results.push(null);
          } else {
            errors.push(null);
            results.push(payload[0]);
          }
        });
        return {
          errors,
          results
        };
      });
    }
    __name(promiseAll, "promiseAll");
    function promisify(nodeStyleFunction) {
      return function() {
        const args = Array.prototype.slice.call(arguments);
        return new Promise((resolve2, reject) => {
          args.push((err2, data) => {
            if (err2) {
              reject(err2);
            } else {
              resolve2(data);
            }
          });
          nodeStyleFunction.apply(null, args);
        });
      };
    }
    __name(promisify, "promisify");
    function promisifySave(nodeStyleFunction) {
      return function() {
        const args = Array.prototype.slice.call(arguments);
        return new Promise((resolve2) => {
          args.push((err2, data) => {
            resolve2(data);
          });
          nodeStyleFunction.apply(null, args);
        });
      };
    }
    __name(promisifySave, "promisifySave");
    function linuxVersion() {
      let result = "";
      if (_linux) {
        try {
          result = execSync("uname -v", execOptsLinux).toString();
        } catch {
          result = "";
        }
      }
      return result;
    }
    __name(linuxVersion, "linuxVersion");
    function plistParser(xmlStr) {
      const tags = ["array", "dict", "key", "string", "integer", "date", "real", "data", "boolean", "arrayEmpty"];
      const startStr = "<plist version";
      let pos = xmlStr.indexOf(startStr);
      let len = xmlStr.length;
      while (xmlStr[pos] !== ">" && pos < len) {
        pos++;
      }
      let depth = 0;
      let inTagStart = false;
      let inTagContent = false;
      let inTagEnd = false;
      let metaData = [{ tagStart: "", tagEnd: "", tagContent: "", key: "", data: null }];
      let c = "";
      let cn = xmlStr[pos];
      while (pos < len) {
        c = cn;
        if (pos + 1 < len) {
          cn = xmlStr[pos + 1];
        }
        if (c === "<") {
          inTagContent = false;
          if (cn === "/") {
            inTagEnd = true;
          } else if (metaData[depth].tagStart) {
            metaData[depth].tagContent = "";
            if (!metaData[depth].data) {
              metaData[depth].data = metaData[depth].tagStart === "array" ? [] : {};
            }
            depth++;
            metaData.push({ tagStart: "", tagEnd: "", tagContent: "", key: null, data: null });
            inTagStart = true;
            inTagContent = false;
          } else if (!inTagStart) {
            inTagStart = true;
          }
        } else if (c === ">") {
          if (metaData[depth].tagStart === "true/") {
            inTagStart = false;
            inTagEnd = true;
            metaData[depth].tagStart = "";
            metaData[depth].tagEnd = "/boolean";
            metaData[depth].data = true;
          }
          if (metaData[depth].tagStart === "false/") {
            inTagStart = false;
            inTagEnd = true;
            metaData[depth].tagStart = "";
            metaData[depth].tagEnd = "/boolean";
            metaData[depth].data = false;
          }
          if (metaData[depth].tagStart === "array/") {
            inTagStart = false;
            inTagEnd = true;
            metaData[depth].tagStart = "";
            metaData[depth].tagEnd = "/arrayEmpty";
            metaData[depth].data = [];
          }
          if (inTagContent) {
            inTagContent = false;
          }
          if (inTagStart) {
            inTagStart = false;
            inTagContent = true;
            if (metaData[depth].tagStart === "array") {
              metaData[depth].data = [];
            }
            if (metaData[depth].tagStart === "dict") {
              metaData[depth].data = {};
            }
          }
          if (inTagEnd) {
            inTagEnd = false;
            if (metaData[depth].tagEnd && tags.indexOf(metaData[depth].tagEnd.substr(1)) >= 0) {
              if (metaData[depth].tagEnd === "/dict" || metaData[depth].tagEnd === "/array") {
                if (depth > 1 && metaData[depth - 2].tagStart === "array") {
                  metaData[depth - 2].data.push(metaData[depth - 1].data);
                }
                if (depth > 1 && metaData[depth - 2].tagStart === "dict" && !isProtoKey(metaData[depth - 1].key)) {
                  metaData[depth - 2].data[metaData[depth - 1].key] = metaData[depth - 1].data;
                }
                depth--;
                metaData.pop();
                metaData[depth].tagContent = "";
                metaData[depth].tagStart = "";
                metaData[depth].tagEnd = "";
              } else {
                if (metaData[depth].tagEnd === "/key" && metaData[depth].tagContent) {
                  metaData[depth].key = metaData[depth].tagContent;
                } else {
                  if (metaData[depth].tagEnd === "/real" && metaData[depth].tagContent) {
                    metaData[depth].data = parseFloat(metaData[depth].tagContent) || 0;
                  }
                  if (metaData[depth].tagEnd === "/integer" && metaData[depth].tagContent) {
                    metaData[depth].data = parseInt(metaData[depth].tagContent) || 0;
                  }
                  if (metaData[depth].tagEnd === "/string" && metaData[depth].tagContent) {
                    metaData[depth].data = metaData[depth].tagContent || "";
                  }
                  if (metaData[depth].tagEnd === "/boolean") {
                    metaData[depth].data = metaData[depth].tagContent || false;
                  }
                  if (metaData[depth].tagEnd === "/arrayEmpty") {
                    metaData[depth].data = metaData[depth].tagContent || [];
                  }
                  if (depth > 0 && metaData[depth - 1].tagStart === "array") {
                    metaData[depth - 1].data.push(metaData[depth].data);
                  }
                  if (depth > 0 && metaData[depth - 1].tagStart === "dict" && !isProtoKey(metaData[depth].key)) {
                    metaData[depth - 1].data[metaData[depth].key] = metaData[depth].data;
                  }
                }
                metaData[depth].tagContent = "";
                metaData[depth].tagStart = "";
                metaData[depth].tagEnd = "";
              }
            }
            metaData[depth].tagEnd = "";
            inTagStart = false;
            inTagContent = false;
          }
        } else {
          if (inTagStart) {
            metaData[depth].tagStart += c;
          }
          if (inTagEnd) {
            metaData[depth].tagEnd += c;
          }
          if (inTagContent) {
            metaData[depth].tagContent += c;
          }
        }
        pos++;
      }
      return metaData[0].data;
    }
    __name(plistParser, "plistParser");
    function strIsNumeric(str) {
      return typeof str === "string" && !isNaN(str) && !isNaN(parseFloat(str));
    }
    __name(strIsNumeric, "strIsNumeric");
    function plistReader(output) {
      const lines = output.split("\n");
      for (let i = 0; i < lines.length; i++) {
        if (lines[i].indexOf(" = ") >= 0) {
          const lineParts = lines[i].split(" = ");
          lineParts[0] = lineParts[0].trim();
          if (!lineParts[0].startsWith('"')) {
            lineParts[0] = '"' + lineParts[0] + '"';
          }
          lineParts[1] = lineParts[1].trim();
          if (lineParts[1].indexOf('"') === -1 && lineParts[1].endsWith(";")) {
            const valueString = lineParts[1].substring(0, lineParts[1].length - 1);
            if (!strIsNumeric(valueString)) {
              lineParts[1] = `"${valueString}";`;
            }
          }
          if (lineParts[1].indexOf('"') >= 0 && lineParts[1].endsWith(";")) {
            const valueString = lineParts[1].substring(0, lineParts[1].length - 1).replace(/"/g, "");
            if (strIsNumeric(valueString)) {
              lineParts[1] = `${valueString};`;
            }
          }
          lines[i] = lineParts.join(" : ");
        }
        lines[i] = lines[i].replace(/\(/g, "[").replace(/\)/g, "]").replace(/;/g, ",").trim();
        if (lines[i].startsWith("}") && lines[i - 1] && lines[i - 1].endsWith(",")) {
          lines[i - 1] = lines[i - 1].substring(0, lines[i - 1].length - 1);
        }
      }
      output = lines.join("");
      let obj = {};
      try {
        obj = JSON.parse(output);
      } catch (e) {
        noop();
      }
      return obj;
    }
    __name(plistReader, "plistReader");
    function semverCompare(v1, v2) {
      let res = 0;
      const parts1 = v1.split(".").map((p) => parseInt(p, 10) || 0);
      const parts2 = v2.split(".").map((p) => parseInt(p, 10) || 0);
      if (parts1[0] < parts2[0]) {
        res = 1;
      } else if (parts1[0] > parts2[0]) {
        res = -1;
      } else if (parts1[0] === parts2[0] && parts1.length >= 2 && parts2.length >= 2) {
        if (parts1[1] < parts2[1]) {
          res = 1;
        } else if (parts1[1] > parts2[1]) {
          res = -1;
        } else if (parts1[1] === parts2[1]) {
          if (parts1.length >= 3 && parts2.length >= 3) {
            if (parts1[2] < parts2[2]) {
              res = 1;
            } else if (parts1[2] > parts2[2]) {
              res = -1;
            }
          } else if (parts2.length >= 3) {
            res = 1;
          }
        }
      }
      return res;
    }
    __name(semverCompare, "semverCompare");
    function getAppleModel(key) {
      const appleModelIds = [
        {
          key: "Mac17,9",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M5 Pro",
          year: "2026",
          additional: ""
        },
        {
          key: "Mac17,8",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M5 Pro",
          year: "2026",
          additional: ""
        },
        {
          key: "Mac17,7",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M5 Max",
          year: "2026",
          additional: ""
        },
        {
          key: "Mac17,6",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M5 Max",
          year: "2026",
          additional: ""
        },
        {
          key: "Mac17,5",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M5 Pro",
          year: "2026",
          additional: ""
        },
        {
          key: "Mac17,4",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M5 Pro",
          year: "2026",
          additional: ""
        },
        {
          key: "Mac17,1",
          name: "MacBook Neo",
          size: "14-inch",
          processor: "A18 Pro",
          year: "2026",
          additional: ""
        },
        {
          key: "Mac17,3",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M5",
          year: "2025",
          additional: ""
        },
        {
          key: "Mac17,2",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M5",
          year: "2025",
          additional: ""
        },
        {
          key: "Mac16,13",
          name: "MacBook Air",
          size: "15-inch",
          processor: "M4",
          year: "2025",
          additional: ""
        },
        {
          key: "Mac16,12",
          name: "MacBook Air",
          size: "13-inch",
          processor: "M4",
          year: "2025",
          additional: ""
        },
        {
          key: "Mac15,13",
          name: "MacBook Air",
          size: "15-inch",
          processor: "M3",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac15,12",
          name: "MacBook Air",
          size: "13-inch",
          processor: "M3",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac14,15",
          name: "MacBook Air",
          size: "15-inch",
          processor: "M2",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac14,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "M2",
          year: "2022",
          additional: ""
        },
        {
          key: "MacBookAir10,1",
          name: "MacBook Air",
          size: "13-inch",
          processor: "M1",
          year: "2020",
          additional: ""
        },
        {
          key: "MacBookAir9,1",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "2020",
          additional: ""
        },
        {
          key: "MacBookAir8,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "2019",
          additional: ""
        },
        {
          key: "MacBookAir8,1",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "2018",
          additional: ""
        },
        {
          key: "MacBookAir7,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "2017",
          additional: ""
        },
        {
          key: "MacBookAir7,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "Early 2015",
          additional: ""
        },
        {
          key: "MacBookAir7,1",
          name: "MacBook Air",
          size: "11-inch",
          processor: "",
          year: "Early 2015",
          additional: ""
        },
        {
          key: "MacBookAir6,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "Early 2014",
          additional: ""
        },
        {
          key: "MacBookAir6,1",
          name: "MacBook Air",
          size: "11-inch",
          processor: "",
          year: "Early 2014",
          additional: ""
        },
        {
          key: "MacBookAir6,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "Mid 2013",
          additional: ""
        },
        {
          key: "MacBookAir6,1",
          name: "MacBook Air",
          size: "11-inch",
          processor: "",
          year: "Mid 2013",
          additional: ""
        },
        {
          key: "MacBookAir5,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "Mid 2012",
          additional: ""
        },
        {
          key: "MacBookAir5,1",
          name: "MacBook Air",
          size: "11-inch",
          processor: "",
          year: "Mid 2012",
          additional: ""
        },
        {
          key: "MacBookAir4,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "Mid 2011",
          additional: ""
        },
        {
          key: "MacBookAir4,1",
          name: "MacBook Air",
          size: "11-inch",
          processor: "",
          year: "Mid 2011",
          additional: ""
        },
        {
          key: "MacBookAir3,2",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "Late 2010",
          additional: ""
        },
        {
          key: "MacBookAir3,1",
          name: "MacBook Air",
          size: "11-inch",
          processor: "",
          year: "Late 2010",
          additional: ""
        },
        {
          key: "MacBookAir2,1",
          name: "MacBook Air",
          size: "13-inch",
          processor: "",
          year: "Mid 2009",
          additional: ""
        },
        {
          key: "Mac16,1",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M4",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac16,6",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M4 Pro",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac16,8",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M4 Max",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac16,5",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M4 Pro",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac16,6",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M4 Max",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac15,3",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M3",
          year: "Nov 2023",
          additional: ""
        },
        {
          key: "Mac15,6",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M3 Pro",
          year: "Nov 2023",
          additional: ""
        },
        {
          key: "Mac15,8",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M3 Pro",
          year: "Nov 2023",
          additional: ""
        },
        {
          key: "Mac15,10",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M3 Max",
          year: "Nov 2023",
          additional: ""
        },
        {
          key: "Mac15,7",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M3 Pro",
          year: "Nov 2023",
          additional: ""
        },
        {
          key: "Mac15,9",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M3 Pro",
          year: "Nov 2023",
          additional: ""
        },
        {
          key: "Mac15,11",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M3 Max",
          year: "Nov 2023",
          additional: ""
        },
        {
          key: "Mac14,5",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M2 Max",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac14,9",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M2 Max",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac14,6",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M2 Max",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac14,10",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M2 Max",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac14,7",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "M2",
          year: "2022",
          additional: ""
        },
        {
          key: "MacBookPro18,3",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M1 Pro",
          year: "2021",
          additional: ""
        },
        {
          key: "MacBookPro18,4",
          name: "MacBook Pro",
          size: "14-inch",
          processor: "M1 Max",
          year: "2021",
          additional: ""
        },
        {
          key: "MacBookPro18,1",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M1 Pro",
          year: "2021",
          additional: ""
        },
        {
          key: "MacBookPro18,2",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "M1 Max",
          year: "2021",
          additional: ""
        },
        {
          key: "MacBookPro17,1",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "M1",
          year: "2020",
          additional: ""
        },
        {
          key: "MacBookPro16,3",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2020",
          additional: "Two Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro16,2",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2020",
          additional: "Four Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro16,1",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "",
          year: "2019",
          additional: ""
        },
        {
          key: "MacBookPro16,4",
          name: "MacBook Pro",
          size: "16-inch",
          processor: "",
          year: "2019",
          additional: ""
        },
        {
          key: "MacBookPro15,3",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "2019",
          additional: ""
        },
        {
          key: "MacBookPro15,2",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2019",
          additional: ""
        },
        {
          key: "MacBookPro15,1",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "2019",
          additional: ""
        },
        {
          key: "MacBookPro15,4",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2019",
          additional: "Two Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro15,1",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "2018",
          additional: ""
        },
        {
          key: "MacBookPro15,2",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2018",
          additional: "Four Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro14,1",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2017",
          additional: "Two Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro14,2",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2017",
          additional: "Four Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro14,3",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "2017",
          additional: ""
        },
        {
          key: "MacBookPro13,1",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2016",
          additional: "Two Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro13,2",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "2016",
          additional: "Four Thunderbolt 3 ports"
        },
        {
          key: "MacBookPro13,3",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "2016",
          additional: ""
        },
        {
          key: "MacBookPro11,4",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Mid 2015",
          additional: ""
        },
        {
          key: "MacBookPro11,5",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Mid 2015",
          additional: ""
        },
        {
          key: "MacBookPro12,1",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "Early 2015",
          additional: ""
        },
        {
          key: "MacBookPro11,2",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Late 2013",
          additional: ""
        },
        {
          key: "MacBookPro11,3",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Late 2013",
          additional: ""
        },
        {
          key: "MacBookPro11,1",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "Late 2013",
          additional: ""
        },
        {
          key: "MacBookPro10,1",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Mid 2012",
          additional: ""
        },
        {
          key: "MacBookPro10,2",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "Late 2012",
          additional: ""
        },
        {
          key: "MacBookPro9,1",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Mid 2012",
          additional: ""
        },
        {
          key: "MacBookPro9,2",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "Mid 2012",
          additional: ""
        },
        {
          key: "MacBookPro8,3",
          name: "MacBook Pro",
          size: "17-inch",
          processor: "",
          year: "Early 2011",
          additional: ""
        },
        {
          key: "MacBookPro8,2",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Early 2011",
          additional: ""
        },
        {
          key: "MacBookPro8,1",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "Early 2011",
          additional: ""
        },
        {
          key: "MacBookPro6,1",
          name: "MacBook Pro",
          size: "17-inch",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "MacBookPro6,2",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "MacBookPro7,1",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "MacBookPro5,2",
          name: "MacBook Pro",
          size: "17-inch",
          processor: "",
          year: "Early 2009",
          additional: ""
        },
        {
          key: "MacBookPro5,3",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Mid 2009",
          additional: ""
        },
        {
          key: "MacBookPro5,5",
          name: "MacBook Pro",
          size: "13-inch",
          processor: "",
          year: "Mid 2009",
          additional: ""
        },
        {
          key: "MacBookPro5,1",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Late 2008",
          additional: ""
        },
        {
          key: "MacBookPro4,1",
          name: "MacBook Pro",
          size: "15-inch",
          processor: "",
          year: "Early 2008",
          additional: ""
        },
        {
          key: "MacBook10,1",
          name: "MacBook",
          size: "12-inch",
          processor: "",
          year: "2017",
          additional: ""
        },
        {
          key: "MacBook9,1",
          name: "MacBook",
          size: "12-inch",
          processor: "",
          year: "Early 2016",
          additional: ""
        },
        {
          key: "MacBook8,1",
          name: "MacBook",
          size: "12-inch",
          processor: "",
          year: "Early 2015",
          additional: ""
        },
        {
          key: "MacBook7,1",
          name: "MacBook",
          size: "13-inch",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "MacBook6,1",
          name: "MacBook",
          size: "13-inch",
          processor: "",
          year: "Late 2009",
          additional: ""
        },
        {
          key: "MacBook5,2",
          name: "MacBook",
          size: "13-inch",
          processor: "",
          year: "Early 2009",
          additional: ""
        },
        {
          key: "Mac14,13",
          name: "Mac Studio",
          size: "",
          processor: "M2 Max",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac14,14",
          name: "Mac Studio",
          size: "",
          processor: "M2 Ultra",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac15,14",
          name: "Mac Studio",
          size: "",
          processor: "M3 Ultra",
          year: "2025",
          additional: ""
        },
        {
          key: "Mac16,9",
          name: "Mac Studio",
          size: "",
          processor: "M4 Max",
          year: "2025",
          additional: ""
        },
        {
          key: "Mac13,1",
          name: "Mac Studio",
          size: "",
          processor: "M1 Max",
          year: "2022",
          additional: ""
        },
        {
          key: "Mac13,2",
          name: "Mac Studio",
          size: "",
          processor: "M1 Ultra",
          year: "2022",
          additional: ""
        },
        {
          key: "Mac16,11",
          name: "Mac mini",
          size: "",
          processor: "M4 Pro",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac16,10",
          name: "Mac mini",
          size: "",
          processor: "M4",
          year: "2024",
          additional: ""
        },
        {
          key: "Mac14,3",
          name: "Mac mini",
          size: "",
          processor: "M2",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac14,12",
          name: "Mac mini",
          size: "",
          processor: "M2 Pro",
          year: "2023",
          additional: ""
        },
        {
          key: "Macmini9,1",
          name: "Mac mini",
          size: "",
          processor: "M1",
          year: "2020",
          additional: ""
        },
        {
          key: "Macmini8,1",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Late 2018",
          additional: ""
        },
        {
          key: "Macmini7,1",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Late 2014",
          additional: ""
        },
        {
          key: "Macmini6,1",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Late 2012",
          additional: ""
        },
        {
          key: "Macmini6,2",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Late 2012",
          additional: ""
        },
        {
          key: "Macmini5,1",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Mid 2011",
          additional: ""
        },
        {
          key: "Macmini5,2",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Mid 2011",
          additional: ""
        },
        {
          key: "Macmini4,1",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "Macmini3,1",
          name: "Mac mini",
          size: "",
          processor: "",
          year: "Early 2009",
          additional: ""
        },
        {
          key: "Mac16,3",
          name: "iMac",
          size: "24-inch",
          processor: "M4",
          year: "2024",
          additional: "Four ports"
        },
        {
          key: "Mac16,2",
          name: "iMac",
          size: "24-inch",
          processor: "M4",
          year: "2024",
          additional: "Two ports"
        },
        {
          key: "Mac15,5",
          name: "iMac",
          size: "24-inch",
          processor: "M3",
          year: "2023",
          additional: "Four ports"
        },
        {
          key: "Mac15,4",
          name: "iMac",
          size: "24-inch",
          processor: "M3",
          year: "2023",
          additional: "Two ports"
        },
        {
          key: "iMac21,1",
          name: "iMac",
          size: "24-inch",
          processor: "M1",
          year: "2021",
          additional: ""
        },
        {
          key: "iMac21,2",
          name: "iMac",
          size: "24-inch",
          processor: "M1",
          year: "2021",
          additional: ""
        },
        {
          key: "iMac20,1",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "2020",
          additional: "Retina 5K"
        },
        {
          key: "iMac20,2",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "2020",
          additional: "Retina 5K"
        },
        {
          key: "iMac19,1",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "2019",
          additional: "Retina 5K"
        },
        {
          key: "iMac19,2",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "2019",
          additional: "Retina 4K"
        },
        {
          key: "iMacPro1,1",
          name: "iMac Pro",
          size: "",
          processor: "",
          year: "2017",
          additional: ""
        },
        {
          key: "iMac18,3",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "2017",
          additional: "Retina 5K"
        },
        {
          key: "iMac18,2",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "2017",
          additional: "Retina 4K"
        },
        {
          key: "iMac18,1",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "2017",
          additional: ""
        },
        {
          key: "iMac17,1",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "Late 2015",
          additional: "Retina 5K"
        },
        {
          key: "iMac16,2",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Late 2015",
          additional: "Retina 4K"
        },
        {
          key: "iMac16,1",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Late 2015",
          additional: ""
        },
        {
          key: "iMac15,1",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "Late 2014",
          additional: "Retina 5K"
        },
        {
          key: "iMac14,4",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Mid 2014",
          additional: ""
        },
        {
          key: "iMac14,2",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "Late 2013",
          additional: ""
        },
        {
          key: "iMac14,1",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Late 2013",
          additional: ""
        },
        {
          key: "iMac13,2",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "Late 2012",
          additional: ""
        },
        {
          key: "iMac13,1",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Late 2012",
          additional: ""
        },
        {
          key: "iMac12,2",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "Mid 2011",
          additional: ""
        },
        {
          key: "iMac12,1",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Mid 2011",
          additional: ""
        },
        {
          key: "iMac11,3",
          name: "iMac",
          size: "27-inch",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "iMac11,2",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "iMac10,1",
          name: "iMac",
          size: "21.5-inch",
          processor: "",
          year: "Late 2009",
          additional: ""
        },
        {
          key: "iMac9,1",
          name: "iMac",
          size: "20-inch",
          processor: "",
          year: "Early 2009",
          additional: ""
        },
        {
          key: "Mac14,8",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "2023",
          additional: ""
        },
        {
          key: "Mac14,8",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "2023",
          additional: "Rack"
        },
        {
          key: "MacPro7,1",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "2019",
          additional: ""
        },
        {
          key: "MacPro7,1",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "2019",
          additional: "Rack"
        },
        {
          key: "MacPro6,1",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "Late 2013",
          additional: ""
        },
        {
          key: "MacPro5,1",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "Mid 2012",
          additional: ""
        },
        {
          key: "MacPro5,1",
          name: "Mac Pro Server",
          size: "",
          processor: "",
          year: "Mid 2012",
          additional: "Server"
        },
        {
          key: "MacPro5,1",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "Mid 2010",
          additional: ""
        },
        {
          key: "MacPro5,1",
          name: "Mac Pro Server",
          size: "",
          processor: "",
          year: "Mid 2010",
          additional: "Server"
        },
        {
          key: "MacPro4,1",
          name: "Mac Pro",
          size: "",
          processor: "",
          year: "Early 2009",
          additional: ""
        }
      ];
      const list = appleModelIds.filter((model) => model.key === key);
      if (list.length === 0) {
        return {
          key,
          model: "Apple",
          version: "Unknown"
        };
      }
      const features = [];
      if (list[0].size) {
        features.push(list[0].size);
      }
      if (list[0].processor) {
        features.push(list[0].processor);
      }
      if (list[0].year) {
        features.push(list[0].year);
      }
      if (list[0].additional) {
        features.push(list[0].additional);
      }
      return {
        key,
        model: list[0].name,
        version: list[0].name + " (" + features.join(", ") + ")"
      };
    }
    __name(getAppleModel, "getAppleModel");
    function checkWebsite(url, timeout = 5e3) {
      const http = url.startsWith("https:") || url.indexOf(":443/") > 0 || url.indexOf(":8443/") > 0 ? __require("https") : __require("http");
      const t = Date.now();
      return new Promise((resolve2) => {
        const request = http.get(url, (res) => {
          res.on("data", () => {
          });
          res.on("end", () => {
            resolve2({
              url,
              statusCode: res.statusCode,
              message: res.statusMessage,
              time: Date.now() - t
            });
          });
        }).on("error", (e) => {
          resolve2({
            url,
            statusCode: 404,
            message: e.message,
            time: Date.now() - t
          });
        }).setTimeout(timeout, () => {
          request.destroy();
          resolve2({
            url,
            statusCode: 408,
            message: "Request Timeout",
            time: Date.now() - t
          });
        });
      });
    }
    __name(checkWebsite, "checkWebsite");
    function cleanString(str) {
      return str.replace(/To Be Filled By O.E.M./g, "");
    }
    __name(cleanString, "cleanString");
    function grep(str, pattern) {
      const result = str.split("\n").filter((line) => line.includes(pattern)).join("\n");
      return result;
    }
    __name(grep, "grep");
    function noop() {
    }
    __name(noop, "noop");
    function isProtoKey(key) {
      return key === "__proto__" || key === "constructor" || key === "prototype";
    }
    __name(isProtoKey, "isProtoKey");
    exports.toInt = toInt;
    exports.splitByNumber = splitByNumber;
    exports.execOptsWin = execOptsWin;
    exports.execOptsLinux = execOptsLinux;
    exports.getCodepage = getCodepage;
    exports.execWin = execWin;
    exports.isFunction = isFunction;
    exports.unique = unique;
    exports.sortByKey = sortByKey;
    exports.cores = cores;
    exports.getValue = getValue;
    exports.decodeEscapeSequence = decodeEscapeSequence;
    exports.parseDateTime = parseDateTime;
    exports.parseHead = parseHead;
    exports.findObjectByKey = findObjectByKey;
    exports.darwinXcodeExists = darwinXcodeExists;
    exports.getVboxmanage = getVboxmanage;
    exports.powerShell = powerShell;
    exports.powerShellStart = powerShellStart;
    exports.powerShellRelease = powerShellRelease;
    exports.execSafe = execSafe;
    exports.nanoSeconds = nanoSeconds;
    exports.countUniqueLines = countUniqueLines;
    exports.countLines = countLines;
    exports.noop = noop;
    exports.isRaspberry = isRaspberry;
    exports.isRaspbian = isRaspbian;
    exports.sanitizeShellString = sanitizeShellString;
    exports.sanitizeContainerID = sanitizeContainerID;
    exports.sanitizeImageID = sanitizeImageID;
    exports.isPrototypePolluted = isPrototypePolluted;
    exports.sanitizeString = sanitizeString;
    exports.decodePiCpuinfo = decodePiCpuinfo;
    exports.getRpiGpu = getRpiGpu;
    exports.promiseAll = promiseAll;
    exports.promisify = promisify;
    exports.promisifySave = promisifySave;
    exports.smartMonToolsInstalled = smartMonToolsInstalled;
    exports.linuxVersion = linuxVersion;
    exports.plistParser = plistParser;
    exports.plistReader = plistReader;
    exports.stringObj = stringObj;
    exports.stringReplace = stringReplace;
    exports.stringToLower = stringToLower;
    exports.stringToString = stringToString;
    exports.stringSubstr = stringSubstr;
    exports.stringSubstring = stringSubstring;
    exports.stringTrim = stringTrim;
    exports.stringStartWith = stringStartWith;
    exports.mathMin = mathMin;
    exports.WINDIR = WINDIR;
    exports.getFilesInPath = getFilesInPath;
    exports.semverCompare = semverCompare;
    exports.getAppleModel = getAppleModel;
    exports.checkWebsite = checkWebsite;
    exports.cleanString = cleanString;
    exports.grep = grep;
    exports.getPowershell = getPowershell;
  }
});

// ../../node_modules/.bun/systeminformation@5.33.1/node_modules/systeminformation/lib/network.js
var require_network = __commonJS({
  "../../node_modules/.bun/systeminformation@5.33.1/node_modules/systeminformation/lib/network.js"(exports) {
    "use strict";
    init_esm();
    var os = __require("os");
    var exec = __require("child_process").exec;
    var execSync = __require("child_process").execSync;
    var execFileSync = __require("child_process").execFileSync;
    var readFileSync2 = __require("fs").readFileSync;
    var fs4 = __require("fs");
    var util2 = require_util2();
    var _platform = process.platform;
    var _linux = _platform === "linux" || _platform === "android";
    var _darwin = _platform === "darwin";
    var _windows = _platform === "win32";
    var _freebsd = _platform === "freebsd";
    var _openbsd = _platform === "openbsd";
    var _netbsd = _platform === "netbsd";
    var _sunos = _platform === "sunos";
    var _network = {};
    var _default_iface = "";
    var _ifaces = {};
    var _dhcpNics = [];
    var _networkInterfaces = [];
    var _mac = {};
    var pathToIp;
    function getDefaultNetworkInterface() {
      let ifacename = "";
      let ifacenameFirst = "";
      try {
        const ifaces = os.networkInterfaces();
        let scopeid = 9999;
        for (let dev in ifaces) {
          if ({}.hasOwnProperty.call(ifaces, dev)) {
            ifaces[dev].forEach((details) => {
              if (details && details.internal === false) {
                ifacenameFirst = ifacenameFirst || dev;
                if (details.scopeid && details.scopeid < scopeid) {
                  ifacename = dev;
                  scopeid = details.scopeid;
                }
              }
            });
          }
        }
        ifacename = ifacename || ifacenameFirst || "";
        if (_windows) {
          let defaultIp = "";
          const cmd = "netstat -r";
          const result = execSync(cmd, util2.execOptsWin);
          const lines = result.toString().split(os.EOL);
          lines.forEach((line) => {
            line = line.replace(/\s+/g, " ").trim();
            if (line.indexOf("0.0.0.0 0.0.0.0") > -1 && !/[a-zA-Z]/.test(line)) {
              const parts = line.split(" ");
              if (parts.length >= 5) {
                defaultIp = parts[parts.length - 2];
              }
            }
          });
          if (defaultIp) {
            for (let dev in ifaces) {
              if ({}.hasOwnProperty.call(ifaces, dev)) {
                ifaces[dev].forEach((details) => {
                  if (details && details.address && details.address === defaultIp) {
                    ifacename = dev;
                  }
                });
              }
            }
          }
        }
        if (_linux) {
          const cmd = "ip route 2> /dev/null | grep default";
          const result = execSync(cmd, util2.execOptsLinux);
          const parts = result.toString().split("\n")[0].split(/\s+/);
          if (parts[0] === "none" && parts[5]) {
            ifacename = parts[5];
          } else if (parts[4]) {
            ifacename = parts[4];
          }
          if (ifacename.indexOf(":") > -1) {
            ifacename = ifacename.split(":")[1].trim();
          }
        }
        if (_darwin || _freebsd || _openbsd || _netbsd || _sunos) {
          let cmd = "";
          if (_linux) {
            cmd = "ip route 2> /dev/null | grep default | awk '{print $5}'";
          }
          if (_darwin) {
            cmd = "route -n get default 2>/dev/null | grep interface: | awk '{print $2}'";
          }
          if (_freebsd || _openbsd || _netbsd || _sunos) {
            cmd = "route get 0.0.0.0 | grep interface:";
          }
          const result = execSync(cmd);
          ifacename = result.toString().split("\n")[0];
          if (ifacename.indexOf(":") > -1) {
            ifacename = ifacename.split(":")[1].trim();
          }
        }
      } catch {
        util2.noop();
      }
      if (ifacename) {
        _default_iface = ifacename;
      }
      return _default_iface;
    }
    __name(getDefaultNetworkInterface, "getDefaultNetworkInterface");
    exports.getDefaultNetworkInterface = getDefaultNetworkInterface;
    function getMacAddresses() {
      let iface = "";
      let mac = "";
      const result = {};
      if (_linux || _freebsd || _openbsd || _netbsd) {
        if (typeof pathToIp === "undefined") {
          try {
            const lines = execSync("which ip", util2.execOptsLinux).toString().split("\n");
            if (lines.length && lines[0].indexOf(":") === -1 && lines[0].indexOf("/") === 0) {
              pathToIp = lines[0];
            } else {
              pathToIp = "";
            }
          } catch {
            pathToIp = "";
          }
        }
        try {
          const cmd = "export LC_ALL=C; " + (pathToIp ? pathToIp + " link show up" : "/sbin/ifconfig") + "; unset LC_ALL";
          const res = execSync(cmd, util2.execOptsLinux);
          const lines = res.toString().split("\n");
          for (let i = 0; i < lines.length; i++) {
            if (lines[i] && lines[i][0] !== " ") {
              if (pathToIp) {
                const nextline = lines[i + 1].trim().split(" ");
                if (nextline[0] === "link/ether") {
                  iface = lines[i].split(" ")[1];
                  iface = iface.slice(0, iface.length - 1);
                  mac = nextline[1];
                }
              } else {
                iface = lines[i].split(" ")[0];
                mac = lines[i].split("HWaddr ")[1];
              }
              if (iface && mac) {
                result[iface] = mac.trim();
                iface = "";
                mac = "";
              }
            }
          }
        } catch {
          util2.noop();
        }
      }
      if (_darwin) {
        try {
          const cmd = "/sbin/ifconfig";
          const res = execSync(cmd);
          const lines = res.toString().split("\n");
          for (let i = 0; i < lines.length; i++) {
            if (lines[i] && lines[i][0] !== "	" && lines[i].indexOf(":") > 0) {
              iface = lines[i].split(":")[0];
            } else if (lines[i].indexOf("	ether ") === 0) {
              mac = lines[i].split("	ether ")[1];
              if (iface && mac) {
                result[iface] = mac.trim();
                iface = "";
                mac = "";
              }
            }
          }
        } catch {
          util2.noop();
        }
      }
      return result;
    }
    __name(getMacAddresses, "getMacAddresses");
    function networkInterfaceDefault(callback) {
      return new Promise((resolve2) => {
        process.nextTick(() => {
          const result = getDefaultNetworkInterface();
          if (callback) {
            callback(result);
          }
          resolve2(result);
        });
      });
    }
    __name(networkInterfaceDefault, "networkInterfaceDefault");
    exports.networkInterfaceDefault = networkInterfaceDefault;
    function parseLinesWindowsNics(sections, nconfigsections) {
      const nics = [];
      for (let i in sections) {
        try {
          if ({}.hasOwnProperty.call(sections, i)) {
            if (sections[i].trim() !== "") {
              const lines = sections[i].trim().split("\r\n");
              let linesNicConfig = null;
              try {
                linesNicConfig = nconfigsections && nconfigsections[i] ? nconfigsections[i].trim().split("\r\n") : [];
              } catch {
                util2.noop();
              }
              const netEnabled = util2.getValue(lines, "NetEnabled", ":");
              let adapterType = util2.getValue(lines, "AdapterTypeID", ":") === "9" ? "wireless" : "wired";
              const ifacename = util2.getValue(lines, "Name", ":").replace(/\]/g, ")").replace(/\[/g, "(");
              const iface = util2.getValue(lines, "NetConnectionID", ":").replace(/\]/g, ")").replace(/\[/g, "(");
              if (ifacename.toLowerCase().indexOf("wi-fi") >= 0 || ifacename.toLowerCase().indexOf("wireless") >= 0) {
                adapterType = "wireless";
              }
              if (netEnabled !== "") {
                const speed = parseInt(util2.getValue(lines, "speed", ":").trim(), 10) / 1e6;
                nics.push({
                  mac: util2.getValue(lines, "MACAddress", ":").toLowerCase(),
                  dhcp: util2.getValue(linesNicConfig, "dhcpEnabled", ":").toLowerCase() === "true",
                  name: ifacename,
                  iface,
                  netEnabled: netEnabled === "TRUE",
                  speed: isNaN(speed) ? null : speed,
                  operstate: util2.getValue(lines, "NetConnectionStatus", ":") === "2" ? "up" : "down",
                  type: adapterType
                });
              }
            }
          }
        } catch {
          util2.noop();
        }
      }
      return nics;
    }
    __name(parseLinesWindowsNics, "parseLinesWindowsNics");
    function getWindowsNics() {
      return new Promise((resolve2) => {
        process.nextTick(() => {
          let cmd = "Get-CimInstance Win32_NetworkAdapter | fl *; echo '#-#-#-#';";
          cmd += "Get-CimInstance Win32_NetworkAdapterConfiguration | fl DHCPEnabled";
          try {
            util2.powerShell(cmd).then((data) => {
              data = data.split("#-#-#-#");
              const nsections = (data[0] || "").split(/\n\s*\n/);
              const nconfigsections = (data[1] || "").split(/\n\s*\n/);
              resolve2(parseLinesWindowsNics(nsections, nconfigsections));
            });
          } catch {
            resolve2([]);
          }
        });
      });
    }
    __name(getWindowsNics, "getWindowsNics");
    function getWindowsDNSsuffixes() {
      let iface = {};
      const dnsSuffixes = {
        primaryDNS: "",
        exitCode: 0,
        ifaces: []
      };
      try {
        const ipconfig = execSync("ipconfig /all", util2.execOptsWin);
        const ipconfigArray = ipconfig.split("\r\n\r\n");
        ipconfigArray.forEach((element, index) => {
          if (index === 1) {
            const longPrimaryDNS = element.split("\r\n").filter((element2) => {
              return element2.toUpperCase().includes("DNS");
            });
            const primaryDNS = longPrimaryDNS[0].substring(longPrimaryDNS[0].lastIndexOf(":") + 1);
            dnsSuffixes.primaryDNS = primaryDNS.trim();
            if (!dnsSuffixes.primaryDNS) {
              dnsSuffixes.primaryDNS = "Not defined";
            }
          }
          if (index > 1) {
            if (index % 2 === 0) {
              const name = element.substring(element.lastIndexOf(" ") + 1).replace(":", "");
              iface.name = name;
            } else {
              const connectionSpecificDNS = element.split("\r\n").filter((element2) => {
                return element2.toUpperCase().includes("DNS");
              });
              const dnsSuffix = connectionSpecificDNS[0].substring(connectionSpecificDNS[0].lastIndexOf(":") + 1);
              iface.dnsSuffix = dnsSuffix.trim();
              dnsSuffixes.ifaces.push(iface);
              iface = {};
            }
          }
        });
        return dnsSuffixes;
      } catch {
        return {
          primaryDNS: "",
          exitCode: 0,
          ifaces: []
        };
      }
    }
    __name(getWindowsDNSsuffixes, "getWindowsDNSsuffixes");
    function getWindowsIfaceDNSsuffix(ifaces, ifacename) {
      let dnsSuffix = "";
      const interfaceName = ifacename + ".";
      try {
        const connectionDnsSuffix = ifaces.filter((iface) => {
          return interfaceName.includes(iface.name + ".");
        }).map((iface) => iface.dnsSuffix);
        if (connectionDnsSuffix[0]) {
          dnsSuffix = connectionDnsSuffix[0];
        }
        if (!dnsSuffix) {
          dnsSuffix = "";
        }
        return dnsSuffix;
      } catch {
        return "Unknown";
      }
    }
    __name(getWindowsIfaceDNSsuffix, "getWindowsIfaceDNSsuffix");
    function getWindowsWiredProfilesInformation() {
      try {
        const result = execSync("netsh lan show profiles", util2.execOptsWin);
        const profileList = result.split("\r\nProfile on interface");
        return profileList;
      } catch (error) {
        if (error.status === 1 && error.stdout.includes("AutoConfig")) {
          return "Disabled";
        }
        return [];
      }
    }
    __name(getWindowsWiredProfilesInformation, "getWindowsWiredProfilesInformation");
    function getWindowsWirelessIfaceSSID(interfaceName) {
      try {
        const result = execSync(`netsh wlan show  interface name="${interfaceName}" | findstr "SSID"`, util2.execOptsWin);
        const SSID = result.split("\r\n").shift();
        const parseSSID = SSID.split(":").pop().trim();
        return parseSSID;
      } catch {
        return "Unknown";
      }
    }
    __name(getWindowsWirelessIfaceSSID, "getWindowsWirelessIfaceSSID");
    function getWindowsIEEE8021x(connectionType, iface, ifaces) {
      const i8021x = {
        state: "Unknown",
        protocol: "Unknown"
      };
      if (ifaces === "Disabled") {
        i8021x.state = "Disabled";
        i8021x.protocol = "Not defined";
        return i8021x;
      }
      if (connectionType === "wired" && ifaces.length > 0) {
        try {
          const iface8021xInfo = ifaces.find((element) => {
            return element.includes(iface + "\r\n");
          });
          const arrayIface8021xInfo = iface8021xInfo.split("\r\n");
          const state8021x = arrayIface8021xInfo.find((element) => {
            return element.includes("802.1x");
          });
          if (state8021x.includes("Disabled")) {
            i8021x.state = "Disabled";
            i8021x.protocol = "Not defined";
          } else if (state8021x.includes("Enabled")) {
            const protocol8021x = arrayIface8021xInfo.find((element) => {
              return element.includes("EAP");
            });
            i8021x.protocol = protocol8021x.split(":").pop();
            i8021x.state = "Enabled";
          }
        } catch {
          return i8021x;
        }
      } else if (connectionType === "wireless") {
        let i8021xState = "";
        let i8021xProtocol = "";
        try {
          const SSID = getWindowsWirelessIfaceSSID(iface);
          if (SSID !== "Unknown") {
            const ifaceSanitized = util2.sanitizeString(SSID);
            const profiles = execSync(`netsh wlan show profiles "${ifaceSanitized}"`, util2.execOptsWin).split("\r\n");
            i8021xState = (profiles.find((l) => l.indexOf("802.1X") >= 0) || "").trim();
            i8021xProtocol = (profiles.find((l) => l.indexOf("EAP") >= 0) || "").trim();
          }
          if (i8021xState.includes(":") && i8021xProtocol.includes(":")) {
            i8021x.state = i8021xState.split(":").pop();
            i8021x.protocol = i8021xProtocol.split(":").pop();
          }
        } catch (error) {
          if (error.status === 1 && error.stdout.includes("AutoConfig")) {
            i8021x.state = "Disabled";
            i8021x.protocol = "Not defined";
          }
          return i8021x;
        }
      }
      return i8021x;
    }
    __name(getWindowsIEEE8021x, "getWindowsIEEE8021x");
    function splitSectionsNics(lines) {
      const result = [];
      let section = [];
      lines.forEach((line) => {
        if (!line.startsWith("	") && !line.startsWith(" ")) {
          if (section.length) {
            result.push(section);
            section = [];
          }
        }
        section.push(line);
      });
      if (section.length) {
        result.push(section);
      }
      return result;
    }
    __name(splitSectionsNics, "splitSectionsNics");
    function parseLinesDarwinNics(sections) {
      const nics = [];
      sections.forEach((section) => {
        const nic = {
          iface: "",
          mtu: null,
          mac: "",
          ip6: "",
          ip4: "",
          speed: null,
          type: "",
          operstate: "",
          duplex: "",
          internal: false
        };
        const first = section[0];
        nic.iface = first.split(":")[0].trim();
        const parts = first.split("> mtu");
        nic.mtu = parts.length > 1 ? parseInt(parts[1], 10) : null;
        if (isNaN(nic.mtu)) {
          nic.mtu = null;
        }
        nic.internal = parts[0].toLowerCase().indexOf("loopback") > -1;
        section.forEach((line) => {
          if (line.trim().startsWith("ether ")) {
            nic.mac = line.split("ether ")[1].toLowerCase().trim();
          }
          if (line.trim().startsWith("inet6 ") && !nic.ip6) {
            nic.ip6 = line.split("inet6 ")[1].toLowerCase().split("%")[0].split(" ")[0];
          }
          if (line.trim().startsWith("inet ") && !nic.ip4) {
            nic.ip4 = line.split("inet ")[1].toLowerCase().split(" ")[0];
          }
        });
        let speed = util2.getValue(section, "link rate");
        nic.speed = speed ? parseFloat(speed) : null;
        if (nic.speed === null) {
          speed = util2.getValue(section, "uplink rate");
          nic.speed = speed ? parseFloat(speed) : null;
          if (nic.speed !== null && speed.toLowerCase().indexOf("gbps") >= 0) {
            nic.speed = nic.speed * 1e3;
          }
        } else {
          if (speed.toLowerCase().indexOf("gbps") >= 0) {
            nic.speed = nic.speed * 1e3;
          }
        }
        nic.type = util2.getValue(section, "type").toLowerCase().indexOf("wi-fi") > -1 ? "wireless" : "wired";
        const operstate = util2.getValue(section, "status").toLowerCase();
        nic.operstate = operstate === "active" ? "up" : operstate === "inactive" ? "down" : "unknown";
        nic.duplex = util2.getValue(section, "media").toLowerCase().indexOf("half-duplex") > -1 ? "half" : "full";
        if (nic.ip6 || nic.ip4 || nic.mac) {
          nics.push(nic);
        }
      });
      return nics;
    }
    __name(parseLinesDarwinNics, "parseLinesDarwinNics");
    function getDarwinNics() {
      const cmd = "/sbin/ifconfig -v";
      try {
        const lines = execSync(cmd, { maxBuffer: 1024 * 102400 }).toString().split("\n");
        const nsections = splitSectionsNics(lines);
        return parseLinesDarwinNics(nsections);
      } catch {
        return [];
      }
    }
    __name(getDarwinNics, "getDarwinNics");
    function getLinuxIfaceConnectionName(interfaceName) {
      try {
        const output = execFileSync("nmcli", ["device", "status"], { ...util2.execOptsLinux, stdio: ["ignore", "pipe", "ignore"] }).toString();
        const result = util2.grep(output, interfaceName);
        const resultFormat = result.replace(/\s+/g, " ").trim();
        const connectionNameLines = resultFormat.split(" ").slice(3);
        const connectionName = connectionNameLines.join(" ");
        const connectionNameSanitized = util2.sanitizeString(connectionName, false);
        return connectionNameSanitized !== "--" ? connectionNameSanitized : "";
      } catch {
        return "";
      }
    }
    __name(getLinuxIfaceConnectionName, "getLinuxIfaceConnectionName");
    function checkLinuxDCHPInterfaces(file, depth) {
      let result = [];
      depth = depth || 0;
      if (depth > 10) {
        return result;
      }
      try {
        const content = readFileSync2(file, { encoding: "utf8" });
        const lines = content.split("\n").filter((l) => /iface|source/.test(l));
        lines.forEach((line) => {
          const parts = line.replace(/\s+/g, " ").trim().split(" ");
          if (parts.length >= 4) {
            if (line.toLowerCase().indexOf(" inet ") >= 0 && line.toLowerCase().indexOf("dhcp") >= 0) {
              result.push(parts[1]);
            }
          }
          if (line.toLowerCase().includes("source")) {
            const file2 = line.split(" ")[1];
            result = result.concat(checkLinuxDCHPInterfaces(file2, depth + 1));
          }
        });
      } catch {
        util2.noop();
      }
      return result;
    }
    __name(checkLinuxDCHPInterfaces, "checkLinuxDCHPInterfaces");
    function getLinuxDHCPNics() {
      const cmd = "ip a 2> /dev/null";
      let result = [];
      try {
        const lines = execSync(cmd, util2.execOptsLinux).toString().split("\n");
        const nsections = splitSectionsNics(lines);
        result = parseLinuxDHCPNics(nsections);
      } catch {
        util2.noop();
      }
      try {
        result = checkLinuxDCHPInterfaces("/etc/network/interfaces");
      } catch {
        util2.noop();
      }
      return result;
    }
    __name(getLinuxDHCPNics, "getLinuxDHCPNics");
    function parseLinuxDHCPNics(sections) {
      const result = [];
      if (sections && sections.length) {
        sections.forEach((lines) => {
          if (lines && lines.length) {
            const parts = lines[0].split(":");
            if (parts.length > 2) {
              for (let line of lines) {
                if (line.indexOf(" inet ") >= 0 && line.indexOf(" dynamic ") >= 0) {
                  const parts2 = line.split(" ");
                  const nic = parts2[parts2.length - 1].trim();
                  result.push(nic);
                  break;
                }
              }
            }
          }
        });
      }
      return result;
    }
    __name(parseLinuxDHCPNics, "parseLinuxDHCPNics");
    function getLinuxIfaceDHCPstatus(iface, connectionName, DHCPNics) {
      let result = false;
      if (connectionName) {
        try {
          const output = execFileSync("nmcli", ["connection", "show", connectionName], { ...util2.execOptsLinux, stdio: ["ignore", "pipe", "ignore"] }).toString();
          const lines = util2.grep(output, "ipv4.method");
          const resultFormat = lines.replace(/\s+/g, " ").trim();
          const dhcStatus = resultFormat.split(" ").slice(1).toString();
          switch (dhcStatus) {
            case "auto":
              result = true;
              break;
            default:
              result = false;
              break;
          }
          return result;
        } catch {
          return DHCPNics.indexOf(iface) >= 0;
        }
      } else {
        return DHCPNics.indexOf(iface) >= 0;
      }
    }
    __name(getLinuxIfaceDHCPstatus, "getLinuxIfaceDHCPstatus");
    function getDarwinIfaceDHCPstatus(iface) {
      let result = false;
      try {
        const output = execFileSync("ipconfig", ["getpacket", iface], { ...util2.execOptsLinux, stdio: ["ignore", "pipe", "ignore"] }).toString();
        const lines = util2.grep(output, "lease_time");
        if (lines.length && lines[0].startsWith("lease_time")) {
          result = true;
        }
      } catch {
        util2.noop();
      }
      return result;
    }
    __name(getDarwinIfaceDHCPstatus, "getDarwinIfaceDHCPstatus");
    function getLinuxIfaceDNSsuffix(connectionName) {
      if (connectionName) {
        try {
          const output = execFileSync("nmcli", ["connection", "show", connectionName], { ...util2.execOptsLinux, stdio: ["ignore", "pipe", "ignore"] }).toString();
          const result = util2.grep(output, "ipv4.dns-search");
          const resultFormat = result.replace(/\s+/g, " ").trim();
          const dnsSuffix = resultFormat.split(" ").slice(1).toString();
          return dnsSuffix === "--" ? "Not defined" : dnsSuffix;
        } catch {
          return "Unknown";
        }
      } else {
        return "Unknown";
      }
    }
    __name(getLinuxIfaceDNSsuffix, "getLinuxIfaceDNSsuffix");
    function getLinuxIfaceIEEE8021xAuth(connectionName) {
      if (connectionName) {
        try {
          const output = execFileSync("nmcli", ["connection", "show", connectionName], { ...util2.execOptsLinux, stdio: ["ignore", "pipe", "ignore"] }).toString();
          const result = util2.grep(output, "802-1x.eap");
          const resultFormat = result.replace(/\s+/g, " ").trim();
          const authenticationProtocol = resultFormat.split(" ").slice(1).toString();
          return authenticationProtocol === "--" ? "" : authenticationProtocol;
        } catch {
          return "Not defined";
        }
      } else {
        return "Not defined";
      }
    }
    __name(getLinuxIfaceIEEE8021xAuth, "getLinuxIfaceIEEE8021xAuth");
    function getLinuxIfaceIEEE8021xState(authenticationProtocol) {
      if (authenticationProtocol) {
        if (authenticationProtocol === "Not defined") {
          return "Disabled";
        }
        return "Enabled";
      } else {
        return "Unknown";
      }
    }
    __name(getLinuxIfaceIEEE8021xState, "getLinuxIfaceIEEE8021xState");
    function testVirtualNic(iface, ifaceName, mac) {
      const virtualMacs = [
        "00:00:00:00:00:00",
        "00:03:FF",
        "00:05:69",
        "00:0C:29",
        "00:0F:4B",
        "00:13:07",
        "00:13:BE",
        "00:15:5d",
        "00:16:3E",
        "00:1C:42",
        "00:21:F6",
        "00:24:0B",
        "00:50:56",
        "00:A0:B1",
        "00:E0:C8",
        "08:00:27",
        "0A:00:27",
        "18:92:2C",
        "16:DF:49",
        "3C:F3:92",
        "54:52:00",
        "FC:15:97"
      ];
      if (mac) {
        return virtualMacs.filter((item) => {
          return mac.toUpperCase().toUpperCase().startsWith(item.substring(0, mac.length));
        }).length > 0 || iface.toLowerCase().indexOf(" virtual ") > -1 || ifaceName.toLowerCase().indexOf(" virtual ") > -1 || iface.toLowerCase().indexOf("vethernet ") > -1 || ifaceName.toLowerCase().indexOf("vethernet ") > -1 || iface.toLowerCase().startsWith("veth") || ifaceName.toLowerCase().startsWith("veth") || iface.toLowerCase().startsWith("vboxnet") || ifaceName.toLowerCase().startsWith("vboxnet");
      } else {
        return false;
      }
    }
    __name(testVirtualNic, "testVirtualNic");
    function networkInterfaces(callback, rescan, defaultString) {
      if (typeof callback === "string") {
        defaultString = callback;
        rescan = true;
        callback = null;
      }
      if (typeof callback === "boolean") {
        rescan = callback;
        callback = null;
        defaultString = "";
      }
      if (typeof rescan === "undefined") {
        rescan = true;
      }
      defaultString = defaultString || "";
      defaultString = "" + defaultString;
      return new Promise((resolve2) => {
        process.nextTick(() => {
          const ifaces = os.networkInterfaces();
          let result = [];
          let nics = [];
          let dnsSuffixes = [];
          let nics8021xInfo = [];
          if (_darwin || _freebsd || _openbsd || _netbsd) {
            if (JSON.stringify(ifaces) === JSON.stringify(_ifaces) && !rescan) {
              result = _networkInterfaces;
              if (callback) {
                callback(result);
              }
              resolve2(result);
            } else {
              const defaultInterface = getDefaultNetworkInterface();
              _ifaces = JSON.parse(JSON.stringify(ifaces));
              nics = getDarwinNics();
              nics.forEach((nic) => {
                let ip4link = "";
                let ip4linksubnet = "";
                let ip6link = "";
                let ip6linksubnet = "";
                nic.ip4 = "";
                nic.ip6 = "";
                if ({}.hasOwnProperty.call(ifaces, nic.iface)) {
                  ifaces[nic.iface].forEach((details) => {
                    if (details.family === "IPv4" || details.family === 4) {
                      if (!nic.ip4 && !nic.ip4.match(/^169.254/i)) {
                        nic.ip4 = details.address;
                        nic.ip4subnet = details.netmask;
                      }
                      if (nic.ip4.match(/^169.254/i)) {
                        ip4link = details.address;
                        ip4linksubnet = details.netmask;
                      }
                    }
                    if (details.family === "IPv6" || details.family === 6) {
                      if (!nic.ip6 && !nic.ip6.match(/^fe80::/i)) {
                        nic.ip6 = details.address;
                        nic.ip6subnet = details.netmask;
                      }
                      if (nic.ip6.match(/^fe80::/i)) {
                        ip6link = details.address;
                        ip6linksubnet = details.netmask;
                      }
                    }
                  });
                }
                if (!nic.ip4 && ip4link) {
                  nic.ip4 = ip4link;
                  nic.ip4subnet = ip4linksubnet;
                }
                if (!nic.ip6 && ip6link) {
                  nic.ip6 = ip6link;
                  nic.ip6subnet = ip6linksubnet;
                }
                const ifaceSanitized = util2.sanitizeString(nic.iface);
                result.push({
                  iface: nic.iface,
                  ifaceName: nic.iface,
                  default: nic.iface === defaultInterface,
                  ip4: nic.ip4,
                  ip4subnet: nic.ip4subnet || "",
                  ip6: nic.ip6,
                  ip6subnet: nic.ip6subnet || "",
                  mac: nic.mac,
                  internal: nic.internal,
                  virtual: nic.internal ? false : testVirtualNic(nic.iface, nic.iface, nic.mac),
                  operstate: nic.operstate,
                  type: nic.type,
                  duplex: nic.duplex,
                  mtu: nic.mtu,
                  speed: nic.speed,
                  dhcp: getDarwinIfaceDHCPstatus(ifaceSanitized),
                  dnsSuffix: "",
                  ieee8021xAuth: "",
                  ieee8021xState: "",
                  carrierChanges: 0
                });
              });
              _networkInterfaces = result;
              if (defaultString.toLowerCase().indexOf("default") >= 0) {
                result = result.filter((item) => item.default);
                if (result.length > 0) {
                  result = result[0];
                } else {
                  result = [];
                }
              }
              if (callback) {
                callback(result);
              }
              resolve2(result);
            }
          }
          if (_linux) {
            if (JSON.stringify(ifaces) === JSON.stringify(_ifaces) && !rescan) {
              result = _networkInterfaces;
              if (callback) {
                callback(result);
              }
              resolve2(result);
            } else {
              _ifaces = JSON.parse(JSON.stringify(ifaces));
              _dhcpNics = getLinuxDHCPNics();
              const defaultInterface = getDefaultNetworkInterface();
              for (let dev in ifaces) {
                let ip4 = "";
                let ip4subnet = "";
                let ip6 = "";
                let ip6subnet = "";
                let mac = "";
                let duplex = "";
                let mtu = "";
                let speed = null;
                let carrierChanges = 0;
                let dhcp = false;
                let dnsSuffix = "";
                let ieee8021xAuth = "";
                let ieee8021xState = "";
                let type = "";
                let ip4link = "";
                let ip4linksubnet = "";
                let ip6link = "";
                let ip6linksubnet = "";
                if ({}.hasOwnProperty.call(ifaces, dev)) {
                  const ifaceName = dev;
                  ifaces[dev].forEach((details) => {
                    if (details.family === "IPv4" || details.family === 4) {
                      if (!ip4 && !ip4.match(/^169.254/i)) {
                        ip4 = details.address;
                        ip4subnet = details.netmask;
                      }
                      if (ip4.match(/^169.254/i)) {
                        ip4link = details.address;
                        ip4linksubnet = details.netmask;
                      }
                    }
                    if (details.family === "IPv6" || details.family === 6) {
                      if (!ip6 && !ip6.match(/^fe80::/i)) {
                        ip6 = details.address;
                        ip6subnet = details.netmask;
                      }
                      if (ip6.match(/^fe80::/i)) {
                        ip6link = details.address;
                        ip6linksubnet = details.netmask;
                      }
                    }
                    mac = details.mac;
                    const nodeMainVersion = parseInt(process.versions.node.split("."), 10);
                    if (mac.indexOf("00:00:0") > -1 && (_linux || _darwin) && !details.internal && nodeMainVersion >= 8 && nodeMainVersion <= 11) {
                      if (Object.keys(_mac).length === 0) {
                        _mac = getMacAddresses();
                      }
                      mac = _mac[dev] || "";
                    }
                  });
                  if (!ip4 && ip4link) {
                    ip4 = ip4link;
                    ip4subnet = ip4linksubnet;
                  }
                  if (!ip6 && ip6link) {
                    ip6 = ip6link;
                    ip6subnet = ip6linksubnet;
                  }
                  const iface = dev.split(":")[0].trim();
                  const ifaceSanitized = util2.sanitizeString(iface);
                  const cmd = `echo -n "addr_assign_type: "; cat /sys/class/net/${ifaceSanitized}/addr_assign_type 2>/dev/null; echo;
            echo -n "address: "; cat /sys/class/net/${ifaceSanitized}/address 2>/dev/null; echo;
            echo -n "addr_len: "; cat /sys/class/net/${ifaceSanitized}/addr_len 2>/dev/null; echo;
            echo -n "broadcast: "; cat /sys/class/net/${ifaceSanitized}/broadcast 2>/dev/null; echo;
            echo -n "carrier: "; cat /sys/class/net/${ifaceSanitized}/carrier 2>/dev/null; echo;
            echo -n "carrier_changes: "; cat /sys/class/net/${ifaceSanitized}/carrier_changes 2>/dev/null; echo;
            echo -n "dev_id: "; cat /sys/class/net/${ifaceSanitized}/dev_id 2>/dev/null; echo;
            echo -n "dev_port: "; cat /sys/class/net/${ifaceSanitized}/dev_port 2>/dev/null; echo;
            echo -n "dormant: "; cat /sys/class/net/${ifaceSanitized}/dormant 2>/dev/null; echo;
            echo -n "duplex: "; cat /sys/class/net/${ifaceSanitized}/duplex 2>/dev/null; echo;
            echo -n "flags: "; cat /sys/class/net/${ifaceSanitized}/flags 2>/dev/null; echo;
            echo -n "gro_flush_timeout: "; cat /sys/class/net/${ifaceSanitized}/gro_flush_timeout 2>/dev/null; echo;
            echo -n "ifalias: "; cat /sys/class/net/${ifaceSanitized}/ifalias 2>/dev/null; echo;
            echo -n "ifindex: "; cat /sys/class/net/${ifaceSanitized}/ifindex 2>/dev/null; echo;
            echo -n "iflink: "; cat /sys/class/net/${ifaceSanitized}/iflink 2>/dev/null; echo;
            echo -n "link_mode: "; cat /sys/class/net/${ifaceSanitized}/link_mode 2>/dev/null; echo;
            echo -n "mtu: "; cat /sys/class/net/${ifaceSanitized}/mtu 2>/dev/null; echo;
            echo -n "netdev_group: "; cat /sys/class/net/${ifaceSanitized}/netdev_group 2>/dev/null; echo;
            echo -n "operstate: "; cat /sys/class/net/${ifaceSanitized}/operstate 2>/dev/null; echo;
            echo -n "proto_down: "; cat /sys/class/net/${ifaceSanitized}/proto_down 2>/dev/null; echo;
            echo -n "speed: "; cat /sys/class/net/${ifaceSanitized}/speed 2>/dev/null; echo;
            echo -n "tx_queue_len: "; cat /sys/class/net/${ifaceSanitized}/tx_queue_len 2>/dev/null; echo;
            echo -n "type: "; cat /sys/class/net/${ifaceSanitized}/type 2>/dev/null; echo;
            echo -n "wireless: "; cat /proc/net/wireless 2>/dev/null | grep ${ifaceSanitized}; echo;
            echo -n "wirelessspeed: "; iw dev ${ifaceSanitized} link 2>&1 | grep bitrate; echo;`;
                  let lines = [];
                  try {
                    lines = execSync(cmd, util2.execOptsLinux).toString().split("\n");
                    const connectionName = getLinuxIfaceConnectionName(ifaceSanitized);
                    dhcp = getLinuxIfaceDHCPstatus(ifaceSanitized, connectionName, _dhcpNics);
                    dnsSuffix = getLinuxIfaceDNSsuffix(connectionName);
                    ieee8021xAuth = getLinuxIfaceIEEE8021xAuth(connectionName);
                    ieee8021xState = getLinuxIfaceIEEE8021xState(ieee8021xAuth);
                  } catch {
                    util2.noop();
                  }
                  duplex = util2.getValue(lines, "duplex");
                  duplex = duplex.startsWith("cat") ? "" : duplex;
                  mtu = parseInt(util2.getValue(lines, "mtu"), 10);
                  let myspeed = parseInt(util2.getValue(lines, "speed"), 10);
                  speed = isNaN(myspeed) ? null : myspeed;
                  const wirelessspeed = util2.getValue(lines, "tx bitrate");
                  if (speed === null && wirelessspeed) {
                    myspeed = parseFloat(wirelessspeed);
                    speed = isNaN(myspeed) ? null : myspeed;
                  }
                  carrierChanges = parseInt(util2.getValue(lines, "carrier_changes"), 10);
                  const operstate = util2.getValue(lines, "operstate");
                  type = operstate === "up" ? util2.getValue(lines, "wireless").trim() ? "wireless" : "wired" : "unknown";
                  if (ifaceSanitized === "lo" || ifaceSanitized.startsWith("bond")) {
                    type = "virtual";
                  }
                  let internal = ifaces[dev] && ifaces[dev][0] ? ifaces[dev][0].internal : false;
                  if (dev.toLowerCase().indexOf("loopback") > -1 || ifaceName.toLowerCase().indexOf("loopback") > -1) {
                    internal = true;
                  }
                  const virtual = internal ? false : testVirtualNic(dev, ifaceName, mac);
                  result.push({
                    iface: ifaceSanitized,
                    ifaceName,
                    default: iface === defaultInterface,
                    ip4,
                    ip4subnet,
                    ip6,
                    ip6subnet,
                    mac,
                    internal,
                    virtual,
                    operstate,
                    type,
                    duplex,
                    mtu,
                    speed,
                    dhcp,
                    dnsSuffix,
                    ieee8021xAuth,
                    ieee8021xState,
                    carrierChanges
                  });
                }
              }
              _networkInterfaces = result;
              if (defaultString.toLowerCase().indexOf("default") >= 0) {
                result = result.filter((item) => item.default);
                if (result.length > 0) {
                  result = result[0];
                } else {
                  result = [];
                }
              }
              if (callback) {
                callback(result);
              }
              resolve2(result);
            }
          }
          if (_windows) {
            if (JSON.stringify(ifaces) === JSON.stringify(_ifaces) && !rescan) {
              result = _networkInterfaces;
              if (callback) {
                callback(result);
              }
              resolve2(result);
            } else {
              _ifaces = JSON.parse(JSON.stringify(ifaces));
              const defaultInterface = getDefaultNetworkInterface();
              getWindowsNics().then((nics2) => {
                nics2.forEach((nic) => {
                  let found = false;
                  Object.keys(ifaces).forEach((key) => {
                    if (!found) {
                      ifaces[key].forEach((value2) => {
                        if (Object.keys(value2).indexOf("mac") >= 0) {
                          found = value2["mac"] === nic.mac;
                        }
                      });
                    }
                  });
                  if (!found) {
                    ifaces[nic.name] = [{ mac: nic.mac }];
                  }
                });
                nics8021xInfo = getWindowsWiredProfilesInformation();
                dnsSuffixes = getWindowsDNSsuffixes();
                for (let dev in ifaces) {
                  const ifaceSanitized = util2.sanitizeString(dev);
                  let iface = dev;
                  let ip4 = "";
                  let ip4subnet = "";
                  let ip6 = "";
                  let ip6subnet = "";
                  let mac = "";
                  let duplex = "";
                  let mtu = "";
                  let speed = null;
                  let carrierChanges = 0;
                  let operstate = "down";
                  let dhcp = false;
                  let dnsSuffix = "";
                  let ieee8021xAuth = "";
                  let ieee8021xState = "";
                  let type = "";
                  if ({}.hasOwnProperty.call(ifaces, dev)) {
                    let ifaceName = dev;
                    ifaces[dev].forEach((details) => {
                      if (details.family === "IPv4" || details.family === 4) {
                        ip4 = details.address;
                        ip4subnet = details.netmask;
                      }
                      if (details.family === "IPv6" || details.family === 6) {
                        if (!ip6 || ip6.match(/^fe80::/i)) {
                          ip6 = details.address;
                          ip6subnet = details.netmask;
                        }
                      }
                      mac = details.mac;
                      const nodeMainVersion = parseInt(process.versions.node.split("."), 10);
                      if (mac.indexOf("00:00:0") > -1 && (_linux || _darwin) && !details.internal && nodeMainVersion >= 8 && nodeMainVersion <= 11) {
                        if (Object.keys(_mac).length === 0) {
                          _mac = getMacAddresses();
                        }
                        mac = _mac[dev] || "";
                      }
                    });
                    dnsSuffix = getWindowsIfaceDNSsuffix(dnsSuffixes.ifaces, ifaceSanitized);
                    let foundFirst = false;
                    nics2.forEach((detail) => {
                      if (detail.mac === mac && !foundFirst) {
                        iface = detail.iface || iface;
                        ifaceName = detail.name;
                        dhcp = detail.dhcp;
                        operstate = detail.operstate;
                        speed = operstate === "up" ? detail.speed : 0;
                        type = detail.type;
                        foundFirst = true;
                      }
                    });
                    if (dev.toLowerCase().indexOf("wlan") >= 0 || ifaceName.toLowerCase().indexOf("wlan") >= 0 || ifaceName.toLowerCase().indexOf("802.11n") >= 0 || ifaceName.toLowerCase().indexOf("wireless") >= 0 || ifaceName.toLowerCase().indexOf("wi-fi") >= 0 || ifaceName.toLowerCase().indexOf("wifi") >= 0) {
                      type = "wireless";
                    }
                    const IEEE8021x = getWindowsIEEE8021x(type, ifaceSanitized, nics8021xInfo);
                    ieee8021xAuth = IEEE8021x.protocol;
                    ieee8021xState = IEEE8021x.state;
                    let internal = ifaces[dev] && ifaces[dev][0] ? ifaces[dev][0].internal : false;
                    if (dev.toLowerCase().indexOf("loopback") > -1 || ifaceName.toLowerCase().indexOf("loopback") > -1) {
                      internal = true;
                    }
                    const virtual = internal ? false : testVirtualNic(dev, ifaceName, mac);
                    result.push({
                      iface,
                      ifaceName,
                      default: iface === defaultInterface,
                      ip4,
                      ip4subnet,
                      ip6,
                      ip6subnet,
                      mac,
                      internal,
                      virtual,
                      operstate,
                      type,
                      duplex,
                      mtu,
                      speed,
                      dhcp,
                      dnsSuffix,
                      ieee8021xAuth,
                      ieee8021xState,
                      carrierChanges
                    });
                  }
                }
                _networkInterfaces = result;
                if (defaultString.toLowerCase().indexOf("default") >= 0) {
                  result = result.filter((item) => item.default);
                  if (result.length > 0) {
                    result = result[0];
                  } else {
                    result = [];
                  }
                }
                if (callback) {
                  callback(result);
                }
                resolve2(result);
              });
            }
          }
        });
      });
    }
    __name(networkInterfaces, "networkInterfaces");
    exports.networkInterfaces = networkInterfaces;
    function calcNetworkSpeed(iface, rx_bytes, tx_bytes, operstate, rx_dropped, rx_errors, tx_dropped, tx_errors) {
      const result = {
        iface,
        operstate,
        rx_bytes,
        rx_dropped,
        rx_errors,
        tx_bytes,
        tx_dropped,
        tx_errors,
        rx_sec: null,
        tx_sec: null,
        ms: 0
      };
      if (_network[iface] && _network[iface].ms) {
        result.ms = Date.now() - _network[iface].ms;
        result.rx_sec = rx_bytes - _network[iface].rx_bytes >= 0 ? (rx_bytes - _network[iface].rx_bytes) / (result.ms / 1e3) : 0;
        result.tx_sec = tx_bytes - _network[iface].tx_bytes >= 0 ? (tx_bytes - _network[iface].tx_bytes) / (result.ms / 1e3) : 0;
        _network[iface].rx_bytes = rx_bytes;
        _network[iface].tx_bytes = tx_bytes;
        _network[iface].rx_sec = result.rx_sec;
        _network[iface].tx_sec = result.tx_sec;
        _network[iface].ms = Date.now();
        _network[iface].last_ms = result.ms;
        _network[iface].operstate = operstate;
      } else {
        if (!_network[iface]) {
          _network[iface] = {};
        }
        _network[iface].rx_bytes = rx_bytes;
        _network[iface].tx_bytes = tx_bytes;
        _network[iface].rx_sec = null;
        _network[iface].tx_sec = null;
        _network[iface].ms = Date.now();
        _network[iface].last_ms = 0;
        _network[iface].operstate = operstate;
      }
      return result;
    }
    __name(calcNetworkSpeed, "calcNetworkSpeed");
    function networkStats(ifaces, callback) {
      let ifacesArray = [];
      return new Promise((resolve2) => {
        process.nextTick(() => {
          if (util2.isFunction(ifaces) && !callback) {
            callback = ifaces;
            ifacesArray = [getDefaultNetworkInterface()];
          } else {
            if (typeof ifaces !== "string" && ifaces !== void 0) {
              if (callback) {
                callback([]);
              }
              return resolve2([]);
            }
            ifaces = ifaces || getDefaultNetworkInterface();
            try {
              ifaces.__proto__.toLowerCase = util2.stringToLower;
              ifaces.__proto__.replace = util2.stringReplace;
              ifaces.__proto__.toString = util2.stringToString;
              ifaces.__proto__.substr = util2.stringSubstr;
              ifaces.__proto__.substring = util2.stringSubstring;
              ifaces.__proto__.trim = util2.stringTrim;
              ifaces.__proto__.startsWith = util2.stringStartWith;
            } catch {
              Object.setPrototypeOf(ifaces, util2.stringObj);
            }
            ifaces = ifaces.trim().replace(/,+/g, "|");
            ifacesArray = ifaces.split("|");
          }
          const result = [];
          const workload = [];
          if (ifacesArray.length && ifacesArray[0].trim() === "*") {
            ifacesArray = [];
            networkInterfaces(false).then((allIFaces) => {
              for (let iface of allIFaces) {
                ifacesArray.push(iface.iface);
              }
              networkStats(ifacesArray.join(",")).then((result2) => {
                if (callback) {
                  callback(result2);
                }
                resolve2(result2);
              });
            });
          } else {
            for (let iface of ifacesArray) {
              workload.push(networkStatsSingle(iface.trim()));
            }
            if (workload.length) {
              Promise.all(workload).then((data) => {
                if (callback) {
                  callback(data);
                }
                resolve2(data);
              });
            } else {
              if (callback) {
                callback(result);
              }
              resolve2(result);
            }
          }
        });
      });
    }
    __name(networkStats, "networkStats");
    function networkStatsSingle(iface) {
      function parseLinesWindowsPerfData(sections) {
        const perfData = [];
        for (let i in sections) {
          if ({}.hasOwnProperty.call(sections, i)) {
            if (sections[i].trim() !== "") {
              const lines = sections[i].trim().split("\r\n");
              perfData.push({
                name: util2.getValue(lines, "Name", ":").toLowerCase(),
                desc: util2.getValue(lines, "InterfaceDescription", ":").replace(/[()[\] ]+/g, "").replace(/#|\//g, "_").toLowerCase(),
                rx_bytes: parseInt(util2.getValue(lines, "ReceivedBytes", ":"), 10),
                rx_errors: parseInt(util2.getValue(lines, "ReceivedPacketErrors", ":"), 10),
                rx_dropped: parseInt(util2.getValue(lines, "ReceivedDiscardedPackets", ":"), 10),
                tx_bytes: parseInt(util2.getValue(lines, "SentBytes", ":"), 10),
                tx_errors: parseInt(util2.getValue(lines, "OutboundPacketErrors", ":"), 10),
                tx_dropped: parseInt(util2.getValue(lines, "OutboundDiscardedPackets", ":"), 10)
              });
            }
          }
        }
        return perfData;
      }
      __name(parseLinesWindowsPerfData, "parseLinesWindowsPerfData");
      return new Promise((resolve2) => {
        process.nextTick(() => {
          const ifaceSanitized = util2.sanitizeString(iface, true);
          let result = {
            iface: ifaceSanitized,
            operstate: "unknown",
            rx_bytes: 0,
            rx_dropped: 0,
            rx_errors: 0,
            tx_bytes: 0,
            tx_dropped: 0,
            tx_errors: 0,
            rx_sec: null,
            tx_sec: null,
            ms: 0
          };
          let operstate = "unknown";
          let rx_bytes = 0;
          let tx_bytes = 0;
          let rx_dropped = 0;
          let rx_errors = 0;
          let tx_dropped = 0;
          let tx_errors = 0;
          let cmd, lines, stats;
          if (!_network[ifaceSanitized] || _network[ifaceSanitized] && !_network[ifaceSanitized].ms || _network[ifaceSanitized] && _network[ifaceSanitized].ms && Date.now() - _network[ifaceSanitized].ms >= 500) {
            if (_linux) {
              if (fs4.existsSync("/sys/class/net/" + ifaceSanitized)) {
                cmd = "cat /sys/class/net/" + ifaceSanitized + "/operstate; cat /sys/class/net/" + ifaceSanitized + "/statistics/rx_bytes; cat /sys/class/net/" + ifaceSanitized + "/statistics/tx_bytes; cat /sys/class/net/" + ifaceSanitized + "/statistics/rx_dropped; cat /sys/class/net/" + ifaceSanitized + "/statistics/rx_errors; cat /sys/class/net/" + ifaceSanitized + "/statistics/tx_dropped; cat /sys/class/net/" + ifaceSanitized + "/statistics/tx_errors; ";
                exec(cmd, (error, stdout) => {
                  if (!error) {
                    lines = stdout.toString().split("\n");
                    operstate = lines[0].trim();
                    rx_bytes = parseInt(lines[1], 10);
                    tx_bytes = parseInt(lines[2], 10);
                    rx_dropped = parseInt(lines[3], 10);
                    rx_errors = parseInt(lines[4], 10);
                    tx_dropped = parseInt(lines[5], 10);
                    tx_errors = parseInt(lines[6], 10);
                    result = calcNetworkSpeed(ifaceSanitized, rx_bytes, tx_bytes, operstate, rx_dropped, rx_errors, tx_dropped, tx_errors);
                  }
                  resolve2(result);
                });
              } else {
                resolve2(result);
              }
            }
            if (_freebsd || _openbsd || _netbsd) {
              cmd = "netstat -ibndI " + ifaceSanitized;
              exec(cmd, (error, stdout) => {
                if (!error) {
                  lines = stdout.toString().split("\n");
                  for (let i = 1; i < lines.length; i++) {
                    const line = lines[i].replace(/ +/g, " ").split(" ");
                    if (line && line[0] && line[7] && line[10]) {
                      rx_bytes = rx_bytes + parseInt(line[7]);
                      if (line[6].trim() !== "-") {
                        rx_dropped = rx_dropped + parseInt(line[6]);
                      }
                      if (line[5].trim() !== "-") {
                        rx_errors = rx_errors + parseInt(line[5]);
                      }
                      tx_bytes = tx_bytes + parseInt(line[10]);
                      if (line[12] && line[12].trim() !== "-") {
                        tx_dropped = tx_dropped + parseInt(line[12]);
                      }
                      if (line[9].trim() !== "-") {
                        tx_errors = tx_errors + parseInt(line[9]);
                      }
                      operstate = "up";
                    }
                  }
                  result = calcNetworkSpeed(ifaceSanitized, rx_bytes, tx_bytes, operstate, rx_dropped, rx_errors, tx_dropped, tx_errors);
                }
                resolve2(result);
              });
            }
            if (_darwin) {
              cmd = "ifconfig " + ifaceSanitized + ' | grep "status"';
              exec(cmd, (error, stdout) => {
                result.operstate = (stdout.toString().split(":")[1] || "").trim();
                result.operstate = (result.operstate || "").toLowerCase();
                result.operstate = result.operstate === "active" ? "up" : result.operstate === "inactive" ? "down" : "unknown";
                cmd = "netstat -bdnI " + ifaceSanitized;
                exec(cmd, (error2, stdout2) => {
                  if (!error2) {
                    lines = stdout2.toString().split("\n");
                    if (lines.length > 1 && lines[1].trim() !== "") {
                      stats = lines[1].replace(/ +/g, " ").split(" ");
                      const offset = stats.length > 11 ? 1 : 0;
                      rx_bytes = parseInt(stats[offset + 5]);
                      rx_dropped = parseInt(stats[offset + 10]);
                      rx_errors = parseInt(stats[offset + 4]);
                      tx_bytes = parseInt(stats[offset + 8]);
                      tx_dropped = parseInt(stats[offset + 10]);
                      tx_errors = parseInt(stats[offset + 7]);
                      result = calcNetworkSpeed(ifaceSanitized, rx_bytes, tx_bytes, result.operstate, rx_dropped, rx_errors, tx_dropped, tx_errors);
                    }
                  }
                  resolve2(result);
                });
              });
            }
            if (_windows) {
              let perfData = [];
              let ifaceName = ifaceSanitized;
              util2.powerShell(
                "Get-NetAdapterStatistics | select Name,InterfaceDescription,ReceivedBytes,ReceivedPacketErrors,ReceivedDiscardedPackets,SentBytes,OutboundPacketErrors,OutboundDiscardedPackets | fl"
              ).then((stdout, error) => {
                if (!error) {
                  const psections = stdout.toString().split(/\n\s*\n/);
                  perfData = parseLinesWindowsPerfData(psections);
                }
                networkInterfaces(false).then((interfaces) => {
                  rx_bytes = 0;
                  tx_bytes = 0;
                  perfData.forEach((detail) => {
                    interfaces.forEach((det) => {
                      if ((det.iface.toLowerCase() === ifaceSanitized.toLowerCase() || det.mac.toLowerCase() === ifaceSanitized.toLowerCase() || det.ip4.toLowerCase() === ifaceSanitized.toLowerCase() || det.ip6.toLowerCase() === ifaceSanitized.toLowerCase() || det.ifaceName.replace(/[()[\] ]+/g, "").replace(/#|\//g, "_").toLowerCase() === ifaceSanitized.replace(/[()[\] ]+/g, "").replace("#", "_").toLowerCase()) && (det.iface.toLowerCase() === detail.name || det.ifaceName.replace(/[()[\] ]+/g, "").replace(/#|\//g, "_").toLowerCase() === detail.desc)) {
                        ifaceName = det.iface;
                        rx_bytes = detail.rx_bytes;
                        rx_dropped = detail.rx_dropped;
                        rx_errors = detail.rx_errors;
                        tx_bytes = detail.tx_bytes;
                        tx_dropped = detail.tx_dropped;
                        tx_errors = detail.tx_errors;
                        operstate = det.operstate;
                      }
                    });
                  });
                  if (rx_bytes && tx_bytes) {
                    result = calcNetworkSpeed(ifaceName, parseInt(rx_bytes), parseInt(tx_bytes), operstate, rx_dropped, rx_errors, tx_dropped, tx_errors);
                  }
                  resolve2(result);
                });
              });
            }
          } else {
            result.rx_bytes = _network[ifaceSanitized].rx_bytes;
            result.tx_bytes = _network[ifaceSanitized].tx_bytes;
            result.rx_sec = _network[ifaceSanitized].rx_sec;
            result.tx_sec = _network[ifaceSanitized].tx_sec;
            result.ms = _network[ifaceSanitized].last_ms;
            result.operstate = _network[ifaceSanitized].operstate;
            resolve2(result);
          }
        });
      });
    }
    __name(networkStatsSingle, "networkStatsSingle");
    exports.networkStats = networkStats;
    function getProcessName(processes, pid) {
      let cmd = "";
      processes.forEach((line) => {
        const parts = line.split(" ");
        const id = parseInt(parts[0], 10) || -1;
        if (id === pid) {
          parts.shift();
          cmd = parts.join(" ").split(":")[0];
        }
      });
      cmd = cmd.split(" -")[0];
      cmd = cmd.split(" /")[0];
      return cmd;
    }
    __name(getProcessName, "getProcessName");
    function networkConnections(callback) {
      return new Promise((resolve2) => {
        process.nextTick(() => {
          const result = [];
          if (_linux || _freebsd || _openbsd || _netbsd) {
            let cmd = 'export LC_ALL=C; netstat -tunap | grep "ESTABLISHED\\|SYN_SENT\\|SYN_RECV\\|FIN_WAIT1\\|FIN_WAIT2\\|TIME_WAIT\\|CLOSE\\|CLOSE_WAIT\\|LAST_ACK\\|LISTEN\\|CLOSING\\|UNKNOWN"; unset LC_ALL';
            if (_freebsd || _openbsd || _netbsd) {
              cmd = 'export LC_ALL=C; netstat -na | grep "ESTABLISHED\\|SYN_SENT\\|SYN_RECV\\|FIN_WAIT1\\|FIN_WAIT2\\|TIME_WAIT\\|CLOSE\\|CLOSE_WAIT\\|LAST_ACK\\|LISTEN\\|CLOSING\\|UNKNOWN"; unset LC_ALL';
            }
            exec(cmd, { maxBuffer: 1024 * 102400 }, (error, stdout) => {
              let lines = stdout.toString().split("\n");
              if (!error && (lines.length > 1 || lines[0] !== "")) {
                lines.forEach((line) => {
                  line = line.replace(/ +/g, " ").split(" ");
                  if (line.length >= 7) {
                    let localip = line[3];
                    let localport = "";
                    const localaddress = line[3].split(":");
                    if (localaddress.length > 1) {
                      localport = localaddress[localaddress.length - 1];
                      localaddress.pop();
                      localip = localaddress.join(":");
                    }
                    let peerip = line[4];
                    let peerport = "";
                    const peeraddress = line[4].split(":");
                    if (peeraddress.length > 1) {
                      peerport = peeraddress[peeraddress.length - 1];
                      peeraddress.pop();
                      peerip = peeraddress.join(":");
                    }
                    const connstate = line[5];
                    const proc = line[6].split("/");
                    if (connstate) {
                      result.push({
                        protocol: line[0],
                        localAddress: localip,
                        localPort: localport,
                        peerAddress: peerip,
                        peerPort: peerport,
                        state: connstate,
                        pid: proc[0] && proc[0] !== "-" ? parseInt(proc[0], 10) : null,
                        process: proc[1] ? proc[1].split(" ")[0].split(":")[0] : ""
                      });
                    }
                  }
                });
                if (callback) {
                  callback(result);
                }
                resolve2(result);
              } else {
                cmd = 'ss -tunap | grep "ESTAB\\|SYN-SENT\\|SYN-RECV\\|FIN-WAIT1\\|FIN-WAIT2\\|TIME-WAIT\\|CLOSE\\|CLOSE-WAIT\\|LAST-ACK\\|LISTEN\\|CLOSING"';
                exec(cmd, { maxBuffer: 1024 * 102400 }, (error2, stdout2) => {
                  if (!error2) {
                    const lines2 = stdout2.toString().split("\n");
                    lines2.forEach((line) => {
                      line = line.replace(/ +/g, " ").split(" ");
                      if (line.length >= 6) {
                        let localip = line[4];
                        let localport = "";
                        const localaddress = line[4].split(":");
                        if (localaddress.length > 1) {
                          localport = localaddress[localaddress.length - 1];
                          localaddress.pop();
                          localip = localaddress.join(":");
                        }
                        let peerip = line[5];
                        let peerport = "";
                        const peeraddress = line[5].split(":");
                        if (peeraddress.length > 1) {
                          peerport = peeraddress[peeraddress.length - 1];
                          peeraddress.pop();
                          peerip = peeraddress.join(":");
                        }
                        let connstate = line[1];
                        if (connstate === "ESTAB") {
                          connstate = "ESTABLISHED";
                        }
                        if (connstate === "TIME-WAIT") {
                          connstate = "TIME_WAIT";
                        }
                        let pid = null;
                        let process2 = "";
                        if (line.length >= 7 && line[6].indexOf("users:") > -1) {
                          const proc = line[6].replace('users:(("', "").replace(/"/g, "").replace("pid=", "").split(",");
                          if (proc.length > 2) {
                            process2 = proc[0];
                            const pidValue = parseInt(proc[1], 10);
                            if (pidValue > 0) {
                              pid = pidValue;
                            }
                          }
                        }
                        if (connstate) {
                          result.push({
                            protocol: line[0],
                            localAddress: localip,
                            localPort: localport,
                            peerAddress: peerip,
                            peerPort: peerport,
                            state: connstate,
                            pid,
                            process: process2
                          });
                        }
                      }
                    });
                  }
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                });
              }
            });
          }
          if (_darwin) {
            const cmd = 'netstat -natvln | head -n2; netstat -natvln | grep "tcp4\\|tcp6\\|udp4\\|udp6"';
            const states = "ESTABLISHED|SYN_SENT|SYN_RECV|FIN_WAIT1|FIN_WAIT_1|FIN_WAIT2|FIN_WAIT_2|TIME_WAIT|CLOSE|CLOSE_WAIT|LAST_ACK|LISTEN|CLOSING|UNKNOWN".split("|");
            exec(cmd, { maxBuffer: 1024 * 102400 }, (error, stdout) => {
              if (!error) {
                exec("ps -axo pid,command", { maxBuffer: 1024 * 102400 }, (err2, stdout2) => {
                  let processes = stdout2.toString().split("\n");
                  processes = processes.map((line) => {
                    return line.trim().replace(/ +/g, " ");
                  });
                  const lines = stdout.toString().split("\n");
                  lines.shift();
                  let pidPos = 8;
                  if (lines.length > 1 && lines[0].indexOf("pid") > 0) {
                    const header = (lines.shift() || "").replace(/ Address/g, "_Address").replace(/process:/g, "").replace(/ +/g, " ").split(" ");
                    pidPos = header.indexOf("pid");
                  }
                  lines.forEach((line) => {
                    line = line.replace(/ +/g, " ").split(" ");
                    if (line.length >= 8) {
                      let localip = line[3];
                      let localport = "";
                      const localaddress = line[3].split(".");
                      if (localaddress.length > 1) {
                        localport = localaddress[localaddress.length - 1];
                        localaddress.pop();
                        localip = localaddress.join(".");
                      }
                      let peerip = line[4];
                      let peerport = "";
                      const peeraddress = line[4].split(".");
                      if (peeraddress.length > 1) {
                        peerport = peeraddress[peeraddress.length - 1];
                        peeraddress.pop();
                        peerip = peeraddress.join(".");
                      }
                      const hasState = states.indexOf(line[5]) >= 0;
                      const connstate = hasState ? line[5] : "UNKNOWN";
                      let pidField = "";
                      if (line[line.length - 9] && line[line.length - 9].indexOf(":") >= 0) {
                        pidField = line[line.length - 9].split(":")[1];
                      } else {
                        pidField = line[pidPos + (hasState ? 0 : -1)] || "";
                        if (pidField.indexOf(":") >= 0) {
                          pidField = pidField.split(":")[1];
                        }
                      }
                      const pid = parseInt(pidField, 10);
                      if (connstate) {
                        result.push({
                          protocol: line[0],
                          localAddress: localip,
                          localPort: localport,
                          peerAddress: peerip,
                          peerPort: peerport,
                          state: connstate,
                          pid,
                          process: getProcessName(processes, pid)
                        });
                      }
                    }
                  });
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                });
              } else {
                if (callback) {
                  callback(result);
                }
                resolve2(result);
              }
            });
          }
          if (_windows) {
            let cmd = "netstat -nao";
            try {
              exec(cmd, util2.execOptsWin, (error, stdout) => {
                if (!error) {
                  let lines = stdout.toString().split("\r\n");
                  lines.forEach((line) => {
                    line = line.trim().replace(/ +/g, " ").split(" ");
                    if (line.length >= 4) {
                      let localip = line[1];
                      let localport = "";
                      const localaddress = line[1].split(":");
                      if (localaddress.length > 1) {
                        localport = localaddress[localaddress.length - 1];
                        localaddress.pop();
                        localip = localaddress.join(":");
                      }
                      localip = localip.replace(/\[/g, "").replace(/\]/g, "");
                      let peerip = line[2];
                      let peerport = "";
                      const peeraddress = line[2].split(":");
                      if (peeraddress.length > 1) {
                        peerport = peeraddress[peeraddress.length - 1];
                        peeraddress.pop();
                        peerip = peeraddress.join(":");
                      }
                      peerip = peerip.replace(/\[/g, "").replace(/\]/g, "");
                      const pid = util2.toInt(line[4]);
                      let connstate = line[3];
                      if (connstate === "HERGESTELLT") {
                        connstate = "ESTABLISHED";
                      }
                      if (connstate.startsWith("ABH")) {
                        connstate = "LISTEN";
                      }
                      if (connstate === "SCHLIESSEN_WARTEN") {
                        connstate = "CLOSE_WAIT";
                      }
                      if (connstate === "WARTEND") {
                        connstate = "TIME_WAIT";
                      }
                      if (connstate === "SYN_GESENDET") {
                        connstate = "SYN_SENT";
                      }
                      if (connstate === "LISTENING") {
                        connstate = "LISTEN";
                      }
                      if (connstate === "SYN_RECEIVED") {
                        connstate = "SYN_RECV";
                      }
                      if (connstate === "FIN_WAIT_1") {
                        connstate = "FIN_WAIT1";
                      }
                      if (connstate === "FIN_WAIT_2") {
                        connstate = "FIN_WAIT2";
                      }
                      if (line[0].toLowerCase() !== "udp" && connstate) {
                        result.push({
                          protocol: line[0].toLowerCase(),
                          localAddress: localip,
                          localPort: localport,
                          peerAddress: peerip,
                          peerPort: peerport,
                          state: connstate,
                          pid,
                          process: ""
                        });
                      } else if (line[0].toLowerCase() === "udp") {
                        result.push({
                          protocol: line[0].toLowerCase(),
                          localAddress: localip,
                          localPort: localport,
                          peerAddress: peerip,
                          peerPort: peerport,
                          state: "",
                          pid: parseInt(line[3], 10),
                          process: ""
                        });
                      }
                    }
                  });
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                } else {
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                }
              });
            } catch {
              if (callback) {
                callback(result);
              }
              resolve2(result);
            }
          }
        });
      });
    }
    __name(networkConnections, "networkConnections");
    exports.networkConnections = networkConnections;
    function networkGatewayDefault(callback) {
      return new Promise((resolve2) => {
        process.nextTick(() => {
          let result = "";
          if (_linux || _freebsd || _openbsd || _netbsd) {
            const cmd = "ip route get 1";
            try {
              exec(cmd, { maxBuffer: 1024 * 102400 }, (error, stdout) => {
                if (!error) {
                  let lines = stdout.toString().split("\n");
                  const line = lines && lines[0] ? lines[0] : "";
                  let parts = line.split(" via ");
                  if (parts && parts[1]) {
                    parts = parts[1].split(" ");
                    result = parts[0];
                  }
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                } else {
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                }
              });
            } catch {
              if (callback) {
                callback(result);
              }
              resolve2(result);
            }
          }
          if (_darwin) {
            let cmd = "route -n get default";
            try {
              exec(cmd, { maxBuffer: 1024 * 102400 }, (error, stdout) => {
                if (!error) {
                  const lines = stdout.toString().split("\n").map((line) => line.trim());
                  result = util2.getValue(lines, "gateway");
                }
                if (!result) {
                  cmd = "netstat -rn | awk '/default/ {print $2}'";
                  exec(cmd, { maxBuffer: 1024 * 102400 }, (error2, stdout2) => {
                    const lines = stdout2.toString().split("\n").map((line) => line.trim());
                    result = lines.find(
                      (line) => /^(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$/.test(line)
                    );
                    if (callback) {
                      callback(result);
                    }
                    resolve2(result);
                  });
                } else {
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                }
              });
            } catch {
              if (callback) {
                callback(result);
              }
              resolve2(result);
            }
          }
          if (_windows) {
            try {
              exec("netstat -r", util2.execOptsWin, (error, stdout) => {
                const lines = stdout.toString().split(os.EOL);
                lines.forEach((line) => {
                  line = line.replace(/\s+/g, " ").trim();
                  if (line.indexOf("0.0.0.0 0.0.0.0") > -1 && !/[a-zA-Z]/.test(line)) {
                    const parts = line.split(" ");
                    if (parts.length >= 5 && parts[parts.length - 3].indexOf(".") > -1) {
                      result = parts[parts.length - 3];
                    }
                  }
                });
                if (!result) {
                  util2.powerShell("Get-CimInstance -ClassName Win32_IP4RouteTable | Where-Object { $_.Destination -eq '0.0.0.0' -and $_.Mask -eq '0.0.0.0' }").then((data) => {
                    let lines2 = data.toString().split("\r\n");
                    if (lines2.length > 1 && !result) {
                      result = util2.getValue(lines2, "NextHop");
                      if (callback) {
                        callback(result);
                      }
                      resolve2(result);
                    }
                  });
                } else {
                  if (callback) {
                    callback(result);
                  }
                  resolve2(result);
                }
              });
            } catch {
              if (callback) {
                callback(result);
              }
              resolve2(result);
            }
          }
        });
      });
    }
    __name(networkGatewayDefault, "networkGatewayDefault");
    exports.networkGatewayDefault = networkGatewayDefault;
  }
});

// ../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/stats/si.js
var require_si = __commonJS({
  "../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/stats/si.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getNetworkData = void 0;
    var network_1 = require_network();
    function getNetworkData() {
      return new Promise((resolve2) => {
        (0, network_1.networkStats)().then(resolve2).catch(() => {
          resolve2([]);
        });
      });
    }
    __name(getNetworkData, "getNetworkData");
    exports.getNetworkData = getNetworkData;
  }
});

// ../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/metric.js
var require_metric = __commonJS({
  "../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/metric.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HostMetrics = void 0;
    var BaseMetrics_1 = require_BaseMetrics();
    var semconv_1 = require_semconv2();
    var common_1 = require_common();
    var si_1 = require_si();
    var HostMetrics2 = class extends BaseMetrics_1.BaseMetrics {
      static {
        __name(this, "HostMetrics");
      }
      _batchUpdateCpuUsages(observableResult, cpuUsages) {
        for (let i = 0, j = cpuUsages.length; i < j; i++) {
          const cpuUsage = cpuUsages[i];
          observableResult.observe(this._cpuTime, cpuUsage.user, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_USER,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuTime, cpuUsage.system, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_SYSTEM,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuTime, cpuUsage.idle, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_IDLE,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuTime, cpuUsage.interrupt, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_INTERRUPT,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuTime, cpuUsage.nice, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_NICE,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuUtilization, cpuUsage.userP, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_USER,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuUtilization, cpuUsage.systemP, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_SYSTEM,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuUtilization, cpuUsage.idleP, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_IDLE,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuUtilization, cpuUsage.interruptP, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_INTERRUPT,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
          observableResult.observe(this._cpuUtilization, cpuUsage.niceP, {
            [semconv_1.ATTR_SYSTEM_CPU_STATE]: semconv_1.SYSTEM_CPU_STATE_VALUE_NICE,
            [semconv_1.ATTR_SYSTEM_CPU_LOGICAL_NUMBER]: cpuUsage.cpuNumber
          });
        }
      }
      _batchUpdateProcessCpuUsages(observableResult, processCpuUsage) {
        observableResult.observe(this._processCpuTime, processCpuUsage.user, {
          [semconv_1.ATTR_PROCESS_CPU_STATE]: semconv_1.PROCESS_CPU_STATE_VALUE_USER
        });
        observableResult.observe(this._processCpuTime, processCpuUsage.system, {
          [semconv_1.ATTR_PROCESS_CPU_STATE]: semconv_1.PROCESS_CPU_STATE_VALUE_SYSTEM
        });
        observableResult.observe(this._processCpuUtilization, processCpuUsage.userP, {
          [semconv_1.ATTR_PROCESS_CPU_STATE]: semconv_1.PROCESS_CPU_STATE_VALUE_USER
        });
        observableResult.observe(this._processCpuUtilization, processCpuUsage.systemP, {
          [semconv_1.ATTR_PROCESS_CPU_STATE]: semconv_1.PROCESS_CPU_STATE_VALUE_SYSTEM
        });
      }
      _batchUpdateMemUsages(observableResult, memUsage) {
        observableResult.observe(this._memoryUsage, memUsage.used, {
          [semconv_1.ATTR_SYSTEM_MEMORY_STATE]: semconv_1.SYSTEM_MEMORY_STATE_VALUE_USED
        });
        observableResult.observe(this._memoryUsage, memUsage.free, {
          [semconv_1.ATTR_SYSTEM_MEMORY_STATE]: semconv_1.SYSTEM_MEMORY_STATE_VALUE_FREE
        });
        observableResult.observe(this._memoryUtilization, memUsage.usedP, {
          [semconv_1.ATTR_SYSTEM_MEMORY_STATE]: semconv_1.SYSTEM_MEMORY_STATE_VALUE_USED
        });
        observableResult.observe(this._memoryUtilization, memUsage.freeP, {
          [semconv_1.ATTR_SYSTEM_MEMORY_STATE]: semconv_1.SYSTEM_MEMORY_STATE_VALUE_FREE
        });
      }
      _batchUpdateProcessMemUsage(observableResult, memoryUsage) {
        observableResult.observe(this._processMemoryUsage, memoryUsage);
      }
      _batchUpdateNetworkData(observableResult, networkUsages) {
        for (let i = 0, j = networkUsages.length; i < j; i++) {
          const networkUsage = networkUsages[i];
          observableResult.observe(this._networkDropped, networkUsage.rx_dropped, {
            [semconv_1.ATTR_SYSTEM_DEVICE]: networkUsage.iface,
            [semconv_1.ATTR_NETWORK_IO_DIRECTION]: semconv_1.NETWORK_IO_DIRECTION_VALUE_RECEIVE
          });
          observableResult.observe(this._networkDropped, networkUsage.tx_dropped, {
            [semconv_1.ATTR_SYSTEM_DEVICE]: networkUsage.iface,
            [semconv_1.ATTR_NETWORK_IO_DIRECTION]: semconv_1.NETWORK_IO_DIRECTION_VALUE_TRANSMIT
          });
          observableResult.observe(this._networkErrors, networkUsage.rx_errors, {
            [semconv_1.ATTR_SYSTEM_DEVICE]: networkUsage.iface,
            [semconv_1.ATTR_NETWORK_IO_DIRECTION]: semconv_1.NETWORK_IO_DIRECTION_VALUE_RECEIVE
          });
          observableResult.observe(this._networkErrors, networkUsage.tx_errors, {
            [semconv_1.ATTR_SYSTEM_DEVICE]: networkUsage.iface,
            [semconv_1.ATTR_NETWORK_IO_DIRECTION]: semconv_1.NETWORK_IO_DIRECTION_VALUE_TRANSMIT
          });
          observableResult.observe(this._networkIo, networkUsage.rx_bytes, {
            [semconv_1.ATTR_SYSTEM_DEVICE]: networkUsage.iface,
            [semconv_1.ATTR_NETWORK_IO_DIRECTION]: semconv_1.NETWORK_IO_DIRECTION_VALUE_RECEIVE
          });
          observableResult.observe(this._networkIo, networkUsage.tx_bytes, {
            [semconv_1.ATTR_SYSTEM_DEVICE]: networkUsage.iface,
            [semconv_1.ATTR_NETWORK_IO_DIRECTION]: semconv_1.NETWORK_IO_DIRECTION_VALUE_TRANSMIT
          });
        }
      }
      /**
       * Creates metrics
       */
      _createMetrics() {
        const observables = [];
        const systemCpuGroupEnabled = !this._metricGroups || this._metricGroups.includes("system.cpu");
        const systemMemoryGroupEnabled = !this._metricGroups || this._metricGroups.includes("system.memory");
        const systemNetworkGroupEnabled = !this._metricGroups || this._metricGroups.includes("system.network");
        const processCpuGroupEnabled = !this._metricGroups || this._metricGroups.includes("process.cpu");
        const processMemoryGroupEnabled = !this._metricGroups || this._metricGroups.includes("process.memory");
        if (systemCpuGroupEnabled) {
          this._cpuTime = this._meter.createObservableCounter(semconv_1.METRIC_SYSTEM_CPU_TIME, {
            description: "Cpu time in seconds",
            unit: "s"
          });
          observables.push(this._cpuTime);
          this._cpuUtilization = this._meter.createObservableGauge(semconv_1.METRIC_SYSTEM_CPU_UTILIZATION, {
            description: "Cpu usage time 0-1"
          });
          observables.push(this._cpuUtilization);
        }
        if (systemMemoryGroupEnabled) {
          this._memoryUsage = this._meter.createObservableGauge(semconv_1.METRIC_SYSTEM_MEMORY_USAGE, {
            description: "Memory usage in bytes"
          });
          observables.push(this._memoryUsage);
          this._memoryUtilization = this._meter.createObservableGauge(semconv_1.METRIC_SYSTEM_MEMORY_UTILIZATION, {
            description: "Memory usage 0-1"
          });
          observables.push(this._memoryUtilization);
        }
        if (systemNetworkGroupEnabled) {
          this._networkDropped = this._meter.createObservableCounter(
            // There is no semconv pkg export for this in v1.37.0 because
            // https://github.com/open-telemetry/semantic-conventions/issues/2828.
            // TODO: update to `METRIC_SYSTEM_NETWORK_PACKET_DROPPED` (breaking change)
            "system.network.dropped",
            {
              description: "Network dropped packets"
            }
          );
          observables.push(this._networkDropped);
          this._networkErrors = this._meter.createObservableCounter(semconv_1.METRIC_SYSTEM_NETWORK_ERRORS, {
            description: "Network errors counter"
          });
          observables.push(this._networkErrors);
          this._networkIo = this._meter.createObservableCounter(semconv_1.METRIC_SYSTEM_NETWORK_IO, {
            description: "Network transmit and received bytes"
          });
          observables.push(this._networkIo);
        }
        if (processCpuGroupEnabled) {
          this._processCpuTime = this._meter.createObservableCounter(semconv_1.METRIC_PROCESS_CPU_TIME, {
            description: "Process Cpu time in seconds",
            unit: "s"
          });
          observables.push(this._processCpuTime);
          this._processCpuUtilization = this._meter.createObservableGauge(semconv_1.METRIC_PROCESS_CPU_UTILIZATION, {
            description: "Process Cpu usage time 0-1"
          });
          observables.push(this._processCpuUtilization);
        }
        if (processMemoryGroupEnabled) {
          this._processMemoryUsage = this._meter.createObservableGauge(semconv_1.METRIC_PROCESS_MEMORY_USAGE, {
            description: "Process Memory usage in bytes"
          });
          observables.push(this._processMemoryUsage);
        }
        this._meter.addBatchObservableCallback(async (observableResult) => {
          if (systemCpuGroupEnabled) {
            const cpuUsages = (0, common_1.getCpuUsageData)();
            this._batchUpdateCpuUsages(observableResult, cpuUsages);
          }
          if (systemMemoryGroupEnabled) {
            const memoryUsages = (0, common_1.getMemoryData)();
            this._batchUpdateMemUsages(observableResult, memoryUsages);
          }
          if (processCpuGroupEnabled) {
            const processCpuUsages = (0, common_1.getProcessCpuUsageData)();
            this._batchUpdateProcessCpuUsages(observableResult, processCpuUsages);
          }
          if (processMemoryGroupEnabled) {
            const processMemoryUsages = (0, common_1.getProcessMemoryData)();
            this._batchUpdateProcessMemUsage(observableResult, processMemoryUsages);
          }
          if (systemNetworkGroupEnabled) {
            const networkData = await (0, si_1.getNetworkData)();
            this._batchUpdateNetworkData(observableResult, networkData);
          }
        }, observables);
      }
      /**
       * Starts collecting metrics
       */
      start() {
        this._createMetrics();
      }
      // The metrics are created in `_createMetrics`.
      _cpuTime;
      _cpuUtilization;
      _memoryUsage;
      _memoryUtilization;
      _processCpuTime;
      _processCpuUtilization;
      _processMemoryUsage;
      _networkDropped;
      _networkErrors;
      _networkIo;
    };
    exports.HostMetrics = HostMetrics2;
  }
});

// ../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/index.js
var require_src11 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+host-metrics@0.38.3+e40b0dfdd726a224/node_modules/@opentelemetry/host-metrics/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.HostMetrics = exports.BaseMetrics = void 0;
    var BaseMetrics_1 = require_BaseMetrics();
    Object.defineProperty(exports, "BaseMetrics", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return BaseMetrics_1.BaseMetrics;
    }, "get") });
    var metric_1 = require_metric();
    Object.defineProperty(exports, "HostMetrics", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return metric_1.HostMetrics;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/autoLoaderUtils.js
var require_autoLoaderUtils = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/autoLoaderUtils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.disableInstrumentations = exports.enableInstrumentations = void 0;
    function enableInstrumentations(instrumentations, tracerProvider, meterProvider, loggerProvider) {
      for (let i = 0, j = instrumentations.length; i < j; i++) {
        const instrumentation = instrumentations[i];
        if (tracerProvider) {
          instrumentation.setTracerProvider(tracerProvider);
        }
        if (meterProvider) {
          instrumentation.setMeterProvider(meterProvider);
        }
        if (loggerProvider && instrumentation.setLoggerProvider) {
          instrumentation.setLoggerProvider(loggerProvider);
        }
        if (!instrumentation.getConfig().enabled) {
          instrumentation.enable();
        }
      }
    }
    __name(enableInstrumentations, "enableInstrumentations");
    exports.enableInstrumentations = enableInstrumentations;
    function disableInstrumentations(instrumentations) {
      instrumentations.forEach((instrumentation) => instrumentation.disable());
    }
    __name(disableInstrumentations, "disableInstrumentations");
    exports.disableInstrumentations = disableInstrumentations;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/autoLoader.js
var require_autoLoader = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/autoLoader.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.registerInstrumentations = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var api_logs_1 = require_src3();
    var autoLoaderUtils_1 = require_autoLoaderUtils();
    function registerInstrumentations2(options) {
      const tracerProvider = options.tracerProvider || api_1.trace.getTracerProvider();
      const meterProvider = options.meterProvider || api_1.metrics.getMeterProvider();
      const loggerProvider = options.loggerProvider || api_logs_1.logs.getLoggerProvider();
      const instrumentations = options.instrumentations?.flat() ?? [];
      (0, autoLoaderUtils_1.enableInstrumentations)(instrumentations, tracerProvider, meterProvider, loggerProvider);
      return () => {
        (0, autoLoaderUtils_1.disableInstrumentations)(instrumentations);
      };
    }
    __name(registerInstrumentations2, "registerInstrumentations");
    exports.registerInstrumentations = registerInstrumentations2;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/semver.js
var require_semver = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/semver.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.satisfies = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var VERSION_REGEXP = /^(?:v)?(?<version>(?<major>0|[1-9]\d*)\.(?<minor>0|[1-9]\d*)\.(?<patch>0|[1-9]\d*))(?:-(?<prerelease>(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+(?<build>[0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$/;
    var RANGE_REGEXP = /^(?<op><|>|=|==|<=|>=|~|\^|~>)?\s*(?:v)?(?<version>(?<major>x|X|\*|0|[1-9]\d*)(?:\.(?<minor>x|X|\*|0|[1-9]\d*))?(?:\.(?<patch>x|X|\*|0|[1-9]\d*))?)(?:-(?<prerelease>(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+(?<build>[0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$/;
    var operatorResMap = {
      ">": [1],
      ">=": [0, 1],
      "=": [0],
      "<=": [-1, 0],
      "<": [-1],
      "!=": [-1, 1]
    };
    function satisfies(version, range, options) {
      if (!_validateVersion(version)) {
        api_1.diag.error(`Invalid version: ${version}`);
        return false;
      }
      if (!range) {
        return true;
      }
      range = range.replace(/([<>=~^]+)\s+/g, "$1");
      const parsedVersion = _parseVersion(version);
      if (!parsedVersion) {
        return false;
      }
      const allParsedRanges = [];
      const checkResult = _doSatisfies(parsedVersion, range, allParsedRanges, options);
      if (checkResult && !options?.includePrerelease) {
        return _doPreleaseCheck(parsedVersion, allParsedRanges);
      }
      return checkResult;
    }
    __name(satisfies, "satisfies");
    exports.satisfies = satisfies;
    function _validateVersion(version) {
      return typeof version === "string" && VERSION_REGEXP.test(version);
    }
    __name(_validateVersion, "_validateVersion");
    function _doSatisfies(parsedVersion, range, allParsedRanges, options) {
      if (range.includes("||")) {
        const ranges = range.trim().split("||");
        for (const r of ranges) {
          if (_checkRange(parsedVersion, r, allParsedRanges, options)) {
            return true;
          }
        }
        return false;
      } else if (range.includes(" - ")) {
        range = replaceHyphen(range, options);
      } else if (range.includes(" ")) {
        const ranges = range.trim().replace(/\s{2,}/g, " ").split(" ");
        for (const r of ranges) {
          if (!_checkRange(parsedVersion, r, allParsedRanges, options)) {
            return false;
          }
        }
        return true;
      }
      return _checkRange(parsedVersion, range, allParsedRanges, options);
    }
    __name(_doSatisfies, "_doSatisfies");
    function _checkRange(parsedVersion, range, allParsedRanges, options) {
      range = _normalizeRange(range, options);
      if (range.includes(" ")) {
        return _doSatisfies(parsedVersion, range, allParsedRanges, options);
      } else {
        const parsedRange = _parseRange(range);
        allParsedRanges.push(parsedRange);
        return _satisfies(parsedVersion, parsedRange);
      }
    }
    __name(_checkRange, "_checkRange");
    function _satisfies(parsedVersion, parsedRange) {
      if (parsedRange.invalid) {
        return false;
      }
      if (!parsedRange.version || _isWildcard(parsedRange.version)) {
        return true;
      }
      let comparisonResult = _compareVersionSegments(parsedVersion.versionSegments || [], parsedRange.versionSegments || []);
      if (comparisonResult === 0) {
        const versionPrereleaseSegments = parsedVersion.prereleaseSegments || [];
        const rangePrereleaseSegments = parsedRange.prereleaseSegments || [];
        if (!versionPrereleaseSegments.length && !rangePrereleaseSegments.length) {
          comparisonResult = 0;
        } else if (!versionPrereleaseSegments.length && rangePrereleaseSegments.length) {
          comparisonResult = 1;
        } else if (versionPrereleaseSegments.length && !rangePrereleaseSegments.length) {
          comparisonResult = -1;
        } else {
          comparisonResult = _compareVersionSegments(versionPrereleaseSegments, rangePrereleaseSegments);
        }
      }
      return operatorResMap[parsedRange.op]?.includes(comparisonResult);
    }
    __name(_satisfies, "_satisfies");
    function _doPreleaseCheck(parsedVersion, allParsedRanges) {
      if (parsedVersion.prerelease) {
        return allParsedRanges.some((r) => r.prerelease && r.version === parsedVersion.version);
      }
      return true;
    }
    __name(_doPreleaseCheck, "_doPreleaseCheck");
    function _normalizeRange(range, options) {
      range = range.trim();
      range = replaceCaret(range, options);
      range = replaceTilde(range);
      range = replaceXRange(range, options);
      range = range.trim();
      return range;
    }
    __name(_normalizeRange, "_normalizeRange");
    function isX(id) {
      return !id || id.toLowerCase() === "x" || id === "*";
    }
    __name(isX, "isX");
    function _parseVersion(versionString) {
      const match = versionString.match(VERSION_REGEXP);
      if (!match) {
        api_1.diag.error(`Invalid version: ${versionString}`);
        return void 0;
      }
      const version = match.groups.version;
      const prerelease = match.groups.prerelease;
      const build = match.groups.build;
      const versionSegments = version.split(".");
      const prereleaseSegments = prerelease?.split(".");
      return {
        op: void 0,
        version,
        versionSegments,
        versionSegmentCount: versionSegments.length,
        prerelease,
        prereleaseSegments,
        prereleaseSegmentCount: prereleaseSegments ? prereleaseSegments.length : 0,
        build
      };
    }
    __name(_parseVersion, "_parseVersion");
    function _parseRange(rangeString) {
      if (!rangeString) {
        return {};
      }
      const match = rangeString.match(RANGE_REGEXP);
      if (!match) {
        api_1.diag.error(`Invalid range: ${rangeString}`);
        return {
          invalid: true
        };
      }
      let op = match.groups.op;
      const version = match.groups.version;
      const prerelease = match.groups.prerelease;
      const build = match.groups.build;
      const versionSegments = version.split(".");
      const prereleaseSegments = prerelease?.split(".");
      if (op === "==") {
        op = "=";
      }
      return {
        op: op || "=",
        version,
        versionSegments,
        versionSegmentCount: versionSegments.length,
        prerelease,
        prereleaseSegments,
        prereleaseSegmentCount: prereleaseSegments ? prereleaseSegments.length : 0,
        build
      };
    }
    __name(_parseRange, "_parseRange");
    function _isWildcard(s) {
      return s === "*" || s === "x" || s === "X";
    }
    __name(_isWildcard, "_isWildcard");
    function _parseVersionString(v) {
      const n = parseInt(v, 10);
      return isNaN(n) ? v : n;
    }
    __name(_parseVersionString, "_parseVersionString");
    function _normalizeVersionType(a, b) {
      if (typeof a === typeof b) {
        if (typeof a === "number") {
          return [a, b];
        } else if (typeof a === "string") {
          return [a, b];
        } else {
          throw new Error("Version segments can only be strings or numbers");
        }
      } else {
        return [String(a), String(b)];
      }
    }
    __name(_normalizeVersionType, "_normalizeVersionType");
    function _compareVersionStrings(v1, v2) {
      if (_isWildcard(v1) || _isWildcard(v2)) {
        return 0;
      }
      const [parsedV1, parsedV2] = _normalizeVersionType(_parseVersionString(v1), _parseVersionString(v2));
      if (parsedV1 > parsedV2) {
        return 1;
      } else if (parsedV1 < parsedV2) {
        return -1;
      }
      return 0;
    }
    __name(_compareVersionStrings, "_compareVersionStrings");
    function _compareVersionSegments(v1, v2) {
      for (let i = 0; i < Math.max(v1.length, v2.length); i++) {
        const res = _compareVersionStrings(v1[i] || "0", v2[i] || "0");
        if (res !== 0) {
          return res;
        }
      }
      return 0;
    }
    __name(_compareVersionSegments, "_compareVersionSegments");
    var LETTERDASHNUMBER = "[a-zA-Z0-9-]";
    var NUMERICIDENTIFIER = "0|[1-9]\\d*";
    var NONNUMERICIDENTIFIER = `\\d*[a-zA-Z-]${LETTERDASHNUMBER}*`;
    var GTLT = "((?:<|>)?=?)";
    var PRERELEASEIDENTIFIER = `(?:${NUMERICIDENTIFIER}|${NONNUMERICIDENTIFIER})`;
    var PRERELEASE = `(?:-(${PRERELEASEIDENTIFIER}(?:\\.${PRERELEASEIDENTIFIER})*))`;
    var BUILDIDENTIFIER = `${LETTERDASHNUMBER}+`;
    var BUILD = `(?:\\+(${BUILDIDENTIFIER}(?:\\.${BUILDIDENTIFIER})*))`;
    var XRANGEIDENTIFIER = `${NUMERICIDENTIFIER}|x|X|\\*`;
    var XRANGEPLAIN = `[v=\\s]*(${XRANGEIDENTIFIER})(?:\\.(${XRANGEIDENTIFIER})(?:\\.(${XRANGEIDENTIFIER})(?:${PRERELEASE})?${BUILD}?)?)?`;
    var XRANGE = `^${GTLT}\\s*${XRANGEPLAIN}$`;
    var XRANGE_REGEXP = new RegExp(XRANGE);
    var HYPHENRANGE = `^\\s*(${XRANGEPLAIN})\\s+-\\s+(${XRANGEPLAIN})\\s*$`;
    var HYPHENRANGE_REGEXP = new RegExp(HYPHENRANGE);
    var LONETILDE = "(?:~>?)";
    var TILDE = `^${LONETILDE}${XRANGEPLAIN}$`;
    var TILDE_REGEXP = new RegExp(TILDE);
    var LONECARET = "(?:\\^)";
    var CARET = `^${LONECARET}${XRANGEPLAIN}$`;
    var CARET_REGEXP = new RegExp(CARET);
    function replaceTilde(comp) {
      const r = TILDE_REGEXP;
      return comp.replace(r, (_, M, m, p, pr) => {
        let ret;
        if (isX(M)) {
          ret = "";
        } else if (isX(m)) {
          ret = `>=${M}.0.0 <${+M + 1}.0.0-0`;
        } else if (isX(p)) {
          ret = `>=${M}.${m}.0 <${M}.${+m + 1}.0-0`;
        } else if (pr) {
          ret = `>=${M}.${m}.${p}-${pr} <${M}.${+m + 1}.0-0`;
        } else {
          ret = `>=${M}.${m}.${p} <${M}.${+m + 1}.0-0`;
        }
        return ret;
      });
    }
    __name(replaceTilde, "replaceTilde");
    function replaceCaret(comp, options) {
      const r = CARET_REGEXP;
      const z = options?.includePrerelease ? "-0" : "";
      return comp.replace(r, (_, M, m, p, pr) => {
        let ret;
        if (isX(M)) {
          ret = "";
        } else if (isX(m)) {
          ret = `>=${M}.0.0${z} <${+M + 1}.0.0-0`;
        } else if (isX(p)) {
          if (M === "0") {
            ret = `>=${M}.${m}.0${z} <${M}.${+m + 1}.0-0`;
          } else {
            ret = `>=${M}.${m}.0${z} <${+M + 1}.0.0-0`;
          }
        } else if (pr) {
          if (M === "0") {
            if (m === "0") {
              ret = `>=${M}.${m}.${p}-${pr} <${M}.${m}.${+p + 1}-0`;
            } else {
              ret = `>=${M}.${m}.${p}-${pr} <${M}.${+m + 1}.0-0`;
            }
          } else {
            ret = `>=${M}.${m}.${p}-${pr} <${+M + 1}.0.0-0`;
          }
        } else {
          if (M === "0") {
            if (m === "0") {
              ret = `>=${M}.${m}.${p}${z} <${M}.${m}.${+p + 1}-0`;
            } else {
              ret = `>=${M}.${m}.${p}${z} <${M}.${+m + 1}.0-0`;
            }
          } else {
            ret = `>=${M}.${m}.${p} <${+M + 1}.0.0-0`;
          }
        }
        return ret;
      });
    }
    __name(replaceCaret, "replaceCaret");
    function replaceXRange(comp, options) {
      const r = XRANGE_REGEXP;
      return comp.replace(r, (ret, gtlt, M, m, p, pr) => {
        const xM = isX(M);
        const xm = xM || isX(m);
        const xp = xm || isX(p);
        const anyX = xp;
        if (gtlt === "=" && anyX) {
          gtlt = "";
        }
        pr = options?.includePrerelease ? "-0" : "";
        if (xM) {
          if (gtlt === ">" || gtlt === "<") {
            ret = "<0.0.0-0";
          } else {
            ret = "*";
          }
        } else if (gtlt && anyX) {
          if (xm) {
            m = 0;
          }
          p = 0;
          if (gtlt === ">") {
            gtlt = ">=";
            if (xm) {
              M = +M + 1;
              m = 0;
              p = 0;
            } else {
              m = +m + 1;
              p = 0;
            }
          } else if (gtlt === "<=") {
            gtlt = "<";
            if (xm) {
              M = +M + 1;
            } else {
              m = +m + 1;
            }
          }
          if (gtlt === "<") {
            pr = "-0";
          }
          ret = `${gtlt + M}.${m}.${p}${pr}`;
        } else if (xm) {
          ret = `>=${M}.0.0${pr} <${+M + 1}.0.0-0`;
        } else if (xp) {
          ret = `>=${M}.${m}.0${pr} <${M}.${+m + 1}.0-0`;
        }
        return ret;
      });
    }
    __name(replaceXRange, "replaceXRange");
    function replaceHyphen(comp, options) {
      const r = HYPHENRANGE_REGEXP;
      return comp.replace(r, (_, from, fM, fm, fp, fpr, fb, to, tM, tm, tp, tpr) => {
        if (isX(fM)) {
          from = "";
        } else if (isX(fm)) {
          from = `>=${fM}.0.0${options?.includePrerelease ? "-0" : ""}`;
        } else if (isX(fp)) {
          from = `>=${fM}.${fm}.0${options?.includePrerelease ? "-0" : ""}`;
        } else if (fpr) {
          from = `>=${from}`;
        } else {
          from = `>=${from}${options?.includePrerelease ? "-0" : ""}`;
        }
        if (isX(tM)) {
          to = "";
        } else if (isX(tm)) {
          to = `<${+tM + 1}.0.0-0`;
        } else if (isX(tp)) {
          to = `<${tM}.${+tm + 1}.0-0`;
        } else if (tpr) {
          to = `<=${tM}.${tm}.${tp}-${tpr}`;
        } else if (options?.includePrerelease) {
          to = `<${tM}.${tm}.${+tp + 1}-0`;
        } else {
          to = `<=${to}`;
        }
        return `${from} ${to}`.trim();
      });
    }
    __name(replaceHyphen, "replaceHyphen");
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/shimmer.js
var require_shimmer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/shimmer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.massUnwrap = exports.unwrap = exports.massWrap = exports.wrap = void 0;
    var logger = console.error.bind(console);
    function defineProperty(obj, name, value2) {
      const enumerable = !!obj[name] && Object.prototype.propertyIsEnumerable.call(obj, name);
      Object.defineProperty(obj, name, {
        configurable: true,
        enumerable,
        writable: true,
        value: value2
      });
    }
    __name(defineProperty, "defineProperty");
    var wrap = /* @__PURE__ */ __name((nodule, name, wrapper) => {
      if (!nodule || !nodule[name]) {
        logger("no original function " + String(name) + " to wrap");
        return;
      }
      if (!wrapper) {
        logger("no wrapper function");
        logger(new Error().stack);
        return;
      }
      const original = nodule[name];
      if (typeof original !== "function" || typeof wrapper !== "function") {
        logger("original object and wrapper must be functions");
        return;
      }
      const wrapped = wrapper(original, name);
      defineProperty(wrapped, "__original", original);
      defineProperty(wrapped, "__unwrap", () => {
        if (nodule[name] === wrapped) {
          defineProperty(nodule, name, original);
        }
      });
      defineProperty(wrapped, "__wrapped", true);
      defineProperty(nodule, name, wrapped);
      return wrapped;
    }, "wrap");
    exports.wrap = wrap;
    var massWrap = /* @__PURE__ */ __name((nodules, names, wrapper) => {
      if (!nodules) {
        logger("must provide one or more modules to patch");
        logger(new Error().stack);
        return;
      } else if (!Array.isArray(nodules)) {
        nodules = [nodules];
      }
      if (!(names && Array.isArray(names))) {
        logger("must provide one or more functions to wrap on modules");
        return;
      }
      nodules.forEach((nodule) => {
        names.forEach((name) => {
          (0, exports.wrap)(nodule, name, wrapper);
        });
      });
    }, "massWrap");
    exports.massWrap = massWrap;
    var unwrap = /* @__PURE__ */ __name((nodule, name) => {
      if (!nodule || !nodule[name]) {
        logger("no function to unwrap.");
        logger(new Error().stack);
        return;
      }
      const wrapped = nodule[name];
      if (!wrapped.__unwrap) {
        logger("no original to unwrap to -- has " + String(name) + " already been unwrapped?");
      } else {
        wrapped.__unwrap();
        return;
      }
    }, "unwrap");
    exports.unwrap = unwrap;
    var massUnwrap = /* @__PURE__ */ __name((nodules, names) => {
      if (!nodules) {
        logger("must provide one or more modules to patch");
        logger(new Error().stack);
        return;
      } else if (!Array.isArray(nodules)) {
        nodules = [nodules];
      }
      if (!(names && Array.isArray(names))) {
        logger("must provide one or more functions to unwrap on modules");
        return;
      }
      nodules.forEach((nodule) => {
        names.forEach((name) => {
          (0, exports.unwrap)(nodule, name);
        });
      });
    }, "massUnwrap");
    exports.massUnwrap = massUnwrap;
    function shimmer(options) {
      if (options && options.logger) {
        if (typeof options.logger !== "function") {
          logger("new logger isn't a function, not replacing");
        } else {
          logger = options.logger;
        }
      }
    }
    __name(shimmer, "shimmer");
    exports.default = shimmer;
    shimmer.wrap = exports.wrap;
    shimmer.massWrap = exports.massWrap;
    shimmer.unwrap = exports.unwrap;
    shimmer.massUnwrap = exports.massUnwrap;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/instrumentation.js
var require_instrumentation = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/instrumentation.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InstrumentationAbstract = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var api_logs_1 = require_src3();
    var shimmer = require_shimmer();
    var InstrumentationAbstract = class {
      static {
        __name(this, "InstrumentationAbstract");
      }
      _config = {};
      _tracer;
      _meter;
      _logger;
      _diag;
      instrumentationName;
      instrumentationVersion;
      constructor(instrumentationName, instrumentationVersion, config) {
        this.instrumentationName = instrumentationName;
        this.instrumentationVersion = instrumentationVersion;
        this.setConfig(config);
        this._diag = api_1.diag.createComponentLogger({
          namespace: instrumentationName
        });
        this._tracer = api_1.trace.getTracer(instrumentationName, instrumentationVersion);
        this._meter = api_1.metrics.getMeter(instrumentationName, instrumentationVersion);
        this._logger = api_logs_1.logs.getLogger(instrumentationName, instrumentationVersion);
        this._updateMetricInstruments();
      }
      /* Api to wrap instrumented method */
      _wrap = shimmer.wrap;
      /* Api to unwrap instrumented methods */
      _unwrap = shimmer.unwrap;
      /* Api to mass wrap instrumented method */
      _massWrap = shimmer.massWrap;
      /* Api to mass unwrap instrumented methods */
      _massUnwrap = shimmer.massUnwrap;
      /* Returns meter */
      get meter() {
        return this._meter;
      }
      /**
       * Sets MeterProvider to this plugin
       * @param meterProvider
       */
      setMeterProvider(meterProvider) {
        this._meter = meterProvider.getMeter(this.instrumentationName, this.instrumentationVersion);
        this._updateMetricInstruments();
      }
      /* Returns logger */
      get logger() {
        return this._logger;
      }
      /**
       * Sets LoggerProvider to this plugin
       * @param loggerProvider
       */
      setLoggerProvider(loggerProvider) {
        this._logger = loggerProvider.getLogger(this.instrumentationName, this.instrumentationVersion);
      }
      /**
       * @experimental
       *
       * Get module definitions defined by {@link init}.
       * This can be used for experimental compile-time instrumentation.
       *
       * @returns an array of {@link InstrumentationModuleDefinition}
       */
      getModuleDefinitions() {
        const initResult = this.init() ?? [];
        if (!Array.isArray(initResult)) {
          return [initResult];
        }
        return initResult;
      }
      /**
       * Sets the new metric instruments with the current Meter.
       */
      _updateMetricInstruments() {
        return;
      }
      /* Returns InstrumentationConfig */
      getConfig() {
        return this._config;
      }
      /**
       * Sets InstrumentationConfig to this plugin
       * @param config
       */
      setConfig(config) {
        this._config = {
          enabled: true,
          ...config
        };
      }
      /**
       * Sets TraceProvider to this plugin
       * @param tracerProvider
       */
      setTracerProvider(tracerProvider) {
        this._tracer = tracerProvider.getTracer(this.instrumentationName, this.instrumentationVersion);
      }
      /* Returns tracer */
      get tracer() {
        return this._tracer;
      }
      /**
       * Execute span customization hook, if configured, and log any errors.
       * Any semantics of the trigger and info are defined by the specific instrumentation.
       * @param hookHandler The optional hook handler which the user has configured via instrumentation config
       * @param triggerName The name of the trigger for executing the hook for logging purposes
       * @param span The span to which the hook should be applied
       * @param info The info object to be passed to the hook, with useful data the hook may use
       */
      _runSpanCustomizationHook(hookHandler, triggerName, span, info) {
        if (!hookHandler) {
          return;
        }
        try {
          hookHandler(span, info);
        } catch (e) {
          this._diag.error(`Error running span customization hook due to exception in handler`, { triggerName }, e);
        }
      }
    };
    exports.InstrumentationAbstract = InstrumentationAbstract;
  }
});

// ../../node_modules/.bun/module-details-from-path@1.0.4/node_modules/module-details-from-path/index.js
var require_module_details_from_path = __commonJS({
  "../../node_modules/.bun/module-details-from-path@1.0.4/node_modules/module-details-from-path/index.js"(exports, module) {
    "use strict";
    init_esm();
    var sep = __require("path").sep;
    module.exports = function(file) {
      var segments = file.split(sep);
      var index = segments.lastIndexOf("node_modules");
      if (index === -1) return;
      if (!segments[index + 1]) return;
      var scoped = segments[index + 1][0] === "@";
      var name = scoped ? segments[index + 1] + "/" + segments[index + 2] : segments[index + 1];
      var offset = scoped ? 3 : 2;
      var basedir = "";
      var lastBaseDirSegmentIndex = index + offset - 1;
      for (var i = 0; i <= lastBaseDirSegmentIndex; i++) {
        if (i === lastBaseDirSegmentIndex) {
          basedir += segments[i];
        } else {
          basedir += segments[i] + sep;
        }
      }
      var path2 = "";
      var lastSegmentIndex = segments.length - 1;
      for (var i2 = index + offset; i2 <= lastSegmentIndex; i2++) {
        if (i2 === lastSegmentIndex) {
          path2 += segments[i2];
        } else {
          path2 += segments[i2] + sep;
        }
      }
      return {
        name,
        basedir,
        path: path2
      };
    };
  }
});

// ../../node_modules/.bun/require-in-the-middle@8.0.1+2b91fc17bf64bdfd/node_modules/require-in-the-middle/index.js
var require_require_in_the_middle = __commonJS({
  "../../node_modules/.bun/require-in-the-middle@8.0.1+2b91fc17bf64bdfd/node_modules/require-in-the-middle/index.js"(exports, module) {
    "use strict";
    init_esm();
    var path2 = __require("path");
    var Module = __require("module");
    var debug4 = require_src4()("require-in-the-middle");
    var moduleDetailsFromPath = require_module_details_from_path();
    module.exports = Hook;
    module.exports.Hook = Hook;
    var builtinModules;
    var isCore;
    if (Module.isBuiltin) {
      isCore = Module.isBuiltin;
    } else if (Module.builtinModules) {
      isCore = /* @__PURE__ */ __name((moduleName) => {
        if (moduleName.startsWith("node:")) {
          return true;
        }
        if (builtinModules === void 0) {
          builtinModules = new Set(Module.builtinModules);
        }
        return builtinModules.has(moduleName);
      }, "isCore");
    } else {
      throw new Error("'require-in-the-middle' requires Node.js >=v9.3.0 or >=v8.10.0");
    }
    var normalize = /([/\\]index)?(\.js)?$/;
    var ExportsCache = class {
      static {
        __name(this, "ExportsCache");
      }
      constructor() {
        this._localCache = /* @__PURE__ */ new Map();
        this._kRitmExports = Symbol("RitmExports");
      }
      has(filename, isBuiltin) {
        if (this._localCache.has(filename)) {
          return true;
        } else if (!isBuiltin) {
          const mod = __require.cache[filename];
          return !!(mod && this._kRitmExports in mod);
        } else {
          return false;
        }
      }
      get(filename, isBuiltin) {
        const cachedExports = this._localCache.get(filename);
        if (cachedExports !== void 0) {
          return cachedExports;
        } else if (!isBuiltin) {
          const mod = __require.cache[filename];
          return mod && mod[this._kRitmExports];
        }
      }
      set(filename, exports2, isBuiltin) {
        if (isBuiltin) {
          this._localCache.set(filename, exports2);
        } else if (filename in __require.cache) {
          __require.cache[filename][this._kRitmExports] = exports2;
        } else {
          debug4('non-core module is unexpectedly not in require.cache: "%s"', filename);
          this._localCache.set(filename, exports2);
        }
      }
    };
    function Hook(modules, options, onrequire) {
      if (this instanceof Hook === false) return new Hook(modules, options, onrequire);
      if (typeof modules === "function") {
        onrequire = modules;
        modules = null;
        options = null;
      } else if (typeof options === "function") {
        onrequire = options;
        options = null;
      }
      if (typeof Module._resolveFilename !== "function") {
        console.error("Error: Expected Module._resolveFilename to be a function (was: %s) - aborting!", typeof Module._resolveFilename);
        console.error("Please report this error as an issue related to Node.js %s at https://github.com/nodejs/require-in-the-middle/issues", process.version);
        return;
      }
      this._cache = new ExportsCache();
      this._unhooked = false;
      this._origRequire = Module.prototype.require;
      const self = this;
      const patching = /* @__PURE__ */ new Set();
      const internals = options ? options.internals === true : false;
      const hasWhitelist = Array.isArray(modules);
      debug4("registering require hook");
      this._require = Module.prototype.require = function(id) {
        if (self._unhooked === true) {
          debug4("ignoring require call - module is soft-unhooked");
          return self._origRequire.apply(this, arguments);
        }
        return patchedRequire.call(this, arguments, false);
      };
      if (typeof process.getBuiltinModule === "function") {
        this._origGetBuiltinModule = process.getBuiltinModule;
        this._getBuiltinModule = process.getBuiltinModule = function(id) {
          if (self._unhooked === true) {
            debug4("ignoring process.getBuiltinModule call - module is soft-unhooked");
            return self._origGetBuiltinModule.apply(this, arguments);
          }
          return patchedRequire.call(this, arguments, true);
        };
      }
      function patchedRequire(args, coreOnly) {
        const id = args[0];
        const core = isCore(id);
        let filename;
        if (core) {
          filename = id;
          if (id.startsWith("node:")) {
            const idWithoutPrefix = id.slice(5);
            if (isCore(idWithoutPrefix)) {
              filename = idWithoutPrefix;
            }
          }
        } else if (coreOnly) {
          debug4("call to process.getBuiltinModule with unknown built-in id");
          return self._origGetBuiltinModule.apply(this, args);
        } else {
          try {
            filename = Module._resolveFilename(id, this);
          } catch (resolveErr) {
            debug4('Module._resolveFilename("%s") threw %j, calling original Module.require', id, resolveErr.message);
            return self._origRequire.apply(this, args);
          }
        }
        let moduleName, basedir;
        debug4("processing %s module require('%s'): %s", core === true ? "core" : "non-core", id, filename);
        if (self._cache.has(filename, core) === true) {
          debug4("returning already patched cached module: %s", filename);
          return self._cache.get(filename, core);
        }
        const isPatching = patching.has(filename);
        if (isPatching === false) {
          patching.add(filename);
        }
        const exports2 = coreOnly ? self._origGetBuiltinModule.apply(this, args) : self._origRequire.apply(this, args);
        if (isPatching === true) {
          debug4("module is in the process of being patched already - ignoring: %s", filename);
          return exports2;
        }
        patching.delete(filename);
        if (core === true) {
          if (hasWhitelist === true && modules.includes(filename) === false) {
            debug4("ignoring core module not on whitelist: %s", filename);
            return exports2;
          }
          moduleName = filename;
        } else if (hasWhitelist === true && modules.includes(filename)) {
          const parsedPath = path2.parse(filename);
          moduleName = parsedPath.name;
          basedir = parsedPath.dir;
        } else {
          const stat = moduleDetailsFromPath(filename);
          if (stat === void 0) {
            debug4("could not parse filename: %s", filename);
            return exports2;
          }
          moduleName = stat.name;
          basedir = stat.basedir;
          const fullModuleName = resolveModuleName(stat);
          debug4("resolved filename to module: %s (id: %s, resolved: %s, basedir: %s)", moduleName, id, fullModuleName, basedir);
          let matchFound = false;
          if (hasWhitelist) {
            if (!id.startsWith(".") && modules.includes(id)) {
              moduleName = id;
              matchFound = true;
            }
            if (!modules.includes(moduleName) && !modules.includes(fullModuleName)) {
              return exports2;
            }
            if (modules.includes(fullModuleName) && fullModuleName !== moduleName) {
              moduleName = fullModuleName;
              matchFound = true;
            }
          }
          if (!matchFound) {
            let res;
            try {
              res = __require.resolve(moduleName, { paths: [basedir] });
            } catch (e) {
              debug4("could not resolve module: %s", moduleName);
              self._cache.set(filename, exports2, core);
              return exports2;
            }
            if (res !== filename) {
              if (internals === true) {
                moduleName = moduleName + path2.sep + path2.relative(basedir, filename);
                debug4("preparing to process require of internal file: %s", moduleName);
              } else {
                debug4("ignoring require of non-main module file: %s", res);
                self._cache.set(filename, exports2, core);
                return exports2;
              }
            }
          }
        }
        self._cache.set(filename, exports2, core);
        debug4("calling require hook: %s", moduleName);
        const patchedExports = onrequire(exports2, moduleName, basedir);
        self._cache.set(filename, patchedExports, core);
        debug4("returning module: %s", moduleName);
        return patchedExports;
      }
      __name(patchedRequire, "patchedRequire");
    }
    __name(Hook, "Hook");
    Hook.prototype.unhook = function() {
      this._unhooked = true;
      if (this._require === Module.prototype.require) {
        Module.prototype.require = this._origRequire;
        debug4("require unhook successful");
      } else {
        debug4("require unhook unsuccessful");
      }
      if (process.getBuiltinModule !== void 0) {
        if (this._getBuiltinModule === process.getBuiltinModule) {
          process.getBuiltinModule = this._origGetBuiltinModule;
          debug4("process.getBuiltinModule unhook successful");
        } else {
          debug4("process.getBuiltinModule unhook unsuccessful");
        }
      }
    };
    function resolveModuleName(stat) {
      const normalizedPath = path2.sep !== "/" ? stat.path.split(path2.sep).join("/") : stat.path;
      return path2.posix.join(stat.name, normalizedPath).replace(normalize, "");
    }
    __name(resolveModuleName, "resolveModuleName");
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/ModuleNameTrie.js
var require_ModuleNameTrie = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/ModuleNameTrie.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ModuleNameTrie = exports.ModuleNameSeparator = void 0;
    exports.ModuleNameSeparator = "/";
    var ModuleNameTrieNode = class {
      static {
        __name(this, "ModuleNameTrieNode");
      }
      hooks = [];
      children = /* @__PURE__ */ new Map();
    };
    var ModuleNameTrie = class {
      static {
        __name(this, "ModuleNameTrie");
      }
      _trie = new ModuleNameTrieNode();
      _counter = 0;
      /**
       * Insert a module hook into the trie
       *
       * @param {Hooked} hook Hook
       */
      insert(hook) {
        let trieNode = this._trie;
        for (const moduleNamePart of hook.moduleName.split(exports.ModuleNameSeparator)) {
          let nextNode = trieNode.children.get(moduleNamePart);
          if (!nextNode) {
            nextNode = new ModuleNameTrieNode();
            trieNode.children.set(moduleNamePart, nextNode);
          }
          trieNode = nextNode;
        }
        trieNode.hooks.push({ hook, insertedId: this._counter++ });
      }
      /**
       * Search for matching hooks in the trie
       *
       * @param {string} moduleName Module name
       * @param {boolean} maintainInsertionOrder Whether to return the results in insertion order
       * @param {boolean} fullOnly Whether to return only full matches
       * @returns {Hooked[]} Matching hooks
       */
      search(moduleName, { maintainInsertionOrder, fullOnly } = {}) {
        let trieNode = this._trie;
        const results = [];
        let foundFull = true;
        for (const moduleNamePart of moduleName.split(exports.ModuleNameSeparator)) {
          const nextNode = trieNode.children.get(moduleNamePart);
          if (!nextNode) {
            foundFull = false;
            break;
          }
          if (!fullOnly) {
            results.push(...nextNode.hooks);
          }
          trieNode = nextNode;
        }
        if (fullOnly && foundFull) {
          results.push(...trieNode.hooks);
        }
        if (results.length === 0) {
          return [];
        }
        if (results.length === 1) {
          return [results[0].hook];
        }
        if (maintainInsertionOrder) {
          results.sort((a, b) => a.insertedId - b.insertedId);
        }
        return results.map(({ hook }) => hook);
      }
    };
    exports.ModuleNameTrie = ModuleNameTrie;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/RequireInTheMiddleSingleton.js
var require_RequireInTheMiddleSingleton = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/RequireInTheMiddleSingleton.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RequireInTheMiddleSingleton = void 0;
    var require_in_the_middle_1 = require_require_in_the_middle();
    var path2 = __require("path");
    var ModuleNameTrie_1 = require_ModuleNameTrie();
    var isMocha = [
      "afterEach",
      "after",
      "beforeEach",
      "before",
      "describe",
      "it"
    ].every((fn) => {
      return typeof global[fn] === "function";
    });
    var RequireInTheMiddleSingleton = class _RequireInTheMiddleSingleton {
      static {
        __name(this, "RequireInTheMiddleSingleton");
      }
      _moduleNameTrie = new ModuleNameTrie_1.ModuleNameTrie();
      static _instance;
      constructor() {
        this._initialize();
      }
      _initialize() {
        new require_in_the_middle_1.Hook(
          // Intercept all `require` calls; we will filter the matching ones below
          null,
          { internals: true },
          (exports2, name, basedir) => {
            const normalizedModuleName = normalizePathSeparators(name);
            const matches = this._moduleNameTrie.search(normalizedModuleName, {
              maintainInsertionOrder: true,
              // For core modules (e.g. `fs`), do not match on sub-paths (e.g. `fs/promises').
              // This matches the behavior of `require-in-the-middle`.
              // `basedir` is always `undefined` for core modules.
              fullOnly: basedir === void 0
            });
            for (const { onRequire } of matches) {
              exports2 = onRequire(exports2, name, basedir);
            }
            return exports2;
          }
        );
      }
      /**
       * Register a hook with `require-in-the-middle`
       *
       * @param {string} moduleName Module name
       * @param {OnRequireFn} onRequire Hook function
       * @returns {Hooked} Registered hook
       */
      register(moduleName, onRequire) {
        const hooked = { moduleName, onRequire };
        this._moduleNameTrie.insert(hooked);
        return hooked;
      }
      /**
       * Get the `RequireInTheMiddleSingleton` singleton
       *
       * @returns {RequireInTheMiddleSingleton} Singleton of `RequireInTheMiddleSingleton`
       */
      static getInstance() {
        if (isMocha)
          return new _RequireInTheMiddleSingleton();
        return this._instance = this._instance ?? new _RequireInTheMiddleSingleton();
      }
    };
    exports.RequireInTheMiddleSingleton = RequireInTheMiddleSingleton;
    function normalizePathSeparators(moduleNameOrPath) {
      return path2.sep !== ModuleNameTrie_1.ModuleNameSeparator ? moduleNameOrPath.split(path2.sep).join(ModuleNameTrie_1.ModuleNameSeparator) : moduleNameOrPath;
    }
    __name(normalizePathSeparators, "normalizePathSeparators");
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/utils.js
var require_utils6 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/utils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isWrapped = exports.safeExecuteInTheMiddleAsync = exports.safeExecuteInTheMiddle = void 0;
    function safeExecuteInTheMiddle(execute, onFinish, preventThrowingError) {
      let error;
      let result;
      try {
        result = execute();
      } catch (e) {
        error = e;
      } finally {
        onFinish(error, result);
        if (error && !preventThrowingError) {
          throw error;
        }
        return result;
      }
    }
    __name(safeExecuteInTheMiddle, "safeExecuteInTheMiddle");
    exports.safeExecuteInTheMiddle = safeExecuteInTheMiddle;
    async function safeExecuteInTheMiddleAsync(execute, onFinish, preventThrowingError) {
      let error;
      let result;
      try {
        result = await execute();
      } catch (e) {
        error = e;
      } finally {
        await onFinish(error, result);
        if (error && !preventThrowingError) {
          throw error;
        }
        return result;
      }
    }
    __name(safeExecuteInTheMiddleAsync, "safeExecuteInTheMiddleAsync");
    exports.safeExecuteInTheMiddleAsync = safeExecuteInTheMiddleAsync;
    function isWrapped(func) {
      return typeof func === "function" && typeof func.__original === "function" && typeof func.__unwrap === "function" && func.__wrapped === true;
    }
    __name(isWrapped, "isWrapped");
    exports.isWrapped = isWrapped;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/instrumentation.js
var require_instrumentation2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/instrumentation.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InstrumentationBase = void 0;
    var path2 = __require("path");
    var util_1 = __require("util");
    var semver_1 = require_semver();
    var shimmer_1 = require_shimmer();
    var instrumentation_1 = require_instrumentation();
    var RequireInTheMiddleSingleton_1 = require_RequireInTheMiddleSingleton();
    var import_in_the_middle_1 = __require("import-in-the-middle");
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var require_in_the_middle_1 = require_require_in_the_middle();
    var fs_1 = __require("fs");
    var utils_1 = require_utils6();
    var InstrumentationBase = class extends instrumentation_1.InstrumentationAbstract {
      static {
        __name(this, "InstrumentationBase");
      }
      _modules;
      _hooks = [];
      _requireInTheMiddleSingleton = RequireInTheMiddleSingleton_1.RequireInTheMiddleSingleton.getInstance();
      _enabled = false;
      constructor(instrumentationName, instrumentationVersion, config) {
        super(instrumentationName, instrumentationVersion, config);
        let modules = this.init();
        if (modules && !Array.isArray(modules)) {
          modules = [modules];
        }
        this._modules = modules || [];
        if (this._config.enabled) {
          this.enable();
        }
      }
      _wrap = /* @__PURE__ */ __name((moduleExports, name, wrapper) => {
        if ((0, utils_1.isWrapped)(moduleExports[name])) {
          this._unwrap(moduleExports, name);
        }
        if (!util_1.types.isProxy(moduleExports)) {
          return (0, shimmer_1.wrap)(moduleExports, name, wrapper);
        } else {
          const wrapped = (0, shimmer_1.wrap)(Object.assign({}, moduleExports), name, wrapper);
          Object.defineProperty(moduleExports, name, {
            value: wrapped
          });
          return wrapped;
        }
      }, "_wrap");
      _unwrap = /* @__PURE__ */ __name((moduleExports, name) => {
        if (!util_1.types.isProxy(moduleExports)) {
          return (0, shimmer_1.unwrap)(moduleExports, name);
        } else {
          return Object.defineProperty(moduleExports, name, {
            value: moduleExports[name]
          });
        }
      }, "_unwrap");
      _massWrap = /* @__PURE__ */ __name((moduleExportsArray, names, wrapper) => {
        if (!moduleExportsArray) {
          api_1.diag.error("must provide one or more modules to patch");
          return;
        } else if (!Array.isArray(moduleExportsArray)) {
          moduleExportsArray = [moduleExportsArray];
        }
        if (!(names && Array.isArray(names))) {
          api_1.diag.error("must provide one or more functions to wrap on modules");
          return;
        }
        moduleExportsArray.forEach((moduleExports) => {
          names.forEach((name) => {
            this._wrap(moduleExports, name, wrapper);
          });
        });
      }, "_massWrap");
      _massUnwrap = /* @__PURE__ */ __name((moduleExportsArray, names) => {
        if (!moduleExportsArray) {
          api_1.diag.error("must provide one or more modules to patch");
          return;
        } else if (!Array.isArray(moduleExportsArray)) {
          moduleExportsArray = [moduleExportsArray];
        }
        if (!(names && Array.isArray(names))) {
          api_1.diag.error("must provide one or more functions to wrap on modules");
          return;
        }
        moduleExportsArray.forEach((moduleExports) => {
          names.forEach((name) => {
            this._unwrap(moduleExports, name);
          });
        });
      }, "_massUnwrap");
      _warnOnPreloadedModules() {
        const nodeRequire = globalThis.require;
        if (!nodeRequire?.resolve || !nodeRequire?.cache)
          return;
        this._modules.forEach((module2) => {
          const { name } = module2;
          try {
            const resolvedModule = nodeRequire.resolve(name);
            if (nodeRequire.cache[resolvedModule]?.loaded) {
              this._diag.warn(`Module ${name} has been loaded before ${this.instrumentationName} so it might not work, please initialize it before requiring ${name}`);
            }
          } catch {
          }
        });
      }
      _extractPackageVersion(baseDir) {
        try {
          const json = (0, fs_1.readFileSync)(path2.join(baseDir, "package.json"), {
            encoding: "utf8"
          });
          const version = JSON.parse(json).version;
          return typeof version === "string" ? version : void 0;
        } catch {
          api_1.diag.warn("Failed extracting version", baseDir);
        }
        return void 0;
      }
      _onRequire(module2, exports2, name, baseDir) {
        if (!baseDir) {
          if (typeof module2.patch === "function") {
            module2.moduleExports = exports2;
            if (this._enabled) {
              this._diag.debug("Applying instrumentation patch for nodejs core module on require hook", {
                module: module2.name
              });
              return module2.patch(exports2);
            }
          }
          return exports2;
        }
        const version = this._extractPackageVersion(baseDir);
        module2.moduleVersion = version;
        if (module2.name === name) {
          if (isSupported(module2.supportedVersions, version, module2.includePrerelease)) {
            if (typeof module2.patch === "function") {
              module2.moduleExports = exports2;
              if (this._enabled) {
                this._diag.debug("Applying instrumentation patch for module on require hook", {
                  module: module2.name,
                  version: module2.moduleVersion,
                  baseDir
                });
                return module2.patch(exports2, module2.moduleVersion);
              }
            }
          }
          return exports2;
        }
        const files = module2.files ?? [];
        const normalizedName = path2.normalize(name);
        const supportedFileInstrumentations = files.filter((f) => f.name === normalizedName && isSupported(f.supportedVersions, version, module2.includePrerelease));
        return supportedFileInstrumentations.reduce((patchedExports, file) => {
          file.moduleExports = patchedExports;
          if (this._enabled) {
            this._diag.debug("Applying instrumentation patch for nodejs module file on require hook", {
              module: module2.name,
              version: module2.moduleVersion,
              fileName: file.name,
              baseDir
            });
            return file.patch(patchedExports, module2.moduleVersion);
          }
          return patchedExports;
        }, exports2);
      }
      enable() {
        if (this._enabled) {
          return;
        }
        this._enabled = true;
        if (this._hooks.length > 0) {
          for (const module2 of this._modules) {
            if (typeof module2.patch === "function" && module2.moduleExports) {
              this._diag.debug("Applying instrumentation patch for nodejs module on instrumentation enabled", {
                module: module2.name,
                version: module2.moduleVersion
              });
              module2.patch(module2.moduleExports, module2.moduleVersion);
            }
            for (const file of module2.files) {
              if (file.moduleExports) {
                this._diag.debug("Applying instrumentation patch for nodejs module file on instrumentation enabled", {
                  module: module2.name,
                  version: module2.moduleVersion,
                  fileName: file.name
                });
                file.patch(file.moduleExports, module2.moduleVersion);
              }
            }
          }
          return;
        }
        this._warnOnPreloadedModules();
        for (const module2 of this._modules) {
          const hookFn = /* @__PURE__ */ __name((exports2, name, baseDir) => {
            if (!baseDir && path2.isAbsolute(name)) {
              const parsedPath = path2.parse(name);
              name = parsedPath.name;
              baseDir = parsedPath.dir;
            }
            return this._onRequire(module2, exports2, name, baseDir);
          }, "hookFn");
          const onRequire = /* @__PURE__ */ __name((exports2, name, baseDir) => {
            return this._onRequire(module2, exports2, name, baseDir);
          }, "onRequire");
          const hook = path2.isAbsolute(module2.name) ? new require_in_the_middle_1.Hook([module2.name], { internals: true }, onRequire) : this._requireInTheMiddleSingleton.register(module2.name, onRequire);
          this._hooks.push(hook);
          const esmHook = new import_in_the_middle_1.Hook([module2.name], { internals: true }, hookFn);
          this._hooks.push(esmHook);
        }
      }
      disable() {
        if (!this._enabled) {
          return;
        }
        this._enabled = false;
        for (const module2 of this._modules) {
          if (typeof module2.unpatch === "function" && module2.moduleExports) {
            this._diag.debug("Removing instrumentation patch for nodejs module on instrumentation disabled", {
              module: module2.name,
              version: module2.moduleVersion
            });
            module2.unpatch(module2.moduleExports, module2.moduleVersion);
          }
          for (const file of module2.files) {
            if (file.moduleExports) {
              this._diag.debug("Removing instrumentation patch for nodejs module file on instrumentation disabled", {
                module: module2.name,
                version: module2.moduleVersion,
                fileName: file.name
              });
              file.unpatch(file.moduleExports, module2.moduleVersion);
            }
          }
        }
      }
      isEnabled() {
        return this._enabled;
      }
    };
    exports.InstrumentationBase = InstrumentationBase;
    function isSupported(supportedVersions, version, includePrerelease) {
      if (typeof version === "undefined") {
        return supportedVersions.includes("*");
      }
      return supportedVersions.some((supportedVersion) => {
        return (0, semver_1.satisfies)(version, supportedVersion, { includePrerelease });
      });
    }
    __name(isSupported, "isSupported");
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/normalize.js
var require_normalize = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/normalize.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.normalize = void 0;
    var path_1 = __require("path");
    Object.defineProperty(exports, "normalize", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return path_1.normalize;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/index.js
var require_node5 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/node/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.normalize = exports.InstrumentationBase = void 0;
    var instrumentation_1 = require_instrumentation2();
    Object.defineProperty(exports, "InstrumentationBase", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return instrumentation_1.InstrumentationBase;
    }, "get") });
    var normalize_1 = require_normalize();
    Object.defineProperty(exports, "normalize", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return normalize_1.normalize;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/index.js
var require_platform5 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/platform/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.normalize = exports.InstrumentationBase = void 0;
    var node_1 = require_node5();
    Object.defineProperty(exports, "InstrumentationBase", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.InstrumentationBase;
    }, "get") });
    Object.defineProperty(exports, "normalize", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.normalize;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/instrumentationNodeModuleDefinition.js
var require_instrumentationNodeModuleDefinition = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/instrumentationNodeModuleDefinition.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InstrumentationNodeModuleDefinition = void 0;
    var InstrumentationNodeModuleDefinition = class {
      static {
        __name(this, "InstrumentationNodeModuleDefinition");
      }
      files;
      name;
      supportedVersions;
      patch;
      unpatch;
      constructor(name, supportedVersions, patch, unpatch, files) {
        this.files = files || [];
        this.name = name;
        this.supportedVersions = supportedVersions;
        this.patch = patch;
        this.unpatch = unpatch;
      }
    };
    exports.InstrumentationNodeModuleDefinition = InstrumentationNodeModuleDefinition;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/instrumentationNodeModuleFile.js
var require_instrumentationNodeModuleFile = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/instrumentationNodeModuleFile.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InstrumentationNodeModuleFile = void 0;
    var index_1 = require_platform5();
    var InstrumentationNodeModuleFile = class {
      static {
        __name(this, "InstrumentationNodeModuleFile");
      }
      name;
      supportedVersions;
      patch;
      unpatch;
      constructor(name, supportedVersions, patch, unpatch) {
        this.name = (0, index_1.normalize)(name);
        this.supportedVersions = supportedVersions;
        this.patch = patch;
        this.unpatch = unpatch;
      }
    };
    exports.InstrumentationNodeModuleFile = InstrumentationNodeModuleFile;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/semconvStability.js
var require_semconvStability = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/semconvStability.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.semconvStabilityFromStr = exports.SemconvStability = void 0;
    var SemconvStability;
    (function(SemconvStability2) {
      SemconvStability2[SemconvStability2["STABLE"] = 1] = "STABLE";
      SemconvStability2[SemconvStability2["OLD"] = 2] = "OLD";
      SemconvStability2[SemconvStability2["DUPLICATE"] = 3] = "DUPLICATE";
    })(SemconvStability = exports.SemconvStability || (exports.SemconvStability = {}));
    function semconvStabilityFromStr(namespace, str) {
      let semconvStability = SemconvStability.OLD;
      const entries = str?.split(",").map((v) => v.trim()).filter((s) => s !== "");
      for (const entry of entries ?? []) {
        if (entry.toLowerCase() === namespace + "/dup") {
          semconvStability = SemconvStability.DUPLICATE;
          break;
        } else if (entry.toLowerCase() === namespace) {
          semconvStability = SemconvStability.STABLE;
        }
      }
      return semconvStability;
    }
    __name(semconvStabilityFromStr, "semconvStabilityFromStr");
    exports.semconvStabilityFromStr = semconvStabilityFromStr;
  }
});

// ../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/index.js
var require_src12 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+instrumentation@0.218.0+02ce4e9376d68682/node_modules/@opentelemetry/instrumentation/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.semconvStabilityFromStr = exports.SemconvStability = exports.safeExecuteInTheMiddleAsync = exports.safeExecuteInTheMiddle = exports.isWrapped = exports.InstrumentationNodeModuleFile = exports.InstrumentationNodeModuleDefinition = exports.InstrumentationBase = exports.registerInstrumentations = void 0;
    var autoLoader_1 = require_autoLoader();
    Object.defineProperty(exports, "registerInstrumentations", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return autoLoader_1.registerInstrumentations;
    }, "get") });
    var index_1 = require_platform5();
    Object.defineProperty(exports, "InstrumentationBase", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return index_1.InstrumentationBase;
    }, "get") });
    var instrumentationNodeModuleDefinition_1 = require_instrumentationNodeModuleDefinition();
    Object.defineProperty(exports, "InstrumentationNodeModuleDefinition", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return instrumentationNodeModuleDefinition_1.InstrumentationNodeModuleDefinition;
    }, "get") });
    var instrumentationNodeModuleFile_1 = require_instrumentationNodeModuleFile();
    Object.defineProperty(exports, "InstrumentationNodeModuleFile", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return instrumentationNodeModuleFile_1.InstrumentationNodeModuleFile;
    }, "get") });
    var utils_1 = require_utils6();
    Object.defineProperty(exports, "isWrapped", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return utils_1.isWrapped;
    }, "get") });
    Object.defineProperty(exports, "safeExecuteInTheMiddle", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return utils_1.safeExecuteInTheMiddle;
    }, "get") });
    Object.defineProperty(exports, "safeExecuteInTheMiddleAsync", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return utils_1.safeExecuteInTheMiddleAsync;
    }, "get") });
    var semconvStability_1 = require_semconvStability();
    Object.defineProperty(exports, "SemconvStability", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return semconvStability_1.SemconvStability;
    }, "get") });
    Object.defineProperty(exports, "semconvStabilityFromStr", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return semconvStability_1.semconvStabilityFromStr;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/utils/validation.js
var require_validation = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/utils/validation.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isLogAttributeValue = void 0;
    function isLogAttributeValue(val) {
      return isLogAttributeValueInternal(val, /* @__PURE__ */ new WeakSet());
    }
    __name(isLogAttributeValue, "isLogAttributeValue");
    exports.isLogAttributeValue = isLogAttributeValue;
    function isLogAttributeValueInternal(val, visited) {
      if (val == null) {
        return true;
      }
      if (typeof val === "string" || typeof val === "number" || typeof val === "boolean") {
        return true;
      }
      if (val instanceof Uint8Array) {
        return true;
      }
      if (typeof val === "object") {
        if (visited.has(val)) {
          return false;
        }
        visited.add(val);
        if (Array.isArray(val)) {
          return val.every((item) => isLogAttributeValueInternal(item, visited));
        }
        const obj = val;
        if (obj.constructor !== Object && obj.constructor !== void 0) {
          return false;
        }
        return Object.values(obj).every((item) => isLogAttributeValueInternal(item, visited));
      }
      return false;
    }
    __name(isLogAttributeValueInternal, "isLogAttributeValueInternal");
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/LogRecordImpl.js
var require_LogRecordImpl = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/LogRecordImpl.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LogRecordImpl = void 0;
    var api = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var semantic_conventions_1 = (init_esm3(), __toCommonJS(esm_exports2));
    var validation_1 = require_validation();
    var LogRecordImpl = class {
      static {
        __name(this, "LogRecordImpl");
      }
      hrTime;
      hrTimeObserved;
      spanContext;
      resource;
      instrumentationScope;
      attributes = {};
      _severityText;
      _severityNumber;
      _body;
      _eventName;
      _attributesCount = 0;
      _droppedAttributesCount = 0;
      _isReadonly = false;
      _logRecordLimits;
      set severityText(severityText) {
        if (this._isLogRecordReadonly()) {
          return;
        }
        this._severityText = severityText;
      }
      get severityText() {
        return this._severityText;
      }
      set severityNumber(severityNumber) {
        if (this._isLogRecordReadonly()) {
          return;
        }
        this._severityNumber = severityNumber;
      }
      get severityNumber() {
        return this._severityNumber;
      }
      set body(body) {
        if (this._isLogRecordReadonly()) {
          return;
        }
        this._body = body;
      }
      get body() {
        return this._body;
      }
      get eventName() {
        return this._eventName;
      }
      set eventName(eventName) {
        if (this._isLogRecordReadonly()) {
          return;
        }
        this._eventName = eventName;
      }
      get droppedAttributesCount() {
        return this._droppedAttributesCount;
      }
      constructor(_sharedState, instrumentationScope, logRecord) {
        const { timestamp, observedTimestamp, eventName, severityNumber, severityText, body, attributes = {}, exception, context: context2 } = logRecord;
        const now = Date.now();
        this.hrTime = (0, core_1.timeInputToHrTime)(timestamp ?? now);
        this.hrTimeObserved = (0, core_1.timeInputToHrTime)(observedTimestamp ?? now);
        if (context2) {
          const spanContext = api.trace.getSpanContext(context2);
          if (spanContext && api.isSpanContextValid(spanContext)) {
            this.spanContext = spanContext;
          }
        }
        this.severityNumber = severityNumber;
        this.severityText = severityText;
        this.body = body;
        this.resource = _sharedState.resource;
        this.instrumentationScope = instrumentationScope;
        this._logRecordLimits = _sharedState.logRecordLimits;
        this._eventName = eventName;
        this.setAttributes(attributes);
        if (exception != null) {
          this._setException(exception);
        }
      }
      setAttribute(key, value2) {
        if (this._isLogRecordReadonly()) {
          return this;
        }
        if (key.length === 0) {
          api.diag.warn(`Invalid attribute key: ${key}`);
          return this;
        }
        if (!(0, validation_1.isLogAttributeValue)(value2)) {
          api.diag.warn(`Invalid attribute value set for key: ${key}`);
          return this;
        }
        const isNewKey = !Object.prototype.hasOwnProperty.call(this.attributes, key);
        if (isNewKey && this._attributesCount >= this._logRecordLimits.attributeCountLimit) {
          this._droppedAttributesCount++;
          if (this._droppedAttributesCount === 1) {
            api.diag.warn("Dropping extra attributes.");
          }
          return this;
        }
        this.attributes[key] = this._truncateToSize(value2);
        if (isNewKey) {
          this._attributesCount++;
        }
        return this;
      }
      setAttributes(attributes) {
        for (const [k, v] of Object.entries(attributes)) {
          this.setAttribute(k, v);
        }
        return this;
      }
      setBody(body) {
        this.body = body;
        return this;
      }
      setEventName(eventName) {
        this.eventName = eventName;
        return this;
      }
      setSeverityNumber(severityNumber) {
        this.severityNumber = severityNumber;
        return this;
      }
      setSeverityText(severityText) {
        this.severityText = severityText;
        return this;
      }
      /**
       * @internal
       * A LogRecordProcessor may freely modify logRecord for the duration of the OnEmit call.
       * If logRecord is needed after OnEmit returns (i.e. for asynchronous processing) only reads are permitted.
       */
      _makeReadonly() {
        this._isReadonly = true;
      }
      _truncateToSize(value2) {
        const limit = this._logRecordLimits.attributeValueLengthLimit;
        if (limit <= 0) {
          api.diag.warn(`Attribute value limit must be positive, got ${limit}`);
          return value2;
        }
        if (value2 == null) {
          return value2;
        }
        if (typeof value2 === "string") {
          return this._truncateToLimitUtil(value2, limit);
        }
        if (value2 instanceof Uint8Array) {
          return value2;
        }
        if (Array.isArray(value2)) {
          return value2.map((val) => this._truncateToSize(val));
        }
        if (typeof value2 === "object") {
          const truncatedObj = {};
          for (const [k, v] of Object.entries(value2)) {
            truncatedObj[k] = this._truncateToSize(v);
          }
          return truncatedObj;
        }
        return value2;
      }
      _setException(exception) {
        let hasMinimumAttributes = false;
        if (typeof exception === "string" || typeof exception === "number") {
          if (!Object.hasOwn(this.attributes, semantic_conventions_1.ATTR_EXCEPTION_MESSAGE)) {
            this.setAttribute(semantic_conventions_1.ATTR_EXCEPTION_MESSAGE, String(exception));
          }
          hasMinimumAttributes = true;
        } else if (exception && typeof exception === "object") {
          const exceptionObj = exception;
          if (exceptionObj.code) {
            if (!Object.hasOwn(this.attributes, semantic_conventions_1.ATTR_EXCEPTION_TYPE)) {
              this.setAttribute(semantic_conventions_1.ATTR_EXCEPTION_TYPE, exceptionObj.code.toString());
            }
            hasMinimumAttributes = true;
          } else if (exceptionObj.name) {
            if (!Object.hasOwn(this.attributes, semantic_conventions_1.ATTR_EXCEPTION_TYPE)) {
              this.setAttribute(semantic_conventions_1.ATTR_EXCEPTION_TYPE, exceptionObj.name);
            }
            hasMinimumAttributes = true;
          }
          if (exceptionObj.message) {
            if (!Object.hasOwn(this.attributes, semantic_conventions_1.ATTR_EXCEPTION_MESSAGE)) {
              this.setAttribute(semantic_conventions_1.ATTR_EXCEPTION_MESSAGE, exceptionObj.message);
            }
            hasMinimumAttributes = true;
          }
          if (exceptionObj.stack) {
            if (!Object.hasOwn(this.attributes, semantic_conventions_1.ATTR_EXCEPTION_STACKTRACE)) {
              this.setAttribute(semantic_conventions_1.ATTR_EXCEPTION_STACKTRACE, exceptionObj.stack);
            }
            hasMinimumAttributes = true;
          }
        }
        if (!hasMinimumAttributes) {
          api.diag.warn(`Failed to record an exception ${exception}`);
        }
      }
      _truncateToLimitUtil(value2, limit) {
        if (value2.length <= limit) {
          return value2;
        }
        return value2.substring(0, limit);
      }
      _isLogRecordReadonly() {
        if (this._isReadonly) {
          api.diag.warn("Can not execute the operation on emitted log record");
        }
        return this._isReadonly;
      }
    };
    exports.LogRecordImpl = LogRecordImpl;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/Logger.js
var require_Logger = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/Logger.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Logger = void 0;
    var api_logs_1 = require_src3();
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var LogRecordImpl_1 = require_LogRecordImpl();
    var Logger = class {
      static {
        __name(this, "Logger");
      }
      instrumentationScope;
      _sharedState;
      _loggerConfig;
      constructor(instrumentationScope, sharedState) {
        this.instrumentationScope = instrumentationScope;
        this._sharedState = sharedState;
        this._loggerConfig = this._sharedState.getLoggerConfig(this.instrumentationScope);
      }
      emit(logRecord) {
        const currentContext = logRecord.context || api_1.context.active();
        if (!this.enabled(logRecord)) {
          return;
        }
        const logRecordInstance = new LogRecordImpl_1.LogRecordImpl(this._sharedState, this.instrumentationScope, {
          context: currentContext,
          ...logRecord
        });
        this._sharedState.loggerMetrics.emitLog();
        this._sharedState.activeProcessor.onEmit(logRecordInstance, currentContext);
        logRecordInstance._makeReadonly();
      }
      enabled(options) {
        const loggerConfig = this._loggerConfig;
        if (loggerConfig.disabled) {
          return false;
        }
        const severityNumber = options?.severityNumber;
        if (typeof severityNumber === "number" && severityNumber !== api_logs_1.SeverityNumber.UNSPECIFIED && severityNumber < loggerConfig.minimumSeverity) {
          return false;
        }
        const currentContext = options?.context || api_1.context.active();
        if (loggerConfig.traceBased) {
          const spanContext = api_1.trace.getSpanContext(currentContext);
          if (spanContext && (0, api_1.isSpanContextValid)(spanContext)) {
            const isSampled = (spanContext.traceFlags & api_1.TraceFlags.SAMPLED) === api_1.TraceFlags.SAMPLED;
            if (!isSampled) {
              return false;
            }
          }
        }
        const enabledOpts = {
          context: currentContext,
          instrumentationScope: this.instrumentationScope,
          severityNumber: options?.severityNumber,
          eventName: options?.eventName
        };
        for (const processor of this._sharedState.processors) {
          if (!processor.enabled || processor.enabled(enabledOpts)) {
            return true;
          }
        }
        return false;
      }
    };
    exports.Logger = Logger;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/NoopLogRecordProcessor.js
var require_NoopLogRecordProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/NoopLogRecordProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.NoopLogRecordProcessor = void 0;
    var NoopLogRecordProcessor = class {
      static {
        __name(this, "NoopLogRecordProcessor");
      }
      forceFlush() {
        return Promise.resolve();
      }
      onEmit(_logRecord, _context) {
      }
      shutdown() {
        return Promise.resolve();
      }
      enabled(_options) {
        return false;
      }
    };
    exports.NoopLogRecordProcessor = NoopLogRecordProcessor;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/MultiLogRecordProcessor.js
var require_MultiLogRecordProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/MultiLogRecordProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MultiLogRecordProcessor = void 0;
    var core_1 = require_src();
    var MultiLogRecordProcessor = class {
      static {
        __name(this, "MultiLogRecordProcessor");
      }
      processors;
      forceFlushTimeoutMillis;
      constructor(processors, forceFlushTimeoutMillis) {
        this.processors = processors;
        this.forceFlushTimeoutMillis = forceFlushTimeoutMillis;
      }
      async forceFlush() {
        const timeout = this.forceFlushTimeoutMillis;
        await Promise.all(this.processors.map((processor) => (0, core_1.callWithTimeout)(processor.forceFlush(), timeout)));
      }
      onEmit(logRecord, context2) {
        this.processors.forEach((processors) => processors.onEmit(logRecord, context2));
      }
      async shutdown() {
        await Promise.all(this.processors.map((processor) => processor.shutdown()));
      }
      enabled(options) {
        for (const processor of this.processors) {
          if (!processor.enabled || processor.enabled(options)) {
            return true;
          }
        }
        return false;
      }
    };
    exports.MultiLogRecordProcessor = MultiLogRecordProcessor;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/internal/utils.js
var require_utils7 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/internal/utils.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getInstrumentationScopeKey = void 0;
    function getInstrumentationScopeKey(scope) {
      return `${scope.name}@${scope.version || ""}:${scope.schemaUrl || ""}`;
    }
    __name(getInstrumentationScopeKey, "getInstrumentationScopeKey");
    exports.getInstrumentationScopeKey = getInstrumentationScopeKey;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/semconv.js
var require_semconv3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/semconv.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.METRIC_OTEL_SDK_LOG_CREATED = void 0;
    exports.METRIC_OTEL_SDK_LOG_CREATED = "otel.sdk.log.created";
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/LoggerMetrics.js
var require_LoggerMetrics = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/LoggerMetrics.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LoggerMetrics = void 0;
    var semconv_1 = require_semconv3();
    var LoggerMetrics = class {
      static {
        __name(this, "LoggerMetrics");
      }
      createdLogs;
      constructor(meter) {
        this.createdLogs = meter.createCounter(semconv_1.METRIC_OTEL_SDK_LOG_CREATED, {
          unit: "{log_record}",
          description: "The number of logs submitted to enabled SDK Loggers."
        });
      }
      emitLog() {
        this.createdLogs.add(1);
      }
    };
    exports.LoggerMetrics = LoggerMetrics;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/version.js
var require_version2 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/version.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VERSION = void 0;
    exports.VERSION = "0.218.0";
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/internal/LoggerProviderSharedState.js
var require_LoggerProviderSharedState = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/internal/LoggerProviderSharedState.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LoggerProviderSharedState = exports.DEFAULT_LOGGER_CONFIGURATOR = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var api_logs_1 = require_src3();
    var NoopLogRecordProcessor_1 = require_NoopLogRecordProcessor();
    var MultiLogRecordProcessor_1 = require_MultiLogRecordProcessor();
    var utils_1 = require_utils7();
    var LoggerMetrics_1 = require_LoggerMetrics();
    var version_1 = require_version2();
    var DEFAULT_LOGGER_CONFIG = {
      disabled: false,
      minimumSeverity: api_logs_1.SeverityNumber.UNSPECIFIED,
      traceBased: false
    };
    var DEFAULT_LOGGER_CONFIGURATOR = /* @__PURE__ */ __name(() => ({
      ...DEFAULT_LOGGER_CONFIG
    }), "DEFAULT_LOGGER_CONFIGURATOR");
    exports.DEFAULT_LOGGER_CONFIGURATOR = DEFAULT_LOGGER_CONFIGURATOR;
    var LoggerProviderSharedState = class {
      static {
        __name(this, "LoggerProviderSharedState");
      }
      loggers = /* @__PURE__ */ new Map();
      activeProcessor;
      registeredLogRecordProcessors = [];
      resource;
      forceFlushTimeoutMillis;
      logRecordLimits;
      processors;
      loggerMetrics;
      _loggerConfigurator;
      _loggerConfigs = /* @__PURE__ */ new Map();
      constructor(resource, forceFlushTimeoutMillis, logRecordLimits, processors, loggerConfigurator, meterProvider) {
        this.resource = resource;
        this.forceFlushTimeoutMillis = forceFlushTimeoutMillis;
        this.logRecordLimits = logRecordLimits;
        this.processors = processors;
        if (processors.length > 0) {
          this.registeredLogRecordProcessors = processors;
          this.activeProcessor = new MultiLogRecordProcessor_1.MultiLogRecordProcessor(this.registeredLogRecordProcessors, this.forceFlushTimeoutMillis);
        } else {
          this.activeProcessor = new NoopLogRecordProcessor_1.NoopLogRecordProcessor();
        }
        this._loggerConfigurator = loggerConfigurator ?? exports.DEFAULT_LOGGER_CONFIGURATOR;
        const meter = meterProvider ? meterProvider.getMeter("@opentelemetry/sdk-logs", version_1.VERSION) : (0, api_1.createNoopMeter)();
        this.loggerMetrics = new LoggerMetrics_1.LoggerMetrics(meter);
      }
      /**
       * Get the LoggerConfig for a given instrumentation scope.
       * Uses the LoggerConfigurator function to compute the config on first access
       * and caches the result.
       *
       * @experimental This feature is in development as per the OpenTelemetry specification.
       */
      getLoggerConfig(instrumentationScope) {
        const key = (0, utils_1.getInstrumentationScopeKey)(instrumentationScope);
        let config = this._loggerConfigs.get(key);
        if (config) {
          return config;
        }
        config = this._loggerConfigurator(instrumentationScope);
        this._loggerConfigs.set(key, config);
        return config;
      }
    };
    exports.LoggerProviderSharedState = LoggerProviderSharedState;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/LoggerProvider.js
var require_LoggerProvider = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/LoggerProvider.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LoggerProvider = exports.DEFAULT_LOGGER_NAME = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var api_logs_1 = require_src3();
    var resources_1 = require_src5();
    var core_1 = require_src();
    var Logger_1 = require_Logger();
    var LoggerProviderSharedState_1 = require_LoggerProviderSharedState();
    exports.DEFAULT_LOGGER_NAME = "unknown";
    var LoggerProvider2 = class {
      static {
        __name(this, "LoggerProvider");
      }
      _shutdownOnce;
      _sharedState;
      constructor(config = {}) {
        const mergedConfig = {
          resource: config.resource ?? (0, resources_1.defaultResource)(),
          forceFlushTimeoutMillis: config.forceFlushTimeoutMillis ?? 3e4,
          logRecordLimits: {
            attributeCountLimit: config.logRecordLimits?.attributeCountLimit ?? 128,
            attributeValueLengthLimit: config.logRecordLimits?.attributeValueLengthLimit ?? Infinity
          },
          loggerConfigurator: config.loggerConfigurator ?? LoggerProviderSharedState_1.DEFAULT_LOGGER_CONFIGURATOR,
          processors: config.processors ?? [],
          meterProvider: config.meterProvider
        };
        this._sharedState = new LoggerProviderSharedState_1.LoggerProviderSharedState(mergedConfig.resource, mergedConfig.forceFlushTimeoutMillis, mergedConfig.logRecordLimits, mergedConfig.processors, mergedConfig.loggerConfigurator, mergedConfig.meterProvider);
        this._shutdownOnce = new core_1.BindOnceFuture(this._shutdown, this);
      }
      /**
       * Get a logger with the configuration of the LoggerProvider.
       */
      getLogger(name, version, options) {
        if (this._shutdownOnce.isCalled) {
          api_1.diag.warn("A shutdown LoggerProvider cannot provide a Logger");
          return api_logs_1.NOOP_LOGGER;
        }
        if (!name) {
          api_1.diag.warn("Logger requested without instrumentation scope name.");
        }
        const loggerName = name || exports.DEFAULT_LOGGER_NAME;
        const key = `${loggerName}@${version || ""}:${options?.schemaUrl || ""}`;
        if (!this._sharedState.loggers.has(key)) {
          this._sharedState.loggers.set(key, new Logger_1.Logger({ name: loggerName, version, schemaUrl: options?.schemaUrl }, this._sharedState));
        }
        return this._sharedState.loggers.get(key);
      }
      /**
       * Notifies all registered LogRecordProcessor to flush any buffered data.
       *
       * Returns a promise which is resolved when all flushes are complete.
       */
      forceFlush() {
        if (this._shutdownOnce.isCalled) {
          api_1.diag.warn("invalid attempt to force flush after LoggerProvider shutdown");
          return this._shutdownOnce.promise;
        }
        return this._sharedState.activeProcessor.forceFlush();
      }
      /**
       * Flush all buffered data and shut down the LoggerProvider and all registered
       * LogRecordProcessor.
       *
       * Returns a promise which is resolved when all flushes are complete.
       */
      shutdown() {
        if (this._shutdownOnce.isCalled) {
          api_1.diag.warn("shutdown may only be called once per LoggerProvider");
          return this._shutdownOnce.promise;
        }
        return this._shutdownOnce.call();
      }
      _shutdown() {
        return this._sharedState.activeProcessor.shutdown();
      }
    };
    exports.LoggerProvider = LoggerProvider2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/ConsoleLogRecordExporter.js
var require_ConsoleLogRecordExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/ConsoleLogRecordExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConsoleLogRecordExporter = void 0;
    var core_1 = require_src();
    var ConsoleLogRecordExporter = class {
      static {
        __name(this, "ConsoleLogRecordExporter");
      }
      /**
       * Export logs.
       * @param logs
       * @param resultCallback
       */
      export(logs2, resultCallback) {
        this._sendLogRecords(logs2, resultCallback);
      }
      /**
       * ForceFlush the exporter.
       * No-op for {@link ConsoleLogRecordExporter}
       */
      async forceFlush() {
      }
      /**
       * Shutdown the exporter.
       */
      async shutdown() {
      }
      /**
       * converts logRecord info into more readable format
       * @param logRecord
       */
      _exportInfo(logRecord) {
        return {
          resource: {
            attributes: logRecord.resource.attributes
          },
          instrumentationScope: logRecord.instrumentationScope,
          timestamp: (0, core_1.hrTimeToMicroseconds)(logRecord.hrTime),
          traceId: logRecord.spanContext?.traceId,
          spanId: logRecord.spanContext?.spanId,
          traceFlags: logRecord.spanContext?.traceFlags,
          severityText: logRecord.severityText,
          severityNumber: logRecord.severityNumber,
          eventName: logRecord.eventName,
          body: logRecord.body,
          attributes: logRecord.attributes
        };
      }
      /**
       * Showing logs  in console
       * @param logRecords
       * @param done
       */
      _sendLogRecords(logRecords, done) {
        for (const logRecord of logRecords) {
          console.dir(this._exportInfo(logRecord), { depth: 3 });
        }
        done?.({ code: core_1.ExportResultCode.SUCCESS });
      }
    };
    exports.ConsoleLogRecordExporter = ConsoleLogRecordExporter;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/SimpleLogRecordProcessor.js
var require_SimpleLogRecordProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/SimpleLogRecordProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SimpleLogRecordProcessor = void 0;
    var core_1 = require_src();
    var SimpleLogRecordProcessor2 = class {
      static {
        __name(this, "SimpleLogRecordProcessor");
      }
      _exporter;
      _shutdownOnce;
      _unresolvedExports;
      constructor(exporter) {
        this._exporter = exporter;
        this._shutdownOnce = new core_1.BindOnceFuture(this._shutdown, this);
        this._unresolvedExports = /* @__PURE__ */ new Set();
      }
      onEmit(logRecord, _context) {
        if (this._shutdownOnce.isCalled) {
          return;
        }
        const doExport = /* @__PURE__ */ __name(() => core_1.internal._export(this._exporter, [logRecord]).then((result) => {
          if (result.code !== core_1.ExportResultCode.SUCCESS) {
            (0, core_1.globalErrorHandler)(result.error ?? new Error(`SimpleLogRecordProcessor: log record export failed (status ${result})`));
          }
        }).catch(core_1.globalErrorHandler), "doExport");
        if (logRecord.resource.asyncAttributesPending) {
          const exportPromise = logRecord.resource.waitForAsyncAttributes?.().then(() => {
            this._unresolvedExports.delete(exportPromise);
            return doExport();
          }, core_1.globalErrorHandler);
          if (exportPromise != null) {
            this._unresolvedExports.add(exportPromise);
          }
        } else {
          void doExport();
        }
      }
      async forceFlush() {
        await Promise.all(Array.from(this._unresolvedExports));
      }
      shutdown() {
        return this._shutdownOnce.call();
      }
      _shutdown() {
        return this._exporter.shutdown();
      }
    };
    exports.SimpleLogRecordProcessor = SimpleLogRecordProcessor2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/InMemoryLogRecordExporter.js
var require_InMemoryLogRecordExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/InMemoryLogRecordExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InMemoryLogRecordExporter = void 0;
    var core_1 = require_src();
    var InMemoryLogRecordExporter = class {
      static {
        __name(this, "InMemoryLogRecordExporter");
      }
      _finishedLogRecords = [];
      /**
       * Indicates if the exporter has been "shutdown."
       * When false, exported log records will not be stored in-memory.
       */
      _stopped = false;
      export(logs2, resultCallback) {
        if (this._stopped) {
          return resultCallback({
            code: core_1.ExportResultCode.FAILED,
            error: new Error("Exporter has been stopped")
          });
        }
        this._finishedLogRecords.push(...logs2);
        resultCallback({ code: core_1.ExportResultCode.SUCCESS });
      }
      async shutdown() {
        this._stopped = true;
        this.reset();
      }
      async forceFlush() {
      }
      getFinishedLogRecords() {
        return this._finishedLogRecords;
      }
      reset() {
        this._finishedLogRecords = [];
      }
    };
    exports.InMemoryLogRecordExporter = InMemoryLogRecordExporter;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/BatchLogRecordProcessorBase.js
var require_BatchLogRecordProcessorBase = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/export/BatchLogRecordProcessorBase.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BatchLogRecordProcessorBase = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    async function waitForResources(logRecords) {
      const pendingResources = [];
      for (let i = 0, len = logRecords.length; i < len; i++) {
        const logRecord = logRecords[i];
        if (logRecord.resource.asyncAttributesPending && logRecord.resource.waitForAsyncAttributes) {
          pendingResources.push(logRecord.resource.waitForAsyncAttributes());
        }
      }
      if (pendingResources != null && pendingResources.length > 0) {
        await Promise.all(pendingResources);
      }
    }
    __name(waitForResources, "waitForResources");
    var ExportOperation = class {
      static {
        __name(this, "ExportOperation");
      }
      _exportCompleted;
      _exportScheduledPromise;
      _exportScheduledResolve;
      constructor(exporter, logRecords, exportTimeoutMillis) {
        this._exportScheduledPromise = new Promise((resolve2) => {
          this._exportScheduledResolve = resolve2;
        });
        this._exportCompleted = this._executeExport(exporter, logRecords, exportTimeoutMillis);
      }
      /** Get the promise that resolves when the export completes */
      get exportCompleted() {
        return this._exportCompleted;
      }
      /** Get the promise that resolves when exporter.export() has been called */
      get exportScheduled() {
        return this._exportScheduledPromise;
      }
      async _executeExport(exporter, logRecords, exportTimeoutMillis) {
        try {
          await waitForResources(logRecords);
          await api_1.context.with((0, core_1.suppressTracing)(api_1.context.active()), async () => {
            return this._exportWithTimeout(exporter, logRecords, exportTimeoutMillis);
          });
        } catch (e) {
          (0, core_1.globalErrorHandler)(e);
          this._exportScheduledResolve();
        }
      }
      async _exportWithTimeout(exporter, logRecords, exportTimeoutMillis) {
        return new Promise((resolve2, reject) => {
          const timer = setTimeout(() => {
            reject(new Error("Timeout"));
          }, exportTimeoutMillis);
          exporter.export(logRecords, (result) => {
            clearTimeout(timer);
            if (result.code === core_1.ExportResultCode.SUCCESS) {
              resolve2();
            } else {
              reject(result.error ?? new Error("BatchLogRecordProcessor: log record export failed"));
            }
          });
          this._exportScheduledResolve();
        });
      }
    };
    var BatchLogRecordProcessorBase = class {
      static {
        __name(this, "BatchLogRecordProcessorBase");
      }
      _maxExportBatchSize;
      _maxQueueSize;
      _scheduledDelayMillis;
      _exportTimeoutMillis;
      _exporter;
      _currentExport = null;
      _finishedLogRecords = [];
      _timer;
      _shutdownOnce;
      _flushing = false;
      constructor(exporter, config) {
        this._exporter = exporter;
        this._maxExportBatchSize = config?.maxExportBatchSize ?? 512;
        this._maxQueueSize = config?.maxQueueSize ?? 2048;
        this._scheduledDelayMillis = config?.scheduledDelayMillis ?? 5e3;
        this._exportTimeoutMillis = config?.exportTimeoutMillis ?? 3e4;
        this._shutdownOnce = new core_1.BindOnceFuture(this._shutdown, this);
        if (this._maxExportBatchSize > this._maxQueueSize) {
          api_1.diag.warn("BatchLogRecordProcessor: maxExportBatchSize must be smaller or equal to maxQueueSize, setting maxExportBatchSize to match maxQueueSize");
          this._maxExportBatchSize = this._maxQueueSize;
        }
      }
      onEmit(logRecord) {
        if (this._shutdownOnce.isCalled) {
          return;
        }
        this._addToBuffer(logRecord);
      }
      forceFlush() {
        if (this._shutdownOnce.isCalled) {
          return this._shutdownOnce.promise;
        }
        return this._flushAll();
      }
      /** Add a LogRecord in the buffer. */
      _addToBuffer(logRecord) {
        if (this._finishedLogRecords.length >= this._maxQueueSize) {
          return;
        }
        this._finishedLogRecords.push(logRecord);
        this._maybeStartTimer();
      }
      shutdown() {
        return this._shutdownOnce.call();
      }
      async _shutdown() {
        this.onShutdown();
        await this._flushAll();
        await this._exporter.shutdown();
      }
      /**
       * Send all LogRecords to the exporter respecting the batch size limit
       * This function is used only on forceFlush or shutdown,
       * for all other cases _exportOneBatch should be used
       * */
      async _flushAll() {
        if (this._flushing) {
          return;
        }
        this._flushing = true;
        let toFlush = this._finishedLogRecords;
        this._finishedLogRecords = [];
        this._clearTimer();
        if (this._currentExport !== null) {
          await this._exporter.forceFlush();
          await this._currentExport.exportCompleted;
          this._currentExport = null;
        }
        while (toFlush.length > 0) {
          let batch;
          if (toFlush.length <= this._maxExportBatchSize) {
            batch = toFlush;
            toFlush = [];
          } else {
            batch = toFlush.splice(0, this._maxExportBatchSize);
          }
          const exportOp = new ExportOperation(this._exporter, batch, this._exportTimeoutMillis);
          this._currentExport = exportOp;
          try {
            await exportOp.exportScheduled;
            await this._exporter.forceFlush();
            await exportOp.exportCompleted;
          } catch (e) {
            (0, core_1.globalErrorHandler)(e);
          } finally {
            this._currentExport = null;
          }
        }
        this._flushing = false;
        this._maybeStartTimer();
      }
      /**
       * Extracts one batch from the buffer.
       * Returns null if buffer is empty.
       */
      _extractBatch() {
        if (this._finishedLogRecords.length === 0) {
          return null;
        }
        if (this._finishedLogRecords.length <= this._maxExportBatchSize) {
          const batch = this._finishedLogRecords;
          this._finishedLogRecords = [];
          return batch;
        } else {
          return this._finishedLogRecords.splice(0, this._maxExportBatchSize);
        }
      }
      _exportOneBatch() {
        this._clearTimer();
        const logRecords = this._extractBatch();
        if (logRecords === null) {
          return;
        }
        const exportOp = new ExportOperation(this._exporter, logRecords, this._exportTimeoutMillis);
        this._currentExport = exportOp;
        exportOp.exportCompleted.then(() => {
          this._currentExport = null;
          this._maybeStartTimer();
        }).catch((error) => {
          this._currentExport = null;
          (0, core_1.globalErrorHandler)(error);
          this._maybeStartTimer();
        });
      }
      _maybeStartTimer() {
        if (this._shutdownOnce.isCalled) {
          return;
        }
        if (this._flushing) {
          return;
        }
        if (this._finishedLogRecords.length === 0) {
          return;
        }
        if (this._currentExport !== null) {
          return;
        }
        if (this._finishedLogRecords.length >= this._maxExportBatchSize) {
          this._exportOneBatch();
          return;
        }
        if (this._timer !== void 0) {
          return;
        }
        this._timer = setTimeout(() => {
          this._timer = void 0;
          this._exportOneBatch();
        }, this._scheduledDelayMillis);
        if (typeof this._timer !== "number") {
          this._timer.unref();
        }
      }
      _clearTimer() {
        if (this._timer !== void 0) {
          clearTimeout(this._timer);
          this._timer = void 0;
        }
      }
    };
    exports.BatchLogRecordProcessorBase = BatchLogRecordProcessorBase;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/platform/node/export/BatchLogRecordProcessor.js
var require_BatchLogRecordProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/platform/node/export/BatchLogRecordProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BatchLogRecordProcessor = void 0;
    var BatchLogRecordProcessorBase_1 = require_BatchLogRecordProcessorBase();
    var BatchLogRecordProcessor2 = class extends BatchLogRecordProcessorBase_1.BatchLogRecordProcessorBase {
      static {
        __name(this, "BatchLogRecordProcessor");
      }
      onShutdown() {
      }
    };
    exports.BatchLogRecordProcessor = BatchLogRecordProcessor2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/platform/node/index.js
var require_node6 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/platform/node/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BatchLogRecordProcessor = void 0;
    var BatchLogRecordProcessor_1 = require_BatchLogRecordProcessor();
    Object.defineProperty(exports, "BatchLogRecordProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return BatchLogRecordProcessor_1.BatchLogRecordProcessor;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/platform/index.js
var require_platform6 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/platform/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BatchLogRecordProcessor = void 0;
    var node_1 = require_node6();
    Object.defineProperty(exports, "BatchLogRecordProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.BatchLogRecordProcessor;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/config/LoggerConfigurators.js
var require_LoggerConfigurators = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/config/LoggerConfigurators.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createLoggerConfigurator = void 0;
    var api_logs_1 = require_src3();
    var DEFAULT_LOGGER_CONFIG = {
      disabled: false,
      minimumSeverity: api_logs_1.SeverityNumber.UNSPECIFIED,
      traceBased: false
    };
    function createLoggerConfigurator(patterns) {
      return (loggerScope) => {
        const loggerName = loggerScope.name;
        for (const { pattern, config } of patterns) {
          if (matchesPattern(loggerName, pattern)) {
            return {
              disabled: config.disabled ?? DEFAULT_LOGGER_CONFIG.disabled,
              minimumSeverity: config.minimumSeverity ?? DEFAULT_LOGGER_CONFIG.minimumSeverity,
              traceBased: config.traceBased ?? DEFAULT_LOGGER_CONFIG.traceBased
            };
          }
        }
        return { ...DEFAULT_LOGGER_CONFIG };
      };
    }
    __name(createLoggerConfigurator, "createLoggerConfigurator");
    exports.createLoggerConfigurator = createLoggerConfigurator;
    function matchesPattern(name, pattern) {
      if (pattern === name) {
        return true;
      }
      if (pattern.includes("*")) {
        const regexPattern = pattern.split("*").map((part) => part.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")).join(".*");
        const regex = new RegExp(`^${regexPattern}$`);
        return regex.test(name);
      }
      return false;
    }
    __name(matchesPattern, "matchesPattern");
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/index.js
var require_src13 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-logs@0.218.0+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-logs/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createLoggerConfigurator = exports.BatchLogRecordProcessor = exports.InMemoryLogRecordExporter = exports.SimpleLogRecordProcessor = exports.ConsoleLogRecordExporter = exports.LoggerProvider = void 0;
    var LoggerProvider_1 = require_LoggerProvider();
    Object.defineProperty(exports, "LoggerProvider", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return LoggerProvider_1.LoggerProvider;
    }, "get") });
    var ConsoleLogRecordExporter_1 = require_ConsoleLogRecordExporter();
    Object.defineProperty(exports, "ConsoleLogRecordExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ConsoleLogRecordExporter_1.ConsoleLogRecordExporter;
    }, "get") });
    var SimpleLogRecordProcessor_1 = require_SimpleLogRecordProcessor();
    Object.defineProperty(exports, "SimpleLogRecordProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return SimpleLogRecordProcessor_1.SimpleLogRecordProcessor;
    }, "get") });
    var InMemoryLogRecordExporter_1 = require_InMemoryLogRecordExporter();
    Object.defineProperty(exports, "InMemoryLogRecordExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return InMemoryLogRecordExporter_1.InMemoryLogRecordExporter;
    }, "get") });
    var platform_1 = require_platform6();
    Object.defineProperty(exports, "BatchLogRecordProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.BatchLogRecordProcessor;
    }, "get") });
    var LoggerConfigurators_1 = require_LoggerConfigurators();
    Object.defineProperty(exports, "createLoggerConfigurator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return LoggerConfigurators_1.createLoggerConfigurator;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/enums.js
var require_enums = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/enums.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ExceptionEventName = void 0;
    exports.ExceptionEventName = "exception";
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/Span.js
var require_Span = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/Span.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SpanImpl = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var semantic_conventions_1 = (init_esm3(), __toCommonJS(esm_exports2));
    var enums_1 = require_enums();
    var SpanImpl = class {
      static {
        __name(this, "SpanImpl");
      }
      // Below properties are included to implement ReadableSpan for export
      // purposes but are not intended to be written-to directly.
      _spanContext;
      kind;
      parentSpanContext;
      attributes = {};
      links = [];
      events = [];
      startTime;
      resource;
      instrumentationScope;
      _droppedAttributesCount = 0;
      _droppedEventsCount = 0;
      _droppedLinksCount = 0;
      _attributesCount = 0;
      name;
      status = {
        code: api_1.SpanStatusCode.UNSET
      };
      endTime = [0, 0];
      _ended = false;
      _duration = [-1, -1];
      _spanProcessor;
      _spanLimits;
      _attributeValueLengthLimit;
      _recordEndMetrics;
      _performanceStartTime;
      _performanceOffset;
      _startTimeProvided;
      /**
       * Constructs a new SpanImpl instance.
       */
      constructor(opts) {
        const now = Date.now();
        this._spanContext = opts.spanContext;
        this._performanceStartTime = core_1.otperformance.now();
        this._performanceOffset = now - (this._performanceStartTime + core_1.otperformance.timeOrigin);
        this._startTimeProvided = opts.startTime != null;
        this._spanLimits = opts.spanLimits;
        this._attributeValueLengthLimit = this._spanLimits.attributeValueLengthLimit ?? 0;
        this._spanProcessor = opts.spanProcessor;
        this.name = opts.name;
        this.parentSpanContext = opts.parentSpanContext;
        this.kind = opts.kind;
        if (opts.links) {
          for (const link of opts.links) {
            this.addLink(link);
          }
        }
        this.startTime = this._getTime(opts.startTime ?? now);
        this.resource = opts.resource;
        this.instrumentationScope = opts.scope;
        this._recordEndMetrics = opts.recordEndMetrics;
        if (opts.attributes != null) {
          this.setAttributes(opts.attributes);
        }
        this._spanProcessor.onStart(this, opts.context);
      }
      spanContext() {
        return this._spanContext;
      }
      setAttribute(key, value2) {
        if (value2 == null || this._isSpanEnded())
          return this;
        if (key.length === 0) {
          api_1.diag.warn(`Invalid attribute key: ${key}`);
          return this;
        }
        if (!(0, core_1.isAttributeValue)(value2)) {
          api_1.diag.warn(`Invalid attribute value set for key: ${key}`);
          return this;
        }
        const { attributeCountLimit } = this._spanLimits;
        const isNewKey = !Object.prototype.hasOwnProperty.call(this.attributes, key);
        if (attributeCountLimit !== void 0 && this._attributesCount >= attributeCountLimit && isNewKey) {
          this._droppedAttributesCount++;
          return this;
        }
        this.attributes[key] = this._truncateToSize(value2);
        if (isNewKey) {
          this._attributesCount++;
        }
        return this;
      }
      setAttributes(attributes) {
        for (const key in attributes) {
          if (Object.prototype.hasOwnProperty.call(attributes, key)) {
            this.setAttribute(key, attributes[key]);
          }
        }
        return this;
      }
      /**
       *
       * @param name Span Name
       * @param [attributesOrStartTime] Span attributes or start time
       *     if type is {@type TimeInput} and 3rd param is undefined
       * @param [timeStamp] Specified time stamp for the event
       */
      addEvent(name, attributesOrStartTime, timeStamp) {
        if (this._isSpanEnded())
          return this;
        const { eventCountLimit } = this._spanLimits;
        if (eventCountLimit === 0) {
          api_1.diag.warn("No events allowed.");
          this._droppedEventsCount++;
          return this;
        }
        if (eventCountLimit !== void 0 && this.events.length >= eventCountLimit) {
          if (this._droppedEventsCount === 0) {
            api_1.diag.debug("Dropping extra events.");
          }
          this.events.shift();
          this._droppedEventsCount++;
        }
        if ((0, core_1.isTimeInput)(attributesOrStartTime)) {
          if (!(0, core_1.isTimeInput)(timeStamp)) {
            timeStamp = attributesOrStartTime;
          }
          attributesOrStartTime = void 0;
        }
        const sanitized = (0, core_1.sanitizeAttributes)(attributesOrStartTime);
        const { attributePerEventCountLimit } = this._spanLimits;
        const attributes = {};
        let droppedAttributesCount = 0;
        let eventAttributesCount = 0;
        for (const attr in sanitized) {
          if (!Object.prototype.hasOwnProperty.call(sanitized, attr)) {
            continue;
          }
          const attrVal = sanitized[attr];
          if (attributePerEventCountLimit !== void 0 && eventAttributesCount >= attributePerEventCountLimit) {
            droppedAttributesCount++;
            continue;
          }
          attributes[attr] = this._truncateToSize(attrVal);
          eventAttributesCount++;
        }
        this.events.push({
          name,
          attributes,
          time: this._getTime(timeStamp),
          droppedAttributesCount
        });
        return this;
      }
      addLink(link) {
        if (this._isSpanEnded())
          return this;
        const { linkCountLimit } = this._spanLimits;
        if (linkCountLimit === 0) {
          this._droppedLinksCount++;
          return this;
        }
        if (linkCountLimit !== void 0 && this.links.length >= linkCountLimit) {
          if (this._droppedLinksCount === 0) {
            api_1.diag.debug("Dropping extra links.");
          }
          this.links.shift();
          this._droppedLinksCount++;
        }
        const { attributePerLinkCountLimit } = this._spanLimits;
        const sanitized = (0, core_1.sanitizeAttributes)(link.attributes);
        const attributes = {};
        let droppedAttributesCount = 0;
        let linkAttributesCount = 0;
        for (const attr in sanitized) {
          if (!Object.prototype.hasOwnProperty.call(sanitized, attr)) {
            continue;
          }
          const attrVal = sanitized[attr];
          if (attributePerLinkCountLimit !== void 0 && linkAttributesCount >= attributePerLinkCountLimit) {
            droppedAttributesCount++;
            continue;
          }
          attributes[attr] = this._truncateToSize(attrVal);
          linkAttributesCount++;
        }
        const processedLink = { context: link.context };
        if (linkAttributesCount > 0) {
          processedLink.attributes = attributes;
        }
        if (droppedAttributesCount > 0) {
          processedLink.droppedAttributesCount = droppedAttributesCount;
        }
        this.links.push(processedLink);
        return this;
      }
      addLinks(links) {
        for (const link of links) {
          this.addLink(link);
        }
        return this;
      }
      setStatus(status) {
        if (this._isSpanEnded())
          return this;
        if (status.code === api_1.SpanStatusCode.UNSET)
          return this;
        if (this.status.code === api_1.SpanStatusCode.OK)
          return this;
        const newStatus = { code: status.code };
        if (status.code === api_1.SpanStatusCode.ERROR) {
          if (typeof status.message === "string") {
            newStatus.message = status.message;
          } else if (status.message != null) {
            api_1.diag.warn(`Dropping invalid status.message of type '${typeof status.message}', expected 'string'`);
          }
        }
        this.status = newStatus;
        return this;
      }
      updateName(name) {
        if (this._isSpanEnded())
          return this;
        this.name = name;
        return this;
      }
      end(endTime) {
        if (this._isSpanEnded()) {
          api_1.diag.error(`${this.name} ${this._spanContext.traceId}-${this._spanContext.spanId} - You can only call end() on a span once.`);
          return;
        }
        this.endTime = this._getTime(endTime);
        this._duration = (0, core_1.hrTimeDuration)(this.startTime, this.endTime);
        if (this._duration[0] < 0) {
          api_1.diag.warn("Inconsistent start and end time, startTime > endTime. Setting span duration to 0ms.", this.startTime, this.endTime);
          this.endTime = this.startTime.slice();
          this._duration = [0, 0];
        }
        if (this._droppedEventsCount > 0) {
          api_1.diag.warn(`Dropped ${this._droppedEventsCount} events because eventCountLimit reached`);
        }
        if (this._droppedLinksCount > 0) {
          api_1.diag.warn(`Dropped ${this._droppedLinksCount} links because linkCountLimit reached`);
        }
        if (this._spanProcessor.onEnding) {
          this._spanProcessor.onEnding(this);
        }
        this._recordEndMetrics?.();
        this._ended = true;
        this._spanProcessor.onEnd(this);
      }
      _getTime(inp) {
        if (typeof inp === "number" && inp <= core_1.otperformance.now()) {
          return (0, core_1.hrTime)(inp + this._performanceOffset);
        }
        if (typeof inp === "number") {
          return (0, core_1.millisToHrTime)(inp);
        }
        if (inp instanceof Date) {
          return (0, core_1.millisToHrTime)(inp.getTime());
        }
        if ((0, core_1.isTimeInputHrTime)(inp)) {
          return inp;
        }
        if (this._startTimeProvided) {
          return (0, core_1.millisToHrTime)(Date.now());
        }
        const msDuration = core_1.otperformance.now() - this._performanceStartTime;
        return (0, core_1.addHrTimes)(this.startTime, (0, core_1.millisToHrTime)(msDuration));
      }
      isRecording() {
        return this._ended === false;
      }
      recordException(exception, time) {
        const attributes = {};
        if (typeof exception === "string") {
          attributes[semantic_conventions_1.ATTR_EXCEPTION_MESSAGE] = exception;
        } else if (exception) {
          if (exception.code) {
            attributes[semantic_conventions_1.ATTR_EXCEPTION_TYPE] = exception.code.toString();
          } else if (exception.name) {
            attributes[semantic_conventions_1.ATTR_EXCEPTION_TYPE] = exception.name;
          }
          if (exception.message) {
            attributes[semantic_conventions_1.ATTR_EXCEPTION_MESSAGE] = exception.message;
          }
          if (exception.stack) {
            attributes[semantic_conventions_1.ATTR_EXCEPTION_STACKTRACE] = exception.stack;
          }
        }
        if (attributes[semantic_conventions_1.ATTR_EXCEPTION_TYPE] || attributes[semantic_conventions_1.ATTR_EXCEPTION_MESSAGE]) {
          this.addEvent(enums_1.ExceptionEventName, attributes, time);
        } else {
          api_1.diag.warn(`Failed to record an exception ${exception}`);
        }
      }
      get duration() {
        return this._duration;
      }
      get ended() {
        return this._ended;
      }
      get droppedAttributesCount() {
        return this._droppedAttributesCount;
      }
      get droppedEventsCount() {
        return this._droppedEventsCount;
      }
      get droppedLinksCount() {
        return this._droppedLinksCount;
      }
      _isSpanEnded() {
        if (this._ended) {
          const error = new Error(`Operation attempted on ended Span {traceId: ${this._spanContext.traceId}, spanId: ${this._spanContext.spanId}}`);
          api_1.diag.warn(`Cannot execute the operation on ended Span {traceId: ${this._spanContext.traceId}, spanId: ${this._spanContext.spanId}}`, error);
        }
        return this._ended;
      }
      // Utility function to truncate given value within size
      // for value type of string, will truncate to given limit
      // for type of non-string, will return same value
      _truncateToLimitUtil(value2, limit) {
        if (value2.length <= limit) {
          return value2;
        }
        return value2.substring(0, limit);
      }
      /**
       * If the given attribute value is of type string and has more characters than given {@code attributeValueLengthLimit} then
       * return string with truncated to {@code attributeValueLengthLimit} characters
       *
       * If the given attribute value is array of strings then
       * return new array of strings with each element truncated to {@code attributeValueLengthLimit} characters
       *
       * Otherwise return same Attribute {@code value}
       *
       * @param value Attribute value
       * @returns truncated attribute value if required, otherwise same value
       */
      _truncateToSize(value2) {
        const limit = this._attributeValueLengthLimit;
        if (limit <= 0) {
          api_1.diag.warn(`Attribute value limit must be positive, got ${limit}`);
          return value2;
        }
        if (typeof value2 === "string") {
          return this._truncateToLimitUtil(value2, limit);
        }
        if (Array.isArray(value2)) {
          return value2.map((val) => typeof val === "string" ? this._truncateToLimitUtil(val, limit) : val);
        }
        return value2;
      }
    };
    exports.SpanImpl = SpanImpl;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/Sampler.js
var require_Sampler = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/Sampler.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SamplingDecision = void 0;
    var SamplingDecision;
    (function(SamplingDecision2) {
      SamplingDecision2[SamplingDecision2["NOT_RECORD"] = 0] = "NOT_RECORD";
      SamplingDecision2[SamplingDecision2["RECORD"] = 1] = "RECORD";
      SamplingDecision2[SamplingDecision2["RECORD_AND_SAMPLED"] = 2] = "RECORD_AND_SAMPLED";
    })(SamplingDecision = exports.SamplingDecision || (exports.SamplingDecision = {}));
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/AlwaysOffSampler.js
var require_AlwaysOffSampler = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/AlwaysOffSampler.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AlwaysOffSampler = void 0;
    var Sampler_1 = require_Sampler();
    var AlwaysOffSampler = class {
      static {
        __name(this, "AlwaysOffSampler");
      }
      shouldSample() {
        return {
          decision: Sampler_1.SamplingDecision.NOT_RECORD
        };
      }
      toString() {
        return "AlwaysOffSampler";
      }
    };
    exports.AlwaysOffSampler = AlwaysOffSampler;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/AlwaysOnSampler.js
var require_AlwaysOnSampler = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/AlwaysOnSampler.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AlwaysOnSampler = void 0;
    var Sampler_1 = require_Sampler();
    var AlwaysOnSampler = class {
      static {
        __name(this, "AlwaysOnSampler");
      }
      shouldSample() {
        return {
          decision: Sampler_1.SamplingDecision.RECORD_AND_SAMPLED
        };
      }
      toString() {
        return "AlwaysOnSampler";
      }
    };
    exports.AlwaysOnSampler = AlwaysOnSampler;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/ParentBasedSampler.js
var require_ParentBasedSampler = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/ParentBasedSampler.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ParentBasedSampler = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var AlwaysOffSampler_1 = require_AlwaysOffSampler();
    var AlwaysOnSampler_1 = require_AlwaysOnSampler();
    var ParentBasedSampler = class {
      static {
        __name(this, "ParentBasedSampler");
      }
      _root;
      _remoteParentSampled;
      _remoteParentNotSampled;
      _localParentSampled;
      _localParentNotSampled;
      constructor(config) {
        this._root = config.root;
        if (!this._root) {
          (0, core_1.globalErrorHandler)(new Error("ParentBasedSampler must have a root sampler configured"));
          this._root = new AlwaysOnSampler_1.AlwaysOnSampler();
        }
        this._remoteParentSampled = config.remoteParentSampled ?? new AlwaysOnSampler_1.AlwaysOnSampler();
        this._remoteParentNotSampled = config.remoteParentNotSampled ?? new AlwaysOffSampler_1.AlwaysOffSampler();
        this._localParentSampled = config.localParentSampled ?? new AlwaysOnSampler_1.AlwaysOnSampler();
        this._localParentNotSampled = config.localParentNotSampled ?? new AlwaysOffSampler_1.AlwaysOffSampler();
      }
      shouldSample(context2, traceId, spanName, spanKind, attributes, links) {
        const parentContext = api_1.trace.getSpanContext(context2);
        if (!parentContext || !(0, api_1.isSpanContextValid)(parentContext)) {
          return this._root.shouldSample(context2, traceId, spanName, spanKind, attributes, links);
        }
        if (parentContext.isRemote) {
          if (parentContext.traceFlags & api_1.TraceFlags.SAMPLED) {
            return this._remoteParentSampled.shouldSample(context2, traceId, spanName, spanKind, attributes, links);
          }
          return this._remoteParentNotSampled.shouldSample(context2, traceId, spanName, spanKind, attributes, links);
        }
        if (parentContext.traceFlags & api_1.TraceFlags.SAMPLED) {
          return this._localParentSampled.shouldSample(context2, traceId, spanName, spanKind, attributes, links);
        }
        return this._localParentNotSampled.shouldSample(context2, traceId, spanName, spanKind, attributes, links);
      }
      toString() {
        return `ParentBased{root=${this._root.toString()}, remoteParentSampled=${this._remoteParentSampled.toString()}, remoteParentNotSampled=${this._remoteParentNotSampled.toString()}, localParentSampled=${this._localParentSampled.toString()}, localParentNotSampled=${this._localParentNotSampled.toString()}}`;
      }
    };
    exports.ParentBasedSampler = ParentBasedSampler;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/TraceIdRatioBasedSampler.js
var require_TraceIdRatioBasedSampler = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/sampler/TraceIdRatioBasedSampler.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TraceIdRatioBasedSampler = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var Sampler_1 = require_Sampler();
    var TraceIdRatioBasedSampler = class {
      static {
        __name(this, "TraceIdRatioBasedSampler");
      }
      _ratio;
      _upperBound;
      constructor(ratio = 0) {
        this._ratio = this._normalize(ratio);
        this._upperBound = Math.floor(this._ratio * 4294967295);
      }
      shouldSample(context2, traceId) {
        return {
          decision: (0, api_1.isValidTraceId)(traceId) && this._accumulate(traceId) < this._upperBound ? Sampler_1.SamplingDecision.RECORD_AND_SAMPLED : Sampler_1.SamplingDecision.NOT_RECORD
        };
      }
      toString() {
        return `TraceIdRatioBased{${this._ratio}}`;
      }
      _normalize(ratio) {
        if (typeof ratio !== "number" || isNaN(ratio))
          return 0;
        return ratio >= 1 ? 1 : ratio <= 0 ? 0 : ratio;
      }
      _accumulate(traceId) {
        let accumulation = 0;
        for (let i = 0; i < 32; i += 8) {
          let part = 0;
          for (let j = 0; j < 8; j++) {
            const c = traceId.charCodeAt(i + j);
            const v = c < 58 ? c - 48 : c < 71 ? c - 55 : c - 87;
            part = part << 4 | v;
          }
          accumulation = (accumulation ^ part) >>> 0;
        }
        return accumulation;
      }
    };
    exports.TraceIdRatioBasedSampler = TraceIdRatioBasedSampler;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/config.js
var require_config = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/config.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.buildSamplerFromEnv = exports.loadDefaultConfig = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var AlwaysOffSampler_1 = require_AlwaysOffSampler();
    var AlwaysOnSampler_1 = require_AlwaysOnSampler();
    var ParentBasedSampler_1 = require_ParentBasedSampler();
    var TraceIdRatioBasedSampler_1 = require_TraceIdRatioBasedSampler();
    var TracesSamplerValues;
    (function(TracesSamplerValues2) {
      TracesSamplerValues2["AlwaysOff"] = "always_off";
      TracesSamplerValues2["AlwaysOn"] = "always_on";
      TracesSamplerValues2["ParentBasedAlwaysOff"] = "parentbased_always_off";
      TracesSamplerValues2["ParentBasedAlwaysOn"] = "parentbased_always_on";
      TracesSamplerValues2["ParentBasedTraceIdRatio"] = "parentbased_traceidratio";
      TracesSamplerValues2["TraceIdRatio"] = "traceidratio";
    })(TracesSamplerValues || (TracesSamplerValues = {}));
    var DEFAULT_RATIO = 1;
    function loadDefaultConfig() {
      return {
        sampler: buildSamplerFromEnv(),
        forceFlushTimeoutMillis: 3e4,
        generalLimits: {
          attributeValueLengthLimit: (0, core_1.getNumberFromEnv)("OTEL_ATTRIBUTE_VALUE_LENGTH_LIMIT") ?? Infinity,
          attributeCountLimit: (0, core_1.getNumberFromEnv)("OTEL_ATTRIBUTE_COUNT_LIMIT") ?? 128
        },
        spanLimits: {
          attributeValueLengthLimit: (0, core_1.getNumberFromEnv)("OTEL_SPAN_ATTRIBUTE_VALUE_LENGTH_LIMIT") ?? Infinity,
          attributeCountLimit: (0, core_1.getNumberFromEnv)("OTEL_SPAN_ATTRIBUTE_COUNT_LIMIT") ?? 128,
          linkCountLimit: (0, core_1.getNumberFromEnv)("OTEL_SPAN_LINK_COUNT_LIMIT") ?? 128,
          eventCountLimit: (0, core_1.getNumberFromEnv)("OTEL_SPAN_EVENT_COUNT_LIMIT") ?? 128,
          attributePerEventCountLimit: (0, core_1.getNumberFromEnv)("OTEL_SPAN_ATTRIBUTE_PER_EVENT_COUNT_LIMIT") ?? 128,
          attributePerLinkCountLimit: (0, core_1.getNumberFromEnv)("OTEL_SPAN_ATTRIBUTE_PER_LINK_COUNT_LIMIT") ?? 128
        }
      };
    }
    __name(loadDefaultConfig, "loadDefaultConfig");
    exports.loadDefaultConfig = loadDefaultConfig;
    function buildSamplerFromEnv() {
      const sampler = (0, core_1.getStringFromEnv)("OTEL_TRACES_SAMPLER") ?? TracesSamplerValues.ParentBasedAlwaysOn;
      switch (sampler) {
        case TracesSamplerValues.AlwaysOn:
          return new AlwaysOnSampler_1.AlwaysOnSampler();
        case TracesSamplerValues.AlwaysOff:
          return new AlwaysOffSampler_1.AlwaysOffSampler();
        case TracesSamplerValues.ParentBasedAlwaysOn:
          return new ParentBasedSampler_1.ParentBasedSampler({
            root: new AlwaysOnSampler_1.AlwaysOnSampler()
          });
        case TracesSamplerValues.ParentBasedAlwaysOff:
          return new ParentBasedSampler_1.ParentBasedSampler({
            root: new AlwaysOffSampler_1.AlwaysOffSampler()
          });
        case TracesSamplerValues.TraceIdRatio:
          return new TraceIdRatioBasedSampler_1.TraceIdRatioBasedSampler(getSamplerProbabilityFromEnv());
        case TracesSamplerValues.ParentBasedTraceIdRatio:
          return new ParentBasedSampler_1.ParentBasedSampler({
            root: new TraceIdRatioBasedSampler_1.TraceIdRatioBasedSampler(getSamplerProbabilityFromEnv())
          });
        default:
          api_1.diag.error(`OTEL_TRACES_SAMPLER value "${sampler}" invalid, defaulting to "${TracesSamplerValues.ParentBasedAlwaysOn}".`);
          return new ParentBasedSampler_1.ParentBasedSampler({
            root: new AlwaysOnSampler_1.AlwaysOnSampler()
          });
      }
    }
    __name(buildSamplerFromEnv, "buildSamplerFromEnv");
    exports.buildSamplerFromEnv = buildSamplerFromEnv;
    function getSamplerProbabilityFromEnv() {
      const probability = (0, core_1.getNumberFromEnv)("OTEL_TRACES_SAMPLER_ARG");
      if (probability == null) {
        api_1.diag.error(`OTEL_TRACES_SAMPLER_ARG is blank, defaulting to ${DEFAULT_RATIO}.`);
        return DEFAULT_RATIO;
      }
      if (probability < 0 || probability > 1) {
        api_1.diag.error(`OTEL_TRACES_SAMPLER_ARG=${probability} was given, but it is out of range ([0..1]), defaulting to ${DEFAULT_RATIO}.`);
        return DEFAULT_RATIO;
      }
      return probability;
    }
    __name(getSamplerProbabilityFromEnv, "getSamplerProbabilityFromEnv");
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/utility.js
var require_utility = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/utility.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.reconfigureLimits = exports.mergeConfig = exports.DEFAULT_ATTRIBUTE_VALUE_LENGTH_LIMIT = exports.DEFAULT_ATTRIBUTE_COUNT_LIMIT = void 0;
    var config_1 = require_config();
    var core_1 = require_src();
    exports.DEFAULT_ATTRIBUTE_COUNT_LIMIT = 128;
    exports.DEFAULT_ATTRIBUTE_VALUE_LENGTH_LIMIT = Infinity;
    function mergeConfig(userConfig) {
      const perInstanceDefaults = {
        sampler: (0, config_1.buildSamplerFromEnv)()
      };
      const DEFAULT_CONFIG = (0, config_1.loadDefaultConfig)();
      const target = Object.assign({}, DEFAULT_CONFIG, perInstanceDefaults, userConfig);
      target.generalLimits = Object.assign({}, DEFAULT_CONFIG.generalLimits, userConfig.generalLimits || {});
      target.spanLimits = Object.assign({}, DEFAULT_CONFIG.spanLimits, userConfig.spanLimits || {});
      return target;
    }
    __name(mergeConfig, "mergeConfig");
    exports.mergeConfig = mergeConfig;
    function reconfigureLimits(userConfig) {
      const spanLimits = Object.assign({}, userConfig.spanLimits);
      spanLimits.attributeCountLimit = userConfig.spanLimits?.attributeCountLimit ?? userConfig.generalLimits?.attributeCountLimit ?? (0, core_1.getNumberFromEnv)("OTEL_SPAN_ATTRIBUTE_COUNT_LIMIT") ?? (0, core_1.getNumberFromEnv)("OTEL_ATTRIBUTE_COUNT_LIMIT") ?? exports.DEFAULT_ATTRIBUTE_COUNT_LIMIT;
      spanLimits.attributeValueLengthLimit = userConfig.spanLimits?.attributeValueLengthLimit ?? userConfig.generalLimits?.attributeValueLengthLimit ?? (0, core_1.getNumberFromEnv)("OTEL_SPAN_ATTRIBUTE_VALUE_LENGTH_LIMIT") ?? (0, core_1.getNumberFromEnv)("OTEL_ATTRIBUTE_VALUE_LENGTH_LIMIT") ?? exports.DEFAULT_ATTRIBUTE_VALUE_LENGTH_LIMIT;
      return Object.assign({}, userConfig, { spanLimits });
    }
    __name(reconfigureLimits, "reconfigureLimits");
    exports.reconfigureLimits = reconfigureLimits;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/BatchSpanProcessorBase.js
var require_BatchSpanProcessorBase = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/BatchSpanProcessorBase.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BatchSpanProcessorBase = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var BatchSpanProcessorBase = class {
      static {
        __name(this, "BatchSpanProcessorBase");
      }
      _maxExportBatchSize;
      _maxQueueSize;
      _scheduledDelayMillis;
      _exportTimeoutMillis;
      _exporter;
      _isExporting = false;
      _finishedSpans = [];
      _timer;
      _shutdownOnce;
      _droppedSpansCount = 0;
      constructor(exporter, config) {
        this._exporter = exporter;
        this._maxExportBatchSize = typeof config?.maxExportBatchSize === "number" ? config.maxExportBatchSize : (0, core_1.getNumberFromEnv)("OTEL_BSP_MAX_EXPORT_BATCH_SIZE") ?? 512;
        this._maxQueueSize = typeof config?.maxQueueSize === "number" ? config.maxQueueSize : (0, core_1.getNumberFromEnv)("OTEL_BSP_MAX_QUEUE_SIZE") ?? 2048;
        this._scheduledDelayMillis = typeof config?.scheduledDelayMillis === "number" ? config.scheduledDelayMillis : (0, core_1.getNumberFromEnv)("OTEL_BSP_SCHEDULE_DELAY") ?? 5e3;
        this._exportTimeoutMillis = typeof config?.exportTimeoutMillis === "number" ? config.exportTimeoutMillis : (0, core_1.getNumberFromEnv)("OTEL_BSP_EXPORT_TIMEOUT") ?? 3e4;
        this._shutdownOnce = new core_1.BindOnceFuture(this._shutdown, this);
        if (this._maxExportBatchSize > this._maxQueueSize) {
          api_1.diag.warn("BatchSpanProcessor: maxExportBatchSize must be smaller or equal to maxQueueSize, setting maxExportBatchSize to match maxQueueSize");
          this._maxExportBatchSize = this._maxQueueSize;
        }
      }
      forceFlush() {
        if (this._shutdownOnce.isCalled) {
          return this._shutdownOnce.promise;
        }
        return this._flushAll();
      }
      // does nothing.
      onStart(_span, _parentContext) {
      }
      onEnd(span) {
        if (this._shutdownOnce.isCalled) {
          return;
        }
        if ((span.spanContext().traceFlags & api_1.TraceFlags.SAMPLED) === 0) {
          return;
        }
        this._addToBuffer(span);
      }
      shutdown() {
        return this._shutdownOnce.call();
      }
      _shutdown() {
        return Promise.resolve().then(() => {
          return this.onShutdown();
        }).then(() => {
          return this._flushAll();
        }).then(() => {
          return this._exporter.shutdown();
        });
      }
      /** Add a span in the buffer. */
      _addToBuffer(span) {
        if (this._finishedSpans.length >= this._maxQueueSize) {
          if (this._droppedSpansCount === 0) {
            api_1.diag.debug("maxQueueSize reached, dropping spans");
          }
          this._droppedSpansCount++;
          return;
        }
        if (this._droppedSpansCount > 0) {
          api_1.diag.warn(`Dropped ${this._droppedSpansCount} spans because maxQueueSize reached`);
          this._droppedSpansCount = 0;
        }
        this._finishedSpans.push(span);
        this._maybeStartTimer();
      }
      /**
       * Send all spans to the exporter respecting the batch size limit
       * This function is used only on forceFlush or shutdown,
       * for all other cases _flush should be used
       * */
      _flushAll() {
        return new Promise((resolve2, reject) => {
          const promises = [];
          const count = Math.ceil(this._finishedSpans.length / this._maxExportBatchSize);
          for (let i = 0, j = count; i < j; i++) {
            promises.push(this._flushOneBatch());
          }
          Promise.all(promises).then(() => {
            resolve2();
          }).catch(reject);
        });
      }
      _flushOneBatch() {
        this._clearTimer();
        if (this._finishedSpans.length === 0) {
          return Promise.resolve();
        }
        return new Promise((resolve2, reject) => {
          const timer = setTimeout(() => {
            reject(new Error("Timeout"));
          }, this._exportTimeoutMillis);
          api_1.context.with((0, core_1.suppressTracing)(api_1.context.active()), () => {
            let spans;
            if (this._finishedSpans.length <= this._maxExportBatchSize) {
              spans = this._finishedSpans;
              this._finishedSpans = [];
            } else {
              spans = this._finishedSpans.splice(0, this._maxExportBatchSize);
            }
            const doExport = /* @__PURE__ */ __name(() => this._exporter.export(spans, (result) => {
              clearTimeout(timer);
              if (result.code === core_1.ExportResultCode.SUCCESS) {
                resolve2();
              } else {
                reject(result.error ?? new Error("BatchSpanProcessor: span export failed"));
              }
            }), "doExport");
            let pendingResources = null;
            for (let i = 0, len = spans.length; i < len; i++) {
              const span = spans[i];
              if (span.resource.asyncAttributesPending && span.resource.waitForAsyncAttributes) {
                pendingResources ??= [];
                pendingResources.push(span.resource.waitForAsyncAttributes());
              }
            }
            if (pendingResources === null) {
              doExport();
            } else {
              Promise.all(pendingResources).then(doExport, (err2) => {
                (0, core_1.globalErrorHandler)(err2);
                reject(err2);
              });
            }
          });
        });
      }
      _maybeStartTimer() {
        if (this._isExporting)
          return;
        const flush = /* @__PURE__ */ __name(() => {
          this._isExporting = true;
          this._flushOneBatch().finally(() => {
            this._isExporting = false;
            if (this._finishedSpans.length > 0) {
              this._clearTimer();
              this._maybeStartTimer();
            }
          }).catch((e) => {
            this._isExporting = false;
            (0, core_1.globalErrorHandler)(e);
          });
        }, "flush");
        if (this._finishedSpans.length >= this._maxExportBatchSize) {
          return flush();
        }
        if (this._timer !== void 0)
          return;
        this._timer = setTimeout(() => flush(), this._scheduledDelayMillis);
        if (typeof this._timer !== "number") {
          this._timer.unref();
        }
      }
      _clearTimer() {
        if (this._timer !== void 0) {
          clearTimeout(this._timer);
          this._timer = void 0;
        }
      }
    };
    exports.BatchSpanProcessorBase = BatchSpanProcessorBase;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/node/export/BatchSpanProcessor.js
var require_BatchSpanProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/node/export/BatchSpanProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BatchSpanProcessor = void 0;
    var BatchSpanProcessorBase_1 = require_BatchSpanProcessorBase();
    var BatchSpanProcessor2 = class extends BatchSpanProcessorBase_1.BatchSpanProcessorBase {
      static {
        __name(this, "BatchSpanProcessor");
      }
      onShutdown() {
      }
    };
    exports.BatchSpanProcessor = BatchSpanProcessor2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/node/RandomIdGenerator.js
var require_RandomIdGenerator = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/node/RandomIdGenerator.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RandomIdGenerator = void 0;
    var SPAN_ID_BYTES = 8;
    var TRACE_ID_BYTES = 16;
    var RandomIdGenerator2 = class {
      static {
        __name(this, "RandomIdGenerator");
      }
      /**
       * Returns a random 16-byte trace ID formatted/encoded as a 32 lowercase hex
       * characters corresponding to 128 bits.
       */
      generateTraceId = getIdGenerator(TRACE_ID_BYTES);
      /**
       * Returns a random 8-byte span ID formatted/encoded as a 16 lowercase hex
       * characters corresponding to 64 bits.
       */
      generateSpanId = getIdGenerator(SPAN_ID_BYTES);
    };
    exports.RandomIdGenerator = RandomIdGenerator2;
    var SHARED_BUFFER = Buffer.allocUnsafe(TRACE_ID_BYTES);
    function getIdGenerator(bytes) {
      return /* @__PURE__ */ __name(function generateId() {
        for (let i = 0; i < bytes / 4; i++) {
          SHARED_BUFFER.writeUInt32BE(Math.random() * 2 ** 32 >>> 0, i * 4);
        }
        for (let i = 0; i < bytes; i++) {
          if (SHARED_BUFFER[i] > 0) {
            break;
          } else if (i === bytes - 1) {
            SHARED_BUFFER[bytes - 1] = 1;
          }
        }
        return SHARED_BUFFER.toString("hex", 0, bytes);
      }, "generateId");
    }
    __name(getIdGenerator, "getIdGenerator");
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/node/index.js
var require_node7 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/node/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RandomIdGenerator = exports.BatchSpanProcessor = void 0;
    var BatchSpanProcessor_1 = require_BatchSpanProcessor();
    Object.defineProperty(exports, "BatchSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return BatchSpanProcessor_1.BatchSpanProcessor;
    }, "get") });
    var RandomIdGenerator_1 = require_RandomIdGenerator();
    Object.defineProperty(exports, "RandomIdGenerator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return RandomIdGenerator_1.RandomIdGenerator;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/index.js
var require_platform7 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/platform/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RandomIdGenerator = exports.BatchSpanProcessor = void 0;
    var node_1 = require_node7();
    Object.defineProperty(exports, "BatchSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.BatchSpanProcessor;
    }, "get") });
    Object.defineProperty(exports, "RandomIdGenerator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return node_1.RandomIdGenerator;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/semconv.js
var require_semconv4 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/semconv.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.METRIC_OTEL_SDK_SPAN_STARTED = exports.METRIC_OTEL_SDK_SPAN_LIVE = exports.ATTR_OTEL_SPAN_SAMPLING_RESULT = exports.ATTR_OTEL_SPAN_PARENT_ORIGIN = void 0;
    exports.ATTR_OTEL_SPAN_PARENT_ORIGIN = "otel.span.parent.origin";
    exports.ATTR_OTEL_SPAN_SAMPLING_RESULT = "otel.span.sampling_result";
    exports.METRIC_OTEL_SDK_SPAN_LIVE = "otel.sdk.span.live";
    exports.METRIC_OTEL_SDK_SPAN_STARTED = "otel.sdk.span.started";
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/TracerMetrics.js
var require_TracerMetrics = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/TracerMetrics.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TracerMetrics = void 0;
    var Sampler_1 = require_Sampler();
    var semconv_1 = require_semconv4();
    var TracerMetrics = class {
      static {
        __name(this, "TracerMetrics");
      }
      startedSpans;
      liveSpans;
      constructor(meter) {
        this.startedSpans = meter.createCounter(semconv_1.METRIC_OTEL_SDK_SPAN_STARTED, {
          unit: "{span}",
          description: "The number of created spans."
        });
        this.liveSpans = meter.createUpDownCounter(semconv_1.METRIC_OTEL_SDK_SPAN_LIVE, {
          unit: "{span}",
          description: "The number of currently live spans."
        });
      }
      startSpan(parentSpanCtx, samplingDecision) {
        const samplingDecisionStr = samplingDecisionToString(samplingDecision);
        this.startedSpans.add(1, {
          [semconv_1.ATTR_OTEL_SPAN_PARENT_ORIGIN]: parentOrigin(parentSpanCtx),
          [semconv_1.ATTR_OTEL_SPAN_SAMPLING_RESULT]: samplingDecisionStr
        });
        if (samplingDecision === Sampler_1.SamplingDecision.NOT_RECORD) {
          return () => {
          };
        }
        const liveSpanAttributes = {
          [semconv_1.ATTR_OTEL_SPAN_SAMPLING_RESULT]: samplingDecisionStr
        };
        this.liveSpans.add(1, liveSpanAttributes);
        return () => {
          this.liveSpans.add(-1, liveSpanAttributes);
        };
      }
    };
    exports.TracerMetrics = TracerMetrics;
    function parentOrigin(parentSpanContext) {
      if (!parentSpanContext) {
        return "none";
      }
      if (parentSpanContext.isRemote) {
        return "remote";
      }
      return "local";
    }
    __name(parentOrigin, "parentOrigin");
    function samplingDecisionToString(decision) {
      switch (decision) {
        case Sampler_1.SamplingDecision.RECORD_AND_SAMPLED:
          return "RECORD_AND_SAMPLE";
        case Sampler_1.SamplingDecision.RECORD:
          return "RECORD_ONLY";
        case Sampler_1.SamplingDecision.NOT_RECORD:
          return "DROP";
      }
    }
    __name(samplingDecisionToString, "samplingDecisionToString");
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/version.js
var require_version3 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/version.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VERSION = void 0;
    exports.VERSION = "2.7.1";
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/Tracer.js
var require_Tracer = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/Tracer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Tracer = void 0;
    var api = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var Span_1 = require_Span();
    var utility_1 = require_utility();
    var platform_1 = require_platform7();
    var TracerMetrics_1 = require_TracerMetrics();
    var version_1 = require_version3();
    var Tracer = class {
      static {
        __name(this, "Tracer");
      }
      _sampler;
      _generalLimits;
      _spanLimits;
      _idGenerator;
      instrumentationScope;
      _resource;
      _spanProcessor;
      _tracerMetrics;
      /**
       * Constructs a new Tracer instance.
       */
      constructor(instrumentationScope, config, resource, spanProcessor) {
        const localConfig = (0, utility_1.mergeConfig)(config);
        this._sampler = localConfig.sampler;
        this._generalLimits = localConfig.generalLimits;
        this._spanLimits = localConfig.spanLimits;
        this._idGenerator = config.idGenerator || new platform_1.RandomIdGenerator();
        this._resource = resource;
        this._spanProcessor = spanProcessor;
        this.instrumentationScope = instrumentationScope;
        const meter = localConfig.meterProvider ? localConfig.meterProvider.getMeter("@opentelemetry/sdk-trace", version_1.VERSION) : api.createNoopMeter();
        this._tracerMetrics = new TracerMetrics_1.TracerMetrics(meter);
      }
      /**
       * Starts a new Span or returns the default NoopSpan based on the sampling
       * decision.
       */
      startSpan(name, options = {}, context2 = api.context.active()) {
        if (options.root) {
          context2 = api.trace.deleteSpan(context2);
        }
        const parentSpan = api.trace.getSpan(context2);
        if ((0, core_1.isTracingSuppressed)(context2)) {
          api.diag.debug("Instrumentation suppressed, returning Noop Span");
          const nonRecordingSpan = api.trace.wrapSpanContext(api.INVALID_SPAN_CONTEXT);
          return nonRecordingSpan;
        }
        const parentSpanContext = parentSpan?.spanContext();
        const spanId = this._idGenerator.generateSpanId();
        let validParentSpanContext;
        let traceId;
        let traceState;
        if (!parentSpanContext || !api.trace.isSpanContextValid(parentSpanContext)) {
          traceId = this._idGenerator.generateTraceId();
        } else {
          traceId = parentSpanContext.traceId;
          traceState = parentSpanContext.traceState;
          validParentSpanContext = parentSpanContext;
        }
        const spanKind = options.kind ?? api.SpanKind.INTERNAL;
        const links = (options.links ?? []).map((link) => {
          return {
            context: link.context,
            attributes: (0, core_1.sanitizeAttributes)(link.attributes)
          };
        });
        const attributes = (0, core_1.sanitizeAttributes)(options.attributes);
        const samplingResult = this._sampler.shouldSample(context2, traceId, name, spanKind, attributes, links);
        const recordEndMetrics = this._tracerMetrics.startSpan(parentSpanContext, samplingResult.decision);
        traceState = samplingResult.traceState ?? traceState;
        const traceFlags = samplingResult.decision === api.SamplingDecision.RECORD_AND_SAMPLED ? api.TraceFlags.SAMPLED : api.TraceFlags.NONE;
        const spanContext = { traceId, spanId, traceFlags, traceState };
        if (samplingResult.decision === api.SamplingDecision.NOT_RECORD) {
          api.diag.debug("Recording is off, propagating context in a non-recording span");
          const nonRecordingSpan = api.trace.wrapSpanContext(spanContext);
          return nonRecordingSpan;
        }
        const initAttributes = (0, core_1.sanitizeAttributes)(Object.assign(attributes, samplingResult.attributes));
        const span = new Span_1.SpanImpl({
          resource: this._resource,
          scope: this.instrumentationScope,
          context: context2,
          spanContext,
          name,
          kind: spanKind,
          links,
          parentSpanContext: validParentSpanContext,
          attributes: initAttributes,
          startTime: options.startTime,
          spanProcessor: this._spanProcessor,
          spanLimits: this._spanLimits,
          recordEndMetrics
        });
        return span;
      }
      startActiveSpan(name, arg2, arg3, arg4) {
        let opts;
        let ctx;
        let fn;
        if (arguments.length < 2) {
          return;
        } else if (arguments.length === 2) {
          fn = arg2;
        } else if (arguments.length === 3) {
          opts = arg2;
          fn = arg3;
        } else {
          opts = arg2;
          ctx = arg3;
          fn = arg4;
        }
        const parentContext = ctx ?? api.context.active();
        const span = this.startSpan(name, opts, parentContext);
        const contextWithSpanSet = api.trace.setSpan(parentContext, span);
        return api.context.with(contextWithSpanSet, fn, void 0, span);
      }
      /** Returns the active {@link GeneralLimits}. */
      getGeneralLimits() {
        return this._generalLimits;
      }
      /** Returns the active {@link SpanLimits}. */
      getSpanLimits() {
        return this._spanLimits;
      }
    };
    exports.Tracer = Tracer;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/MultiSpanProcessor.js
var require_MultiSpanProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/MultiSpanProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MultiSpanProcessor = void 0;
    var core_1 = require_src();
    var MultiSpanProcessor = class {
      static {
        __name(this, "MultiSpanProcessor");
      }
      _spanProcessors;
      constructor(spanProcessors) {
        this._spanProcessors = spanProcessors;
      }
      forceFlush() {
        const promises = [];
        for (const spanProcessor of this._spanProcessors) {
          promises.push(spanProcessor.forceFlush());
        }
        return new Promise((resolve2) => {
          Promise.all(promises).then(() => {
            resolve2();
          }).catch((error) => {
            (0, core_1.globalErrorHandler)(error || new Error("MultiSpanProcessor: forceFlush failed"));
            resolve2();
          });
        });
      }
      onStart(span, context2) {
        for (const spanProcessor of this._spanProcessors) {
          spanProcessor.onStart(span, context2);
        }
      }
      onEnding(span) {
        for (const spanProcessor of this._spanProcessors) {
          if (spanProcessor.onEnding) {
            spanProcessor.onEnding(span);
          }
        }
      }
      onEnd(span) {
        for (const spanProcessor of this._spanProcessors) {
          spanProcessor.onEnd(span);
        }
      }
      shutdown() {
        const promises = [];
        for (const spanProcessor of this._spanProcessors) {
          promises.push(spanProcessor.shutdown());
        }
        return new Promise((resolve2, reject) => {
          Promise.all(promises).then(() => {
            resolve2();
          }, reject);
        });
      }
    };
    exports.MultiSpanProcessor = MultiSpanProcessor;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/BasicTracerProvider.js
var require_BasicTracerProvider = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/BasicTracerProvider.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BasicTracerProvider = exports.ForceFlushState = void 0;
    var core_1 = require_src();
    var resources_1 = require_src5();
    var Tracer_1 = require_Tracer();
    var config_1 = require_config();
    var MultiSpanProcessor_1 = require_MultiSpanProcessor();
    var utility_1 = require_utility();
    var ForceFlushState;
    (function(ForceFlushState2) {
      ForceFlushState2[ForceFlushState2["resolved"] = 0] = "resolved";
      ForceFlushState2[ForceFlushState2["timeout"] = 1] = "timeout";
      ForceFlushState2[ForceFlushState2["error"] = 2] = "error";
      ForceFlushState2[ForceFlushState2["unresolved"] = 3] = "unresolved";
    })(ForceFlushState = exports.ForceFlushState || (exports.ForceFlushState = {}));
    var BasicTracerProvider = class {
      static {
        __name(this, "BasicTracerProvider");
      }
      _config;
      _tracers = /* @__PURE__ */ new Map();
      _resource;
      _activeSpanProcessor;
      constructor(config = {}) {
        const mergedConfig = (0, core_1.merge)({}, (0, config_1.loadDefaultConfig)(), (0, utility_1.reconfigureLimits)(config));
        this._resource = mergedConfig.resource ?? (0, resources_1.defaultResource)();
        this._config = Object.assign({}, mergedConfig, {
          resource: this._resource
        });
        const spanProcessors = [];
        if (config.spanProcessors?.length) {
          spanProcessors.push(...config.spanProcessors);
        }
        this._activeSpanProcessor = new MultiSpanProcessor_1.MultiSpanProcessor(spanProcessors);
      }
      getTracer(name, version, options) {
        const key = `${name}@${version || ""}:${options?.schemaUrl || ""}`;
        if (!this._tracers.has(key)) {
          this._tracers.set(key, new Tracer_1.Tracer({ name, version, schemaUrl: options?.schemaUrl }, this._config, this._resource, this._activeSpanProcessor));
        }
        return this._tracers.get(key);
      }
      forceFlush() {
        const timeout = this._config.forceFlushTimeoutMillis;
        const promises = this._activeSpanProcessor["_spanProcessors"].map((spanProcessor) => {
          return new Promise((resolve2) => {
            let state;
            const timeoutInterval = setTimeout(() => {
              resolve2(new Error(`Span processor did not completed within timeout period of ${timeout} ms`));
              state = ForceFlushState.timeout;
            }, timeout);
            spanProcessor.forceFlush().then(() => {
              clearTimeout(timeoutInterval);
              if (state !== ForceFlushState.timeout) {
                state = ForceFlushState.resolved;
                resolve2(state);
              }
            }).catch((error) => {
              clearTimeout(timeoutInterval);
              state = ForceFlushState.error;
              resolve2(error);
            });
          });
        });
        return new Promise((resolve2, reject) => {
          Promise.all(promises).then((results) => {
            const errors = results.filter((result) => result !== ForceFlushState.resolved);
            if (errors.length > 0) {
              reject(errors);
            } else {
              resolve2();
            }
          }).catch((error) => reject([error]));
        });
      }
      shutdown() {
        return this._activeSpanProcessor.shutdown();
      }
    };
    exports.BasicTracerProvider = BasicTracerProvider;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/ConsoleSpanExporter.js
var require_ConsoleSpanExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/ConsoleSpanExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConsoleSpanExporter = void 0;
    var core_1 = require_src();
    var ConsoleSpanExporter = class {
      static {
        __name(this, "ConsoleSpanExporter");
      }
      /**
       * Export spans.
       * @param spans
       * @param resultCallback
       */
      export(spans, resultCallback) {
        return this._sendSpans(spans, resultCallback);
      }
      /**
       * Shutdown the exporter.
       */
      shutdown() {
        this._sendSpans([]);
        return this.forceFlush();
      }
      /**
       * Exports any pending spans in exporter
       */
      forceFlush() {
        return Promise.resolve();
      }
      /**
       * converts span info into more readable format
       * @param span
       */
      _exportInfo(span) {
        return {
          resource: {
            attributes: span.resource.attributes
          },
          instrumentationScope: span.instrumentationScope,
          traceId: span.spanContext().traceId,
          parentSpanContext: span.parentSpanContext,
          traceState: span.spanContext().traceState?.serialize(),
          name: span.name,
          id: span.spanContext().spanId,
          kind: span.kind,
          timestamp: (0, core_1.hrTimeToMicroseconds)(span.startTime),
          duration: (0, core_1.hrTimeToMicroseconds)(span.duration),
          attributes: span.attributes,
          status: span.status,
          events: span.events,
          links: span.links
        };
      }
      /**
       * Showing spans in console
       * @param spans
       * @param done
       */
      _sendSpans(spans, done) {
        for (const span of spans) {
          console.dir(this._exportInfo(span), { depth: 3 });
        }
        if (done) {
          return done({ code: core_1.ExportResultCode.SUCCESS });
        }
      }
    };
    exports.ConsoleSpanExporter = ConsoleSpanExporter;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/InMemorySpanExporter.js
var require_InMemorySpanExporter = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/InMemorySpanExporter.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InMemorySpanExporter = void 0;
    var core_1 = require_src();
    var InMemorySpanExporter = class {
      static {
        __name(this, "InMemorySpanExporter");
      }
      _finishedSpans = [];
      /**
       * Indicates if the exporter has been "shutdown."
       * When false, exported spans will not be stored in-memory.
       */
      _stopped = false;
      export(spans, resultCallback) {
        if (this._stopped)
          return resultCallback({
            code: core_1.ExportResultCode.FAILED,
            error: new Error("Exporter has been stopped")
          });
        this._finishedSpans.push(...spans);
        setTimeout(() => resultCallback({ code: core_1.ExportResultCode.SUCCESS }), 0);
      }
      shutdown() {
        this._stopped = true;
        this._finishedSpans = [];
        return this.forceFlush();
      }
      /**
       * Exports any pending spans in the exporter
       */
      forceFlush() {
        return Promise.resolve();
      }
      reset() {
        this._finishedSpans = [];
      }
      getFinishedSpans() {
        return this._finishedSpans;
      }
    };
    exports.InMemorySpanExporter = InMemorySpanExporter;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/SimpleSpanProcessor.js
var require_SimpleSpanProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/SimpleSpanProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SimpleSpanProcessor = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    var SimpleSpanProcessor2 = class {
      static {
        __name(this, "SimpleSpanProcessor");
      }
      _exporter;
      _shutdownOnce;
      _pendingExports;
      constructor(exporter) {
        this._exporter = exporter;
        this._shutdownOnce = new core_1.BindOnceFuture(this._shutdown, this);
        this._pendingExports = /* @__PURE__ */ new Set();
      }
      async forceFlush() {
        await Promise.all(Array.from(this._pendingExports));
        if (this._exporter.forceFlush) {
          await this._exporter.forceFlush();
        }
      }
      onStart(_span, _parentContext) {
      }
      onEnd(span) {
        if (this._shutdownOnce.isCalled) {
          return;
        }
        if ((span.spanContext().traceFlags & api_1.TraceFlags.SAMPLED) === 0) {
          return;
        }
        const pendingExport = this._doExport(span).catch((err2) => (0, core_1.globalErrorHandler)(err2));
        this._pendingExports.add(pendingExport);
        void pendingExport.finally(() => this._pendingExports.delete(pendingExport));
      }
      async _doExport(span) {
        if (span.resource.asyncAttributesPending) {
          await span.resource.waitForAsyncAttributes?.();
        }
        const result = await core_1.internal._export(this._exporter, [span]);
        if (result.code !== core_1.ExportResultCode.SUCCESS) {
          throw result.error ?? new Error(`SimpleSpanProcessor: span export failed (status ${result})`);
        }
      }
      shutdown() {
        return this._shutdownOnce.call();
      }
      _shutdown() {
        return this._exporter.shutdown();
      }
    };
    exports.SimpleSpanProcessor = SimpleSpanProcessor2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/NoopSpanProcessor.js
var require_NoopSpanProcessor = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/export/NoopSpanProcessor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.NoopSpanProcessor = void 0;
    var NoopSpanProcessor = class {
      static {
        __name(this, "NoopSpanProcessor");
      }
      onStart(_span, _context) {
      }
      onEnd(_span) {
      }
      shutdown() {
        return Promise.resolve();
      }
      forceFlush() {
        return Promise.resolve();
      }
    };
    exports.NoopSpanProcessor = NoopSpanProcessor;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/index.js
var require_src14 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-base@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-base/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SamplingDecision = exports.TraceIdRatioBasedSampler = exports.ParentBasedSampler = exports.AlwaysOnSampler = exports.AlwaysOffSampler = exports.NoopSpanProcessor = exports.SimpleSpanProcessor = exports.InMemorySpanExporter = exports.ConsoleSpanExporter = exports.RandomIdGenerator = exports.BatchSpanProcessor = exports.BasicTracerProvider = void 0;
    var BasicTracerProvider_1 = require_BasicTracerProvider();
    Object.defineProperty(exports, "BasicTracerProvider", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return BasicTracerProvider_1.BasicTracerProvider;
    }, "get") });
    var platform_1 = require_platform7();
    Object.defineProperty(exports, "BatchSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.BatchSpanProcessor;
    }, "get") });
    Object.defineProperty(exports, "RandomIdGenerator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return platform_1.RandomIdGenerator;
    }, "get") });
    var ConsoleSpanExporter_1 = require_ConsoleSpanExporter();
    Object.defineProperty(exports, "ConsoleSpanExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ConsoleSpanExporter_1.ConsoleSpanExporter;
    }, "get") });
    var InMemorySpanExporter_1 = require_InMemorySpanExporter();
    Object.defineProperty(exports, "InMemorySpanExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return InMemorySpanExporter_1.InMemorySpanExporter;
    }, "get") });
    var SimpleSpanProcessor_1 = require_SimpleSpanProcessor();
    Object.defineProperty(exports, "SimpleSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return SimpleSpanProcessor_1.SimpleSpanProcessor;
    }, "get") });
    var NoopSpanProcessor_1 = require_NoopSpanProcessor();
    Object.defineProperty(exports, "NoopSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return NoopSpanProcessor_1.NoopSpanProcessor;
    }, "get") });
    var AlwaysOffSampler_1 = require_AlwaysOffSampler();
    Object.defineProperty(exports, "AlwaysOffSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AlwaysOffSampler_1.AlwaysOffSampler;
    }, "get") });
    var AlwaysOnSampler_1 = require_AlwaysOnSampler();
    Object.defineProperty(exports, "AlwaysOnSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AlwaysOnSampler_1.AlwaysOnSampler;
    }, "get") });
    var ParentBasedSampler_1 = require_ParentBasedSampler();
    Object.defineProperty(exports, "ParentBasedSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ParentBasedSampler_1.ParentBasedSampler;
    }, "get") });
    var TraceIdRatioBasedSampler_1 = require_TraceIdRatioBasedSampler();
    Object.defineProperty(exports, "TraceIdRatioBasedSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return TraceIdRatioBasedSampler_1.TraceIdRatioBasedSampler;
    }, "get") });
    var Sampler_1 = require_Sampler();
    Object.defineProperty(exports, "SamplingDecision", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return Sampler_1.SamplingDecision;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/AbstractAsyncHooksContextManager.js
var require_AbstractAsyncHooksContextManager = __commonJS({
  "../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/AbstractAsyncHooksContextManager.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AbstractAsyncHooksContextManager = void 0;
    var events_1 = __require("events");
    var ADD_LISTENER_METHODS = [
      "addListener",
      "on",
      "once",
      "prependListener",
      "prependOnceListener"
    ];
    var AbstractAsyncHooksContextManager = class {
      static {
        __name(this, "AbstractAsyncHooksContextManager");
      }
      /**
       * Binds a the certain context or the active one to the target function and then returns the target
       * @param context A context (span) to be bind to target
       * @param target a function or event emitter. When target or one of its callbacks is called,
       *  the provided context will be used as the active context for the duration of the call.
       */
      bind(context2, target) {
        if (target instanceof events_1.EventEmitter) {
          return this._bindEventEmitter(context2, target);
        }
        if (typeof target === "function") {
          return this._bindFunction(context2, target);
        }
        return target;
      }
      _bindFunction(context2, target) {
        const manager = this;
        const contextWrapper = /* @__PURE__ */ __name(function(...args) {
          return manager.with(context2, () => target.apply(this, args));
        }, "contextWrapper");
        Object.defineProperty(contextWrapper, "length", {
          enumerable: false,
          configurable: true,
          writable: false,
          value: target.length
        });
        return contextWrapper;
      }
      /**
       * By default, EventEmitter call their callback with their context, which we do
       * not want, instead we will bind a specific context to all callbacks that
       * go through it.
       * @param context the context we want to bind
       * @param ee EventEmitter an instance of EventEmitter to patch
       */
      _bindEventEmitter(context2, ee) {
        const map = this._getPatchMap(ee);
        if (map !== void 0)
          return ee;
        this._createPatchMap(ee);
        ADD_LISTENER_METHODS.forEach((methodName) => {
          if (ee[methodName] === void 0)
            return;
          ee[methodName] = this._patchAddListener(ee, ee[methodName], context2);
        });
        if (typeof ee.removeListener === "function") {
          ee.removeListener = this._patchRemoveListener(ee, ee.removeListener);
        }
        if (typeof ee.off === "function") {
          ee.off = this._patchRemoveListener(ee, ee.off);
        }
        if (typeof ee.removeAllListeners === "function") {
          ee.removeAllListeners = this._patchRemoveAllListeners(ee, ee.removeAllListeners);
        }
        return ee;
      }
      /**
       * Patch methods that remove a given listener so that we match the "patched"
       * version of that listener (the one that propagate context).
       * @param ee EventEmitter instance
       * @param original reference to the patched method
       */
      _patchRemoveListener(ee, original) {
        const contextManager = this;
        return function(event, listener) {
          const events = contextManager._getPatchMap(ee)?.[event];
          if (events === void 0) {
            return original.call(this, event, listener);
          }
          const patchedListener = events.get(listener);
          return original.call(this, event, patchedListener || listener);
        };
      }
      /**
       * Patch methods that remove all listeners so we remove our
       * internal references for a given event.
       * @param ee EventEmitter instance
       * @param original reference to the patched method
       */
      _patchRemoveAllListeners(ee, original) {
        const contextManager = this;
        return function(event) {
          const map = contextManager._getPatchMap(ee);
          if (map !== void 0) {
            if (arguments.length === 0) {
              contextManager._createPatchMap(ee);
            } else if (map[event] !== void 0) {
              delete map[event];
            }
          }
          return original.apply(this, arguments);
        };
      }
      /**
       * Patch methods on an event emitter instance that can add listeners so we
       * can force them to propagate a given context.
       * @param ee EventEmitter instance
       * @param original reference to the patched method
       * @param [context] context to propagate when calling listeners
       */
      _patchAddListener(ee, original, context2) {
        const contextManager = this;
        return function(event, listener) {
          if (contextManager._wrapped) {
            return original.call(this, event, listener);
          }
          let map = contextManager._getPatchMap(ee);
          if (map === void 0) {
            map = contextManager._createPatchMap(ee);
          }
          let listeners = map[event];
          if (listeners === void 0) {
            listeners = /* @__PURE__ */ new WeakMap();
            map[event] = listeners;
          }
          const patchedListener = contextManager.bind(context2, listener);
          listeners.set(listener, patchedListener);
          contextManager._wrapped = true;
          try {
            return original.call(this, event, patchedListener);
          } finally {
            contextManager._wrapped = false;
          }
        };
      }
      _createPatchMap(ee) {
        const map = /* @__PURE__ */ Object.create(null);
        ee[this._kOtListeners] = map;
        return map;
      }
      _getPatchMap(ee) {
        return ee[this._kOtListeners];
      }
      _kOtListeners = Symbol("OtListeners");
      _wrapped = false;
    };
    exports.AbstractAsyncHooksContextManager = AbstractAsyncHooksContextManager;
  }
});

// ../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/AsyncHooksContextManager.js
var require_AsyncHooksContextManager = __commonJS({
  "../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/AsyncHooksContextManager.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AsyncHooksContextManager = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var asyncHooks = __require("async_hooks");
    var AbstractAsyncHooksContextManager_1 = require_AbstractAsyncHooksContextManager();
    var AsyncHooksContextManager = class extends AbstractAsyncHooksContextManager_1.AbstractAsyncHooksContextManager {
      static {
        __name(this, "AsyncHooksContextManager");
      }
      _asyncHook;
      _contexts = /* @__PURE__ */ new Map();
      _stack = [];
      constructor() {
        super();
        this._asyncHook = asyncHooks.createHook({
          init: this._init.bind(this),
          before: this._before.bind(this),
          after: this._after.bind(this),
          destroy: this._destroy.bind(this),
          promiseResolve: this._destroy.bind(this)
        });
      }
      active() {
        return this._stack[this._stack.length - 1] ?? api_1.ROOT_CONTEXT;
      }
      with(context2, fn, thisArg, ...args) {
        this._enterContext(context2);
        try {
          return fn.call(thisArg, ...args);
        } finally {
          this._exitContext();
        }
      }
      enable() {
        this._asyncHook.enable();
        return this;
      }
      disable() {
        this._asyncHook.disable();
        this._contexts.clear();
        this._stack = [];
        return this;
      }
      /**
       * Init hook will be called when userland create a async context, setting the
       * context as the current one if it exist.
       * @param uid id of the async context
       * @param type the resource type
       */
      _init(uid, type) {
        if (type === "TIMERWRAP")
          return;
        const context2 = this._stack[this._stack.length - 1];
        if (context2 !== void 0) {
          this._contexts.set(uid, context2);
        }
      }
      /**
       * Destroy hook will be called when a given context is no longer used so we can
       * remove its attached context.
       * @param uid uid of the async context
       */
      _destroy(uid) {
        this._contexts.delete(uid);
      }
      /**
       * Before hook is called just before executing a async context.
       * @param uid uid of the async context
       */
      _before(uid) {
        const context2 = this._contexts.get(uid);
        if (context2 !== void 0) {
          this._enterContext(context2);
        }
      }
      /**
       * After hook is called just after completing the execution of a async context.
       */
      _after() {
        this._exitContext();
      }
      /**
       * Set the given context as active
       */
      _enterContext(context2) {
        this._stack.push(context2);
      }
      /**
       * Remove the context at the root of the stack
       */
      _exitContext() {
        this._stack.pop();
      }
    };
    exports.AsyncHooksContextManager = AsyncHooksContextManager;
  }
});

// ../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/AsyncLocalStorageContextManager.js
var require_AsyncLocalStorageContextManager = __commonJS({
  "../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/AsyncLocalStorageContextManager.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AsyncLocalStorageContextManager = void 0;
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var async_hooks_1 = __require("async_hooks");
    var AbstractAsyncHooksContextManager_1 = require_AbstractAsyncHooksContextManager();
    var AsyncLocalStorageContextManager = class extends AbstractAsyncHooksContextManager_1.AbstractAsyncHooksContextManager {
      static {
        __name(this, "AsyncLocalStorageContextManager");
      }
      _asyncLocalStorage;
      constructor() {
        super();
        this._asyncLocalStorage = new async_hooks_1.AsyncLocalStorage();
      }
      active() {
        return this._asyncLocalStorage.getStore() ?? api_1.ROOT_CONTEXT;
      }
      with(context2, fn, thisArg, ...args) {
        const cb = thisArg == null ? fn : fn.bind(thisArg);
        return this._asyncLocalStorage.run(context2, cb, ...args);
      }
      enable() {
        return this;
      }
      disable() {
        this._asyncLocalStorage.disable();
        return this;
      }
    };
    exports.AsyncLocalStorageContextManager = AsyncLocalStorageContextManager;
  }
});

// ../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/index.js
var require_src15 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+context-async-hooks@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/context-async-hooks/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AsyncLocalStorageContextManager = exports.AsyncHooksContextManager = void 0;
    var AsyncHooksContextManager_1 = require_AsyncHooksContextManager();
    Object.defineProperty(exports, "AsyncHooksContextManager", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AsyncHooksContextManager_1.AsyncHooksContextManager;
    }, "get") });
    var AsyncLocalStorageContextManager_1 = require_AsyncLocalStorageContextManager();
    Object.defineProperty(exports, "AsyncLocalStorageContextManager", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AsyncLocalStorageContextManager_1.AsyncLocalStorageContextManager;
    }, "get") });
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-node@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-node/build/src/NodeTracerProvider.js
var require_NodeTracerProvider = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-node@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-node/build/src/NodeTracerProvider.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.NodeTracerProvider = void 0;
    var context_async_hooks_1 = require_src15();
    var sdk_trace_base_1 = require_src14();
    var api_1 = (init_esm2(), __toCommonJS(esm_exports));
    var core_1 = require_src();
    function setupContextManager(contextManager) {
      if (contextManager === null) {
        return;
      }
      if (contextManager === void 0) {
        const defaultContextManager = new context_async_hooks_1.AsyncLocalStorageContextManager();
        defaultContextManager.enable();
        api_1.context.setGlobalContextManager(defaultContextManager);
        return;
      }
      contextManager.enable();
      api_1.context.setGlobalContextManager(contextManager);
    }
    __name(setupContextManager, "setupContextManager");
    function setupPropagator(propagator) {
      if (propagator === null) {
        return;
      }
      if (propagator === void 0) {
        api_1.propagation.setGlobalPropagator(new core_1.CompositePropagator({
          propagators: [
            new core_1.W3CTraceContextPropagator(),
            new core_1.W3CBaggagePropagator()
          ]
        }));
        return;
      }
      api_1.propagation.setGlobalPropagator(propagator);
    }
    __name(setupPropagator, "setupPropagator");
    var NodeTracerProvider2 = class extends sdk_trace_base_1.BasicTracerProvider {
      static {
        __name(this, "NodeTracerProvider");
      }
      constructor(config = {}) {
        super(config);
      }
      /**
       * Register this TracerProvider for use with the OpenTelemetry API.
       * Undefined values may be replaced with defaults, and
       * null values will be skipped.
       *
       * @param config Configuration object for SDK registration
       */
      register(config = {}) {
        api_1.trace.setGlobalTracerProvider(this);
        setupContextManager(config.contextManager);
        setupPropagator(config.propagator);
      }
    };
    exports.NodeTracerProvider = NodeTracerProvider2;
  }
});

// ../../node_modules/.bun/@opentelemetry+sdk-trace-node@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-node/build/src/index.js
var require_src16 = __commonJS({
  "../../node_modules/.bun/@opentelemetry+sdk-trace-node@2.7.1+e40b0dfdd726a224/node_modules/@opentelemetry/sdk-trace-node/build/src/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TraceIdRatioBasedSampler = exports.SimpleSpanProcessor = exports.SamplingDecision = exports.RandomIdGenerator = exports.ParentBasedSampler = exports.NoopSpanProcessor = exports.InMemorySpanExporter = exports.ConsoleSpanExporter = exports.BatchSpanProcessor = exports.BasicTracerProvider = exports.AlwaysOnSampler = exports.AlwaysOffSampler = exports.NodeTracerProvider = void 0;
    var NodeTracerProvider_1 = require_NodeTracerProvider();
    Object.defineProperty(exports, "NodeTracerProvider", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return NodeTracerProvider_1.NodeTracerProvider;
    }, "get") });
    var sdk_trace_base_1 = require_src14();
    Object.defineProperty(exports, "AlwaysOffSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.AlwaysOffSampler;
    }, "get") });
    Object.defineProperty(exports, "AlwaysOnSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.AlwaysOnSampler;
    }, "get") });
    Object.defineProperty(exports, "BasicTracerProvider", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.BasicTracerProvider;
    }, "get") });
    Object.defineProperty(exports, "BatchSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.BatchSpanProcessor;
    }, "get") });
    Object.defineProperty(exports, "ConsoleSpanExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.ConsoleSpanExporter;
    }, "get") });
    Object.defineProperty(exports, "InMemorySpanExporter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.InMemorySpanExporter;
    }, "get") });
    Object.defineProperty(exports, "NoopSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.NoopSpanProcessor;
    }, "get") });
    Object.defineProperty(exports, "ParentBasedSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.ParentBasedSampler;
    }, "get") });
    Object.defineProperty(exports, "RandomIdGenerator", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.RandomIdGenerator;
    }, "get") });
    Object.defineProperty(exports, "SamplingDecision", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.SamplingDecision;
    }, "get") });
    Object.defineProperty(exports, "SimpleSpanProcessor", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.SimpleSpanProcessor;
    }, "get") });
    Object.defineProperty(exports, "TraceIdRatioBasedSampler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return sdk_trace_base_1.TraceIdRatioBasedSampler;
    }, "get") });
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/base64.js
var require_base64 = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/base64.js"(exports) {
    init_esm();
    var intToCharMap = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split("");
    exports.encode = function(number) {
      if (0 <= number && number < intToCharMap.length) {
        return intToCharMap[number];
      }
      throw new TypeError("Must be between 0 and 63: " + number);
    };
    exports.decode = function(charCode) {
      var bigA = 65;
      var bigZ = 90;
      var littleA = 97;
      var littleZ = 122;
      var zero = 48;
      var nine = 57;
      var plus = 43;
      var slash = 47;
      var littleOffset = 26;
      var numberOffset = 52;
      if (bigA <= charCode && charCode <= bigZ) {
        return charCode - bigA;
      }
      if (littleA <= charCode && charCode <= littleZ) {
        return charCode - littleA + littleOffset;
      }
      if (zero <= charCode && charCode <= nine) {
        return charCode - zero + numberOffset;
      }
      if (charCode == plus) {
        return 62;
      }
      if (charCode == slash) {
        return 63;
      }
      return -1;
    };
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/base64-vlq.js
var require_base64_vlq = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/base64-vlq.js"(exports) {
    init_esm();
    var base64 = require_base64();
    var VLQ_BASE_SHIFT = 5;
    var VLQ_BASE = 1 << VLQ_BASE_SHIFT;
    var VLQ_BASE_MASK = VLQ_BASE - 1;
    var VLQ_CONTINUATION_BIT = VLQ_BASE;
    function toVLQSigned(aValue) {
      return aValue < 0 ? (-aValue << 1) + 1 : (aValue << 1) + 0;
    }
    __name(toVLQSigned, "toVLQSigned");
    function fromVLQSigned(aValue) {
      var isNegative = (aValue & 1) === 1;
      var shifted = aValue >> 1;
      return isNegative ? -shifted : shifted;
    }
    __name(fromVLQSigned, "fromVLQSigned");
    exports.encode = /* @__PURE__ */ __name(function base64VLQ_encode(aValue) {
      var encoded = "";
      var digit;
      var vlq = toVLQSigned(aValue);
      do {
        digit = vlq & VLQ_BASE_MASK;
        vlq >>>= VLQ_BASE_SHIFT;
        if (vlq > 0) {
          digit |= VLQ_CONTINUATION_BIT;
        }
        encoded += base64.encode(digit);
      } while (vlq > 0);
      return encoded;
    }, "base64VLQ_encode");
    exports.decode = /* @__PURE__ */ __name(function base64VLQ_decode(aStr, aIndex, aOutParam) {
      var strLen = aStr.length;
      var result = 0;
      var shift = 0;
      var continuation, digit;
      do {
        if (aIndex >= strLen) {
          throw new Error("Expected more digits in base 64 VLQ value.");
        }
        digit = base64.decode(aStr.charCodeAt(aIndex++));
        if (digit === -1) {
          throw new Error("Invalid base64 digit: " + aStr.charAt(aIndex - 1));
        }
        continuation = !!(digit & VLQ_CONTINUATION_BIT);
        digit &= VLQ_BASE_MASK;
        result = result + (digit << shift);
        shift += VLQ_BASE_SHIFT;
      } while (continuation);
      aOutParam.value = fromVLQSigned(result);
      aOutParam.rest = aIndex;
    }, "base64VLQ_decode");
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/util.js
var require_util3 = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/util.js"(exports) {
    init_esm();
    function getArg(aArgs, aName, aDefaultValue) {
      if (aName in aArgs) {
        return aArgs[aName];
      } else if (arguments.length === 3) {
        return aDefaultValue;
      } else {
        throw new Error('"' + aName + '" is a required argument.');
      }
    }
    __name(getArg, "getArg");
    exports.getArg = getArg;
    var urlRegexp = /^(?:([\w+\-.]+):)?\/\/(?:(\w+:\w+)@)?([\w.-]*)(?::(\d+))?(.*)$/;
    var dataUrlRegexp = /^data:.+\,.+$/;
    function urlParse(aUrl) {
      var match = aUrl.match(urlRegexp);
      if (!match) {
        return null;
      }
      return {
        scheme: match[1],
        auth: match[2],
        host: match[3],
        port: match[4],
        path: match[5]
      };
    }
    __name(urlParse, "urlParse");
    exports.urlParse = urlParse;
    function urlGenerate(aParsedUrl) {
      var url = "";
      if (aParsedUrl.scheme) {
        url += aParsedUrl.scheme + ":";
      }
      url += "//";
      if (aParsedUrl.auth) {
        url += aParsedUrl.auth + "@";
      }
      if (aParsedUrl.host) {
        url += aParsedUrl.host;
      }
      if (aParsedUrl.port) {
        url += ":" + aParsedUrl.port;
      }
      if (aParsedUrl.path) {
        url += aParsedUrl.path;
      }
      return url;
    }
    __name(urlGenerate, "urlGenerate");
    exports.urlGenerate = urlGenerate;
    function normalize(aPath) {
      var path2 = aPath;
      var url = urlParse(aPath);
      if (url) {
        if (!url.path) {
          return aPath;
        }
        path2 = url.path;
      }
      var isAbsolute = exports.isAbsolute(path2);
      var parts = path2.split(/\/+/);
      for (var part, up = 0, i = parts.length - 1; i >= 0; i--) {
        part = parts[i];
        if (part === ".") {
          parts.splice(i, 1);
        } else if (part === "..") {
          up++;
        } else if (up > 0) {
          if (part === "") {
            parts.splice(i + 1, up);
            up = 0;
          } else {
            parts.splice(i, 2);
            up--;
          }
        }
      }
      path2 = parts.join("/");
      if (path2 === "") {
        path2 = isAbsolute ? "/" : ".";
      }
      if (url) {
        url.path = path2;
        return urlGenerate(url);
      }
      return path2;
    }
    __name(normalize, "normalize");
    exports.normalize = normalize;
    function join(aRoot, aPath) {
      if (aRoot === "") {
        aRoot = ".";
      }
      if (aPath === "") {
        aPath = ".";
      }
      var aPathUrl = urlParse(aPath);
      var aRootUrl = urlParse(aRoot);
      if (aRootUrl) {
        aRoot = aRootUrl.path || "/";
      }
      if (aPathUrl && !aPathUrl.scheme) {
        if (aRootUrl) {
          aPathUrl.scheme = aRootUrl.scheme;
        }
        return urlGenerate(aPathUrl);
      }
      if (aPathUrl || aPath.match(dataUrlRegexp)) {
        return aPath;
      }
      if (aRootUrl && !aRootUrl.host && !aRootUrl.path) {
        aRootUrl.host = aPath;
        return urlGenerate(aRootUrl);
      }
      var joined = aPath.charAt(0) === "/" ? aPath : normalize(aRoot.replace(/\/+$/, "") + "/" + aPath);
      if (aRootUrl) {
        aRootUrl.path = joined;
        return urlGenerate(aRootUrl);
      }
      return joined;
    }
    __name(join, "join");
    exports.join = join;
    exports.isAbsolute = function(aPath) {
      return aPath.charAt(0) === "/" || urlRegexp.test(aPath);
    };
    function relative(aRoot, aPath) {
      if (aRoot === "") {
        aRoot = ".";
      }
      aRoot = aRoot.replace(/\/$/, "");
      var level = 0;
      while (aPath.indexOf(aRoot + "/") !== 0) {
        var index = aRoot.lastIndexOf("/");
        if (index < 0) {
          return aPath;
        }
        aRoot = aRoot.slice(0, index);
        if (aRoot.match(/^([^\/]+:\/)?\/*$/)) {
          return aPath;
        }
        ++level;
      }
      return Array(level + 1).join("../") + aPath.substr(aRoot.length + 1);
    }
    __name(relative, "relative");
    exports.relative = relative;
    var supportsNullProto = function() {
      var obj = /* @__PURE__ */ Object.create(null);
      return !("__proto__" in obj);
    }();
    function identity(s) {
      return s;
    }
    __name(identity, "identity");
    function toSetString(aStr) {
      if (isProtoString(aStr)) {
        return "$" + aStr;
      }
      return aStr;
    }
    __name(toSetString, "toSetString");
    exports.toSetString = supportsNullProto ? identity : toSetString;
    function fromSetString(aStr) {
      if (isProtoString(aStr)) {
        return aStr.slice(1);
      }
      return aStr;
    }
    __name(fromSetString, "fromSetString");
    exports.fromSetString = supportsNullProto ? identity : fromSetString;
    function isProtoString(s) {
      if (!s) {
        return false;
      }
      var length = s.length;
      if (length < 9) {
        return false;
      }
      if (s.charCodeAt(length - 1) !== 95 || s.charCodeAt(length - 2) !== 95 || s.charCodeAt(length - 3) !== 111 || s.charCodeAt(length - 4) !== 116 || s.charCodeAt(length - 5) !== 111 || s.charCodeAt(length - 6) !== 114 || s.charCodeAt(length - 7) !== 112 || s.charCodeAt(length - 8) !== 95 || s.charCodeAt(length - 9) !== 95) {
        return false;
      }
      for (var i = length - 10; i >= 0; i--) {
        if (s.charCodeAt(i) !== 36) {
          return false;
        }
      }
      return true;
    }
    __name(isProtoString, "isProtoString");
    function compareByOriginalPositions(mappingA, mappingB, onlyCompareOriginal) {
      var cmp = strcmp(mappingA.source, mappingB.source);
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.originalLine - mappingB.originalLine;
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.originalColumn - mappingB.originalColumn;
      if (cmp !== 0 || onlyCompareOriginal) {
        return cmp;
      }
      cmp = mappingA.generatedColumn - mappingB.generatedColumn;
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.generatedLine - mappingB.generatedLine;
      if (cmp !== 0) {
        return cmp;
      }
      return strcmp(mappingA.name, mappingB.name);
    }
    __name(compareByOriginalPositions, "compareByOriginalPositions");
    exports.compareByOriginalPositions = compareByOriginalPositions;
    function compareByGeneratedPositionsDeflated(mappingA, mappingB, onlyCompareGenerated) {
      var cmp = mappingA.generatedLine - mappingB.generatedLine;
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.generatedColumn - mappingB.generatedColumn;
      if (cmp !== 0 || onlyCompareGenerated) {
        return cmp;
      }
      cmp = strcmp(mappingA.source, mappingB.source);
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.originalLine - mappingB.originalLine;
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.originalColumn - mappingB.originalColumn;
      if (cmp !== 0) {
        return cmp;
      }
      return strcmp(mappingA.name, mappingB.name);
    }
    __name(compareByGeneratedPositionsDeflated, "compareByGeneratedPositionsDeflated");
    exports.compareByGeneratedPositionsDeflated = compareByGeneratedPositionsDeflated;
    function strcmp(aStr1, aStr2) {
      if (aStr1 === aStr2) {
        return 0;
      }
      if (aStr1 === null) {
        return 1;
      }
      if (aStr2 === null) {
        return -1;
      }
      if (aStr1 > aStr2) {
        return 1;
      }
      return -1;
    }
    __name(strcmp, "strcmp");
    function compareByGeneratedPositionsInflated(mappingA, mappingB) {
      var cmp = mappingA.generatedLine - mappingB.generatedLine;
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.generatedColumn - mappingB.generatedColumn;
      if (cmp !== 0) {
        return cmp;
      }
      cmp = strcmp(mappingA.source, mappingB.source);
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.originalLine - mappingB.originalLine;
      if (cmp !== 0) {
        return cmp;
      }
      cmp = mappingA.originalColumn - mappingB.originalColumn;
      if (cmp !== 0) {
        return cmp;
      }
      return strcmp(mappingA.name, mappingB.name);
    }
    __name(compareByGeneratedPositionsInflated, "compareByGeneratedPositionsInflated");
    exports.compareByGeneratedPositionsInflated = compareByGeneratedPositionsInflated;
    function parseSourceMapInput(str) {
      return JSON.parse(str.replace(/^\)]}'[^\n]*\n/, ""));
    }
    __name(parseSourceMapInput, "parseSourceMapInput");
    exports.parseSourceMapInput = parseSourceMapInput;
    function computeSourceURL(sourceRoot, sourceURL, sourceMapURL) {
      sourceURL = sourceURL || "";
      if (sourceRoot) {
        if (sourceRoot[sourceRoot.length - 1] !== "/" && sourceURL[0] !== "/") {
          sourceRoot += "/";
        }
        sourceURL = sourceRoot + sourceURL;
      }
      if (sourceMapURL) {
        var parsed = urlParse(sourceMapURL);
        if (!parsed) {
          throw new Error("sourceMapURL could not be parsed");
        }
        if (parsed.path) {
          var index = parsed.path.lastIndexOf("/");
          if (index >= 0) {
            parsed.path = parsed.path.substring(0, index + 1);
          }
        }
        sourceURL = join(urlGenerate(parsed), sourceURL);
      }
      return normalize(sourceURL);
    }
    __name(computeSourceURL, "computeSourceURL");
    exports.computeSourceURL = computeSourceURL;
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/array-set.js
var require_array_set = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/array-set.js"(exports) {
    init_esm();
    var util2 = require_util3();
    var has2 = Object.prototype.hasOwnProperty;
    var hasNativeMap = typeof Map !== "undefined";
    function ArraySet() {
      this._array = [];
      this._set = hasNativeMap ? /* @__PURE__ */ new Map() : /* @__PURE__ */ Object.create(null);
    }
    __name(ArraySet, "ArraySet");
    ArraySet.fromArray = /* @__PURE__ */ __name(function ArraySet_fromArray(aArray, aAllowDuplicates) {
      var set = new ArraySet();
      for (var i = 0, len = aArray.length; i < len; i++) {
        set.add(aArray[i], aAllowDuplicates);
      }
      return set;
    }, "ArraySet_fromArray");
    ArraySet.prototype.size = /* @__PURE__ */ __name(function ArraySet_size() {
      return hasNativeMap ? this._set.size : Object.getOwnPropertyNames(this._set).length;
    }, "ArraySet_size");
    ArraySet.prototype.add = /* @__PURE__ */ __name(function ArraySet_add(aStr, aAllowDuplicates) {
      var sStr = hasNativeMap ? aStr : util2.toSetString(aStr);
      var isDuplicate = hasNativeMap ? this.has(aStr) : has2.call(this._set, sStr);
      var idx = this._array.length;
      if (!isDuplicate || aAllowDuplicates) {
        this._array.push(aStr);
      }
      if (!isDuplicate) {
        if (hasNativeMap) {
          this._set.set(aStr, idx);
        } else {
          this._set[sStr] = idx;
        }
      }
    }, "ArraySet_add");
    ArraySet.prototype.has = /* @__PURE__ */ __name(function ArraySet_has(aStr) {
      if (hasNativeMap) {
        return this._set.has(aStr);
      } else {
        var sStr = util2.toSetString(aStr);
        return has2.call(this._set, sStr);
      }
    }, "ArraySet_has");
    ArraySet.prototype.indexOf = /* @__PURE__ */ __name(function ArraySet_indexOf(aStr) {
      if (hasNativeMap) {
        var idx = this._set.get(aStr);
        if (idx >= 0) {
          return idx;
        }
      } else {
        var sStr = util2.toSetString(aStr);
        if (has2.call(this._set, sStr)) {
          return this._set[sStr];
        }
      }
      throw new Error('"' + aStr + '" is not in the set.');
    }, "ArraySet_indexOf");
    ArraySet.prototype.at = /* @__PURE__ */ __name(function ArraySet_at(aIdx) {
      if (aIdx >= 0 && aIdx < this._array.length) {
        return this._array[aIdx];
      }
      throw new Error("No element indexed by " + aIdx);
    }, "ArraySet_at");
    ArraySet.prototype.toArray = /* @__PURE__ */ __name(function ArraySet_toArray() {
      return this._array.slice();
    }, "ArraySet_toArray");
    exports.ArraySet = ArraySet;
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/mapping-list.js
var require_mapping_list = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/mapping-list.js"(exports) {
    init_esm();
    var util2 = require_util3();
    function generatedPositionAfter(mappingA, mappingB) {
      var lineA = mappingA.generatedLine;
      var lineB = mappingB.generatedLine;
      var columnA = mappingA.generatedColumn;
      var columnB = mappingB.generatedColumn;
      return lineB > lineA || lineB == lineA && columnB >= columnA || util2.compareByGeneratedPositionsInflated(mappingA, mappingB) <= 0;
    }
    __name(generatedPositionAfter, "generatedPositionAfter");
    function MappingList() {
      this._array = [];
      this._sorted = true;
      this._last = { generatedLine: -1, generatedColumn: 0 };
    }
    __name(MappingList, "MappingList");
    MappingList.prototype.unsortedForEach = /* @__PURE__ */ __name(function MappingList_forEach(aCallback, aThisArg) {
      this._array.forEach(aCallback, aThisArg);
    }, "MappingList_forEach");
    MappingList.prototype.add = /* @__PURE__ */ __name(function MappingList_add(aMapping) {
      if (generatedPositionAfter(this._last, aMapping)) {
        this._last = aMapping;
        this._array.push(aMapping);
      } else {
        this._sorted = false;
        this._array.push(aMapping);
      }
    }, "MappingList_add");
    MappingList.prototype.toArray = /* @__PURE__ */ __name(function MappingList_toArray() {
      if (!this._sorted) {
        this._array.sort(util2.compareByGeneratedPositionsInflated);
        this._sorted = true;
      }
      return this._array;
    }, "MappingList_toArray");
    exports.MappingList = MappingList;
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/source-map-generator.js
var require_source_map_generator = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/source-map-generator.js"(exports) {
    init_esm();
    var base64VLQ = require_base64_vlq();
    var util2 = require_util3();
    var ArraySet = require_array_set().ArraySet;
    var MappingList = require_mapping_list().MappingList;
    function SourceMapGenerator(aArgs) {
      if (!aArgs) {
        aArgs = {};
      }
      this._file = util2.getArg(aArgs, "file", null);
      this._sourceRoot = util2.getArg(aArgs, "sourceRoot", null);
      this._skipValidation = util2.getArg(aArgs, "skipValidation", false);
      this._sources = new ArraySet();
      this._names = new ArraySet();
      this._mappings = new MappingList();
      this._sourcesContents = null;
    }
    __name(SourceMapGenerator, "SourceMapGenerator");
    SourceMapGenerator.prototype._version = 3;
    SourceMapGenerator.fromSourceMap = /* @__PURE__ */ __name(function SourceMapGenerator_fromSourceMap(aSourceMapConsumer) {
      var sourceRoot = aSourceMapConsumer.sourceRoot;
      var generator = new SourceMapGenerator({
        file: aSourceMapConsumer.file,
        sourceRoot
      });
      aSourceMapConsumer.eachMapping(function(mapping) {
        var newMapping = {
          generated: {
            line: mapping.generatedLine,
            column: mapping.generatedColumn
          }
        };
        if (mapping.source != null) {
          newMapping.source = mapping.source;
          if (sourceRoot != null) {
            newMapping.source = util2.relative(sourceRoot, newMapping.source);
          }
          newMapping.original = {
            line: mapping.originalLine,
            column: mapping.originalColumn
          };
          if (mapping.name != null) {
            newMapping.name = mapping.name;
          }
        }
        generator.addMapping(newMapping);
      });
      aSourceMapConsumer.sources.forEach(function(sourceFile) {
        var sourceRelative = sourceFile;
        if (sourceRoot !== null) {
          sourceRelative = util2.relative(sourceRoot, sourceFile);
        }
        if (!generator._sources.has(sourceRelative)) {
          generator._sources.add(sourceRelative);
        }
        var content = aSourceMapConsumer.sourceContentFor(sourceFile);
        if (content != null) {
          generator.setSourceContent(sourceFile, content);
        }
      });
      return generator;
    }, "SourceMapGenerator_fromSourceMap");
    SourceMapGenerator.prototype.addMapping = /* @__PURE__ */ __name(function SourceMapGenerator_addMapping(aArgs) {
      var generated = util2.getArg(aArgs, "generated");
      var original = util2.getArg(aArgs, "original", null);
      var source = util2.getArg(aArgs, "source", null);
      var name = util2.getArg(aArgs, "name", null);
      if (!this._skipValidation) {
        this._validateMapping(generated, original, source, name);
      }
      if (source != null) {
        source = String(source);
        if (!this._sources.has(source)) {
          this._sources.add(source);
        }
      }
      if (name != null) {
        name = String(name);
        if (!this._names.has(name)) {
          this._names.add(name);
        }
      }
      this._mappings.add({
        generatedLine: generated.line,
        generatedColumn: generated.column,
        originalLine: original != null && original.line,
        originalColumn: original != null && original.column,
        source,
        name
      });
    }, "SourceMapGenerator_addMapping");
    SourceMapGenerator.prototype.setSourceContent = /* @__PURE__ */ __name(function SourceMapGenerator_setSourceContent(aSourceFile, aSourceContent) {
      var source = aSourceFile;
      if (this._sourceRoot != null) {
        source = util2.relative(this._sourceRoot, source);
      }
      if (aSourceContent != null) {
        if (!this._sourcesContents) {
          this._sourcesContents = /* @__PURE__ */ Object.create(null);
        }
        this._sourcesContents[util2.toSetString(source)] = aSourceContent;
      } else if (this._sourcesContents) {
        delete this._sourcesContents[util2.toSetString(source)];
        if (Object.keys(this._sourcesContents).length === 0) {
          this._sourcesContents = null;
        }
      }
    }, "SourceMapGenerator_setSourceContent");
    SourceMapGenerator.prototype.applySourceMap = /* @__PURE__ */ __name(function SourceMapGenerator_applySourceMap(aSourceMapConsumer, aSourceFile, aSourceMapPath) {
      var sourceFile = aSourceFile;
      if (aSourceFile == null) {
        if (aSourceMapConsumer.file == null) {
          throw new Error(
            `SourceMapGenerator.prototype.applySourceMap requires either an explicit source file, or the source map's "file" property. Both were omitted.`
          );
        }
        sourceFile = aSourceMapConsumer.file;
      }
      var sourceRoot = this._sourceRoot;
      if (sourceRoot != null) {
        sourceFile = util2.relative(sourceRoot, sourceFile);
      }
      var newSources = new ArraySet();
      var newNames = new ArraySet();
      this._mappings.unsortedForEach(function(mapping) {
        if (mapping.source === sourceFile && mapping.originalLine != null) {
          var original = aSourceMapConsumer.originalPositionFor({
            line: mapping.originalLine,
            column: mapping.originalColumn
          });
          if (original.source != null) {
            mapping.source = original.source;
            if (aSourceMapPath != null) {
              mapping.source = util2.join(aSourceMapPath, mapping.source);
            }
            if (sourceRoot != null) {
              mapping.source = util2.relative(sourceRoot, mapping.source);
            }
            mapping.originalLine = original.line;
            mapping.originalColumn = original.column;
            if (original.name != null) {
              mapping.name = original.name;
            }
          }
        }
        var source = mapping.source;
        if (source != null && !newSources.has(source)) {
          newSources.add(source);
        }
        var name = mapping.name;
        if (name != null && !newNames.has(name)) {
          newNames.add(name);
        }
      }, this);
      this._sources = newSources;
      this._names = newNames;
      aSourceMapConsumer.sources.forEach(function(sourceFile2) {
        var content = aSourceMapConsumer.sourceContentFor(sourceFile2);
        if (content != null) {
          if (aSourceMapPath != null) {
            sourceFile2 = util2.join(aSourceMapPath, sourceFile2);
          }
          if (sourceRoot != null) {
            sourceFile2 = util2.relative(sourceRoot, sourceFile2);
          }
          this.setSourceContent(sourceFile2, content);
        }
      }, this);
    }, "SourceMapGenerator_applySourceMap");
    SourceMapGenerator.prototype._validateMapping = /* @__PURE__ */ __name(function SourceMapGenerator_validateMapping(aGenerated, aOriginal, aSource, aName) {
      if (aOriginal && typeof aOriginal.line !== "number" && typeof aOriginal.column !== "number") {
        throw new Error(
          "original.line and original.column are not numbers -- you probably meant to omit the original mapping entirely and only map the generated position. If so, pass null for the original mapping instead of an object with empty or null values."
        );
      }
      if (aGenerated && "line" in aGenerated && "column" in aGenerated && aGenerated.line > 0 && aGenerated.column >= 0 && !aOriginal && !aSource && !aName) {
        return;
      } else if (aGenerated && "line" in aGenerated && "column" in aGenerated && aOriginal && "line" in aOriginal && "column" in aOriginal && aGenerated.line > 0 && aGenerated.column >= 0 && aOriginal.line > 0 && aOriginal.column >= 0 && aSource) {
        return;
      } else {
        throw new Error("Invalid mapping: " + JSON.stringify({
          generated: aGenerated,
          source: aSource,
          original: aOriginal,
          name: aName
        }));
      }
    }, "SourceMapGenerator_validateMapping");
    SourceMapGenerator.prototype._serializeMappings = /* @__PURE__ */ __name(function SourceMapGenerator_serializeMappings() {
      var previousGeneratedColumn = 0;
      var previousGeneratedLine = 1;
      var previousOriginalColumn = 0;
      var previousOriginalLine = 0;
      var previousName = 0;
      var previousSource = 0;
      var result = "";
      var next;
      var mapping;
      var nameIdx;
      var sourceIdx;
      var mappings = this._mappings.toArray();
      for (var i = 0, len = mappings.length; i < len; i++) {
        mapping = mappings[i];
        next = "";
        if (mapping.generatedLine !== previousGeneratedLine) {
          previousGeneratedColumn = 0;
          while (mapping.generatedLine !== previousGeneratedLine) {
            next += ";";
            previousGeneratedLine++;
          }
        } else {
          if (i > 0) {
            if (!util2.compareByGeneratedPositionsInflated(mapping, mappings[i - 1])) {
              continue;
            }
            next += ",";
          }
        }
        next += base64VLQ.encode(mapping.generatedColumn - previousGeneratedColumn);
        previousGeneratedColumn = mapping.generatedColumn;
        if (mapping.source != null) {
          sourceIdx = this._sources.indexOf(mapping.source);
          next += base64VLQ.encode(sourceIdx - previousSource);
          previousSource = sourceIdx;
          next += base64VLQ.encode(mapping.originalLine - 1 - previousOriginalLine);
          previousOriginalLine = mapping.originalLine - 1;
          next += base64VLQ.encode(mapping.originalColumn - previousOriginalColumn);
          previousOriginalColumn = mapping.originalColumn;
          if (mapping.name != null) {
            nameIdx = this._names.indexOf(mapping.name);
            next += base64VLQ.encode(nameIdx - previousName);
            previousName = nameIdx;
          }
        }
        result += next;
      }
      return result;
    }, "SourceMapGenerator_serializeMappings");
    SourceMapGenerator.prototype._generateSourcesContent = /* @__PURE__ */ __name(function SourceMapGenerator_generateSourcesContent(aSources, aSourceRoot) {
      return aSources.map(function(source) {
        if (!this._sourcesContents) {
          return null;
        }
        if (aSourceRoot != null) {
          source = util2.relative(aSourceRoot, source);
        }
        var key = util2.toSetString(source);
        return Object.prototype.hasOwnProperty.call(this._sourcesContents, key) ? this._sourcesContents[key] : null;
      }, this);
    }, "SourceMapGenerator_generateSourcesContent");
    SourceMapGenerator.prototype.toJSON = /* @__PURE__ */ __name(function SourceMapGenerator_toJSON() {
      var map = {
        version: this._version,
        sources: this._sources.toArray(),
        names: this._names.toArray(),
        mappings: this._serializeMappings()
      };
      if (this._file != null) {
        map.file = this._file;
      }
      if (this._sourceRoot != null) {
        map.sourceRoot = this._sourceRoot;
      }
      if (this._sourcesContents) {
        map.sourcesContent = this._generateSourcesContent(map.sources, map.sourceRoot);
      }
      return map;
    }, "SourceMapGenerator_toJSON");
    SourceMapGenerator.prototype.toString = /* @__PURE__ */ __name(function SourceMapGenerator_toString() {
      return JSON.stringify(this.toJSON());
    }, "SourceMapGenerator_toString");
    exports.SourceMapGenerator = SourceMapGenerator;
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/binary-search.js
var require_binary_search = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/binary-search.js"(exports) {
    init_esm();
    exports.GREATEST_LOWER_BOUND = 1;
    exports.LEAST_UPPER_BOUND = 2;
    function recursiveSearch(aLow, aHigh, aNeedle, aHaystack, aCompare, aBias) {
      var mid = Math.floor((aHigh - aLow) / 2) + aLow;
      var cmp = aCompare(aNeedle, aHaystack[mid], true);
      if (cmp === 0) {
        return mid;
      } else if (cmp > 0) {
        if (aHigh - mid > 1) {
          return recursiveSearch(mid, aHigh, aNeedle, aHaystack, aCompare, aBias);
        }
        if (aBias == exports.LEAST_UPPER_BOUND) {
          return aHigh < aHaystack.length ? aHigh : -1;
        } else {
          return mid;
        }
      } else {
        if (mid - aLow > 1) {
          return recursiveSearch(aLow, mid, aNeedle, aHaystack, aCompare, aBias);
        }
        if (aBias == exports.LEAST_UPPER_BOUND) {
          return mid;
        } else {
          return aLow < 0 ? -1 : aLow;
        }
      }
    }
    __name(recursiveSearch, "recursiveSearch");
    exports.search = /* @__PURE__ */ __name(function search(aNeedle, aHaystack, aCompare, aBias) {
      if (aHaystack.length === 0) {
        return -1;
      }
      var index = recursiveSearch(
        -1,
        aHaystack.length,
        aNeedle,
        aHaystack,
        aCompare,
        aBias || exports.GREATEST_LOWER_BOUND
      );
      if (index < 0) {
        return -1;
      }
      while (index - 1 >= 0) {
        if (aCompare(aHaystack[index], aHaystack[index - 1], true) !== 0) {
          break;
        }
        --index;
      }
      return index;
    }, "search");
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/quick-sort.js
var require_quick_sort = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/quick-sort.js"(exports) {
    init_esm();
    function swap(ary, x, y) {
      var temp = ary[x];
      ary[x] = ary[y];
      ary[y] = temp;
    }
    __name(swap, "swap");
    function randomIntInRange(low, high) {
      return Math.round(low + Math.random() * (high - low));
    }
    __name(randomIntInRange, "randomIntInRange");
    function doQuickSort(ary, comparator, p, r) {
      if (p < r) {
        var pivotIndex = randomIntInRange(p, r);
        var i = p - 1;
        swap(ary, pivotIndex, r);
        var pivot = ary[r];
        for (var j = p; j < r; j++) {
          if (comparator(ary[j], pivot) <= 0) {
            i += 1;
            swap(ary, i, j);
          }
        }
        swap(ary, i + 1, j);
        var q = i + 1;
        doQuickSort(ary, comparator, p, q - 1);
        doQuickSort(ary, comparator, q + 1, r);
      }
    }
    __name(doQuickSort, "doQuickSort");
    exports.quickSort = function(ary, comparator) {
      doQuickSort(ary, comparator, 0, ary.length - 1);
    };
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/source-map-consumer.js
var require_source_map_consumer = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/source-map-consumer.js"(exports) {
    init_esm();
    var util2 = require_util3();
    var binarySearch = require_binary_search();
    var ArraySet = require_array_set().ArraySet;
    var base64VLQ = require_base64_vlq();
    var quickSort = require_quick_sort().quickSort;
    function SourceMapConsumer(aSourceMap, aSourceMapURL) {
      var sourceMap = aSourceMap;
      if (typeof aSourceMap === "string") {
        sourceMap = util2.parseSourceMapInput(aSourceMap);
      }
      return sourceMap.sections != null ? new IndexedSourceMapConsumer(sourceMap, aSourceMapURL) : new BasicSourceMapConsumer(sourceMap, aSourceMapURL);
    }
    __name(SourceMapConsumer, "SourceMapConsumer");
    SourceMapConsumer.fromSourceMap = function(aSourceMap, aSourceMapURL) {
      return BasicSourceMapConsumer.fromSourceMap(aSourceMap, aSourceMapURL);
    };
    SourceMapConsumer.prototype._version = 3;
    SourceMapConsumer.prototype.__generatedMappings = null;
    Object.defineProperty(SourceMapConsumer.prototype, "_generatedMappings", {
      configurable: true,
      enumerable: true,
      get: /* @__PURE__ */ __name(function() {
        if (!this.__generatedMappings) {
          this._parseMappings(this._mappings, this.sourceRoot);
        }
        return this.__generatedMappings;
      }, "get")
    });
    SourceMapConsumer.prototype.__originalMappings = null;
    Object.defineProperty(SourceMapConsumer.prototype, "_originalMappings", {
      configurable: true,
      enumerable: true,
      get: /* @__PURE__ */ __name(function() {
        if (!this.__originalMappings) {
          this._parseMappings(this._mappings, this.sourceRoot);
        }
        return this.__originalMappings;
      }, "get")
    });
    SourceMapConsumer.prototype._charIsMappingSeparator = /* @__PURE__ */ __name(function SourceMapConsumer_charIsMappingSeparator(aStr, index) {
      var c = aStr.charAt(index);
      return c === ";" || c === ",";
    }, "SourceMapConsumer_charIsMappingSeparator");
    SourceMapConsumer.prototype._parseMappings = /* @__PURE__ */ __name(function SourceMapConsumer_parseMappings(aStr, aSourceRoot) {
      throw new Error("Subclasses must implement _parseMappings");
    }, "SourceMapConsumer_parseMappings");
    SourceMapConsumer.GENERATED_ORDER = 1;
    SourceMapConsumer.ORIGINAL_ORDER = 2;
    SourceMapConsumer.GREATEST_LOWER_BOUND = 1;
    SourceMapConsumer.LEAST_UPPER_BOUND = 2;
    SourceMapConsumer.prototype.eachMapping = /* @__PURE__ */ __name(function SourceMapConsumer_eachMapping(aCallback, aContext, aOrder) {
      var context2 = aContext || null;
      var order = aOrder || SourceMapConsumer.GENERATED_ORDER;
      var mappings;
      switch (order) {
        case SourceMapConsumer.GENERATED_ORDER:
          mappings = this._generatedMappings;
          break;
        case SourceMapConsumer.ORIGINAL_ORDER:
          mappings = this._originalMappings;
          break;
        default:
          throw new Error("Unknown order of iteration.");
      }
      var sourceRoot = this.sourceRoot;
      mappings.map(function(mapping) {
        var source = mapping.source === null ? null : this._sources.at(mapping.source);
        source = util2.computeSourceURL(sourceRoot, source, this._sourceMapURL);
        return {
          source,
          generatedLine: mapping.generatedLine,
          generatedColumn: mapping.generatedColumn,
          originalLine: mapping.originalLine,
          originalColumn: mapping.originalColumn,
          name: mapping.name === null ? null : this._names.at(mapping.name)
        };
      }, this).forEach(aCallback, context2);
    }, "SourceMapConsumer_eachMapping");
    SourceMapConsumer.prototype.allGeneratedPositionsFor = /* @__PURE__ */ __name(function SourceMapConsumer_allGeneratedPositionsFor(aArgs) {
      var line = util2.getArg(aArgs, "line");
      var needle = {
        source: util2.getArg(aArgs, "source"),
        originalLine: line,
        originalColumn: util2.getArg(aArgs, "column", 0)
      };
      needle.source = this._findSourceIndex(needle.source);
      if (needle.source < 0) {
        return [];
      }
      var mappings = [];
      var index = this._findMapping(
        needle,
        this._originalMappings,
        "originalLine",
        "originalColumn",
        util2.compareByOriginalPositions,
        binarySearch.LEAST_UPPER_BOUND
      );
      if (index >= 0) {
        var mapping = this._originalMappings[index];
        if (aArgs.column === void 0) {
          var originalLine = mapping.originalLine;
          while (mapping && mapping.originalLine === originalLine) {
            mappings.push({
              line: util2.getArg(mapping, "generatedLine", null),
              column: util2.getArg(mapping, "generatedColumn", null),
              lastColumn: util2.getArg(mapping, "lastGeneratedColumn", null)
            });
            mapping = this._originalMappings[++index];
          }
        } else {
          var originalColumn = mapping.originalColumn;
          while (mapping && mapping.originalLine === line && mapping.originalColumn == originalColumn) {
            mappings.push({
              line: util2.getArg(mapping, "generatedLine", null),
              column: util2.getArg(mapping, "generatedColumn", null),
              lastColumn: util2.getArg(mapping, "lastGeneratedColumn", null)
            });
            mapping = this._originalMappings[++index];
          }
        }
      }
      return mappings;
    }, "SourceMapConsumer_allGeneratedPositionsFor");
    exports.SourceMapConsumer = SourceMapConsumer;
    function BasicSourceMapConsumer(aSourceMap, aSourceMapURL) {
      var sourceMap = aSourceMap;
      if (typeof aSourceMap === "string") {
        sourceMap = util2.parseSourceMapInput(aSourceMap);
      }
      var version = util2.getArg(sourceMap, "version");
      var sources = util2.getArg(sourceMap, "sources");
      var names = util2.getArg(sourceMap, "names", []);
      var sourceRoot = util2.getArg(sourceMap, "sourceRoot", null);
      var sourcesContent = util2.getArg(sourceMap, "sourcesContent", null);
      var mappings = util2.getArg(sourceMap, "mappings");
      var file = util2.getArg(sourceMap, "file", null);
      if (version != this._version) {
        throw new Error("Unsupported version: " + version);
      }
      if (sourceRoot) {
        sourceRoot = util2.normalize(sourceRoot);
      }
      sources = sources.map(String).map(util2.normalize).map(function(source) {
        return sourceRoot && util2.isAbsolute(sourceRoot) && util2.isAbsolute(source) ? util2.relative(sourceRoot, source) : source;
      });
      this._names = ArraySet.fromArray(names.map(String), true);
      this._sources = ArraySet.fromArray(sources, true);
      this._absoluteSources = this._sources.toArray().map(function(s) {
        return util2.computeSourceURL(sourceRoot, s, aSourceMapURL);
      });
      this.sourceRoot = sourceRoot;
      this.sourcesContent = sourcesContent;
      this._mappings = mappings;
      this._sourceMapURL = aSourceMapURL;
      this.file = file;
    }
    __name(BasicSourceMapConsumer, "BasicSourceMapConsumer");
    BasicSourceMapConsumer.prototype = Object.create(SourceMapConsumer.prototype);
    BasicSourceMapConsumer.prototype.consumer = SourceMapConsumer;
    BasicSourceMapConsumer.prototype._findSourceIndex = function(aSource) {
      var relativeSource = aSource;
      if (this.sourceRoot != null) {
        relativeSource = util2.relative(this.sourceRoot, relativeSource);
      }
      if (this._sources.has(relativeSource)) {
        return this._sources.indexOf(relativeSource);
      }
      var i;
      for (i = 0; i < this._absoluteSources.length; ++i) {
        if (this._absoluteSources[i] == aSource) {
          return i;
        }
      }
      return -1;
    };
    BasicSourceMapConsumer.fromSourceMap = /* @__PURE__ */ __name(function SourceMapConsumer_fromSourceMap(aSourceMap, aSourceMapURL) {
      var smc = Object.create(BasicSourceMapConsumer.prototype);
      var names = smc._names = ArraySet.fromArray(aSourceMap._names.toArray(), true);
      var sources = smc._sources = ArraySet.fromArray(aSourceMap._sources.toArray(), true);
      smc.sourceRoot = aSourceMap._sourceRoot;
      smc.sourcesContent = aSourceMap._generateSourcesContent(
        smc._sources.toArray(),
        smc.sourceRoot
      );
      smc.file = aSourceMap._file;
      smc._sourceMapURL = aSourceMapURL;
      smc._absoluteSources = smc._sources.toArray().map(function(s) {
        return util2.computeSourceURL(smc.sourceRoot, s, aSourceMapURL);
      });
      var generatedMappings = aSourceMap._mappings.toArray().slice();
      var destGeneratedMappings = smc.__generatedMappings = [];
      var destOriginalMappings = smc.__originalMappings = [];
      for (var i = 0, length = generatedMappings.length; i < length; i++) {
        var srcMapping = generatedMappings[i];
        var destMapping = new Mapping();
        destMapping.generatedLine = srcMapping.generatedLine;
        destMapping.generatedColumn = srcMapping.generatedColumn;
        if (srcMapping.source) {
          destMapping.source = sources.indexOf(srcMapping.source);
          destMapping.originalLine = srcMapping.originalLine;
          destMapping.originalColumn = srcMapping.originalColumn;
          if (srcMapping.name) {
            destMapping.name = names.indexOf(srcMapping.name);
          }
          destOriginalMappings.push(destMapping);
        }
        destGeneratedMappings.push(destMapping);
      }
      quickSort(smc.__originalMappings, util2.compareByOriginalPositions);
      return smc;
    }, "SourceMapConsumer_fromSourceMap");
    BasicSourceMapConsumer.prototype._version = 3;
    Object.defineProperty(BasicSourceMapConsumer.prototype, "sources", {
      get: /* @__PURE__ */ __name(function() {
        return this._absoluteSources.slice();
      }, "get")
    });
    function Mapping() {
      this.generatedLine = 0;
      this.generatedColumn = 0;
      this.source = null;
      this.originalLine = null;
      this.originalColumn = null;
      this.name = null;
    }
    __name(Mapping, "Mapping");
    BasicSourceMapConsumer.prototype._parseMappings = /* @__PURE__ */ __name(function SourceMapConsumer_parseMappings(aStr, aSourceRoot) {
      var generatedLine = 1;
      var previousGeneratedColumn = 0;
      var previousOriginalLine = 0;
      var previousOriginalColumn = 0;
      var previousSource = 0;
      var previousName = 0;
      var length = aStr.length;
      var index = 0;
      var cachedSegments = {};
      var temp = {};
      var originalMappings = [];
      var generatedMappings = [];
      var mapping, str, segment, end, value2;
      while (index < length) {
        if (aStr.charAt(index) === ";") {
          generatedLine++;
          index++;
          previousGeneratedColumn = 0;
        } else if (aStr.charAt(index) === ",") {
          index++;
        } else {
          mapping = new Mapping();
          mapping.generatedLine = generatedLine;
          for (end = index; end < length; end++) {
            if (this._charIsMappingSeparator(aStr, end)) {
              break;
            }
          }
          str = aStr.slice(index, end);
          segment = cachedSegments[str];
          if (segment) {
            index += str.length;
          } else {
            segment = [];
            while (index < end) {
              base64VLQ.decode(aStr, index, temp);
              value2 = temp.value;
              index = temp.rest;
              segment.push(value2);
            }
            if (segment.length === 2) {
              throw new Error("Found a source, but no line and column");
            }
            if (segment.length === 3) {
              throw new Error("Found a source and line, but no column");
            }
            cachedSegments[str] = segment;
          }
          mapping.generatedColumn = previousGeneratedColumn + segment[0];
          previousGeneratedColumn = mapping.generatedColumn;
          if (segment.length > 1) {
            mapping.source = previousSource + segment[1];
            previousSource += segment[1];
            mapping.originalLine = previousOriginalLine + segment[2];
            previousOriginalLine = mapping.originalLine;
            mapping.originalLine += 1;
            mapping.originalColumn = previousOriginalColumn + segment[3];
            previousOriginalColumn = mapping.originalColumn;
            if (segment.length > 4) {
              mapping.name = previousName + segment[4];
              previousName += segment[4];
            }
          }
          generatedMappings.push(mapping);
          if (typeof mapping.originalLine === "number") {
            originalMappings.push(mapping);
          }
        }
      }
      quickSort(generatedMappings, util2.compareByGeneratedPositionsDeflated);
      this.__generatedMappings = generatedMappings;
      quickSort(originalMappings, util2.compareByOriginalPositions);
      this.__originalMappings = originalMappings;
    }, "SourceMapConsumer_parseMappings");
    BasicSourceMapConsumer.prototype._findMapping = /* @__PURE__ */ __name(function SourceMapConsumer_findMapping(aNeedle, aMappings, aLineName, aColumnName, aComparator, aBias) {
      if (aNeedle[aLineName] <= 0) {
        throw new TypeError("Line must be greater than or equal to 1, got " + aNeedle[aLineName]);
      }
      if (aNeedle[aColumnName] < 0) {
        throw new TypeError("Column must be greater than or equal to 0, got " + aNeedle[aColumnName]);
      }
      return binarySearch.search(aNeedle, aMappings, aComparator, aBias);
    }, "SourceMapConsumer_findMapping");
    BasicSourceMapConsumer.prototype.computeColumnSpans = /* @__PURE__ */ __name(function SourceMapConsumer_computeColumnSpans() {
      for (var index = 0; index < this._generatedMappings.length; ++index) {
        var mapping = this._generatedMappings[index];
        if (index + 1 < this._generatedMappings.length) {
          var nextMapping = this._generatedMappings[index + 1];
          if (mapping.generatedLine === nextMapping.generatedLine) {
            mapping.lastGeneratedColumn = nextMapping.generatedColumn - 1;
            continue;
          }
        }
        mapping.lastGeneratedColumn = Infinity;
      }
    }, "SourceMapConsumer_computeColumnSpans");
    BasicSourceMapConsumer.prototype.originalPositionFor = /* @__PURE__ */ __name(function SourceMapConsumer_originalPositionFor(aArgs) {
      var needle = {
        generatedLine: util2.getArg(aArgs, "line"),
        generatedColumn: util2.getArg(aArgs, "column")
      };
      var index = this._findMapping(
        needle,
        this._generatedMappings,
        "generatedLine",
        "generatedColumn",
        util2.compareByGeneratedPositionsDeflated,
        util2.getArg(aArgs, "bias", SourceMapConsumer.GREATEST_LOWER_BOUND)
      );
      if (index >= 0) {
        var mapping = this._generatedMappings[index];
        if (mapping.generatedLine === needle.generatedLine) {
          var source = util2.getArg(mapping, "source", null);
          if (source !== null) {
            source = this._sources.at(source);
            source = util2.computeSourceURL(this.sourceRoot, source, this._sourceMapURL);
          }
          var name = util2.getArg(mapping, "name", null);
          if (name !== null) {
            name = this._names.at(name);
          }
          return {
            source,
            line: util2.getArg(mapping, "originalLine", null),
            column: util2.getArg(mapping, "originalColumn", null),
            name
          };
        }
      }
      return {
        source: null,
        line: null,
        column: null,
        name: null
      };
    }, "SourceMapConsumer_originalPositionFor");
    BasicSourceMapConsumer.prototype.hasContentsOfAllSources = /* @__PURE__ */ __name(function BasicSourceMapConsumer_hasContentsOfAllSources() {
      if (!this.sourcesContent) {
        return false;
      }
      return this.sourcesContent.length >= this._sources.size() && !this.sourcesContent.some(function(sc) {
        return sc == null;
      });
    }, "BasicSourceMapConsumer_hasContentsOfAllSources");
    BasicSourceMapConsumer.prototype.sourceContentFor = /* @__PURE__ */ __name(function SourceMapConsumer_sourceContentFor(aSource, nullOnMissing) {
      if (!this.sourcesContent) {
        return null;
      }
      var index = this._findSourceIndex(aSource);
      if (index >= 0) {
        return this.sourcesContent[index];
      }
      var relativeSource = aSource;
      if (this.sourceRoot != null) {
        relativeSource = util2.relative(this.sourceRoot, relativeSource);
      }
      var url;
      if (this.sourceRoot != null && (url = util2.urlParse(this.sourceRoot))) {
        var fileUriAbsPath = relativeSource.replace(/^file:\/\//, "");
        if (url.scheme == "file" && this._sources.has(fileUriAbsPath)) {
          return this.sourcesContent[this._sources.indexOf(fileUriAbsPath)];
        }
        if ((!url.path || url.path == "/") && this._sources.has("/" + relativeSource)) {
          return this.sourcesContent[this._sources.indexOf("/" + relativeSource)];
        }
      }
      if (nullOnMissing) {
        return null;
      } else {
        throw new Error('"' + relativeSource + '" is not in the SourceMap.');
      }
    }, "SourceMapConsumer_sourceContentFor");
    BasicSourceMapConsumer.prototype.generatedPositionFor = /* @__PURE__ */ __name(function SourceMapConsumer_generatedPositionFor(aArgs) {
      var source = util2.getArg(aArgs, "source");
      source = this._findSourceIndex(source);
      if (source < 0) {
        return {
          line: null,
          column: null,
          lastColumn: null
        };
      }
      var needle = {
        source,
        originalLine: util2.getArg(aArgs, "line"),
        originalColumn: util2.getArg(aArgs, "column")
      };
      var index = this._findMapping(
        needle,
        this._originalMappings,
        "originalLine",
        "originalColumn",
        util2.compareByOriginalPositions,
        util2.getArg(aArgs, "bias", SourceMapConsumer.GREATEST_LOWER_BOUND)
      );
      if (index >= 0) {
        var mapping = this._originalMappings[index];
        if (mapping.source === needle.source) {
          return {
            line: util2.getArg(mapping, "generatedLine", null),
            column: util2.getArg(mapping, "generatedColumn", null),
            lastColumn: util2.getArg(mapping, "lastGeneratedColumn", null)
          };
        }
      }
      return {
        line: null,
        column: null,
        lastColumn: null
      };
    }, "SourceMapConsumer_generatedPositionFor");
    exports.BasicSourceMapConsumer = BasicSourceMapConsumer;
    function IndexedSourceMapConsumer(aSourceMap, aSourceMapURL) {
      var sourceMap = aSourceMap;
      if (typeof aSourceMap === "string") {
        sourceMap = util2.parseSourceMapInput(aSourceMap);
      }
      var version = util2.getArg(sourceMap, "version");
      var sections = util2.getArg(sourceMap, "sections");
      if (version != this._version) {
        throw new Error("Unsupported version: " + version);
      }
      this._sources = new ArraySet();
      this._names = new ArraySet();
      var lastOffset = {
        line: -1,
        column: 0
      };
      this._sections = sections.map(function(s) {
        if (s.url) {
          throw new Error("Support for url field in sections not implemented.");
        }
        var offset = util2.getArg(s, "offset");
        var offsetLine = util2.getArg(offset, "line");
        var offsetColumn = util2.getArg(offset, "column");
        if (offsetLine < lastOffset.line || offsetLine === lastOffset.line && offsetColumn < lastOffset.column) {
          throw new Error("Section offsets must be ordered and non-overlapping.");
        }
        lastOffset = offset;
        return {
          generatedOffset: {
            // The offset fields are 0-based, but we use 1-based indices when
            // encoding/decoding from VLQ.
            generatedLine: offsetLine + 1,
            generatedColumn: offsetColumn + 1
          },
          consumer: new SourceMapConsumer(util2.getArg(s, "map"), aSourceMapURL)
        };
      });
    }
    __name(IndexedSourceMapConsumer, "IndexedSourceMapConsumer");
    IndexedSourceMapConsumer.prototype = Object.create(SourceMapConsumer.prototype);
    IndexedSourceMapConsumer.prototype.constructor = SourceMapConsumer;
    IndexedSourceMapConsumer.prototype._version = 3;
    Object.defineProperty(IndexedSourceMapConsumer.prototype, "sources", {
      get: /* @__PURE__ */ __name(function() {
        var sources = [];
        for (var i = 0; i < this._sections.length; i++) {
          for (var j = 0; j < this._sections[i].consumer.sources.length; j++) {
            sources.push(this._sections[i].consumer.sources[j]);
          }
        }
        return sources;
      }, "get")
    });
    IndexedSourceMapConsumer.prototype.originalPositionFor = /* @__PURE__ */ __name(function IndexedSourceMapConsumer_originalPositionFor(aArgs) {
      var needle = {
        generatedLine: util2.getArg(aArgs, "line"),
        generatedColumn: util2.getArg(aArgs, "column")
      };
      var sectionIndex = binarySearch.search(
        needle,
        this._sections,
        function(needle2, section2) {
          var cmp = needle2.generatedLine - section2.generatedOffset.generatedLine;
          if (cmp) {
            return cmp;
          }
          return needle2.generatedColumn - section2.generatedOffset.generatedColumn;
        }
      );
      var section = this._sections[sectionIndex];
      if (!section) {
        return {
          source: null,
          line: null,
          column: null,
          name: null
        };
      }
      return section.consumer.originalPositionFor({
        line: needle.generatedLine - (section.generatedOffset.generatedLine - 1),
        column: needle.generatedColumn - (section.generatedOffset.generatedLine === needle.generatedLine ? section.generatedOffset.generatedColumn - 1 : 0),
        bias: aArgs.bias
      });
    }, "IndexedSourceMapConsumer_originalPositionFor");
    IndexedSourceMapConsumer.prototype.hasContentsOfAllSources = /* @__PURE__ */ __name(function IndexedSourceMapConsumer_hasContentsOfAllSources() {
      return this._sections.every(function(s) {
        return s.consumer.hasContentsOfAllSources();
      });
    }, "IndexedSourceMapConsumer_hasContentsOfAllSources");
    IndexedSourceMapConsumer.prototype.sourceContentFor = /* @__PURE__ */ __name(function IndexedSourceMapConsumer_sourceContentFor(aSource, nullOnMissing) {
      for (var i = 0; i < this._sections.length; i++) {
        var section = this._sections[i];
        var content = section.consumer.sourceContentFor(aSource, true);
        if (content) {
          return content;
        }
      }
      if (nullOnMissing) {
        return null;
      } else {
        throw new Error('"' + aSource + '" is not in the SourceMap.');
      }
    }, "IndexedSourceMapConsumer_sourceContentFor");
    IndexedSourceMapConsumer.prototype.generatedPositionFor = /* @__PURE__ */ __name(function IndexedSourceMapConsumer_generatedPositionFor(aArgs) {
      for (var i = 0; i < this._sections.length; i++) {
        var section = this._sections[i];
        if (section.consumer._findSourceIndex(util2.getArg(aArgs, "source")) === -1) {
          continue;
        }
        var generatedPosition = section.consumer.generatedPositionFor(aArgs);
        if (generatedPosition) {
          var ret = {
            line: generatedPosition.line + (section.generatedOffset.generatedLine - 1),
            column: generatedPosition.column + (section.generatedOffset.generatedLine === generatedPosition.line ? section.generatedOffset.generatedColumn - 1 : 0)
          };
          return ret;
        }
      }
      return {
        line: null,
        column: null
      };
    }, "IndexedSourceMapConsumer_generatedPositionFor");
    IndexedSourceMapConsumer.prototype._parseMappings = /* @__PURE__ */ __name(function IndexedSourceMapConsumer_parseMappings(aStr, aSourceRoot) {
      this.__generatedMappings = [];
      this.__originalMappings = [];
      for (var i = 0; i < this._sections.length; i++) {
        var section = this._sections[i];
        var sectionMappings = section.consumer._generatedMappings;
        for (var j = 0; j < sectionMappings.length; j++) {
          var mapping = sectionMappings[j];
          var source = section.consumer._sources.at(mapping.source);
          source = util2.computeSourceURL(section.consumer.sourceRoot, source, this._sourceMapURL);
          this._sources.add(source);
          source = this._sources.indexOf(source);
          var name = null;
          if (mapping.name) {
            name = section.consumer._names.at(mapping.name);
            this._names.add(name);
            name = this._names.indexOf(name);
          }
          var adjustedMapping = {
            source,
            generatedLine: mapping.generatedLine + (section.generatedOffset.generatedLine - 1),
            generatedColumn: mapping.generatedColumn + (section.generatedOffset.generatedLine === mapping.generatedLine ? section.generatedOffset.generatedColumn - 1 : 0),
            originalLine: mapping.originalLine,
            originalColumn: mapping.originalColumn,
            name
          };
          this.__generatedMappings.push(adjustedMapping);
          if (typeof adjustedMapping.originalLine === "number") {
            this.__originalMappings.push(adjustedMapping);
          }
        }
      }
      quickSort(this.__generatedMappings, util2.compareByGeneratedPositionsDeflated);
      quickSort(this.__originalMappings, util2.compareByOriginalPositions);
    }, "IndexedSourceMapConsumer_parseMappings");
    exports.IndexedSourceMapConsumer = IndexedSourceMapConsumer;
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/source-node.js
var require_source_node = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/lib/source-node.js"(exports) {
    init_esm();
    var SourceMapGenerator = require_source_map_generator().SourceMapGenerator;
    var util2 = require_util3();
    var REGEX_NEWLINE = /(\r?\n)/;
    var NEWLINE_CODE = 10;
    var isSourceNode = "$$$isSourceNode$$$";
    function SourceNode(aLine, aColumn, aSource, aChunks, aName) {
      this.children = [];
      this.sourceContents = {};
      this.line = aLine == null ? null : aLine;
      this.column = aColumn == null ? null : aColumn;
      this.source = aSource == null ? null : aSource;
      this.name = aName == null ? null : aName;
      this[isSourceNode] = true;
      if (aChunks != null) this.add(aChunks);
    }
    __name(SourceNode, "SourceNode");
    SourceNode.fromStringWithSourceMap = /* @__PURE__ */ __name(function SourceNode_fromStringWithSourceMap(aGeneratedCode, aSourceMapConsumer, aRelativePath) {
      var node = new SourceNode();
      var remainingLines = aGeneratedCode.split(REGEX_NEWLINE);
      var remainingLinesIndex = 0;
      var shiftNextLine = /* @__PURE__ */ __name(function() {
        var lineContents = getNextLine();
        var newLine = getNextLine() || "";
        return lineContents + newLine;
        function getNextLine() {
          return remainingLinesIndex < remainingLines.length ? remainingLines[remainingLinesIndex++] : void 0;
        }
        __name(getNextLine, "getNextLine");
      }, "shiftNextLine");
      var lastGeneratedLine = 1, lastGeneratedColumn = 0;
      var lastMapping = null;
      aSourceMapConsumer.eachMapping(function(mapping) {
        if (lastMapping !== null) {
          if (lastGeneratedLine < mapping.generatedLine) {
            addMappingWithCode(lastMapping, shiftNextLine());
            lastGeneratedLine++;
            lastGeneratedColumn = 0;
          } else {
            var nextLine = remainingLines[remainingLinesIndex] || "";
            var code = nextLine.substr(0, mapping.generatedColumn - lastGeneratedColumn);
            remainingLines[remainingLinesIndex] = nextLine.substr(mapping.generatedColumn - lastGeneratedColumn);
            lastGeneratedColumn = mapping.generatedColumn;
            addMappingWithCode(lastMapping, code);
            lastMapping = mapping;
            return;
          }
        }
        while (lastGeneratedLine < mapping.generatedLine) {
          node.add(shiftNextLine());
          lastGeneratedLine++;
        }
        if (lastGeneratedColumn < mapping.generatedColumn) {
          var nextLine = remainingLines[remainingLinesIndex] || "";
          node.add(nextLine.substr(0, mapping.generatedColumn));
          remainingLines[remainingLinesIndex] = nextLine.substr(mapping.generatedColumn);
          lastGeneratedColumn = mapping.generatedColumn;
        }
        lastMapping = mapping;
      }, this);
      if (remainingLinesIndex < remainingLines.length) {
        if (lastMapping) {
          addMappingWithCode(lastMapping, shiftNextLine());
        }
        node.add(remainingLines.splice(remainingLinesIndex).join(""));
      }
      aSourceMapConsumer.sources.forEach(function(sourceFile) {
        var content = aSourceMapConsumer.sourceContentFor(sourceFile);
        if (content != null) {
          if (aRelativePath != null) {
            sourceFile = util2.join(aRelativePath, sourceFile);
          }
          node.setSourceContent(sourceFile, content);
        }
      });
      return node;
      function addMappingWithCode(mapping, code) {
        if (mapping === null || mapping.source === void 0) {
          node.add(code);
        } else {
          var source = aRelativePath ? util2.join(aRelativePath, mapping.source) : mapping.source;
          node.add(new SourceNode(
            mapping.originalLine,
            mapping.originalColumn,
            source,
            code,
            mapping.name
          ));
        }
      }
      __name(addMappingWithCode, "addMappingWithCode");
    }, "SourceNode_fromStringWithSourceMap");
    SourceNode.prototype.add = /* @__PURE__ */ __name(function SourceNode_add(aChunk) {
      if (Array.isArray(aChunk)) {
        aChunk.forEach(function(chunk) {
          this.add(chunk);
        }, this);
      } else if (aChunk[isSourceNode] || typeof aChunk === "string") {
        if (aChunk) {
          this.children.push(aChunk);
        }
      } else {
        throw new TypeError(
          "Expected a SourceNode, string, or an array of SourceNodes and strings. Got " + aChunk
        );
      }
      return this;
    }, "SourceNode_add");
    SourceNode.prototype.prepend = /* @__PURE__ */ __name(function SourceNode_prepend(aChunk) {
      if (Array.isArray(aChunk)) {
        for (var i = aChunk.length - 1; i >= 0; i--) {
          this.prepend(aChunk[i]);
        }
      } else if (aChunk[isSourceNode] || typeof aChunk === "string") {
        this.children.unshift(aChunk);
      } else {
        throw new TypeError(
          "Expected a SourceNode, string, or an array of SourceNodes and strings. Got " + aChunk
        );
      }
      return this;
    }, "SourceNode_prepend");
    SourceNode.prototype.walk = /* @__PURE__ */ __name(function SourceNode_walk(aFn) {
      var chunk;
      for (var i = 0, len = this.children.length; i < len; i++) {
        chunk = this.children[i];
        if (chunk[isSourceNode]) {
          chunk.walk(aFn);
        } else {
          if (chunk !== "") {
            aFn(chunk, {
              source: this.source,
              line: this.line,
              column: this.column,
              name: this.name
            });
          }
        }
      }
    }, "SourceNode_walk");
    SourceNode.prototype.join = /* @__PURE__ */ __name(function SourceNode_join(aSep) {
      var newChildren;
      var i;
      var len = this.children.length;
      if (len > 0) {
        newChildren = [];
        for (i = 0; i < len - 1; i++) {
          newChildren.push(this.children[i]);
          newChildren.push(aSep);
        }
        newChildren.push(this.children[i]);
        this.children = newChildren;
      }
      return this;
    }, "SourceNode_join");
    SourceNode.prototype.replaceRight = /* @__PURE__ */ __name(function SourceNode_replaceRight(aPattern, aReplacement) {
      var lastChild = this.children[this.children.length - 1];
      if (lastChild[isSourceNode]) {
        lastChild.replaceRight(aPattern, aReplacement);
      } else if (typeof lastChild === "string") {
        this.children[this.children.length - 1] = lastChild.replace(aPattern, aReplacement);
      } else {
        this.children.push("".replace(aPattern, aReplacement));
      }
      return this;
    }, "SourceNode_replaceRight");
    SourceNode.prototype.setSourceContent = /* @__PURE__ */ __name(function SourceNode_setSourceContent(aSourceFile, aSourceContent) {
      this.sourceContents[util2.toSetString(aSourceFile)] = aSourceContent;
    }, "SourceNode_setSourceContent");
    SourceNode.prototype.walkSourceContents = /* @__PURE__ */ __name(function SourceNode_walkSourceContents(aFn) {
      for (var i = 0, len = this.children.length; i < len; i++) {
        if (this.children[i][isSourceNode]) {
          this.children[i].walkSourceContents(aFn);
        }
      }
      var sources = Object.keys(this.sourceContents);
      for (var i = 0, len = sources.length; i < len; i++) {
        aFn(util2.fromSetString(sources[i]), this.sourceContents[sources[i]]);
      }
    }, "SourceNode_walkSourceContents");
    SourceNode.prototype.toString = /* @__PURE__ */ __name(function SourceNode_toString() {
      var str = "";
      this.walk(function(chunk) {
        str += chunk;
      });
      return str;
    }, "SourceNode_toString");
    SourceNode.prototype.toStringWithSourceMap = /* @__PURE__ */ __name(function SourceNode_toStringWithSourceMap(aArgs) {
      var generated = {
        code: "",
        line: 1,
        column: 0
      };
      var map = new SourceMapGenerator(aArgs);
      var sourceMappingActive = false;
      var lastOriginalSource = null;
      var lastOriginalLine = null;
      var lastOriginalColumn = null;
      var lastOriginalName = null;
      this.walk(function(chunk, original) {
        generated.code += chunk;
        if (original.source !== null && original.line !== null && original.column !== null) {
          if (lastOriginalSource !== original.source || lastOriginalLine !== original.line || lastOriginalColumn !== original.column || lastOriginalName !== original.name) {
            map.addMapping({
              source: original.source,
              original: {
                line: original.line,
                column: original.column
              },
              generated: {
                line: generated.line,
                column: generated.column
              },
              name: original.name
            });
          }
          lastOriginalSource = original.source;
          lastOriginalLine = original.line;
          lastOriginalColumn = original.column;
          lastOriginalName = original.name;
          sourceMappingActive = true;
        } else if (sourceMappingActive) {
          map.addMapping({
            generated: {
              line: generated.line,
              column: generated.column
            }
          });
          lastOriginalSource = null;
          sourceMappingActive = false;
        }
        for (var idx = 0, length = chunk.length; idx < length; idx++) {
          if (chunk.charCodeAt(idx) === NEWLINE_CODE) {
            generated.line++;
            generated.column = 0;
            if (idx + 1 === length) {
              lastOriginalSource = null;
              sourceMappingActive = false;
            } else if (sourceMappingActive) {
              map.addMapping({
                source: original.source,
                original: {
                  line: original.line,
                  column: original.column
                },
                generated: {
                  line: generated.line,
                  column: generated.column
                },
                name: original.name
              });
            }
          } else {
            generated.column++;
          }
        }
      });
      this.walkSourceContents(function(sourceFile, sourceContent) {
        map.setSourceContent(sourceFile, sourceContent);
      });
      return { code: generated.code, map };
    }, "SourceNode_toStringWithSourceMap");
    exports.SourceNode = SourceNode;
  }
});

// ../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/source-map.js
var require_source_map = __commonJS({
  "../../node_modules/.bun/source-map@0.6.1/node_modules/source-map/source-map.js"(exports) {
    init_esm();
    exports.SourceMapGenerator = require_source_map_generator().SourceMapGenerator;
    exports.SourceMapConsumer = require_source_map_consumer().SourceMapConsumer;
    exports.SourceNode = require_source_node().SourceNode;
  }
});

// ../../node_modules/.bun/buffer-from@1.1.2/node_modules/buffer-from/index.js
var require_buffer_from = __commonJS({
  "../../node_modules/.bun/buffer-from@1.1.2/node_modules/buffer-from/index.js"(exports, module) {
    init_esm();
    var toString = Object.prototype.toString;
    var isModern = typeof Buffer !== "undefined" && typeof Buffer.alloc === "function" && typeof Buffer.allocUnsafe === "function" && typeof Buffer.from === "function";
    function isArrayBuffer(input) {
      return toString.call(input).slice(8, -1) === "ArrayBuffer";
    }
    __name(isArrayBuffer, "isArrayBuffer");
    function fromArrayBuffer(obj, byteOffset, length) {
      byteOffset >>>= 0;
      var maxLength = obj.byteLength - byteOffset;
      if (maxLength < 0) {
        throw new RangeError("'offset' is out of bounds");
      }
      if (length === void 0) {
        length = maxLength;
      } else {
        length >>>= 0;
        if (length > maxLength) {
          throw new RangeError("'length' is out of bounds");
        }
      }
      return isModern ? Buffer.from(obj.slice(byteOffset, byteOffset + length)) : new Buffer(new Uint8Array(obj.slice(byteOffset, byteOffset + length)));
    }
    __name(fromArrayBuffer, "fromArrayBuffer");
    function fromString(string, encoding) {
      if (typeof encoding !== "string" || encoding === "") {
        encoding = "utf8";
      }
      if (!Buffer.isEncoding(encoding)) {
        throw new TypeError('"encoding" must be a valid string encoding');
      }
      return isModern ? Buffer.from(string, encoding) : new Buffer(string, encoding);
    }
    __name(fromString, "fromString");
    function bufferFrom(value2, encodingOrOffset, length) {
      if (typeof value2 === "number") {
        throw new TypeError('"value" argument must not be a number');
      }
      if (isArrayBuffer(value2)) {
        return fromArrayBuffer(value2, encodingOrOffset, length);
      }
      if (typeof value2 === "string") {
        return fromString(value2, encodingOrOffset);
      }
      return isModern ? Buffer.from(value2) : new Buffer(value2);
    }
    __name(bufferFrom, "bufferFrom");
    module.exports = bufferFrom;
  }
});

// ../../node_modules/.bun/source-map-support@0.5.21/node_modules/source-map-support/source-map-support.js
var require_source_map_support = __commonJS({
  "../../node_modules/.bun/source-map-support@0.5.21/node_modules/source-map-support/source-map-support.js"(exports, module) {
    init_esm();
    var SourceMapConsumer = require_source_map().SourceMapConsumer;
    var path2 = __require("path");
    var fs4;
    try {
      fs4 = __require("fs");
      if (!fs4.existsSync || !fs4.readFileSync) {
        fs4 = null;
      }
    } catch (err2) {
    }
    var bufferFrom = require_buffer_from();
    function dynamicRequire(mod, request) {
      return mod.require(request);
    }
    __name(dynamicRequire, "dynamicRequire");
    var errorFormatterInstalled = false;
    var uncaughtShimInstalled = false;
    var emptyCacheBetweenOperations = false;
    var environment = "auto";
    var fileContentsCache = {};
    var sourceMapCache = {};
    var reSourceMap = /^data:application\/json[^,]+base64,/;
    var retrieveFileHandlers = [];
    var retrieveMapHandlers = [];
    function isInBrowser() {
      if (environment === "browser")
        return true;
      if (environment === "node")
        return false;
      return typeof window !== "undefined" && typeof XMLHttpRequest === "function" && !(window.require && window.module && window.process && window.process.type === "renderer");
    }
    __name(isInBrowser, "isInBrowser");
    function hasGlobalProcessEventEmitter() {
      return typeof process === "object" && process !== null && typeof process.on === "function";
    }
    __name(hasGlobalProcessEventEmitter, "hasGlobalProcessEventEmitter");
    function globalProcessVersion() {
      if (typeof process === "object" && process !== null) {
        return process.version;
      } else {
        return "";
      }
    }
    __name(globalProcessVersion, "globalProcessVersion");
    function globalProcessStderr() {
      if (typeof process === "object" && process !== null) {
        return process.stderr;
      }
    }
    __name(globalProcessStderr, "globalProcessStderr");
    function globalProcessExit(code) {
      if (typeof process === "object" && process !== null && typeof process.exit === "function") {
        return process.exit(code);
      }
    }
    __name(globalProcessExit, "globalProcessExit");
    function handlerExec(list) {
      return function(arg) {
        for (var i = 0; i < list.length; i++) {
          var ret = list[i](arg);
          if (ret) {
            return ret;
          }
        }
        return null;
      };
    }
    __name(handlerExec, "handlerExec");
    var retrieveFile = handlerExec(retrieveFileHandlers);
    retrieveFileHandlers.push(function(path3) {
      path3 = path3.trim();
      if (/^file:/.test(path3)) {
        path3 = path3.replace(/file:\/\/\/(\w:)?/, function(protocol, drive) {
          return drive ? "" : (
            // file:///C:/dir/file -> C:/dir/file
            "/"
          );
        });
      }
      if (path3 in fileContentsCache) {
        return fileContentsCache[path3];
      }
      var contents = "";
      try {
        if (!fs4) {
          var xhr = new XMLHttpRequest();
          xhr.open(
            "GET",
            path3,
            /** async */
            false
          );
          xhr.send(null);
          if (xhr.readyState === 4 && xhr.status === 200) {
            contents = xhr.responseText;
          }
        } else if (fs4.existsSync(path3)) {
          contents = fs4.readFileSync(path3, "utf8");
        }
      } catch (er) {
      }
      return fileContentsCache[path3] = contents;
    });
    function supportRelativeURL(file, url) {
      if (!file) return url;
      var dir = path2.dirname(file);
      var match = /^\w+:\/\/[^\/]*/.exec(dir);
      var protocol = match ? match[0] : "";
      var startPath = dir.slice(protocol.length);
      if (protocol && /^\/\w\:/.test(startPath)) {
        protocol += "/";
        return protocol + path2.resolve(dir.slice(protocol.length), url).replace(/\\/g, "/");
      }
      return protocol + path2.resolve(dir.slice(protocol.length), url);
    }
    __name(supportRelativeURL, "supportRelativeURL");
    function retrieveSourceMapURL(source) {
      var fileData;
      if (isInBrowser()) {
        try {
          var xhr = new XMLHttpRequest();
          xhr.open("GET", source, false);
          xhr.send(null);
          fileData = xhr.readyState === 4 ? xhr.responseText : null;
          var sourceMapHeader = xhr.getResponseHeader("SourceMap") || xhr.getResponseHeader("X-SourceMap");
          if (sourceMapHeader) {
            return sourceMapHeader;
          }
        } catch (e) {
        }
      }
      fileData = retrieveFile(source);
      var re = /(?:\/\/[@#][\s]*sourceMappingURL=([^\s'"]+)[\s]*$)|(?:\/\*[@#][\s]*sourceMappingURL=([^\s*'"]+)[\s]*(?:\*\/)[\s]*$)/mg;
      var lastMatch, match;
      while (match = re.exec(fileData)) lastMatch = match;
      if (!lastMatch) return null;
      return lastMatch[1];
    }
    __name(retrieveSourceMapURL, "retrieveSourceMapURL");
    var retrieveSourceMap = handlerExec(retrieveMapHandlers);
    retrieveMapHandlers.push(function(source) {
      var sourceMappingURL = retrieveSourceMapURL(source);
      if (!sourceMappingURL) return null;
      var sourceMapData;
      if (reSourceMap.test(sourceMappingURL)) {
        var rawData = sourceMappingURL.slice(sourceMappingURL.indexOf(",") + 1);
        sourceMapData = bufferFrom(rawData, "base64").toString();
        sourceMappingURL = source;
      } else {
        sourceMappingURL = supportRelativeURL(source, sourceMappingURL);
        sourceMapData = retrieveFile(sourceMappingURL);
      }
      if (!sourceMapData) {
        return null;
      }
      return {
        url: sourceMappingURL,
        map: sourceMapData
      };
    });
    function mapSourcePosition(position) {
      var sourceMap = sourceMapCache[position.source];
      if (!sourceMap) {
        var urlAndMap = retrieveSourceMap(position.source);
        if (urlAndMap) {
          sourceMap = sourceMapCache[position.source] = {
            url: urlAndMap.url,
            map: new SourceMapConsumer(urlAndMap.map)
          };
          if (sourceMap.map.sourcesContent) {
            sourceMap.map.sources.forEach(function(source, i) {
              var contents = sourceMap.map.sourcesContent[i];
              if (contents) {
                var url = supportRelativeURL(sourceMap.url, source);
                fileContentsCache[url] = contents;
              }
            });
          }
        } else {
          sourceMap = sourceMapCache[position.source] = {
            url: null,
            map: null
          };
        }
      }
      if (sourceMap && sourceMap.map && typeof sourceMap.map.originalPositionFor === "function") {
        var originalPosition = sourceMap.map.originalPositionFor(position);
        if (originalPosition.source !== null) {
          originalPosition.source = supportRelativeURL(
            sourceMap.url,
            originalPosition.source
          );
          return originalPosition;
        }
      }
      return position;
    }
    __name(mapSourcePosition, "mapSourcePosition");
    function mapEvalOrigin(origin) {
      var match = /^eval at ([^(]+) \((.+):(\d+):(\d+)\)$/.exec(origin);
      if (match) {
        var position = mapSourcePosition({
          source: match[2],
          line: +match[3],
          column: match[4] - 1
        });
        return "eval at " + match[1] + " (" + position.source + ":" + position.line + ":" + (position.column + 1) + ")";
      }
      match = /^eval at ([^(]+) \((.+)\)$/.exec(origin);
      if (match) {
        return "eval at " + match[1] + " (" + mapEvalOrigin(match[2]) + ")";
      }
      return origin;
    }
    __name(mapEvalOrigin, "mapEvalOrigin");
    function CallSiteToString() {
      var fileName;
      var fileLocation = "";
      if (this.isNative()) {
        fileLocation = "native";
      } else {
        fileName = this.getScriptNameOrSourceURL();
        if (!fileName && this.isEval()) {
          fileLocation = this.getEvalOrigin();
          fileLocation += ", ";
        }
        if (fileName) {
          fileLocation += fileName;
        } else {
          fileLocation += "<anonymous>";
        }
        var lineNumber = this.getLineNumber();
        if (lineNumber != null) {
          fileLocation += ":" + lineNumber;
          var columnNumber = this.getColumnNumber();
          if (columnNumber) {
            fileLocation += ":" + columnNumber;
          }
        }
      }
      var line = "";
      var functionName = this.getFunctionName();
      var addSuffix = true;
      var isConstructor = this.isConstructor();
      var isMethodCall = !(this.isToplevel() || isConstructor);
      if (isMethodCall) {
        var typeName = this.getTypeName();
        if (typeName === "[object Object]") {
          typeName = "null";
        }
        var methodName = this.getMethodName();
        if (functionName) {
          if (typeName && functionName.indexOf(typeName) != 0) {
            line += typeName + ".";
          }
          line += functionName;
          if (methodName && functionName.indexOf("." + methodName) != functionName.length - methodName.length - 1) {
            line += " [as " + methodName + "]";
          }
        } else {
          line += typeName + "." + (methodName || "<anonymous>");
        }
      } else if (isConstructor) {
        line += "new " + (functionName || "<anonymous>");
      } else if (functionName) {
        line += functionName;
      } else {
        line += fileLocation;
        addSuffix = false;
      }
      if (addSuffix) {
        line += " (" + fileLocation + ")";
      }
      return line;
    }
    __name(CallSiteToString, "CallSiteToString");
    function cloneCallSite(frame) {
      var object = {};
      Object.getOwnPropertyNames(Object.getPrototypeOf(frame)).forEach(function(name) {
        object[name] = /^(?:is|get)/.test(name) ? function() {
          return frame[name].call(frame);
        } : frame[name];
      });
      object.toString = CallSiteToString;
      return object;
    }
    __name(cloneCallSite, "cloneCallSite");
    function wrapCallSite(frame, state) {
      if (state === void 0) {
        state = { nextPosition: null, curPosition: null };
      }
      if (frame.isNative()) {
        state.curPosition = null;
        return frame;
      }
      var source = frame.getFileName() || frame.getScriptNameOrSourceURL();
      if (source) {
        var line = frame.getLineNumber();
        var column = frame.getColumnNumber() - 1;
        var noHeader = /^v(10\.1[6-9]|10\.[2-9][0-9]|10\.[0-9]{3,}|1[2-9]\d*|[2-9]\d|\d{3,}|11\.11)/;
        var headerLength = noHeader.test(globalProcessVersion()) ? 0 : 62;
        if (line === 1 && column > headerLength && !isInBrowser() && !frame.isEval()) {
          column -= headerLength;
        }
        var position = mapSourcePosition({
          source,
          line,
          column
        });
        state.curPosition = position;
        frame = cloneCallSite(frame);
        var originalFunctionName = frame.getFunctionName;
        frame.getFunctionName = function() {
          if (state.nextPosition == null) {
            return originalFunctionName();
          }
          return state.nextPosition.name || originalFunctionName();
        };
        frame.getFileName = function() {
          return position.source;
        };
        frame.getLineNumber = function() {
          return position.line;
        };
        frame.getColumnNumber = function() {
          return position.column + 1;
        };
        frame.getScriptNameOrSourceURL = function() {
          return position.source;
        };
        return frame;
      }
      var origin = frame.isEval() && frame.getEvalOrigin();
      if (origin) {
        origin = mapEvalOrigin(origin);
        frame = cloneCallSite(frame);
        frame.getEvalOrigin = function() {
          return origin;
        };
        return frame;
      }
      return frame;
    }
    __name(wrapCallSite, "wrapCallSite");
    function prepareStackTrace(error, stack) {
      if (emptyCacheBetweenOperations) {
        fileContentsCache = {};
        sourceMapCache = {};
      }
      var name = error.name || "Error";
      var message = error.message || "";
      var errorString = name + ": " + message;
      var state = { nextPosition: null, curPosition: null };
      var processedStack = [];
      for (var i = stack.length - 1; i >= 0; i--) {
        processedStack.push("\n    at " + wrapCallSite(stack[i], state));
        state.nextPosition = state.curPosition;
      }
      state.curPosition = state.nextPosition = null;
      return errorString + processedStack.reverse().join("");
    }
    __name(prepareStackTrace, "prepareStackTrace");
    function getErrorSource(error) {
      var match = /\n    at [^(]+ \((.*):(\d+):(\d+)\)/.exec(error.stack);
      if (match) {
        var source = match[1];
        var line = +match[2];
        var column = +match[3];
        var contents = fileContentsCache[source];
        if (!contents && fs4 && fs4.existsSync(source)) {
          try {
            contents = fs4.readFileSync(source, "utf8");
          } catch (er) {
            contents = "";
          }
        }
        if (contents) {
          var code = contents.split(/(?:\r\n|\r|\n)/)[line - 1];
          if (code) {
            return source + ":" + line + "\n" + code + "\n" + new Array(column).join(" ") + "^";
          }
        }
      }
      return null;
    }
    __name(getErrorSource, "getErrorSource");
    function printErrorAndExit(error) {
      var source = getErrorSource(error);
      var stderr = globalProcessStderr();
      if (stderr && stderr._handle && stderr._handle.setBlocking) {
        stderr._handle.setBlocking(true);
      }
      if (source) {
        console.error();
        console.error(source);
      }
      console.error(error.stack);
      globalProcessExit(1);
    }
    __name(printErrorAndExit, "printErrorAndExit");
    function shimEmitUncaughtException() {
      var origEmit = process.emit;
      process.emit = function(type) {
        if (type === "uncaughtException") {
          var hasStack = arguments[1] && arguments[1].stack;
          var hasListeners = this.listeners(type).length > 0;
          if (hasStack && !hasListeners) {
            return printErrorAndExit(arguments[1]);
          }
        }
        return origEmit.apply(this, arguments);
      };
    }
    __name(shimEmitUncaughtException, "shimEmitUncaughtException");
    var originalRetrieveFileHandlers = retrieveFileHandlers.slice(0);
    var originalRetrieveMapHandlers = retrieveMapHandlers.slice(0);
    exports.wrapCallSite = wrapCallSite;
    exports.getErrorSource = getErrorSource;
    exports.mapSourcePosition = mapSourcePosition;
    exports.retrieveSourceMap = retrieveSourceMap;
    exports.install = function(options) {
      options = options || {};
      if (options.environment) {
        environment = options.environment;
        if (["node", "browser", "auto"].indexOf(environment) === -1) {
          throw new Error("environment " + environment + " was unknown. Available options are {auto, browser, node}");
        }
      }
      if (options.retrieveFile) {
        if (options.overrideRetrieveFile) {
          retrieveFileHandlers.length = 0;
        }
        retrieveFileHandlers.unshift(options.retrieveFile);
      }
      if (options.retrieveSourceMap) {
        if (options.overrideRetrieveSourceMap) {
          retrieveMapHandlers.length = 0;
        }
        retrieveMapHandlers.unshift(options.retrieveSourceMap);
      }
      if (options.hookRequire && !isInBrowser()) {
        var Module = dynamicRequire(module, "module");
        var $compile = Module.prototype._compile;
        if (!$compile.__sourceMapSupport) {
          Module.prototype._compile = function(content, filename) {
            fileContentsCache[filename] = content;
            sourceMapCache[filename] = void 0;
            return $compile.call(this, content, filename);
          };
          Module.prototype._compile.__sourceMapSupport = true;
        }
      }
      if (!emptyCacheBetweenOperations) {
        emptyCacheBetweenOperations = "emptyCacheBetweenOperations" in options ? options.emptyCacheBetweenOperations : false;
      }
      if (!errorFormatterInstalled) {
        errorFormatterInstalled = true;
        Error.prepareStackTrace = prepareStackTrace;
      }
      if (!uncaughtShimInstalled) {
        var installHandler = "handleUncaughtExceptions" in options ? options.handleUncaughtExceptions : true;
        try {
          var worker_threads = dynamicRequire(module, "worker_threads");
          if (worker_threads.isMainThread === false) {
            installHandler = false;
          }
        } catch (e) {
        }
        if (installHandler && hasGlobalProcessEventEmitter()) {
          uncaughtShimInstalled = true;
          shimEmitUncaughtException();
        }
      }
    };
    exports.resetRetrieveHandlers = function() {
      retrieveFileHandlers.length = 0;
      retrieveMapHandlers.length = 0;
      retrieveFileHandlers = originalRetrieveFileHandlers.slice(0);
      retrieveMapHandlers = originalRetrieveMapHandlers.slice(0);
      retrieveSourceMap = handlerExec(retrieveMapHandlers);
      retrieveFile = handlerExec(retrieveFileHandlers);
    };
  }
});

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/otel/tracingSDK.js
init_esm();
init_esm2();
var import_api_logs = __toESM(require_src3(), 1);
var import_core5 = __toESM(require_src(), 1);
var import_exporter_logs_otlp_http = __toESM(require_src8(), 1);
var import_exporter_trace_otlp_http = __toESM(require_src9(), 1);
var import_exporter_metrics_otlp_http = __toESM(require_src10(), 1);
var import_host_metrics = __toESM(require_src11(), 1);
var import_instrumentation = __toESM(require_src12(), 1);
var import_resources = __toESM(require_src5(), 1);
var import_sdk_logs = __toESM(require_src13(), 1);
var import_sdk_metrics = __toESM(require_src6(), 1);
var import_sdk_trace_base = __toESM(require_src14(), 1);
var import_sdk_trace_node = __toESM(require_src16(), 1);

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/taskContext/otelProcessors.js
init_esm();
init_esm2();
var import_core4 = __toESM(require_src(), 1);
var TaskContextSpanProcessor = class {
  static {
    __name(this, "TaskContextSpanProcessor");
  }
  _innerProcessor;
  _tracer;
  constructor(version, innerProcessor) {
    this._tracer = trace.getTracer("trigger-dev-worker", version);
    this._innerProcessor = innerProcessor;
  }
  // Called when a span starts
  onStart(span, parentContext) {
    if (taskContext.ctx) {
      span.setAttributes(flattenAttributes(taskContext.attributes, SemanticInternalAttributes.METADATA));
      if (!taskContext.isRunDisabled && taskContext.ctx.run.tags?.length) {
        span.setAttribute(SemanticInternalAttributes.RUN_TAGS, taskContext.ctx.run.tags);
      }
      if (taskContext.conversationId) {
        span.setAttribute(SemanticInternalAttributes.GEN_AI_CONVERSATION_ID, taskContext.conversationId);
      }
    }
    if (!isPartialSpan(span) && !skipPartialSpan(span)) {
      const partialSpan = createPartialSpan(this._tracer, span, parentContext);
      partialSpan.end();
    }
    this._innerProcessor.onStart(span, parentContext);
  }
  // Delegate the rest of the methods to the wrapped processor
  onEnd(span) {
    this._innerProcessor.onEnd(span);
  }
  shutdown() {
    return this._innerProcessor.shutdown();
  }
  forceFlush() {
    return this._innerProcessor.forceFlush();
  }
};
function isPartialSpan(span) {
  return span.attributes[SemanticInternalAttributes.SPAN_PARTIAL] === true;
}
__name(isPartialSpan, "isPartialSpan");
function skipPartialSpan(span) {
  return span.attributes[SemanticInternalAttributes.SKIP_SPAN_PARTIAL] === true;
}
__name(skipPartialSpan, "skipPartialSpan");
function createPartialSpan(tracer, span, parentContext) {
  const partialSpan = tracer.startSpan(span.name, {
    attributes: {
      [SemanticInternalAttributes.SPAN_PARTIAL]: true,
      [SemanticInternalAttributes.SPAN_ID]: span.spanContext().spanId,
      ...span.attributes
    }
  }, parentContext);
  if (taskContext.ctx) {
    partialSpan.setAttributes(flattenAttributes(taskContext.attributes, SemanticInternalAttributes.METADATA));
  }
  if (span.events) {
    for (const event of span.events) {
      partialSpan.addEvent(event.name, event.attributes, event.time);
    }
  }
  return partialSpan;
}
__name(createPartialSpan, "createPartialSpan");
var TaskContextLogProcessor = class {
  static {
    __name(this, "TaskContextLogProcessor");
  }
  _innerProcessor;
  constructor(innerProcessor) {
    this._innerProcessor = innerProcessor;
  }
  forceFlush() {
    return this._innerProcessor.forceFlush();
  }
  onEmit(logRecord, context2) {
    if (taskContext.ctx) {
      logRecord.setAttributes(flattenAttributes(taskContext.attributes, SemanticInternalAttributes.METADATA));
    }
    this._innerProcessor.onEmit(logRecord, context2);
  }
  shutdown() {
    return this._innerProcessor.shutdown();
  }
};
var TaskContextMetricExporter = class {
  static {
    __name(this, "TaskContextMetricExporter");
  }
  _innerExporter;
  selectAggregationTemporality;
  selectAggregation;
  constructor(_innerExporter) {
    this._innerExporter = _innerExporter;
    if (_innerExporter.selectAggregationTemporality) {
      this.selectAggregationTemporality = _innerExporter.selectAggregationTemporality.bind(_innerExporter);
    }
    if (_innerExporter.selectAggregation) {
      this.selectAggregation = _innerExporter.selectAggregation.bind(_innerExporter);
    }
  }
  export(metrics2, resultCallback) {
    if (!taskContext.ctx) {
      this._innerExporter.export(metrics2, resultCallback);
      return;
    }
    const ctx = taskContext.ctx;
    let contextAttrs;
    if (taskContext.isRunDisabled) {
      contextAttrs = {
        [SemanticInternalAttributes.ENVIRONMENT_ID]: ctx.environment.id,
        [SemanticInternalAttributes.ENVIRONMENT_TYPE]: ctx.environment.type,
        [SemanticInternalAttributes.ORGANIZATION_ID]: ctx.organization.id,
        [SemanticInternalAttributes.PROJECT_ID]: ctx.project.id,
        [SemanticInternalAttributes.MACHINE_PRESET_NAME]: ctx.machine?.name
      };
    } else {
      contextAttrs = {
        [SemanticInternalAttributes.RUN_ID]: ctx.run.id,
        [SemanticInternalAttributes.TASK_SLUG]: ctx.task.id,
        [SemanticInternalAttributes.ATTEMPT_NUMBER]: ctx.attempt.number,
        [SemanticInternalAttributes.ENVIRONMENT_ID]: ctx.environment.id,
        [SemanticInternalAttributes.ORGANIZATION_ID]: ctx.organization.id,
        [SemanticInternalAttributes.PROJECT_ID]: ctx.project.id,
        [SemanticInternalAttributes.MACHINE_PRESET_NAME]: ctx.machine?.name,
        [SemanticInternalAttributes.ENVIRONMENT_TYPE]: ctx.environment.type
      };
    }
    if (taskContext.worker) {
      contextAttrs[SemanticInternalAttributes.WORKER_ID] = taskContext.worker.id;
      contextAttrs[SemanticInternalAttributes.WORKER_VERSION] = taskContext.worker.version;
    }
    if (!taskContext.isRunDisabled && ctx.run.tags?.length) {
      contextAttrs[SemanticInternalAttributes.RUN_TAGS] = ctx.run.tags;
    }
    if (taskContext.conversationId) {
      contextAttrs[SemanticInternalAttributes.GEN_AI_CONVERSATION_ID] = taskContext.conversationId;
    }
    const modified = {
      resource: metrics2.resource,
      scopeMetrics: metrics2.scopeMetrics.map((scope) => ({
        ...scope,
        metrics: scope.metrics.map((metric) => ({
          ...metric,
          dataPoints: metric.dataPoints.map((dp) => ({
            ...dp,
            attributes: { ...dp.attributes, ...contextAttrs }
          }))
        }))
      }))
    };
    this._innerExporter.export(modified, resultCallback);
  }
  forceFlush() {
    return this._innerExporter.forceFlush();
  }
  shutdown() {
    return this._innerExporter.shutdown();
  }
};
function isFiniteDataPointValue(value2) {
  if (typeof value2 === "number") {
    return Number.isFinite(value2);
  }
  if (typeof value2 === "object" && value2 !== null) {
    return ["sum", "min", "max"].every((key) => {
      const component = value2[key];
      return typeof component !== "number" || Number.isFinite(component);
    });
  }
  return true;
}
__name(isFiniteDataPointValue, "isFiniteDataPointValue");
function dropNonFiniteDataPoints(metrics2) {
  return {
    ...metrics2,
    scopeMetrics: metrics2.scopeMetrics.map((scope) => ({
      ...scope,
      metrics: scope.metrics.map((metric) => ({
        ...metric,
        dataPoints: metric.dataPoints.filter((dp) => isFiniteDataPointValue(dp.value))
      }))
    }))
  };
}
__name(dropNonFiniteDataPoints, "dropNonFiniteDataPoints");
var BufferingMetricExporter = class {
  static {
    __name(this, "BufferingMetricExporter");
  }
  _innerExporter;
  _flushIntervalMs;
  selectAggregationTemporality;
  selectAggregation;
  _buffer = [];
  _lastFlushTime = Date.now();
  constructor(_innerExporter, _flushIntervalMs) {
    this._innerExporter = _innerExporter;
    this._flushIntervalMs = _flushIntervalMs;
    if (_innerExporter.selectAggregationTemporality) {
      this.selectAggregationTemporality = _innerExporter.selectAggregationTemporality.bind(_innerExporter);
    }
    if (_innerExporter.selectAggregation) {
      this.selectAggregation = _innerExporter.selectAggregation.bind(_innerExporter);
    }
  }
  export(metrics2, resultCallback) {
    this._buffer.push(dropNonFiniteDataPoints(metrics2));
    const now = Date.now();
    if (now - this._lastFlushTime >= this._flushIntervalMs) {
      this._lastFlushTime = now;
      const merged = this._mergeBuffer();
      this._innerExporter.export(merged, resultCallback);
    } else {
      resultCallback({ code: import_core4.ExportResultCode.SUCCESS });
    }
  }
  forceFlush() {
    if (this._buffer.length > 0) {
      this._lastFlushTime = Date.now();
      const merged = this._mergeBuffer();
      return new Promise((resolve2, reject) => {
        this._innerExporter.export(merged, (result) => {
          if (result.code === import_core4.ExportResultCode.SUCCESS) {
            resolve2();
          } else {
            reject(result.error ?? new Error("Export failed"));
          }
        });
      }).then(() => this._innerExporter.forceFlush());
    }
    return this._innerExporter.forceFlush();
  }
  shutdown() {
    return this.forceFlush().then(() => this._innerExporter.shutdown());
  }
  _mergeBuffer() {
    const batch = this._buffer;
    this._buffer = [];
    if (batch.length === 1) {
      return batch[0];
    }
    const base = batch[0];
    const scopeMap = /* @__PURE__ */ new Map();
    for (const rm of batch) {
      for (const sm of rm.scopeMetrics) {
        const scopeKey = sm.scope.name;
        let scopeEntry = scopeMap.get(scopeKey);
        if (!scopeEntry) {
          scopeEntry = { scope: sm.scope, metricsMap: /* @__PURE__ */ new Map() };
          scopeMap.set(scopeKey, scopeEntry);
        }
        for (const metric of sm.metrics) {
          const metricKey = metric.descriptor.name;
          const existing = scopeEntry.metricsMap.get(metricKey);
          if (existing) {
            scopeEntry.metricsMap.set(metricKey, {
              ...existing,
              dataPoints: [...existing.dataPoints, ...metric.dataPoints]
            });
          } else {
            scopeEntry.metricsMap.set(metricKey, {
              ...metric,
              dataPoints: [...metric.dataPoints]
            });
          }
        }
      }
    }
    return {
      resource: base.resource,
      scopeMetrics: Array.from(scopeMap.values()).map(({ scope, metricsMap }) => ({
        scope,
        metrics: Array.from(metricsMap.values())
      }))
    };
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/otel/machineId.js
init_esm();
var machineId = getEnvVar("TRIGGER_MACHINE_ID") ?? generateFriendlyId("machine");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/otel/diskIoMetrics.js
init_esm();
import * as fs2 from "node:fs";
import * as fsPromises from "node:fs/promises";
var SECTOR_SIZE = 512;
var FILTERED_DEVICE_PREFIXES = ["loop", "ram", "dm-"];
function parseProcDiskstats(content) {
  const entries = [];
  for (const line of content.split("\n")) {
    const trimmed = line.trim();
    if (!trimmed)
      continue;
    const fields = trimmed.split(/\s+/);
    if (fields.length < 14)
      continue;
    const device = fields[2];
    if (FILTERED_DEVICE_PREFIXES.some((prefix) => device.startsWith(prefix))) {
      continue;
    }
    entries.push({
      device,
      readsCompleted: parseInt(fields[3], 10),
      sectorsRead: parseInt(fields[5], 10),
      writesCompleted: parseInt(fields[7], 10),
      sectorsWritten: parseInt(fields[9], 10)
    });
  }
  return entries;
}
__name(parseProcDiskstats, "parseProcDiskstats");
function startDiskIoMetrics(meterProvider) {
  try {
    fs2.accessSync("/proc/diskstats", fs2.constants.R_OK);
  } catch {
    return;
  }
  const meter = meterProvider.getMeter("system-disk", "1.0.0");
  const ioCounter = meter.createObservableCounter("system.disk.io", {
    description: "Disk I/O bytes read and written per device",
    unit: "By"
  });
  const opsCounter = meter.createObservableCounter("system.disk.operations", {
    description: "Disk read/write operation counts per device",
    unit: "{operation}"
  });
  meter.addBatchObservableCallback(async (obs) => {
    try {
      const content = await fsPromises.readFile("/proc/diskstats", "utf-8");
      const stats = parseProcDiskstats(content);
      for (const entry of stats) {
        const readAttrs = {
          "system.device": entry.device,
          "disk.io.direction": "read"
        };
        const writeAttrs = {
          "system.device": entry.device,
          "disk.io.direction": "write"
        };
        obs.observe(ioCounter, entry.sectorsRead * SECTOR_SIZE, readAttrs);
        obs.observe(ioCounter, entry.sectorsWritten * SECTOR_SIZE, writeAttrs);
        obs.observe(opsCounter, entry.readsCompleted, readAttrs);
        obs.observe(opsCounter, entry.writesCompleted, writeAttrs);
      }
    } catch {
    }
  }, [ioCounter, opsCounter]);
}
__name(startDiskIoMetrics, "startDiskIoMetrics");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/otel/filesystemMetrics.js
init_esm();
import * as fs3 from "node:fs";
import * as fsPromises2 from "node:fs/promises";
var VIRTUAL_FS_TYPES = /* @__PURE__ */ new Set([
  "proc",
  "sysfs",
  "devpts",
  "tmpfs",
  "devtmpfs",
  "cgroup",
  "cgroup2",
  "squashfs",
  "autofs",
  "debugfs",
  "securityfs",
  "pstore",
  "bpf",
  "tracefs",
  "hugetlbfs",
  "mqueue",
  "fusectl",
  "configfs",
  "binfmt_misc"
]);
function parseProcMounts(content) {
  const entries = [];
  for (const line of content.split("\n")) {
    if (!line.trim())
      continue;
    const parts = line.split(" ");
    if (parts.length < 4)
      continue;
    const fsType = parts[2];
    if (VIRTUAL_FS_TYPES.has(fsType))
      continue;
    entries.push({
      device: parts[0],
      mountpoint: unescapeMountPath(parts[1]),
      fsType,
      options: parts[3]
    });
  }
  return entries;
}
__name(parseProcMounts, "parseProcMounts");
function unescapeMountPath(path2) {
  return path2.replace(/\\040/g, " ").replace(/\\011/g, "	");
}
__name(unescapeMountPath, "unescapeMountPath");
function startFilesystemMetrics(meterProvider) {
  try {
    fs3.accessSync("/proc/mounts", fs3.constants.R_OK);
  } catch {
    return;
  }
  if (typeof fsPromises2.statfs !== "function") {
    return;
  }
  const meter = meterProvider.getMeter("system-filesystem", "1.0.0");
  const usageCounter = meter.createObservableUpDownCounter("system.filesystem.usage", {
    description: "Filesystem bytes used, free, and reserved per mountpoint",
    unit: "By"
  });
  const utilizationGauge = meter.createObservableGauge("system.filesystem.utilization", {
    description: "Fraction of filesystem space used (0-1)",
    unit: "1"
  });
  meter.addBatchObservableCallback(async (obs) => {
    try {
      const mountsContent = await fsPromises2.readFile("/proc/mounts", "utf-8");
      const mounts = parseProcMounts(mountsContent);
      for (const mount of mounts) {
        try {
          const stats = await fsPromises2.statfs(mount.mountpoint);
          const bsize = stats.bsize;
          const total = stats.blocks * bsize;
          const free = stats.bavail * bsize;
          const reserved = (stats.bfree - stats.bavail) * bsize;
          const used = total - stats.bfree * bsize;
          const mode = mount.options.startsWith("ro") ? "ro" : "rw";
          const baseAttrs = {
            "system.device": mount.device,
            "system.filesystem.type": mount.fsType,
            "system.filesystem.mountpoint": mount.mountpoint,
            "system.filesystem.mode": mode
          };
          obs.observe(usageCounter, used, {
            ...baseAttrs,
            "system.filesystem.state": "used"
          });
          obs.observe(usageCounter, free, {
            ...baseAttrs,
            "system.filesystem.state": "free"
          });
          obs.observe(usageCounter, reserved, {
            ...baseAttrs,
            "system.filesystem.state": "reserved"
          });
          if (total > 0) {
            obs.observe(utilizationGauge, used / total, baseAttrs);
          }
        } catch {
        }
      }
    } catch {
    }
  }, [usageCounter, utilizationGauge]);
}
__name(startFilesystemMetrics, "startFilesystemMetrics");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/otel/nodejsRuntimeMetrics.js
init_esm();
import { performance as performance2, monitorEventLoopDelay } from "node:perf_hooks";
function tryMonitorEventLoopDelay() {
  try {
    const eld = monitorEventLoopDelay({ resolution: 20 });
    eld.enable();
    return eld;
  } catch {
    return void 0;
  }
}
__name(tryMonitorEventLoopDelay, "tryMonitorEventLoopDelay");
function startNodejsRuntimeMetrics(meterProvider) {
  const meter = meterProvider.getMeter("nodejs-runtime", "1.0.0");
  let lastElu = performance2.eventLoopUtilization();
  const eluGauge = meter.createObservableGauge("nodejs.event_loop.utilization", {
    description: "Event loop utilization over the last collection interval",
    unit: "1"
  });
  const eld = tryMonitorEventLoopDelay();
  const observables = [eluGauge];
  let eldP95;
  let eldMax;
  if (eld) {
    eldP95 = meter.createObservableGauge("nodejs.event_loop.delay.p95", {
      description: "p95 event loop delay",
      unit: "s"
    });
    eldMax = meter.createObservableGauge("nodejs.event_loop.delay.max", {
      description: "Max event loop delay",
      unit: "s"
    });
    observables.push(eldP95, eldMax);
  }
  const heapUsed = meter.createObservableGauge("nodejs.heap.used", {
    description: "V8 heap used",
    unit: "By"
  });
  const heapTotal = meter.createObservableGauge("nodejs.heap.total", {
    description: "V8 heap total allocated",
    unit: "By"
  });
  observables.push(heapUsed, heapTotal);
  meter.addBatchObservableCallback((obs) => {
    const currentElu = performance2.eventLoopUtilization();
    const diff = performance2.eventLoopUtilization(currentElu, lastElu);
    lastElu = currentElu;
    obs.observe(eluGauge, diff.utilization);
    if (eld && eldP95 && eldMax) {
      obs.observe(eldP95, eld.percentile(95) / 1e9);
      obs.observe(eldMax, eld.max / 1e9);
      eld.reset();
    }
    const mem = process.memoryUsage();
    obs.observe(heapUsed, mem.heapUsed);
    obs.observe(heapTotal, mem.heapTotal);
  }, observables);
}
__name(startNodejsRuntimeMetrics, "startNodejsRuntimeMetrics");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/otel/tracingSDK.js
var idGenerator = new import_sdk_trace_base.RandomIdGenerator();
var TracingSDK = class {
  static {
    __name(this, "TracingSDK");
  }
  config;
  _logProvider;
  _spanExporter;
  _traceProvider;
  _meterProvider;
  _metricReaders;
  getLogger;
  getTracer;
  constructor(config) {
    this.config = config;
    setLogLevel(config.diagLogLevel ?? "none");
    const envResourceAttributesSerialized = getEnvVar("TRIGGER_OTEL_RESOURCE_ATTRIBUTES");
    const envResourceAttributes = envResourceAttributesSerialized ? JSON.parse(envResourceAttributesSerialized) : {};
    const customEnvResourceAttributes = parseOtelResourceAttributes(getEnvVar("CUSTOM_OTEL_RESOURCE_ATTRIBUTES"));
    const commonResources = (0, import_resources.detectResources)({
      detectors: [import_resources.processDetector]
    }).merge((0, import_resources.resourceFromAttributes)({
      "cloud.provider": "trigger.dev",
      "service.name": getEnvVar("TRIGGER_OTEL_SERVICE_NAME") ?? "trigger.dev",
      [SemanticInternalAttributes.TRIGGER]: true,
      [SemanticInternalAttributes.CLI_VERSION]: VERSION,
      [SemanticInternalAttributes.SDK_VERSION]: VERSION,
      [SemanticInternalAttributes.SDK_LANGUAGE]: "typescript",
      [SemanticInternalAttributes.MACHINE_ID]: machineId
    })).merge((0, import_resources.resourceFromAttributes)(envResourceAttributes)).merge((0, import_resources.resourceFromAttributes)(customEnvResourceAttributes)).merge((0, import_resources.resourceFromAttributes)(taskContext.resourceAttributes)).merge(config.resource ?? (0, import_resources.resourceFromAttributes)({}));
    const spanExporter = new import_exporter_trace_otlp_http.OTLPTraceExporter({
      url: `${config.url}/v1/traces`,
      timeoutMillis: config.forceFlushTimeoutMillis
    });
    const spanProcessors = [];
    spanProcessors.push(new TaskContextSpanProcessor(VERSION, getEnvVar("TRIGGER_OTEL_BATCH_PROCESSING_ENABLED") === "1" ? new import_sdk_trace_node.BatchSpanProcessor(spanExporter, {
      maxExportBatchSize: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_MAX_EXPORT_BATCH_SIZE") ?? "64"),
      scheduledDelayMillis: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_SCHEDULED_DELAY_MILLIS") ?? "200"),
      exportTimeoutMillis: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_EXPORT_TIMEOUT_MILLIS") ?? "30000"),
      maxQueueSize: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_MAX_QUEUE_SIZE") ?? "512")
    }) : new import_sdk_trace_node.SimpleSpanProcessor(spanExporter)));
    const fallbackTraceIds = new FallbackExternalTraceIds(idGenerator.generateTraceId());
    for (const exporter of config.exporters ?? []) {
      spanProcessors.push(getEnvVar("TRIGGER_OTEL_BATCH_PROCESSING_ENABLED") === "1" ? new import_sdk_trace_node.BatchSpanProcessor(new ExternalSpanExporterWrapper(exporter, fallbackTraceIds), {
        maxExportBatchSize: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_MAX_EXPORT_BATCH_SIZE") ?? "64"),
        scheduledDelayMillis: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_SCHEDULED_DELAY_MILLIS") ?? "200"),
        exportTimeoutMillis: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_EXPORT_TIMEOUT_MILLIS") ?? "30000"),
        maxQueueSize: parseInt(getEnvVar("TRIGGER_OTEL_SPAN_MAX_QUEUE_SIZE") ?? "512")
      }) : new import_sdk_trace_node.SimpleSpanProcessor(new ExternalSpanExporterWrapper(exporter, fallbackTraceIds)));
    }
    const traceProvider = new import_sdk_trace_node.NodeTracerProvider({
      forceFlushTimeoutMillis: config.forceFlushTimeoutMillis,
      resource: commonResources,
      spanLimits: {
        attributeCountLimit: OTEL_SPAN_ATTRIBUTE_COUNT_LIMIT,
        attributeValueLengthLimit: OTEL_SPAN_ATTRIBUTE_VALUE_LENGTH_LIMIT,
        eventCountLimit: OTEL_SPAN_EVENT_COUNT_LIMIT,
        attributePerEventCountLimit: OTEL_ATTRIBUTE_PER_EVENT_COUNT_LIMIT,
        linkCountLimit: OTEL_LINK_COUNT_LIMIT,
        attributePerLinkCountLimit: OTEL_ATTRIBUTE_PER_LINK_COUNT_LIMIT
      },
      spanProcessors
    });
    traceProvider.register();
    (0, import_instrumentation.registerInstrumentations)({
      instrumentations: config.instrumentations ?? [],
      tracerProvider: traceProvider
    });
    const logExporter = new import_exporter_logs_otlp_http.OTLPLogExporter({
      url: `${config.url}/v1/logs`
    });
    const logProcessors = [
      new TaskContextLogProcessor(getEnvVar("TRIGGER_OTEL_BATCH_PROCESSING_ENABLED") === "1" ? new import_sdk_logs.BatchLogRecordProcessor(logExporter, {
        maxExportBatchSize: parseInt(getEnvVar("TRIGGER_OTEL_LOG_MAX_EXPORT_BATCH_SIZE") ?? "64"),
        scheduledDelayMillis: parseInt(getEnvVar("TRIGGER_OTEL_LOG_SCHEDULED_DELAY_MILLIS") ?? "200"),
        exportTimeoutMillis: parseInt(getEnvVar("TRIGGER_OTEL_LOG_EXPORT_TIMEOUT_MILLIS") ?? "30000"),
        maxQueueSize: parseInt(getEnvVar("TRIGGER_OTEL_LOG_MAX_QUEUE_SIZE") ?? "512")
      }) : new import_sdk_logs.SimpleLogRecordProcessor(logExporter))
    ];
    for (const externalLogExporter of config.logExporters ?? []) {
      logProcessors.push(getEnvVar("TRIGGER_OTEL_BATCH_PROCESSING_ENABLED") === "1" ? new import_sdk_logs.BatchLogRecordProcessor(new ExternalLogRecordExporterWrapper(externalLogExporter, fallbackTraceIds), {
        maxExportBatchSize: parseInt(getEnvVar("TRIGGER_OTEL_LOG_MAX_EXPORT_BATCH_SIZE") ?? "64"),
        scheduledDelayMillis: parseInt(getEnvVar("TRIGGER_OTEL_LOG_SCHEDULED_DELAY_MILLIS") ?? "200"),
        exportTimeoutMillis: parseInt(getEnvVar("TRIGGER_OTEL_LOG_EXPORT_TIMEOUT_MILLIS") ?? "30000"),
        maxQueueSize: parseInt(getEnvVar("TRIGGER_OTEL_LOG_MAX_QUEUE_SIZE") ?? "512")
      }) : new import_sdk_logs.SimpleLogRecordProcessor(new ExternalLogRecordExporterWrapper(externalLogExporter, fallbackTraceIds)));
    }
    const loggerProvider = new import_sdk_logs.LoggerProvider({
      resource: commonResources,
      logRecordLimits: {
        attributeCountLimit: OTEL_LOG_ATTRIBUTE_COUNT_LIMIT,
        attributeValueLengthLimit: OTEL_LOG_ATTRIBUTE_VALUE_LENGTH_LIMIT
      },
      processors: logProcessors
    });
    this._logProvider = loggerProvider;
    this._spanExporter = spanExporter;
    this._traceProvider = traceProvider;
    import_api_logs.logs.setGlobalLoggerProvider(loggerProvider);
    const metricsUrl = config.metricsUrl ?? getEnvVar("TRIGGER_OTEL_METRICS_ENDPOINT") ?? `${config.url}/v1/metrics`;
    const rawMetricExporter = new import_exporter_metrics_otlp_http.OTLPMetricExporter({
      url: metricsUrl,
      timeoutMillis: config.forceFlushTimeoutMillis
    });
    const collectionIntervalMs = parseInt(getEnvVar("TRIGGER_OTEL_METRICS_COLLECTION_INTERVAL_MILLIS") ?? "10000");
    const exportIntervalMs = parseInt(getEnvVar("TRIGGER_OTEL_METRICS_EXPORT_INTERVAL_MILLIS") ?? "30000");
    const bufferingExporter = new BufferingMetricExporter(rawMetricExporter, exportIntervalMs);
    const metricExporter = new TaskContextMetricExporter(bufferingExporter);
    const exportTimeoutMillis = parseInt(getEnvVar("TRIGGER_OTEL_METRICS_EXPORT_TIMEOUT_MILLIS") ?? "30000");
    const metricReaders = [
      new import_sdk_metrics.PeriodicExportingMetricReader({
        exporter: metricExporter,
        exportIntervalMillis: collectionIntervalMs,
        exportTimeoutMillis: Math.min(exportTimeoutMillis, collectionIntervalMs)
      }),
      ...(config.metricExporters ?? []).map((exporter) => new import_sdk_metrics.PeriodicExportingMetricReader({
        exporter,
        exportIntervalMillis: collectionIntervalMs,
        exportTimeoutMillis: Math.min(exportTimeoutMillis, collectionIntervalMs)
      })),
      ...config.metricReaders ?? []
    ];
    const meterProvider = new import_sdk_metrics.MeterProvider({
      resource: commonResources,
      readers: metricReaders,
      views: (config.droppedMetrics ?? []).map((pattern) => ({
        instrumentName: pattern,
        aggregation: { type: import_sdk_metrics.AggregationType.DROP }
      }))
    });
    this._meterProvider = meterProvider;
    this._metricReaders = metricReaders;
    metrics.setGlobalMeterProvider(meterProvider);
    if (config.hostMetrics) {
      const hostMetrics = new import_host_metrics.HostMetrics({
        meterProvider,
        metricGroups: config.hostMetricGroups
      });
      hostMetrics.start();
    }
    if (config.nodejsRuntimeMetrics) {
      startNodejsRuntimeMetrics(meterProvider);
    }
    if (config.filesystemMetrics) {
      startFilesystemMetrics(meterProvider);
    }
    if (config.diskIoMetrics) {
      startDiskIoMetrics(meterProvider);
    }
    this.getLogger = loggerProvider.getLogger.bind(loggerProvider);
    this.getTracer = traceProvider.getTracer.bind(traceProvider);
  }
  async flush() {
    await Promise.all([
      this._traceProvider.forceFlush(),
      this._logProvider.forceFlush(),
      this._flushMetricReadersSerially()
    ]);
  }
  async _flushMetricReadersSerially() {
    await this._eachMetricReaderSerially("flush", (reader) => reader.forceFlush());
  }
  async _shutdownMetricReadersSerially() {
    await this._eachMetricReaderSerially("shut down", (reader) => reader.shutdown());
    await this._meterProvider.shutdown();
  }
  async _eachMetricReaderSerially(action, run) {
    const errors = [];
    for (const reader of this._metricReaders) {
      try {
        await run(reader);
      } catch (error) {
        console.error(`Failed to ${action} metric reader ${reader.constructor.name}`, error);
        errors.push(error);
      }
    }
    if (errors.length > 0) {
      throw errors[0];
    }
  }
  async shutdown() {
    await Promise.all([
      this._traceProvider.shutdown(),
      this._logProvider.shutdown(),
      this._shutdownMetricReadersSerially()
    ]);
  }
};
function setLogLevel(level) {
  let diagLogLevel;
  switch (level) {
    case "none":
      diagLogLevel = DiagLogLevel.NONE;
      break;
    case "error":
      diagLogLevel = DiagLogLevel.ERROR;
      break;
    case "warn":
      diagLogLevel = DiagLogLevel.WARN;
      break;
    case "info":
      diagLogLevel = DiagLogLevel.INFO;
      break;
    case "debug":
      diagLogLevel = DiagLogLevel.DEBUG;
      break;
    case "verbose":
      diagLogLevel = DiagLogLevel.VERBOSE;
      break;
    case "all":
      diagLogLevel = DiagLogLevel.ALL;
      break;
    default:
      diagLogLevel = DiagLogLevel.NONE;
  }
  diag.setLogger(new DiagConsoleLogger(), diagLogLevel);
}
__name(setLogLevel, "setLogLevel");
var MAX_TRACKED_INTERNAL_TRACES = 64;
var FallbackExternalTraceIds = class {
  static {
    __name(this, "FallbackExternalTraceIds");
  }
  seed;
  traceIdGenerator;
  byInternalTrace = /* @__PURE__ */ new Map();
  constructor(seed, traceIdGenerator = idGenerator) {
    this.seed = seed;
    this.traceIdGenerator = traceIdGenerator;
  }
  /** False when no external trace id was configured, i.e. external export is off. */
  get enabled() {
    return !!this.seed;
  }
  forInternalTrace(internalTraceId) {
    if (!this.seed) {
      return this.seed;
    }
    const known = this.byInternalTrace.get(internalTraceId);
    if (known) {
      this.byInternalTrace.delete(internalTraceId);
      this.byInternalTrace.set(internalTraceId, known);
      return known;
    }
    const traceId = this.byInternalTrace.size === 0 ? this.seed : this.traceIdGenerator.generateTraceId();
    this.byInternalTrace.set(internalTraceId, traceId);
    if (this.byInternalTrace.size > MAX_TRACKED_INTERNAL_TRACES) {
      const stalest = this.byInternalTrace.keys().next().value;
      if (stalest !== void 0) {
        this.byInternalTrace.delete(stalest);
      }
    }
    return traceId;
  }
};
var ExternalSpanExporterWrapper = class {
  static {
    __name(this, "ExternalSpanExporterWrapper");
  }
  underlyingExporter;
  fallback;
  constructor(underlyingExporter, fallback) {
    this.underlyingExporter = underlyingExporter;
    this.fallback = fallback;
  }
  transformSpan(span) {
    const externalTraceContext = traceContext.getExternalTraceContext();
    const isExternallySampled = externalTraceContext ? isTraceFlagSampled(externalTraceContext.traceFlags) : this.fallback.enabled;
    if (!isExternallySampled) {
      return;
    }
    if (isSpanInternalOnly(span)) {
      return;
    }
    const externalTraceId = externalTraceContext ? externalTraceContext.traceId : this.fallback.forInternalTrace(span.spanContext().traceId);
    const isAttemptSpan = span.attributes[SemanticInternalAttributes.SPAN_ATTEMPT];
    const spanContext = span.spanContext();
    let parentSpanContext = span.parentSpanContext;
    if (parentSpanContext) {
      parentSpanContext = {
        ...parentSpanContext,
        traceId: externalTraceId
      };
    }
    if (isAttemptSpan && externalTraceContext) {
      parentSpanContext = {
        ...parentSpanContext,
        traceId: externalTraceId,
        spanId: externalTraceContext.spanId,
        traceState: externalTraceContext.tracestate ? new import_core5.TraceState(externalTraceContext.tracestate) : void 0,
        traceFlags: externalTraceContext.traceFlags
      };
    } else if (isAttemptSpan) {
      parentSpanContext = void 0;
    }
    return {
      ...span,
      spanContext: /* @__PURE__ */ __name(() => ({ ...spanContext, traceId: externalTraceId }), "spanContext"),
      parentSpanContext
    };
  }
  export(spans, resultCallback) {
    try {
      const modifiedSpans = spans.map(this.transformSpan.bind(this));
      this.underlyingExporter.export(modifiedSpans.filter(Boolean), resultCallback);
    } catch (e) {
      console.error(e);
    }
  }
  shutdown() {
    return this.underlyingExporter.shutdown();
  }
  forceFlush() {
    return this.underlyingExporter.forceFlush ? this.underlyingExporter.forceFlush() : Promise.resolve();
  }
};
var ExternalLogRecordExporterWrapper = class {
  static {
    __name(this, "ExternalLogRecordExporterWrapper");
  }
  underlyingExporter;
  fallback;
  constructor(underlyingExporter, fallback) {
    this.underlyingExporter = underlyingExporter;
    this.fallback = fallback;
  }
  export(logs2, resultCallback) {
    const externalTraceContext = traceContext.getExternalTraceContext();
    const isExternallySampled = externalTraceContext ? isTraceFlagSampled(externalTraceContext.traceFlags) : this.fallback.enabled;
    if (!isExternallySampled) {
      this.underlyingExporter.export([], resultCallback);
      return;
    }
    const modifiedLogs = logs2.map((log) => this.transformLogRecord(log, externalTraceContext));
    this.underlyingExporter.export(modifiedLogs, resultCallback);
  }
  shutdown() {
    return this.underlyingExporter.shutdown();
  }
  forceFlush() {
    const underlyingExporter = this.underlyingExporter;
    return underlyingExporter.forceFlush ? underlyingExporter.forceFlush() : Promise.resolve();
  }
  transformLogRecord(logRecord, externalTraceContext) {
    if (!logRecord.spanContext) {
      return logRecord;
    }
    const externalTraceId = externalTraceContext ? externalTraceContext.traceId : this.fallback.forInternalTrace(logRecord.spanContext.traceId);
    if (!externalTraceId) {
      return logRecord;
    }
    return new Proxy(logRecord, {
      get(target, prop, receiver) {
        if (prop === "spanContext") {
          const originalSpanContext = target.spanContext;
          if (originalSpanContext) {
            return {
              ...originalSpanContext,
              traceId: externalTraceId
              // Override traceId.
            };
          }
          return void 0;
        }
        return Reflect.get(target, prop, receiver);
      }
    });
  }
};
function isSpanInternalOnly(span) {
  if (span.attributes[SemanticInternalAttributes.SPAN_PARTIAL]) {
    return true;
  }
  const urlPath = span.attributes["url.path"];
  if (typeof urlPath === "string" && urlPath === "/api/v1/usage/ingest") {
    return true;
  }
  const httpUrl = span.attributes["http.url"] ?? span.attributes["url.full"];
  const url = safeParseUrl(httpUrl);
  if (!url) {
    return false;
  }
  const internalHosts = [
    "api.trigger.dev",
    "billing.trigger.dev",
    "cloud.trigger.dev",
    "engine.trigger.dev",
    "platform.trigger.dev"
  ];
  return internalHosts.some((host) => url.hostname.includes(host)) || url.pathname.includes("/api/v1/usage/ingest");
}
__name(isSpanInternalOnly, "isSpanInternalOnly");
function safeParseUrl(url) {
  if (typeof url !== "string") {
    return void 0;
  }
  try {
    return new URL(url);
  } catch (_e) {
    return void 0;
  }
}
__name(safeParseUrl, "safeParseUrl");
function isTraceFlagSampled(traceFlags) {
  if (typeof traceFlags !== "number") {
    return true;
  }
  return (traceFlags & TraceFlags.SAMPLED) === TraceFlags.SAMPLED;
}
__name(isTraceFlagSampled, "isTraceFlagSampled");
function isPrintableAscii(str) {
  for (let i = 0; i < str.length; i++) {
    const code = str.charCodeAt(i);
    if (code < 32 || code > 126)
      return false;
  }
  return true;
}
__name(isPrintableAscii, "isPrintableAscii");
function isValid(name) {
  if (!name)
    return false;
  return typeof name === "string" && name.length <= 255 && isPrintableAscii(name);
}
__name(isValid, "isValid");
function isValidAndNotEmpty(name) {
  if (!name)
    return false;
  return isValid(name) && name.length > 0;
}
__name(isValidAndNotEmpty, "isValidAndNotEmpty");
function parseOtelResourceAttributes(rawEnvAttributes) {
  if (!rawEnvAttributes)
    return {};
  const COMMA = ",";
  const KV = "=";
  const attributes = {};
  const rawAttributes = rawEnvAttributes.split(COMMA, -1);
  for (const rawAttribute of rawAttributes) {
    const keyValuePair = rawAttribute.split(KV, -1);
    if (keyValuePair.length !== 2) {
      continue;
    }
    let [key, value2] = keyValuePair;
    key = key?.trim();
    value2 = value2?.trim().replace(/^"|"$/g, "");
    if (!value2 || !key) {
      continue;
    }
    if (!isValidAndNotEmpty(key)) {
      throw new Error(`Attribute key should be a ASCII string with a length greater than 0 and not exceed 255 characters.`);
    }
    if (!isValid(value2)) {
      throw new Error(`Attribute value should be a ASCII string with a length not exceed 255 characters.`);
    }
    try {
      attributes[key] = decodeURIComponent(value2);
    } catch (e) {
      if (e instanceof Error) {
        throw new Error(`Failed to decode attribute value for key ${key}: ${e.message}`);
      }
      throw new Error(`Failed to decode attribute value for key ${key}`);
    }
  }
  return attributes;
}
__name(parseOtelResourceAttributes, "parseOtelResourceAttributes");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/otel/index.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/workers/taskExecutor.js
init_esm();
init_esm2();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/realtimeStreams/streamsWriterV2.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/index.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/basin.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/client/index.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/core/bodySerializer.gen.js
init_esm();
var jsonBodySerializer = {
  bodySerializer: /* @__PURE__ */ __name((body) => JSON.stringify(body, (_key, value2) => typeof value2 === "bigint" ? value2.toString() : value2), "bodySerializer")
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/client/client.gen.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/core/serverSentEvents.gen.js
init_esm();
var createSseClient = /* @__PURE__ */ __name(({ onRequest, onSseError, onSseEvent, responseTransformer, responseValidator, sseDefaultRetryDelay, sseMaxRetryAttempts, sseMaxRetryDelay, sseSleepFn, url, ...options }) => {
  let lastEventId;
  const sleep = sseSleepFn ?? ((ms) => new Promise((resolve2) => setTimeout(resolve2, ms)));
  const createStream2 = /* @__PURE__ */ __name(async function* () {
    let retryDelay = sseDefaultRetryDelay ?? 3e3;
    let attempt = 0;
    const signal = options.signal ?? new AbortController().signal;
    while (true) {
      if (signal.aborted)
        break;
      attempt++;
      const headers = options.headers instanceof Headers ? options.headers : new Headers(options.headers);
      if (lastEventId !== void 0) {
        headers.set("Last-Event-ID", lastEventId);
      }
      try {
        const requestInit = {
          redirect: options.redirect ?? "follow",
          cache: options.cache,
          credentials: options.credentials,
          integrity: options.integrity,
          keepalive: options.keepalive,
          method: options.method,
          mode: options.mode,
          priority: options.priority,
          referrer: options.referrer,
          referrerPolicy: options.referrerPolicy,
          body: options.serializedBody,
          headers,
          signal
        };
        let request = new Request(url, requestInit);
        if (onRequest) {
          request = await onRequest(url, requestInit);
        }
        const _fetch = options.fetch ?? globalThis.fetch;
        const response = await _fetch(request);
        if (!response.ok)
          throw new Error(`SSE failed: ${response.status} ${response.statusText}`);
        if (!response.body)
          throw new Error("No body in SSE response");
        const reader = response.body.pipeThrough(new TextDecoderStream()).getReader();
        let buffer = "";
        const abortHandler = /* @__PURE__ */ __name(() => {
          try {
            reader.cancel();
          } catch {
          }
        }, "abortHandler");
        signal.addEventListener("abort", abortHandler);
        try {
          while (true) {
            const { done, value: value2 } = await reader.read();
            if (done)
              break;
            buffer += value2;
            const chunks = buffer.split("\n\n");
            buffer = chunks.pop() ?? "";
            for (const chunk of chunks) {
              const lines = chunk.split("\n");
              const dataLines = [];
              let eventName;
              for (const line of lines) {
                if (line.startsWith("data:")) {
                  dataLines.push(line.replace(/^data:\s*/, ""));
                } else if (line.startsWith("event:")) {
                  eventName = line.replace(/^event:\s*/, "");
                } else if (line.startsWith("id:")) {
                  lastEventId = line.replace(/^id:\s*/, "");
                } else if (line.startsWith("retry:")) {
                  const parsed = Number.parseInt(line.replace(/^retry:\s*/, ""), 10);
                  if (!Number.isNaN(parsed)) {
                    retryDelay = parsed;
                  }
                }
              }
              let data;
              let parsedJson = false;
              if (dataLines.length) {
                const rawData = dataLines.join("\n");
                try {
                  data = JSON.parse(rawData);
                  parsedJson = true;
                } catch {
                  data = rawData;
                }
              }
              if (parsedJson) {
                if (responseValidator) {
                  await responseValidator(data);
                }
                if (responseTransformer) {
                  data = await responseTransformer(data);
                }
              }
              onSseEvent?.({
                data,
                event: eventName,
                id: lastEventId,
                retry: retryDelay
              });
              if (dataLines.length) {
                yield data;
              }
            }
          }
        } finally {
          signal.removeEventListener("abort", abortHandler);
          reader.releaseLock();
        }
        break;
      } catch (error) {
        onSseError?.(error);
        if (sseMaxRetryAttempts !== void 0 && attempt >= sseMaxRetryAttempts) {
          break;
        }
        const backoff = Math.min(retryDelay * 2 ** (attempt - 1), sseMaxRetryDelay ?? 3e4);
        await sleep(backoff);
      }
    }
  }, "createStream");
  const stream = createStream2();
  return { stream };
}, "createSseClient");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/core/utils.gen.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/core/pathSerializer.gen.js
init_esm();
var separatorArrayExplode = /* @__PURE__ */ __name((style) => {
  switch (style) {
    case "label":
      return ".";
    case "matrix":
      return ";";
    case "simple":
      return ",";
    default:
      return "&";
  }
}, "separatorArrayExplode");
var separatorArrayNoExplode = /* @__PURE__ */ __name((style) => {
  switch (style) {
    case "form":
      return ",";
    case "pipeDelimited":
      return "|";
    case "spaceDelimited":
      return "%20";
    default:
      return ",";
  }
}, "separatorArrayNoExplode");
var separatorObjectExplode = /* @__PURE__ */ __name((style) => {
  switch (style) {
    case "label":
      return ".";
    case "matrix":
      return ";";
    case "simple":
      return ",";
    default:
      return "&";
  }
}, "separatorObjectExplode");
var serializeArrayParam = /* @__PURE__ */ __name(({ allowReserved, explode, name, style, value: value2 }) => {
  if (!explode) {
    const joinedValues2 = (allowReserved ? value2 : value2.map((v) => encodeURIComponent(v))).join(separatorArrayNoExplode(style));
    switch (style) {
      case "label":
        return `.${joinedValues2}`;
      case "matrix":
        return `;${name}=${joinedValues2}`;
      case "simple":
        return joinedValues2;
      default:
        return `${name}=${joinedValues2}`;
    }
  }
  const separator = separatorArrayExplode(style);
  const joinedValues = value2.map((v) => {
    if (style === "label" || style === "simple") {
      return allowReserved ? v : encodeURIComponent(v);
    }
    return serializePrimitiveParam({
      allowReserved,
      name,
      value: v
    });
  }).join(separator);
  return style === "label" || style === "matrix" ? separator + joinedValues : joinedValues;
}, "serializeArrayParam");
var serializePrimitiveParam = /* @__PURE__ */ __name(({ allowReserved, name, value: value2 }) => {
  if (value2 === void 0 || value2 === null) {
    return "";
  }
  if (typeof value2 === "object") {
    throw new Error("Deeply-nested arrays/objects aren’t supported. Provide your own `querySerializer()` to handle these.");
  }
  return `${name}=${allowReserved ? value2 : encodeURIComponent(value2)}`;
}, "serializePrimitiveParam");
var serializeObjectParam = /* @__PURE__ */ __name(({ allowReserved, explode, name, style, value: value2, valueOnly }) => {
  if (value2 instanceof Date) {
    return valueOnly ? value2.toISOString() : `${name}=${value2.toISOString()}`;
  }
  if (style !== "deepObject" && !explode) {
    let values = [];
    Object.entries(value2).forEach(([key, v]) => {
      values = [
        ...values,
        key,
        allowReserved ? v : encodeURIComponent(v)
      ];
    });
    const joinedValues2 = values.join(",");
    switch (style) {
      case "form":
        return `${name}=${joinedValues2}`;
      case "label":
        return `.${joinedValues2}`;
      case "matrix":
        return `;${name}=${joinedValues2}`;
      default:
        return joinedValues2;
    }
  }
  const separator = separatorObjectExplode(style);
  const joinedValues = Object.entries(value2).map(([key, v]) => serializePrimitiveParam({
    allowReserved,
    name: style === "deepObject" ? `${name}[${key}]` : key,
    value: v
  })).join(separator);
  return style === "label" || style === "matrix" ? separator + joinedValues : joinedValues;
}, "serializeObjectParam");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/core/utils.gen.js
var PATH_PARAM_RE = /\{[^{}]+\}/g;
var defaultPathSerializer = /* @__PURE__ */ __name(({ path: path2, url: _url }) => {
  let url = _url;
  const matches = _url.match(PATH_PARAM_RE);
  if (matches) {
    for (const match of matches) {
      let explode = false;
      let name = match.substring(1, match.length - 1);
      let style = "simple";
      if (name.endsWith("*")) {
        explode = true;
        name = name.substring(0, name.length - 1);
      }
      if (name.startsWith(".")) {
        name = name.substring(1);
        style = "label";
      } else if (name.startsWith(";")) {
        name = name.substring(1);
        style = "matrix";
      }
      const value2 = path2[name];
      if (value2 === void 0 || value2 === null) {
        continue;
      }
      if (Array.isArray(value2)) {
        url = url.replace(match, serializeArrayParam({ explode, name, style, value: value2 }));
        continue;
      }
      if (typeof value2 === "object") {
        url = url.replace(match, serializeObjectParam({
          explode,
          name,
          style,
          value: value2,
          valueOnly: true
        }));
        continue;
      }
      if (style === "matrix") {
        url = url.replace(match, `;${serializePrimitiveParam({
          name,
          value: value2
        })}`);
        continue;
      }
      const replaceValue = encodeURIComponent(style === "label" ? `.${value2}` : value2);
      url = url.replace(match, replaceValue);
    }
  }
  return url;
}, "defaultPathSerializer");
var getUrl = /* @__PURE__ */ __name(({ baseUrl, path: path2, query, querySerializer, url: _url }) => {
  const pathUrl = _url.startsWith("/") ? _url : `/${_url}`;
  let url = (baseUrl ?? "") + pathUrl;
  if (path2) {
    url = defaultPathSerializer({ path: path2, url });
  }
  let search = query ? querySerializer(query) : "";
  if (search.startsWith("?")) {
    search = search.substring(1);
  }
  if (search) {
    url += `?${search}`;
  }
  return url;
}, "getUrl");
function getValidRequestBody(options) {
  const hasBody = options.body !== void 0;
  const isSerializedBody = hasBody && options.bodySerializer;
  if (isSerializedBody) {
    if ("serializedBody" in options) {
      const hasSerializedBody = options.serializedBody !== void 0 && options.serializedBody !== "";
      return hasSerializedBody ? options.serializedBody : null;
    }
    return options.body !== "" ? options.body : null;
  }
  if (hasBody) {
    return options.body;
  }
  return void 0;
}
__name(getValidRequestBody, "getValidRequestBody");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/client/utils.gen.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/core/auth.gen.js
init_esm();
var getAuthToken = /* @__PURE__ */ __name(async (auth, callback) => {
  const token = typeof callback === "function" ? await callback(auth) : callback;
  if (!token) {
    return;
  }
  if (auth.scheme === "bearer") {
    return `Bearer ${token}`;
  }
  if (auth.scheme === "basic") {
    return `Basic ${btoa(token)}`;
  }
  return token;
}, "getAuthToken");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/client/utils.gen.js
var createQuerySerializer = /* @__PURE__ */ __name(({ parameters = {}, ...args } = {}) => {
  const querySerializer = /* @__PURE__ */ __name((queryParams) => {
    const search = [];
    if (queryParams && typeof queryParams === "object") {
      for (const name in queryParams) {
        const value2 = queryParams[name];
        if (value2 === void 0 || value2 === null) {
          continue;
        }
        const options = parameters[name] || args;
        if (Array.isArray(value2)) {
          const serializedArray = serializeArrayParam({
            allowReserved: options.allowReserved,
            explode: true,
            name,
            style: "form",
            value: value2,
            ...options.array
          });
          if (serializedArray)
            search.push(serializedArray);
        } else if (typeof value2 === "object") {
          const serializedObject = serializeObjectParam({
            allowReserved: options.allowReserved,
            explode: true,
            name,
            style: "deepObject",
            value: value2,
            ...options.object
          });
          if (serializedObject)
            search.push(serializedObject);
        } else {
          const serializedPrimitive = serializePrimitiveParam({
            allowReserved: options.allowReserved,
            name,
            value: value2
          });
          if (serializedPrimitive)
            search.push(serializedPrimitive);
        }
      }
    }
    return search.join("&");
  }, "querySerializer");
  return querySerializer;
}, "createQuerySerializer");
var getParseAs = /* @__PURE__ */ __name((contentType) => {
  if (!contentType) {
    return "stream";
  }
  const cleanContent = contentType.split(";")[0]?.trim();
  if (!cleanContent) {
    return;
  }
  if (cleanContent.startsWith("application/json") || cleanContent.endsWith("+json")) {
    return "json";
  }
  if (cleanContent === "multipart/form-data") {
    return "formData";
  }
  if (["application/", "audio/", "image/", "video/"].some((type) => cleanContent.startsWith(type))) {
    return "blob";
  }
  if (cleanContent.startsWith("text/")) {
    return "text";
  }
  return;
}, "getParseAs");
var checkForExistence = /* @__PURE__ */ __name((options, name) => {
  if (!name) {
    return false;
  }
  if (options.headers.has(name) || options.query?.[name] || options.headers.get("Cookie")?.includes(`${name}=`)) {
    return true;
  }
  return false;
}, "checkForExistence");
var setAuthParams = /* @__PURE__ */ __name(async ({ security, ...options }) => {
  for (const auth of security) {
    if (checkForExistence(options, auth.name)) {
      continue;
    }
    const token = await getAuthToken(auth, options.auth);
    if (!token) {
      continue;
    }
    const name = auth.name ?? "Authorization";
    switch (auth.in) {
      case "query":
        if (!options.query) {
          options.query = {};
        }
        options.query[name] = token;
        break;
      case "cookie":
        options.headers.append("Cookie", `${name}=${token}`);
        break;
      case "header":
      default:
        options.headers.set(name, token);
        break;
    }
  }
}, "setAuthParams");
var buildUrl = /* @__PURE__ */ __name((options) => getUrl({
  baseUrl: options.baseUrl,
  path: options.path,
  query: options.query,
  querySerializer: typeof options.querySerializer === "function" ? options.querySerializer : createQuerySerializer(options.querySerializer),
  url: options.url
}), "buildUrl");
var mergeConfigs = /* @__PURE__ */ __name((a, b) => {
  const config = { ...a, ...b };
  if (config.baseUrl?.endsWith("/")) {
    config.baseUrl = config.baseUrl.substring(0, config.baseUrl.length - 1);
  }
  config.headers = mergeHeaders2(a.headers, b.headers);
  return config;
}, "mergeConfigs");
var headersEntries = /* @__PURE__ */ __name((headers) => {
  const entries = [];
  headers.forEach((value2, key) => {
    entries.push([key, value2]);
  });
  return entries;
}, "headersEntries");
var mergeHeaders2 = /* @__PURE__ */ __name((...headers) => {
  const mergedHeaders = new Headers();
  for (const header of headers) {
    if (!header) {
      continue;
    }
    const iterator = header instanceof Headers ? headersEntries(header) : Object.entries(header);
    for (const [key, value2] of iterator) {
      if (value2 === null) {
        mergedHeaders.delete(key);
      } else if (Array.isArray(value2)) {
        for (const v of value2) {
          mergedHeaders.append(key, v);
        }
      } else if (value2 !== void 0) {
        mergedHeaders.set(key, typeof value2 === "object" ? JSON.stringify(value2) : value2);
      }
    }
  }
  return mergedHeaders;
}, "mergeHeaders");
var Interceptors = class {
  static {
    __name(this, "Interceptors");
  }
  fns = [];
  clear() {
    this.fns = [];
  }
  eject(id) {
    const index = this.getInterceptorIndex(id);
    if (this.fns[index]) {
      this.fns[index] = null;
    }
  }
  exists(id) {
    const index = this.getInterceptorIndex(id);
    return Boolean(this.fns[index]);
  }
  getInterceptorIndex(id) {
    if (typeof id === "number") {
      return this.fns[id] ? id : -1;
    }
    return this.fns.indexOf(id);
  }
  update(id, fn) {
    const index = this.getInterceptorIndex(id);
    if (this.fns[index]) {
      this.fns[index] = fn;
      return id;
    }
    return false;
  }
  use(fn) {
    this.fns.push(fn);
    return this.fns.length - 1;
  }
};
var createInterceptors = /* @__PURE__ */ __name(() => ({
  error: new Interceptors(),
  request: new Interceptors(),
  response: new Interceptors()
}), "createInterceptors");
var defaultQuerySerializer = createQuerySerializer({
  allowReserved: false,
  array: {
    explode: true,
    style: "form"
  },
  object: {
    explode: true,
    style: "deepObject"
  }
});
var defaultHeaders = {
  "Content-Type": "application/json"
};
var createConfig = /* @__PURE__ */ __name((override = {}) => ({
  ...jsonBodySerializer,
  headers: defaultHeaders,
  parseAs: "auto",
  querySerializer: defaultQuerySerializer,
  ...override
}), "createConfig");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/client/client.gen.js
var createClient = /* @__PURE__ */ __name((config = {}) => {
  let _config = mergeConfigs(createConfig(), config);
  const getConfig = /* @__PURE__ */ __name(() => ({ ..._config }), "getConfig");
  const setConfig = /* @__PURE__ */ __name((config2) => {
    _config = mergeConfigs(_config, config2);
    return getConfig();
  }, "setConfig");
  const interceptors = createInterceptors();
  const beforeRequest = /* @__PURE__ */ __name(async (options) => {
    const opts = {
      ..._config,
      ...options,
      fetch: options.fetch ?? _config.fetch ?? globalThis.fetch,
      headers: mergeHeaders2(_config.headers, options.headers),
      serializedBody: void 0
    };
    if (opts.security) {
      await setAuthParams({
        ...opts,
        security: opts.security
      });
    }
    if (opts.requestValidator) {
      await opts.requestValidator(opts);
    }
    if (opts.body !== void 0 && opts.bodySerializer) {
      opts.serializedBody = opts.bodySerializer(opts.body);
    }
    if (opts.body === void 0 || opts.serializedBody === "") {
      opts.headers.delete("Content-Type");
    }
    const url = buildUrl(opts);
    return { opts, url };
  }, "beforeRequest");
  const request = /* @__PURE__ */ __name(async (options) => {
    const { opts, url } = await beforeRequest(options);
    const requestInit = {
      redirect: opts.redirect ?? "follow",
      cache: opts.cache,
      credentials: opts.credentials,
      headers: opts.headers,
      integrity: opts.integrity,
      keepalive: opts.keepalive,
      method: opts.method,
      mode: opts.mode,
      priority: opts.priority,
      referrer: opts.referrer,
      referrerPolicy: opts.referrerPolicy,
      signal: opts.signal,
      body: getValidRequestBody(opts),
      ..."duplex" in opts ? { duplex: opts.duplex } : void 0
    };
    let request2 = new Request(url, requestInit);
    for (const fn of interceptors.request.fns) {
      if (fn) {
        request2 = await fn(request2, opts);
      }
    }
    const _fetch = opts.fetch;
    let response = await _fetch(request2);
    for (const fn of interceptors.response.fns) {
      if (fn) {
        response = await fn(response, request2, opts);
      }
    }
    const result = {
      request: request2,
      response
    };
    if (response.ok) {
      const parseAs = (opts.parseAs === "auto" ? getParseAs(response.headers.get("Content-Type")) : opts.parseAs) ?? "json";
      if (response.status === 204 || response.headers.get("Content-Length") === "0") {
        let emptyData;
        switch (parseAs) {
          case "arrayBuffer":
          case "blob":
          case "text":
            emptyData = await response[parseAs]();
            break;
          case "formData":
            emptyData = new FormData();
            break;
          case "stream":
            emptyData = response.body;
            break;
          case "json":
          default:
            emptyData = {};
            break;
        }
        return opts.responseStyle === "data" ? emptyData : {
          data: emptyData,
          ...result
        };
      }
      let data;
      switch (parseAs) {
        case "arrayBuffer":
        case "blob":
        case "formData":
        case "json":
        case "text":
          data = await response[parseAs]();
          break;
        case "stream":
          return opts.responseStyle === "data" ? response.body : {
            data: response.body,
            ...result
          };
      }
      if (parseAs === "json") {
        if (opts.responseValidator) {
          await opts.responseValidator(data);
        }
        if (opts.responseTransformer) {
          data = await opts.responseTransformer(data);
        }
      }
      return opts.responseStyle === "data" ? data : {
        data,
        ...result
      };
    }
    const textError = await response.text();
    let jsonError;
    try {
      jsonError = JSON.parse(textError);
    } catch {
    }
    const error = jsonError ?? textError;
    let finalError = error;
    for (const fn of interceptors.error.fns) {
      if (fn) {
        finalError = await fn(error, response, request2, opts);
      }
    }
    finalError = finalError || {};
    if (opts.throwOnError) {
      throw finalError;
    }
    return opts.responseStyle === "data" ? void 0 : {
      error: finalError,
      ...result
    };
  }, "request");
  const makeMethodFn = /* @__PURE__ */ __name((method) => (options) => request({ ...options, method }), "makeMethodFn");
  const makeSseFn = /* @__PURE__ */ __name((method) => async (options) => {
    const { opts, url } = await beforeRequest(options);
    return createSseClient({
      ...opts,
      body: opts.body,
      headers: opts.headers,
      method,
      onRequest: /* @__PURE__ */ __name(async (url2, init) => {
        let request2 = new Request(url2, init);
        for (const fn of interceptors.request.fns) {
          if (fn) {
            request2 = await fn(request2, opts);
          }
        }
        return request2;
      }, "onRequest"),
      url
    });
  }, "makeSseFn");
  return {
    buildUrl,
    connect: makeMethodFn("CONNECT"),
    delete: makeMethodFn("DELETE"),
    get: makeMethodFn("GET"),
    getConfig,
    head: makeMethodFn("HEAD"),
    interceptors,
    options: makeMethodFn("OPTIONS"),
    patch: makeMethodFn("PATCH"),
    post: makeMethodFn("POST"),
    put: makeMethodFn("PUT"),
    request,
    setConfig,
    sse: {
      connect: makeSseFn("CONNECT"),
      delete: makeSseFn("DELETE"),
      get: makeSseFn("GET"),
      head: makeSseFn("HEAD"),
      options: makeSseFn("OPTIONS"),
      patch: makeSseFn("PATCH"),
      post: makeSseFn("POST"),
      put: makeSseFn("PUT"),
      trace: makeSseFn("TRACE")
    },
    trace: makeMethodFn("TRACE")
  };
}, "createClient");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/stream.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/sdk.gen.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/client.gen.js
init_esm();
var client = createClient(createConfig({
  baseUrl: "https://a.s2.dev/v1"
}));

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/generated/sdk.gen.js
var listAccessTokens = /* @__PURE__ */ __name((options) => {
  return (options?.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/access-tokens",
    ...options
  });
}, "listAccessTokens");
var issueAccessToken = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).post({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/access-tokens",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "issueAccessToken");
var revokeAccessToken = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).delete({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/access-tokens/{id}",
    ...options
  });
}, "revokeAccessToken");
var listBasins = /* @__PURE__ */ __name((options) => {
  return (options?.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/basins",
    ...options
  });
}, "listBasins");
var createBasin = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).post({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/basins",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "createBasin");
var deleteBasin = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).delete({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/basins/{basin}",
    ...options
  });
}, "deleteBasin");
var getBasinConfig = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/basins/{basin}",
    ...options
  });
}, "getBasinConfig");
var reconfigureBasin = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).patch({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/basins/{basin}",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "reconfigureBasin");
var ensureBasin = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).put({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/basins/{basin}",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "ensureBasin");
var listLocations = /* @__PURE__ */ __name((options) => {
  return (options?.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/locations",
    ...options
  });
}, "listLocations");
var getDefaultLocation = /* @__PURE__ */ __name((options) => {
  return (options?.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/locations/default",
    ...options
  });
}, "getDefaultLocation");
var setDefaultLocation = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).put({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/locations/default",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "setDefaultLocation");
var accountMetrics = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/metrics",
    ...options
  });
}, "accountMetrics");
var basinMetrics = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/metrics/{basin}",
    ...options
  });
}, "basinMetrics");
var streamMetrics = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/metrics/{basin}/{stream}",
    ...options
  });
}, "streamMetrics");
var listStreams = /* @__PURE__ */ __name((options) => {
  return (options?.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams",
    ...options
  });
}, "listStreams");
var createStream = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).post({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "createStream");
var deleteStream = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).delete({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams/{stream}",
    ...options
  });
}, "deleteStream");
var getStreamConfig = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams/{stream}",
    ...options
  });
}, "getStreamConfig");
var reconfigureStream = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).patch({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams/{stream}",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "reconfigureStream");
var ensureStream = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).put({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams/{stream}",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "ensureStream");
var read = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams/{stream}/records",
    ...options
  });
}, "read");
var append = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).post({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams/{stream}/records",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers
    }
  });
}, "append");
var checkTail = /* @__PURE__ */ __name((options) => {
  return (options.client ?? client).get({
    security: [
      {
        scheme: "bearer",
        type: "http"
      }
    ],
    url: "/streams/{stream}/records/tail",
    ...options
  });
}, "checkTail");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/lib/stream/factory.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/lib/stream/transport/fetch/index.js
init_esm();
var import_debug2 = __toESM(require_src4(), 1);

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/lib/event-stream.js
init_esm();
var import_debug = __toESM(require_src4(), 1);
var debug = (0, import_debug.default)("s2:event-stream");
var EventStream = class extends ReadableStream {
  static {
    __name(this, "EventStream");
  }
  constructor(responseBody, parse) {
    const upstream = responseBody.getReader();
    let buffer = new Uint8Array();
    const eventStreamId = Math.random().toString(36).slice(2);
    super({
      async pull(downstream) {
        try {
          while ((downstream.desiredSize ?? 0) > 0) {
            debug("pull loop, eventstream=%s", eventStreamId);
            const match = findBoundary(buffer);
            if (!match) {
              const chunk = await upstream.read();
              if (chunk.done) {
                if (buffer.length > 0) {
                  throw new S2Error({
                    message: "SSE stream closed with incomplete data remaining",
                    status: 502,
                    code: "STREAM_CLOSED_PREMATURELY",
                    origin: "sdk"
                  });
                }
                return downstream.close();
              }
              buffer = concatBuffer(buffer, chunk.value);
              continue;
            }
            const message = buffer.slice(0, match.index);
            buffer = buffer.slice(match.index + match.length);
            const item = parseMessage(message, parse);
            if (item) {
              if (item.batch) {
                for (const chunk of item.value) {
                  downstream.enqueue(chunk);
                }
              } else if (item.value !== void 0) {
                downstream.enqueue(item.value);
              } else if (item.done) {
                await upstream.cancel("done");
                downstream.close();
                return;
              }
            }
          }
        } catch (e) {
          downstream.error(e);
          try {
            await upstream.cancel(e);
          } catch {
          }
        }
      },
      cancel: /* @__PURE__ */ __name((reason) => upstream.cancel(reason), "cancel")
    });
  }
  async [Symbol.asyncDispose]() {
    await this.cancel("disposed");
  }
  // Polyfill for older browsers
  [Symbol.asyncIterator]() {
    const proto = ReadableStream.prototype;
    const fn = proto[Symbol.asyncIterator];
    if (typeof fn === "function") {
      try {
        return fn.call(this);
      } catch {
      }
    }
    const reader = this.getReader();
    return {
      next: /* @__PURE__ */ __name(async () => {
        const r = await reader.read();
        if (r.done) {
          reader.releaseLock();
          return { done: true, value: void 0 };
        }
        return { done: false, value: r.value };
      }, "next"),
      throw: /* @__PURE__ */ __name(async (e) => {
        try {
          await reader.cancel(e);
        } catch (err2) {
          if (err2?.code !== "ERR_INVALID_STATE")
            throw err2;
        }
        reader.releaseLock();
        return { done: true, value: void 0 };
      }, "throw"),
      return: /* @__PURE__ */ __name(async () => {
        try {
          await reader.cancel("done");
        } catch (err2) {
          if (err2?.code !== "ERR_INVALID_STATE")
            throw err2;
        }
        reader.releaseLock();
        return { done: true, value: void 0 };
      }, "return"),
      [Symbol.asyncIterator]() {
        return this;
      }
    };
  }
};
function concatBuffer(a, b) {
  const c = new Uint8Array(a.length + b.length);
  c.set(a, 0);
  c.set(b, a.length);
  return c;
}
__name(concatBuffer, "concatBuffer");
function findBoundary(buf) {
  const len = buf.length;
  const CR = 13;
  const LF = 10;
  for (let i = 0; i < len; i++) {
    let firstLen;
    if (buf[i] === CR && i + 1 < len && buf[i + 1] === LF) {
      firstLen = 2;
    } else if (buf[i] === CR) {
      firstLen = 1;
    } else if (buf[i] === LF) {
      firstLen = 1;
    } else {
      continue;
    }
    const j = i + firstLen;
    if (j >= len)
      return null;
    let secondLen;
    if (buf[j] === CR && j + 1 < len && buf[j + 1] === LF) {
      secondLen = 2;
    } else if (buf[j] === CR) {
      secondLen = 1;
    } else if (buf[j] === LF) {
      secondLen = 1;
    } else {
      continue;
    }
    return { index: i, length: firstLen + secondLen };
  }
  return null;
}
__name(findBoundary, "findBoundary");
function parseMessage(chunk, parse) {
  const text = new TextDecoder().decode(chunk);
  const lines = text.split(/\r\n|\r|\n/);
  const dataLines = [];
  const ret = {};
  let ignore = true;
  for (const line of lines) {
    if (!line || line.startsWith(":"))
      continue;
    ignore = false;
    const i = line.indexOf(":");
    let field;
    let value2;
    if (i === -1) {
      field = line;
      value2 = "";
    } else {
      field = line.slice(0, i);
      value2 = line[i + 1] === " " ? line.slice(i + 2) : line.slice(i + 1);
    }
    if (field === "data")
      dataLines.push(value2);
    else if (field === "event")
      ret.event = value2;
    else if (field === "id")
      ret.id = value2;
    else if (field === "retry") {
      const n = Number(value2);
      if (!isNaN(n))
        ret.retry = n;
    }
  }
  if (ignore)
    return;
  if (dataLines.length)
    ret.data = dataLines.join("\n");
  return parse(ret);
}
__name(parseMessage, "parseMessage");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/lib/stream/transport/fetch/shared.js
init_esm();
function mergeHeaders3(...headersList) {
  const merged = Object.assign({}, ...headersList.filter((headers) => Boolean(headers)));
  return Object.keys(merged).length > 0 ? merged : void 0;
}
__name(mergeHeaders3, "mergeHeaders");
async function streamRead(stream, client2, args, options) {
  const { as, ignore_command_records, ...queryParams } = args ?? {};
  const { headers: customHeaders, ...requestOptions } = options ?? {};
  const wantsBytes = (as ?? "string") === "bytes";
  let response;
  try {
    response = await read({
      client: client2,
      path: {
        stream
      },
      headers: mergeHeaders3(customHeaders, wantsBytes ? { Accept: "application/protobuf" } : void 0),
      query: queryParams,
      parseAs: wantsBytes ? "arrayBuffer" : void 0,
      ...requestOptions
    });
  } catch (error) {
    throw s2Error(error);
  }
  if (response.error || !response.response.ok) {
    const status = response.response.status;
    if (status === 416) {
      const err2 = response.error;
      throw new RangeNotSatisfiableError({
        status,
        tail: err2?.tail ? fromAPIStreamPosition(err2.tail) : void 0,
        code: err2?.code
      });
    }
    throw makeServerError({ status, statusText: response.response.statusText }, response.error);
  }
  if (wantsBytes) {
    const batch = decodeProtoReadBatch(response.data);
    return batch;
  }
  const res = {
    ...response.data,
    records: response.data.records?.map((record) => ({
      ...record,
      headers: record.headers || void 0
    })) ?? []
  };
  return res;
}
__name(streamRead, "streamRead");
async function streamAppend(stream, client2, input, options) {
  const { preferProtobuf, headers: customHeaders, ...requestOptions } = options ?? {};
  const hasAnyBytesRecords = input.records.some((record) => computeAppendRecordFormat(record) === "bytes");
  const useProtobuf = hasAnyBytesRecords || preferProtobuf === true;
  let response;
  if (useProtobuf) {
    const protoBody = encodeProtoAppendInput(input);
    const headers = mergeHeaders3(customHeaders, {
      Accept: "application/protobuf",
      "Content-Type": "application/protobuf"
    });
    try {
      response = await append({
        client: client2,
        path: {
          stream
        },
        body: protoBody,
        bodySerializer: null,
        parseAs: "arrayBuffer",
        headers,
        ...requestOptions
      });
    } catch (error) {
      throw s2Error(error);
    }
    if (response.error || !response.response.ok) {
      const status = response.response.status;
      if (status === 412) {
        throw makeAppendPreconditionError(status, response.error);
      }
      throw makeServerError({ status, statusText: response.response.statusText }, response.error);
    }
    const ack = decodeProtoAppendAck(response.data);
    return protoAppendAckToJson(ack);
  }
  const encodedRecords = [...input.records].map(toAPIAppendRecord);
  try {
    response = await append({
      client: client2,
      path: {
        stream
      },
      body: {
        fencing_token: input.fencingToken,
        match_seq_num: input.matchSeqNum,
        records: encodedRecords
      },
      headers: customHeaders,
      ...requestOptions
    });
  } catch (error) {
    throw s2Error(error);
  }
  if (response.error || !response.response.ok) {
    const status = response.response.status;
    if (status === 412) {
      throw makeAppendPreconditionError(status, response.error);
    }
    throw makeServerError({ status, statusText: response.response.statusText }, response.error);
  }
  return fromAPIAppendAck(response.data);
}
__name(streamAppend, "streamAppend");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/lib/stream/transport/fetch/index.js
var debug2 = (0, import_debug2.default)("s2:fetch");
var FetchReadSession = class _FetchReadSession extends ReadableStream {
  static {
    __name(this, "FetchReadSession");
  }
  static async create(client2, name, args, options) {
    debug2("FetchReadSession.create stream=%s args=%o", name, args);
    const { as, ignore_command_records, ...queryParams } = args ?? {};
    const response = await read({
      client: client2,
      path: {
        stream: name
      },
      headers: {
        accept: "text/event-stream",
        ...as === "bytes" ? { "s2-format": "base64" } : {}
      },
      query: queryParams,
      parseAs: "stream",
      ...options
    });
    if (response.error) {
      const status = response.response.status;
      if (status === 416) {
        const err2 = typeof response.error === "object" && response.error !== null ? response.error : void 0;
        throw new RangeNotSatisfiableError({
          status,
          code: err2?.code,
          tail: err2?.tail ? fromAPIStreamPosition(err2.tail) : void 0
        });
      }
      const error = typeof response.error === "object" && response.error !== null && "message" in response.error ? new S2Error({
        message: response.error.message,
        code: response.error.code ?? void 0,
        status
      }) : new S2Error({
        message: response.response.statusText ?? "Request failed",
        status
      });
      throw error;
    }
    if (!response.response.body) {
      throw new S2Error({
        message: "No body in SSE response",
        code: "INVALID_RESPONSE",
        status: 502,
        origin: "sdk"
      });
    }
    const format = args?.as ?? "string";
    return new _FetchReadSession(response.response.body, format);
  }
  /** @internal */
  static _createForTesting(stream, format) {
    return new _FetchReadSession(stream, format);
  }
  _lastObservedTail = void 0;
  constructor(stream, format) {
    let parserError = null;
    let lastPingTimeMs = performance.now();
    const PING_TIMEOUT_MS = 25e3;
    const eventStream = new EventStream(stream, (msg) => {
      lastPingTimeMs = performance.now();
      if (msg.event === "batch" && msg.data) {
        const rawBatch = JSON.parse(msg.data);
        const batch = (() => {
          if (format === "bytes") {
            return {
              ...rawBatch,
              records: rawBatch.records.map((record) => ({
                ...record,
                body: record.body ? decodeFromBase64(record.body) : void 0,
                headers: record.headers?.map((header) => header.map((h) => decodeFromBase64(h)))
              }))
            };
          } else {
            return {
              ...rawBatch,
              records: rawBatch.records.map((record) => ({
                ...record,
                headers: record.headers || void 0
              }))
            };
          }
        })();
        if (batch.tail) {
          this._lastObservedTail = batch.tail;
        }
        return { done: false, value: { ok: true, batch } };
      }
      if (msg.event === "error") {
        debug2("parse event error");
        parserError = new S2Error({
          message: msg.data ?? "Unknown error",
          status: 503
        });
        return { done: true };
      }
      if (msg.event === "ping") {
        debug2("ping");
        if (msg.data) {
          try {
            const ping = JSON.parse(msg.data);
            if (ping.tail) {
              this._lastObservedTail = ping.tail;
              return {
                done: false,
                value: {
                  ok: true,
                  batch: { records: [], tail: ping.tail }
                }
              };
            }
          } catch {
          }
        }
      }
      return { done: false };
    });
    const reader = eventStream.getReader();
    const cancelReader = /* @__PURE__ */ __name(async (reason) => {
      try {
        await reader.cancel(reason);
      } catch {
      }
    }, "cancelReader");
    let done = false;
    let pendingRead = null;
    super({
      pull: /* @__PURE__ */ __name(async (controller) => {
        while (true) {
          if (done) {
            controller.close();
            return;
          }
          const capturedLastPingTime = lastPingTimeMs;
          const remainingTimeMs = PING_TIMEOUT_MS - (performance.now() - lastPingTimeMs);
          if (!pendingRead) {
            pendingRead = reader.read();
          }
          const currentRead = pendingRead;
          let timeoutHandle;
          try {
            const result = await Promise.race([
              currentRead.then((r) => {
                pendingRead = null;
                return { type: "data", value: r };
              }),
              new Promise((resolve2) => {
                timeoutHandle = setTimeout(() => {
                  if (lastPingTimeMs === capturedLastPingTime) {
                    debug2("read session ping timeout");
                    resolve2({ type: "timeout" });
                  } else {
                    resolve2({ type: "stale" });
                  }
                }, remainingTimeMs);
              })
            ]);
            if (result.type === "stale") {
              continue;
            }
            if (result.type === "timeout") {
              const elapsed = performance.now() - lastPingTimeMs;
              const timeoutError = new S2Error({
                message: `No ping received for ${Math.floor(elapsed / 1e3)}s (timeout: ${PING_TIMEOUT_MS / 1e3}s)`,
                status: 408,
                code: "TIMEOUT"
              });
              debug2("read session timeout elapsed_ms=%d timeout_ms=%d status=%d code=%s", Math.floor(elapsed), PING_TIMEOUT_MS, timeoutError.status, timeoutError.code);
              controller.enqueue({ ok: false, error: timeoutError });
              done = true;
              await cancelReader(timeoutError);
              controller.close();
              return;
            }
            if (result.value.done) {
              done = true;
              if (parserError) {
                controller.enqueue({ ok: false, error: parserError });
              }
              controller.close();
            } else {
              controller.enqueue(result.value.value);
            }
            return;
          } catch (error) {
            const readError = s2Error(error);
            debug2("read session body error status=%d code=%s origin=%s message=%s", readError.status, readError.code, readError.origin, readError.message);
            controller.enqueue({ ok: false, error: readError });
            done = true;
            await cancelReader(error);
            controller.close();
            return;
          } finally {
            if (timeoutHandle)
              clearTimeout(timeoutHandle);
          }
        }
      }, "pull"),
      cancel: /* @__PURE__ */ __name(async () => {
        await cancelReader("cancelled");
      }, "cancel")
    });
  }
  lastObservedTail() {
    return this._lastObservedTail;
  }
  // Implement AsyncIterable (for await...of support)
  [Symbol.asyncIterator]() {
    const proto = ReadableStream.prototype;
    const fn = proto[Symbol.asyncIterator];
    if (typeof fn === "function") {
      try {
        return fn.call(this);
      } catch {
      }
    }
    const reader = this.getReader();
    return {
      next: /* @__PURE__ */ __name(async () => {
        const r = await reader.read();
        if (r.done)
          return { done: true, value: void 0 };
        return { done: false, value: r.value };
      }, "next"),
      return: /* @__PURE__ */ __name(async (value2) => {
        try {
          await reader.cancel();
        } catch (err2) {
          if (err2?.code !== "ERR_INVALID_STATE")
            throw err2;
        }
        reader.releaseLock();
        return { done: true, value: value2 };
      }, "return"),
      throw: /* @__PURE__ */ __name(async (e) => {
        try {
          await reader.cancel(e);
        } catch (err2) {
          if (err2?.code !== "ERR_INVALID_STATE")
            throw err2;
        }
        reader.releaseLock();
        throw e;
      }, "throw"),
      [Symbol.asyncIterator]() {
        return this;
      }
    };
  }
  // Implement AsyncDisposable
  async [Symbol.asyncDispose]() {
    await this.cancel();
  }
};
var FetchAppendSession = class _FetchAppendSession {
  static {
    __name(this, "FetchAppendSession");
  }
  queue = new FifoQueue();
  pendingResolvers = new FifoQueue();
  inFlight = false;
  _effectSignalled = false;
  options;
  stream;
  closed = false;
  processingPromise = null;
  client;
  static async create(stream, transportConfig, sessionOptions, requestOptions) {
    return new _FetchAppendSession(stream, transportConfig, sessionOptions, requestOptions);
  }
  constructor(stream, transportConfig, sessionOptions, requestOptions) {
    this.options = requestOptions;
    this.stream = stream;
    debug2("[%s] FetchAppendSession created", stream);
    const headers = {};
    if (transportConfig.basinName) {
      headers["s2-basin"] = transportConfig.basinName;
    }
    if (transportConfig.encryptionKey) {
      headers[S2_ENCRYPTION_KEY_HEADER] = value(transportConfig.encryptionKey);
    }
    if (canSetUserAgentHeader()) {
      headers["user-agent"] = DEFAULT_USER_AGENT;
    }
    this.client = createClient(createConfig({
      baseUrl: transportConfig.baseUrl,
      auth: /* @__PURE__ */ __name(() => value(transportConfig.accessToken), "auth"),
      headers
    }));
  }
  /**
   * Returns true if data may have been sent to the server since the
   * last time the session was dormant (queue fully drained with no errors).
   * Only resets after the processing loop completes successfully.
   */
  effectSignalled() {
    return this._effectSignalled;
  }
  /**
   * Close the append session.
   * Waits for all pending appends to complete before resolving.
   * Never throws - returns CloseResult.
   */
  async close() {
    debug2("[%s] FetchAppendSession close requested", this.stream);
    try {
      this.closed = true;
      await this.waitForDrain();
      debug2("[%s] FetchAppendSession close complete", this.stream);
      return okClose();
    } catch (error) {
      const s2Err = s2Error(error);
      debug2("[%s] FetchAppendSession close error: %s", this.stream, s2Err.message);
      return errClose(s2Err);
    }
  }
  /**
   * Submit an append request to the session.
   * The request will be queued and sent when no other request is in-flight.
   * Never throws - returns AppendResult discriminated union.
   */
  submit(input) {
    debug2("[%s] FetchAppendSession.submit: records=%d, match_seq_num=%s, queueLen=%d", this.stream, input.records.length, input.matchSeqNum ?? "none", this.queue.length);
    if (this.closed) {
      debug2("[%s] FetchAppendSession.submit: session closed, rejecting", this.stream);
      return Promise.resolve(err(new S2Error({ message: "AppendSession is closed", status: 400 })));
    }
    if (input.records.length > 1e3) {
      debug2("[%s] FetchAppendSession.submit: batch too large (%d records)", this.stream, input.records.length);
      return Promise.resolve(err(new S2Error({
        message: `Batch of ${input.records.length} exceeds maximum batch size of 1000 records`,
        status: 400,
        code: "INVALID_ARGUMENT"
      })));
    }
    const batchMeteredSize = input.meteredBytes;
    if (batchMeteredSize > 1024 * 1024) {
      debug2("[%s] FetchAppendSession.submit: batch too large (%d bytes)", this.stream, batchMeteredSize);
      return Promise.resolve(err(new S2Error({
        message: `Batch size ${batchMeteredSize} bytes exceeds maximum of 1 MiB (1048576 bytes)`,
        status: 400,
        code: "INVALID_ARGUMENT"
      })));
    }
    return new Promise((resolve2) => {
      this.queue.push(input);
      this.pendingResolvers.push({ resolve: resolve2 });
      debug2("[%s] FetchAppendSession.submit: queued, queueLen=%d", this.stream, this.queue.length);
      if (!this.processingPromise) {
        this.processingPromise = this.processLoop().catch(() => {
        });
      }
    });
  }
  /**
   * Main processing loop that sends queued requests one at a time.
   * Single-flight: only one request in progress at a time.
   */
  async processLoop() {
    debug2("[%s] FetchAppendSession.processLoop: starting", this.stream);
    while (this.queue.length > 0) {
      this.inFlight = true;
      const input = this.queue.shift();
      const resolver = this.pendingResolvers.shift();
      debug2("[%s] FetchAppendSession.processLoop: sending %d records, match_seq_num=%s", this.stream, input.records.length, input.matchSeqNum ?? "none");
      this._effectSignalled = true;
      try {
        const preferProtobuf = input.records.some((record) => computeAppendRecordFormat(record) === "bytes");
        const appendOptions = this.options ? { ...this.options, preferProtobuf } : { preferProtobuf };
        const ack = await streamAppend(this.stream, this.client, input, appendOptions);
        debug2("[%s] FetchAppendSession.processLoop: success, seq_num=%d-%d", this.stream, ack.start.seqNum, ack.end.seqNum);
        resolver.resolve(ok(ack));
      } catch (error) {
        const s2Err = s2Error(error);
        debug2("[%s] FetchAppendSession.processLoop: error, status=%s, message=%s", this.stream, s2Err.status, s2Err.message);
        resolver.resolve(err(s2Err));
        debug2("[%s] FetchAppendSession.processLoop: failing %d queued requests", this.stream, this.pendingResolvers.length);
        for (const pendingResolver of this.pendingResolvers) {
          pendingResolver.resolve(err(s2Err));
        }
        this.pendingResolvers.clear();
        this.queue.clear();
        this.inFlight = false;
        this.processingPromise = null;
        return;
      }
      this.inFlight = false;
    }
    this._effectSignalled = false;
    debug2("[%s] FetchAppendSession.processLoop: done", this.stream);
    this.processingPromise = null;
  }
  async waitForDrain() {
    if (this.processingPromise) {
      await this.processingPromise;
    }
    while (this.queue.length > 0 || this.inFlight) {
      await new Promise((resolve2) => setTimeout(resolve2, 10));
    }
  }
};
var FetchTransport = class {
  static {
    __name(this, "FetchTransport");
  }
  client;
  transportConfig;
  constructor(config) {
    const headers = {};
    if (config.basinName) {
      headers["s2-basin"] = config.basinName;
    }
    if (config.encryptionKey) {
      headers[S2_ENCRYPTION_KEY_HEADER] = value(config.encryptionKey);
    }
    if (canSetUserAgentHeader()) {
      headers["user-agent"] = DEFAULT_USER_AGENT;
    }
    this.client = createClient(createConfig({
      baseUrl: config.baseUrl,
      auth: /* @__PURE__ */ __name(() => value(config.accessToken), "auth"),
      headers
    }));
    this.transportConfig = config;
  }
  async makeAppendSession(stream, sessionOptions, requestOptions) {
    return RetryAppendSession.create((myOptions) => {
      return FetchAppendSession.create(stream, this.transportConfig, myOptions, requestOptions);
    }, sessionOptions, this.transportConfig.retry, stream);
  }
  async makeReadSession(stream, args, options) {
    return RetryReadSession.create((myArgs) => {
      return FetchReadSession.create(this.client, stream, myArgs, options);
    }, args, this.transportConfig.retry);
  }
  async close() {
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/lib/stream/factory.js
async function createSessionTransport(config) {
  if (config?.forceTransport === "fetch") {
    return new FetchTransport(config);
  } else if (config?.forceTransport === "s2s") {
    const { S2STransport } = await import("./s2s-42PP5S3P.mjs");
    return new S2STransport(config);
  }
  if (supportsHttp2()) {
    const { S2STransport } = await import("./s2s-42PP5S3P.mjs");
    return new S2STransport(config);
  }
  return new FetchTransport(config);
}
__name(createSessionTransport, "createSessionTransport");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/stream.js
var S2Stream = class _S2Stream {
  static {
    __name(this, "S2Stream");
  }
  client;
  transportConfig;
  retryConfig;
  _transportPromise;
  closed = false;
  closePromise;
  name;
  constructor(name, client2, transportConfig, retryConfig) {
    this.name = name;
    this.client = client2;
    this.transportConfig = transportConfig;
    this.retryConfig = retryConfig;
  }
  /**
   * Get or create the transport instance
   */
  async getTransport() {
    this.ensureOpen();
    if (!this._transportPromise) {
      this._transportPromise = createSessionTransport(this.transportConfig).catch((err2) => {
        this._transportPromise = void 0;
        throw err2;
      });
    }
    return this._transportPromise;
  }
  ensureOpen() {
    if (this.closed) {
      throw new S2Error({ message: "S2Stream is closed" });
    }
  }
  withEncryptionHeaders(options) {
    const encryptionKey = this.transportConfig.encryptionKey;
    if (!encryptionKey) {
      return options;
    }
    const requestOptions = options ?? {};
    return {
      ...requestOptions,
      headers: {
        ...requestOptions.headers ?? {},
        [S2_ENCRYPTION_KEY_HEADER]: value(encryptionKey)
      }
    };
  }
  /**
   * Return a new stream handle that sends the supplied encryption key on append/read requests.
   */
  withEncryptionKey(encryptionKey) {
    return new _S2Stream(this.name, this.client, {
      ...this.transportConfig,
      encryptionKey: resolveEncryptionKey(encryptionKey)
    }, this.retryConfig);
  }
  /**
   * Check the tail of the stream.
   *
   * Returns the next sequence number and timestamp to be assigned (`tail`).
   */
  async checkTail(options) {
    this.ensureOpen();
    return await withRetries(this.retryConfig, async () => {
      const response = await withS2Data(() => checkTail({
        client: this.client,
        path: {
          stream: this.name
        },
        ...options
      }));
      return fromAPITailResponse(response);
    });
  }
  /**
   * Read records from the stream.
   *
   * - When `as: "bytes"` is provided, bodies and headers are decoded from base64 to `Uint8Array`.
   * - Supports starting position by `seq_num`, `timestamp`, or `tail_offset` and can clamp to the tail.
   * - Non-streaming reads are bounded by `count` and `bytes` (defaults 1000 and 1 MiB).
   * - When `ignoreCommandRecords` is set, command records are filtered from the single
   *   returned batch, which may then be empty. Use `readSession` to keep reading until
   *   data records are found.
   */
  async read(input, options) {
    this.ensureOpen();
    const { as, ...requestOptions } = options ?? {};
    return await withRetries(this.retryConfig, async () => {
      const readArgs = {
        ...toAPIReadQuery(input),
        as,
        ignore_command_records: input?.ignoreCommandRecords
      };
      const genBatch = await streamRead(this.name, this.client, readArgs, this.withEncryptionHeaders(requestOptions));
      const batch = as === "bytes" ? fromAPIReadBatchBytes(genBatch) : fromAPIReadBatchString(genBatch);
      if (!input?.ignoreCommandRecords) {
        return batch;
      }
      return {
        ...batch,
        records: batch.records.filter((r) => !isCommandRecord(r))
      };
    });
  }
  /**
   * Append a batch of records to the stream.
   *
   * - Supports conditional appends via `fencingToken` and `matchSeqNum` in the input.
   * - Returns the acknowledged range and the stream tail after the append.
   * - A batch may contain both string and bytes records.
   *
   * Use {@link AppendInput.create} to construct a validated AppendInput.
   * For high-throughput sequential appends, use `appendSession()` instead.
   *
   * @param input The append input containing records and optional conditions
   * @param options Optional request options
   */
  async append(input, options) {
    this.ensureOpen();
    return await withRetries(this.retryConfig, async () => {
      return await streamAppend(this.name, this.client, input, this.withEncryptionHeaders(options));
    }, (config, error) => {
      if ((config.appendRetryPolicy ?? "all") === "noSideEffects") {
        return error.hasNoSideEffects();
      }
      return true;
    });
  }
  /**
   * Open a streaming read session
   *
   * Use the returned session as an async iterable or as a readable stream.
   * When `as: "bytes"` is provided, bodies and headers are decoded to `Uint8Array`.
   */
  async readSession(input, options) {
    this.ensureOpen();
    const { as, ...requestOptions } = options ?? {};
    const transport = await this.getTransport();
    this.ensureOpen();
    const readArgs = {
      ...toAPIReadQuery(input),
      as,
      ignore_command_records: input?.ignoreCommandRecords
    };
    return await transport.makeReadSession(this.name, readArgs, requestOptions);
  }
  /**
   * Create an append session that guarantees ordering of submissions.
   *
   * Use this to coordinate high-throughput, sequential appends with backpressure.
   * Records can be either string or bytes format - the format is specified in each record.
   *
   * @param sessionOptions Options that control append session behavior
   * @param requestOptions Optional request options
   */
  async appendSession(sessionOptions, requestOptions) {
    this.ensureOpen();
    const transport = await this.getTransport();
    this.ensureOpen();
    return await transport.makeAppendSession(this.name, sessionOptions, requestOptions);
  }
  async close() {
    if (this.closePromise) {
      return this.closePromise;
    }
    this.closePromise = (async () => {
      if (this.closed) {
        return;
      }
      this.closed = true;
      if (this._transportPromise) {
        try {
          const transport = await this._transportPromise;
          await transport.close();
        } finally {
          this._transportPromise = void 0;
        }
      }
    })();
    try {
      await this.closePromise;
    } finally {
      this.closePromise = void 0;
    }
  }
  async [Symbol.asyncDispose]() {
    await this.close();
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/streams.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/internal/case-transform.js
init_esm();
function snakeToCamelString(str) {
  const parts = str.split("_");
  return parts[0] + parts.slice(1).map((part) => part.charAt(0).toUpperCase() + part.slice(1)).join("");
}
__name(snakeToCamelString, "snakeToCamelString");
function camelToSnakeString(str) {
  return str.replace(/[A-Z]/g, (letter) => `_${letter.toLowerCase()}`);
}
__name(camelToSnakeString, "camelToSnakeString");
function toCamelCase(obj) {
  if (obj === null || obj === void 0) {
    return obj;
  }
  if (typeof obj !== "object") {
    return obj;
  }
  if (Array.isArray(obj)) {
    return obj.map((item) => toCamelCase(item));
  }
  if (ArrayBuffer.isView(obj)) {
    return obj;
  }
  const proto = Object.getPrototypeOf(obj);
  if (proto !== Object.prototype && proto !== null) {
    return obj;
  }
  const result = {};
  for (const [key, value2] of Object.entries(obj)) {
    result[snakeToCamelString(key)] = toCamelCase(value2);
  }
  return result;
}
__name(toCamelCase, "toCamelCase");
function toSnakeCase(obj) {
  if (obj === null || obj === void 0) {
    return obj;
  }
  if (typeof obj !== "object") {
    return obj;
  }
  if (Array.isArray(obj)) {
    return obj.map((item) => toSnakeCase(item));
  }
  if (ArrayBuffer.isView(obj)) {
    return obj;
  }
  const proto = Object.getPrototypeOf(obj);
  if (proto !== Object.prototype && proto !== null) {
    return obj;
  }
  const result = {};
  for (const [key, value2] of Object.entries(obj)) {
    result[camelToSnakeString(key)] = toSnakeCase(value2);
  }
  return result;
}
__name(toSnakeCase, "toSnakeCase");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/internal/provisioning.js
init_esm();
var PROVISION_RESULT_HEADER = "s2-provision-result";
function provisionResultFromResponse(response) {
  const value2 = response?.headers?.get(PROVISION_RESULT_HEADER);
  if (value2 === "created" || value2 === "updated" || value2 === "noop") {
    return value2;
  }
  return response?.status === 201 ? "created" : "updated";
}
__name(provisionResultFromResponse, "provisionResultFromResponse");
async function withS2DataAndResponse(fn) {
  const result = await withS2Error(fn);
  if (result && typeof result === "object" && (Object.prototype.hasOwnProperty.call(result, "data") || Object.prototype.hasOwnProperty.call(result, "response") || Object.prototype.hasOwnProperty.call(result, "error"))) {
    const response = result.response;
    const data = result.data;
    if (typeof data !== "undefined") {
      return { data, response };
    }
    throw new S2Error({
      message: "Empty response",
      status: response?.status ?? 500,
      origin: "server"
    });
  }
  return { data: result };
}
__name(withS2DataAndResponse, "withS2DataAndResponse");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/lib/paginate.js
init_esm();
var import_debug3 = __toESM(require_src4(), 1);
var debug3 = (0, import_debug3.default)("s2:paginate");
function paginate(fetcher, args, getCursor) {
  return {
    [Symbol.asyncIterator]: async function* () {
      let cursor;
      while (true) {
        debug3({ args, cursor });
        const { items, hasMore } = await fetcher({
          ...args,
          startAfter: cursor
        });
        for (const item of items) {
          yield item;
        }
        if (!hasMore || items.length === 0) {
          break;
        }
        cursor = getCursor(items[items.length - 1]);
      }
    }
  };
}
__name(paginate, "paginate");
function filterAsync(source, predicate) {
  return {
    [Symbol.asyncIterator]: async function* () {
      for await (const item of source) {
        if (predicate(item)) {
          yield item;
        }
      }
    }
  };
}
__name(filterAsync, "filterAsync");

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/streams.js
function toDate(value2) {
  if (value2 === null)
    return null;
  if (value2 === void 0)
    return void 0;
  return new Date(value2);
}
__name(toDate, "toDate");
function transformStreamInfo(stream) {
  return {
    ...stream,
    createdAt: toDate(stream.createdAt),
    deletedAt: toDate(stream.deletedAt)
  };
}
__name(transformStreamInfo, "transformStreamInfo");
function toAPIRetentionPolicy(policy) {
  if (policy === null)
    return null;
  if (policy === void 0)
    return void 0;
  if ("ageSecs" in policy) {
    return { age: Math.floor(policy.ageSecs) };
  }
  return policy;
}
__name(toAPIRetentionPolicy, "toAPIRetentionPolicy");
function toSDKRetentionPolicy(policy) {
  if (policy === null)
    return null;
  if (policy === void 0)
    return void 0;
  if ("age" in policy) {
    return { ageSecs: policy.age };
  }
  return policy;
}
__name(toSDKRetentionPolicy, "toSDKRetentionPolicy");
function toAPIDeleteOnEmpty(deleteOnEmpty) {
  if (!deleteOnEmpty)
    return deleteOnEmpty;
  return {
    ...deleteOnEmpty,
    minAgeSecs: deleteOnEmpty.minAgeSecs === void 0 ? void 0 : Math.max(0, Math.floor(deleteOnEmpty.minAgeSecs))
  };
}
__name(toAPIDeleteOnEmpty, "toAPIDeleteOnEmpty");
function toAPIStreamConfig(config) {
  if (config === null || config === void 0)
    return config;
  return {
    ...config,
    deleteOnEmpty: toAPIDeleteOnEmpty(config.deleteOnEmpty),
    retentionPolicy: toAPIRetentionPolicy(config.retentionPolicy)
  };
}
__name(toAPIStreamConfig, "toAPIStreamConfig");
function toSDKStreamConfig(config) {
  return {
    ...config,
    retentionPolicy: toSDKRetentionPolicy(config?.retentionPolicy)
  };
}
__name(toSDKStreamConfig, "toSDKStreamConfig");
var S2Streams = class {
  static {
    __name(this, "S2Streams");
  }
  client;
  retryConfig;
  constructor(client2, retryConfig) {
    this.client = client2;
    this.retryConfig = retryConfig;
  }
  /**
   * List streams in the basin.
   *
   * @param args.prefix Return streams whose names start with the given prefix
   * @param args.startAfter Name to start after (for pagination)
   * @param args.limit Max results (up to 1000)
   */
  async list(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => listStreams({
        client: this.client,
        query: toSnakeCase(args),
        ...options
      }));
    });
    const camelCased = toCamelCase(response);
    return {
      ...camelCased,
      streams: camelCased.streams.map(transformStreamInfo)
    };
  }
  /**
   * List all streams in the basin with automatic pagination.
   * Returns a lazy async iterable that fetches pages as needed.
   *
   * @param args - Optional options: `prefix` to filter by name prefix, `limit` for max results per page, `includeDeleted` to include streams pending deletion
   *
   * @example
   * ```ts
   * for await (const stream of basin.streams.listAll({ prefix: "events-" })) {
   *   console.log(stream.name);
   * }
   * ```
   */
  listAll(args, options) {
    const { includeDeleted, ...listArgs } = args ?? {};
    const allItems = paginate((a) => this.list(a, options).then((r) => ({
      items: r.streams,
      hasMore: r.hasMore
    })), listArgs, (stream) => stream.name);
    if (includeDeleted) {
      return allItems;
    }
    return filterAsync(allItems, (s) => !s.deletedAt);
  }
  /**
   * Create a stream.
   *
   * @param args.stream Stream name (1-512 bytes, unique within the basin)
   * @param args.config Stream configuration (retentionPolicy, storageClass, timestamping, deleteOnEmpty)
   */
  async create(args, options) {
    const requestToken = randomToken();
    const apiArgs = {
      ...args,
      config: toAPIStreamConfig(args.config)
    };
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => createStream({
        client: this.client,
        body: toSnakeCase(apiArgs),
        headers: { "s2-request-token": requestToken },
        ...options
      }));
    });
    return transformStreamInfo(toCamelCase(response));
  }
  /**
   * Get stream configuration.
   *
   * @param args.stream Stream name
   */
  async getConfig(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => getStreamConfig({
        client: this.client,
        path: args,
        ...options
      }));
    });
    return toSDKStreamConfig(toCamelCase(response));
  }
  /**
   * Delete a stream.
   *
   * @param args.stream Stream name
   */
  async delete(args, options) {
    await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => deleteStream({
        client: this.client,
        path: args,
        ...options
      }));
    });
  }
  /**
   * Ensure a stream.
   *
   * Creates the stream if it doesn't exist, or ensures its config exactly matches the provided
   * configuration after basin defaults and global defaults are applied. Uses HTTP PUT semantics
   * and is always idempotent.
   *
   * Returns `result: "created"` with the stream info if the stream was newly created,
   * `result: "updated"` if its config changed, or `result: "noop"` if no write was needed.
   */
  async ensure(args, options) {
    const body = args.config === void 0 ? void 0 : toSnakeCase(toAPIStreamConfig(args.config));
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2DataAndResponse(() => ensureStream({
        client: this.client,
        path: { stream: args.stream },
        body,
        ...options
      }));
    });
    const stream = toCamelCase(response.data);
    return {
      result: provisionResultFromResponse(response.response),
      stream: transformStreamInfo(stream)
    };
  }
  /**
   * Reconfigure a stream.
   *
   * @param args Configuration for the stream to reconfigure (including stream name and fields to change)
   */
  async reconfigure(args, options) {
    const { stream, ...reconfigArgs } = args;
    const apiArgs = {
      ...reconfigArgs,
      deleteOnEmpty: toAPIDeleteOnEmpty(reconfigArgs.deleteOnEmpty),
      retentionPolicy: toAPIRetentionPolicy(args.retentionPolicy)
    };
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => reconfigureStream({
        client: this.client,
        path: { stream },
        body: toSnakeCase(apiArgs),
        ...options
      }));
    });
    const camelCased = toCamelCase(response);
    return toSDKStreamConfig(camelCased);
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/basin.js
var S2Basin = class {
  static {
    __name(this, "S2Basin");
  }
  client;
  transportConfig;
  retryConfig;
  name;
  streams;
  /**
   * Create a basin-scoped client that talks to `https://{basin}.b.s2.dev/v1`.
   *
   * Use this to work with streams inside a single basin.
   * @param name Basin name
   * @param options Configuration for the basin-scoped client
   */
  constructor(name, options) {
    this.name = name;
    this.retryConfig = options.retryConfig;
    this.transportConfig = {
      baseUrl: options.baseUrl,
      accessToken: options.accessToken,
      basinName: options.includeBasinHeader ? name : void 0,
      connectionTimeoutMillis: options.retryConfig?.connectionTimeoutMillis,
      requestTimeoutMillis: options.retryConfig?.requestTimeoutMillis,
      retry: options.retryConfig,
      compression: options.compression,
      http2: options.http2
    };
    const headers = {};
    if (options.includeBasinHeader) {
      headers["s2-basin"] = name;
    }
    if (canSetUserAgentHeader()) {
      headers["user-agent"] = DEFAULT_USER_AGENT;
    }
    this.client = createClient(createConfig({
      baseUrl: options.baseUrl,
      auth: /* @__PURE__ */ __name(() => value(this.transportConfig.accessToken), "auth"),
      headers
    }));
    this.streams = new S2Streams(this.client, this.retryConfig);
  }
  /**
   * Create a stream-scoped helper bound to `this` basin.
   * @param name Stream name
   */
  stream(name, options) {
    return new S2Stream(name, this.client, {
      ...this.transportConfig,
      encryptionKey: resolveEncryptionKey(options?.encryptionKey),
      forceTransport: options?.forceTransport
    }, this.retryConfig);
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/endpoints.js
init_esm();
var DEFAULT_SCHEME = "https";
var DEFAULT_API_PATH = "/v1";
var BASIN_PLACEHOLDER_SENTINEL = "__basin__";
function hasScheme(input) {
  return /^[a-zA-Z][a-zA-Z0-9+.-]*:\/\//.test(input);
}
__name(hasScheme, "hasScheme");
function firstDelimiterIndex(value2, fromIndex) {
  const slash = value2.indexOf("/", fromIndex);
  const query = value2.indexOf("?", fromIndex);
  const hash = value2.indexOf("#", fromIndex);
  const candidates = [slash, query, hash].filter((idx) => idx !== -1);
  return candidates.length ? Math.min(...candidates) : -1;
}
__name(firstDelimiterIndex, "firstDelimiterIndex");
function hasExplicitPath(input) {
  const trimmed = input.trim();
  const schemeMatch = trimmed.match(/^([a-zA-Z][a-zA-Z0-9+.-]*):\/\//);
  const authorityStart = schemeMatch ? schemeMatch[0].length : 0;
  const delim = firstDelimiterIndex(trimmed, authorityStart);
  return delim !== -1 && trimmed[delim] === "/";
}
__name(hasExplicitPath, "hasExplicitPath");
function normalizeForUrlParsing(input) {
  const trimmed = input.trim();
  const withScheme = hasScheme(trimmed) ? trimmed : `${DEFAULT_SCHEME}://${trimmed}`;
  return withScheme.replaceAll("{basin}", BASIN_PLACEHOLDER_SENTINEL);
}
__name(normalizeForUrlParsing, "normalizeForUrlParsing");
function asScheme(protocol) {
  const normalized = protocol.replace(":", "").toLowerCase();
  if (normalized === "http" || normalized === "https")
    return normalized;
  throw new Error(`Unsupported scheme: ${protocol}`);
}
__name(asScheme, "asScheme");
var EndpointTemplate = class {
  static {
    __name(this, "EndpointTemplate");
  }
  raw;
  scheme;
  hostTemplate;
  port;
  pathTemplate;
  hasBasinPlaceholder;
  explicitPathProvided;
  constructor({ endpoint }) {
    const raw = endpoint.trim();
    if (!raw) {
      throw new Error("Endpoint cannot be empty");
    }
    const explicitPathProvided = hasExplicitPath(raw);
    const parsed = new URL(normalizeForUrlParsing(raw));
    this.raw = raw;
    this.scheme = asScheme(parsed.protocol);
    this.hostTemplate = parsed.hostname.replaceAll(BASIN_PLACEHOLDER_SENTINEL, "{basin}");
    this.port = parsed.port;
    if (parsed.search || parsed.hash) {
      throw new Error(`Endpoint cannot include query string or hash fragment: "${raw}". Use headers for authentication or path prefixes for routing.`);
    }
    const parsedPath = parsed.pathname.replaceAll(BASIN_PLACEHOLDER_SENTINEL, "{basin}");
    this.pathTemplate = explicitPathProvided ? parsedPath : DEFAULT_API_PATH;
    this.explicitPathProvided = explicitPathProvided;
    this.hasBasinPlaceholder = this.raw.includes("{basin}") || this.hostTemplate.includes("{basin}") || this.pathTemplate.includes("{basin}");
  }
  /**
   * Resolve the template into a base URL string.
   *
   * - If `{basin}` appears in the hostname, it is substituted verbatim (basin names are validated by S2.basin()).
   * - If `{basin}` appears in the path/query/hash, it is substituted via `encodeURIComponent`.
   */
  baseUrl(basin) {
    const host = basin ? this.hostTemplate.replaceAll("{basin}", basin) : this.hostTemplate;
    const path2 = basin ? this.pathTemplate.replaceAll("{basin}", encodeURIComponent(basin)) : this.pathTemplate;
    const authority = this.port ? `${host}:${this.port}` : host;
    return `${this.scheme}://${authority}${path2}`;
  }
};
var DEFAULT_ACCOUNT_ENDPOINT = "a.s2.dev";
var DEFAULT_BASIN_ENDPOINT = "{basin}.b.s2.dev";
var S2Endpoints = class {
  static {
    __name(this, "S2Endpoints");
  }
  account;
  basin;
  /**
   * When true, include `s2-basin: <name>` header on basin-scoped requests.
   *
   * Per project convention: enabled whenever a non-default basin endpoint is provided.
   */
  includeBasinHeader;
  constructor(init) {
    this.account = new EndpointTemplate({
      endpoint: init?.account ?? DEFAULT_ACCOUNT_ENDPOINT
    });
    this.basin = new EndpointTemplate({
      endpoint: init?.basin ?? DEFAULT_BASIN_ENDPOINT
    });
    this.includeBasinHeader = init?.basin !== void 0;
  }
  accountBaseUrl() {
    return this.account.baseUrl();
  }
  basinBaseUrl(basin) {
    return this.basin.baseUrl(basin);
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/s2.js
init_esm();

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/accessTokens.js
init_esm();
function toISOString(value2) {
  if (value2 === null)
    return null;
  if (value2 === void 0)
    return void 0;
  if (value2 instanceof Date)
    return value2.toISOString();
  if (typeof value2 === "number")
    return new Date(value2).toISOString();
  return value2;
}
__name(toISOString, "toISOString");
function toDate2(value2) {
  if (value2 === null)
    return null;
  if (value2 === void 0)
    return void 0;
  return new Date(value2);
}
__name(toDate2, "toDate");
function transformTokenInfo(token) {
  return {
    ...token,
    expiresAt: toDate2(token.expiresAt)
  };
}
__name(transformTokenInfo, "transformTokenInfo");
var S2AccessTokens = class {
  static {
    __name(this, "S2AccessTokens");
  }
  client;
  retryConfig;
  constructor(client2, retryConfig) {
    this.client = client2;
    this.retryConfig = retryConfig;
  }
  /**
   * List access tokens.
   *
   * @param args.prefix Filter to IDs beginning with this prefix
   * @param args.startAfter Filter to IDs lexicographically after this value
   * @param args.limit Max results (up to 1000)
   */
  async list(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => listAccessTokens({
        client: this.client,
        query: toSnakeCase(args),
        ...options
      }));
    });
    const camelCased = toCamelCase(response);
    return {
      ...camelCased,
      accessTokens: camelCased.accessTokens.map(transformTokenInfo)
    };
  }
  /**
   * List all access tokens with automatic pagination.
   * Returns a lazy async iterable that fetches pages as needed.
   *
   * @param args - Optional filtering options: `prefix` to filter by ID prefix, `limit` for max results per page
   *
   * @example
   * ```ts
   * for await (const token of s2.accessTokens.listAll({ prefix: "service-" })) {
   *   console.log(token.id);
   * }
   * ```
   */
  listAll(args, options) {
    return paginate((a) => this.list(a, options).then((r) => ({
      items: r.accessTokens,
      hasMore: r.hasMore
    })), args ?? {}, (token) => token.id);
  }
  /**
   * Issue a new access token.
   *
   * @param args.id Unique token ID (1-96 bytes)
   * @param args.scope Token scope (operations and resource sets)
   * @param args.autoPrefixStreams Namespace stream names by configured prefix scope
   * @param args.expiresAt Expiration time (Date or RFC 3339 string); defaults to requestor's token expiry
   */
  async issue(args, options) {
    const apiArgs = {
      ...args,
      expiresAt: toISOString(args.expiresAt)
    };
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => issueAccessToken({
        client: this.client,
        body: toSnakeCase(apiArgs),
        ...options
      }));
    });
    return toCamelCase(response);
  }
  /**
   * Revoke an access token by ID.
   *
   * @param args.id Token ID to revoke
   */
  async revoke(args, options) {
    await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => revokeAccessToken({
        client: this.client,
        path: args,
        ...options
      }));
    });
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/basins.js
init_esm();
function toDate3(value2) {
  if (value2 === null)
    return null;
  if (value2 === void 0)
    return void 0;
  return new Date(value2);
}
__name(toDate3, "toDate");
function transformBasinInfo(basin) {
  return {
    name: basin.name,
    location: basin.location,
    createdAt: toDate3(basin.createdAt),
    deletedAt: toDate3(basin.deletedAt)
  };
}
__name(transformBasinInfo, "transformBasinInfo");
function toAPIRetentionPolicy2(policy) {
  if (policy === null)
    return null;
  if (policy === void 0)
    return void 0;
  if ("ageSecs" in policy) {
    return { age: Math.floor(policy.ageSecs) };
  }
  return policy;
}
__name(toAPIRetentionPolicy2, "toAPIRetentionPolicy");
function toSDKRetentionPolicy2(policy) {
  if (policy === null)
    return null;
  if (policy === void 0)
    return void 0;
  if ("age" in policy) {
    return { ageSecs: policy.age };
  }
  return policy;
}
__name(toSDKRetentionPolicy2, "toSDKRetentionPolicy");
function toAPIStreamConfig2(config) {
  if (config === null || config === void 0)
    return config;
  return {
    ...config,
    deleteOnEmpty: config.deleteOnEmpty ? {
      ...config.deleteOnEmpty,
      minAgeSecs: config.deleteOnEmpty.minAgeSecs === void 0 ? void 0 : Math.max(0, Math.floor(config.deleteOnEmpty.minAgeSecs))
    } : config.deleteOnEmpty,
    retentionPolicy: toAPIRetentionPolicy2(config.retentionPolicy)
  };
}
__name(toAPIStreamConfig2, "toAPIStreamConfig");
function toSDKStreamConfig2(config) {
  if (config === null || config === void 0)
    return config;
  return {
    ...config,
    retentionPolicy: toSDKRetentionPolicy2(config?.retentionPolicy)
  };
}
__name(toSDKStreamConfig2, "toSDKStreamConfig");
function toAPIBasinConfig(config) {
  if (config === null || config === void 0)
    return config;
  return {
    ...config,
    defaultStreamConfig: toAPIStreamConfig2(config.defaultStreamConfig)
  };
}
__name(toAPIBasinConfig, "toAPIBasinConfig");
function toSDKBasinConfig(config) {
  return {
    ...config,
    defaultStreamConfig: toSDKStreamConfig2(config?.defaultStreamConfig)
  };
}
__name(toSDKBasinConfig, "toSDKBasinConfig");
var S2Basins = class {
  static {
    __name(this, "S2Basins");
  }
  client;
  retryConfig;
  constructor(client2, retryConfig) {
    this.client = client2;
    this.retryConfig = retryConfig;
  }
  /**
   * List basins.
   *
   * @param args.prefix Return basins whose names start with the given prefix
   * @param args.startAfter Name to start after (for pagination)
   * @param args.limit Max results (up to 1000)
   */
  async list(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => listBasins({
        client: this.client,
        query: toSnakeCase(args),
        ...options
      }));
    });
    const camelCased = toCamelCase(response);
    return {
      ...camelCased,
      basins: camelCased.basins.map(transformBasinInfo)
    };
  }
  /**
   * List all basins with automatic pagination.
   * Returns a lazy async iterable that fetches pages as needed.
   *
   * @param args - Optional options: `prefix` to filter by name prefix, `limit` for max results per page, `includeDeleted` to include basins pending deletion
   *
   * @example
   * ```ts
   * for await (const basin of s2.basins.listAll({ prefix: "my-" })) {
   *   console.log(basin.name);
   * }
   * ```
   */
  listAll(args, options) {
    const { includeDeleted, ...listArgs } = args ?? {};
    const allItems = paginate((a) => this.list(a, options).then((r) => ({
      items: r.basins,
      hasMore: r.hasMore
    })), listArgs, (basin) => basin.name);
    if (includeDeleted) {
      return allItems;
    }
    return filterAsync(allItems, (b) => !b.deletedAt);
  }
  /**
   * Create a basin.
   *
   * @param args.basin Globally unique basin name (8-48 chars, lowercase letters, numbers, hyphens; cannot begin or end with a hyphen)
   * @param args.config Optional basin configuration (e.g. defaultStreamConfig)
   * @param args.location Basin location
   */
  async create(args, options) {
    const requestToken = randomToken();
    const apiArgs = {
      ...args,
      config: toAPIBasinConfig(args.config)
    };
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => createBasin({
        client: this.client,
        body: toSnakeCase(apiArgs),
        headers: { "s2-request-token": requestToken },
        ...options
      }));
    });
    return transformBasinInfo(toCamelCase(response));
  }
  /**
   * Get basin configuration.
   *
   * @param args.basin Basin name
   */
  async getConfig(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => getBasinConfig({
        client: this.client,
        path: args,
        ...options
      }));
    });
    return toSDKBasinConfig(toCamelCase(response));
  }
  /**
   * Delete a basin.
   *
   * @param args.basin Basin name
   */
  async delete(args, options) {
    await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => deleteBasin({
        client: this.client,
        path: args,
        ...options
      }));
    });
  }
  /**
   * Ensure a basin.
   *
   * Creates the basin if it doesn't exist, or ensures its config exactly matches the
   * provided configuration after defaults are applied. Uses HTTP PUT semantics and is always
   * idempotent.
   *
   * Returns `result: "created"` with the basin info if the basin was newly created,
   * `result: "updated"` if its config changed, or `result: "noop"` if no write was needed.
   */
  async ensure(args, options) {
    const { basin, ...ensureArgs } = args;
    const apiArgs = {
      ...ensureArgs,
      config: toAPIBasinConfig(args.config)
    };
    const hasBody = apiArgs.config !== void 0 || apiArgs.location !== void 0;
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2DataAndResponse(() => ensureBasin({
        client: this.client,
        path: { basin },
        body: hasBody ? toSnakeCase(apiArgs) : void 0,
        ...options
      }));
    });
    return {
      result: provisionResultFromResponse(response.response),
      basin: transformBasinInfo(toCamelCase(response.data))
    };
  }
  /**
   * Reconfigure a basin.
   *
   * @param args Configuration for the basin to reconfigure (including basin name and fields to change)
   */
  async reconfigure(args, options) {
    const { basin, ...reconfigArgs } = args;
    const apiArgs = {
      ...reconfigArgs,
      defaultStreamConfig: toAPIStreamConfig2(args.defaultStreamConfig)
    };
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => reconfigureBasin({
        client: this.client,
        path: { basin },
        body: toSnakeCase(apiArgs),
        ...options
      }));
    });
    return toSDKBasinConfig(toCamelCase(response));
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/locations.js
init_esm();
function transformLocationInfo(location) {
  return toCamelCase(location);
}
__name(transformLocationInfo, "transformLocationInfo");
var S2Locations = class {
  static {
    __name(this, "S2Locations");
  }
  client;
  retryConfig;
  constructor(client2, retryConfig) {
    this.client = client2;
    this.retryConfig = retryConfig;
  }
  /**
   * List available locations.
   */
  async list(options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => listLocations({
        client: this.client,
        ...options
      }));
    });
    return response.map(transformLocationInfo);
  }
  /**
   * Get the default location used when creating basins without an explicit location.
   */
  async getDefault(options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => getDefaultLocation({
        client: this.client,
        ...options
      }));
    });
    return transformLocationInfo(response);
  }
  /**
   * Set the default location used when creating basins without an explicit location.
   *
   * @param args.location Location name
   */
  async setDefault(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => setDefaultLocation({
        client: this.client,
        body: args.location,
        ...options
      }));
    });
    return transformLocationInfo(response);
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/metrics.js
init_esm();
function toEpochSeconds(value2) {
  if (value2 === void 0)
    return void 0;
  const ms = typeof value2 === "number" ? value2 : value2.getTime();
  return Math.floor(ms / 1e3);
}
__name(toEpochSeconds, "toEpochSeconds");
function fromAPIMetricSetResponse(response) {
  const camelCased = toCamelCase(response);
  return {
    values: camelCased.values.map((metric) => {
      if ("accumulation" in metric) {
        return {
          accumulation: {
            ...metric.accumulation,
            values: metric.accumulation.values.map(([ts, value2]) => [new Date(ts * 1e3), value2])
          }
        };
      }
      if ("gauge" in metric) {
        return {
          gauge: {
            ...metric.gauge,
            values: metric.gauge.values.map(([ts, value2]) => [new Date(ts * 1e3), value2])
          }
        };
      }
      return metric;
    })
  };
}
__name(fromAPIMetricSetResponse, "fromAPIMetricSetResponse");
var S2Metrics = class {
  static {
    __name(this, "S2Metrics");
  }
  client;
  retryConfig;
  constructor(client2, retryConfig) {
    this.client = client2;
    this.retryConfig = retryConfig;
  }
  /**
   * Account-level metrics.
   *
   * @param args.set Metric set to return
   * @param args.start Optional start timestamp (milliseconds since Unix epoch)
   * @param args.end Optional end timestamp (milliseconds since Unix epoch)
   * @param args.interval Optional aggregation interval for timeseries sets
   */
  async account(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => accountMetrics({
        client: this.client,
        query: toSnakeCase({
          ...args,
          start: toEpochSeconds(args.start),
          end: toEpochSeconds(args.end)
        }),
        ...options
      }));
    });
    return fromAPIMetricSetResponse(response);
  }
  /**
   * Basin-level metrics.
   *
   * @param args.basin Basin name
   * @param args.set Metric set to return
   * @param args.start Optional start timestamp (milliseconds since Unix epoch)
   * @param args.end Optional end timestamp (milliseconds since Unix epoch)
   * @param args.interval Optional aggregation interval for timeseries sets
   */
  async basin(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => basinMetrics({
        client: this.client,
        path: { basin: args.basin },
        query: toSnakeCase({
          set: args.set,
          start: toEpochSeconds(args.start),
          end: toEpochSeconds(args.end),
          interval: args.interval
        }),
        ...options
      }));
    });
    return fromAPIMetricSetResponse(response);
  }
  /**
   * Stream-level metrics.
   *
   * @param args.basin Basin name
   * @param args.stream Stream name
   * @param args.set Metric set to return
   * @param args.start Optional start timestamp (milliseconds since Unix epoch)
   * @param args.end Optional end timestamp (milliseconds since Unix epoch)
   * @param args.interval Optional aggregation interval for timeseries sets
   */
  async stream(args, options) {
    const response = await withRetries(this.retryConfig, async () => {
      return await withS2Data(() => streamMetrics({
        client: this.client,
        path: { basin: args.basin, stream: args.stream },
        query: toSnakeCase({
          set: args.set,
          start: toEpochSeconds(args.start),
          end: toEpochSeconds(args.end),
          interval: args.interval
        }),
        ...options
      }));
    });
    return fromAPIMetricSetResponse(response);
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/s2.js
var BASIN_NAME_REGEX = /^[a-z0-9][a-z0-9-]{6,46}[a-z0-9]$/;
var S2 = class {
  static {
    __name(this, "S2");
  }
  accessToken;
  client;
  endpoints;
  retryConfig;
  compression;
  http2;
  /**
   * Account-scoped basin management operations.
   *
   * - List, create, ensure, delete and reconfigure basins.
   */
  basins;
  /** Manage access tokens for the account (list, issue, revoke). */
  accessTokens;
  /** Account location operations. */
  locations;
  /** Account, basin and stream level metrics. */
  metrics;
  /**
   * Create a new S2 client.
   *
   * @param options Access token configuration.
   */
  constructor(options) {
    this.accessToken = make(options.accessToken);
    this.retryConfig = {
      ...options.retry,
      ...options.requestTimeoutMillis !== void 0 && {
        requestTimeoutMillis: options.requestTimeoutMillis
      },
      ...options.connectionTimeoutMillis !== void 0 && {
        connectionTimeoutMillis: options.connectionTimeoutMillis
      }
    };
    this.endpoints = options.endpoints instanceof S2Endpoints ? options.endpoints : new S2Endpoints(options.endpoints);
    this.compression = options.compression;
    this.http2 = options.http2 ? { ...options.http2 } : void 0;
    const headers = {};
    if (canSetUserAgentHeader()) {
      headers["user-agent"] = DEFAULT_USER_AGENT;
    }
    this.client = createClient(createConfig({
      baseUrl: this.endpoints.accountBaseUrl(),
      auth: /* @__PURE__ */ __name(() => value(this.accessToken), "auth"),
      headers
    }));
    this.client.interceptors.error.use((err2, res) => {
      return makeServerError(res, err2);
    });
    this.basins = new S2Basins(this.client, this.retryConfig);
    this.accessTokens = new S2AccessTokens(this.client, this.retryConfig);
    this.locations = new S2Locations(this.client, this.retryConfig);
    this.metrics = new S2Metrics(this.client, this.retryConfig);
  }
  /**
   * Create a basin-scoped client bound to a specific basin name.
   *
   * @param name Basin name (8-48 characters, lowercase alphanumeric and hyphens, no leading/trailing hyphens).
   * @throws {S2Error} If the basin name is invalid.
   */
  basin(name) {
    if (!BASIN_NAME_REGEX.test(name)) {
      throw new S2Error({
        message: `Invalid basin name: "${name}". Basin names must be 8-48 characters, contain only lowercase letters, numbers, and hyphens, and cannot start or end with a hyphen.`,
        origin: "sdk"
      });
    }
    return new S2Basin(name, {
      accessToken: this.accessToken,
      baseUrl: this.endpoints.basinBaseUrl(name),
      includeBasinHeader: this.endpoints.includeBasinHeader,
      retryConfig: this.retryConfig,
      compression: this.compression,
      http2: this.http2
    });
  }
};

// ../../node_modules/.bun/@s2-dev+streamstore@0.25.0+2b91fc17bf64bdfd/node_modules/@s2-dev/streamstore/dist/esm/batch-transform.js
init_esm();
var BatchTransform = class extends TransformStream {
  static {
    __name(this, "BatchTransform");
  }
  currentBatch = [];
  currentBatchSize = 0;
  lingerTimer = null;
  controller = null;
  maxBatchRecords;
  maxBatchBytes;
  lingerDuration;
  fencingToken;
  nextMatchSeqNum;
  constructor(args) {
    let controller;
    super({
      start: /* @__PURE__ */ __name((c) => {
        controller = c;
      }, "start"),
      transform: /* @__PURE__ */ __name((chunk) => {
        this.handleRecord(chunk);
      }, "transform"),
      flush: /* @__PURE__ */ __name(() => {
        this.flush();
      }, "flush"),
      cancel: /* @__PURE__ */ __name(() => {
        this.cancelLingerTimer();
      }, "cancel")
    });
    this.controller = controller;
    if (args?.maxBatchRecords !== void 0) {
      if (!Number.isFinite(args.maxBatchRecords) || args.maxBatchRecords < 1 || args.maxBatchRecords > 1e3) {
        throw new S2Error({
          message: `maxBatchRecords must be a finite number between 1 and 1000 (inclusive); got ${args.maxBatchRecords}`,
          status: 400,
          origin: "sdk"
        });
      }
    }
    if (args?.maxBatchBytes !== void 0) {
      const max = 1024 * 1024;
      if (!Number.isFinite(args.maxBatchBytes) || args.maxBatchBytes < 1 || args.maxBatchBytes > max) {
        throw new S2Error({
          message: `maxBatchBytes must be a finite number between 1 and ${max} (1 MiB) bytes (inclusive); got ${args.maxBatchBytes}`,
          status: 400,
          origin: "sdk"
        });
      }
    }
    if (args?.lingerDurationMillis !== void 0) {
      if (!Number.isFinite(args.lingerDurationMillis) || args.lingerDurationMillis < 0) {
        throw new S2Error({
          message: `lingerDurationMillis must be a finite number >= 0; got ${args.lingerDurationMillis}`,
          status: 400,
          origin: "sdk"
        });
      }
    }
    this.maxBatchRecords = args?.maxBatchRecords ?? 1e3;
    this.maxBatchBytes = args?.maxBatchBytes ?? 1024 * 1024;
    this.lingerDuration = args?.lingerDurationMillis ?? 5;
    this.fencingToken = args?.fencingToken;
    this.nextMatchSeqNum = args?.matchSeqNum;
  }
  handleRecord(record) {
    const recordSize = meteredBytes(record);
    if (recordSize > this.maxBatchBytes) {
      this.cancelLingerTimer();
      throw new S2Error({
        message: `Record size ${recordSize} bytes exceeds maximum batch size of ${this.maxBatchBytes} bytes`,
        status: 400,
        origin: "sdk"
      });
    }
    if (this.currentBatch.length === 0 && this.lingerDuration >= 0) {
      this.startLingerTimer();
    }
    const wouldExceedRecords = this.currentBatch.length + 1 > this.maxBatchRecords;
    const wouldExceedBytes = this.currentBatchSize + recordSize > this.maxBatchBytes;
    if (wouldExceedRecords || wouldExceedBytes) {
      this.flush();
      if (this.lingerDuration >= 0) {
        this.startLingerTimer();
      }
    }
    this.currentBatch.push(record);
    this.currentBatchSize += recordSize;
    const nowExceedsRecords = this.currentBatch.length >= this.maxBatchRecords;
    const nowExceedsBytes = this.currentBatchSize >= this.maxBatchBytes;
    if (nowExceedsRecords || nowExceedsBytes) {
      this.flush();
    }
  }
  flush() {
    this.cancelLingerTimer();
    if (this.currentBatch.length === 0) {
      return;
    }
    const matchSeqNum = this.nextMatchSeqNum;
    if (this.nextMatchSeqNum !== void 0) {
      this.nextMatchSeqNum += this.currentBatch.length;
    }
    if (this.controller) {
      const batch = AppendInput.create([...this.currentBatch], {
        fencingToken: this.fencingToken,
        matchSeqNum
      });
      this.controller.enqueue(batch);
    }
    this.currentBatch = [];
    this.currentBatchSize = 0;
  }
  startLingerTimer() {
    this.cancelLingerTimer();
    this.lingerTimer = setTimeout(() => {
      this.lingerTimer = null;
      if (this.currentBatch.length > 0) {
        try {
          this.flush();
        } catch (err2) {
          if (err2 instanceof TypeError) {
            return;
          }
          try {
            this.controller?.error(err2);
          } catch {
          }
        }
      }
    }, this.lingerDuration);
  }
  cancelLingerTimer() {
    if (this.lingerTimer) {
      clearTimeout(this.lingerTimer);
      this.lingerTimer = null;
    }
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/realtimeStreams/streamsWriterV2.js
var RECORD_BODY_MAX_BYTES = 1024 * 1024 - 1024;
var utf8Encoder = new TextEncoder();
var StreamsWriterV2 = class {
  static {
    __name(this, "StreamsWriterV2");
  }
  options;
  s2Client;
  serverStream;
  consumerStream;
  streamPromise;
  flushIntervalMs;
  debug;
  maxInflightBytes;
  aborted = false;
  sessionWritable = null;
  lastSeqNum;
  constructor(options) {
    this.options = options;
    this.debug = options.debug ?? false;
    this.s2Client = new S2({
      accessToken: options.accessToken,
      ...options.endpoint ? {
        endpoints: {
          account: options.endpoint,
          basin: options.endpoint
        }
      } : {}
    });
    this.flushIntervalMs = options.flushIntervalMs ?? 200;
    this.maxInflightBytes = options.maxInflightBytes ?? 1024 * 1024 * 10;
    this.log(`[S2MetadataStream] Initializing: basin=${options.basin}, stream=${options.stream}, flushIntervalMs=${this.flushIntervalMs}, maxInflightBytes=${this.maxInflightBytes}`);
    if (options.signal?.aborted) {
      this.aborted = true;
      this.log("[S2MetadataStream] Signal already aborted, skipping initialization");
      this.serverStream = new ReadableStream();
      this.consumerStream = new ReadableStream();
      this.streamPromise = Promise.resolve();
      return;
    }
    if (options.signal) {
      options.signal.addEventListener("abort", () => {
        this.log("[S2MetadataStream] Abort signal received");
        this.handleAbort();
      });
    }
    const [serverStream, consumerStream] = this.options.source.tee();
    this.serverStream = serverStream;
    this.consumerStream = consumerStream;
    this.streamPromise = this.initializeServerStream();
  }
  handleAbort() {
    if (this.aborted) {
      return;
    }
    this.aborted = true;
    this.log("[S2MetadataStream] Handling abort - cleaning up resources");
    if (this.sessionWritable) {
      this.sessionWritable.abort("Aborted").catch((error) => {
        this.logError("[S2MetadataStream] Error aborting writable stream:", error);
      }).finally(() => {
        this.log("[S2MetadataStream] Writable stream aborted");
      });
    }
    this.log("[S2MetadataStream] Abort cleanup complete");
  }
  async initializeServerStream() {
    try {
      if (this.aborted) {
        this.log("[S2MetadataStream] Stream initialization aborted");
        return;
      }
      this.log("[S2MetadataStream] Getting S2 basin and stream");
      const basin = this.s2Client.basin(this.options.basin);
      const stream = basin.stream(this.options.stream);
      const session = await stream.appendSession({
        maxInflightBytes: this.maxInflightBytes
      });
      this.sessionWritable = session.writable;
      this.log(`[S2MetadataStream] Starting stream pipeline`);
      await this.serverStream.pipeThrough(new TransformStream({
        transform: /* @__PURE__ */ __name((chunk, controller) => {
          if (this.aborted) {
            controller.error(new Error("Stream aborted"));
            return;
          }
          const encoded = encodeChunkOrError(chunk);
          if (!encoded.ok) {
            controller.error(encoded.error);
            return;
          }
          controller.enqueue(AppendRecord.string({ body: encoded.body }));
        }, "transform")
      })).pipeThrough(new BatchTransform({
        lingerDurationMillis: this.flushIntervalMs
      })).pipeTo(session.writable);
      this.log("[S2MetadataStream] Stream pipeline completed successfully");
      const lastAcked = session.lastAckedPosition();
      if (lastAcked?.end) {
        this.lastSeqNum = lastAcked.end.seqNum - 1;
        this.log(`[S2MetadataStream] Wrote through seqNum=${this.lastSeqNum}`);
      }
    } catch (error) {
      if (this.aborted) {
        this.log("[S2MetadataStream] Stream error occurred but stream was aborted");
        return;
      }
      this.logError("[S2MetadataStream] Error in stream pipeline:", error);
      throw error;
    }
  }
  async wait() {
    await this.streamPromise;
    return { lastEventId: this.lastSeqNum?.toString() };
  }
  [Symbol.asyncIterator]() {
    return streamToAsyncIterator(this.consumerStream);
  }
  // Helper methods
  log(message) {
    if (this.debug) {
      console.log(message);
    }
  }
  logError(message, error) {
    if (this.debug) {
      console.error(message, error);
    }
  }
};
async function* streamToAsyncIterator(stream) {
  const reader = stream.getReader();
  try {
    while (true) {
      const { done, value: value2 } = await reader.read();
      if (done)
        return;
      yield value2;
    }
  } finally {
    safeReleaseLock(reader);
  }
}
__name(streamToAsyncIterator, "streamToAsyncIterator");
function safeReleaseLock(reader) {
  try {
    reader.releaseLock();
  } catch (_error) {
  }
}
__name(safeReleaseLock, "safeReleaseLock");
function extractChunkType(chunk) {
  if (!chunk || typeof chunk !== "object")
    return void 0;
  const c = chunk;
  if (typeof c.type === "string")
    return c.type;
  if (typeof c.kind === "string")
    return c.kind;
  return void 0;
}
__name(extractChunkType, "extractChunkType");
function encodeChunkOrError(chunk) {
  const body = JSON.stringify({ data: chunk, id: nanoid(7) });
  const size = utf8Encoder.encode(body).length;
  if (size > RECORD_BODY_MAX_BYTES) {
    return {
      ok: false,
      error: new ChatChunkTooLargeError(size, RECORD_BODY_MAX_BYTES, extractChunkType(chunk))
    };
  }
  return { ok: true, body };
}
__name(encodeChunkOrError, "encodeChunkOrError");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/workers/taskExecutor.js
var TaskExecutor = class {
  static {
    __name(this, "TaskExecutor");
  }
  task;
  _tracingSDK;
  _tracer;
  _consoleInterceptor;
  _retries;
  _isWarmStart;
  _executionCount;
  constructor(task, options) {
    this.task = task;
    this._tracingSDK = options.tracingSDK;
    this._tracer = options.tracer;
    this._consoleInterceptor = options.consoleInterceptor;
    this._retries = options.retries;
    this._isWarmStart = options.isWarmStart;
    this._executionCount = options.executionCount;
  }
  async execute(execution, ctx, signal) {
    const attemptMessage = `Attempt ${execution.attempt.number}`;
    const originalPacket = {
      data: execution.run.payload,
      dataType: execution.run.payloadType
    };
    if (execution.run.metadata) {
      runMetadata.enterWithMetadata(execution.run.metadata);
    }
    const result = await this._tracer.startActiveSpan(attemptMessage, async (span) => {
      const attemptContext = context.active();
      return await this._consoleInterceptor.intercept(console, async () => {
        let parsedPayload;
        let initOutput;
        const [inputError, payloadResult] = await tryCatch(runTimelineMetrics.measureMetric("trigger.dev/execution", "payload", async () => {
          const payloadPacket = await conditionallyImportPacket(originalPacket, this._tracer);
          return await parsePacket(payloadPacket);
        }));
        if (inputError) {
          recordSpanException(span, inputError);
          return this.#internalErrorResult(execution, TaskRunErrorCodes.TASK_INPUT_ERROR, inputError);
        }
        const [parsePayloadError, parsedPayloadResult] = await tryCatch(this.#parsePayload(payloadResult));
        if (parsePayloadError) {
          recordSpanException(span, parsePayloadError);
          return this.#internalErrorResult(execution, TaskRunErrorCodes.TASK_INPUT_ERROR, parsePayloadError, true);
        }
        parsedPayload = parsedPayloadResult;
        lifecycleHooks.registerOnWaitHookListener(async (wait) => {
          await this.#callOnWaitFunctions(wait, parsedPayload, ctx, initOutput, signal);
        });
        lifecycleHooks.registerOnResumeHookListener(async (wait) => {
          await this.#callOnResumeFunctions(wait, parsedPayload, ctx, initOutput, signal);
        });
        const { promise: runPromise, resolve: runResolve, reject: runReject } = promiseWithResolvers();
        runPromise.catch(() => {
        });
        lifecycleHooks.registerOnCancelHookListener(async () => {
          await this.#callOnCancelFunctions(runPromise, parsedPayload, ctx, initOutput, signal, attemptContext);
        });
        const executeTask = /* @__PURE__ */ __name(async (payload) => {
          const [runError, output] = await tryCatch((async () => {
            initOutput = await this.#callInitFunctions(payload, ctx, signal);
            if (execution.attempt.number === 1) {
              await this.#callOnStartFunctions(payload, ctx, initOutput, signal);
            }
            await this.#callOnStartAttemptFunctions(payload, ctx, signal);
            try {
              return await this.#callRun(payload, ctx, initOutput, signal);
            } catch (error) {
              if (isCompleteTaskWithOutput(error)) {
                return error.output;
              }
              throw error;
            }
          })());
          if (runError) {
            runReject(runError);
            const [handleErrorError, handleErrorResult] = await tryCatch(this.#handleError(execution, runError, payload, ctx, initOutput, signal));
            if (handleErrorError) {
              recordSpanException(span, handleErrorError);
              return this.#internalErrorResult(execution, TaskRunErrorCodes.HANDLE_ERROR_ERROR, handleErrorError);
            }
            recordSpanException(span, handleErrorResult.error ?? runError);
            if (handleErrorResult.status !== "retry") {
              await this.#callOnFailureFunctions(payload, handleErrorResult.error ?? runError, ctx, initOutput, signal);
              await this.#callOnCompleteFunctions(payload, { ok: false, error: handleErrorResult.error ?? runError }, ctx, initOutput, signal);
            }
            await this.#cleanupAndWaitUntil(payload, ctx, initOutput, signal);
            return {
              id: execution.run.id,
              ok: false,
              error: sanitizeError(handleErrorResult.error ? parseError(handleErrorResult.error) : parseError(runError)),
              retry: handleErrorResult.status === "retry" ? handleErrorResult.retry : void 0,
              skippedRetrying: handleErrorResult.status === "skipped"
            };
          }
          runResolve(output);
          const [outputError, stringifiedOutput] = await tryCatch(stringifyIO(output));
          if (outputError) {
            recordSpanException(span, outputError);
            await this.#cleanupAndWaitUntil(payload, ctx, initOutput, signal);
            return this.#internalErrorResult(execution, TaskRunErrorCodes.TASK_OUTPUT_ERROR, outputError);
          }
          const [exportError, finalOutput] = await tryCatch(conditionallyExportPacket(stringifiedOutput, `${attemptKey(ctx)}/output`, this._tracer));
          if (exportError) {
            recordSpanException(span, exportError);
            await this.#cleanupAndWaitUntil(payload, ctx, initOutput, signal);
            return this.#internalErrorResult(execution, TaskRunErrorCodes.TASK_OUTPUT_ERROR, exportError);
          }
          const [attrError, attributes] = await tryCatch(createPacketAttributes(finalOutput, SemanticInternalAttributes.OUTPUT, SemanticInternalAttributes.OUTPUT_TYPE));
          if (!attrError && attributes) {
            span.setAttributes(attributes);
          }
          await this.#callOnSuccessFunctions(payload, output, ctx, initOutput, signal);
          await this.#callOnCompleteFunctions(payload, { ok: true, data: output }, ctx, initOutput, signal);
          await this.#cleanupAndWaitUntil(payload, ctx, initOutput, signal);
          return {
            ok: true,
            id: execution.run.id,
            output: finalOutput.data,
            outputType: finalOutput.dataType
          };
        }, "executeTask");
        const globalMiddlewareHooks = lifecycleHooks.getGlobalMiddlewareHooks();
        const taskMiddlewareHook = lifecycleHooks.getTaskMiddlewareHook(this.task.id);
        const middlewareHooks = [
          ...globalMiddlewareHooks,
          taskMiddlewareHook ? { id: this.task.id, fn: taskMiddlewareHook } : void 0
        ].filter(Boolean);
        return await this.#executeTaskWithMiddlewareHooks(parsedPayload, ctx, execution, middlewareHooks, executeTask, signal);
      });
    }, {
      kind: SpanKind.CONSUMER,
      attributes: {
        [SemanticInternalAttributes.STYLE_ICON]: "attempt",
        [SemanticInternalAttributes.ENTITY_TYPE]: "attempt",
        [SemanticInternalAttributes.SPAN_ATTEMPT]: true,
        ...execution.attempt.number === 1 ? runTimelineMetrics.convertMetricsToSpanAttributes() : {},
        [SemanticInternalAttributes.STYLE_VARIANT]: this._isWarmStart ? WARM_VARIANT : COLD_VARIANT,
        ...typeof this._executionCount === "number" ? {
          [SemanticInternalAttributes.ATTEMPT_EXECUTION_COUNT]: this._executionCount
        } : {}
      },
      events: execution.attempt.number === 1 ? runTimelineMetrics.convertMetricsToSpanEvents() : void 0
    }, traceContext.extractContext());
    return { result };
  }
  async #executeTaskWithMiddlewareHooks(payload, ctx, execution, hooks, executeTask, signal) {
    let output;
    let executeError;
    const runner = hooks.reduceRight((next, hook) => {
      return async () => {
        await this._tracer.startActiveSpan("middleware()", async (span) => {
          await hook.fn({ payload, ctx, signal, task: this.task.id, next });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-middleware",
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        });
      };
    }, async () => {
      const [error, result] = await tryCatch(executeTask(payload));
      if (error) {
        executeError = error;
      } else {
        output = result;
      }
    });
    const [runnerError] = await tryCatch(runner());
    if (runnerError) {
      return this.#internalErrorResult(execution, TaskRunErrorCodes.TASK_MIDDLEWARE_ERROR, runnerError);
    }
    if (executeError) {
      throw executeError;
    }
    return output;
  }
  async #callRun(payload, ctx, init, signal) {
    const runFn = this.task.fns.run;
    if (!runFn) {
      throw new Error("Task does not have a run function");
    }
    return runTimelineMetrics.measureMetric("trigger.dev/execution", "run", async () => {
      return await this._tracer.startActiveSpan("run()", async (span) => {
        return await runFn(payload, { ctx, init, signal });
      }, {
        attributes: { [SemanticInternalAttributes.STYLE_ICON]: "task-fn-run" }
      });
    });
  }
  async #callOnWaitFunctions(wait, payload, ctx, initOutput, signal) {
    const globalWaitHooks = lifecycleHooks.getGlobalWaitHooks();
    const taskWaitHook = lifecycleHooks.getTaskWaitHook(this.task.id);
    if (globalWaitHooks.length === 0 && !taskWaitHook) {
      return;
    }
    const _result = await runTimelineMetrics.measureMetric("trigger.dev/execution", "onWait", async () => {
      for (const hook of globalWaitHooks) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onWait()", async (span) => {
          await hook.fn({ payload, ctx, signal, task: this.task.id, wait, init: initOutput });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onWait",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
      if (taskWaitHook) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onWait()", async (span) => {
          await taskWaitHook({
            payload,
            ctx,
            signal,
            task: this.task.id,
            wait,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onWait",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
    });
  }
  async #callOnCancelFunctions(runPromise, payload, ctx, initOutput, signal, attemptContext) {
    const globalCancelHooks = lifecycleHooks.getGlobalCancelHooks();
    const taskCancelHook = lifecycleHooks.getTaskCancelHook(this.task.id);
    if (globalCancelHooks.length === 0 && !taskCancelHook) {
      return;
    }
    const _result = await runTimelineMetrics.measureMetric("trigger.dev/execution", "onCancel", async () => {
      for (const hook of globalCancelHooks) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onCancel()", async (span) => {
          await hook.fn({
            payload,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput,
            runPromise
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onCancel",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }, attemptContext));
        if (hookError) {
          throw hookError;
        }
      }
      if (taskCancelHook) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onCancel()", async (span) => {
          await taskCancelHook({
            payload,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput,
            runPromise
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onCancel",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }, attemptContext));
        if (hookError) {
          throw hookError;
        }
      }
    });
  }
  async #callOnResumeFunctions(wait, payload, ctx, initOutput, signal) {
    const globalResumeHooks = lifecycleHooks.getGlobalResumeHooks();
    const taskResumeHook = lifecycleHooks.getTaskResumeHook(this.task.id);
    if (globalResumeHooks.length === 0 && !taskResumeHook) {
      return;
    }
    const _result = await runTimelineMetrics.measureMetric("trigger.dev/execution", "onResume", async () => {
      for (const hook of globalResumeHooks) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onResume()", async (span) => {
          await hook.fn({ payload, ctx, signal, task: this.task.id, wait, init: initOutput });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onResume",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
      if (taskResumeHook) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onResume()", async (span) => {
          await taskResumeHook({
            payload,
            ctx,
            signal,
            task: this.task.id,
            wait,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onResume",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
    });
  }
  async #callInitFunctions(payload, ctx, signal) {
    const globalInitHooks = lifecycleHooks.getGlobalInitHooks();
    const taskInitHook = lifecycleHooks.getTaskInitHook(this.task.id);
    if (globalInitHooks.length === 0 && !taskInitHook) {
      return {};
    }
    const result = await runTimelineMetrics.measureMetric("trigger.dev/execution", "init", async () => {
      const globalResults = [];
      for (const hook of globalInitHooks) {
        const [hookError, result2] = await tryCatch(this._tracer.startActiveSpan("init()", async (span) => {
          const result3 = await hook.fn({ payload, ctx, signal, task: this.task.id });
          if (result3 && typeof result3 === "object" && !Array.isArray(result3)) {
            span.setAttributes(flattenAttributes(result3, void 0, OTEL_SPAN_ATTRIBUTE_COUNT_LIMIT));
            return result3;
          }
          return {};
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-init",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
        if (hookError) {
          throw hookError;
        }
        if (result2 && typeof result2 === "object" && !Array.isArray(result2)) {
          globalResults.push(result2);
        }
      }
      const mergedGlobalResults = Object.assign({}, ...globalResults);
      if (taskInitHook) {
        const [hookError, taskResult] = await tryCatch(this._tracer.startActiveSpan("init()", async (span) => {
          const result2 = await taskInitHook({ payload, ctx, signal, task: this.task.id });
          if (result2 && typeof result2 === "object" && !Array.isArray(result2)) {
            span.setAttributes(flattenAttributes(result2, void 0, OTEL_SPAN_ATTRIBUTE_COUNT_LIMIT));
            return result2;
          }
          return {};
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-init",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
        if (hookError) {
          throw hookError;
        }
        if (taskResult && typeof taskResult === "object" && !Array.isArray(taskResult)) {
          return { ...mergedGlobalResults, ...taskResult };
        }
        return mergedGlobalResults;
      }
      return mergedGlobalResults;
    });
    if (result && typeof result === "object" && !Array.isArray(result)) {
      return result;
    }
    return;
  }
  async #callOnSuccessFunctions(payload, output, ctx, initOutput, signal) {
    const globalSuccessHooks = lifecycleHooks.getGlobalSuccessHooks();
    const taskSuccessHook = lifecycleHooks.getTaskSuccessHook(this.task.id);
    if (globalSuccessHooks.length === 0 && !taskSuccessHook) {
      return;
    }
    return await runTimelineMetrics.measureMetric("trigger.dev/execution", "success", async () => {
      for (const hook of globalSuccessHooks) {
        await tryCatch(this._tracer.startActiveSpan("onSuccess()", async (span) => {
          await hook.fn({
            payload,
            output,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onSuccess",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
      }
      if (taskSuccessHook) {
        await tryCatch(this._tracer.startActiveSpan("onSuccess()", async (span) => {
          await taskSuccessHook({
            payload,
            output,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onSuccess",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
      }
    });
  }
  async #callOnFailureFunctions(payload, error, ctx, initOutput, signal) {
    const globalFailureHooks = lifecycleHooks.getGlobalFailureHooks();
    const taskFailureHook = lifecycleHooks.getTaskFailureHook(this.task.id);
    if (globalFailureHooks.length === 0 && !taskFailureHook) {
      return;
    }
    return await runTimelineMetrics.measureMetric("trigger.dev/execution", "failure", async () => {
      for (const hook of globalFailureHooks) {
        await tryCatch(this._tracer.startActiveSpan("onFailure()", async (span) => {
          await hook.fn({
            payload,
            error,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onFailure",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
      }
      if (taskFailureHook) {
        await tryCatch(this._tracer.startActiveSpan("onFailure()", async (span) => {
          await taskFailureHook({
            payload,
            error,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onFailure",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
      }
    });
  }
  async #parsePayload(payload) {
    if (!this.task.fns.parsePayload) {
      return payload;
    }
    const [parseError2, result] = await tryCatch(this.task.fns.parsePayload(payload));
    if (parseError2) {
      throw new TaskPayloadParsedError(parseError2);
    }
    return result;
  }
  async #callOnStartFunctions(payload, ctx, initOutput, signal) {
    const globalStartHooks = lifecycleHooks.getGlobalStartHooks();
    const taskStartHook = lifecycleHooks.getTaskStartHook(this.task.id);
    if (globalStartHooks.length === 0 && !taskStartHook) {
      return;
    }
    return await runTimelineMetrics.measureMetric("trigger.dev/execution", "start", async () => {
      for (const hook of globalStartHooks) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onStart()", async (span) => {
          await hook.fn({ payload, ctx, signal, task: this.task.id, init: initOutput });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onStart",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
      if (taskStartHook) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onStart()", async (span) => {
          await taskStartHook({
            payload,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onStart",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
    });
  }
  async #callOnStartAttemptFunctions(payload, ctx, signal) {
    const globalStartHooks = lifecycleHooks.getGlobalStartAttemptHooks();
    const taskStartHook = lifecycleHooks.getTaskStartAttemptHook(this.task.id);
    if (globalStartHooks.length === 0 && !taskStartHook) {
      return;
    }
    return await runTimelineMetrics.measureMetric("trigger.dev/execution", "startAttempt", async () => {
      for (const hook of globalStartHooks) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onStartAttempt()", async (span) => {
          await hook.fn({ payload, ctx, signal, task: this.task.id });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onStartAttempt",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
      if (taskStartHook) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("onStartAttempt()", async (span) => {
          await taskStartHook({
            payload,
            ctx,
            signal,
            task: this.task.id
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onStartAttempt",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
    });
  }
  async #cleanupAndWaitUntil(payload, ctx, initOutput, signal) {
    await this.#callCleanupFunctions(payload, ctx, initOutput, signal);
    inputStreams.clearHandlers();
    sessionStreams.clearHandlers();
    await this.#blockForWaitUntil();
  }
  async #callCleanupFunctions(payload, ctx, initOutput, signal) {
    const globalCleanupHooks = lifecycleHooks.getGlobalCleanupHooks();
    const taskCleanupHook = lifecycleHooks.getTaskCleanupHook(this.task.id);
    if (globalCleanupHooks.length === 0 && !taskCleanupHook) {
      return;
    }
    return await runTimelineMetrics.measureMetric("trigger.dev/execution", "cleanup", async () => {
      for (const hook of globalCleanupHooks) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("cleanup()", async (span) => {
          await hook.fn({
            payload,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-cleanup",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
      if (taskCleanupHook) {
        const [hookError] = await tryCatch(this._tracer.startActiveSpan("cleanup()", async (span) => {
          await taskCleanupHook({
            payload,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-cleanup",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
        if (hookError) {
          throw hookError;
        }
      }
    });
  }
  async #blockForWaitUntil() {
    if (!waitUntil.requiresResolving()) {
      return;
    }
    return this._tracer.startActiveSpan("waitUntil", async (span) => {
      return await waitUntil.blockUntilSettled();
    }, {
      attributes: {
        [SemanticInternalAttributes.STYLE_ICON]: "tabler-clock",
        [SemanticInternalAttributes.COLLAPSED]: true
      }
    });
  }
  async #handleError(execution, error, payload, ctx, init, signal) {
    const retriesConfig = this._retries;
    const retry = this.task.retry ?? retriesConfig?.default;
    if (isInternalError(error) && error.skipRetrying) {
      return { status: "skipped", error };
    }
    if (error instanceof Error && (error.name === "AbortTaskRunError" || error.name === "TaskPayloadParsedError")) {
      return { status: "skipped" };
    }
    if (error instanceof Error && error.name === "TriggerApiError" && "status" in error && typeof error.status === "number") {
      const status = error.status;
      if (status && status >= 400 && status < 500 && status !== 408 && status !== 429) {
        return { status: "skipped", error };
      }
    }
    let defaultDelay;
    if (retry) {
      if (execution.run.maxAttempts) {
        retry.maxAttempts = Math.max(execution.run.maxAttempts, 1);
      }
      defaultDelay = calculateNextRetryDelay(retry, execution.attempt.number);
      if (defaultDelay && error instanceof Error && error.name === "TriggerApiError" && error.status === 429) {
        const rateLimitError = error;
        const rateLimitDelay = rateLimitError.millisecondsUntilReset;
        if (rateLimitDelay) {
          defaultDelay = rateLimitDelay;
        }
      }
    }
    const defaultRetryResult = typeof defaultDelay === "undefined" ? { status: "noop" } : {
      status: "retry",
      retry: { timestamp: Date.now() + defaultDelay, delay: defaultDelay }
    };
    if (execution.environment.type === "DEVELOPMENT" && typeof retriesConfig?.enabledInDev === "boolean" && !retriesConfig.enabledInDev) {
      return { status: "skipped" };
    }
    const taskCatchErrorHook = lifecycleHooks.getTaskCatchErrorHook(this.task.id);
    const globalCatchErrorHooks = lifecycleHooks.getGlobalCatchErrorHooks();
    if (globalCatchErrorHooks.length === 0 && !taskCatchErrorHook) {
      return defaultRetryResult;
    }
    return this._tracer.startActiveSpan("catchError", async (span) => {
      if (taskCatchErrorHook) {
        const result = await taskCatchErrorHook({
          payload,
          error,
          ctx,
          init,
          retry,
          retryDelayInMs: defaultDelay,
          retryAt: defaultDelay ? new Date(Date.now() + defaultDelay) : void 0,
          signal,
          task: this.task.id
        });
        if (result) {
          return this.#processHandleErrorResult(result, execution.attempt.number, defaultDelay);
        }
      }
      for (const hook of globalCatchErrorHooks) {
        const result = await hook.fn({
          payload,
          error,
          ctx,
          init,
          retry,
          retryDelayInMs: defaultDelay,
          retryAt: defaultDelay ? new Date(Date.now() + defaultDelay) : void 0,
          signal,
          task: this.task.id
        });
        if (result) {
          return this.#processHandleErrorResult(result, execution.attempt.number, defaultDelay);
        }
      }
      return defaultRetryResult;
    }, {
      attributes: {
        [SemanticInternalAttributes.STYLE_ICON]: "task-hook-catchError",
        [SemanticInternalAttributes.COLLAPSED]: true
      }
    });
  }
  // Helper method to process handle error results
  #processHandleErrorResult(result, attemptNumber, defaultDelay) {
    if (result.skipRetrying) {
      return { status: "skipped", error: result.error };
    }
    if (typeof result.retryAt !== "undefined") {
      return {
        status: "retry",
        retry: {
          timestamp: result.retryAt.getTime(),
          delay: result.retryAt.getTime() - Date.now()
        },
        error: result.error
      };
    }
    if (typeof result.retryDelayInMs === "number") {
      return {
        status: "retry",
        retry: {
          timestamp: Date.now() + result.retryDelayInMs,
          delay: result.retryDelayInMs
        },
        error: result.error
      };
    }
    if (result.retry && typeof result.retry === "object") {
      const delay = calculateNextRetryDelay(result.retry, attemptNumber);
      return typeof delay === "undefined" ? { status: "noop", error: result.error } : {
        status: "retry",
        retry: { timestamp: Date.now() + delay, delay },
        error: result.error
      };
    }
    return { status: "noop", error: result.error };
  }
  async #callOnCompleteFunctions(payload, result, ctx, initOutput, signal) {
    const globalCompleteHooks = lifecycleHooks.getGlobalCompleteHooks();
    const taskCompleteHook = lifecycleHooks.getTaskCompleteHook(this.task.id);
    if (globalCompleteHooks.length === 0 && !taskCompleteHook) {
      return;
    }
    return await runTimelineMetrics.measureMetric("trigger.dev/execution", "complete", async () => {
      for (const hook of globalCompleteHooks) {
        await tryCatch(this._tracer.startActiveSpan("onComplete()", async (span) => {
          await hook.fn({
            payload,
            result,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onComplete",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes(hook.name)
          }
        }));
      }
      if (taskCompleteHook) {
        await tryCatch(this._tracer.startActiveSpan("onComplete()", async (span) => {
          await taskCompleteHook({
            payload,
            result,
            ctx,
            signal,
            task: this.task.id,
            init: initOutput
          });
        }, {
          attributes: {
            [SemanticInternalAttributes.STYLE_ICON]: "task-hook-onComplete",
            [SemanticInternalAttributes.COLLAPSED]: true,
            ...this.#lifecycleHookAccessoryAttributes("task")
          }
        }));
      }
    });
  }
  #internalErrorResult(execution, code, error, skippedRetrying) {
    return {
      ok: false,
      id: execution.run.id,
      error: {
        type: "INTERNAL_ERROR",
        code,
        message: error instanceof Error ? `${error.name}: ${error.message}` : typeof error === "string" ? error : void 0,
        stackTrace: error instanceof Error ? error.stack : void 0
      },
      skippedRetrying
    };
  }
  #lifecycleHookAccessoryAttributes(name) {
    return accessoryAttributes({
      items: [
        {
          text: name ?? "global",
          variant: "normal"
        }
      ],
      style: "codepath"
    });
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/clock/preciseWallClock.js
init_esm();
var import_precise_date = __toESM(require_src2(), 1);
var PreciseWallClock = class {
  static {
    __name(this, "PreciseWallClock");
  }
  _origin;
  get #originClockTime() {
    return this._origin.clockTime;
  }
  get #originPreciseDate() {
    return this._origin.preciseDate;
  }
  constructor(options = {}) {
    this._origin = {
      clockTime: options.origin ?? process.hrtime(),
      preciseDate: options.now ?? new import_precise_date.PreciseDate()
    };
  }
  preciseNow() {
    const elapsedHrTime = process.hrtime(this.#originClockTime);
    const elapsedNanoseconds = BigInt(elapsedHrTime[0]) * BigInt(1e9) + BigInt(elapsedHrTime[1]);
    const preciseDate = new import_precise_date.PreciseDate(this.#originPreciseDate.getFullTime() + elapsedNanoseconds);
    const dateStruct = preciseDate.toStruct();
    return [dateStruct.seconds, dateStruct.nanos];
  }
  reset() {
    this._origin = {
      clockTime: process.hrtime(),
      preciseDate: new import_precise_date.PreciseDate()
    };
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/consoleInterceptor.js
init_esm();
var import_api_logs2 = __toESM(require_src3(), 1);
import util from "node:util";
var ConsoleInterceptor = class {
  static {
    __name(this, "ConsoleInterceptor");
  }
  logger;
  sendToStdIO;
  interceptingDisabled;
  maxAttributeCount;
  constructor(logger, sendToStdIO, interceptingDisabled, maxAttributeCount) {
    this.logger = logger;
    this.sendToStdIO = sendToStdIO;
    this.interceptingDisabled = interceptingDisabled;
    this.maxAttributeCount = maxAttributeCount;
  }
  // Intercept the console and send logs to the OpenTelemetry logger
  // during the execution of the callback
  async intercept(console2, callback) {
    if (this.interceptingDisabled) {
      return await callback();
    }
    const originalConsole = {
      log: console2.log,
      info: console2.info,
      warn: console2.warn,
      error: console2.error,
      debug: console2.debug
    };
    console2.log = this.log.bind(this);
    console2.info = this.info.bind(this);
    console2.warn = this.warn.bind(this);
    console2.error = this.error.bind(this);
    console2.debug = this.debug.bind(this);
    try {
      return await callback();
    } finally {
      console2.log = originalConsole.log;
      console2.info = originalConsole.info;
      console2.warn = originalConsole.warn;
      console2.error = originalConsole.error;
      console2.debug = originalConsole.debug;
    }
  }
  debug(...args) {
    this.#handleLog(import_api_logs2.SeverityNumber.DEBUG, this.#getTimestampInHrTime(), "Debug", ...args);
  }
  log(...args) {
    this.#handleLog(import_api_logs2.SeverityNumber.INFO, this.#getTimestampInHrTime(), "Log", ...args);
  }
  info(...args) {
    this.#handleLog(import_api_logs2.SeverityNumber.INFO, this.#getTimestampInHrTime(), "Info", ...args);
  }
  warn(...args) {
    this.#handleLog(import_api_logs2.SeverityNumber.WARN, this.#getTimestampInHrTime(), "Warn", ...args);
  }
  error(...args) {
    this.#handleLog(import_api_logs2.SeverityNumber.ERROR, this.#getTimestampInHrTime(), "Error", ...args);
  }
  #handleLog(severityNumber, timestamp, severityText, ...args) {
    const body = util.format(...args);
    if (this.sendToStdIO) {
      if (severityNumber === import_api_logs2.SeverityNumber.ERROR) {
        process.stderr.write(body);
      } else {
        process.stdout.write(body);
      }
    }
    const parsed = tryParseJSON(body);
    if (parsed.ok) {
      this.logger.emit({
        severityNumber,
        severityText,
        body: getLogMessage(parsed.value, severityText),
        attributes: {
          ...this.#getAttributes(severityNumber),
          ...flattenAttributes(parsed.value, void 0, this.maxAttributeCount)
        },
        timestamp
      });
      return;
    }
    this.logger.emit({
      severityNumber,
      severityText,
      body,
      attributes: this.#getAttributes(severityNumber),
      timestamp
    });
  }
  #getTimestampInHrTime() {
    return clock.preciseNow();
  }
  #getAttributes(severityNumber) {
    const icon = iconStringForSeverity(severityNumber);
    let result = {};
    if (icon !== void 0) {
      result[SemanticInternalAttributes.STYLE_ICON] = icon;
    }
    return result;
  }
};
function getLogMessage(value2, fallback) {
  if (typeof value2["message"] === "string") {
    return value2["message"];
  }
  if (typeof value2["msg"] === "string") {
    return value2["msg"];
  }
  if (typeof value2["body"] === "string") {
    return value2["body"];
  }
  if (typeof value2["error"] === "string") {
    return value2["error"];
  }
  return fallback;
}
__name(getLogMessage, "getLogMessage");
function tryParseJSON(value2) {
  try {
    const parsed = JSON.parse(value2);
    if (typeof parsed === "object" && parsed !== null && !Array.isArray(parsed)) {
      return { ok: true, value: parsed };
    }
    return { ok: false, value: value2 };
  } catch (_e) {
    return { ok: false, value: value2 };
  }
}
__name(tryParseJSON, "tryParseJSON");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/resource-catalog/standardResourceCatalog.js
init_esm();
var NO_FILE_CONTEXT = "<no-context>";
var StandardResourceCatalog = class {
  static {
    __name(this, "StandardResourceCatalog");
  }
  _taskSchemas = /* @__PURE__ */ new Map();
  _taskMetadata = /* @__PURE__ */ new Map();
  _taskFunctions = /* @__PURE__ */ new Map();
  _taskFileMetadata = /* @__PURE__ */ new Map();
  _promptMetadata = /* @__PURE__ */ new Map();
  _promptFunctions = /* @__PURE__ */ new Map();
  _promptFileMetadata = /* @__PURE__ */ new Map();
  _promptSchemas = /* @__PURE__ */ new Map();
  _currentFileContext;
  _queueMetadata = /* @__PURE__ */ new Map();
  _skillMetadata = /* @__PURE__ */ new Map();
  _skillFileMetadata = /* @__PURE__ */ new Map();
  _webhookMetadata = /* @__PURE__ */ new Map();
  _webhookFileMetadata = /* @__PURE__ */ new Map();
  _webhookIdCollisions = [];
  _declaredSessionWebhooks = /* @__PURE__ */ new Set();
  _claimedSessionWebhooks = /* @__PURE__ */ new Set();
  _sentinelContextWarned = /* @__PURE__ */ new Set();
  // Task ids registered more than once (across files and task types). Tasks are
  // keyed by id below, so a second registration silently overwrites the first;
  // we record the collision here so the indexer can fail loudly instead. Only
  // consumed by the index workers — runtime never reads it.
  _taskIdCollisions = [];
  setCurrentFileContext(filePath, entryPoint) {
    this._currentFileContext = { filePath, entryPoint };
  }
  clearCurrentFileContext() {
    this._currentFileContext = void 0;
  }
  // Task ids that were registered more than once during this indexing pass.
  listTaskIdCollisions() {
    return this._taskIdCollisions;
  }
  registerQueueMetadata(queue) {
    const existingQueue = this._queueMetadata.get(queue.name);
    if (existingQueue) {
      const isConcurrencyLimitDifferent = existingQueue.concurrencyLimit !== queue.concurrencyLimit;
      if (isConcurrencyLimitDifferent) {
        let message = `Queue "${queue.name}" is defined twice, with different settings.`;
        if (isConcurrencyLimitDifferent) {
          message += `
        - concurrencyLimit: ${existingQueue.concurrencyLimit} vs ${queue.concurrencyLimit}`;
        }
        message += "\n       Keeping the first definition:";
        message += `
        - concurrencyLimit: ${existingQueue.concurrencyLimit}`;
        console.warn(message);
        return;
      }
    }
    this._queueMetadata.set(queue.name, queue);
  }
  registerWorkerManifest(workerManifest) {
    for (const task of workerManifest.tasks) {
      this._taskFileMetadata.set(task.id, {
        filePath: task.filePath,
        entryPoint: task.entryPoint
      });
    }
  }
  registerTaskMetadata(task) {
    if (!this._currentFileContext) {
      return;
    }
    const { fns, schema, ...metadata } = task;
    if (!task.id) {
      return;
    }
    if (this._currentFileContext.filePath === NO_FILE_CONTEXT && !this._sentinelContextWarned.has(task.id)) {
      this._sentinelContextWarned.add(task.id);
      console.warn(`[trigger.dev] task "${task.id}" was registered via dynamic import during another task's run(); move to a static import if you notice any issues.`);
    }
    if (this._taskMetadata.has(task.id) && this._currentFileContext.filePath !== NO_FILE_CONTEXT) {
      const existingFilePath = this._taskFileMetadata.get(task.id)?.filePath;
      const currentFilePath = this._currentFileContext.filePath;
      const collision = this._taskIdCollisions.find((c) => c.id === task.id);
      if (collision) {
        collision.filePaths.push(currentFilePath);
      } else {
        this._taskIdCollisions.push({
          id: task.id,
          filePaths: [existingFilePath ?? currentFilePath, currentFilePath]
        });
      }
    }
    this._taskFileMetadata.set(task.id, {
      ...this._currentFileContext
    });
    this._taskMetadata.set(task.id, metadata);
    this._taskFunctions.set(task.id, fns);
    if (schema) {
      this._taskSchemas.set(task.id, schema);
    }
  }
  updateTaskMetadata(id, updates) {
    const { fns, schema, ...metadataUpdates } = updates;
    const existingMetadata = this._taskMetadata.get(id);
    if (existingMetadata && Object.keys(metadataUpdates).length > 0) {
      this._taskMetadata.set(id, {
        ...existingMetadata,
        ...metadataUpdates
      });
    }
    if (fns) {
      const existingFunctions = this._taskFunctions.get(id);
      if (existingFunctions) {
        this._taskFunctions.set(id, {
          ...existingFunctions,
          ...fns
        });
      }
    }
    if (schema) {
      this._taskSchemas.set(id, schema);
    }
  }
  // Return all the tasks, without the functions
  listTaskManifests() {
    const result = [];
    for (const [id, metadata] of this._taskMetadata) {
      const fileMetadata = this._taskFileMetadata.get(id);
      if (!fileMetadata) {
        continue;
      }
      const taskManifest = {
        ...metadata,
        ...fileMetadata
      };
      result.push(taskManifest);
    }
    return result;
  }
  getTaskSchema(id) {
    return this._taskSchemas.get(id);
  }
  listQueueManifests() {
    return Array.from(this._queueMetadata.values());
  }
  getTaskManifest(id) {
    const metadata = this._taskMetadata.get(id);
    const fileMetadata = this._taskFileMetadata.get(id);
    if (!metadata || !fileMetadata) {
      return void 0;
    }
    return {
      ...metadata,
      ...fileMetadata
    };
  }
  getTask(id) {
    const metadata = this._taskMetadata.get(id);
    const fileMetadata = this._taskFileMetadata.get(id);
    const fns = this._taskFunctions.get(id);
    if (!metadata || !fns || !fileMetadata) {
      return void 0;
    }
    return {
      ...metadata,
      ...fileMetadata,
      fns
    };
  }
  taskExists(id) {
    return this._taskMetadata.has(id);
  }
  registerPromptMetadata(prompt) {
    if (!this._currentFileContext) {
      return;
    }
    const { fns, schema, ...metadata } = prompt;
    if (!prompt.id) {
      return;
    }
    this._promptFileMetadata.set(prompt.id, {
      ...this._currentFileContext
    });
    this._promptMetadata.set(prompt.id, metadata);
    this._promptFunctions.set(prompt.id, fns);
    if (schema) {
      this._promptSchemas.set(prompt.id, schema);
    }
  }
  getPromptSchema(id) {
    return this._promptSchemas.get(id);
  }
  listPromptManifests() {
    const result = [];
    for (const [id, metadata] of this._promptMetadata) {
      const fileMetadata = this._promptFileMetadata.get(id);
      if (!fileMetadata) {
        continue;
      }
      result.push({
        ...metadata,
        ...fileMetadata
      });
    }
    return result;
  }
  getPrompt(id) {
    const metadata = this._promptMetadata.get(id);
    const fileMetadata = this._promptFileMetadata.get(id);
    const fns = this._promptFunctions.get(id);
    if (!metadata || !fns || !fileMetadata) {
      return void 0;
    }
    return {
      ...metadata,
      ...fileMetadata,
      fns
    };
  }
  registerSkillMetadata(skill) {
    if (!this._currentFileContext) {
      return;
    }
    if (!skill.id) {
      return;
    }
    const existing = this._skillMetadata.get(skill.id);
    if (existing && existing.sourcePath !== skill.sourcePath) {
      console.warn(`Skill "${skill.id}" is defined twice with different paths. Keeping the first:
  existing: ${existing.sourcePath}
  ignored:  ${skill.sourcePath}`);
      return;
    }
    this._skillFileMetadata.set(skill.id, {
      ...this._currentFileContext
    });
    this._skillMetadata.set(skill.id, skill);
  }
  listSkillManifests() {
    const result = [];
    for (const [id, metadata] of this._skillMetadata) {
      const fileMetadata = this._skillFileMetadata.get(id);
      if (!fileMetadata)
        continue;
      result.push({
        ...metadata,
        ...fileMetadata
      });
    }
    return result;
  }
  getSkillManifest(id) {
    const metadata = this._skillMetadata.get(id);
    const fileMetadata = this._skillFileMetadata.get(id);
    if (!metadata || !fileMetadata)
      return void 0;
    return {
      ...metadata,
      ...fileMetadata
    };
  }
  registerWebhookMetadata(webhook) {
    if (!this._currentFileContext) {
      return;
    }
    if (!webhook.id) {
      return;
    }
    if (this._webhookMetadata.has(webhook.id) && this._currentFileContext.filePath !== NO_FILE_CONTEXT) {
      const existingFilePath = this._webhookFileMetadata.get(webhook.id)?.filePath;
      const currentFilePath = this._currentFileContext.filePath;
      const collision = this._webhookIdCollisions.find((c) => c.id === webhook.id);
      if (collision) {
        collision.filePaths.push(currentFilePath);
      } else {
        this._webhookIdCollisions.push({
          id: webhook.id,
          filePaths: [existingFilePath ?? currentFilePath, currentFilePath]
        });
      }
    }
    this._webhookFileMetadata.set(webhook.id, { ...this._currentFileContext });
    this._webhookMetadata.set(webhook.id, webhook);
  }
  listWebhookIdCollisions() {
    return this._webhookIdCollisions;
  }
  registerDeclaredSessionWebhook(id) {
    if (!id)
      return;
    this._declaredSessionWebhooks.add(id);
  }
  markSessionWebhookClaimed(id) {
    if (!id)
      return;
    this._claimedSessionWebhooks.add(id);
  }
  listUnclaimedSessionWebhooks() {
    return [...this._declaredSessionWebhooks].filter((id) => !this._claimedSessionWebhooks.has(id));
  }
  listWebhookManifests() {
    const result = [];
    for (const [id, metadata] of this._webhookMetadata) {
      const fileMetadata = this._webhookFileMetadata.get(id);
      if (!fileMetadata)
        continue;
      result.push({ ...metadata, ...fileMetadata });
    }
    return result;
  }
  getWebhookManifest(id) {
    const metadata = this._webhookMetadata.get(id);
    const fileMetadata = this._webhookFileMetadata.get(id);
    if (!metadata || !fileMetadata)
      return void 0;
    return { ...metadata, ...fileMetadata };
  }
  disable() {
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/usage/devUsageManager.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/clock/clock.js
init_esm();
function calculateDurationInMs(start, end) {
  const [startSeconds, startNanoseconds] = start;
  const [endSeconds, endNanoseconds] = end;
  const seconds = endSeconds - startSeconds;
  const nanoseconds = endNanoseconds - startNanoseconds;
  return Math.floor(seconds * 1e3 + nanoseconds / 1e6);
}
__name(calculateDurationInMs, "calculateDurationInMs");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/usage/devUsageManager.js
var DevUsageMeasurement = class {
  static {
    __name(this, "DevUsageMeasurement");
  }
  id;
  startedAt;
  _pauses = /* @__PURE__ */ new Map();
  _endedAt;
  constructor(id, startedAt = clock.preciseNow()) {
    this.id = id;
    this.startedAt = startedAt;
  }
  stop() {
    this._endedAt = clock.preciseNow();
  }
  sample() {
    const endedAt = this._endedAt ?? clock.preciseNow();
    const wallTime = this.startedAt ? calculateDurationInMs(this.startedAt, endedAt) : 0;
    if (wallTime === 0) {
      return { cpuTime: 0, wallTime: 0 };
    }
    const totalPauses = Array.from(this._pauses.values()).reduce((total, pause) => {
      return total + calculateDurationInMs(pause.start, pause.end ?? endedAt);
    }, 0);
    const cpuTime = wallTime - totalPauses;
    return {
      wallTime,
      cpuTime
    };
  }
  registerPause(pauseId, start, end) {
    this._pauses.set(pauseId, { start, end });
  }
};
var DevUsageManager = class {
  static {
    __name(this, "DevUsageManager");
  }
  _firstMeasurement;
  _currentMeasurements = /* @__PURE__ */ new Map();
  _pauses = /* @__PURE__ */ new Map();
  _initialState = {
    cpuTime: 0,
    costInCents: 0
  };
  disable() {
  }
  async flush() {
  }
  reset() {
    this._firstMeasurement = void 0;
    this._currentMeasurements.clear();
    this._pauses.clear();
    this._initialState = {
      cpuTime: 0,
      costInCents: 0
    };
  }
  setInitialState(state) {
    this._initialState = state;
  }
  getInitialState() {
    return this._initialState;
  }
  sample() {
    return this._firstMeasurement?.sample();
  }
  start() {
    const id = generateRandomString();
    const measurement = new DevUsageMeasurement(id);
    if (!this._firstMeasurement) {
      this._firstMeasurement = measurement;
    }
    this._currentMeasurements.set(id, measurement);
    return measurement;
  }
  stop(measurement) {
    measurement.stop();
    const sample = measurement.sample();
    if (this._currentMeasurements.has(measurement.id)) {
      this._currentMeasurements.delete(measurement.id);
    }
    return sample;
  }
  async pauseAsync(cb) {
    const pauseId = generateRandomString();
    const pauseStart = clock.preciseNow();
    try {
      this._pauses.set(pauseId, { start: pauseStart });
      for (const measurement of this._currentMeasurements.values()) {
        measurement.registerPause(pauseId, pauseStart);
      }
      return await cb();
    } finally {
      const pauseEnd = clock.preciseNow();
      this._pauses.set(pauseId, { start: pauseStart, end: pauseEnd });
      for (const measurement of this._currentMeasurements.values()) {
        measurement.registerPause(pauseId, pauseStart, pauseEnd);
      }
    }
  }
};
function generateRandomString() {
  let result = "";
  const characters = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
  const charactersLength = characters.length;
  for (var i = 0; i < 16; i++) {
    result += characters.charAt(Math.floor(Math.random() * charactersLength));
  }
  return result;
}
__name(generateRandomString, "generateRandomString");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/timeout/usageTimeoutManager.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/timeout/types.js
init_esm();
var TaskRunExceededMaxDuration = class extends Error {
  static {
    __name(this, "TaskRunExceededMaxDuration");
  }
  timeoutInSeconds;
  usageInSeconds;
  constructor(timeoutInSeconds, usageInSeconds) {
    super(`Run exceeded maxDuration of ${timeoutInSeconds} seconds`);
    this.timeoutInSeconds = timeoutInSeconds;
    this.usageInSeconds = usageInSeconds;
    this.name = "TaskRunExceededMaxDuration";
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/timeout/usageTimeoutManager.js
var UsageTimeoutManager = class {
  static {
    __name(this, "UsageTimeoutManager");
  }
  usageManager;
  _abortController;
  _abortSignal;
  _intervalId;
  _listener;
  constructor(usageManager) {
    this.usageManager = usageManager;
    this._abortController = new AbortController();
  }
  registerListener(listener) {
    this._listener = listener;
  }
  get signal() {
    return this._abortSignal;
  }
  reset() {
    this._abortController = new AbortController();
    this._abortSignal = void 0;
    if (this._intervalId) {
      clearInterval(this._intervalId);
      this._intervalId = void 0;
    }
  }
  abortAfterTimeout(timeoutInSeconds) {
    this._abortSignal = this._abortController.signal;
    if (!timeoutInSeconds) {
      return this._abortController;
    }
    if (this._intervalId) {
      clearInterval(this._intervalId);
    }
    this._intervalId = setInterval(() => {
      const sample = this.usageManager.sample();
      if (sample) {
        if (sample.cpuTime > timeoutInSeconds * 1e3) {
          clearInterval(this._intervalId);
          const elapsedTimeInSeconds = sample.cpuTime / 1e3;
          if (this._listener) {
            void this._listener(timeoutInSeconds, elapsedTimeInSeconds);
          }
          this._abortController.abort(new TaskRunExceededMaxDuration(timeoutInSeconds, elapsedTimeInSeconds));
        }
      }
    }, 1e3);
    return this._abortController;
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/runMetadata/manager.js
init_esm();

// ../../node_modules/.bun/dequal@2.0.3/node_modules/dequal/lite/index.mjs
init_esm();
var has = Object.prototype.hasOwnProperty;
function dequal(foo, bar) {
  var ctor, len;
  if (foo === bar) return true;
  if (foo && bar && (ctor = foo.constructor) === bar.constructor) {
    if (ctor === Date) return foo.getTime() === bar.getTime();
    if (ctor === RegExp) return foo.toString() === bar.toString();
    if (ctor === Array) {
      if ((len = foo.length) === bar.length) {
        while (len-- && dequal(foo[len], bar[len])) ;
      }
      return len === -1;
    }
    if (!ctor || typeof foo === "object") {
      len = 0;
      for (ctor in foo) {
        if (has.call(foo, ctor) && ++len && !has.call(bar, ctor)) return false;
        if (!(ctor in bar) || !dequal(foo[ctor], bar[ctor])) return false;
      }
      return Object.keys(bar).length === len;
    }
  }
  return foo !== foo && bar !== bar;
}
__name(dequal, "dequal");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/runMetadata/manager.js
var StandardMetadataManager = class {
  static {
    __name(this, "StandardMetadataManager");
  }
  apiClient;
  flushTimeoutId = null;
  isFlushing = false;
  store;
  queuedOperations = /* @__PURE__ */ new Set();
  queuedParentOperations = /* @__PURE__ */ new Set();
  queuedRootOperations = /* @__PURE__ */ new Set();
  runId;
  runIdIsRoot = false;
  constructor(apiClient) {
    this.apiClient = apiClient;
  }
  reset() {
    this.queuedOperations.clear();
    this.queuedParentOperations.clear();
    this.queuedRootOperations.clear();
    this.store = void 0;
    this.runId = void 0;
    this.runIdIsRoot = false;
    if (this.flushTimeoutId) {
      clearTimeout(this.flushTimeoutId);
      this.flushTimeoutId = null;
    }
    this.isFlushing = false;
  }
  get parent() {
    const self = this;
    const parentUpdater = {
      set: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.set(key, value2);
        }
        self.queuedParentOperations.add({ type: "set", key, value: value2 });
        return parentUpdater;
      }, "set"),
      del: /* @__PURE__ */ __name((key) => {
        if (self.runIdIsRoot) {
          return self.del(key);
        }
        self.queuedParentOperations.add({ type: "delete", key });
        return parentUpdater;
      }, "del"),
      append: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.append(key, value2);
        }
        self.queuedParentOperations.add({ type: "append", key, value: value2 });
        return parentUpdater;
      }, "append"),
      remove: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.remove(key, value2);
        }
        self.queuedParentOperations.add({ type: "remove", key, value: value2 });
        return parentUpdater;
      }, "remove"),
      increment: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.increment(key, value2);
        }
        self.queuedParentOperations.add({ type: "increment", key, value: value2 });
        return parentUpdater;
      }, "increment"),
      decrement: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.decrement(key, value2);
        }
        self.queuedParentOperations.add({ type: "increment", key, value: -Math.abs(value2) });
        return parentUpdater;
      }, "decrement"),
      update: /* @__PURE__ */ __name((value2) => {
        if (self.runIdIsRoot) {
          return self.update(value2);
        }
        self.queuedParentOperations.add({ type: "update", value: value2 });
        return parentUpdater;
      }, "update"),
      stream: /* @__PURE__ */ __name((key, value2, signal) => {
        if (self.runIdIsRoot) {
          return self.doStream(key, value2, "self", parentUpdater, signal);
        }
        return self.doStream(key, value2, "parent", parentUpdater, signal);
      }, "stream")
    };
    return parentUpdater;
  }
  get root() {
    const self = this;
    const rootUpdater = {
      set: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.set(key, value2);
        }
        self.queuedRootOperations.add({ type: "set", key, value: value2 });
        return rootUpdater;
      }, "set"),
      del: /* @__PURE__ */ __name((key) => {
        if (self.runIdIsRoot) {
          return self.del(key);
        }
        self.queuedRootOperations.add({ type: "delete", key });
        return rootUpdater;
      }, "del"),
      append: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.append(key, value2);
        }
        self.queuedRootOperations.add({ type: "append", key, value: value2 });
        return rootUpdater;
      }, "append"),
      remove: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.remove(key, value2);
        }
        self.queuedRootOperations.add({ type: "remove", key, value: value2 });
        return rootUpdater;
      }, "remove"),
      increment: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.increment(key, value2);
        }
        self.queuedRootOperations.add({ type: "increment", key, value: value2 });
        return rootUpdater;
      }, "increment"),
      decrement: /* @__PURE__ */ __name((key, value2) => {
        if (self.runIdIsRoot) {
          return self.decrement(key, value2);
        }
        self.queuedRootOperations.add({ type: "increment", key, value: -Math.abs(value2) });
        return rootUpdater;
      }, "decrement"),
      update: /* @__PURE__ */ __name((value2) => {
        if (self.runIdIsRoot) {
          return self.update(value2);
        }
        self.queuedRootOperations.add({ type: "update", value: value2 });
        return rootUpdater;
      }, "update"),
      stream: /* @__PURE__ */ __name((key, value2, signal) => {
        if (self.runIdIsRoot) {
          return self.doStream(key, value2, "self", rootUpdater, signal);
        }
        return self.doStream(key, value2, "root", rootUpdater, signal);
      }, "stream")
    };
    return rootUpdater;
  }
  enterWithMetadata(metadata) {
    this.store = metadata ?? {};
  }
  current() {
    return this.store;
  }
  getKey(key) {
    return this.store?.[key];
  }
  enqueueOperation(operation) {
    const applyResults = applyMetadataOperations(this.store ?? {}, operation);
    if (applyResults.unappliedOperations.length > 0) {
      return;
    }
    if (dequal(this.store, applyResults.newMetadata)) {
      return;
    }
    this.queuedOperations.add(operation);
    this.store = applyResults.newMetadata;
  }
  set(key, value2) {
    if (!this.runId) {
      return this;
    }
    this.enqueueOperation({ type: "set", key, value: value2 });
    return this;
  }
  del(key) {
    if (!this.runId) {
      return this;
    }
    this.enqueueOperation({ type: "delete", key });
    return this;
  }
  append(key, value2) {
    if (!this.runId) {
      return this;
    }
    this.enqueueOperation({ type: "append", key, value: value2 });
    return this;
  }
  remove(key, value2) {
    if (!this.runId) {
      return this;
    }
    this.enqueueOperation({ type: "remove", key, value: value2 });
    return this;
  }
  increment(key, increment = 1) {
    if (!this.runId) {
      return this;
    }
    this.enqueueOperation({ type: "increment", key, value: increment });
    return this;
  }
  decrement(key, decrement = 1) {
    return this.increment(key, -decrement);
  }
  update(metadata) {
    if (!this.runId) {
      return this;
    }
    this.enqueueOperation({ type: "update", value: metadata });
    return this;
  }
  async stream(key, value2, signal) {
    return this.doStream(key, value2, "self", this, signal);
  }
  async fetchStream(key, signal) {
    if (!this.runId) {
      throw new Error("Run ID is not set. fetchStream() can only be used inside a task.");
    }
    return await this.apiClient.fetchStream(this.runId, key, {
      signal,
      timeoutInSeconds: 60,
      lastEventId: void 0
    });
  }
  async doStream(key, value2, target, updater = this, signal) {
    const $value = value2;
    if (!this.runId) {
      return $value;
    }
    const streamInstance = realtimeStreams.pipe(key, value2, {
      signal,
      target
    });
    return streamInstance.stream;
  }
  async refresh(requestOptions) {
    if (!this.runId) {
      return;
    }
    try {
      const metadata = await this.apiClient.getRunMetadata(this.runId, requestOptions);
      this.store = metadata.metadata;
    } catch (error) {
      console.error("Failed to refresh metadata", error);
      throw error;
    }
  }
  async flush(requestOptions) {
    if (!this.runId) {
      return;
    }
    if (!this.#needsFlush()) {
      return;
    }
    if (this.isFlushing) {
      return;
    }
    this.isFlushing = true;
    const operations = Array.from(this.queuedOperations);
    this.queuedOperations.clear();
    const parentOperations = Array.from(this.queuedParentOperations);
    this.queuedParentOperations.clear();
    const rootOperations = Array.from(this.queuedRootOperations);
    this.queuedRootOperations.clear();
    try {
      const collapsedOperations = collapseOperations(operations);
      const collapsedParentOperations = collapseOperations(parentOperations);
      const collapsedRootOperations = collapseOperations(rootOperations);
      const response = await this.apiClient.updateRunMetadata(this.runId, {
        operations: collapsedOperations,
        parentOperations: collapsedParentOperations,
        rootOperations: collapsedRootOperations
      }, requestOptions);
      this.store = response.metadata;
    } catch (error) {
      console.error("Failed to flush metadata", error);
    } finally {
      this.isFlushing = false;
    }
  }
  startPeriodicFlush(intervalMs = 1e3) {
    const periodicFlush = /* @__PURE__ */ __name(async (intervalMs2) => {
      if (this.isFlushing) {
        return;
      }
      try {
        await this.flush();
      } catch (error) {
        console.error("Failed to flush metadata", error);
        throw error;
      } finally {
        this.isFlushing = false;
        scheduleNext();
      }
    }, "periodicFlush");
    const scheduleNext = /* @__PURE__ */ __name(() => {
      this.flushTimeoutId = setTimeout(() => periodicFlush(intervalMs), intervalMs);
    }, "scheduleNext");
    scheduleNext();
  }
  stopPeriodicFlush() {
    if (this.flushTimeoutId) {
      clearTimeout(this.flushTimeoutId);
      this.flushTimeoutId = null;
    }
  }
  async stopAndReturnLastFlush() {
    this.stopPeriodicFlush();
    this.isFlushing = true;
    if (!this.#needsFlush()) {
      return { dataType: "application/json" };
    }
    const operations = Array.from(this.queuedOperations);
    const parentOperations = Array.from(this.queuedParentOperations);
    const rootOperations = Array.from(this.queuedRootOperations);
    const data = {
      operations: collapseOperations(operations),
      parentOperations: collapseOperations(parentOperations),
      rootOperations: collapseOperations(rootOperations)
    };
    const packet = await stringifyIO(data);
    return packet;
  }
  #needsFlush() {
    return this.queuedOperations.size > 0 || this.queuedParentOperations.size > 0 || this.queuedRootOperations.size > 0;
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/waitUntil/manager.js
init_esm();
var StandardWaitUntilManager = class {
  static {
    __name(this, "StandardWaitUntilManager");
  }
  timeoutInMs;
  maybeDeferredPromises = /* @__PURE__ */ new Set();
  constructor(timeoutInMs = 6e4) {
    this.timeoutInMs = timeoutInMs;
  }
  reset() {
    this.maybeDeferredPromises.clear();
  }
  register(promise) {
    this.maybeDeferredPromises.add(promise);
  }
  async blockUntilSettled() {
    if (this.promisesRequringResolving.length === 0) {
      return;
    }
    const promises = this.promisesRequringResolving.map((p) => typeof p.promise === "function" ? p.promise(this.timeoutInMs) : p.promise);
    await Promise.race([
      Promise.allSettled(promises),
      new Promise((resolve2, _) => setTimeout(() => resolve2(), this.timeoutInMs))
    ]);
    this.maybeDeferredPromises.clear();
  }
  requiresResolving() {
    return this.promisesRequringResolving.length > 0;
  }
  get promisesRequringResolving() {
    return Array.from(this.maybeDeferredPromises).filter((p) => p.requiresResolving());
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/runtime/sharedRuntimeManager.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/runtime/preventMultipleWaits.js
init_esm();
var concurrentWaitErrorMessage = "Parallel waits are not supported, e.g. using Promise.all() around our wait functions.";
function preventMultipleWaits() {
  let isExecutingWait = false;
  return async (cb) => {
    if (isExecutingWait) {
      console.error(concurrentWaitErrorMessage);
      throw new InternalError({
        code: TaskRunErrorCodes.TASK_DID_CONCURRENT_WAIT,
        message: concurrentWaitErrorMessage,
        skipRetrying: true,
        showStackTrace: false
      });
    }
    isExecutingWait = true;
    try {
      await Promise.resolve();
      return await cb();
    } finally {
      isExecutingWait = false;
    }
  };
}
__name(preventMultipleWaits, "preventMultipleWaits");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/runtime/sharedRuntimeManager.js
var SharedRuntimeManager = class {
  static {
    __name(this, "SharedRuntimeManager");
  }
  ipc;
  showLogs;
  /** Maps a resolver ID to a resolver function */
  resolversById = /* @__PURE__ */ new Map();
  /** Stores waitpoints that arrive before their resolvers have been created */
  waitpointsByResolverId = /* @__PURE__ */ new Map();
  _preventMultipleWaits = preventMultipleWaits();
  constructor(ipc, showLogs) {
    this.ipc = ipc;
    this.showLogs = showLogs;
    setInterval(() => {
      this.debugLog("SharedRuntimeManager status");
    }, 3e5);
  }
  reset() {
    this.resolversById.clear();
    this.waitpointsByResolverId.clear();
  }
  disable() {
  }
  async waitForTask(params) {
    return this._preventMultipleWaits(async () => {
      const promise = new Promise((resolve2) => {
        this.resolversById.set(params.id, resolve2);
      });
      this.resolvePendingWaitpoints();
      await lifecycleHooks.callOnWaitHookListeners({
        type: "task",
        runId: params.id
      });
      const waitpoint = await this.suspendable(promise);
      const result = this.waitpointToTaskRunExecutionResult(waitpoint);
      await lifecycleHooks.callOnResumeHookListeners({
        type: "task",
        runId: params.id
      });
      return result;
    });
  }
  async waitForBatch(params) {
    return this._preventMultipleWaits(async () => {
      if (!params.runCount) {
        return Promise.resolve({ id: params.id, items: [] });
      }
      const promises = Array.from({ length: params.runCount }, (_, index) => {
        const resolverId = `${params.id}_${index}`;
        return new Promise((resolve2, reject) => {
          this.resolversById.set(resolverId, resolve2);
        });
      });
      this.resolvePendingWaitpoints();
      await lifecycleHooks.callOnWaitHookListeners({
        type: "batch",
        batchId: params.id,
        runCount: params.runCount
      });
      const waitpoints = await this.suspendable(Promise.all(promises));
      await lifecycleHooks.callOnResumeHookListeners({
        type: "batch",
        batchId: params.id,
        runCount: params.runCount
      });
      return {
        id: params.id,
        items: waitpoints.map(this.waitpointToTaskRunExecutionResult)
      };
    });
  }
  async waitForWaitpoint({ waitpointFriendlyId, finishDate }) {
    return this._preventMultipleWaits(async () => {
      const promise = new Promise((resolve2) => {
        this.resolversById.set(waitpointFriendlyId, resolve2);
      });
      this.resolvePendingWaitpoints();
      if (finishDate) {
        await lifecycleHooks.callOnWaitHookListeners({
          type: "duration",
          date: finishDate
        });
      } else {
        await lifecycleHooks.callOnWaitHookListeners({
          type: "token",
          token: waitpointFriendlyId
        });
      }
      const waitpoint = await this.suspendable(promise);
      if (finishDate) {
        await lifecycleHooks.callOnResumeHookListeners({
          type: "duration",
          date: finishDate
        });
      } else {
        await lifecycleHooks.callOnResumeHookListeners({
          type: "token",
          token: waitpointFriendlyId
        });
      }
      return {
        ok: !waitpoint.outputIsError,
        output: waitpoint.output,
        outputType: waitpoint.outputType
      };
    });
  }
  async resolveWaitpoints(waitpoints) {
    await Promise.all(waitpoints.map((waitpoint) => this.resolveWaitpoint(waitpoint)));
  }
  resolverIdFromWaitpoint(waitpoint) {
    let id;
    switch (waitpoint.type) {
      case "RUN": {
        if (!waitpoint.completedByTaskRun) {
          this.debugLog("no completedByTaskRun for RUN waitpoint", {
            waitpoint: this.waitpointForDebugLog(waitpoint)
          });
          return null;
        }
        if (waitpoint.completedByTaskRun.batch) {
          id = `${waitpoint.completedByTaskRun.batch.friendlyId}_${waitpoint.index}`;
        } else {
          id = waitpoint.completedByTaskRun.friendlyId;
        }
        break;
      }
      case "BATCH": {
        if (!waitpoint.completedByBatch) {
          this.debugLog("no completedByBatch for BATCH waitpoint", {
            waitpoint: this.waitpointForDebugLog(waitpoint)
          });
          return null;
        }
        id = waitpoint.completedByBatch.friendlyId;
        break;
      }
      case "MANUAL":
      case "DATETIME": {
        id = waitpoint.friendlyId;
        break;
      }
      default: {
        assertExhaustive(waitpoint.type);
      }
    }
    return id;
  }
  resolveWaitpoint(waitpoint, resolverId) {
    this.log("resolveWaitpoint", { id: waitpoint.id, type: waitpoint.type });
    if (waitpoint.type === "BATCH") {
      this.debugLog("ignoring BATCH waitpoint", {
        waitpoint: this.waitpointForDebugLog(waitpoint)
      });
      return;
    }
    resolverId = resolverId ?? this.resolverIdFromWaitpoint(waitpoint);
    if (!resolverId) {
      this.debugLog("no resolverId for waitpoint", {
        waitpoint: this.waitpointForDebugLog(waitpoint)
      });
      return;
    }
    const resolve2 = this.resolversById.get(resolverId);
    if (!resolve2) {
      this.debugLog("no resolver found for resolverId", {
        resolverId,
        waitpoint: this.waitpointForDebugLog(waitpoint)
      });
      this.waitpointsByResolverId.set(resolverId, waitpoint);
      return;
    }
    clock.reset();
    resolve2(waitpoint);
    this.resolversById.delete(resolverId);
    this.waitpointsByResolverId.delete(resolverId);
  }
  resolvePendingWaitpoints() {
    for (const [resolverId, waitpoint] of this.waitpointsByResolverId.entries()) {
      this.resolveWaitpoint(waitpoint, resolverId);
    }
  }
  setSuspendable(suspendable) {
    this.ipc.send("SET_SUSPENDABLE", { suspendable });
  }
  async suspendable(promise) {
    this.setSuspendable(true);
    const [error, result] = await tryCatch(promise);
    this.setSuspendable(false);
    if (error) {
      this.debugLog("error in suspendable wrapper", { error: String(error) });
      throw error;
    }
    return result;
  }
  waitpointToTaskRunExecutionResult(waitpoint) {
    if (!waitpoint.completedByTaskRun?.friendlyId)
      throw new Error("Missing completedByTaskRun");
    if (waitpoint.outputIsError) {
      return {
        ok: false,
        id: waitpoint.completedByTaskRun.friendlyId,
        error: waitpoint.output ? JSON.parse(waitpoint.output) : {
          type: "STRING_ERROR",
          message: "Missing error output"
        }
      };
    } else {
      return {
        ok: true,
        id: waitpoint.completedByTaskRun.friendlyId,
        output: waitpoint.output,
        outputType: waitpoint.outputType ?? "application/json"
      };
    }
  }
  waitpointForDebugLog(waitpoint) {
    const { completedAfter, completedAt, output, ...rest } = waitpoint;
    return {
      ...rest,
      output: output?.slice(0, 100),
      completedAfter: completedAfter?.toISOString(),
      completedAt: completedAt?.toISOString(),
      completedAfterDate: completedAfter,
      completedAtDate: completedAt
    };
  }
  debugLog(message, properties) {
    if (this.showLogs) {
      console.log(`[${(/* @__PURE__ */ new Date()).toISOString()}] ${message}`, {
        runtimeStatus: this.status,
        ...properties
      });
    }
    this.ipc.send("SEND_DEBUG_LOG", {
      message,
      properties: {
        runtimeStatus: this.status,
        ...properties
      }
    });
  }
  log(message, ...args) {
    if (!this.showLogs)
      return;
    console.log(`[${(/* @__PURE__ */ new Date()).toISOString()}] ${message}`, args);
  }
  get status() {
    return {
      resolversById: Array.from(this.resolversById.keys()),
      waitpointsByResolverId: Array.from(this.waitpointsByResolverId.keys())
    };
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/workers/populateEnv.js
init_esm();
function populateEnv(envObject, options = {}) {
  const { override = false, debug: debug4 = false, previousEnv } = options;
  if (!envObject || typeof envObject !== "object") {
    return;
  }
  for (const key of Object.keys(envObject)) {
    if (Object.hasOwn(process.env, key)) {
      if (override) {
        process.env[key] = envObject[key];
        if (debug4) {
          console.log(`"${key}" is already defined and WAS overwritten`);
        }
      } else if (debug4) {
        console.log(`"${key}" is already defined and was NOT overwritten`);
      }
    } else {
      process.env[key] = envObject[key];
    }
  }
  if (previousEnv) {
    for (const key of Object.keys(previousEnv)) {
      if (!Object.hasOwn(envObject, key)) {
        delete process.env[key];
      }
    }
  }
}
__name(populateEnv, "populateEnv");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/traceContext/manager.js
init_esm();
init_esm2();
var import_core6 = __toESM(require_src(), 1);
var StandardTraceContextManager = class {
  static {
    __name(this, "StandardTraceContextManager");
  }
  traceContext = {};
  getTraceContext() {
    return this.traceContext;
  }
  reset() {
    this.traceContext = {};
  }
  getExternalTraceContext() {
    return extractExternalTraceContext(this.traceContext?.external);
  }
  extractContext() {
    return propagation.extract(context.active(), this.traceContext ?? {});
  }
  withExternalTrace(fn) {
    const externalTraceContext = this.getExternalTraceContext();
    if (!externalTraceContext) {
      return fn();
    }
    const currentSpanContext = trace.getActiveSpan()?.spanContext();
    if (!currentSpanContext) {
      throw new Error("No active span found. withExternalSpan must be called within an active span context.");
    }
    const spanContext = {
      traceId: externalTraceContext.traceId,
      spanId: currentSpanContext.spanId,
      traceFlags: externalTraceContext.traceFlags,
      isRemote: true
    };
    const contextWithSpan = trace.setSpanContext(context.active(), spanContext);
    return context.with(contextWithSpan, fn);
  }
};
function extractExternalTraceContext(traceContext2) {
  if (typeof traceContext2 !== "object" || traceContext2 === null) {
    return void 0;
  }
  const tracestate = "tracestate" in traceContext2 && typeof traceContext2.tracestate === "string" ? traceContext2.tracestate : void 0;
  if ("traceparent" in traceContext2 && typeof traceContext2.traceparent === "string") {
    const externalSpanContext = (0, import_core6.parseTraceParent)(traceContext2.traceparent);
    if (!externalSpanContext) {
      return void 0;
    }
    return {
      traceId: externalSpanContext.traceId,
      spanId: externalSpanContext.spanId,
      traceFlags: externalSpanContext.traceFlags,
      tracestate
    };
  }
  return void 0;
}
__name(extractExternalTraceContext, "extractExternalTraceContext");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/heartbeats/manager.js
init_esm();
import { setInterval as setInterval2, setTimeout as setTimeout2 } from "node:timers/promises";
var StandardHeartbeatsManager = class {
  static {
    __name(this, "StandardHeartbeatsManager");
  }
  intervalInMs;
  listener = void 0;
  currentAbortController = void 0;
  lastHeartbeatYieldTime = void 0;
  lastHeartbeatDate = void 0;
  constructor(intervalInMs) {
    this.intervalInMs = intervalInMs;
  }
  registerListener(callback) {
    this.listener = callback;
  }
  async yield() {
    if (!this.lastHeartbeatYieldTime) {
      return;
    }
    if (Date.now() - this.lastHeartbeatYieldTime >= this.intervalInMs) {
      await setTimeout2(24);
      this.lastHeartbeatYieldTime = Date.now();
    }
  }
  startHeartbeat(id) {
    this.stopHeartbeat();
    this.currentAbortController = new AbortController();
    this.lastHeartbeatYieldTime = Date.now();
    this.startHeartbeatLoop(id, this.currentAbortController.signal).catch((error) => {
    });
  }
  async startHeartbeatLoop(id, signal) {
    try {
      for await (const _ of setInterval2(this.intervalInMs, void 0, {
        signal
      })) {
        if (this.listener) {
          const [error] = await tryCatch(this.listener(id));
          this.lastHeartbeatDate = /* @__PURE__ */ new Date();
          if (error) {
            console.error("Failed to send HEARTBEAT message", { error: String(error) });
          }
        }
      }
    } catch (_error) {
    }
  }
  stopHeartbeat() {
    this.currentAbortController?.abort();
  }
  get lastHeartbeat() {
    return this.lastHeartbeatDate;
  }
  reset() {
    this.stopHeartbeat();
    this.lastHeartbeatDate = void 0;
    this.lastHeartbeatYieldTime = void 0;
    this.currentAbortController = void 0;
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/realtimeStreams/manager.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/realtimeStreams/streamInstance.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/realtimeStreams/streamsWriterV1.js
init_esm();
import { request as httpsRequest } from "node:https";
import { request as httpRequest } from "node:http";
import { URL as URL2 } from "node:url";
import { randomBytes } from "node:crypto";
var StreamsWriterV1 = class {
  static {
    __name(this, "StreamsWriterV1");
  }
  options;
  controller = new AbortController();
  serverStream;
  consumerStream;
  streamPromise;
  retryCount = 0;
  maxRetries;
  currentChunkIndex = 0;
  baseDelayMs = 1e3;
  // 1 second base delay
  maxDelayMs = 3e4;
  // 30 seconds max delay
  maxBufferSize;
  clientId;
  ringBuffer = [];
  // Ring buffer for recent chunks
  bufferStartIndex = 0;
  // Index of the oldest chunk in buffer
  highestBufferedIndex = -1;
  // Highest chunk index that's been buffered
  streamReader = null;
  bufferReaderTask = null;
  streamComplete = false;
  constructor(options) {
    this.options = options;
    const [serverStream, consumerStream] = this.options.source.tee();
    this.serverStream = serverStream;
    this.consumerStream = consumerStream;
    this.maxRetries = options.maxRetries ?? 10;
    this.maxBufferSize = options.maxBufferSize ?? 1e4;
    this.clientId = options.clientId || this.generateClientId();
    this.startBuffering();
    this.streamPromise = this.initializeServerStream();
  }
  generateClientId() {
    return randomBytes(4).toString("hex");
  }
  startBuffering() {
    this.streamReader = this.serverStream.getReader();
    this.bufferReaderTask = (async () => {
      try {
        let chunkIndex = 0;
        while (true) {
          const { done, value: value2 } = await this.streamReader.read();
          if (done) {
            this.streamComplete = true;
            break;
          }
          this.addToRingBuffer(chunkIndex, value2);
          this.highestBufferedIndex = chunkIndex;
          chunkIndex++;
        }
      } catch (error) {
        throw error;
      }
    })();
  }
  async makeRequest(startFromChunk = 0) {
    return new Promise((resolve2, reject) => {
      const url = new URL2(this.buildUrl());
      const timeout = 15 * 60 * 1e3;
      const requestFn = url.protocol === "https:" ? httpsRequest : httpRequest;
      const req = requestFn({
        method: "POST",
        hostname: url.hostname,
        port: url.port || (url.protocol === "https:" ? 443 : 80),
        path: url.pathname + url.search,
        headers: {
          ...this.options.headers,
          "Content-Type": "application/json",
          "X-Client-Id": this.clientId,
          "X-Resume-From-Chunk": startFromChunk.toString(),
          "X-Stream-Version": this.options.version ?? "v1"
        },
        timeout
      });
      req.on("error", async (error) => {
        const _errorCode = "code" in error ? error.code : void 0;
        const _errorMsg = error instanceof Error ? error.message : String(error);
        if (this.isRetryableError(error)) {
          if (this.retryCount < this.maxRetries) {
            this.retryCount++;
            req.destroy();
            const delayMs = this.calculateBackoffDelay();
            await this.delay(delayMs);
            const serverLastChunk = await this.queryServerLastChunkIndex();
            const resumeFromChunk = serverLastChunk + 1;
            resolve2(this.makeRequest(resumeFromChunk));
            return;
          }
        }
        reject(error);
      });
      req.on("timeout", async () => {
        if (this.retryCount < this.maxRetries) {
          this.retryCount++;
          req.destroy();
          const delayMs = this.calculateBackoffDelay();
          await this.delay(delayMs);
          const serverLastChunk = await this.queryServerLastChunkIndex();
          const resumeFromChunk = serverLastChunk + 1;
          resolve2(this.makeRequest(resumeFromChunk));
          return;
        }
        req.destroy();
        reject(new Error("Request timed out"));
      });
      req.on("response", async (res) => {
        if (res.statusCode && this.isRetryableStatusCode(res.statusCode)) {
          if (this.retryCount < this.maxRetries) {
            this.retryCount++;
            res.resume();
            res.destroy();
            req.destroy();
            const delayMs = this.calculateBackoffDelay();
            await this.delay(delayMs);
            const serverLastChunk = await this.queryServerLastChunkIndex();
            const resumeFromChunk = serverLastChunk + 1;
            resolve2(this.makeRequest(resumeFromChunk));
            return;
          }
          res.destroy();
          req.destroy();
          reject(new Error(`Max retries (${this.maxRetries}) exceeded for status code ${res.statusCode}`));
          return;
        }
        if (res.statusCode && (res.statusCode < 200 || res.statusCode >= 300)) {
          res.destroy();
          req.destroy();
          const error = new Error(`HTTP error! status: ${res.statusCode}`);
          reject(error);
          return;
        }
        this.retryCount = 0;
        res.on("end", () => {
          resolve2();
        });
        res.resume();
      });
      if (this.options.signal) {
        this.options.signal.addEventListener("abort", () => {
          req.destroy(new Error("Request aborted"));
        });
      }
      const processStream = /* @__PURE__ */ __name(async () => {
        try {
          let lastSentIndex = startFromChunk - 1;
          while (true) {
            while (lastSentIndex < this.highestBufferedIndex) {
              lastSentIndex++;
              const chunk = this.ringBuffer.find((c) => c.index === lastSentIndex);
              if (chunk) {
                const stringified = JSON.stringify(chunk.data) + "\n";
                req.write(stringified);
                this.currentChunkIndex = lastSentIndex + 1;
              }
            }
            if (this.streamComplete && lastSentIndex >= this.highestBufferedIndex) {
              req.end();
              break;
            }
            await this.delay(10);
          }
        } catch (error) {
          reject(error);
        }
      }, "processStream");
      processStream().catch((error) => {
        reject(error);
      });
    });
  }
  async initializeServerStream() {
    await this.makeRequest(0);
  }
  async wait() {
    await this.streamPromise;
    return {};
  }
  [Symbol.asyncIterator]() {
    return streamToAsyncIterator2(this.consumerStream);
  }
  buildUrl() {
    return `${this.options.baseUrl}/realtime/v1/streams/${this.options.runId}/${this.options.target ?? "self"}/${this.options.key}`;
  }
  isRetryableError(error) {
    if (!error)
      return false;
    const retryableErrors = [
      "ECONNRESET",
      // Connection reset by peer
      "ECONNREFUSED",
      // Connection refused
      "ETIMEDOUT",
      // Connection timed out
      "ENOTFOUND",
      // DNS lookup failed
      "EPIPE",
      // Broken pipe
      "EHOSTUNREACH",
      // Host unreachable
      "ENETUNREACH",
      // Network unreachable
      "socket hang up"
      // Socket hang up
    ];
    if (error.code && retryableErrors.includes(error.code)) {
      return true;
    }
    if (error.message && error.message.includes("socket hang up")) {
      return true;
    }
    return false;
  }
  isRetryableStatusCode(statusCode) {
    if (statusCode === 408)
      return true;
    if (statusCode === 429)
      return true;
    if (statusCode === 500)
      return true;
    if (statusCode === 502)
      return true;
    if (statusCode === 503)
      return true;
    if (statusCode === 504)
      return true;
    return false;
  }
  async delay(ms) {
    return new Promise((resolve2) => setTimeout(resolve2, ms));
  }
  calculateBackoffDelay() {
    const exponentialDelay = this.baseDelayMs * Math.pow(2, this.retryCount);
    const jitter = Math.random() * 1e3;
    return Math.min(exponentialDelay + jitter, this.maxDelayMs);
  }
  addToRingBuffer(index, data) {
    const chunk = { index, data };
    if (this.ringBuffer.length < this.maxBufferSize) {
      this.ringBuffer.push(chunk);
    } else {
      const bufferIndex = index % this.maxBufferSize;
      this.ringBuffer[bufferIndex] = chunk;
      this.bufferStartIndex = Math.max(this.bufferStartIndex, index - this.maxBufferSize + 1);
    }
  }
  getChunksFromBuffer(startIndex) {
    const result = [];
    for (const chunk of this.ringBuffer) {
      if (chunk.index >= startIndex) {
        result.push(chunk);
      }
    }
    result.sort((a, b) => a.index - b.index);
    return result;
  }
  async queryServerLastChunkIndex(attempt = 0) {
    return new Promise((resolve2, reject) => {
      const url = new URL2(this.buildUrl());
      const maxHeadRetries = 3;
      const requestFn = url.protocol === "https:" ? httpsRequest : httpRequest;
      const req = requestFn({
        method: "HEAD",
        hostname: url.hostname,
        port: url.port || (url.protocol === "https:" ? 443 : 80),
        path: url.pathname + url.search,
        headers: {
          ...this.options.headers,
          "X-Client-Id": this.clientId,
          "X-Stream-Version": this.options.version ?? "v1"
        },
        timeout: 5e3
        // 5 second timeout for HEAD request
      });
      req.on("error", async (error) => {
        if (this.isRetryableError(error) && attempt < maxHeadRetries) {
          req.destroy();
          await this.delay(1e3 * (attempt + 1));
          const result = await this.queryServerLastChunkIndex(attempt + 1);
          resolve2(result);
          return;
        }
        req.destroy();
        resolve2(-1);
      });
      req.on("timeout", async () => {
        req.destroy();
        if (attempt < maxHeadRetries) {
          await this.delay(1e3 * (attempt + 1));
          const result = await this.queryServerLastChunkIndex(attempt + 1);
          resolve2(result);
          return;
        }
        resolve2(-1);
      });
      req.on("response", async (res) => {
        if (res.statusCode && this.isRetryableStatusCode(res.statusCode)) {
          if (attempt < maxHeadRetries) {
            res.resume();
            res.destroy();
            req.destroy();
            await this.delay(1e3 * (attempt + 1));
            const result = await this.queryServerLastChunkIndex(attempt + 1);
            resolve2(result);
            return;
          }
          res.destroy();
          req.destroy();
          resolve2(-1);
          return;
        }
        if (res.statusCode && (res.statusCode < 200 || res.statusCode >= 300)) {
          res.destroy();
          req.destroy();
          resolve2(-1);
          return;
        }
        const lastChunkHeader = res.headers["x-last-chunk-index"];
        if (lastChunkHeader) {
          const lastChunkIndex = parseInt(Array.isArray(lastChunkHeader) ? lastChunkHeader[0] ?? "0" : lastChunkHeader ?? "0", 10);
          resolve2(lastChunkIndex);
        } else {
          resolve2(-1);
        }
        res.resume();
      });
      req.end();
    });
  }
};
async function* streamToAsyncIterator2(stream) {
  const reader = stream.getReader();
  try {
    while (true) {
      const { done, value: value2 } = await reader.read();
      if (done)
        return;
      yield value2;
    }
  } finally {
    safeReleaseLock2(reader);
  }
}
__name(streamToAsyncIterator2, "streamToAsyncIterator");
function safeReleaseLock2(reader) {
  try {
    reader.releaseLock();
  } catch (_error) {
  }
}
__name(safeReleaseLock2, "safeReleaseLock");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/realtimeStreams/streamInstance.js
var StreamInstance = class {
  static {
    __name(this, "StreamInstance");
  }
  options;
  streamPromise;
  constructor(options) {
    this.options = options;
    this.streamPromise = this.initializeWriter();
  }
  async initializeWriter() {
    const createStreamFn = this.options.createStream ?? (() => this.options.apiClient.createStream(this.options.runId, "self", this.options.key, this.options?.requestOptions));
    const { version, headers } = await createStreamFn();
    const parsedResponse = parseCreateStreamResponse(version, headers);
    const streamWriter = parsedResponse.version === "v1" ? new StreamsWriterV1({
      key: this.options.key,
      runId: this.options.runId,
      source: this.options.source,
      baseUrl: this.options.baseUrl,
      headers: this.options.apiClient.getHeaders(),
      signal: this.options.signal,
      version,
      target: "self"
    }) : new StreamsWriterV2({
      basin: parsedResponse.basin,
      stream: parsedResponse.streamName ?? this.options.key,
      accessToken: parsedResponse.accessToken,
      endpoint: parsedResponse.endpoint,
      source: this.options.source,
      signal: this.options.signal,
      debug: this.options.debug,
      flushIntervalMs: parsedResponse.flushIntervalMs,
      maxRetries: parsedResponse.maxRetries
    });
    return streamWriter;
  }
  async wait() {
    const writer = await this.streamPromise;
    return writer.wait();
  }
  get stream() {
    const self = this;
    return new ReadableStream({
      async start(controller) {
        const streamWriter = await self.streamPromise;
        const iterator = streamWriter[Symbol.asyncIterator]();
        while (true) {
          if (self.options.signal?.aborted) {
            controller.close();
            break;
          }
          const { done, value: value2 } = await iterator.next();
          if (done) {
            controller.close();
            break;
          }
          controller.enqueue(value2);
        }
      }
    });
  }
};
function parseCreateStreamResponse(version, headers) {
  if (version === "v1") {
    return { version: "v1" };
  }
  const accessToken = headers?.["x-s2-access-token"];
  const basin = headers?.["x-s2-basin"];
  if (!accessToken || !basin) {
    return { version: "v1" };
  }
  const endpoint = headers?.["x-s2-endpoint"];
  const flushIntervalMs = headers?.["x-s2-flush-interval-ms"];
  const maxRetries = headers?.["x-s2-max-retries"];
  const streamName = headers?.["x-s2-stream-name"];
  return {
    version: "v2",
    accessToken,
    basin,
    endpoint,
    flushIntervalMs: flushIntervalMs ? parseInt(flushIntervalMs) : void 0,
    maxRetries: maxRetries ? parseInt(maxRetries) : void 0,
    streamName
  };
}
__name(parseCreateStreamResponse, "parseCreateStreamResponse");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/realtimeStreams/manager.js
var StandardRealtimeStreamsManager = class {
  static {
    __name(this, "StandardRealtimeStreamsManager");
  }
  apiClient;
  baseUrl;
  debug;
  constructor(apiClient, baseUrl, debug4 = false) {
    this.apiClient = apiClient;
    this.baseUrl = baseUrl;
    this.debug = debug4;
  }
  // Track active streams - using a Set allows multiple streams for the same key to coexist
  activeStreams = /* @__PURE__ */ new Set();
  // Cache of in-flight / resolved `createStream` responses, keyed by
  // `${runId}:${key}`. S2 v2 access tokens are scoped to the org basin
  // (default 1-day TTL server-side) so reusing them across repeated
  // `pipe()` calls for the same `(runId, key)` is safe, and avoids the
  // per-call PUT that pushes `streamId` onto `TaskRun.realtimeStreams`,
  // which under chat-agent-style hot-loop writers caused row-lock
  // contention on the writer DB.
  createStreamCache = /* @__PURE__ */ new Map();
  reset() {
    this.activeStreams.clear();
    this.createStreamCache.clear();
  }
  getCachedCreateStream(runId, key, requestOptions) {
    const cacheKey = `${runId}:${key}`;
    const cached = this.createStreamCache.get(cacheKey);
    if (cached) {
      return cached;
    }
    const promise = this.apiClient.createStream(runId, "self", key, requestOptions);
    this.createStreamCache.set(cacheKey, promise);
    promise.catch((err2) => {
      if (this.createStreamCache.get(cacheKey) === promise) {
        this.createStreamCache.delete(cacheKey);
      }
    });
    return promise;
  }
  /**
   * Reactive invalidation: a writer's `wait()` rejecting can mean the
   * cached S2 credentials have gone stale (expired token, revoked
   * access, basin retired), so evict the cached `createStream` response
   * for `(runId, key)` and let the next `pipe()` re-PUT to mint fresh
   * credentials. Compare by identity so a fresh promise installed by a
   * concurrent caller isn't accidentally cleared.
   */
  evictCreateStreamIfStale(runId, key, expected) {
    const cacheKey = `${runId}:${key}`;
    if (this.createStreamCache.get(cacheKey) === expected) {
      this.createStreamCache.delete(cacheKey);
    }
  }
  pipe(key, source, options) {
    const readableStreamSource = ensureReadableStream(source);
    const runId = getRunIdForOptions(options);
    if (!runId) {
      throw new Error("Could not determine the target run ID for the realtime stream. Please specify a target run ID using the `target` option.");
    }
    const abortController = new AbortController();
    const combinedSignal = options?.signal ? AbortSignal.any?.([options.signal, abortController.signal]) ?? abortController.signal : abortController.signal;
    const activeCreatePromise = this.getCachedCreateStream(runId, key, options?.requestOptions);
    const streamInstance = new StreamInstance({
      apiClient: this.apiClient,
      baseUrl: this.baseUrl,
      runId,
      key,
      source: readableStreamSource,
      signal: combinedSignal,
      requestOptions: options?.requestOptions,
      target: options?.target,
      debug: this.debug,
      createStream: /* @__PURE__ */ __name(() => activeCreatePromise, "createStream")
    });
    const streamInfo = { wait: /* @__PURE__ */ __name(() => streamInstance.wait(), "wait"), abortController };
    this.activeStreams.add(streamInfo);
    streamInstance.wait().then(() => {
      this.activeStreams.delete(streamInfo);
    }, (err2) => {
      this.evictCreateStreamIfStale(runId, key, activeCreatePromise);
      this.activeStreams.delete(streamInfo);
    });
    return {
      wait: /* @__PURE__ */ __name(() => streamInstance.wait(), "wait"),
      stream: streamInstance.stream
    };
  }
  async append(key, part, options) {
    const runId = getRunIdForOptions(options);
    if (!runId) {
      throw new Error("Could not determine the target run ID for the realtime stream. Please specify a target run ID using the `target` option.");
    }
    const result = await this.apiClient.appendToStream(runId, "self", key, part, options?.requestOptions);
    if (!result.ok) {
      throw new Error(`Failed to append to stream: ${result.message ?? "Unknown error"}`);
    }
  }
  hasActiveStreams() {
    return this.activeStreams.size > 0;
  }
  // Waits for all the streams to finish
  async waitForAllStreams(timeout = 6e4) {
    if (this.activeStreams.size === 0) {
      return;
    }
    const promises = Array.from(this.activeStreams).map((stream) => stream.wait());
    const TIMEOUT_SENTINEL = Symbol("timeout");
    const timeoutPromise = new Promise((resolve2) => setTimeout(() => resolve2(TIMEOUT_SENTINEL), timeout));
    const result = await Promise.race([Promise.all(promises), timeoutPromise]);
    if (result === TIMEOUT_SENTINEL) {
      const abortedCount = this.activeStreams.size;
      for (const streamInfo of this.activeStreams) {
        streamInfo.abortController.abort();
        this.activeStreams.delete(streamInfo);
      }
      throw new Error(`Timeout waiting for streams to finish after ${timeout}ms. Aborted ${abortedCount} active stream(s).`);
    }
  }
};
function getRunIdForOptions(options) {
  if (options?.target) {
    if (options.target === "parent") {
      return taskContext.ctx?.run?.parentTaskRunId ?? taskContext.ctx?.run?.id;
    }
    if (options.target === "root") {
      return taskContext.ctx?.run?.rootTaskRunId ?? taskContext.ctx?.run?.id;
    }
    if (options.target === "self") {
      return taskContext.ctx?.run?.id;
    }
    return options.target;
  }
  return taskContext.ctx?.run?.id;
}
__name(getRunIdForOptions, "getRunIdForOptions");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/inputStreams/manager.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/utils/reconnectBackoff.js
init_esm();
function computeReconnectDelayMs(attempt) {
  const safeAttempt = Math.max(0, Math.floor(attempt));
  const base = Math.min(1e3 * 2 ** safeAttempt, 3e4);
  return base + Math.random() * 1e3;
}
__name(computeReconnectDelayMs, "computeReconnectDelayMs");

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/inputStreams/manager.js
var StandardInputStreamManager = class {
  static {
    __name(this, "StandardInputStreamManager");
  }
  apiClient;
  baseUrl;
  debug;
  handlers = /* @__PURE__ */ new Map();
  onceWaiters = /* @__PURE__ */ new Map();
  buffer = /* @__PURE__ */ new Map();
  tails = /* @__PURE__ */ new Map();
  seqNums = /* @__PURE__ */ new Map();
  currentRunId = null;
  streamsVersion;
  // Reconnect attempt counter per streamId. Drives the exponential
  // backoff applied by `#ensureStreamTailConnected`'s `.finally` so a
  // persistent backend failure (auth rejection, 5xx, DNS, etc.) doesn't
  // reconnect in a tight loop. Reset to 0 by `#dispatch` whenever a
  // record flows through.
  reconnectAttempts = /* @__PURE__ */ new Map();
  // Stream IDs that were explicitly torn down by `disconnectStream`. The
  // tail's `.finally` reconnect path consults this set so a deliberate
  // teardown isn't immediately undone by the auto-reconnect when
  // handlers or once-waiters are still registered. Cleared on the next
  // explicit `on()` / `once()` (those are the only legitimate reasons to
  // bring the tail back up).
  explicitlyDisconnected = /* @__PURE__ */ new Set();
  constructor(apiClient, baseUrl, debug4 = false) {
    this.apiClient = apiClient;
    this.baseUrl = baseUrl;
    this.debug = debug4;
  }
  lastSeqNum(streamId) {
    return this.seqNums.get(streamId);
  }
  setLastSeqNum(streamId, seqNum) {
    const current = this.seqNums.get(streamId);
    if (current === void 0 || seqNum > current) {
      this.seqNums.set(streamId, seqNum);
    }
  }
  shiftBuffer(streamId) {
    const buffered = this.buffer.get(streamId);
    if (buffered && buffered.length > 0) {
      buffered.shift();
      if (buffered.length === 0) {
        this.buffer.delete(streamId);
      }
      return true;
    }
    return false;
  }
  setRunId(runId, streamsVersion) {
    this.currentRunId = runId;
    this.streamsVersion = streamsVersion;
  }
  on(streamId, handler) {
    this.#requireV2Streams();
    this.explicitlyDisconnected.delete(streamId);
    let handlerSet = this.handlers.get(streamId);
    if (!handlerSet) {
      handlerSet = /* @__PURE__ */ new Set();
      this.handlers.set(streamId, handlerSet);
    }
    handlerSet.add(handler);
    this.#ensureStreamTailConnected(streamId);
    const buffered = this.buffer.get(streamId);
    if (buffered && buffered.length > 0) {
      for (const data of buffered) {
        this.#invokeHandler(handler, data);
      }
      this.buffer.delete(streamId);
    }
    return {
      off: /* @__PURE__ */ __name(() => {
        handlerSet?.delete(handler);
        if (handlerSet?.size === 0) {
          this.handlers.delete(streamId);
        }
      }, "off")
    };
  }
  once(streamId, options) {
    this.#requireV2Streams();
    this.explicitlyDisconnected.delete(streamId);
    this.#ensureStreamTailConnected(streamId);
    const buffered = this.buffer.get(streamId);
    if (buffered && buffered.length > 0) {
      const data = buffered.shift();
      if (buffered.length === 0) {
        this.buffer.delete(streamId);
      }
      return new InputStreamOncePromise((resolve2) => {
        resolve2({ ok: true, output: data });
      });
    }
    return new InputStreamOncePromise((resolve2, reject) => {
      const waiter = { resolve: resolve2, reject };
      if (options?.signal) {
        if (options.signal.aborted) {
          reject(new Error("Aborted"));
          return;
        }
        const abortHandler = /* @__PURE__ */ __name(() => {
          if (waiter.timeoutHandle) {
            clearTimeout(waiter.timeoutHandle);
          }
          this.#removeOnceWaiter(streamId, waiter);
          reject(new Error("Aborted"));
        }, "abortHandler");
        waiter.signal = options.signal;
        waiter.abortHandler = abortHandler;
        options.signal.addEventListener("abort", abortHandler, { once: true });
      }
      if (options?.timeoutMs) {
        waiter.timeoutHandle = setTimeout(() => {
          this.#removeOnceWaiter(streamId, waiter);
          resolve2({
            ok: false,
            error: new InputStreamTimeoutError(streamId, options.timeoutMs)
          });
        }, options.timeoutMs);
      }
      let waiters = this.onceWaiters.get(streamId);
      if (!waiters) {
        waiters = [];
        this.onceWaiters.set(streamId, waiters);
      }
      waiters.push(waiter);
    });
  }
  peek(streamId) {
    const buffered = this.buffer.get(streamId);
    if (buffered && buffered.length > 0) {
      return buffered[0];
    }
    return void 0;
  }
  clearHandlers() {
    this.handlers.clear();
    for (const [streamId, tail] of this.tails) {
      const hasWaiters = this.onceWaiters.has(streamId) && this.onceWaiters.get(streamId).length > 0;
      if (!hasWaiters) {
        tail.abortController.abort();
        this.tails.delete(streamId);
      }
    }
  }
  disconnectStream(streamId) {
    this.explicitlyDisconnected.add(streamId);
    const tail = this.tails.get(streamId);
    if (tail) {
      tail.abortController.abort();
      this.tails.delete(streamId);
    }
    this.buffer.delete(streamId);
    this.reconnectAttempts.delete(streamId);
  }
  connectTail(runId, _fromSeq) {
  }
  /**
   * Tear down all active tails. Does NOT clear handlers or `onceWaiters`,
   * so any registered listener will trigger an auto-reconnect (with
   * backoff) the moment it sees no live tail — by design, so a transient
   * network blip recovers without the caller re-subscribing. Use
   * `reset()` if you want a full clean state with no resurrection, or
   * `disconnectStream(streamId)` for a single stream that should stay
   * down until a fresh `on()` / `once()` attaches.
   */
  disconnect() {
    for (const [, tail] of this.tails) {
      tail.abortController.abort();
    }
    this.tails.clear();
  }
  reset() {
    this.disconnect();
    this.currentRunId = null;
    this.streamsVersion = void 0;
    this.seqNums.clear();
    this.handlers.clear();
    this.reconnectAttempts.clear();
    this.explicitlyDisconnected.clear();
    for (const [, waiters] of this.onceWaiters) {
      for (const waiter of waiters) {
        if (waiter.timeoutHandle) {
          clearTimeout(waiter.timeoutHandle);
        }
        if (waiter.signal && waiter.abortHandler) {
          waiter.signal.removeEventListener("abort", waiter.abortHandler);
        }
        waiter.reject(new Error("Input stream manager reset"));
      }
    }
    this.onceWaiters.clear();
    this.buffer.clear();
  }
  #requireV2Streams() {
    if (this.currentRunId && this.streamsVersion !== "v2") {
      throw new Error("Input streams require v2 realtime streams. Enable them with: { future: { v2RealtimeStreams: true } }");
    }
  }
  #ensureStreamTailConnected(streamId) {
    if (!this.tails.has(streamId) && this.currentRunId) {
      const abortController = new AbortController();
      const promise = this.#runTail(this.currentRunId, streamId, abortController.signal).catch((error) => {
        if (this.debug) {
          console.error(`[InputStreamManager] Tail error for "${streamId}":`, error);
        }
      }).finally(() => {
        this.tails.delete(streamId);
        if (this.explicitlyDisconnected.has(streamId)) {
          return;
        }
        const hasHandlers = this.handlers.has(streamId) && this.handlers.get(streamId).size > 0;
        const hasWaiters = this.onceWaiters.has(streamId) && this.onceWaiters.get(streamId).length > 0;
        if (hasHandlers || hasWaiters) {
          const attempt = this.reconnectAttempts.get(streamId) ?? 0;
          this.reconnectAttempts.set(streamId, attempt + 1);
          const delayMs = computeReconnectDelayMs(attempt);
          setTimeout(() => {
            if (this.explicitlyDisconnected.has(streamId))
              return;
            if (this.tails.has(streamId))
              return;
            const stillHasHandlers = this.handlers.has(streamId) && this.handlers.get(streamId).size > 0;
            const stillHasWaiters = this.onceWaiters.has(streamId) && this.onceWaiters.get(streamId).length > 0;
            if (!stillHasHandlers && !stillHasWaiters)
              return;
            this.#ensureStreamTailConnected(streamId);
          }, delayMs);
        }
      });
      this.tails.set(streamId, { abortController, promise });
    }
  }
  async #runTail(runId, streamId, signal) {
    try {
      const lastSeq = this.seqNums.get(streamId);
      const stream = await this.apiClient.fetchStream(runId, `input/${streamId}`, {
        signal,
        baseUrl: this.baseUrl,
        // Max allowed by the SSE endpoint is 600s; the tail will reconnect on close
        timeoutInSeconds: 600,
        // Resume from last seen sequence number to avoid replaying history on reconnect
        lastEventId: lastSeq !== void 0 ? String(lastSeq) : void 0,
        onPart: /* @__PURE__ */ __name((part) => {
          const seqNum = parseInt(part.id, 10);
          if (Number.isFinite(seqNum)) {
            this.seqNums.set(streamId, seqNum);
          }
        }, "onPart"),
        onComplete: /* @__PURE__ */ __name(() => {
          if (this.debug) {
            console.log(`[InputStreamManager] Tail stream completed for "${streamId}"`);
          }
        }, "onComplete"),
        onError: /* @__PURE__ */ __name((error) => {
          if (this.debug) {
            console.error(`[InputStreamManager] Tail stream error for "${streamId}":`, error);
          }
        }, "onError")
      });
      for await (const record of stream) {
        if (signal.aborted)
          break;
        let data;
        if (typeof record === "string") {
          try {
            data = JSON.parse(record);
          } catch {
            data = record;
          }
        } else {
          data = record;
        }
        this.#dispatch(streamId, data);
      }
    } catch (error) {
      if (error instanceof Error && error.name === "AbortError") {
        return;
      }
      throw error;
    }
  }
  #dispatch(streamId, data) {
    this.reconnectAttempts.delete(streamId);
    const waiters = this.onceWaiters.get(streamId);
    if (waiters && waiters.length > 0) {
      const waiter = waiters.shift();
      if (waiters.length === 0) {
        this.onceWaiters.delete(streamId);
      }
      if (waiter.timeoutHandle) {
        clearTimeout(waiter.timeoutHandle);
      }
      if (waiter.signal && waiter.abortHandler) {
        waiter.signal.removeEventListener("abort", waiter.abortHandler);
      }
      waiter.resolve({ ok: true, output: data });
      this.#invokeHandlers(streamId, data);
      return;
    }
    const handlers = this.handlers.get(streamId);
    if (handlers && handlers.size > 0) {
      this.#invokeHandlers(streamId, data);
      return;
    }
    let buffered = this.buffer.get(streamId);
    if (!buffered) {
      buffered = [];
      this.buffer.set(streamId, buffered);
    }
    buffered.push(data);
  }
  #invokeHandlers(streamId, data) {
    const handlers = this.handlers.get(streamId);
    if (!handlers)
      return;
    for (const handler of handlers) {
      this.#invokeHandler(handler, data);
    }
  }
  #invokeHandler(handler, data) {
    try {
      const result = handler(data);
      if (result && typeof result === "object" && "catch" in result) {
        result.catch((error) => {
          if (this.debug) {
            console.error("[InputStreamManager] Handler error:", error);
          }
        });
      }
    } catch (error) {
      if (this.debug) {
        console.error("[InputStreamManager] Handler error:", error);
      }
    }
  }
  #removeOnceWaiter(streamId, waiter) {
    if (waiter.signal && waiter.abortHandler) {
      waiter.signal.removeEventListener("abort", waiter.abortHandler);
    }
    const waiters = this.onceWaiters.get(streamId);
    if (!waiters)
      return;
    const index = waiters.indexOf(waiter);
    if (index !== -1) {
      waiters.splice(index, 1);
    }
    if (waiters.length === 0) {
      this.onceWaiters.delete(streamId);
    }
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/sessionStreams/manager.js
init_esm();
function keyFor(sessionId, io) {
  return `${sessionId}:${io}`;
}
__name(keyFor, "keyFor");
var StandardSessionStreamManager = class {
  static {
    __name(this, "StandardSessionStreamManager");
  }
  apiClient;
  baseUrl;
  debug;
  handlers = /* @__PURE__ */ new Map();
  onceWaiters = /* @__PURE__ */ new Map();
  buffer = /* @__PURE__ */ new Map();
  // Parallel to `buffer`: the SSE seq_num of each buffered record. Same
  // length and order as `buffer[key]`. Used so that when `once()` shifts
  // a buffered record into a waiter, the cursor (`lastDispatchedSeqNums`)
  // can advance to that record's seq. Kept as a separate map so the
  // existing `peek()` shape (returns `unknown`) stays unchanged.
  //
  // Entries are `number | undefined` so the array stays length-locked
  // with `buffer` even if a record arrives without a parseable seq —
  // shifting `undefined` is just a no-op for the cursor advance, but
  // the slot still gets consumed. Drifting lengths would map seq_nums
  // to the wrong records on subsequent shifts.
  bufferSeqNums = /* @__PURE__ */ new Map();
  tails = /* @__PURE__ */ new Map();
  // Per-stream lower-bound timestamp filter. When set, records whose
  // SSE timestamp is <= the bound are dropped before dispatch — used by
  // chat.agent on OOM-retry boot to skip session.in records belonging
  // to turns that already completed on the prior attempt. The filter
  // is consulted in `runTail`'s `onPart` so the buffer never sees the
  // dropped records.
  minTimestamps = /* @__PURE__ */ new Map();
  // Keys that were explicitly torn down by `disconnectStream`. The tail's
  // `.finally` reconnect path checks this so a long-lived persistent handler
  // (e.g. `chat.agent`'s run-level `stopInput.on(...)`) doesn't silently
  // resurrect the tail mid-`session.in.wait()` and re-deliver the record
  // that's already being delivered out-of-band via the waitpoint.
  explicitlyDisconnected = /* @__PURE__ */ new Set();
  seqNums = /* @__PURE__ */ new Map();
  // Highest seq_num that has been *consumed* (delivered to a once()
  // waiter or shifted off the buffer into a once() caller) on a channel.
  // Distinct from `seqNums`, which advances whenever any record is
  // received from SSE — even ones still sitting in the local buffer.
  // The committed-consume cursor is what gets persisted on the
  // turn-complete control record's `session-in-event-id` header so the
  // next worker boot can resume `.in` from this point without
  // re-delivering already-handled user messages.
  lastDispatchedSeqNums = /* @__PURE__ */ new Map();
  // Reconnect attempt counter per key. Drives the exponential backoff
  // applied by `#ensureTailConnected`'s `.finally` so a persistent
  // backend failure (auth rejection, 5xx, DNS, etc.) doesn't reconnect
  // in a tight loop. Reset to 0 by `#dispatch` whenever a real record
  // flows through — any successful traffic is taken as a healthy
  // connection.
  reconnectAttempts = /* @__PURE__ */ new Map();
  constructor(apiClient, baseUrl, debug4 = false) {
    this.apiClient = apiClient;
    this.baseUrl = baseUrl;
    this.debug = debug4;
  }
  on(sessionId, io, handler) {
    const key = keyFor(sessionId, io);
    let handlerSet = this.handlers.get(key);
    if (!handlerSet) {
      handlerSet = /* @__PURE__ */ new Set();
      this.handlers.set(key, handlerSet);
    }
    handlerSet.add(handler);
    this.explicitlyDisconnected.delete(key);
    this.#ensureTailConnected(sessionId, io);
    const buffered = this.buffer.get(key);
    if (buffered && buffered.length > 0) {
      const seqList = this.bufferSeqNums.get(key) ?? [];
      const keptRecords = [];
      const keptSeqNums = [];
      for (let i = 0; i < buffered.length; i++) {
        const consumed = this.#invokeHandler(handler, buffered[i]);
        if (consumed) {
          const s = seqList[i];
          if (s !== void 0)
            this.#advanceLastDispatched(key, s);
        } else {
          keptRecords.push(buffered[i]);
          keptSeqNums.push(seqList[i]);
        }
      }
      if (keptRecords.length > 0) {
        this.buffer.set(key, keptRecords);
        this.bufferSeqNums.set(key, keptSeqNums);
      } else {
        this.buffer.delete(key);
        this.bufferSeqNums.delete(key);
      }
    }
    return {
      off: /* @__PURE__ */ __name(() => {
        handlerSet?.delete(handler);
        if (handlerSet?.size === 0) {
          this.handlers.delete(key);
        }
      }, "off")
    };
  }
  once(sessionId, io, options) {
    const key = keyFor(sessionId, io);
    this.explicitlyDisconnected.delete(key);
    this.#ensureTailConnected(sessionId, io);
    const buffered = this.buffer.get(key);
    if (buffered && buffered.length > 0) {
      const data = buffered.shift();
      const seqList = this.bufferSeqNums.get(key);
      const shiftedSeqNum = seqList?.shift();
      if (buffered.length === 0) {
        this.buffer.delete(key);
        this.bufferSeqNums.delete(key);
      }
      if (shiftedSeqNum !== void 0) {
        this.#advanceLastDispatched(key, shiftedSeqNum);
      }
      return new InputStreamOncePromise((resolve2) => {
        resolve2({ ok: true, output: data });
      });
    }
    return new InputStreamOncePromise((resolve2, reject) => {
      const waiter = { resolve: resolve2, reject };
      if (options?.signal) {
        if (options.signal.aborted) {
          reject(new Error("Aborted"));
          return;
        }
        const abortHandler = /* @__PURE__ */ __name(() => {
          if (waiter.timeoutHandle)
            clearTimeout(waiter.timeoutHandle);
          this.#removeOnceWaiter(key, waiter);
          reject(new Error("Aborted"));
        }, "abortHandler");
        waiter.signal = options.signal;
        waiter.abortHandler = abortHandler;
        options.signal.addEventListener("abort", abortHandler, { once: true });
      }
      if (options?.timeoutMs) {
        waiter.timeoutHandle = setTimeout(() => {
          this.#removeOnceWaiter(key, waiter);
          resolve2({
            ok: false,
            error: new InputStreamTimeoutError(key, options.timeoutMs)
          });
        }, options.timeoutMs);
      }
      let waiters = this.onceWaiters.get(key);
      if (!waiters) {
        waiters = [];
        this.onceWaiters.set(key, waiters);
      }
      waiters.push(waiter);
    });
  }
  peek(sessionId, io) {
    const buffered = this.buffer.get(keyFor(sessionId, io));
    if (buffered && buffered.length > 0)
      return buffered[0];
    return void 0;
  }
  lastSeqNum(sessionId, io) {
    return this.seqNums.get(keyFor(sessionId, io));
  }
  setLastSeqNum(sessionId, io, seqNum) {
    const key = keyFor(sessionId, io);
    const current = this.seqNums.get(key);
    if (current === void 0 || seqNum > current) {
      this.seqNums.set(key, seqNum);
    }
  }
  lastDispatchedSeqNum(sessionId, io) {
    return this.lastDispatchedSeqNums.get(keyFor(sessionId, io));
  }
  setLastDispatchedSeqNum(sessionId, io, seqNum) {
    this.#advanceLastDispatched(keyFor(sessionId, io), seqNum);
  }
  #advanceLastDispatched(key, seqNum) {
    const current = this.lastDispatchedSeqNums.get(key);
    if (current === void 0 || seqNum > current) {
      this.lastDispatchedSeqNums.set(key, seqNum);
    }
  }
  setMinTimestamp(sessionId, io, minTimestamp) {
    const key = keyFor(sessionId, io);
    if (minTimestamp === void 0) {
      this.minTimestamps.delete(key);
    } else {
      this.minTimestamps.set(key, minTimestamp);
    }
  }
  shiftBuffer(sessionId, io) {
    const key = keyFor(sessionId, io);
    const buffered = this.buffer.get(key);
    if (buffered && buffered.length > 0) {
      buffered.shift();
      const seqList = this.bufferSeqNums.get(key);
      const shiftedSeqNum = seqList?.shift();
      if (buffered.length === 0) {
        this.buffer.delete(key);
        this.bufferSeqNums.delete(key);
      }
      if (shiftedSeqNum !== void 0) {
        this.#advanceLastDispatched(key, shiftedSeqNum);
      }
      return true;
    }
    return false;
  }
  disconnectStream(sessionId, io) {
    const key = keyFor(sessionId, io);
    const tail = this.tails.get(key);
    const _bufferedSize = this.buffer.get(key)?.length ?? 0;
    this.explicitlyDisconnected.add(key);
    if (tail) {
      tail.abortController.abort();
      this.tails.delete(key);
    }
    this.buffer.delete(key);
    this.bufferSeqNums.delete(key);
    this.reconnectAttempts.delete(key);
  }
  clearHandlers() {
    this.handlers.clear();
    for (const [key, tail] of this.tails) {
      const hasWaiters = this.onceWaiters.has(key) && this.onceWaiters.get(key).length > 0;
      if (!hasWaiters) {
        tail.abortController.abort();
        this.tails.delete(key);
      }
    }
  }
  /**
   * Tear down all active tails. Does NOT clear handlers or `onceWaiters`,
   * so any registered listener will trigger an auto-reconnect (with
   * backoff) the moment it sees no live tail — by design, so a transient
   * network blip recovers without the caller re-subscribing. Use
   * `reset()` if you want a full clean state with no resurrection, or
   * `disconnectStream(sessionId, io)` for a single channel that should
   * stay down until a fresh `on()` / `once()` attaches.
   */
  disconnect() {
    for (const [, tail] of this.tails) {
      tail.abortController.abort();
    }
    this.tails.clear();
  }
  reset() {
    this.disconnect();
    this.seqNums.clear();
    this.lastDispatchedSeqNums.clear();
    this.minTimestamps.clear();
    this.handlers.clear();
    this.reconnectAttempts.clear();
    for (const [, waiters] of this.onceWaiters) {
      for (const waiter of waiters) {
        if (waiter.timeoutHandle)
          clearTimeout(waiter.timeoutHandle);
        if (waiter.signal && waiter.abortHandler) {
          waiter.signal.removeEventListener("abort", waiter.abortHandler);
        }
        waiter.reject(new Error("Session stream manager reset"));
      }
    }
    this.onceWaiters.clear();
    this.buffer.clear();
    this.bufferSeqNums.clear();
  }
  #ensureTailConnected(sessionId, io) {
    const key = keyFor(sessionId, io);
    if (this.tails.has(key))
      return;
    const abortController = new AbortController();
    const promise = this.#runTail(sessionId, io, abortController.signal).catch((error) => {
      if (this.debug) {
        console.error(`[SessionStreamManager] Tail error for "${key}":`, error);
      }
    }).finally(() => {
      this.tails.delete(key);
      if (this.explicitlyDisconnected.has(key)) {
        return;
      }
      const hasHandlers = this.handlers.has(key) && this.handlers.get(key).size > 0;
      const hasWaiters = this.onceWaiters.has(key) && this.onceWaiters.get(key).length > 0;
      if (hasHandlers || hasWaiters) {
        const attempt = this.reconnectAttempts.get(key) ?? 0;
        this.reconnectAttempts.set(key, attempt + 1);
        const delayMs = computeReconnectDelayMs(attempt);
        setTimeout(() => {
          if (this.tails.has(key))
            return;
          if (this.explicitlyDisconnected.has(key))
            return;
          const stillHasHandlers = this.handlers.has(key) && this.handlers.get(key).size > 0;
          const stillHasWaiters = this.onceWaiters.has(key) && this.onceWaiters.get(key).length > 0;
          if (!stillHasHandlers && !stillHasWaiters)
            return;
          this.#ensureTailConnected(sessionId, io);
        }, delayMs);
      }
    });
    this.tails.set(key, { abortController, promise });
  }
  async #runTail(sessionId, io, signal) {
    const key = keyFor(sessionId, io);
    try {
      const lastSeq = this.seqNums.get(key);
      const stream = await this.apiClient.subscribeToSessionStream(sessionId, io, {
        signal,
        baseUrl: this.baseUrl,
        timeoutInSeconds: 600,
        lastEventId: lastSeq !== void 0 ? String(lastSeq) : void 0,
        onPart: /* @__PURE__ */ __name((part) => {
          if (signal.aborted)
            return;
          const seqNum = parseInt(part.id, 10);
          if (Number.isFinite(seqNum)) {
            this.seqNums.set(key, seqNum);
          }
          if (controlSubtype(part.headers)) {
            return;
          }
          const minTs = this.minTimestamps.get(key);
          if (minTs !== void 0 && part.timestamp <= minTs) {
            return;
          }
          let data = part.chunk;
          if (typeof data === "string") {
            try {
              data = JSON.parse(data);
            } catch {
            }
          }
          this.#dispatch(key, data, Number.isFinite(seqNum) ? seqNum : void 0);
        }, "onPart"),
        onComplete: /* @__PURE__ */ __name(() => {
          if (this.debug) {
            console.log(`[SessionStreamManager] Tail completed for "${key}"`);
          }
        }, "onComplete"),
        onError: /* @__PURE__ */ __name((error) => {
          if (this.debug) {
            console.error(`[SessionStreamManager] Tail error for "${key}":`, error);
          }
        }, "onError")
      });
      for await (const _record of stream) {
        if (signal.aborted)
          break;
      }
    } catch (error) {
      if (error instanceof Error && error.name === "AbortError")
        return;
      throw error;
    }
  }
  #dispatch(key, data, seqNum) {
    this.reconnectAttempts.delete(key);
    const waiters = this.onceWaiters.get(key);
    if (waiters && waiters.length > 0) {
      const waiter = waiters.shift();
      if (waiters.length === 0)
        this.onceWaiters.delete(key);
      if (waiter.timeoutHandle)
        clearTimeout(waiter.timeoutHandle);
      if (waiter.signal && waiter.abortHandler) {
        waiter.signal.removeEventListener("abort", waiter.abortHandler);
      }
      if (seqNum !== void 0) {
        this.#advanceLastDispatched(key, seqNum);
      }
      waiter.resolve({ ok: true, output: data });
      this.#invokeHandlers(key, data);
      return;
    }
    const consumed = this.#invokeHandlers(key, data);
    if (consumed) {
      if (seqNum !== void 0) {
        this.#advanceLastDispatched(key, seqNum);
      }
      return;
    }
    let buffered = this.buffer.get(key);
    if (!buffered) {
      buffered = [];
      this.buffer.set(key, buffered);
    }
    buffered.push(data);
    let bufferedSeqs = this.bufferSeqNums.get(key);
    if (!bufferedSeqs) {
      bufferedSeqs = [];
      this.bufferSeqNums.set(key, bufferedSeqs);
    }
    bufferedSeqs.push(seqNum);
  }
  /** Returns true when any handler consumed the record. All handlers are invoked regardless. */
  #invokeHandlers(key, data) {
    const handlers = this.handlers.get(key);
    if (!handlers)
      return false;
    let consumed = false;
    for (const handler of handlers) {
      if (this.#invokeHandler(handler, data)) {
        consumed = true;
      }
    }
    return consumed;
  }
  /** Returns true when the handler synchronously consumed the record (returned `true`). */
  #invokeHandler(handler, data) {
    try {
      const result = handler(data);
      if (result === true)
        return true;
      if (result && typeof result === "object" && "catch" in result) {
        result.catch((error) => {
          if (this.debug) {
            console.error("[SessionStreamManager] Handler error:", error);
          }
        });
      }
    } catch (error) {
      if (this.debug) {
        console.error("[SessionStreamManager] Handler error:", error);
      }
    }
    return false;
  }
  #removeOnceWaiter(key, waiter) {
    if (waiter.signal && waiter.abortHandler) {
      waiter.signal.removeEventListener("abort", waiter.abortHandler);
    }
    const waiters = this.onceWaiters.get(key);
    if (!waiters)
      return;
    const index = waiters.indexOf(waiter);
    if (index !== -1)
      waiters.splice(index, 1);
    if (waiters.length === 0)
      this.onceWaiters.delete(key);
  }
};

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/workers/index.js
init_esm();

// ../../node_modules/.bun/@trigger.dev+core@4.5.12+2b91fc17bf64bdfd/node_modules/@trigger.dev/core/dist/esm/v3/zodMessageHandler.js
init_esm();
var ZodSchemaParsedError = class extends Error {
  static {
    __name(this, "ZodSchemaParsedError");
  }
  error;
  payload;
  constructor(error, payload) {
    super(error.message);
    this.error = error;
    this.payload = payload;
  }
};
var ZodMessageSchema = external_exports.object({
  version: external_exports.literal("v1").default("v1"),
  type: external_exports.string(),
  payload: external_exports.unknown()
});
async function sendMessageInCatalog(catalog, type, payload, sender) {
  const schema = catalog[type];
  if (!schema) {
    throw new Error(`Unknown message type: ${type}`);
  }
  const parsedPayload = schema.safeParse(payload);
  if (!parsedPayload.success) {
    throw new ZodSchemaParsedError(parsedPayload.error, payload);
  }
  await sender({ type, payload, version: "v1" });
}
__name(sendMessageInCatalog, "sendMessageInCatalog");

// ../../node_modules/.bun/trigger.dev@4.5.12+9d0eee97fc866dea/node_modules/trigger.dev/dist/esm/utilities/normalizeImportPath.js
init_esm();
import { pathToFileURL } from "url";
function normalizeImportPath(importPath) {
  return pathToFileURL(importPath).href;
}
__name(normalizeImportPath, "normalizeImportPath");

export {
  TracingSDK,
  TaskExecutor,
  PreciseWallClock,
  ConsoleInterceptor,
  NO_FILE_CONTEXT,
  StandardResourceCatalog,
  DevUsageManager,
  UsageTimeoutManager,
  StandardMetadataManager,
  StandardWaitUntilManager,
  SharedRuntimeManager,
  populateEnv,
  StandardTraceContextManager,
  StandardHeartbeatsManager,
  StandardRealtimeStreamsManager,
  StandardInputStreamManager,
  StandardSessionStreamManager,
  ZodSchemaParsedError,
  sendMessageInCatalog,
  require_source_map_support,
  normalizeImportPath
};
//# sourceMappingURL=chunk-PXUY6BXX.mjs.map
